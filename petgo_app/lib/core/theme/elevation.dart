/// 设计 token —— 阴影层级（UX-DR1）。card / nav / modal / fab / overlay 五档。
class AppElevation {
  AppElevation._();

  static const double card = 1;
  static const double nav = 8;
  static const double modal = 16;
  static const double fab = 6;
  static const double overlay = 12;
}
