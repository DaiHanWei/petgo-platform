# 2026-09-02 电商板块 Staging 验收

**在线报告**：https://claude.ai/code/artifact/cde2e45e-1b01-461e-a522-c7228221f26b

## 文件

| 文件 | 用途 |
|---|---|
| `findings.md` | 完整记录（880 行）：每条缺陷的复现步骤、根因、代码位置、修复方案 |
| `i18n-en-baseline-fix.md` | 英文基线 35 个键的逐条对照与建议译文，可直接照做 |
| `shots/` | 88 张截图 |
| `export-shop.xlsx` | 支付记录 Excel 导出样本（用于核对导出内容） |

## 结论

**23 条缺陷**（P0×4 / P1×9 / P2×10）· **4 项改进** · **16 项验证通过**

编号说明：D-1~D-24 中 **D-5 已撤销**（经复核为自动化假象，非缺陷）；
R-1~R-5 中 **R-5 已撤销**（该功能实际已实现）。编号保留以免引用错位。

## 建议修复顺序

1. **D-20**（P0）后台商品编辑页全部 500 —— 阻断运营最高频操作，修法最轻（一行 null 防护 + 一条 UPDATE）
2. **D-8**（P0）图片上传 403 —— 建议把 CSRF meta 挪进 `layout.html` 统一注入
3. **D-10 + D-13**（P0+P1）退货凭证 —— **必须两端一起修**，只修 App 端运营仍看不到图
4. **D-4**（P0）Total due 漏减 PawCoin —— 照已支付态的写法改
5. 其余按优先级

⚠️ **动 D-8 之前**：先把 `shawn/toko-onto-dev` 的 `5e0dc7e8` 合进来，两者是同一片区域，
否则会冲突或重复实现。那条分支上还有 `bcb5176e`（Toko 商品搜索）和 `ed8da66a`（运维文档）未合。

## 一条横切建议

D-8 / D-12 / D-17 分属四种不同技术栈，却都是「操作失败或成功都不给反馈」，
本轮共撞见 **6 处**。建议定一条全局规范：校验失败必须 toast 并定位到出错字段，
写操作成功必须有确认。否则同类问题会继续在新页面里长出来。

## 待 D-20 修复后才能测

- 售罄态与「See alternatives」（需把某在售商品库存改为 0，当前后台无调库存路径）
- 异常订单的手工处置（需造超卖样本，同上）
- 退货 `WRITE_OFF` 处置分支（未测，`RETURN_TO_USER` 已验，见 V-16）

## staging 遗留

- 10 个缺图商品已下架（可逆，按产品决定）
- 一条测试判例：「猫粮外箱拆封、内层铝箔袋封口完整未破」
- 一张 1×1 PNG 测试图：`stag/public/seed-post/5c26e03f-9ca7-4bd5-9dab-43fb3ed1fc10.png`
- 测试地址「Test Penerima」已设为默认，原地址「shawn / Cilandak」需要时切回
- 若干测试订单与退货单
