name: frontend-ci

on:
  push:
    paths:
      - "petgo_app/**"
      - ".github/workflows/frontend-ci.yml"
  pull_request:
    paths:
      - "petgo_app/**"
      - ".github/workflows/frontend-ci.yml"

defaults:
  run:
    working-directory: petgo_app

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # Android apk 构建用 JDK 17（Flutter 3.44 / AGP 推荐线）
      - name: Set up JDK 17 (Temurin)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Flutter 3.44.x (stable)
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.44.1"
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Generate l10n
        run: flutter gen-l10n

      - name: Analyze (zero warnings)
        run: flutter analyze

      # 🔴🔴 FR-110 边界守护（V1.4.0 Story 9.1，安全攸关）。理由同 backend-ci：
      #    见 petgo-backend 侧同名步骤。两端各扫各的，任一端破了都红。
      - name: 🔴 Guard — 问诊与商品的边界（FR-110）
        run: flutter test test/shop/consult_shop_boundary_test.dart

      - name: Test
        run: flutter test

      - name: Build debug APK
        run: flutter build apk --debug
