---
title: "TailTopia 宠物 App V1.4.0 电商板块产品需求文档（精选自营）"
status: draft
created: 2026-08-10
updated: 2026-08-15
validated: 2026-08-14
validation_report: "_bmad-output/planning-artifacts/prds/prd-petgo-platform-2026-08-14/validation-report.md"
decided_2026_08_15: "C-10 扩展 PaymentIntent（原 D-9/OQ-35/L-8）· C-11 FR-108 挪 1.2.0（原 D-10/OQ-36/L-10）· C-12 行级部分退货 + 去程运费（原 OQ-38，落为 FR-104A）· C-13 砍换货（原 OQ-39）· SPEC-15 提前量 7 天。连带消解：D-5/OQ-29 · A-16 疫苗类分母污染"
carried_findings: "⏳ 待拍板 OQ-37 / OQ-40 / OQ-41（安全攸关）/ OQ-42 · 后台 OQ-30~32 无 D 编号 · SPEC-1~SPEC-25（§9B 待补规格）· DEP-1~DEP-9（其中 DEP-6 已升为版本核心单点依赖）· L-9/L-11/L-12（§8A 未闭合项）"
blocking_backend_dev: "无 —— C-10 拍板后已解除。开工前须先落 §8B 的两项并行护栏（Flyway 独占号段 · 共享枚举追加约定），且须三人共同确认"
---

# PRD：TailTopia 宠物 App V1.4.0 · 精选自营电商

## 0. 文档说明

本 PRD 为迭代版本文档，采用轻量结构——不重复 V1.0.0 已写明的产品愿景、用户旅程和术语表，仅记录电商板块范围内的新增/变更内容。产品整体定位见 `V1.0.0/V1-0-0PRD.md`。

**编号延续规则：** 全仓库现有最大 FR 编号为 **FR-92**（`1.项目总述/Roadmap.md` 1.1.4 章节），本文档 FR 从 **FR-93** 起。待解决问题延续现有最大 OQ-24（`V1.1.2/改版1-1-2prd.md`），本文档 OQ 从 **OQ-25** 起。假设索引延续现有最大 A-14，本文档从 **A-15** 起。管理后台编号见 `v1-4-0电商后台prd.md`，从 **AB-10A** 起（现有模块编号至模块 9）。

**版本定位：** 电商板块是「精选自营」模式的第一个版本——平台自行采购备货、自定零售价、自负履约与售后，收入来自进销毛利而非抽佣。本版本范围 = **最小可售闭环 + 复购引擎**。

**⚠️ 本版本覆盖 Roadmap 既有电商设计。** `1.项目总述/Roadmap.md` 中 1.4.0「精选电商 Phase A（导流模式，跳转 Tokopedia/Shopee 完成购买）」与 2.1.0「精选电商 Phase B（App 内下单、商家自行履约）」两条均为零库存零履约模式，与本版本的自营模式互斥。产品已拍板由自营直接覆盖两者，Roadmap 需同步改写，详见 §7 与同目录 `Roadmap-电商改动建议.md`。

---

## 1. 版本目标

1. **跑通实物商品交易闭环**：从商品浏览到下单、支付、履约、签收、售后的完整链路。这是 App 内**第一次出现实物商品**——此前所有订单（AI 问诊解锁 FR-43A、兽医咨询 FR-43D、PawCoin 充值 FR-50A、身份证高清下载 FR-49D）均为虚拟商品，系统中不存在收货地址、物流、实物退货、库存四类能力，本版本需从零建立。
2. **建立复购引擎，而非货架**（§3.9）：把已有的宠物档案（品种/年龄/体重）与结构化健康记录（FR-45A 疫苗/驱虫）转化为购买触发点。这是本版本区别于 Shopee/Tokopedia 的唯一理由，也是版本的核心主线。
3. **守住「精选」边界**：SKU 数量受控、不开放第三方入驻、不做全站搜索货架。产品定位是「你的宠物档案说该买什么」，不是「你自己搜」。

**范围性质说明：** 目标 1 是交付基础，目标 2 是版本存在的意义。若排期需要取舍，应压缩目标 1 的可选项（如物流 API 深度对接，见 FR-104 的分期建议），不应压缩目标 2——只做完目标 1 等于做了一个更差的 Shopee。

---

## 2. 新增术语表

| 术语 | 定义 |
|------|------|
| **Toko** | 电商板块的印尼语名称（意为「商店」），作为底部 Tab 显示文案。与既有 Tab 显示文案的语言策略一致（如 Discovery 显示为 Jelajah） |
| **精选自营** | 平台向本地授权经销商采购、自持库存、自定零售价、自负履约与售后的经营模式。平台收入 = 零售价 − 进货成本（进销毛利），**不是抽佣**。与 Roadmap 原「导流」「商家自行履约」模式互斥 |
| **商品（Produk）** | 用户可见的售卖单位，一个商品可包含多个规格 |
| **规格 / SKU（Varian）** | 商品下的具体可售单元，是库存与价格的实际承载者。如「Royal Canin 幼犬粮」是商品，「1.5kg」「3kg」「7.5kg」是三个 SKU |
| **可售库存** | 实际库存 − 锁定库存。用户在商品详情与购物车看到的售罄/可买判定以此为准（FR-95） |
| **锁定库存** | 已进入待支付订单、尚未支付成功的库存。支付成功转为扣减，支付超时则释放 |
| **复购触发卡** | 一键补货入口，由粮量见底预估驱动（**FR-109**），展示于 Toko 首页区域 ① 并推送一次。⚠️ **2026-08-15（OQ-36）：由结构化健康记录周期驱动、嵌在 Diary 时间线条目中的那一类（FR-108）已挪 1.2.0，本版本不做** |
| **服务范围** | 平台可配送的行政区范围。首发限大雅加达 Jabodetabek，超范围地址不可下单（FR-100） |

---

## 3. 功能与需求

### 3.1 信息架构与入口

**功能描述：** 电商板块需在底部 Tab Bar 中获得一个位置。

> ⚠️ **L-12（原「已预留位置」表述作废）：** 代码事实是当前 Tab Bar **没有空位**——`petgo_app/lib/shared/widgets/bottom_tab_bar.dart` 的 `AppTab` 为 profile / triage / home / me **四值**，加中间 `[+]` 发布按钮。因此实际只能是**「替换其一」或「扩为 5 Tab（挤压 [+]）」**二选一，本 PRD 初稿的「占用已预留位置」是误判前提。这一选择同时决定 §5 基线埋点的对象（要取哪个被替换 Tab 的曝光基线），归 **DEP-1**。

Tab 的最终位序、图标激活态设计、以及被替换模块的迁移方案由 Tab 改版负责人另行设计，**不在本 PRD 范围内**——本文档仅定义 Toko Tab 自身的内容与行为，并在 §8 依赖项中列出需对方确认的接口。

#### FR-93：Toko Tab 与首页结构

Toko 首页是精选流，**不是搜索驱动的货架**。页面自上而下固定为四个区域：

| 区域 | 内容 | 展示条件 |
|---|---|---|
| ① 补货提醒区 | 复购触发卡（驱虫/疫苗到期、粮量见底），最多 2 张 | 有触发时才出现，无触发则整区不渲染 |
| ② 为我的宠物精选 | 「Pilihan buat {宠物名}」，按档案推荐（FR-107），4–6 个商品 | 状态 A 且已建档 |
| ③ 分类入口 | 4 个固定品类：Makanan（粮）/ Obat & Vitamin（驱虫保健）/ Camilan（零食）/ Perawatan（洗护） | 常驻 |
| ④ 全部精选 | 全部在售商品，按运营排序 | 常驻 |

**后果：**
- **不提供全站搜索框。** 商品总数受 SKU 上限约束（见 OQ-25），四个分类 + 精选流已足够覆盖；搜索框会把产品心智推向通用货架，与战略边界冲突
- 区域 ①② 依赖宠物档案。用户状态与展示的对应关系：

| 用户状态 | ① 补货提醒 | ② 为我的宠物精选 | ③④ 分类与全部 |
|---|---|---|---|
| 游客（未登录） | 不展示 | 不展示 | **展示**（可浏览、可进详情，加购时触发 V1.0.0 FR-0B 软性登录引导） |
| 已登录 · 状态 A · 未建档 | 不展示 | 替换为建档引导卡（复用 FR-0G 引导文案，不新建） | 展示 |
| 已登录 · 状态 A · 已建档 | 按触发展示 | 展示 | 展示 |
| 已登录 · 状态 B/C（无宠物） | 不展示 | 不展示 | 展示 |
- **本版本按单宠物实现**：区域 ①② 直接使用该用户的唯一宠物，不做宠物选择器。多宠物场景待 Roadmap **1.3.0 多宠物账号管理**落地后，按其全局当前宠物概念接入（**1.3.0 后启用**，epics 阶段须标注）
  > **L-11：** 初稿写「复用多宠物管理**已有的**全局当前宠物概念」，该概念**不存在**——`petgo-backend` 的 `profile/service/ProfileService.java:56` 以 `existsByOwnerId` → 409 硬约束单账号单宠物（注释明写「单账号单宠物，FR-11」）。初稿引用的编号 `FR-1.2.0` 也不符合本仓 `FR-<数字>` 格式，无法解析，已改为指向 Roadmap 条目。不阻塞（单宠物下自然退化为唯一宠物）

#### FR-93A：Toko 与既有 Tab 的关系

**后果：**
- Toko 是**非落地 Tab**。V1.1.2 FR-78 确立的落地 Tab 判定规则（只有真正建了宠物档案的用户落 Diary，其余已登录用户落 Discovery，游客落 Diary）不因本版本改变，Toko 不参与落地判定
- 游客点击 Toko **不触发登录引导**——Toko 与 Discovery 一样允许游客浏览。这与 V1.1.2 FR-78 中「未登录点击非落地 Tab 触发登录引导」的既有机制存在差异，是有意为之：商品浏览是转化漏斗的最上层，用登录墙拦截会直接杀掉转化。登录引导推迟到**加入购物车**这一步触发
- Tab 切换需上报埋点（V1.1.2 起底部 Tab 切换已改为手动上报，见 §5）

---

### 3.2 商品与库存

#### FR-94：商品模型与展示

**商品字段：**

| 字段 | 必填 | 说明 |
|---|---|---|
| 商品名称 | 必填 | 印尼语，≤60 字符 |
| 品牌 | 必填 | 用于详情页信任展示 |
| 品类 | 必填 | 四选一：Makanan / Obat & Vitamin / Camilan / Perawatan |
| 主图 + 图集 | 必填 | 主图 1 张 + 图集 ≤8 张 |
| 规格列表（SKU） | 必填，≥1 | 见 FR-94A |
| 适用物种 | 必填 | 犬 / 猫 / 通用。用于 FR-107 档案推荐过滤 |
| 适用体型 | 选填 | 小型 / 中型 / 大型 / 通用 |
| 适用年龄段 | 选填 | 幼年 / 成年 / 老年 / 通用 |
| 商品详情 | 必填 | 富文本（图文），含成分表 |
| 每日建议喂量 | 仅 Makanan 品类必填 | 结构化：`{体重区间, 克/日}` 数组。**FR-109 粮量见底预估的计算依据，不是展示文案** |
| 保质期说明 | 必填 | 展示文案 |
| 退货规则标识 | 必填 | 枚举：`可退` / `开封不退` / `不可退`。见 FR-104。⚠️ **2026-08-15 拍板（OQ-39）：原三值中的「换」已砍**——`可退可换`→`可退`、`不可退换`→`不可退`。理由见 §7「换货」 |

#### FR-94A：规格（SKU）与价格

**后果：**
- 规格是价格与库存的实际承载者。同一商品的不同规格可有不同价格、不同库存、不同退货规则标识
- 商品详情页规格选择器：未选规格时**加购按钮为禁用态**，不默认选中第一个规格（避免用户误购规格）
- 单一规格的商品不展示选择器，直接可加购
- 价格单位 IDR，展示格式 `Rp 185.000`（印尼千分位用点号），不展示小数

#### FR-95：库存与售罄

**库存口径：** `可售库存 = 实际库存 − 锁定库存`。用户侧一切售罄判定以可售库存为准。

**库存流转：**

| 动作 | 库存变化 |
|---|---|
| 提交订单（进入待支付） | 可售 → 锁定 |
| 支付成功 | 锁定 → 扣减（出库） |
| 支付超时 / 用户取消 | 锁定 → 释放回可售 |
| 退货入库并质检通过 | 增加可售（后台操作，见后台 PRD AB-10C） |

**后果：**
- **库存扣减必须由服务端原子操作完成**，不得依赖客户端读取后写入。并发下单时以服务端判定为准，失败方返回明确的「已售罄」错误而非静默失败
- 可售库存 = 0：商品卡与详情页展示「Stok habis」（售罄），加购按钮禁用；商品**不自动下架**，仍可浏览（保留搜索引擎与复购提醒的落点）
- 可售库存 ≤ 运营配置阈值：详情页展示「Sisa {n}」低库存提示，制造合理紧迫感但不虚构数字
- **购物车与结算是两次独立校验**：加购时校验一次，提交订单时再校验一次。第二次失败时，结算页明确标出是哪个 SKU 不可用，并允许用户移除后继续下单，不整单打回

---

### 3.3 购物车与结算

#### FR-96：购物车

**后果：**
- **单店模型，无店铺分组**。平台自营是唯一卖家，购物车是一个平铺列表——这是相对第三方电商的结构性简化，不要照搬 Shopee 的多店铺分组
- 行内可调数量（+/−）、可删除；数量上限受可售库存约束
- **失效商品单独分区**置于列表底部，包含：已下架、已售罄。失效商品不参与合计、不可勾选，提供「清空失效商品」入口
- 购物车角标显示商品件数（非种类数）
- 游客无购物车。游客点击加购时触发登录引导（复用 V1.0.0 FR-0B 软性登录），登录成功后**自动完成本次加购动作并停留原页**，不丢失意图

#### FR-97：结算页

**结算页自上而下：** 收货地址 → 商品清单 → 配送方式 → 支付方式 → 金额明细 → 提交按钮

**金额明细构成：**

| 项 | 说明 |
|---|---|
| 商品小计 | 各 SKU 单价 × 数量之和 |
| 运费 | 按配送方式与地址计算，见 FR-99 |
| 免运费抵扣 | 商品小计 ≥ 运营配置门槛时减免运费，展示为负数行 |
| **应付合计** | 上述之和 |

**后果：**
- 首版**不含优惠券/促销码/会员折扣**。会员制套餐在 Roadmap 中整体暂缓（2026-07-15），本版本不预留会员价字段，避免为暂缓功能提前埋成本
- 无收货地址时，地址区展示为「+ 新增收货地址」的引导态，提交按钮禁用
- 地址超出服务范围时，地址区展示范围外警示，提交按钮禁用并说明原因（见 FR-100）
- 提交订单 = 创建订单 + 锁定库存 + 进入待支付，此时**尚未扣款**

---

### 3.4 收货地址

#### FR-98：收货地址体系

App 内首次引入收货地址。**印尼行政区划为四级**，本版本采集到第三级 + 详细地址 + 邮编。

**字段：**

| 字段 | 必填 | 说明 |
|---|---|---|
| 收件人姓名 | 必填 | ≤40 字符 |
| 手机号 | 必填 | 印尼格式，+62 前缀，8–13 位 |
| Provinsi（省） | 必填 | 级联选择 |
| Kota / Kabupaten（市 / 县） | 必填 | 级联选择 |
| Kecamatan（区） | 必填 | 级联选择 |
| 详细地址 | 必填 | 街道、门牌、楼层、单元，≤120 字符 |
| Kode Pos（邮编） | 必填 | 5 位数字 |
| 地址标签 | 选填 | Rumah（家）/ Kantor（公司）/ 自定义 ≤10 字符 |
| 设为默认 | — | 开关，仅一个默认地址 |

**后果：**
- 地址簿上限 20 条
- 首个创建的地址自动设为默认
- 删除默认地址后，剩余地址中最近使用的一条自动升为默认；地址簿清空则无默认
- **手机号是履约联系方式，不与账号手机号（1.2.0 手机号采集入口）互通**——两者用途不同，不做自动填充也不做同步

#### FR-99：服务范围与运费

**后果：**
- 服务范围按 Kecamatan 粒度配置（后台 AB-11C）。首发限 Jabodetabek 范围内的 Kecamatan
- 地址保存**不校验服务范围**（允许用户存下未来可用的地址），**结算时校验**并阻断下单，提示文案需说明「暂不配送至该区域」而非笼统报错
- 配送方式两档：

| 配送方式 | 承运 | 时效 | 适用范围 |
|---|---|---|---|
| 当日达（Sameday） | GoSend / Grab Express | 当日 | 服务范围内的同城 Kecamatan |
| 标准快递（Reguler） | JNE / SiCepat / Anteraja | 2–4 日 | 服务范围全部 |

- 运费由后台按「配送方式 × 区域」配置固定值（AB-11C），**首版不接承运商实时计费 API**——首发仅 Jabodetabek，区域差异小，固定运费表足够，可省去一整条 API 对接工期
- 超重/超体积附加费：首版不做。SKU 上限受控且品类可预期（最大件为 7.5kg 级狗粮），差异由固定运费表吸收

---

### 3.5 支付

#### FR-100：电商订单支付

**支付通道复用 `V1.1.0/v1-1-0PRD.md` FR-43 通用支付组件**，但**混合支付需扩展支付模型**——**2026-08-15 已拍板：扩展既有 `PaymentIntent`**（**OQ-35 / D-9 / L-8**，方案 a）。

> ✅ **L-8 已闭合（2026-08-15 产品拍板，初稿「不新建支付通道」表述作废）**
>
> **代码事实：** `petgo-backend` 的 `pay/domain/PaymentIntent.java` 为**严格单渠道单金额**模型——`channel` 为单值枚举、`amount` 为单个数值，且**两者均 `updatable = false`**；`PayChannel` 仅 `{QRIS, PAWCOIN}` 二选一，**无混合表达能力**，表达不了 FR-100A 规则 2 要求的「一笔订单 PawCoin 段 + QRIS 段并存，且比例在订单创建时固化」。
>
> **拍板方案（a）—— 扩展共享 `PaymentIntent`，不新建拆分表：**
>
> ```
> payment_intents （既有表，本版本扩展）
> ├ channel       MIXED        ← PayChannel 枚举【末尾追加】一个值
> ├ amount        300_000        总额，updatable=false 保持不变
> ├ coin_amount   100_000      ← 新增列，nullable
> ├ cash_amount   200_000      ← 新增列，nullable
> └ coin_ratio    0.333333     ← 新增列，nullable，下单时固化（FR-100A 规则 2）
> ```
>
> **向后兼容契约（实现时必须守住）：**
> - 既有 4 个 `PaymentPurpose`（AI 问诊解锁 / 兽医咨询 / PawCoin 充值 / 高清图下载）**三个新列全部为 `NULL`**，`channel` 仍为 `QRIS` 或 `PAWCOIN`，**行为逐字节不变**
> - `PayChannel` 新增 `MIXED` **只在枚举末尾追加**，不重排、不删除、不改既有值拼写（落库为 varchar，重排不报错但会静默改语义——见 §8B 第 3 条）
> - `amount` 的 `updatable=false` 不动；三个新列同样应为写入后不可变（下单时一次性固化）
> - **退款拆分在同一笔 intent 内按 `coin_ratio` 计算 → 天然原子**，无需跨两笔 intent 做分布式一致性
>
> **⚠️ 这条改动触碰三人共享基座**：Flyway 迁移会改共享表 `payment_intents`，必须占**独占号段**并**事先告知另两位同事**（见 §8B 第 2 条）。
>
> **影响面：** FR-100A 全部 7 条规则 · FR-105 退款到账方式 · 后台 AB-12C 退款执行拆分 · AB-13D 对账「PawCoin 与 QRIS 两段须拆分」与「被 PawCoin 抵扣的运费须单独可拆」。**这些 story 现已解除阻塞，可开写。**

**支付方式两种：QRIS（真钱）与 PawCoin（余额），可混合使用。**

**⚠️ 真钱渠道口径以 Roadmap 更正为准：仅 QRIS。** `V1.1.0/v1-1-0PRD.md` FR-43 原文写「QRIS、DANA」，但 `1.项目总述/Roadmap.md` 1.1.0 条目于 **2026-07-15 更正为「目前仅支持 QRIS，DANA 尚未接入」**，后者更新。本版本按 QRIS 单渠道设计，不做真钱渠道选择器。**不支持 GoPay/OVO 直连、不支持银行转账/VA。**

**后果：**
- 支付失败/取消处理复用 FR-43F 通用规则，不另定义
- **待支付超时 = 60 分钟**（超时后自动取消订单、释放锁定库存）。此值明显长于兽医咨询的限时支付窗口，因为实物购买的决策与支付节奏不同，沿用短窗口会造成大量误失效订单
- 超时前 10 分钟推送一次提醒（复用 FR-38 深链接直跳订单详情）
- **PawCoin 可用于购买实物商品**（2026-08-10 产品拍板，覆盖原「首版不可」的建议），使用规则见 FR-100A

#### FR-100A：PawCoin 购买实物商品的使用规则

PawCoin 原本只用于虚拟商品（AI 问诊解锁 FR-43A、兽医咨询 FR-43D、身份证高清下载 FR-49D），这些场景**没有退货、没有实物、没有运费**。扩展到实物商品后，必须补齐以下规则，否则会击穿 FR-50D 的封闭币前提。

##### 规则 1 · 防套现（最承重，不可妥协）

> **PawCoin 支付的部分，退款只能退回 PawCoin，不提供「退回真钱」选项。**

`V1.1.0` FR-50D 明确 PawCoin **不支持一般性提现**。但实物商品**可以退货**——若允许 PawCoin 买的商品退货时退真钱，则形成 `充值 PawCoin → 购买实物 → 申请退货 → 退回真钱` 的路径，**等于变相提现，直接击穿 FR-50D**。

**后果：**
- 退款方式选择页对 PawCoin 支付的部分**不展示「退回真钱」入口**，而不是展示后再拒绝——不给用户产生预期再打破
- 该规则**不因退货原因而变**：即使是平台责任（商品破损/错发），PawCoin 部分仍只退 PawCoin。用户的真钱从未流出过这部分，退真钱不是"补偿"而是"兑现"
- 但平台责任下用户确实承受了"没有选择权"的代价，**补偿走 Coin 侧溢价而非开退现金的口子**——见规则 7
- 混合支付订单的 QRIS 部分**不受此限**，仍按既有 FR-43D 由用户选择退真钱或转 PawCoin

##### 规则 2 · 混合支付与按比例退款

**支持混合支付**：PawCoin 余额不足以覆盖订单时，**余额优先全额抵扣，差额由 QRIS 补足**，不阻断下单。

**为什么支持混合**：PawCoin 余额主要来自两个来源——4 个固定充值档位（FR-50A）与退款转入（FR-43D）。后者多为零头金额。若要求整单 PawCoin，绝大多数余额将无法用于实物商品，这个能力等于白开。

**退款按原支付比例拆分返还：**

```
订单实付：PawCoin 60.000 + QRIS 240.000 = 300.000（PawCoin 占比 20%）
退款 150.000 时：
  → 退 PawCoin 30.000（20%，只能退 PawCoin，规则 1）
  → 退 QRIS 部分 120.000（80%，用户可选退真钱或转 PawCoin，FR-43D）
```

**后果：**
- 比例按**订单实付金额**计算并在订单创建时固化，**不随后续部分退款重算**——否则多次部分退货会让比例漂移
- 该规则天然保证 **用户拿回的真钱永远不超过其投入的真钱**，是规则 1 在混合场景下的数学保证，可用于向法务/财务说明闭环性
- 结算页需**同时展示两段金额**（PawCoin 抵扣多少、还需 QRIS 支付多少），不能只显示一个总数

##### 规则 3 · 运费可用 PawCoin 支付

运费计入订单总额，一并参与 PawCoin 抵扣与比例拆分。

**后果：**
- 运费是平台向承运商支付的**真实现金支出**，用 PawCoin 覆盖等于用预收款抵现金成本。财务侧需知悉：后台对账（AB-13D）须能把「PawCoin 抵扣的运费」单独拆出，否则现金流量表会失真
- 退货运费由用户承担时（FR-104 非质量问题），**从退款金额中扣除后再退回**，不单独向用户发起收款

##### 规则 4 · 单笔上限

- 单笔订单的 PawCoin 支付金额设上限，**建议 Rp 1.000.000**，由后台配置（AB-6D）

**⚠️ 这个上限不是浮存工具，别当浮存工具用。**（2026-08-10 更正，原文误将其论证为「浮存的前置阀门」）

浮存的方向是：**充值 → 浮存增加；消费 → 浮存减少**——用户花掉 Coin 等于平台交付了价值、债务被核销。**因此限制单笔消费额度会让浮存降得更慢，与控制浮存的目标相反。**

**上限真正的用途只有两条：**

| # | 用途 | 说明 |
|---|---|---|
| 1 | **故障 / 欺诈的单次爆炸半径** | Coin 扣减链路若出现 bug 或被利用，上限把单次事故的损失锁死在一个可承受的量级 |
| 2 | **DEP-7 的监管姿态** | 闭环储值工具若单笔交易额不设任何限制，在 uang elektronik 的口径下更容易被认定为需要牌照的电子货币。自设上限是防御性姿态。⚠️ 但监管通常更关注**余额上限**而非单笔消费上限——具体以 DEP-7 的法务结论为准，必要时改为余额上限 |

**为什么 Rp 1.000.000 仍是合适的数**：客单约 Rp 285.000，即使多件订单也很少触及 100 万，**该上限在正常经营中几乎不会触发**——正因如此它才是廉价保险：拿到了上述两条防护，又基本不妨碍存量余额的正常消耗。**上限定得太低反而有害**，会把用户的余额锁在账上出不去。

##### 规则 5 · 与浮存门槛（AB-6C）联动

> **AB-6C 触发充值暂停时，已有余额仍可正常用于购物。**

**后果：**
- 暂停的是**充值入口**，不是**消费出口**。已持有余额的用户若因此买不了东西，等于对已付款用户违约
- 充值暂停期间，结算页 PawCoin 区正常可用，仅「余额不足 → 去充值」的引导按钮置为不可用并说明原因

##### 规则 6 · 不参与的场景

- PawCoin **不可用于**支付 PPN 增值税之外的任何平台外部结算（本版本无此场景，此条为防御性声明）
- PawCoin 余额**不可转让**给其他用户——维持封闭闭环，见 DEP-7 合规依赖

##### 规则 7 · 平台责任时的 Coin 溢价补偿

> **退货类型 = 质量问题时，PawCoin 退款部分按配置比例额外补偿 Coin。**

**这条规则解决的问题：** 规则 1 规定 PawCoin 段只退 Coin 且不因退货原因而变。但当退货是**平台的错**（商品破损 / 临期 / 错发）时，用户不仅东西有问题，还失去了拿回现金的选择权——这是本设计中最容易引发客诉、且 CS 手上没有变通手段的一处（后台按 AB-12C 不提供改判入口）。

**产品决定（2026-08-10）：补偿走 Coin 侧溢价，不开退现金的口子。** 既安抚了用户，又不破坏闭环——退回的现金依然永远不超过投入的现金。

**触发条件：**

| 退货类型（AB-12A 已有枚举） | 是否给溢价 |
|---|---|
| **质量问题**（破损 / 临期 / 错发） | ✅ **给** |
| 非质量问题（不想要了 / 买错规格） | ❌ 不给 |
| 拒收（未签收） | ❌ 不给 |
| 待发货前取消 | ❌ 不给 |

**后果：**
- **复用 AB-12A 已有的「退货类型」判定结果，不新增 CS 判定项**——CS 本来就要选退货类型，溢价由系统据此自动计算，不增加人工环节
- **溢价只作用于 PawCoin 段**。混合支付订单的 QRIS 段本来就可以选退现金（全额挽回），无需补偿
- **不给溢价的情形必须是默认**：用户自身原因退货若也给溢价，会变成"退货就有奖励"，诱发无意义退货
- 溢价比例与上限由后台配置，**必须与既有 AB-6A「用户主动选择转 PawCoin」的激励溢价分开配置**——两者触发条件与目的完全不同（一个是激励用户不走人工退现金，一个是补偿用户没有选择权），共用一个比例会导致调其一误伤其二。见 AB-6A（扩展）
- **溢价发放会增加 PawCoin 浮存**，须计入 AB-6C 监控；会计上属平台售后成本，须在 AB-13A 毛利看板与 AB-13D 对账中可拆出
- 刷单风险可控：质量问题需上传凭证图（FR-104）+ CS 判定 + AB-12D 判例库沉淀；且为拿一笔小额 Coin 溢价而走完整退货流程，成本远高于收益
- 比例建议值与上限见 **OQ-34**

---

### 3.6 订单与履约

#### FR-101：电商订单卡片（FR-54 第 5 类）

**复用 `V1.1.0/v1-1-0PRD.md` FR-54 订单页主体结构**，不新建订单入口。既有 4 类卡片为 FR-54A 兽医咨询 / FR-54B PawCoin 充值 / FR-54C AI 问诊解锁 / FR-54D 高清图购买，本版本新增第 5 类。

**电商订单卡片内容：** 订单状态标签 · 商品主图（多商品时展示首图 + 「等 N 件」）· 商品名与规格 · 数量 · 实付金额 · 主操作按钮（随状态变化）

**后果：**
- 订单列表筛选 chips 新增「Belanja」（购物）类型，与既有类型并列
- 电商订单与虚拟商品订单在同一列表按时间倒序混排，不分栏

#### FR-102：订单状态机

| 状态 | 进入条件 | 用户可执行操作 |
|---|---|---|
| `待支付` | 提交订单 | 去支付 / 取消订单 |
| `待发货` | 支付成功 | 申请取消（走退款审核） |
| `已发货` | 后台填入物流单号 | 查看物流 |
| `已送达` | 承运商标记送达 或 后台标记 | 确认收货 / 申请退货 |
| `已完成` | 用户确认收货 或 送达后 7 日自动确认 | 评价 / 申请退货（签收后 7 日内） |
| `已取消` | 待支付超时 / 用户取消 / 后台取消 | 再次购买 |
| `退款中` | 退货申请提交且 CS 受理 | 查看进度 |
| `已退款` | 退款完成 | — |

**后果：**
- **终态为 `已完成` / `已取消` / `已退款` 三个，无悬空态**
- `已送达` → `已完成` 的自动确认为 7 日。自动确认后仍保留 7 日退货窗口（自签收起算），两者不冲突
- 订单号规则：**对外展示的订单号必须是不可枚举 token**（与既有虚拟商品订单号规则隔离，便于对账区分自营实物与虚拟商品收入）。对账所需的连续序号走**内部序列，不外露**
  > ⚠️ **初稿的 `TT` + `YYYYMMDD` + 8 位自增作废——违反项目强制护栏。** `CLAUDE.md` §强制护栏写明「对外暴露标识一律**不可枚举 token**，不用自增 id 直接外露」（违反即返工）。日期 + 自增序列是教科书式可枚举标识：任何用户从自己的订单号即可推断平台当日单量与累计单量——**对一个还在验证 A-15「宠主是否愿意在 TailTopia 买粮」的产品，这是直接泄露最敏感的经营数据**，且可枚举遍历探测他人订单。对账区分的诉求用「不可枚举 token + 独立内部序列」同样满足，不需要把序号外露

#### FR-103：物流追踪

**后果：**
- **首版为轻量实现**：后台录入承运商 + 物流单号 → App 展示单号、承运商名、复制按钮、跳转承运商官网查询的链接。**不接承运商 API、不在 App 内渲染物流轨迹**
- 分期理由：首发仅 Jabodetabek + 当日达为主，多承运商 API 对接（GoSend/Grab/JNE/SiCepat/Anteraja 五家）工期显著，而其带来的体验增量在同城当日达场景下有限。轨迹渲染建议在跨城单量起来后单独排期
- 发货时推送通知（复用 FR-38 深链接直跳订单详情）

---

### 3.7 退货与退款

> **2026-08-15 拍板（OQ-39）：本节原名「退换货」，「换货」已砍出本版本。** 两份 PRD 中换货流程为**零实现**——无换货状态、无质检后补发、无补发发货、无换货出入库配对。运营一旦把商品标成「可退可换」，用户端会据此展示可换承诺，**而平台没有任何执行能力**。已改为二义枚举（`可退` / `开封不退` / `不可退`），「换货」写进 §7。

#### FR-104：退货规则

这是与既有虚拟商品退款的**本质差异**——虚拟商品退款是纯资金操作，实物退货需要退回实物、质检、并确定运费归属。

**可退情形与运费归属：**

| 情形 | 时限 | 是否需退回实物 | 退货运费 |
|---|---|---|---|
| 待发货前取消 | 发货前 | 否 | 无 |
| 拒收（未签收） | 配送中 | 否（承运商原路退回） | 平台承担 |
| 质量问题（破损/临期/错发） | 签收后 7 日内 | 是 | **平台承担** |
| 非质量问题（不想要了/买错规格） | 签收后 7 日内 | 是，且需未拆封 | **用户承担** |

**不可退情形：**
- **退货规则标识为「开封不退」的商品一经开封不可退**（FR-94 字段）。宠物食品与保健品出于食品与用药安全，开封后无法二次销售，此规则须在**商品详情页、结算页、退货申请页三处**明示，不能只写在规则页里
- 标识为「不可退」的商品不提供退货入口

**退货粒度与去程运费（FR-104A，2026-08-15 拍板，OQ-38）：**

| 维度 | 规则 |
|---|---|
| **退货粒度** | **支持行级部分退货**——用户可只退订单中的某个/某几个 SKU 行，不必整单退 |
| **并发约束** | **同一订单同时仅允许一个进行中的退货申请。** 已有申请处于 `待CS审核` / `待用户寄回` / `质检中` / `退款执行中` 任一状态时，该订单的退货入口置灰并提示「已有退货申请处理中」 |
| **去程运费** | **整单退 → 去程运费全额退回；部分退 → 去程运费不退。** 须在**退货申请页**明示，不能只写在规则页 |
| **订单主状态** | 部分退货**不改变订单主状态**。退款单独立于订单状态机（§4.2），**仅当订单全部行的退款均完成时，订单才转 `已退款`**；否则订单停留在原状态（`已完成` 等） |

> **为什么去程运费部分退不退：** 免运费门槛（FR-99）叠加部分退货会构成**可复现的凑单-退货套利**——用户凑单到免运门槛下单，收货后退掉凑单商品，若去程运费仍全退，则等于用退货行为白嫖了运费减免。故部分退不退去程运费。整单退时订单整体撤销，不存在该套利，全额退回。

**后果：**
- 退货申请需上传凭证图 ≤6 张，质量问题必填
- 申请提交后进入 CS 审核，**复用 `V1.1.0/v1-1-0后台prd.md` AB-5E 退款申请流程与 AB-5B 工单详情处理**，不另建审核通道
- CS 驳回时须填写理由并回告用户，复用既有工单回告机制（FR-52A）

#### FR-105：退款到账方式

**退款先按 FR-100A 规则 2 拆成两段，再分别处理。**

| 支付来源 | 退款去向 | 用户可选 | 成本 / 补偿 |
|---|---|---|---|
| **PawCoin 部分** | **只能退回 PawCoin** | ❌ 无选项 | 无手续费；**退货类型 = 质量问题时额外补偿 Coin 溢价**（规则 7） |
| **QRIS 部分** | 退回真钱 **或** 转 PawCoin | ✅ 用户选择（复用 FR-43D） | 退真钱：BCA 免手续费，OVO/GoPay 自付 Rp2,500<br>转 PawCoin：即时到账 + 激励溢价（AB-6A 既有配置） |

> ⚠️ 表中出现两种 Coin 溢价，**触发条件与目的不同，配置项必须分开**：QRIS 段的是**激励**（换取用户不走人工退现金流程）；PawCoin 段的是**补偿**（用户没有选择权）。

**后果：**
- **PawCoin 部分无选择权，是 FR-100A 规则 1 的直接体现**，不是遗漏。退款方式页对这一段只展示结果、不展示选项，避免先给预期再打破
- 纯 QRIS 支付的订单行为与既有虚拟商品退款完全一致，无新增
- 部分退款按 `实退金额 × 订单固化的支付比例`（`payment_intents.coin_ratio`，D-9 方案 a）执行；**回程**运费归属按 FR-104 的表判定，**去程**运费是否退回按 **FR-104A**（整单退全退 / 部分退不退）
- 退货运费由用户承担时，从退款金额中扣除后再拆分返还（FR-100A 规则 3）
- 转入的 PawCoin 可用于 AI 问诊、兽医咨询、身份证下载等既有场景，以及本版本新增的实物商品（FR-50C 消费范围需相应扩写，见 §6）

---

### 3.8 评价

#### FR-106：商品评价

**后果：**
- 订单进入 `已完成` 后可评价，每个订单每个 SKU 各评一次
- 内容：1–5 星（必填）+ 文字 ≤500 字（选填）+ 图 ≤6 张（选填）
- **复用既有内容审核机制**：评价提交时走三方同步过滤，命中高风险即拦截不发布（对应 AB-3C 激活的处理方式），不走「先发布后审核」
- 商品详情页展示评价列表，默认按时间倒序；无评价时展示空状态，**不伪造或预填评价**
- 首版不做追评、不做商家回复（自营模式下"商家回复"即平台回复，价值低于其运营成本）

---

### 3.9 复购引擎（版本核心）

**功能描述：** 本节是电商板块存在的意义。狗粮与驱虫药是宠主唯一天然月月复购的品类，而 TailTopia 已经掌握了触发复购所需的全部数据——宠物档案（品种/年龄/体重）与结构化健康记录（FR-45A 疫苗/驱虫/绝育）。三条机制把这些既有数据转化为购买触发点，**复用现有推送通道，不新建提醒引擎**。

> **⚠️ 2026-08-15 范围收敛：本版本实际交付两条机制。** FR-107（档案推荐）与 FR-109（粮量见底预估）在本版本内实现；**FR-108（健康记录触发）已拍板挪 Roadmap 1.2.0**（OQ-36 / D-10 方案 a，理由与代价见下方「前置数据资产」与 §7）。上段「三条机制」为原始设计意图，**排期与拆 story 时按两条计**。

##### ⚠️ 前置数据资产（初稿把三项输入全部声明为「已有」，其中两项不成立）

本节三条机制依赖三个数据输入。落库时逐条核对源码后，实际状态如下——**排期时必须先看这张表，否则 FR-109 会 100% 不触发地上线**：

| 输入 | 当前状态 | 补齐责任 | 影响 |
|---|---|---|---|
| 档案**体重**（+ 绝育状态） | ❌ **不存在**。`profile/domain/PetProfile.java` 字段全集为 `ownerId / avatarUrl / petType / name / systemDefaultName / breed / birthday / intro / cardToken / ogImageUrl / serialId` + 时间戳 | **本版本 story**：`pet_profiles` 加体重列 + 建档/编辑表单加字段 + 存量用户回填引导（FR-107 的「补全档案，推荐更准」引导卡正是入口，但字段须先有）。绝育状态若 FR-107 需按其过滤则一并加 | **L-9** · FR-109 恒不触发；FR-107 退化为按物种硬过滤 |
| 健康记录**到期间隔** | ❌ **不存在**。`profile/domain/HealthRecord.java` 仅有 `eventDate`，无 `next_due_date`、无 `interval`；FR-108 引用的「1.2.0 健康提醒的间隔配置」排在 Roadmap 1.2.0，当前代码中没有 | ✅ **2026-08-15 拍板（OQ-36 / D-10 方案 a）：本版本不补，FR-108 整体挪到 1.2.0** | **L-10 已闭合** · 代价见下方 ⚠️ |
| 商品**每日建议喂量** | ⏳ 字段已在 FR-94 定义，但**数据未到位**，依赖 **DEP-6**（首批 SKU 清单与每日建议喂量数据，责任方 Rendy） | 商品录入前置 | **DEP-6 的阻塞面已升为「版本核心论证」**——见下方 ⚠️ |

> **为什么这张表是 critical：** §1 与 `decision-log.md` N-1 都把复购引擎认定为「版本存在的意义」与财务月交易率缺口的答案。按当前代码上线，三条机制里 FR-109 归零、FR-108 无判定源、FR-107 退化为「给狗的商品」——正是 §1 自己警告的「只做完目标 1 等于做了一个更差的 Shopee」。
>
> `HealthRecordType` 已含 `VACCINE` / `DEWORM`，FR-108「仅驱虫疫苗参与触发」的**类型假设成立** ✅；不成立的只是周期/到期字段。

> ### ⚠️ 2026-08-15 拍板后的版本核心风险（排期时必读）
>
> D-10 选 (a) 后，**复购引擎本版本只剩两条机制**：
>
> | 机制 | 本版本状态 | 前置 |
> |---|---|---|
> | **FR-107 档案推荐** | ✅ 做 | L-9 体重字段（本版本 story 内补，**可控**） |
> | **FR-108 健康记录触发** | ❌ **不做，挪 1.2.0** | 等 1.2.0 健康提醒落地 |
> | **FR-109 粮量见底预估** | ⚠️ **做，但可用性卡在他方** | L-9 体重字段（可控）+ **DEP-6 每日建议喂量数据（Rendy，不可控）** |
>
> **由此，DEP-6 的阻塞级别实质升为「阻塞版本核心论证」**，不再只是「阻塞商品录入」：
>
> - FR-108 已被移出本版本 → 复购引擎的**冗余度归零**
> - 若 DEP-6 到上线仍未到位 → FR-109 按其自身规则「缺输入不触发、不做兜底猜测」将**恒不触发**
> - 此时复购引擎**实际只剩 FR-107 的规则式过滤**（按物种/年龄/体型筛商品），而这正是 §1 警告的「只做完目标 1 等于做了一个更差的 Shopee」
>
> **处置：** DEP-6 须在**商品录入排期前**拿到，而不是「录商品时顺便补」。若确认拿不到，应回头重议 D-10（改选方案 b，用驱虫间隔默认值把 FR-108 救回来），而不是让复购引擎带着单点依赖上线。

#### FR-107：档案推荐

**数据源：** 宠物档案（物种、品种、年龄）— V1.0.0 FR-11 已有；**体重与绝育状态 — 本版本新增字段**（见上方「前置数据资产」L-9）

**推荐逻辑（规则式，非算法）：**
1. 按 `适用物种` 硬过滤（FR-94 字段）
2. 按 `适用年龄段` 匹配档案年龄（幼年 <1 岁 / 成年 1–7 岁 / 老年 >7 岁，犬猫阈值可后台配置）
3. 按 `适用体型` 匹配档案体重
4. 结果按运营权重排序，取前 4–6 个

**后果：**
- 展示位：Toko 首页区域 ②（FR-93）与 Diary 档案页推荐区
- 卡片文案带出推荐理由，如「Untuk anjing dewasa 10–25 kg」（适合 10–25kg 成年犬），**让推荐可解释**——不可解释的推荐在信任驱动的产品里是负资产
- 档案数据不完整（缺体重/年龄）时降级为按物种推荐，并在推荐区尾部附一张「补全档案，推荐更准」的引导卡，跳转档案编辑
- **首版为规则式，不引入个性化算法**。SKU 数量受控（见 OQ-25），规则式已足够；算法推荐待 SKU 规模扩大后再议

#### ~~FR-108：健康记录触发复购~~ → ❌ **本版本不做，挪 Roadmap 1.2.0**

> ### 🚫 2026-08-15 产品拍板（OQ-36 / D-10 方案 a）：FR-108 移出本版本范围
>
> **理由：** 触发所需的「到期间隔」在当前代码中不存在（**L-10**：`HealthRecord.java` 仅有 `eventDate`，无 `next_due_date`、无 `interval`），而该间隔的权威来源是排在 Roadmap **1.2.0 的健康提醒**功能。本版本不自建一套间隔默认值。
>
> **对下游的硬约束：**
> - **不拆 story、不建表、不加列。** `health_records` 本版本**不做任何 schema 改动**
> - **UX 不出 FR-108 相关屏**（Diary 时间线内嵌 CTA、健康提醒推送内附商品直达，两处都不做）
> - **埋点不埋 FR-108 相关事件**；§5 中 `repurchase_card_*` 系列本版本**只由 FR-109 触发**
> - 已写进 **§7 非本版本范围**
>
> **连带自动解决的两条既有缺陷：**
> - **OQ-29 / D-5**（与 1.2.0 健康提醒推送是否合并）——本版本无健康记录触发推送，**冲突消失**，该问题整体随 FR-108 顺延到 1.2.0
> - **A-16 疫苗类污染**——疫苗触发卡跳健康模块、结构上恒不转化却计入转化率分母的问题，因 FR-108 不做而**分母天然干净**
>
> **顺延到 1.2.0 时须一并带走的未决项：** **SPEC-15**（触发提前量，2026-08-15 已定为 **7 天**，与 FR-109 对齐，1.2.0 直接取用）· **SPEC-16**（复购卡优先级与频控，届时需重新计算，因为本版本区域① 只有 FR-109 一个来源）。
>
> ---
>
> **以下为原 FR-108 正文，保留作 1.2.0 的输入，本版本不实现：**

**数据源：** FR-45A 结构化健康记录（疫苗 / 驱虫 / 月经 / 绝育 / 自定义）+ **到期间隔**（1.2.0 健康提醒提供）

**后果：**
- 当健康提醒判定某项（**仅限驱虫、疫苗两类**）临近到期时，除既有提醒外，额外在两处生成复购触发卡：
  1. **Diary 时间线**对应健康记录条目内嵌 CTA（宿主为 V1.1.2 FR-82 五类条目中的「结构化健康记录」条目）
  2. **健康提醒推送**内附商品直达（复用 FR-38 深链接规则，直跳商品详情）
- 触发卡内容：`{宠物名}的驱虫周期快到了` + 关联商品 1 个 + 「查看」按钮
- **仅驱虫与疫苗参与触发**。月经、绝育、自定义类型不触发——月经无对应商品，绝育是一次性事件，自定义类型无结构化语义（FR-45A 已明确自定义类型不参与任何联动）
- **疫苗触发的是「预约问诊/线下接种」而非直接卖疫苗**。疫苗为处方类医疗行为，平台不自营销售疫苗，该触发卡跳转健康模块而非商品详情
- 频控：同一宠物同一健康事件类型的复购触发卡 **30 日内最多出现一次**，避免提醒疲劳
- **提前量 = 到期日前 7 天**（SPEC-15，2026-08-15 定，与 FR-109 的「耗尽日前 7 天」对齐；原文「临近到期」无定义）

> 🚫 **以上 FR-108 正文本版本不实现，到此为止。** 下方 FR-109 起恢复为本版本范围内。

#### FR-109：粮量见底预估

**数据源：** 电商订单历史（最近一次 Makanan 品类购买的 SKU 规格与日期）+ 商品的 `每日建议喂量`（FR-94）+ 宠物档案体重

**计算：**
```
日喂量(克) = 按档案体重在商品「每日建议喂量」区间表中匹配
预计可用天数 = SKU 净含量(克) ÷ 日喂量
预计耗尽日 = 上次购买日 + 预计可用天数
```

**后果：**
- 耗尽日前 **7 天**开始，在 Toko 首页区域 ① 展示补货卡，并推送一次
- 卡片文案给出估算依据而非断言，如「Makanan Mochi diperkirakan habis ~5 hari lagi」（预计约 5 天后吃完），**不写成确定事实**——这是估算值，档案体重不准或用户混喂时会有偏差
- 用户完成该 SKU 的再次购买后，预估周期从新订单日重新起算，旧提醒立即消失
- 缺少任一输入（无购买历史 / 商品未配日喂量 / 档案无体重）时不触发，**不做兜底猜测**

#### FR-110：问诊与商品的边界（约束性需求）

**这是一条限制性需求，不是功能。**

**后果：**
- **兽医在问诊会话中不得推荐具体 SKU**，不提供「兽医推荐商品」组件、不给兽医端任何商品链接插入能力
- 问诊结论页可出现的商品关联仅限一种形式：**按结论中的结构化健康记录类型（如驱虫）跳转对应品类**，由系统生成、非兽医选择
- 理由：`TailTopia-战略决策快照.md` 中「靠什么赢」的第一条是 AI 问诊与本地信任。问诊一旦被用户感知为销售前端，这条护城河即失效，其损失远大于电商的短期转化收益。此约束优先于任何转化率优化

---

## 4. 状态机汇总

### 4.1 订单状态机

```
                    ┌────────────────────────────────┐
                    ↓                                │
  [提交订单] → 待支付 ──支付成功──→ 待发货 ──发货──→ 已发货
                 │                    │                 │
       超时60min │           申请取消 │        承运商送达│
       /用户取消 │                    ↓                 ↓
                 ↓                 退款中 ←──申请退货── 已送达
              已取消(终)               │                 │
                                       ↓        确认收货 │
                                   已退款(终)   /7日自动 ↓
                                                    已完成(终)
                                                         │
                                            签收7日内申请退货
                                                         ↓
                                                      退款中
```

**终态：** `已取消` / `已退款` / `已完成`（`已完成` 在签收 7 日内仍可转入 `退款中`，7 日后为绝对终态）

> **⚠️ 订单状态机与退款状态机的关系（2026-08-15 定，OQ-38 / FR-104A）：**
>
> **退款单（§4.2）独立于订单状态机，不是订单的子状态。** 行级部分退货支持后，两者的耦合点只有一处：
>
> - **部分退货不改变订单主状态**——订单仍停留在 `已完成`（或 `已发货`/`已送达`，取决于申请时点），退款进度只体现在退款单上
> - **仅当该订单全部 SKU 行的退款均达到 `已退款` 时，订单主状态才转 `已退款`**
> - 上图中 `已发货`/`已送达`/`已完成` → `退款中` 三条边，**仅在整单退货时成立**；部分退货时订单状态不迁移
> - 同一订单**同时只允许一个进行中的退款单**（FR-104A 并发约束），因此不存在多个退款单并行改写同一订单状态的竞态

### 4.2 退款状态机

```
[提交退货申请] → 待CS审核 ──驳回──→ 已驳回(终，可申诉复用FR-52)
                    │
                  批准
                    ↓
            待用户寄回 ──超时未寄回(7日)──→ 已关闭(终)
                    │
                 已寄回
                    ↓
                 质检中 ──质检不通过──→ 已驳回(终)
                    │
                质检通过
                    ↓
              退款执行中 ──→ 已退款(终)
```

**说明：** 「待发货前取消」与「拒收」两类情形跳过 `待用户寄回` 与 `质检中`，由 `待CS审核` 批准后直接进入 `退款执行中`。

**退款单的作用域（2026-08-15 定，OQ-38 / FR-104A）：**
- 一张退款单**承载一个或多个 SKU 行**（用户在申请时勾选要退的行），不是「一单一行」
- 退款单**独立于订单状态机**，其状态迁移不写订单主状态；仅在「该订单全部行均已退款」时由系统回写订单为 `已退款`
- **同一订单同时仅允许一张进行中的退款单**（状态处于 `待CS审核` / `待用户寄回` / `质检中` / `退款执行中` 之一），并发申请在入口层拦截
- 退款金额按 `实退商品金额 ± 运费调整` 后，再按 `payment_intents.coin_ratio` 拆成 PawCoin 段与 QRIS 段（FR-100A 规则 2 · FR-105）

### 4.3 库存状态机

```
可售 ──提交订单──→ 锁定 ──支付成功──→ 已扣减(出库)
  ↑                  │
  │      支付超时/取消 │
  └──────────────────┘
  ↑
  └── 退货质检通过后入库（后台操作）
```

---

## 5. 埋点

事件写入 `3.数据埋点/埋点文件/埋点清单v140.md`，并**同步工程侧 `petgo-platform/docs/analytics-posthog-tracking.md`（该文件明确要求两边内容一致）**。平台 PostHog。

| 事件 | 关键属性 | 用途 |
|---|---|---|
| `toko_tab_viewed` | `entry_source` | Tab 曝光分母 |
| `product_impression` | `product_id`, `sku_id`, `zone`(补货/精选/分类/全部) | 各区域曝光效率 |
| `product_detail_viewed` | `product_id`, `entry_source` | 详情转化分母 |
| `add_to_cart` | `sku_id`, `qty`, `entry_source` | 加购转化 |
| `checkout_started` | `cart_value`, `item_count` | 结算漏斗 |
| `order_submitted` | `order_id`, `amount`, `shipping_method`, **`items[]{sku_id, qty, entry_source, trigger_type}`** | 下单转化 · **行级归因** |
| `payment_succeeded` | `order_id`, `amount`, `channel`, **`attribution_source`** | 支付成功率 · **归因终点** |
| `payment_failed` | `order_id`, `reason` | 支付失败归因 |
| `repurchase_card_impression` | `trigger_type`, `pet_id` | **复购引擎曝光** |
| `repurchase_card_clicked` | `trigger_type`, `product_id` | **复购引擎点击率** |
| `profile_reco_clicked` | `product_id`, `reco_reason` | 档案推荐有效性 |
| `out_of_stock_viewed` | `sku_id` | 缺货损失量化 |
| `return_requested` | `order_id`, `reason_type` | 售后率 |
| **`cart_viewed`** | `cart_value`, `item_count` | **购物车→结算漏斗**（原清单缺此节，漏斗断了一段） |
| **`checkout_blocked_out_of_range`** | `kecamatan` | **超服务范围阻断**——17 屏专门画了这一屏，但原清单无事件，最确定的流失点不可知 |
| **`repurchase_card_dismissed`** | `trigger_type` | counter · 复购推送疲劳 |
| **`push_permission_revoked`** | `from_screen` | counter · 推送疲劳的终点信号 |
| **`triage_to_category_jump`** | `record_type` | counter · **FR-110 边界侵蚀监控**（问诊→商品跳转占比） |

> **`trigger_type` 的取值域（2026-08-15，OQ-36 拍板后）：** 枚举**仍保留 `DEWORM` / `VACCINE` / `FOOD_LOW` 三值**（供 1.2.0 FR-108 上线时直接追加数据，避免届时改埋点定义、断掉历史序列），但**本版本只会产生 `FOOD_LOW`** —— FR-108 已挪 1.2.0。看板与 AB-13B 口径须据此写明：本版本 `DEWORM` / `VACCINE` 恒为 0，**不是数据丢失**。

**归因链闭合（新增，2026-08-14 validate）：** 后台 AB-13B 把「触发卡转化率 vs 普通商品曝光转化率」列为**判定 A-16 成立与否的直接依据**，但原清单的 `order_submitted` / `payment_succeeded` **不带任何来源属性**，且订单是多商品的——链路只到 `add_to_cart` 为止，**能算点击率，算不出转化率**。故上表为两者补齐归因属性。
> 更可靠的替代实现：**在服务端订单行上持久化加购来源**，由后台直接出数——不受客户端事件丢失与广告拦截影响。二选一由架构 delta 定，但**必须有一个**，否则 A-16 无法被验证（`decision-log` L-6 已有同类前车之鉴）。

**PII 禁入规约（新增，2026-08-14 validate）：** 本版本首次采集收件人姓名 / 手机号 / 详细地址 / 邮编。`CLAUDE.md` 强制护栏写明「**严禁记录 PII / 健康数据 / 令牌 / 签名 URL**」。因此：
- 上述四类字段**一律不得进入 PostHog 事件属性**，包括拼接进任何 id 或文案
- `pet_id` 明确为**不可反查的内部标识**（该事件带健康语义，`trigger_type` 已含驱虫/疫苗）
- 商品图 / 凭证图**只传 id，不传签名 URL**

**⚠️ 必须先取基线，否则本版本的效果无法评估。**

`3.数据埋点/埋点文件/埋点清单v112.md` §0.3 记录了一次真实教训：V1.1.2 改版的埋点与改版**同版本发布**，导致采到的第一个数据点已是改版后，「游客→注册总转化率」「FR-0B 曝光量变化」「B/C 用户留存」三项核心指标**全部不可得**，PRD 中的「唯一裁决指标」与处置原则一并失效。

电商 Tab 会替换掉一个既有 Tab，面临完全相同的风险。因此：

- **`toko_tab_viewed` 及被替换 Tab 的对应曝光/使用事件，必须在电商上线的前一个版本先行发布**，积累至少 2 周基线
- ⚠️ **被替换 Tab 一经 DEP-1 确定，须立即把它的曝光/使用事件写成具名事件补进本表**——当前写法是泛指，无法据以排期。参见 §3.1 L-12：Tab Bar 当前无空位，「替换其一 / 扩为 5 Tab」的选择直接决定基线要取哪个 Tab
- 若做不到，需在本版本 PRD 评审时明确承认「本次 Tab 替换的得失不可评估」，而不是上线后才发现

---

## 6. 跨文档影响说明

| 文档 | 需要的改动 |
|---|---|
| `1.项目总述/Roadmap.md` | ① 1.4.0「精选电商 Phase A（导流/跳转 Tokopedia/Shopee）」改为自营；② 2.1.0「精选电商 Phase B（商家自行履约）」废除或改为品类扩展；③「关键提醒」表中「电商合作商家签约启动（目标 5-10 家精选），月2 启动」改为「自营供应商/经销商签约与首批采购」；④「平台抽佣」表新增自营**进销毛利**行，与 O2O 10% 抽佣区分。详见 `Roadmap-电商改动建议.md` |
| `V1.1.0/v1-1-0PRD.md` FR-43 | 支付渠道原文为「QRIS、DANA」，与 Roadmap 2026-07-15 更正（仅 QRIS）不一致，需以其中之一为准并同步。本 PRD 按仅 QRIS 设计 |
| `V1.1.0/v1-1-0PRD.md` FR-54 | 订单卡片类型由 4 类扩展为 5 类，新增电商订单卡（本文 FR-101） |
| `V1.1.0/v1-1-0PRD.md` FR-50C | **PawCoin 消费范围需扩写纳入实物商品**，并写入 FR-100A 的**七条**使用规则（尤其规则 1「PawCoin 部分只退 PawCoin」——该条是 FR-50D「不支持一般性提现」在实物场景下的必要补强，属对 FR-50 体系的实质性扩展，不是电商局部规则）。⚠️ **是七条不是六条**：初稿的「六条」漏掉了 **规则 7 · 平台责任 Coin 溢价补偿**（C-9 于 2026-08-10 加入）——而那正是唯一涉及**资金发放**的一条，照「六条」回写会把它留在电商 PRD 里 |
| `V1.1.0/v1-1-0PRD.md` FR-50D | 「不支持一般性提现」的论证需补一句：实物商品退货是新出现的潜在提现路径，已由 FR-100A 规则 1 封堵 |
| **`V1.1.0/v1-1-0后台prd.md` AB-6A** | **实质性扩展**：新增第二条「平台责任补偿溢价」配置组。⚠️ 与既有激励溢价**必须是两个独立配置项**（C-9 / D-8），不可复用同一数值 |
| **`V1.1.0/v1-1-0后台prd.md` AB-6C** | **口径变更**：浮存监控需按来源区分（充值币 vs 两类溢价凭空发行的赠币） |
| **`V1.1.0/v1-1-0后台prd.md` AB-8E** | 支付记录需新增电商相关筛选项 |
| **账号注销的数据生命周期** | ⚠️ **新增，2026-08-14 validate。** 本版本新建的表（收货地址簿 / 电商订单 / 订单地址快照 / 物流 / 评价 / 退货申请 + 凭证图）**必须逐类登记进注销级联删除/匿名化**——`CLAUDE.md` 将其列为三类安全攸关节点之一（story 7.3，按决策 D1/D2 落实），而初稿一行未登记。存在**天然冲突需 Legal 表态**：已完成订单需留存对账 vs 用户删除权；且必须先定性 FR-98 地址簿是「履约凭证」还是「个人数据」——两种定性导出**相反**的处置。另见 §9 **OQ-41** |
| `3.数据埋点/` 两份文件 | 新增 `埋点清单v140.md`，同步工程侧 `analytics-posthog-tracking.md`。**须包含 §5 新增的行级归因属性、5 个 counter 事件与 PII 禁入规约** |
| `V1.0.0/icon-system.md` | 需补 Toko Tab 图标的常态/激活态（激活态萌化规范见 V1.1.2 FR-78A）。由 Tab 改版负责人产出，见 §8 |

> **⚠️ 上表原先只登记 App 侧，漏了后台侧。** `decision-log` L-4 强调「这不是电商板块的局部规则，而是对 FR-50 封闭币体系的**实质性扩展**，评审时应由 PawCoin 体系的负责人一并确认」——**同样的论证完全适用于 AB-6A / AB-6C，但 L-4 与本表都没提**，结果是用户端回写有人跟、后台回写没人跟。已补三行。

---

## 7. 非本版本范围（明确排除）

- 第三方商家入驻、商家后台、商家自行履约 —— 与自营模式互斥，且违背战略边界
- 全站商品搜索 —— 见 FR-93
- 优惠券 / 促销码 / 会员价 —— 会员制套餐 Roadmap 整体暂缓
- 直播带货 —— Roadmap 中依赖电商 Phase B，本版本不涉及
- 承运商 API 实时轨迹与实时运费 —— 见 FR-103 / FR-99 的分期理由
- 跨城/全国配送 —— 首发限 Jabodetabek 服务范围
- **换货** —— **2026-08-15 拍板（OQ-39）**。换货需要「质检通过后补发 + 补发发货 + 换货出入库配对 + 换货专属状态」四块能力，本版本零实现。FR-94 的退货规则标识已由三值收为 `可退` / `开封不退` / `不可退`，**避免运营标出平台无法兑现的承诺**。用户遇到需换货的场景走「退货 → 重新下单」
- **FR-108 健康记录触发复购** —— **2026-08-15 拍板（OQ-36 / D-10 方案 a）**，挪 Roadmap **1.2.0**。触发所需的「到期间隔」不在当前代码中（L-10），其权威来源是 1.2.0 的健康提醒功能，本版本不自建间隔默认值。**连带排除**：Diary 时间线内嵌复购 CTA、健康提醒推送内附商品直达、`health_records` 的任何 schema 改动。原正文保留在 §3.9 供 1.2.0 取用
- **用户侧票据 / e-Faktur / 税额单列行** —— ⚠️ **不是拍板排除，是尚未决定**，见 **OQ-40**。DEP-3 已把「PPN 登记与开票」标为高·阻塞上线，此处列出仅为提示「它当前既没被做、也没被正式排除」

---

## 8. 依赖项（本 PRD 范围外，需他方确认）

### 8A. 代码前置项（L-8 ~ L-12）

> **2026-08-14 `bmad-prd` validate 承接。** 以下五条为落库时逐条核对 `petgo-platform` 源码得出的「**PRD 假设 vs 当前代码**」偏差，初稿的 PRD 与 decision-log **均未记录**，正文四处写的是与代码相反的乐观表述。已在对应 FR 处就地修正，此处汇总登记。**下游 UX / 架构 / epics 只读 PRD 也不会再失明。**

| # | 偏差 | 阻塞程度 | 出口 |
|---|---|---|---|
| ~~**L-8**~~ | ~~混合支付与 `pay/domain/PaymentIntent.java` 的单渠道单金额模型冲突~~ | ~~最高~~ → ✅ **已闭合** | ✅ **2026-08-15 拍板（OQ-35 / D-9 方案 a）：扩展既有 `PaymentIntent`**——`PayChannel` 末尾追加 `MIXED`，新增 `coin_amount` / `cash_amount` / `coin_ratio` 三列（nullable，既有 4 个 purpose 全为 NULL、行为不变）。**方案与兼容契约详见 FR-100**。⚠️ 触碰共享表，须占独占 Flyway 号段 + 告知另两人 |
| **L-9** | `profile/domain/PetProfile.java` **无体重字段、无绝育状态** → FR-109 恒 100% 不触发，复购引擎三缺一 | **高** | 排入本版本 story（加列 + 表单 + 存量回填引导），详见 §3.9 前置数据资产 |
| ~~**L-10**~~ | ~~`profile/domain/HealthRecord.java` 仅有 `eventDate`，无 `next_due_date`、无 `interval`~~ | ~~高~~ → ✅ **已闭合** | ✅ **2026-08-15 拍板（OQ-36 / D-10 方案 a）：FR-108 整体挪 1.2.0，本版本不补该字段**。`health_records` 本版本**零 schema 改动**。⚠️ 代价：复购引擎降为两条机制，**DEP-6 升为版本核心单点依赖**——见 §3.9 风险块 |
| **L-11** | `profile/service/ProfileService.java:56` 硬约束**单账号单宠物**（`existsByOwnerId` → 409），FR-93 引用的「全局当前宠物」概念不存在 | 低，不阻塞 | 已改写 FR-93 为单宠物实现；**epics 阶段须标注「1.3.0 后启用」** |
| **L-12** | `bottom_tab_bar.dart` 的 `AppTab` 为 **4 值无预留位**；且初稿 §3.1「已预留」与 §5「替换既有 Tab」**自相矛盾** | 中 | 归 **DEP-1**。已统一 §3.1 表述，并把「当前无空位」写进 DEP-1，使承接方拿到的是问题而非误判前提 |

### 8B. 架构前置决策（须在 `architecture-v1.4.0-delta.md` 中拍板）

本 PRD 是 chain-top（下游 UX → 架构 → epics → story）。以下三项若不在架构 delta 中落成 AD 条目，链条会在第二环断：

1. ~~**混合支付模型二选一**（L-8 / OQ-35）~~ → ✅ **产品侧已拍板方案 a（扩展共享 `PaymentIntent`）**，架构 delta 的任务从「二选一」变为「**把方案 a 落成 AD 条目**」：`PayChannel` 追加 `MIXED` 的枚举契约、三个新列的可空性与不可变性、`coin_ratio` 的精度与舍入规则（拆分金额必须能整数分账、不产生 1 盾漂移）、以及退款按比例拆分的落地写法
2. **Flyway 号段隔离**——仓库现有迁移已占到 **V100**（2026-08-15 实测：99 个文件、**V86 为空洞**、`main`/`stag`/`hex/v1.1.4`/`hex/v1.1.2` **四条分支全部停在 V100**，无人越过）。本版本与另两位同事**并行开发**。`CLAUDE.md` 的「按执行顺序单调分配」是**单人串行前提**，并行下必须升级为**独占号段**（`V60` 文件头与 `sprint-status-v1.1.yaml` 第 38 行记录了 2026-07-11 因跨分支撞号被迫全量重排的先例）
   > **V86 空洞恰好是这条约束可行的证据：** Flyway **从不要求版本号连续**，按号排序依次执行即可，跳号完全合法。所以独占号段不需要任何额外机制，只需三人不写进对方区间。**并行下还必须废掉 E2 的「merge 时号继续单调顺延」**——重排会让已在 stag/测试库应用过的迁移校验和失配，Flyway 直接启动失败。号段规划见架构 delta **§4**（V101–V139），纪律见 `PARALLEL-DEV-CONTRACT.md` **§1**
3. **共享枚举与聚合器的追加约定**——**只在末尾追加，不重排 / 不删除 / 不改既有值拼写**；`order/service/OrderCenterService.java` 加订单类型须**纯追加**（在既有 if 链末尾加分支 + 新增独立 private 方法，不重构既有分支）

> **⚠️ 2026-08-15 实测更正本条两处（原文有误，据源码逐条核对）：**
>
> | 枚举 | 原文 | **实测** | 落库情况 |
> |---|---|---|---|
> | `pay/domain/PaymentPurpose` | 4 值 | ✅ **4 值**（正确） | `payment_intents.purpose` + CHECK |
> | `order/dto/OrderType` | 4 值，「落库为 varchar + CHECK」 | 4 值，但 ❌ **根本不落库** | **全仓无 `order_type` 列**，纯 DTO 枚举 → 改它只有编译期风险，**无运行期静默失效风险**，与另两个不同级 |
> | `notify/domain/NotificationType` | **18 值** | ❌ **19 值** | `notifications.type` + `ck_notifications_type`（以 `V97` 的 CHECK 列表为权威） |
>
> **另有本条完全漏记的一个枚举，而它才是本版本唯一必须改的：** `pay/domain/PayChannel`（2 值 `QRIS`/`PAWCOIN`）——C-10 要加 `MIXED`。它被 **4 个实体**映射（`PaymentIntent` / `ConsultOrder` / `AiConsultOrder` / `IdCardHdPurchase`），取值被 **4 个 CHECK 约束**分别钉在 4 张表上。处置见架构 delta **AD-3**（只放宽 `payment_intents` 一处，另三处刻意不放宽作纵深防御）。

> 三项均须**三人共同确认**，不是单方面写。**✅ 2026-08-15 已起草：`implementation-artifacts/v1.4.0/PARALLEL-DEV-CONTRACT.md`（待三人签字）**，签字后追加摘要到 `CROSS-STORY-DECISIONS.md`（`CLAUDE.md` 指定「遇冲突以此为准」）。
>
> 🔴 **该契约的 §0 记录了两次真实事故，都在本仓库里：** ① **2026-07-11** 跨分支 Flyway 撞号 → V1.1 资金地基迁移号全量 +13 重排（记录在 `V60` 文件头）；② **2026-07-30** 两条工作线各自 `DROP+ADD` 同一个 `ck_notifications_type` → 合并后**审核通知整类静默失效**（记录在 `V97` 文件头）。**事故 ② 与本版本改 `PayChannel` 的处境完全相同**——两边各自测试全绿、合并无冲突、编译不报错，只在真发那类通知时才炸。

### 8C. 外部依赖

| # | 依赖 | 责任方 | 阻塞程度 |
|---|---|---|---|
| DEP-1 | Toko Tab 的最终位序、被替换模块（问诊/健康）的迁移方案、以及 V1.1.2 FR-78 落地 Tab 规则是否随之调整。⚠️ **前提事实（L-12）：当前 Tab Bar 无空位**——`AppTab` 为 profile/triage/home/me 四值 + 中间 `[+]`，因此只能是「替换其一」或「扩为 5 Tab（挤压 [+]）」。该选择同时决定 §5 要取哪个 Tab 的曝光基线 | Tab 改版负责人 | **高**，阻塞 FR-93/93A 定稿 |
| DEP-2 | Toko Tab 图标常态/激活态设计（含 FR-78A 萌化规范） | 设计 | 中，阻塞 UI 交付 |
| DEP-3 | 经营范围合规：PT SDGI Technology Indonesia 的 NIB / KBLI 是否覆盖网络零售；PPN 增值税登记与开票 | Finance + Legal（Joko / Lantip） | **高**，阻塞上线 |
| DEP-4 | 商品来源合规：从本地授权经销商采购的品牌授权书（规避平行进口/假货认定）；不走直接进口以规避农业部许可与 Karantina 检疫 | 商务（Rendy / Stella） | **高**，阻塞备货 |
| DEP-5 | 承运商商务账号：GoSend / Grab Express / JNE 等的企业寄件账号与费率 | 运营 | 中，阻塞 FR-99 运费表 |
| DEP-6 | 首批 SKU 清单与进货价，**以及每日建议喂量数据**（决定 FR-94 的「每日建议喂量」等结构化字段能否填全） | 商务（Rendy） | 🔴 **最高**（2026-08-15 二次上调；08-14 由「中·阻塞商品录入」上调为「高·阻塞 FR-109 可用性」）——**OQ-36 拍板 FR-108 挪 1.2.0 后，复购引擎的冗余度归零**：只剩 FR-107（规则式过滤）与 FR-109，而 FR-109 的唯一计算依据就是这份数据。缺它则复购引擎实际只剩 FR-107，即 §1 自己警告的「更差的 Shopee」。**阻塞面已升为版本核心论证**，须在商品录入排期前拿到，不能「录商品时顺便补」 |
| **DEP-7** | **PawCoin 扩展到实物商品的合规确认**：PawCoin 作为封闭式虚拟币（不可提现 FR-50D、不可转让 FR-100A 规则 6、仅限平台自营商品），扩展至实物零售后是否触及印尼电子货币（uang elektronik）相关监管口径。**关键在于必须维持这三条闭环特征**——FR-100A 规则 1 正是为守住其中的「不可提现」而设 | Finance + Legal（Joko / Lantip） | **高**，阻塞 PawCoin 购物上线（不阻塞电商本身） |
| **DEP-9** | **自营备货的上限与周转纪律**（2026-08-14 新增，承接 A-17 / `decision-log` N-2）：本版本把「先抽佣/代采、控自营备货」这条缓释措施拆掉了，需另行给出备货金额上限、周转天数目标与超限处置 | 商务 + Finance | **高**，阻塞首批采购 |
| **DEP-8** | PawCoin 支付实物商品的**税务与收入确认口径**：PawCoin 属预收款，充值时不确认收入、消费时确认；PPN 开票金额按商品零售价还是实付真钱？运费被 PawCoin 抵扣部分如何入账（FR-100A 规则 3） | Finance（Joko） | 中，阻塞 AB-13D 对账口径 |

---

## 9. 待解决问题（OQ-25 起）

| # | 问题 | 现状建议 |
|---|---|---|
| ~~OQ-25~~ | ~~首批 SKU 数量上限定多少？~~ | ✅ **2026-08-10 已确认：商品不会很多。** 上限锁 **30**（后台 AB-10D 超限告警），首批预计远低于此。这一确认同时坐实了三处依赖它的设计：规则式推荐（FR-107）、固定运费表（FR-99）、不做全站搜索（FR-93） |
| ~~OQ-26~~ | ~~PawCoin 是否可用于购买实物商品？~~ | ✅ **2026-08-10 已确认：可以用于购物，但须增加使用规则。**（覆盖原「建议首版不可」）规则见 **FR-100A** **七条**：防套现 / 混合支付与按比例退款 / 运费 / 单笔上限 / 浮存门槛联动 / 不参与场景 / **平台责任 Coin 溢价补偿**。后台配置见 **AB-6D**，合规确认见依赖项 **DEP-7** |
| **OQ-33** | PawCoin 单笔支付上限定多少？ | **建议 Rp 1.000.000**（FR-100A 规则 4）。⚠️ **它的用途是「故障/欺诈爆炸半径 + DEP-7 监管姿态」，不是控浮存**（限制消费反而让浮存降得更慢）。故上限应设在**几乎不触发**的水平——客单约 Rp 285.000，100 万基本不会碰到，正好是廉价保险。**定低反而有害。** 若 DEP-7 法务结论指向余额限制，应改为余额上限而非单笔上限 |
| **OQ-34** | 平台责任退货的 Coin 溢价比例与上限定多少？（FR-100A 规则 7） | **建议起步 5%，单笔溢价上限 Rp 50.000**。理由：比例太低起不到安抚作用，太高会诱发骗退；上限防止大额订单的溢价失控。**须与 AB-6A 既有的激励溢价分开取值**，不要沿用同一个数。最终值由财务定 |
| **OQ-27** | 自动确认收货 7 日 + 签收后 7 日退货窗口，两者叠加是否过长？ | 建议保持。印尼电商惯例下 7 日属常规，且自营模式下售后成本可控 |
| **OQ-28** | 「开封不退」的开封判定标准由谁认定？ | 建议由 CS 依据用户上传凭证图判定，并在后台沉淀判例（AB-12D）。首版不引入第三方质检 |
| ~~**OQ-29**~~ | ~~复购触发卡（FR-108/109）与 1.2.0 健康提醒推送是否合并为一条推送？~~ | ✅ **2026-08-15 自动消解**：OQ-36 拍板 FR-108 挪 1.2.0 后，本版本**不存在健康记录触发的推送**，与 1.2.0 健康提醒无交叠，冲突消失。**该问题连同 D-5 一并顺延到 1.2.0**，届时须与健康提醒负责人对齐推送模板。FR-109 的补货推送来自 Toko 侧、与健康提醒不同源，不涉及合并 |
| *OQ-30 ~ OQ-32* | *（属运营后台，见 `PRD-v1.4.0-后台.md` §8。OQ 是两份文档共享的单一命名空间）* | ⚠️ 三条**在 decision-log 中无对应 D 编号、无责任方**，而 OQ-30（供应商主数据）与 OQ-31（报损/盘点审批流）会影响模块 10 的表结构与权限设计。建议补 D-11 ~ D-13 |

### 9A. 2026-08-14 `bmad-prd` validate 新增（OQ-35 起）

| # | 问题 | 现状建议 | 责任方 / 阻塞 |
|---|---|---|---|
| ~~**OQ-35**~~ | ~~混合支付模型二选一~~（L-8 / D-9） | ✅ **2026-08-15 拍板：方案 (a) 扩展共享 `PaymentIntent`。** `PayChannel` 末尾追加 `MIXED`；新增 `coin_amount` / `cash_amount` / `coin_ratio` 三列（nullable，既有 4 个 purpose 全 NULL、行为不变）；退款在同一笔 intent 内按 `coin_ratio` 拆分 → **天然原子**，不需跨两笔 intent 做一致性。方案与兼容契约见 **FR-100**。⚠️ 触碰共享表，须占独占 Flyway 号段 + 事先告知另两位同事 | ~~架构~~ → **已解除阻塞**，FR-100A / FR-105 / AB-12C / AB-13D 相关 story 可开写 |
| ~~**OQ-36**~~ | ~~FR-108 的到期判定间隔从哪来~~（L-10 / D-10） | ✅ **2026-08-15 拍板：方案 (a) 依赖 1.2.0，本版本 FR-108 不做**，整体挪 Roadmap 1.2.0。`health_records` 本版本零 schema 改动；UX 不出相关屏；埋点不埋相关事件。**连带消解 OQ-29（推送合并冲突）与 A-16 的疫苗类分母污染。** ⚠️ **代价：复购引擎降为 FR-107 + FR-109 两条，冗余归零，DEP-6 升为版本核心单点依赖**——详见 §3.9 风险块 | ~~产品~~ → **已闭合**；遗留动作在 **DEP-6**（催 Rendy） |
| **OQ-37** | 用户能否选择「**本单不使用 PawCoin**」？ | 建议**给开关**。当前 FR-100A 规则 2 的「余额优先全额抵扣」是无条件的，叠加规则 1（Coin 段只退 Coin）后，**每一单都在把用户可退现金的钱不可逆地转为只能退 Coin 的钱**，哪怕余额只有几千盾；而这些 Coin 多来自 FR-43D 退款转入的零头，本意是留着买问诊。**不用 Coin 不产生任何套现路径**，与规则 1 的目的无关。若坚持强制抵扣，须写成显式决定并给理由 | 产品 |
| ~~**OQ-38**~~ | ~~去程运费在部分退货时是否退回？是否支持行级部分退货？~~ | ✅ **2026-08-15 拍板，落为 FR-104A**：**支持行级部分退货**；**同一订单同时仅允许一个进行中的退货申请**；**整单退 → 去程运费全退，部分退 → 不退**（并在退货申请页明示，堵住免运门槛 + 部分退货的凑单-退货套利）；**部分退货不改订单主状态**，仅全部行退款完成才转 `已退款`。见 FR-104A / §4.1 / §4.2 | ~~产品 + 财务~~ → **已闭合** |
| ~~**OQ-39**~~ | ~~商品标识 `可退可换` 的「换」首版是否砍掉？~~ | ✅ **2026-08-15 拍板：砍掉。** 枚举由三值收为 `可退` / `开封不退` / `不可退`（FR-94）；§3.7 与后台模块 12 标题改为「**退货与退款**」；「换货」已写进 **§7 非本版本范围**。理由：换货流程零实现（无换货状态、无质检后补发、无补发发货、无换货出入库配对），运营标成「可退可换」等于让用户端展示平台无法兑现的承诺。用户走「退货 → 重新下单」 | ~~产品~~ → **已闭合** |
| **OQ-40** | **PPN 与发票**：零售价是否含税？金额明细是否单列税额行？是否向用户提供收据/e-Faktur？ | 无设计侧倾向。⚠️ DEP-3 把「PPN 登记与开票」标为**高·阻塞上线**，但 FR-97 的金额明细只有四行**没有税行**，全文无任何用户侧票据能力，§7 **也没有排除它**——它只是没写。退款/部分退款后的票据冲红同样空白 | Finance + Legal · 关联 DEP-3 / DEP-8 |
| **OQ-41** | 新增数据的**注销级联处置**与留存年限（收货地址簿 / 电商订单 / 地址快照 / 物流 / 评价 / 退货申请 + 凭证图） | 无设计侧倾向。存在真实冲突：**已完成订单需留存对账 vs 用户删除权**；且须先定性 FR-98 地址簿是「履约凭证」还是「个人数据」——两种定性导出相反的处置。另需确认本版本首次建立的 PII 数据集是否触及印尼 **UU PDP**（DEP-1~8 无一条覆盖），以及 AB-11A「可按电话模糊搜全站订单」的后台列表是否需要脱敏/访问审计 | Legal · **安全攸关**（`CLAUDE.md` 三类节点之一） |
| **OQ-42** | 复购引擎的**成功指标目标值与 kill criteria** | 建议建立 SM-1~SM-4 编号，含：触发卡→成单转化率相对全站曝光的**倍数门槛**、30 日复购率目标、**观察窗口（建议上线后 8 周）**、不达标时的处置（收缩 / 继续 / 改机制）。⚠️ 当前 A-16 的判据是「若触发卡点击率不**显著**高于普通商品曝光」——「显著」无数值、无窗口、无处置，**上线后没有任何一句话能裁决复购引擎是否成立**。两处口径污染中，**疫苗类分母污染已随 OQ-36 拍板自动消解**（FR-108 不做，本版本不产生疫苗触发卡）；**剩下的仍需处理**：**冷启动期**（首月无人有电商购买历史，FR-109 对全体用户恒不触发）恰好覆盖验证窗口。⚠️ **2026-08-15 新增权重**：FR-108 移出后复购引擎只剩两条机制，kill criteria 实际是在裁决 FR-107 + FR-109，**指标口径必须写明它评的不是原设计的三条**，否则会用两条的数据否掉三条的设计 | 产品 |

### 9B. 待补规格清单（SPEC-1 起，2026-08-14 validate）

以下条目**不需要拍板，只是没写**——但每条都会让下游拆 story 时卡住或临场发明。按承重程度排序：

| # | 缺什么 | 位置 |
|---|---|---|
| **SPEC-1** | **退货寄回的全链路**：寄到哪个地址、用户自寄还是上门取件、按什么标准计费。而 FR-100A 规则 3 与 AB-12C 都在按「退货运费」算钱，§4.2 也已画了 `待用户寄回` 状态 | FR-104 / FR-99 / AB-11C / AB-12A |
| **SPEC-2** | **`已发货` 的兜底出口**：`已送达` 的两个进入条件里「承运商标记」本版本不存在（不接 API），后台又**没有把「标记已送达」定义成一个操作**，漏标即死锁。需 ① 后台加「标记已送达」动作；② `已发货` 态给用户「确认收货」入口；③ 发货起 M 日无标记自动置 `已送达`（建议 M=7，与配送时效 2–4 日留余量），并给 AB-11B 的 N 取值（N &lt; M） | FR-102 / §4.1 / AB-11A / AB-11B |
| **SPEC-3** | **部分退款的状态表达**：订单级与退货申请级须分离——订单在部分退款后**保持原状态**，仅全部行退款完成时转 `已退款`；并补一张「退款单状态 → 订单状态」映射表（AB-12C 已要求不允许不一致，但没给映射）。另 FR-102 的**表**说后台取消进 `已取消`、§4.1 的**图**说必须经 `退款中`，两者打架 | §4.1 / FR-102 / FR-105 / AB-11D / AB-12C |
| **SPEC-4** | **混合支付的取整规则**：IDR 不展示小数，`实退金额 × 固化比例` 必然产生余额。建议「**现金段向下取整、Coin 段吸收全部余数**」，且最后一笔退款按「实付额 − 已退累计」**轧差**而非按比例算。⚠️ 取整方向选错即破坏规则 2 自称的、用于向法务/财务说明闭环性的不变式「用户拿回的真钱永远不超过其投入的真钱」 | FR-100A 规则 2 / FR-105 / AB-12C / AB-13D |
| **SPEC-5** | **「签收」的定义**：该词在 FR-102 后果、FR-104 时限、§4.1 终态说明中共出现四次，**却既不在术语表、也不是任何一个状态名**。建议定义为「订单进入 `已送达` 的时刻」并全文统一。⚠️ 不定义则 7 日退货窗口起算点在实现层不存在，只能落成「以后台标记时间为准」——**用户的 7 天权利长度由运营的手速决定**；且 OQ-27 讨论的「7+7 叠加是否过长」本身无法回答 | §2 / FR-102 / FR-104 / §4.1 / AB-12A |
| **SPEC-6** | **状态机缺边四条**：① 拒收（AB-12A 有正式类型，但 `已发货` 态用户无入口、图上无 `已发货 → 退款中` 边）；② 退款被驳回后的回边（导致 FR-102「无悬空态」按图论为假）；③ 退款执行失败分支（FR-105 自己提到有手续费，说明存在真实资金通道）；④ 用户主动撤销退货申请 | §4.1 / §4.2 |
| **SPEC-7** | **库存状态机缺三条转移**：**采购入库**（按现图，可售库存的唯一来源是「退货质检通过」，即库存只能靠退货凭空产生）· 报损 · 盘点调整——三者都是 AB-10C 明列的合法途径。另缺 `已扣减 → 可售` 的取消回退，以及下架时已锁定库存的归属 | §4.3 / AB-10C / AB-10D / AB-11D |
| **SPEC-8** | **发货仓/单仓前提**：FR-99 的当日达适用范围写「同城 Kecamatan」，而「同城」相对发货地而言——**发货地在两份文档中从未出现**。建议写死「本版本仅一个发货仓，位于 {Kecamatan}」，当日达资格改为「收货 Kecamatan ∈ 后台配置的可达列表」；多仓列入 §7 | FR-99 / AB-10C / AB-11C |
| **SPEC-9** | **一单多包 / 部分发货**：FR-99 的两档配送在物理上必然分包（7.5kg 狗粮走同城骑手 + 一盒驱虫药不可能同一单号），AB-11D 也明确存在「部分取消」，但订单状态是整单级、物流单号是单数。「送达」以哪个包裹为准、7 日自动确认以哪个为起点均未定义。§7 排除的只是「承运商 API 实时轨迹」，**不是「一单多包」** | FR-102 / FR-103 / AB-11A / AB-11B |
| **SPEC-10** | **超卖的处置动作**：AB-11D 只定义了异常订单**视图**，没定义哪一单被取消、按什么顺序、如何通知、是否补偿、库存回补到哪里；用户端也无「已付款但无货」的流程。另需给 FR-95 一个可测的不变式（如「行级条件更新，可售库存不得为负，超卖目标为 0」），并说明真正的超卖来源其实是盘点/报损/退货入库撤销而非并发 | FR-95 / AB-11D |
| **SPEC-11** | **拒收商品的入库路径**：拒收跳过质检，但退回的是**已出库实物**，几天后会真实回到仓库；而入库单必填「采购单号 · 进货单价」，拒收货两者皆无——**钱已退、货已回、系统里不存在**，并污染 AB-13C 的资金占用读数 | §4.2 / AB-10C / AB-12B |
| **SPEC-12** | **锁定库存的防滥用**：待支付 60 分钟的论证只覆盖了「别误杀」。在 SKU 上限 30、自营备货受现金约束的前提下，脚本反复提交不支付即可把爆款库存锁死并无限续期。需补「同用户并发待支付订单数上限 / 单 SKU 单用户锁定量上限 / 连续超时后冷却」 | FR-95 / FR-100 |
| **SPEC-13** | **地址字段的校验口径**：手机号「+62 前缀，8–13 位」——含不含 +62、是否允许前导 0 未定义（印尼号去 0 后常见 9–12 位，**8 位会放进大量无效号码**）；邮编与所选 Kecamatan **无一致性校验**（运费按 Kecamatan 计、承运商按邮编派送）；三级行政区划的**数据来源从未指定**（内置表？三方 API？Kemendagri 码表？印尼有 7,000+ 个 Kecamatan 且持续调整），而 AB-11C 说「支持批量导入」却没说主键码表 | FR-98 / AB-11C |
| **SPEC-14** | **FR-109 公式修正**：① 漏乘购买数量（`净含量 ÷ 日喂量` 无 × qty，**囤货是宠粮最主流的购买行为**）；② 起算点用「上次购买日」而非收货日（快递 2–4 日，而提前量只有 7 天）；③ 多宠物共食未处理。**这三种情况输入全部齐全、只是算错，「缺输入不触发」的降级策略拦不住** | FR-109 |
| ~~**SPEC-15**~~ | ~~FR-108 的触发提前量~~ | ✅ **2026-08-15 定为「到期日前 7 天」**，与 FR-109 的「耗尽日前 7 天」对齐（口径统一，实现与埋点少一个分支）。⚠️ **本版本用不上**——FR-108 已挪 1.2.0（OQ-36），此结论**随 FR-108 一并顺延，1.2.0 直接取用** | ~~FR-108~~ → 1.2.0 |
| **SPEC-16** | **复购卡的优先级与频控**。⚠️ **2026-08-15 范围缩小**：FR-108 挪 1.2.0 后，本版本区域① 的复购卡**只有 FR-109 一个来源**，跨类型优先级（原建议 粮量 &gt; 驱虫 &gt; 疫苗）**本版本不必定**——但**多个 Makanan SKU 同时见底时的排序仍需定**（区域① 只放 2 张）。仍待补：**FR-109「推送一次」的语义**（一次是指每个 SKU 一次、还是每个预估周期一次？重新计算后是否重置？）· **FR-107 推荐区完全没有频控**。跨类型优先级随 FR-108 顺延 1.2.0 | FR-93 / FR-107 / FR-109 |
| **SPEC-17** | **AB-6D 两个开关的用户端降级形态**：「电商 PawCoin 总开关」与「是否允许抵扣运费」在用户端零行为定义。⚠️ 总开关关闭做的正是规则 5 定性为「**等于对已付款用户违约**」的事，两处定性冲突需调和（建议限定为「仅在资金链路故障时使用 + 站内信告知」）；运费开关关闭还会改变比例拆分基数 | FR-100A / FR-97 / AB-6D |
| **SPEC-18** | **规则 6 的措辞修正**：原文「PawCoin **不可用于**支付 PPN 增值税**之外的**任何平台外部结算」——字面把 PPN 排除在禁止之外，**等于允许用无现金垫底的赠币缴税**。应改为「不可用于任何平台外部结算（含 PPN 等税费）」 | FR-100A 规则 6 |
| **SPEC-19** | **规则 2 与规则 4 的冲突**：余额 1.5M、订单 1.2M 时「余额优先全额抵扣」必被上限截断，行为未定义；且 AB-6D 把上限做成可配置项**无最小值校验**，调低后每单都截断而结算页无解释文案位 | FR-100A 规则 2 / 规则 4 / AB-6D |
| **SPEC-20** | **「开封不退」的三处承载位**：FR-104 要求在商品详情页、结算页、退货申请页三处明示，但 FR-97 定义的结算页结构**没有规则展示位**、退货申请页也只定义了凭证图上传。且 FR-94A 允许同商品不同规格有不同标识——切换规格是否刷新提示、多 SKU 订单如何在一处呈现，均未定义。**A-18 假设的全部执行细节都缺** | FR-104 / FR-97 / FR-94A |
| **SPEC-21** | **质检不通过后的商品处置告知**：AB-12B 要求记录「退回用户 / 报损」，但用户端到 `已驳回(终)` 就结束——用户已把货寄出，被驳回后货去哪、要不要再寄回、运费谁付全无说明 | FR-104 / §4.2 / AB-12B |
| **SPEC-22** | **AB-13A 缺三行财务指标**：运费收入 / 承运成本 / 退款手续费。⚠️ 连带使 **A-19 的验证方式不可执行**（它写「按月核对实际承运成本与收取运费的差额」，而承运成本根本没有录入口），而 A-19 承接的是 FR-99「省掉一整条 API 对接工期」的全部风险 | AB-13A / AB-11B / A-19 |
| **SPEC-23** | **AB-13D 对账须区分「充值币 / 赠币」**：AB-13D 写「PawCoin 是此前已收现金的预收款核销」——这对充值币成立，对两类溢价 Coin **不成立**（AB-6C 自己写它们「不像充值那样有等额现金流入垫底」）。要拆开必须在钱包侧做**余额批次分层与核销顺序**（FIFO？赠币优先？），而两份文档无一处要求 | AB-13D / AB-6C / AB-13A |
| **SPEC-24** | **规则 7 的频次风控**：反刷单论证依赖「小额」，但上限 Rp 50.000 ≈ 两次问诊解锁，在客单 Rp 285.000 下不算小额；而触发凭据是 ≤6 张图 + CS 主观判定，**宠物食品的「包装破损」最易制造最难证伪**。AB-12D 判例库是一致性工具**不是风控**。需补同用户/同地址/同支付账号的频次限制、溢价累计上限、异常名单 | FR-100A 规则 7 / AB-12D |
| **SPEC-25** | **AB-13B 的数据源归属**：AB-13B 与 AB-13C 的指标来自 PostHog（三方 SaaS，带丢失率与广告拦截偏差），AB-13A/13D 来自业务库。**两份文档都没说后台如何合并这两套源**（反拉 PostHog API？补服务端埋点？），也没列为依赖——而 AB-13C 的缺货损失会影响补货决策，**等于把分析口径当财务口径用** | AB-13B / AB-13C / §5 |

---

## 10. 假设（A-15 起）

| # | 假设 | 致命度 | 验证方式 |
|---|---|---|---|
| **A-15** | 宠主愿意在 TailTopia 内购买宠粮，而非继续用已有的 Shopee/Tokopedia 习惯 | **高** | 上线后看 `add_to_cart → payment_succeeded` 漏斗与复购率；这是自营模式成立的地基 |
| **A-16** | 复购引擎（**本版本 = 档案推荐 FR-107 + 粮量预估 FR-109 两条**；健康记录触发 FR-108 已挪 1.2.0）确能显著抬高月交易频次 | **高**（2026-08-15 实质升高——机制由三条减为两条，冗余归零） | `repurchase_card_clicked` 与整体转化对比。若触发卡点击率不显著高于普通商品曝光，则本版本的核心论证不成立。⚠️ **前置条件（08-14 立，08-15 更新）**：① 门槛值与观察窗口见 **OQ-42**（当前「显著」无数值）；② ~~转化口径须剔除疫苗类~~ → ✅ **已随 OQ-36 消解**（FR-108 不做，本版本不产生疫苗触发卡，分母天然干净）；③ 须承认**冷启动期**——FR-109 依赖「最近一次 Makanan 购买」，上线首月对全体用户恒为假，此时复购引擎实际**只剩 FR-107 在跑**，在此窗口内采到的数据必然低估复购引擎，可能导致错误的 kill 决策；④ **2026-08-15 新增**：若 **DEP-6**（每日建议喂量数据）未到位，FR-109 恒不触发，则复购引擎**全周期只剩 FR-107**——此时 A-16 根本不可验证，**不是「假设不成立」而是「没做实验」**，kill 决策必须区分这两种情况。另：`add_to_cart` 之后的归因链原本断裂，已由 §5 补齐 |
| **A-17** | 自营备货的资金占用不拖垮跑道 | **高**（2026-08-14 由「中」上调） | ⚠️ **原写法「承接变现模型假设 D，属财务侧，不在本 PRD 验证」不成立。** `decision-log` N-2 明确：假设 D 的原文缓释措施正是「先抽佣/代采、**控自营备货**」，而「改为直接自营后，**该缓释失效**」——本版本亲手拆掉了这条假设赖以成立的护栏，不能再写成「照旧承接」。需由商务与财务另行给出**备货上限与周转纪律**（见 §8C 新增依赖行），阻塞首批采购 |
| **A-20** | PawCoin 开放购买实物后，**浮存净额下降**（而非上升） | 中 | **2026-08-10 更正，原假设方向写反了。** 正确的因果：开放实物消费**新增了一个 Coin 消费出口**，加快存量余额消耗 → 浮存**降**；理论上它也新增了充值动机，但**平台不做充值促销**（`待讨论事项` #3），用户没有理由先充值再买粮而不直接 QRIS 付，故充值动机几乎不增加。**净效应预期为浮存下降。** 验证：上线后按周对比「电商消费核销的 Coin」与「同期充值额」 |
| **A-21** | 两类溢价（AB-6A 激励 + 平台责任补偿）的发放规模可控 | **高** | **这才是真正的浮存风险源**：溢价是**凭空发行的 Coin，没有等额现金流入垫底**，是唯一能在不增加平台现金的前提下抬高负债的通道。**若浮存增速超预期，正确的处置是收紧溢价比例（AB-6A），而不是下调单笔支付上限**——后者只会让浮存降得更慢，是反向操作 |
| **A-18** | 用户接受「开封不退」规则，不因此产生大量客诉 | 中 | 观察 `return_requested` 中因该规则被驳回的比例与 CSAT |
| **A-19** | 固定运费表（不接实时计费）在 Jabodetabek 范围内不产生显著亏损或超收 | 低 | 上线后按月核对实际承运成本与收取运费的差额 |

---

*配套（**落库后文件名**，与源工作区不同源）：`PRD-v1.4.0-后台.md`（运营后台）｜`Roadmap-电商改动建议.md`（Roadmap 定点改写）｜`decision-log.md`（决策日志）｜`页面/`（UI 规格预览）｜`README.md`（落库说明）*

*评审：`../prds/prd-petgo-platform-2026-08-14/validation-report.md`（2026-08-14，rubric + adversarial + 跨文档一致性三名评审员，128 条 findings）*
