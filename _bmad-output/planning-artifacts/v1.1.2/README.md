# V1.1.2 规划输入（已接受，非候选）

> 本目录是 **V1.1.2 迭代的权威输入**，已随仓库 commit，云端 session clone 后可直接读取。
> 与 `incoming-prd-v1.0.0-20260606/`（那是待评估的变更候选）性质不同——**本目录内容可直接作为下游架构 / epics / story 的依据。**

## 1. 文件清单

| 文件 | 说明 | 源文件 |
|---|---|---|
| `PRD-v1.1.2.md` | V1.1.2 PRD，含 FR-78 / 78A / 79 / 80 / 81 / 82 / 83 / 84 / 86 / **87 / 88 / 89 / 90 / 91** 共 **14** 条需求 + §3 埋点需求 | `TailTopia/V1.1.2/改版1-1-2prd.md` |
| `ui-v1.1.2.html` | UI 视觉稿，**18 屏 / 3 条泳道**：T1–T3（Tab 顺序变更前后 · Discovery 第3页软登录 · Tab 激活萌化方案A）、A1–A9·A2b（游客态 / 状态A未建档 / 状态B·C / 时间线五类 / 空态 / 重试 / 日历 / 当天详情 / 问诊未存档时的里程碑）、P1–P4·P1b（发布页默认Diary / 无档案默认Moment / 手动关同步 / 选Moment / 「我的」页私密与公开区分） | `TailTopia/V1.1.2/ui-standalone-features-integrated-v1.html` |

**未随档**：源目录 `.shots/` 下的 18 组逐屏 PNG/HTML（3.7 MB）。整合稿 `ui-v1.1.2.html` 已覆盖全部屏，PNG 只是渲染产物，为避免仓库膨胀不纳入。需要逐屏图时回源目录取。

## 2. 2026-07-31 订正的 2 处内部矛盾（源文件已同步，两边正文一致）

两处都在文中以 `[订正 2026-07-31]` 引用块标注，可全文搜索定位。均为订正 PRD 内部的自相矛盾，**未新增任何需求**。

| # | 位置 | 源文件写法 | 本副本 | 为什么必须改 |
|---|---|---|---|---|
| 1 | §2.4 FR-83 →「数据模型变更与迁移方向」表首行 | 新发布的 Diary → **私密**（默认值） | 新发布的 Diary → **公开**（默认值） | 2026-07-30 产品已把「同步到 Moment」开关反转为**默认开启**，但这张表没跟着改。它与紧邻其下的说明文字、§2.4 正文、§6.3 覆盖清单**三处都冲突**。照原表实现 = 新发日记全部默认私密 → Discovery 内容供给塌陷 + 老用户发布行为无感知突变。这是整份 PRD 风险最高的一处残留。 |
| 2 | §3.2 埋点清单 T-5 | `diary_guest_login_tapped`（点击 FR-80 底部「已有账号？登录」） | 整条删除（编号不重分配） | FR-80 的「已有账号？登录」次要入口已于 2026-07-30 删除（§2.2 该行已划删除线），按钮不存在，事件无触发点。保留会产出一个恒为 0 的指标，并污染 §3.3「转化路径构成」的分母口径。 |

✅ **源文件已于 2026-07-31 同步这两处改动**，两边正文逐字一致（仅 frontmatter 有归档字段差异）。下游以本副本为准。

## 2b. 2026-08-04 新增 §2.6 Splash 启动屏 + 落地矩阵变更（源文件与本副本已同步）

本轮为**新增需求 + 一处既有需求变更**，非订正。两边正文仍逐字一致。全部改动可全文搜索 `[订正 2026-08-04]` 定位。

| # | 内容 | 影响 |
|---|---|---|
| 1 | **新增 §2.6 Splash 启动屏改版**（FR-87 原生交接消白闪 / FR-88「写名字」动效 / FR-89 启动期时序与慢网提示 / FR-90 资产尺寸字体收口 / **FR-91 超时兜底迟到纠正**），闭合设计侧 `.decision-log.md` 的 L-1「PRD 无 splash 章节」缺口。含 9 条既有缺陷汇总表 | 新增一个 Epic 的工作量；两项需真机复核（OQ-20 / OQ-21） |
| 2 | **⚠️ FR-78 落地矩阵变更：状态 A · 未建档 由 Diary 改为 Discovery**。收敛口径为「只有真正建了档案的人才落 Diary，游客是唯一例外」 | **推翻 Story 2-4 已交付实现**：`user_state.dart:45` 须由 `'/profile'` 改 `'/home'`，`test/shared/diary_gating_and_landing_test.dart:247` 断言须同改。**2-4 仍在 `review`，code-review 时须按新矩阵核对，勿照旧实现放行** |
| 3 | FR-81 建档引导态**页面不删不改，但不再是落地页**，降级为需主动点 Diary Tab 才看到 | Story 2-3 交付物不受影响、无需返工；建档转化率可能下降，见 OQ-24 |
| 4 | 设计侧决策日志三处过期状态已订正：**L-3 六出口已由 Story 2-4 完成**（不再是待办）；D-2（`/me` 超时 3s→5s）与 C-4（`/me` 与动效并行）**均尚未实现**，仍是真实工作量 | 拆 story 时勿把 L-3 重复排入 |
| 5 | FR-0H 提示条的**废止论证前提已被推翻**（原论证依赖「A·未建档不会去 Discovery」，现已落 Discovery）。**废止决定维持**（Story 2-3 已整条下线，含源码级断言），但记为 OQ-24 的首选补偿手段 | 不返工；若转化率下降则作为新版本需求重新立项 |

## 3. 开工前必须闭合的开放问题

| OQ | 事项 | 阻塞什么 |
|---|---|---|
| **OQ-1** | FR-80 示例时间线素材。**2026-08-03 大幅收窄**：印尼语文案**已在 UI 稿 A1 中给出**，无需另产；配图从「9 张」降为 **3 张时间线照片（#3 玩水 / #7 深夜 / #8 晒太阳）+ 1 张宠物头像**——其余 6 条是 banner / 胶囊 / 证件卡，靠图标与版式渲染，**不需要配图**。示例宠物为**猫 Mochi**（非 PRD 初稿的金毛 Milo，以 A1 为准） | **不阻塞开发**（story 用占位图实现）；素材到位后仅替换 `assets/` 资源 |
| OQ-13 | FR-78A Tab 图标萌化装饰：替换还是叠加 pop-art 激活态 | **实质已由视觉稿回答，不再阻塞**：UI 稿 **T3 屏给出「方案A」** —— glyph 形状两态一致，激活时①转品牌紫 + 柔和圆角高亮底 ②叠加一处宠物特征（猫耳/爪印/尾巴/项圈）③≤150ms 轻弹跳。即**替换** pop-art 投影，与 PRD 假设 A-2 一致。需产品确认后回写 PRD OQ-13 与 `icon-system.md` |
| **OQ-19**（2026-07-31 新增） | 第 2 位 Tab 由 Konsultasi 改名 **Health** 后，**显示文案取英文 Health 还是印尼语 Kesehatan** —— App 其余 Tab 显示为印尼语（Discovery 显示 Jelajah）。另需确认：该 Tab 装的是 AI 分诊 + 兽医咨询，改名 Health 后语义变宽，而结构化健康记录实际在 Diary 内，是否会误导用户 | **不阻塞开发**，可先用 Health 占位；定案后走 i18n 文案替换 |
| ~~OQ-18~~ | 私密 Diary 是否仍出现在主动分享的名片 H5 | ✅ **已闭合（2026-08-03 产品拍板）：仍展示，H5 不过滤。** 已写入架构 AD-4 Rule 3 的三分法（作者自视不过滤 / 平台自动分发过滤 / 作者主动分享不过滤）与 Story 4-1 AC4 |
| OQ-11B | 月经图标改红后的具体色值（需与 coral `#F0425A` 拉开距离，避免健康记录列表页三个同色红） | **不阻塞**，可先用 coral 占位 |
| **OQ-20**（2026-08-04 新增） | Android 12+ 原生启动屏图标的**实际渲染尺寸**——系统决定、不可指定，必须真机量一次并把 Flutter 首帧 mark 对齐到该值，否则交接那一帧出现尺寸跳变 | **不阻塞开发**（先按屏宽 42% 实现）；**阻塞 FR-87 视觉验收**（L2，必须本地真机） |
| **OQ-21**（2026-08-04 新增） | Fraunces `WONK` 轴的字形手感，不同渲染引擎差异明显；建议 iOS + Android 各一台确认，过头可退到 `WONK` 0 | **不阻塞开发** |
| **OQ-22**（2026-08-04 新增） | 慢网提示印尼语文案 `Koneksi kamu lambat, tunggu sebentar…` 为设计侧占位，待母语同事审核 | **不阻塞开发**，出稿后仅替换 ARB |
| ~~OQ-23~~ | 慢网 B/C 落错页且不自愈 | ✅ **已闭合（2026-08-04 拍板）：由 FR-91 迟到纠正解决**，不新增重试屏 |
| **OQ-24**（2026-08-04 新增） | A·未建档改落 Discovery 后的**建档转化率变化**（第一屏从催建档变为内容流） | **不阻塞开发**；需埋点侧准备对比口径（`user_state=owner_without_profile` 的建档完成漏斗） |
| OQ-10 | 发布入口是否改为语境化（各 Tab 内发布 / [+] 语境感知） | **不阻塞**，本版本明确不改发布入口位置 |

## 4. 交给架构 delta（下一步）的关键约束

以下是 PRD 中已标 `[工程注意]` 或经比对识别出的、需要在 `architecture-v1.1.2-delta.md` 里拍死的点：

1. **时间线多源合并分页**（FR-82）——数据源从 2 个增至 4 个（Diary 内容 / 结构化健康记录 / 问诊存档 / 里程碑完成 / 身份证生成）。PRD 已给口径：统一游标锚点（事件日期 + 同日排序键）、同日条目不可跨页拆分、归并后再截断。需落成具体方案。
2. **时间线条目分类必须查询时实时计算，严禁落库固化**（FR-86）——否则用户「问诊时跳过存档 → 日后补存」会导致早先落库的类③ banner 不消失，同一件事在时间线出现两条。
3. **内容表新增「公开/私密」字段 + 一次性迁移**（FR-83）——字段 `DEFAULT 公开`，存量全部回填公开；私密只在用户主动关开关时产生。迁移必须在功能开关生效前完成。
4. **日历格子数据结构新增结构化健康记录维度**（FR-84）——`CalendarDayCell` 需补「是否有 / 类型 / 条数」，后端 `petProfileCalendar` 接口补字段。PRD 已核实「当天是否有 Diary 内容」维度**已存在但未被使用**，可直接复用。
5. **Diary 主页解除登录门控时不得连带放行子页面**（FR-78 / 假设 A-7）——现有受控清单按**路径前缀匹配**，解除 Diary 主页会连带解除建档 / 编辑档案 / 当天详情 / 里程碑列表。需改为精确匹配或显式子页白名单。
6. **Tab 索引重排的连带风险**（FR-78）——PRD 明确只约束视觉顺序，是否重排底层索引由工程按成本决定；若重排，需同步核对所有以索引寻址的调用点（登录后回跳目标、推送深链、埋点 Tab 标识）。
7. **埋点是 P0 工程项，不可后置**（§3.1）——底部 Tab 切换目前完全无埋点（走 shell 分支切换、不经路由观察器），不修则本次改版效果无法度量。拆 story 时应**独立成条**，不要散进各功能 story。仓库已有 `implementation-artifacts/spec-posthog-analytics-integration.md`，T-1~T-12 应挂在该链路上。

## 5. 数据库迁移编号

仓库现有 Flyway 迁移脚本最高到 **V97**。V1.1.2 新增迁移从 **V98** 起单调顺延（占位 `V<n>__`，实际号按执行顺序分配，勿照搬示例号——决策 E2）。

## 6. 版本上下文

- V1.0.0：46 story 全部完成（`implementation-artifacts/sprint-status.yaml`）
- V1.1.0：9 Epic 全部完成，Epic 9 收官（`implementation-artifacts/sprint-status-v1.1.yaml`）
- **V1.1.2：本目录，规划中**。零运营后台依赖，App 端 + 服务端即可独立交付
- V1.1.4：内容运营/增长能力（FR-68/70/71/73/74/75/76/85 + 名片 H5 改版），依赖运营后台，排在本版本之后。**本版本不依赖 V1.1.4 的任何 FR**（依赖方向自检见 PRD §6.2）

## 7. 下游产物命名（沿用 V1.1.0 的 delta 模式，并存不覆盖）

- `planning-artifacts/architecture-v1.1.2-delta.md`
- `planning-artifacts/epics-v1.1.2.md`
- `implementation-artifacts/sprint-status-v1.1.2.yaml`
