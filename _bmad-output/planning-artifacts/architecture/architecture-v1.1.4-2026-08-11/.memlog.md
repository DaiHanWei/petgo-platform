---
scope: V1.1.4 增量：内容顶置 FR-68 / 手机号采集 FR-70 / 图片规范化 FR-71 / 单条内容分享卡 FR-73 / 用户标签 FR-74 / 内容装饰标签 FR-75 / 里程碑 S·M 通知 FR-76 / 多触发点推送权限 FR-85 / 宠物主页对外分享 FR-92 / Feed 通栏化与首页互动 FR-93 + 埋点 E-1~E-27 + 运营后台 10 模块
purpose: build-substrate（brownfield 增量 delta，不改变系统形态）
altitude: feature（本 spine 之下保持 epic 一致）
updated: 2026-08-11T17:10
---

- (event) run 开始（新建）。brownfield 增量 delta；基线 architecture.md + architecture-v1.1-delta.md + architecture-v1.1.2-delta.md 只读不改；沿用 AD-n 体例，本 delta 内从 AD-1 起重新编号（与 v1.1.2 delta 同做法）
- (constraint) [继承·不重述] Spring Boot 4 / Java 21 / PostgreSQL + Redis + Flyway；模块化单体+分层；异步只用 @Async + DB 状态机、@Scheduled 定时；禁 MQ / 禁调度中间件 / 禁通用缓存层；DB snake_case ↔ Java/Dart camelCase ↔ JSON camelCase；RFC 9457 ProblemDetail；对外标识不可枚举 token；env 注入凭证不入库；日志禁 PII/健康/令牌/签名URL；ddl-auto=validate schema 归 Flyway
- (constraint) 运维 envelope 零增量：不引新中间件、不改部署形态（德国单机）。后台与 App 同一个 Spring Boot 仓库（Thymeleaf 管理页，现有约 30 页）
- (version) Flyway 基线 V99（代码实读 2026-08-11，migration 目录最大号）→ 本版本新增从 V100 起单调顺延
- (constraint) D-1 已定：宠物性别补成真字段。现状核实——档案编辑页 KELAMIN 选择器是纯前端占位（代码注释明写不提交不持久化），真性别只存在于 IdCard（MALE/FEMALE/UNKNOWN，默认 UNKNOWN，非每宠都有）。PetProfile 现有字段：petType/name/breed/birthday/intro/avatarUrl/cardToken/ogImageUrl/serialId
- (constraint) D-2 已定：H5 里程碑区块按 UI 稿全做（完成数/总数+具名徽章+灰锁位+「+N」）。现成能力核实——MilestoneListResponse 已含 completedCount/totalCount + 分组 items（code/title/level/completed/completedAt）；实际总数猫31/狗31/通用16，UI 稿的 30 与 +23 是示意数字
- (constraint) D-3 已定：H5「最新动态」由已完成里程碑清单 completedAt 最新一条推出，不新增取数
- (constraint) D-4 已定：后端补一份印尼语里程碑标题专供 H5。缺口核实——后端 MilestoneCatalog.title 是中文；印尼语只在 App 前端 milestone_titles.dart，且该文件明写既定规则「显示文案一律在客户端按 locale 出，杜绝任何后端语言（中文）泄漏」，而 H5 是 Thymeleaf 服务端渲染、无客户端。App 侧继续用自己那份 en/id 表不搬不改；两处维护的同步责任须写入 delta
- (constraint) D-5 已定：宠物名片深链落点改为被分享宠物的访客只读视图。现状核实——深链 tailtopia://card/{cardToken}，V1.1.2 让游客落 diary_guest_page.dart（未登录用户的「示例成长本」种草页，零网络、内置 assets），不是被分享的那只宠物
- (constraint) D-6 已定：图片宽高现在就入库。落地形态——新增一个与 content_posts.image_urls（JSONB List<String>）并排的尺寸列、按下标对应，既有读取点（Feed首图/详情/时间线/名片/后台）一处不动，存量为空零迁移。推翻 FR-71「零数据模型变更」选型理由（PRD 已预留退路）。存量永远无尺寸，占位兜底仍需做
- (constraint) D-7 已定：新增 Feed 图片高度护栏。规则=图片区高度上限 = 可视高度 − 条目其余部分(约152pt) − 露出余量(建议56pt)，超出 cover 居中裁切；多图帖按首图锁定后再套护栏。削弱 FR-71「容差区间内不裁剪」承诺（降级为「在装得下的屏幕上不裁」）；小屏机 3:4 竖图约裁 24%，基线仍改善（现状固定4:3对3:4恒裁44%）。数字均按算术推，须实机复核后定死露出余量
- (constraint) FR-92 §② 安全攸关：健康记录与问诊记录必须在后端就不下发（不可仅前端不画）；问诊「次数」保留可见（次数≠记录）。宠物身份证入口访客态整块移除（其 onOpenIdCard 跳 /profile/id-card 即当前登录用户自己的卡）
- (constraint) Feed 取数三处扩张（代码实读）：FeedItemResponse 现有 likeCount，但只有 firstImageUrl（FR-71 轮播需整组图）、无 liked、无 commentCount。ContentDetailResponse 已有 commentCount+liked 可参照。须定死批量取数方式，禁止逐条查
- (constraint) FR-76 现状：NotificationType 仅 MILESTONE_NODE（L级，推送+通知中心），MilestoneNotifyListener 消费 MilestoneCompletedEvent。需新增 S/M 类型：进通知中心不发push、计入未读角标、点击落里程碑列表页、同一操作解锁多个写 N 条且各写明具体名称、存量不回填
- (constraint) FR-85 触发计数：用户侧 3 点（问诊后/建档后/通知中心）各一次按设备存储；触发点4永不唤起原生弹窗（护栏，埋点可告警校验）；兽医侧触发点5无标记位每次都判、不做硬拦截；与 FR-70 手机号软引导两套独立计数不可共用标记位；同时命中按「先推送权限后手机号」串行
- (constraint) H5 埋点从零接入：card.html 目前无任何埋点脚本。服务端上报复用既有 shared/analytics/PostHogAnalyticsClient，不引前端 SDK；E-25/E-26 必须拆两个事件（点击可靠 vs pagehide 时序推断尽力上报）；需匿名 distinct_id 串漏斗。现有 CTA 是三级降级链（Android 深链→market://→Play网页；iOS 深链→App Store），本身即已装/未装判据
- (constraint) FR-73 卡片生成基建须按供后续复用设计（首次实现方）；二维码内容=该条内容分享链接（非通用下载页）、留白≥4码元、导出分辨率下边长≥140px、纯文字模板必须白色底板。单条分享严格只看这一条，与宠物主页分享必须是不同链接类型+不同落地页，不可复用同一 deep link 目标
- (constraint) 后台耦合：AB-3J(2b) 图片宽高比检测与警告关联 FR-71——后台发种子内容无裁剪步骤，超比例图静默进库后被新渲染规则裁切而运营不自知，宜与图片规范化同期交付。后台 4 条必须同版本（AB-10A/10C/11A/12A），6 条可独立排期
- (event) 已载入 D-1~D-7 与 10 条跨 story 口径为既定约束，不再重新征询
- (direction) 待征询：工作模式（聚焦式共同决策 vs 先全 draft 带假设标记）+ 是否需要人话版伴生文档（V1.1.2 有 架构决策-人话版.md 先例）
- (direction) 用户选定：聚焦式共同决策（只就 10 处真分叉逐个征询）；交付物 = 架构 delta + 人话版伴生文档 + Epic 依赖关系图
- (decision) 访客只读取数形态：新建独立「访客视图」只读投影层，两出口（H5 Thymeleaf 直调 + App 端薄 HTTP 接口）共用。该层结构上不持有健康记录/问诊仓库——沿用 v1.1.2 AD-2「分类器不持有 Repository」的同一手法，使泄露在结构上不可能。过滤只存一处。用户 2026-08-11 拍板
- (decision) 定时生效/失效：三处（顶置 FR-68 / 用户标签 FR-74 / 装饰标签 FR-75）统一用查询时时间窗判定（生效中 = now ∈ [start,end)），不建扫描器、不落状态列，无状态漂移。与 v1.1.2 AD-2「实时计算严禁落库」同一思路。顶置的「内容被下架即结束」副作用走订阅下架事件、不靠扫描器。用户 2026-08-11 拍板
- (decision) 两类标签建模：两套独立表（用户标签配置+分配 / 内容装饰标签配置+分配）。理由——两类约束完全不同（装饰标签仅公开内容可打标且影响推荐权重×1.3；用户标签有同时展示 3 个上限），且多态外键在 PostgreSQL 无法建约束、完整性只能靠代码自律。用户 2026-08-11 拍板
- (decision) Feed 批量取数沿用既有模式：FeedService.likeCounts() 已是「一页 ID 一次聚合查」（likes.countByPostIdIn → Map）。liked 与 commentCount 各照此加一次批量查，不加冗余计数列（那会偏离既有写法并引入维护成本）。代价每页多 2 次查询，判为可接受。架构裁定（据代码核实）
- (constraint) [代码实读] 分享卡生成的既有做法是客户端生成：id_card_detail_page.dart / ktp_card.dart / id_card_watermark.dart 用 RepaintBoundary+toImage 导出。服务端另有 OgImageService（1200×630）但用途是 OG 社交预览缩略图、非分享卡。→ FR-73 卡片生成基建应沿用客户端生成
- (constraint) [代码实读] AliyunOssClient 已在用 x-oss-process（EXIF 剥离样式）→ OSS 图片处理通道可用，服务端可用 image/info 不下载原图取宽高
- (question) 🔴 冲突待裁决：用户选「顶置占算法槽位并去重」，但 FR-68 正文明写「该内容同时仍保留在其原有的时间线位置（顶置是额外展示，不是从原位置挪走）」，且 §3.2 E-3 埋点存在的唯一目的就是先观测重复频率再决定要不要去重，算法文档 §7 亦把该问题悬置甩回 FR-68。去重 = 提前推翻三处。待用户在三个方案中确认
- (decision) 顶置去重：采「只首屏让位」——被顶置的 content_id 仅从 Feed 第一页排除，后续页仍可出现。消除「一屏两条一模一样」这个最难看的情况，同时 FR-68「内容仍保留在原有位置」正文成立、无需回写；E-3 埋点语义改为「观测后续页的重复」仍有效。用户 2026-08-11 拍板（原选彻底去重，经三处冲突说明后改选）
- (decision) 未登录用户在 App 内点名片深链：也落被分享宠物的访客只读视图（访客只读投影对未登录开放）。理由——同一链接在浏览器无登录即可看完整 Diary，App 内反而要求登录自相矛盾且会把用户推回浏览器。用户 2026-08-11 拍板
- (decision) 图片宽高数据来源：客户端上报为主（判容差区间时尺寸已在手），缺失或明显不合理时服务端问一次 OSS image/info 补齐；后台发种子内容无客户端，一律走 OSS 那条路。两个写入端都保证有尺寸。用户 2026-08-11 拍板
- (decision) 印尼语里程碑标题存放：Java 编译期常量表，与现有 MilestoneCatalog（中文标题）放同一文件并列。理由——改名字时两种语言在同一屏内，最不容易漏改；不引 Thymeleaf 资源文件（与目录分居两处）、不建库表（本版本无运营自改需求）。用户 2026-08-11 拍板
- (decision) 访客视图内条目的可点性：公开内容可点开进既有内容详情页（那条内容本就公开、零新增暴露面）；作者关闭同步的私密 Diary 不可点，点击给提示「该内容作者未公开发布，暂不可阅读」。私密内容因此始终不越出 token 边界。用户 2026-08-11 拍板（在「只公开可点」基础上自行补了提示文案）
- (constraint) 上条提示文案须出 id/en 两版 ARB，用户给的中文仅为意图表述，不可直接落地
- (decision) FR-85 存量标记迁移：三个触发点一律从「未触发」开始，旧的单一 pushPermissionAsked 键不迁移、不删除、新模型不读它。理由——FR-85 的目的正是唤醒已拒绝的存量用户；且原生弹窗已被系统消费，他们看到的都是「去系统设置」引导、无弹窗骚扰。用户 2026-08-11 拍板
- (decision) [架构裁定] FR-85 标记位载体：走 AppPrefs（SharedPreferences，非敏感偏好，符合既有约定「令牌走 secure_storage、偏好走 prefs」）。三个触发点各一个独立键 + 手机号软引导单独一个键，四键物理隔离，禁止任何两者共用
- (decision) [架构裁定] 图片尺寸列的 JSON 形状定死：与 image_urls 同序等长的对象数组 [{w,h},…]，缺失位用 null 占位以保持下标对齐。等长同序是硬约束——错位即图文不符
- (decision) [架构裁定] 顶置配置归属 content 模块内部（与 Feed 同模块），不新建跨模块依赖；后台管理沿用既有 admin→content 方向。Feed 第一页排除生效中顶置 content_id 属同模块内调用
- (decision) [架构裁定] 标签在 4 处（Feed/详情/评论/迷你主页）与 3 处（Feed/详情/Diary 时间线）展示位的取数一律批量，与 AD「Feed 批量取数」同属一条规则族；评论列表尤其禁止逐条查
- (decision) [架构裁定] 访客视图的失效口径与既有 H5 gone 页一致：档案不存在与账号已注销返回同一失效响应，不区分（防枚举）。App 端薄接口同口径，不得用不同状态码泄露存在性
- (decision) [架构裁定] 埋点 identity：H5 段用匿名持久化 id 串漏斗，App 段用既有用户标识，两段不做跨 identity 合并（服务端拿不到访客账号身份，PRD 已声明不承诺 viewer_state）
- (decision) [架构裁定] 访客态接入 V1.1.2 AD-15「Diary 页根节点单一判定入口」，作为新增的一个状态分支，复用作者态页面结构做减法，不另画页面（PRD 已明令）
- (event) 10 处架构分叉全部落定，进入 distill
- (event) lint_spine 通过（0 findings）。评审关卡两镜头改为父级 inline 执行（本会话禁用子代理）
- (decision) [评审镜头一修正] AD-5 原写「服务端问一次 OSS image/info」——绑了未实地验证的第三方接口细节，架构不应绑。改为「服务端兜底取宽高」，机制（读图头字节 / OSS 图片信息接口）降为实现选择、移入 Deferred
- (question) [对抗性发现1] 访客统计条三列若由访客投影自行重算，会与作者态 archive-stats 算法分叉 → 同一只宠物两个数字。须规定复用唯一统计实现
- (question) [对抗性发现2] 访客时间线是否含已下架/已删除内容未定。作者态可能显示自己的下架内容；访客态若照搬即暴露且可点。须规定访客投影只含已发布未删除
- (question) [对抗性发现3] Feed 第一页排除顶置 content_id 后页大小抖动（返回 19 而非 20），可能影响 hasMore 判断。须规定排除后补足页大小
- (question) [对抗性发现4] AD-9 不落状态列 → 后台列表要自己算「待生效/生效中/已结束」，与 App 取数各算一次，边界口径（左闭右开）可能分叉。须规定唯一判定实现，四处共用
- (question) [对抗性发现5] 写入图片尺寸的责任方未定：App 发布与后台种子发布两个调用方可能各写一套。须规定经由发布服务单一入口
- (question) [对抗性发现6] clamp 与高度护栏在哪端施加未定。若服务端下发 clamp 后比例、客户端再 clamp 一次 → 双重裁切。护栏依赖屏幕尺寸只能在客户端。须规定服务端只下发原始宽高、两段规则一律客户端施加
- (question) [对抗性发现7] 访客视图若复用 /profile 路由，未登录用户会落 V1.1.2 的游客示例页（与 D-5 冲突）。须规定访客视图为带 token 的独立路由
- (event) 对抗性 7 洞已全部收口进 AD-1/2/3/5/6/8/9；技术核实 1 处修正（OSS 接口细节降为实现选择）
- (decision) [输入对账补漏] 三处安静要求原被 AD 结构漏掉，已补进 Consistency Conventions：① 评论入口跳转的「定位评论区」路由参数须两侧同名；② 点赞埋点 source 属性 Feed 与详情页两侧都必须传（只改一侧则 FR-93 效果评估失效）；③ H5 字体策略（接受 Google Fonts 依赖、保 display=swap、补 preconnect、照抄视觉稿 head、字体回退属已知代价不按 bug 处理）
- (decision) [伴生产物] 架构决策-人话版.md 已产出（17 条 AD 的去代码化讲解 + 三条主动接受的取舍 + 四个开放问题）
- (decision) [伴生产物] Epic 依赖关系图已并入 工作拆分预览.md §四（mermaid）。关键结论：E3 图片规范化与 E4 Feed 通栏化强耦合，改同一卡片组件且共用同一次 Feed 取数改造，建议合并为一个「Feed 卡片改造」块，否则同一处代码改两遍
- (assumption) Epic 依赖图中 E2→E10 一边基于 AD-15「两种分享必须不同链接类型+不同落地页」推出；若实现上先做 E10 亦可，只需在 E10 内先定死链接类型划分
- (event) spine finalized
- (decision) [用户质疑后修正] 原写「运维 envelope 零增量：不新增外部依赖」与人话版「这一版没有引入任何新技术」措辞过宽、且与工作拆分预览自相矛盾。修正为：服务端零增量（准确）；App 端唯一依赖增量是 FR-71 裁剪库（项目现无裁剪能力，需引库或自研，属实现选择已在 Deferred）
- (constraint) [代码实读补充] 分享卡（FR-73）四样底层能力均已在生产使用、无新依赖：qr_flutter ^4.1.0 已用于 PawCoin 充值页 / 兽医限时支付页 / 共享支付二维码弹层三处；RepaintBoundary+toImage 截图导出、水印、share_plus+gal 系统分享与存相册均已用于宠物身份证。且身份证导出已在用「按目标画布宽度反算 pixelRatio」——正是 AD-15 二维码「按导出分辨率算边长」所需同一手法，story 应照抄不另起
- (constraint) [代码实读补充] 身份证水印手法可参照：水印是 RepaintBoundary 的 Stack 兄弟节点，故预览与用户手动截屏恒带水印、HD 导出只截 boundary 子树不带水印（bug 20260728-383 的解法）
