---
scope: V1.1.4 社区管控增量：FR-58 用户账号举报（含举报即隐藏、重复举报留存、警告/封号两档处置）、FR-94 用户拉黑（单向屏蔽 + 影子评论 R1/R2 + 互动通知抑制 + 黑名单管理）、AB-3D 运营后台审核工单统一视图（三类工单同队列 + 优先级公式 + 批量处置 + 筛选检索）
purpose: build-substrate
altitude: feature
updated: 2026-08-15T14:38
---

- (constraint) 继承 V1.0/V1.1/V1.1.2 底线：Spring Boot 4 / Java 21 / PG+Redis+Flyway；模块化单体；异步只用 @Async + DB 状态机；禁 MQ / 禁通用缓存层；ddl-auto=validate schema 归 Flyway；RFC 9457 ProblemDetail；日志禁 PII。本版本不引入任何新中间件、部署形态不变（德国单机）。
- (version) Flyway 实测：本分支 hex/v1.1.4 已存在 V100__add_id_card_student_fields.sql（README 所记『本分支只到 V99』已过时）。本版本新增迁移从 V101 起单调顺延。
- (constraint) 代码实证闭合 OQ-A6：FeedService.loadFeed 取 PAGE_SIZE+1 行，全部过滤（含既有『排除本人已举报的帖』NOT EXISTS 子查询）都在 findFeed 的 SQL WHERE 内完成，不是取回后在 Java 侧过滤。故分页是『过滤后凑够 N』，拉黑高产种子号不会产生近乎空白的页。产品层无需兜底。
- (constraint) 代码实证收窄 OQ-A5 前半：CommentRepository 的 countVisibleForViewer / findTopLevel / findReplies / findRepliesForParents 四个方法全部已带 :viewerId 参数（内容审核 D-CM2 遗留）。R1 确为『加一个条件』。
- (constraint) 代码实证推翻 OQ-A5 后半的『迷你主页接口需从匿名可读改造为必须登录』：GET /api/v1/users/*/mini-profile 是 permitAll，但 oauth2ResourceServer 在 permitAll 端点上同样解析 Bearer token。既有 ContentFeedController（同为 permitAll 的 Feed 端点）已用 @AuthenticationPrincipal Jwt 解出可选 viewerId 并交给 findFeed 过滤。故迷你主页只需照抄该模式加可选 viewer，端点保持 permitAll、游客行为不变，不是接口形态改造。
- (constraint) 既有跨模块只读端口先例：admin/moderation/read/ViolationCountReader + ViolationCountReaderConfig（由 moderation 模块提供实现）。本版本的隐藏关系读取应沿用同一形态，不让 notify/content 直读对方仓储。
- (constraint) 既有 ReportService 先例：内容举报的『对举报者隐藏』无独立隐藏表，判据即『存在 (reporter,post) 举报记录』，零新增写；重复举报幂等吞掉（existsByPostIdAndReporterId 直接 return）。账号举报明确不得照抄该幂等做法（PRD 要求留存每次类型与说明）。
- (constraint) 命名陷阱实证：UserStatus 只有 ACTIVE / DEACTIVATED，其中 DEACTIVATED = 运营停用（封号，可重新激活）；用户自己注销走另一条路（User.deleted / AuthorView.deleted，迷你卡 MiniProfileResponse.deactivated() 表达的是注销）。两者在代码里几乎同名而 PRD 对二者要求相反，是本版本最高风险的做反点。
- (constraint) AB-3D 三类工单分散在三张既有表：content_reports（内容举报，moderation 模块）、manual_review_queue（账号标识字段异步审核，admin/moderation 模块）、以及本版本新增的账号举报表。统一视图必须跨模块聚合。
- (decision) AD-1 隐藏关系单表双来源：新建一张隐藏关系表，主动拉黑与举报隐藏各写一行，以 source 字段（BLOCK / REPORT）区分；唯一键 (holder, target, source) —— 幂等只在同源之间成立，先举报后拉黑照常新增 BLOCK 行。Binds: Feed 过滤 / 评论 R1 / 评论 R2 / 主页访问校验四处读取点，举报提交与拉黑提交两处写入点。Prevents: 四处过滤各自查两张表（拉黑表 + 举报工单表）导致热路径双 EXISTS，以及两处过滤对『隐藏』判据取不同口径。Rule: 除主页访问只认 source=BLOCK 外，其余五处过滤一律不区分 source；举报写工单与写隐藏行必须同一事务。用户 2026-08-15 拍板（备选：不建隐藏表、举报隐藏由工单表现查，与既有内容举报做法一致，因热路径双查被否）。
- (decision) AD-2 账号举报工单粒度 = 被举报账号：运营队列里一个被举报账号占一条工单，不是一对 (举报人,被举报人) 一条。Binds: AB-3D 列表形态、优先级公式的计算单位、批量处置的作用对象、处置动作（警告/封号）的落点。Prevents: 同一坏账号被 12 人举报产生 12 条待处理工单、以及『12 人 / 27 次』这两个指标无处安放。Rule: 工单唯一键 = 被举报账号；每次举报作为明细行追加（保留类型与「其他」补充说明，按时间倒序展示）；PRD『同一举报人对同一账号只占一条工单』由本粒度天然满足。用户 2026-08-15 拍板。
- (decision) AD-3 R2 影子评论的读法：不在 SQL 里 join ContentPost 取内容作者，改由 service 把已知的内容作者 id 作为参数传入查询（CommentQueryService.requireVisiblePost 已 findById(postId)，作者是现成的；replies 分支经 Comment.postId 反查一次）。Rule: R2 条件写成『NOT EXISTS 隐藏行 WHERE holder=:postAuthorId AND target=评论作者』，与 R1 的 holder=:viewerId 并列，两条件独立、任一命中即隐藏。架构裁定。
- (decision) AD-4 迷你主页端点保持 permitAll，加可选 viewer：照抄 ContentFeedController 的 @AuthenticationPrincipal Jwt → 可选 viewerId 模式，登录者带 token 时后端认得出并做主动拉黑校验（命中返回统一『你已拉黑该用户』态），游客行为完全不变。Prevents: 把端点改为 authenticated 后游客点头像直接 401，破坏 FR-26 既有的游客可读。架构裁定，闭合 OQ-A5 后半。
- (decision) AD-5 Feed 分页不做产品兜底，闭合 OQ-A6：拉黑过滤并入 findFeed 既有 WHERE（与『排除本人已举报的帖』同一层），DB 侧过滤后再取 PAGE_SIZE+1，页恒满。Rule: 任何『按查看者隐藏』的过滤一律下沉到 SQL WHERE，禁止取回后在 Java 侧 filter —— 后者才会产生近乎空白的页。架构裁定（据代码实证）。
- (decision) AD-6 互动通知抑制拦在 notify 模块入口：ContentNotifyListener 在 onContentLiked / onContentCommented 两处（覆盖赞、评论我的内容、回复我的评论三类）先经只读端口判『接收者是否隐藏了 actor』，命中则直接 return，不发送、不落通知记录、未读角标不 +1。Binds: 三类互动通知的产生。Prevents: 收到通知点进去什么都没有的穿帮（PRD 表列的三种，其中『B 回复我在别人帖子下的评论』跨 R1/R2 边界最易漏）；也防止把抑制逻辑写进 NotificationService.send 而误伤封号 / 审核结果等系统通知。Rule: 抑制只作用于 ContentNotifyListener 内的互动三类，ModerationNotifyListener 等系统通知监听器一律不接入该判断；抑制是『不发』不是『发了再隐藏』；解除拉黑不补发。用户 2026-08-15 拍板。
- (decision) AD-7 AB-3D 统一队列用读时聚合，不建索引表：后台列表以一条跨三张既有表（content_reports / manual_review_queue / 新账号举报表）的联合查询产出，在 DB 内排序分页；不新增统一工单索引表、不双写、存量数据零回填。Binds: AB-3D 列表 / 筛选 / 排序 / 批量处置的读路径。Prevents: 索引表与源表状态不同步（工单已处理而索引仍显示待处理）这一类长期一致性风险。Rule: 后台为运营内部页面、QPS 极低，禁止为其引入缓存或索引表；三类工单的状态仍由各自源表权威持有。用户 2026-08-15 拍板。
- (decision) AD-8 新建社区关系模块承载隐藏关系；账号举报归审核模块：隐藏关系表（拉黑 + 举报隐藏）单独成模块，对外只暴露一个只读端口（沿用 ViolationCountReader 的端口 + Config 形态），content / notify / auth 三侧一律经该端口读，禁止直读其仓储；账号举报工单与明细归既有审核模块，与内容举报作伴。Binds: 包结构、跨模块依赖方向。Prevents: 把『用户自助图清净』写进名为审核的目录，以及 notify/content 直连他人仓储破坏模块化单体边界。Rule: 写入方（举报提交、拉黑提交）经该模块的写服务；读取方只见只读端口。用户 2026-08-15 拍板。
- (decision) AD-9 帖子级举报隐藏不并入新表，两条过滤并存：findFeed 保留既有『排除本人已举报的帖』子查询原样不动，另加一条账号级隐藏条件。Binds: Feed 与内容详情的可见性判定。Prevents: 为求概念统一而改动已上线一年多的内容举报行为 + 存量数据迁移，与本版本『尽快上线』的目标冲突。Rule: 两条件语义不同（一条藏一条帖、一条藏一个人），并列 AND，任一命中即不可见；本版本明确不追求把两者合成一层，PRD §3.2 第 1 条那句『应合并为一次』的实现提示不采纳，理由留痕于此。用户 2026-08-15 拍板。
- (decision) AD-10 新建账号处置记录表，承载警告与封号：每次处置追加一行（被处置账号 / 处置类型 / 处置人 / 时间 / 关联工单），不复用为昵称头像违规设计的既有违规计数。Binds: AB-3D 工单里的『历史违规记录（同账号历史处置次数）』字段、警告可重复下发且不合并不去重的要求。Prevents: 把两种语义不同的计数挤进同一张表；也为 1.1.8 的限流曝光留下天然落点。Rule: 警告与封号一律先写处置记录再触发用户侧后果；警告累计任何次数都不自动升级、不自动置顶工单（升级完全人工）。用户 2026-08-15 拍板。
- (decision) AD-11 主页访问被拦返回专用错误类型：迷你主页与宠物主页两个端点在命中主动拉黑时，按既有 RFC 9457 ProblemDetail 返回一个可识别的 type（如 blocked-user），前端据 type 区分『已拉黑』与『网络失败』两个 Toast（UI 稿 A4 / A5）。Binds: 服务端主页访问校验的对外契约。Prevents: 用 200 + 标记字段的做法在拦截同时仍把昵称头像给出去；也防止前端把拉黑态误判成网络异常。Rule: 该校验只认 source=BLOCK，举报隐藏照常放行（否则 FR-58 的『已举报』状态与重复举报入口全部失效）。用户 2026-08-15 拍板。
- (decision) AD-12 账号举报专用理由枚举，禁复用 ReportReason：新建账号维度五类枚举（垃圾营销 / 仿冒他人 / 持续骚扰 / 发布违规内容 / 其他），落库 varchar + UPPER_SNAKE。Prevents: 研发套用现成的 report_sheet.dart 与内容维度 ReportReason，导致账号举报弹出『虚假信息 / 不当内容』这类对不上的选项。Rule: 『其他』必须带补充说明（上限 200 字），其余四类不提供说明框；每次举报的类型与说明均追加为明细行、不覆盖前次。架构裁定（据 PRD §3.1）。
- (decision) AD-13 评论可见性：R1 与 R2 两个条件并列写进同一批查询，任一命中即隐藏；回复串随父隐藏。R1 条件 holder=:viewerId，R2 条件 holder=:postAuthorId，两者独立 AND NOT EXISTS。回复串：父评论被隐藏时其整条回复串对该视角一并隐藏，不论回复者是谁；被 R2 影子的评论作者本人仍看得到自己那条及他人对他的回复。评论数：countVisibleForViewer 同步套用 R1+R2，故 R2 影子评论及其回复串不计入对外评论数与互动量；R1 隐藏的评论照常计入（R1 只是我不看）。Prevents: 显示 5 条评论却只数得出 4 条的穿帮；以及内容作者看到『回复了某条看不见的评论』的孤儿回复。架构裁定。
- (decision) AD-14 举报成功的『立即』兑现在当前屏，用前端乐观移除：举报成功、用户点关闭收起流程时，当前列表立即移除该作者的全部可见卡片（不是只移除某一条）；从内容详情页进入的先退回上一级列表再同样移除。沿用既有帖子级举报成功后乐观移除该卡片的做法。Rule: 移除必须静默，不弹任何『已不再向你展示 TA 的内容』之类提示（会泄露举报会隐藏内容，与 A-A27 取舍冲突）。架构裁定（据 PRD §3.1）。
- (decision) AD-15 迷你主页卡的三条既有行为改动一次说清：① 评论区（一级评论与二级回复）的头像/昵称接入 showMiniProfile，与 Feed / 详情页作者完全一致，不在评论行内另设「⋯」或滑动手势；② 拉取失败由既有的 catch 静默不弹改为弹网络失败 Toast——这是变更既有行为，须改原 catch 分支而非只加新分支；③ 已注销仍不弹卡（NFR-8，不变），且评论区分支须一并复核该判断。Prevents: 只在评论区骚扰、从不发帖的账号既举报不了也拉黑不了（本版本影子评论/R1/R2/通知抑制全为评论区骚扰设计，入口却不在评论区）。架构裁定（据 PRD §3.1 与 UI 稿 A6）。
- (decision) AD-16 批量处置逐条独立事务：单次上限 50 条、同类型才可批量（选中第一条后其余类型置灰）；每条一个独立事务，部分失败逐条回报失败项与原因、不整批回滚、不静默吞掉；批量封号须二次确认并在弹窗内逐条列出将被封的账号，批量警告不需要二次确认。Prevents: 一个事务里 20 条成功 18 条却整批回滚，运营无从判断哪条真失败。架构裁定（据 PRD §4.1）。
- (decision) AD-17 账号举报零自动预处置，且不得触碰既有帖子级的两条自动通道：账号举报一律人工，不设『达阈值自动挂起该账号内容』的任何通道；既有内容举报的两条自动预处置（违法违规原因单次触发、举报人数达阈值）保持不变、仅作用于内容举报、且不再参与统一队列的排序。排序统一由 AB-3D 的新公式（举报人数 + 高频举报人数，高频阈值累计≥5 次，单人贡献封顶 2 分）接管全部三类工单，分数实时计算不落库快照，同分按最早一次举报时间升序。Prevents: 组织化串通举报即可让一个人在平台上无人复核地消失；以及统一视图里两套互不可比的优先级标记并存。架构裁定（据 PRD §3.1 / §4.1，主动决定非遗漏）。
- (decision) AD-18 运维 envelope 零增量：不新增任何中间件、不新增外部依赖、不改部署形态（德国单机）；无缓存层（隐藏关系每次查库，走唯一索引，禁止为其引 Redis 或本地缓存）；无定时任务、无异步（拉黑与举报均为同步小写）。Prevents: 为『避免每次列表查询回表』而擅自引入缓存，违反架构 Enforcement。架构裁定。
- (decision) AD-19 埋点：拉黑/解除拉黑事件须区分产生来源（主动拉黑 / 举报自动产生）与操作入口（迷你主页 / 黑名单页 / 举报流程 / 评论区）；黑名单页的举报点击单独打点（来源 blocklist）；不对影子评论做单独埋点。Prevents: 隐藏关系总量被举报量灌大，看不出主动拉黑的真实使用情况。架构裁定（据 PRD §3.2）。
- (constraint) 安全攸关三处，写代码时勿埋违反点：① 隐藏过滤属安全规则层，只升不降不可绕过——任何新增的『展示他人内容』的位置默认套用，不做逐场景例外；② 主页访问校验必须服务端（推送深链 / 通知中心历史记录 / 分享链接都能绕过纯前端拦截）；③ 封号（UserStatus.DEACTIVATED，运营停用可恢复）与注销（用户自删，对外匿名化 NFR-8）在代码里近乎同名而 PRD 要求相反——黑名单里封号照常展示不做标记仍可被拉黑，注销则匿名态、去点击态、条目保留。
- (question) OQ-A4（继承，上线阻塞，非架构项）：封号通知印尼语措辞待法务确认，同批送审的还有警告通知、二次确认的『提交后无法撤销』、解除拉黑第二句、FR-51 改后的回告文案共四处新增文案。封号功能不得在其闭环前上线。责任人 PM + Legal。
- (question) UI 缺口（继承，实现时按框外说明处理）：D6 解除确认屏缺『对方也被举报过』的变体，UI 稿未单独出图，文案已在附录给出（Kamu pernah melaporkan akun ini — kontennya tetap tidak akan ditampilkan.）。
- (assumption) [ASSUMPTION] 高频举报阈值 5 无数据依据（A-A23，从产品给的一个示例反推），上线后按实际分布回调；本架构不为该阈值做配置化，硬编码常量即可（改一次发一版的成本可接受，配置化反而多一处运营可误改的入口）。
- (constraint) 跨版本待办（不阻塞本版本）：FR-94『凡展示他人内容的位置一律套用隐藏过滤』需 V1.1.6 的 FR-68 顶置坑位实现时接上，已记入 1.1.6 查漏补做清单 A-1。本版本上线时平台尚无运营顶置位，该条不触发。
- (decision) AD-20 单向屏蔽的边界（评审对账补入，PRD『明确不影响的』整节原先未落入架构）：被拉黑方只在评论一处客观受限（R2 影子），浏览 / 点赞 / 访问发起方主页 / 分享 H5 一概不动；点赞照常写入照常计数不做逐人扣减；曝光浏览点赞统计不因拉黑扣减；历史数据不回溯清理；不可举报自己不可拉黑自己；不豁免运营虚拟账号；不作用于兽医问诊；不设数量与频次上限；被拉黑次数不作风控依据、后台不展示。Prevents: 研发按业界常见 Block 语义做成双向隔断。
- (event) 评审发现（关键）：AD-6 原判据只有『接收者是否隐藏了 actor』，会漏掉第三方那一支——A 拉黑 B，B 在 A 的帖子下回复 C 的一级评论，该回复被 R2 对所有人隐藏，但 C 没拉黑任何人、照样收到『有人回复了你的评论』点进去看不到。PRD §3.2 第 6 条列的三种场景接收者全是拉黑发起方本人，第三方这一支是空白。已在 AD-6 补第二条判据（内容作者是否隐藏了 actor）。
- (event) 评审发现：PRD『本公式统一接管全部三类工单』对第三类不成立——账号标识字段审核工单没有举报人，公式恒为 0 分会永远沉底。已在 AD-17 补默认映射（既有 ReviewPriority P0→10 / P1→2 / P2→0，锚点取自 PRD 自己的两个例子）并列为 OQ-CA1 待产品确认，不阻塞。
- (event) 评审发现：PRD 生效范围第 4 条的『宠物主页』在后端不存在他人可访问端点（/api/v1/pet-profiles/** 全是 /me 口径；唯一对外的 /p/{cardToken} 分享页 PRD 已明确不做拉黑过滤）。本版本只改迷你主页一个端点，该半句归入 S1 通用规则的适用对象。AD-4 已订正。
- (event) 评审发现：AD-10 原未定义处置记录与既有停用链路的事务边界，会出现『用户登不上了但工单页显示从未处置过』。已补：同一事务。
- (question) OQ-CA1（本次新开，不阻塞）：账号标识字段审核工单在统一队列里的优先级分映射（架构默认 P0→10 / P1→2 / P2→0）需产品确认相对权重。责任人：产品。
- (event) Reviewer Gate：lint_spine.py 首轮 12 处 high（9 条 AD 缺 Binds/Prevents）已全部补齐并复跑通过。语义评审因用户要求不派子代理，由主上下文逐条自查完成——已如实记录该局限（缺独立上下文的交叉视角）。两条配置 reviewer 的判定：① 版本核实——本 delta 未绑定任何新技术，所有代码断言均在本次会话对仓库实证，非训练数据推断；② 对抗性——构造出四处『都遵守 AD 却仍会做歪』的路径，均已闭合（见上四条 event）。
- (event) spine finalized —— 产物：_bmad-output/planning-artifacts/architecture-v1.1.4-delta.md（AD-1~AD-20，status: final）；配套 v1.1.4/架构决策-人话版.md 与 v1.1.4/工作拆分预览.md；v1.1.4/README.md 已回写开工前状态。下一步 CE（bmad-create-epics-and-stories）。
