---
scope: TailTopia V1.1.2 增量架构 delta：Tab 顺序/落地页矩阵、Diary 游客态、时间线五类整合、Diary-Moment 同步开关、日历健康记录联动、健康里程碑自动路径补全、埋点 T-1~T-12
purpose: brownfield 增量 delta；基线 architecture.md(V1.0冻结)+architecture-v1.1-delta.md(V1.1完成)；产物 architecture-v1.1.2-delta.md；Flyway V98 起
altitude: feature-set / 单版本增量（下游为 epics-v1.1.2 与 story）
updated: 2026-08-03T14:53
---

- (constraint) 代码核实: 登录门控有两处而非一处。(1) app_router.dart:96 _controlledLocations={'/profile','/triage','/me','/consult','/notifications','/publish'} 按 loc==p||loc.startsWith(p+'/') 前缀匹配(第132行), 管深链; (2) app_shell.dart _onTabSelected 按 AppTab.home.index 判定唯一免门控 Tab, 走 requireLogin() 单一入口, 管 Tab 点击。PRD 假设 A-7 只提到前缀匹配那一处 —— 解除 Diary 门控需同时改两处。
- (constraint) 代码核实(既有缺陷, 非本版本引入): TimelineService.getTimeline 游标用 createdAt(Instant) 拉取, 但排序用 effectiveDate(快乐时刻取 eventDate/健康事件取 createdAt)。游标键与排序键不是同一个 —— 补记的旧日期日记(eventDate 旧/createdAt 新)在翻页时已可能丢失或重复。FR-82 增至4源会放大该缺陷。PRD §2.3「统一游标锚点」口径实际是在修一个现网 bug。
- (constraint) 代码核实: 日历后端比 PRD 认知的更完整。CalendarMonthResponse.DayCell 已有 (day, firstImageUrl, hasHappyMoment, hasHealthEvent, healthRecordType) 五维, 结构化健康记录已于 bug 20260722-352 并入。PRD 说「只有结构化健康记录这一维需要后端新增」不成立 —— 实际缺的只有『当天结构化记录是否多条』(FR-84 优先级4 多条时需通用医疗箱图标)。优先级判定注释明写「交前端」。
- (constraint) 代码核实: getDayDetail 当前只含快乐时刻+问诊健康事件, **不含结构化健康记录**, 且按 created_at 正序。FR-84「当天详情按大类排序 diary>问诊>健康记录」需先把第三类数据源补进去, 不只是改排序。
- (constraint) 代码核实: FR-86 落点极小。MilestoneAutoCompleteListener.onHealthRecordCreated 已有 switch: VACCINE->M3 / DEWORM->M4 / MENSTRUATION,NEUTER,CUSTOM->null, 加 NEUTER->M9 即可; M5 可订阅既有 ConsultClosedEvent(ConsultCloseService:229 已发布, 属 consult 模块=真人兽医, 天然不含 AI triage, 自动满足 OQ-17)。
- (constraint) 代码核实: ContentPost 实体无任何公开/私密字段(有 type/status/petId/eventDate/deletedAt 等), FR-83 需新增。PostHog 已接入: GoRouter observers 挂了 PosthogObserver() 自动上报 $screen。
- (constraint) 代码核实: OQ-10「语境感知发布」已部分存在。app_shell.dart _onAddPressed(bug 20260703-244): 在成长档案 Tab 点[+]且有档案 -> 预选 growthMoment, 其余 Tab 不预选。FR-83「发布页默认 Diary」与这段既有逻辑重叠, 需明确谁说了算。
- (direction) 工作方式=教练式(用户选定); 交付物=架构delta + 人话版说明 + 工作拆分预览。用户自述看不懂代码, 全程用产品语言提问。
- (decision) AD-1 时间线统一游标: 彻底修根因(用户选定, 非最小改动)。排序键与游标键统一为 (eventDate, 同日排序键) 复合锚点; 各源用同一锚点取数后归并、归并后再截断; 同日条目不可跨页拆分(该日整体成页, 允许超默认页大小)。Binds: 全部四类时间线数据源的取数与分页。Prevents: 补记旧日期日记在翻页时丢失或重复(V1.0 起的现网缺陷, 4源后放大)。要求配回归测试而非仅新功能测试。
- (decision) AD-2 五类分类由后端判定: 服务端按 PRD 五步优先级顺序计算, 每条时间线条目下发 itemType 标识(类①~⑤), 前端只按标识选样式。Binds: 时间线条目契约。Prevents: 分类规则在 PRD/前端两处分叉; 同时直接供给埋点 T-10 的 item_type 属性。约束: 必须查询时实时计算, 严禁落库固化(否则补存问诊后旧 banner 不消失 → 同一件事出现两条)。日历格子优先级维持交前端(现状), 不在本版本收回。
- (decision) AD-3 Tab 底层索引一并重排(用户选定, 架构原倾向不重排但用户拍板理顺)。Binds: Tab 枚举顺序/路由分支声明顺序/路径映射数组三处同步重排为 Diary,Konsultasi,[+],Discovery,Me。Prevents: 代码索引顺序与视觉顺序长期不一致造成的认知负担。风险缓解(硬性验收项): 已定位 5 处按索引寻址的调用点须逐个核对 —— app_shell.dart 免门控 Tab 判定(index==AppTab.home.index)、[+]预选 Diary 判定(currentIndex==AppTab.profile.index, bug 20260703-244)、_tabLocations 路径映射数组、BottomTabBar currentIndex 渲染、app_router StatefulShellRoute 分支声明顺序。
- (decision) AD-4 可见范围建为通用字段(用户选定): content_posts 新增 visibility 列(枚举 PUBLIC/PRIVATE, DEFAULT PUBLIC), 三类内容(Diary/Moment/Tips)统一携带, 不做 Diary 专属布尔开关。Binds: Feed 取数、话题聚合、V1.1.4 顶置/装饰标签/互动积分等一切消费公开内容的查询。Prevents: 每处消费点各写一遍「Moment/Tips全要+Diary看开关」的分支逻辑; 日后扩展可见性(仅好友可见等)需再次改表。存量迁移统一回填 PUBLIC。
- (decision) AD-5 埋点集中为一条收尾 story(用户选定, 覆盖架构原倾向的「基建前置+业务跟随」拆法): T-1~T-4/T-6~T-12 共 11 项(T-5 已删)在全部功能逻辑理清后统一实现。
- (decision) AD-6 不取改版前基线(用户选定): 埋点与改版同版本发布, 不为取基线单独发小版本。连带后果(须回写 PRD §3.3): 五项核心指标中「游客→注册总转化率(改版前后对比)」「FR-0B 曝光量变化」「B/C 用户留存(改版前后)」三项因无基线不可执行; 实际可用的只剩「转化路径构成(entry_source 占比)」与「Diary 主动转私密率」两个绝对值指标。PRD §3.3 称总转化率为「本次改版的唯一裁决指标」的表述随之失效, §3.4 处置原则中「若总转化率下降则调 CTA」的触发条件亦无从判定。
- (decision) AD-7 门控解除采用「默认受控 + 精确例外」(架构裁定, 安全默认不可反转): app_router 的 _controlledLocations 继续按前缀匹配保持默认拦截, 另设精确例外集 {'/profile'} 仅放行 Diary 主页本身; app_shell 的 Tab 点击门控从「index==AppTab.home.index」改为按 Tab 语义的免门控白名单 {Discovery, Diary}。Binds: 两处门控入口。Prevents: 反向做法(把子页逐个列进受控清单)会使日后新增成长档案子页忘登记即默认对游客敞开 —— 违反 CLAUDE.md「安全规则层只升不降不可绕过」。
- (decision) AD-8 落地 Tab 按当前用户状态实时计算, 不持久化「上次落在哪」。判定点在 Splash 完成回调(既有分流点: pending 深链 > 兽医直达工作台 > 其余), 扩展为 游客/A建档/A未建档->Diary, B/C->Discovery, 兽医->工作台。Binds: 冷启动落地目标、[+]登录后回跳目标(改回 Diary)、名片深链游客落点(改 Diary 游客态)。Prevents: 状态切换(B/C->A)后落地页不跟随。
- (decision) AD-9 日历后端仅补一处: DayCell 增加「当天结构化健康记录条数」(或是否多条)维度, 供 FR-84 优先级4 在单条时用类型图标、多条时用通用医疗箱图标。其余四维(day/firstImageUrl/hasHappyMoment/hasHealthEvent/healthRecordType)已存在。纯文字日记错显医院图标为纯前端缺陷 —— 后端 hasHappyMoment 已与有无图片解耦, 前端改为按该维度判定即可, 不需要后端改动。
- (decision) AD-10 当天详情(getDayDetail)需补第三个数据源: 现仅含快乐时刻+问诊健康事件, 结构化健康记录未纳入。FR-84「按大类排序 diary>问诊>健康记录, 类内按时间」须先补源再改排序, 不是单纯改排序。
- (decision) AD-11 FR-86 落点: (a) MilestoneAutoCompleteListener.onHealthRecordCreated 的类型映射增加 NEUTER->M9; (b) 新增订阅既有 ConsultClosedEvent -> M5(首次幂等)。M5 因 ConsultClosedEvent 只在 consult 模块(真人兽医)发布、AI triage 不经过, 天然满足 OQ-17「不含 AI 问诊」, 无需额外排除逻辑。打卡路径保留不动, 两条并存幂等。存量不回溯。
- (decision) AD-12 多里程碑同时解锁的庆祝合并: 纯前端, 复用现有 showMilestoneCelebration() 单条组件展示最高级别那一条, 不新增视觉设计; 同批其余里程碑由弹层底部既有「已解锁收藏」圆点带(含 +N 溢出)自然承载。通用规则, 不专属 S4/M5。
- (decision) AD-13 FR-80 示例时间线(Milo 9条)全部内置: 文案走现有双语文案体系, 9 张图作为 App 内置资源。Binds: 游客首屏零网络依赖。Prevents: 游客态门面在弱网下白屏/loading。代价: 换文案或换图需发版; 安装包体积增加。OQ-1 素材到位后按此路径替换。
- (decision) AD-14 Diary/Moment 为同一条内容的两处呈现(包含关系, 非副本), 继承 V1.0 FR-12 语义与 FR-36 删除联动规则, 本版本不新建关联表 —— 由 AD-4 的 visibility 单字段表达, 删除任一处即整条软删。同步状态发布时一次性确定, 不提供任何事后切换入口。
- (constraint) Flyway 基线: 仓库现有迁移最高 V97, 本版本从 V98 起单调顺延(占位号, 实际按执行顺序分配 —— 决策 E2)。本版本预期迁移: content_posts 新增 visibility 列(DEFAULT PUBLIC)+ 存量回填。迁移须在功能开关生效前完成, 不接受先上线再补回填。
- (constraint) 继承 V1.0/V1.1 全部底线不重述: Spring Boot 4/Java 21/PostgreSQL+Redis+Flyway; 模块化单体; 异步只用 @Async+DB状态机与 @Scheduled; 禁 MQ/禁调度中间件/禁通用缓存层; ddl-auto=validate; DB snake_case<->camelCase; RFC 9457 ProblemDetail; 对外标识不可枚举 token; env 注入凭证; 日志禁 PII/健康/令牌/签名URL。本版本不引入任何新中间件、不改变部署形态(德国单机)、不新增外部依赖 —— 运维envelope 完全继承, 无增量。
- (question) OQ-18 未闭合: 手动关私密的 Diary 是否仍出现在用户主动分享的名片 H5。架构影响: H5 取数是否需过滤 visibility。PRD 暂取「仍展示」=不过滤。开工前需产品定论。
- (question) OQ-13 未闭合: FR-78A 图标萌化替换 vs 叠加 pop-art 激活态。纯设计侧, 不影响架构, 但阻塞 FR-78A 的 story 交付。
- (question) 单日条目极多时「同日不跨页拆分」会返回超大页(AD-1)。是否需要单日硬上限保护及其取值 —— 本版本暂不设限, 记为待观察(真实数据下单日条目数极少超过个位数)。
- (assumption) [ASSUMPTION] Tab 切换目前不产生页面浏览事件, 因 goBranch 在 StatefulShellRoute.indexedStack 内切换分支、不经过 PosthogObserver 的路由观察。PRD §3.1 如此描述, 代码结构与之相符(PosthogObserver 已挂在 GoRouter observers 上, 自动上报 $screen), 但未实机验证。埋点 story 实施时须先实测确认。
- (event) coaching 完成, 全部载重决策已由用户拍板, 进入 distill
- (event) lint_spine 通过(0 findings)。对抗性复核由主循环自执行(用户设置禁止派生子代理)。
- (decision) AD-4 Rule 3 补入(对抗性复核发现的洞): visibility 过滤范围严格限定「他人可见」视图, 作者自视视图(成长档案时间线/日历/当天详情/我的发布)一律不过滤。若某 story 为统一口径把 visibility=PUBLIC 加进时间线查询, 用户主动设私密的日记会从自己的成长档案消失 —— 摧毁 FR-83 私密能力的全部意义。两条 story 各自遵守原 Rule 2 即可造成此后果, 故须显式约束。
- (decision) AD-15 新增(对抗性复核发现的洞): Diary 页根节点状态分支收敛为单一判定入口(游客/A未建档/A已建档/B·C 四分支互斥)。FR-80 与 FR-81 两条 story 会同时改同一页面根, 各自加分支会造成判定分散、互相覆盖或漏组合。含 V1.0.0 FR-0H 提示条整条废止。
- (decision) AD-16 新增(对抗性复核发现的洞): 时间线与日历的分类优先级方向相反且都正确(逐条分类 vs 整天取一个代表标记), 属刻意不对称。显式禁止后续开发者误判为缺陷而擅自「统一」。
- (version) 图标标识符已对本机 Flutter SDK(/Users/hexsfile/development/flutter) icons.dart 逐个核实存在: medical_services_outlined / edit_note_outlined / water_drop / water_drop_outlined / vaccines_outlined / medication_outlined / healing_outlined / description_outlined / local_hospital_outlined 全部有效。本版本未引入任何新技术/新依赖, 无需版本联网核实。
- (event) 输入回收检查: 9 条 FR 全部落到 AD 或映射表; UI 稿 A6/A7 空态与重试沿用既有 F13 模式已补入一致性约定。
- (event) spine finalized。产物: planning-artifacts/architecture-v1.1.2-delta.md (16 AD) + v1.1.2/架构决策-人话版.md + v1.1.2/工作拆分预览.md (8 Epic/15 story)
- (constraint) 订正(epics 阶段完整清点 UI 稿时发现): ui-v1.1.2.html 实为 18 屏 / 3 泳道 —— T1-T3(Tab顺序前后·Discovery第3页软登录·Tab激活萌化方案A) / A1-A9·A2b / P1-P4·P1b(发布页默认Diary·无档案默认Moment·手动关同步·选Moment·「我的」页私密与公开区分)。此前架构 delta 只清点了 A 组, 并据此误判「FR-78A 是唯一没有 UI 视觉稿的需求」——实际 T3 屏已给完整「方案A」规格(glyph 形状两态一致; 激活时转品牌紫+柔和圆角高亮底+叠加一处宠物特征+≤150ms 轻弹跳), 即替换 pop-art 投影, 与 PRD 假设 A-2 一致, OQ-13 实质已闭合。已回改 architecture-v1.1.2-delta.md / README / 人话版 / 工作拆分预览 四处。
- (decision) AD-11 Rule 4 反转(用户 2026-07-31 拍板, 推翻 PRD 2026-07-30「两条并存」): 健康类四条 M3/M4/M5/M9 取消打卡路径, 仅自动达成。前端里程碑列表页隐藏这四条的「去打卡/去发布」按钮+徽章弹层改说明文案; 后端 MilestoneCheckInService 须对健康类 code 显式拒绝(仅前端隐藏不合格); 打卡候选集合排除这四条; milestone-checkin-prompt-copy.md 健康类文案作废。非健康类打卡机制完全不变。存量已打卡完成不回溯。已接受后果: 不录健康记录且不看真人兽医的用户永远解锁不了这四条, L4 只能自动达成。架构原判「FR-86 是全版本最小一条」随之失效, 规模由极小上调为小/中。已回改: architecture delta(AD-11 + 决策日志 + FR映射表) / PRD 两份副本 8 处 / 人话版 / 工作拆分预览。
- (question) OQ-19(2026-07-31 用户拍板改名 + 架构提出的待定项): 第2位 Tab 由 Konsultasi 改名 Health。待定 (a) 显示文案取英文 Health 还是印尼语 Kesehatan —— 其余 Tab 显示为印尼语(Discovery 显示 Jelajah), 混语风险; (b) 该 Tab 装 AI 分诊+兽医咨询, 改名 Health 语义变宽, 而结构化健康记录实际在 Diary 内, 存在误导风险。不阻塞, 先用 Health 占位, 定案后走 i18n 替换。已回改 architecture delta AD-3 / epics / PRD 两份副本。
- (event) epics-v1.1.2.md 完成: 6 Epic / 15 story / 88 AC。第四步校验发现并修复 3 处 story 间冲突: (1) Story 1.1 AC3① 与 2.4 AC2 重复认领「免门控 Tab 判定」改动 -> 拆清所有权(1.1 只去索引化、集合仍{Discovery}; 2.4 才扩为{Discovery,Diary}); (2) Story 2.2 建渲染组件需要 itemType 词表, 但该词表由 3.2 定义 -> 改为 2.2 定义词表、3.2 必须采纳, 防两套取值; (3) Epic 5 内 5.1 后端拒绝护栏先于 5.2 前端隐藏会造成「按钮可见但点了报错」-> 护栏移入 5.2 与前端隐藏同批交付。另补 2.2 AC7 说明门控未解时的验收路径。
- (decision) 2026-08-03 用户拍板(story 审核阶段发现 PRD 与 UI 稿两处分叉): (1) FR-80 示例宠物以 UI 稿 A1 为准 = 猫 Mochi(Kucing·Domestik·1 tahun 2 bulan), PRD 初稿的「金毛 Milo」作废; (2) 9 条示例内容以 A1 为准, PRD FR-80 示例表标注作废仅供追溯(两者已分叉: A1 有 30 天陪伴、PRD 有日常遛弯); (3) 实测 A1 稿仅 3 条带 tl-happy-thumb(20 Mei 玩水类②/08 Mei 深夜类①/02 Mei 晒太阳类①), 其余 6 条为 banner×3+胶囊×2+证件卡×1, 靠图标与版式渲染无需配图 —— OQ-1 素材需求由「9 张配图」降为「3 张 + 1 张头像」, 且印尼语文案 A1 已给出无需另产, OQ-1 从「lead time 最长的阻塞项」降为不阻塞。已回改 story 2-2 / README / PRD 两份副本。
- (decision) 2026-08-03 用户新增需求 + 拍板: FR-80 游客态 3 条带图示例可无登录查看详情(零网络, 内置数据); 其余 6 条点击触发建档引导(不跳里程碑/健康记录/身份证页 —— 均受控页, 会在种草页中间弹登录框); 示例详情页点赞/评论/举报展示但点击触发建档引导。架构提示的两处路由陷阱已写入 story: (a) 示例详情不得挂 /profile/ 前缀(AD-7 前缀匹配会拦截, 游客点进去被弹回); (b) 不得复用 /content/:id(示例无后端 ID → 404)。首选页内推入不新增受控路由。连带: 建档引导变三入口, 埋点 T-4 增 source 属性(main_cta/timeline_item/detail_interaction)统一为一个事件, 避免转化率分母碎裂。已回改 story 2-2(AC8) / story 6-1(T-4) / epics / PRD 两份副本。
- (constraint) Story 2-4 随 2-2 AC8 修订(2026-08-03): 门控验收改为双向 —— 除锁定 /profile/* 子页必须被拦, 还须锁定示例详情对游客可达。新增 AC7 作为 Epic 2 收口验收(未登录设备走完整链路: 冷启动→Diary游客态→点带图条目→见详情→返回, 全程零登录框)。理由: 2.1~2.3 交付的页面在门控打开前对真实游客都不可达, 端到端只有到 2-4 才成立。新增护栏: 不得为放行示例详情而扩大 /profile/ 例外集(会连带放行真实子页, 属安全默认反转)。
- (decision) 2026-08-03 对抗性发现(过 story 3-3 时): 共用 tile 组件不得内置跳转行为, 点击回调必须由调用方注入。起因: 2-2 AC8 让游客态与真实态点击语义分叉 —— 游客态点 banner/胶囊/证件卡是弹建档引导, 真实态是跳里程碑列表/健康记录列表/身份证页; 照片卡则一个进示例详情、一个进真实内容详情。若按最自然写法把 context.push 写在组件里, 游客态只能另写一套 tile, NFR-7(共用渲染组件)当场破功。已回改 2-2 AC3+F2(组件签名须留 onItemTap/onBadgeTap)、3-3 新增 AC6+F4 改回调注入式、两侧护栏、epics。属典型「两条 story 各自都对、合起来造不出东西」。
- (event) 2026-08-03 全量共享件扫描完成。识别 12 处跨 story 共享件, 其中 5 处归属不清已修复: (A) Diary 页头组件(2-2定义/3-3复用, 原只写「与游客态一致」会导致两处各实现一遍); (B) visibility 的 App data DTO 镜像原写「随4.2或本Story二选一」归属含糊 -> 明确归 4-1 否则其 AC7 契约同步落空; (C) 「健康类四条」code 集合被 5-2/6-1/3-2 引用但无定义方 -> 5-1 新增 AC8 作为单一定义方; (D) 健康类型图标常量表 2-2 类④ 最早需要而 3-4 才写「抽公共表」-> 明确全项目只能一份、先落地者建、3-4 为权威落地方; (E) 用户状态六态判定 6-1 声明复用 2-4 但 2-4 未被告知 -> 2-4 补「抽为可复用函数」。另补 3-4 AC8(F13 明确覆盖日历与当天详情失败态, 原 story 只有时间线写了)。epics 新增「共享件归属总表」12 行集中呈现。最终: 15 story / 97 AC / 2774 行。
- (constraint) 2026-08-03 用户指出 + 代码核实 + 拍板: Diary 页头的身份证入口, UI 稿 A1/A3 画成带编号卡片「Kartu Identitas #00842」, 但现网实际是 38x38 纯图标按钮(Icons.badge_outlined, growth_archive_page.dart _idCardBtn, 点击跳 /profile/id-card), 无标题无编号。健康记录入口现网确为实心卡(与稿一致)。拍板: 保持现网形态, UI 稿该处作废不实现。理由: (a) 现网无此形态; (b) 身份证编号可空 —— id_card.dart 的 serialId 与 cardNo 均为 nullable, 注释「旧卡为 null」, 且 FR-49 规定老档案需点「立即申请」才分配流水号, 照稿实现会多出「没有编号时显示什么」的未定义状态。已回改 story 2-2/3-3 与 epics 中「双入口卡」的不准确表述。
