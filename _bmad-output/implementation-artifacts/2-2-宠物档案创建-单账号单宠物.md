---
baseline_commit: f47d6e27d0f3ed271be4959e1b279315facb971b
---

# Story 2.2: 宠物档案创建（单账号单宠物）

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a **有宠用户**,
I want **为我的宠物创建一张专属档案**,
so that **我拥有承载宠物记忆的家，并获得可对外分享的名片**。

> 本 Story 在 Epic 2 中**首次建立 `pet_profiles` 表**，是 2.3（成长日历绑定宠物）、2.4（档案 Tab 时间线）、2.6（名片 H5）、2.7（分享 FAB）、2.8（编辑）的数据根。本 Story 只做"创建 + 单账号单宠物约束 + 生成不可枚举名片 token + 已有档案直达"，**不做时间线、不做名片页、不做编辑**。
>
> **依赖前序**：Story 1.1（脚手架/profile 模块空壳/ProblemDetail）、Story 1.3（JWT，创建需登录用户）、Story 1.6/1.7（状态 A 用户 + 创建引导入口，本 Story 承接其「立即创建」按钮跳到本表单）、Story 2.1（头像上传复用其压缩/直传/权限工具）。

## Acceptance Criteria

> **验证层标注**：**L0 静态**（编译/lint/MockMvc/widget 测试）· **L1 集成/运行时**（需 Docker daemon + postgres + redis）· **L2 端到端/外部凭证**（真实阿里 OSS/STS、真机相册）。
> 本 Story 核心落 **L0/L1**；头像直传依赖 2.1 的能力，其真实 OSS 验证已在 2.1 的 L2 完成，本 Story 只需用 2.1 工具产出的 URL 落库（L1）。

### AC1 — 创建档案 + 单账号单宠物 + 生成名片 token

**Given** 状态 A 用户（含 Story 1.7 引导入口或档案 Tab 入口）
**When** 填写头像（相册/拍照）、**宠物类型（必选）**、名字、品种、生日、一句话介绍（≤30 字）并提交
**Then** 创建宠物档案成功（写 `pet_profiles` 表，本故事创建该表），V1 限制单账号单宠物（已存在档案时不允许再建）（FR-11）
**And** 创建后自动生成不可枚举的对外名片 token（供 FR-14 使用），不暴露顺序 id
**And** 宠物类型作为**必选结构化字段**落库 `pet_profiles.pet_type varchar not null`，枚举 `CAT`/`DOG`/`OTHER`（UPPER_SNAKE）；**创建后不可修改**（服务端硬拒任何 pet_type 变更 + 前端编辑态置灰只读）
> 🔄 **PRD V1.0.0 修订（F6 · 2026-06-08）：** 新增「宠物类型」必选结构化字段（`pet_type`，CAT/DOG/OTHER），创建后不可改；它决定 FR-42 里程碑清单（猫/狗 30、其他 15），但里程碑本体属 mini-epic（F2），本 Story 只落字段本身。
> 验证层：**L1**（Flyway 建表迁移成功 + 创建端点写库 + 重复创建被 409 拦 + token 列唯一非顺序 + `pet_type` 非空落库，需 postgres）。Bean Validation（名字 ≤20、介绍 ≤30、**pet_type 必填且枚举合法**、生日合法）与 token 不可枚举生成可在 **L0**（MockMvc/单测）验证。头像 URL 经 2.1 直传得到后随表单提交（**L0** 验 DTO 接收 URL；真实直传 **L2** 已在 2.1 覆盖）。

### AC2 — 已有档案直达，不重复创建

**Given** 已有档案的用户
**When** 再次进入创建入口
**Then** 直接进入该宠物档案，不出现重复创建表单
> 验证层：**L1**（已有档案时 `GET /pet-profiles/me` 返回既有档案；前端据此路由到档案 Tab 而非创建表单）+ **L0**（前端路由守卫/provider 分支 widget 测试）。

### AC3 — 档案字段必填规则 + 生日完整年月日 `[R2]`

> 🆕 **PRD V1.0.0 第二轮断档补齐（R2 · 2026-06-08）：** 明确档案字段的必填/选填边界与生日精度——生日必须完整年月日（不允许只月日），年份用于年龄计算（FR-40「X 岁」）与里程碑触发（FR-42）。

**Given** 状态 A 用户在创建表单填写档案字段
**When** 提交创建请求
**Then** 服务端按必填规则校验：**必填**=宠物类型（已 F6，`pet_type` 非空且枚举合法）/ 名字（`@NotBlank` 且 ≤20 字）/ **生日（完整年月日 `date`，`@NotNull`）**；**选填**=头像（缺省落默认占位图）/ 品种（自由文本纯展示，不做结构化校验）/ 一句话介绍（≤30 字）
**And** 生日**必须为完整年月日**——只传月日（无年份）或非法日期一律 422 字段级 ProblemDetail；年份用于后续年龄计算与里程碑触发，不可缺省
**And** 选填字段缺省时不阻塞提交：头像未传时前端展示默认占位 widget、服务端 `avatar_url` 可为 null（展示侧兜底占位）；品种/介绍为空正常落库
> 验证层：**L1**（后端 Bean Validation：`birthday @NotNull` 完整 `date` + `pet_type`/`name` 必填 + 缺生日/缺名字/缺类型各返 422 字段级，需起栈但校验本身可 MockMvc 在 **L0** 覆盖）+ **L0**（前端表单必填标记 + 日期选择器产出年月日完整 `date`、禁只月日 + 头像缺省渲染默认占位 widget + 品种/介绍选填可空 widget 测试）。

---

## Tasks / Subtasks

> 三段组织。本 Story **前后端均衡**：后端建表 + 创建/查询端点 + 单宠物约束 + token 生成；前端创建表单 + 头像上传（复用 2.1）+ 已有档案直达。建议：后端 → 前端 → 联调。

### 🟦 后端子任务（petgo-backend / `com.petgo.profile`）

- [x] **B1. Flyway `pet_profiles` 表 + pet_type 列** (AC: 1)
  - [x] R1 建表 `V3__init_profile.sql`；**F6 的 `pet_type` 列经新迁移 `V25__add_pet_type.sql` 追加**（V3 已提交/可能已应用，按 Flyway 不可改约束新增迁移：加列→回填 OTHER→SET NOT NULL→CHECK CAT/DOG/OTHER，决策 E2）。
  - [x] 约束/索引：`uq_pet_profiles_owner_id`、`uq_pet_profiles_card_token`、`fk_pet_profiles_owner`→users(id)（V3 已含）。
  - [x] **删除级联设计前置**：FK→users，注释标注注销级联（V3 已含）。
- [x] **B2. 实体 / DTO / Repository** (AC: 1, 2)
  - [x] `PetProfile`(@Entity) 加 `petType`（`@Enumerated(STRING)`，**无 setter**=创建后不可改）+ `PetType` 枚举（CAT/DOG/OTHER）；Repository 既有。
  - [x] `PetProfileCreateRequest` 加 `petType(@NotBlank)` + `birthday(@NotNull)`；`PetProfileResponse` 加 `petType`。C4/C5 四处同步：后端 record + App DTO(`pet_profile.dart`) + `mock_backend.dart` + 契约 test。
- [x] **B3. 名片 token 生成（不可枚举）** (AC: 1) — R1 既有（`CardTokenGenerator` SecureRandom base62）。
- [x] **B4. 创建 + 查询服务/控制器 + 单宠物约束** (AC: 1, 2)
  - [x] `ProfileService.create` 既有 409 单宠物；**F6 增 `parsePetType`（必填+枚举合法，非法 422）+ 传入 `PetProfile.create`**。
  - [x] `ProfileApiController` POST/GET 既有；owner 取自 JWT、限流既有。

### 🟩 前端子任务（petgo_app / `lib/features/profile`）

- [x] **F1. 创建表单页** (AC: 1)
  - [x] 加**宠物类型选择器（ChoiceChip 猫/狗/其他，必选，key `petType_*`，映射 CAT/DOG/OTHER）**；名字≤20、生日 date picker、介绍≤30 既有；必填项 `*` 标记。
  - [x] 提交调 `ProfileRepository.create(petType,name,birthday 必填,...)`，带 `Idempotency-Key`；成功路由庆祝页（1.7）/档案 Tab。
- [x] **F2. data/domain 层** (AC: 1, 2)
  - [x] `profile_repository.create` 加 `petType`/`birthday` 必填参数；`pet_profile.dart` 加 `petType`（copyWith 不暴露=不可改）；`petProfileProvider` 既有。
- [x] **F3. 已有档案直达守卫** (AC: 2) — R1 既有（`petProfileProvider` 非空→跳 `/profile`）。
- [x] **F4. 错误/重复创建处理** (AC: 1, 2) — R1 既有（409→提示+直达；422 字段级提示）。

### 🟦🟩 第二轮断档补齐子任务 `[R2]`

- [x] **R2-B1. 后端必填/生日完整日期校验** (AC: 3)
  - [x] `PetProfileCreateRequest`：`birthday` 加 `@NotNull`（完整 `LocalDate`，框架反序列化拒只月日/非法日期）；`petType @NotBlank`（+ service `parsePetType` 枚举合法校验）、`name @NotBlank @Size(max=20)`、`intro @Size(max=30)` 齐全；`breed`/`avatarUrl` 不加必填约束（选填）。
  - [x] 缺生日/缺名字/缺类型/非法类型 → 422（service 解析失败 / Bean Validation 字段级，不外泄堆栈）；`ProfileServiceTest` 覆盖类型空/非法（L0），`ProfileApiControllerEndpointTest` 覆盖缺类型/缺生日/非法类型/选填缺省成功（**L1 待本地**）。
- [x] **R2-F1. 前端必填标记 + 日期选择器年月日 + 选填占位** (AC: 3)
  - [x] 表单必填项（类型/名字/生日）显式 `*` 必填标记；生日用 date picker 产出完整年月日 `date`（**不提供只月日模式**），未选生日 `_canSubmit` 为 false 不可提交。
  - [x] 头像未传时渲染默认占位图 widget（add_a_photo）；品种/介绍为空正常提交。widget 测试覆盖必填三项拦截 + 日期完整性 + 头像缺省占位。

### 🟨 联调验收子任务

- [ ] **J1. 创建闭环（L1 · 待本地）** (AC: 1) — 起栈→登录态→填表+头像→201→DB 查得记录（含 pet_type）、card_token 非顺序且唯一。端点测试已写（`ProfileApiControllerEndpointTest`），需 Docker pg+redis 真跑。
- [ ] **J2. 单宠物约束（L1 · 待本地）** (AC: 1) — 同用户再次 POST → 409；`GET /pet-profiles/me` 返回既有。
- [x] **J3. 已有档案直达（L0）** (AC: 2) — 守卫 provider 分支 widget 测试（既有）；L1 端到端待本地。
- [x] **J4. 校验（L0）** (AC: 1,3) — service 类型空/非法 22 → `ProfileServiceTest` 10/10；前端必填三项拦截 widget；名字>20/介绍>30/缺必填字段级 422 端点测试（L1 待本地）。`flutter analyze` 0 + `flutter test` 253。

---

## Dev Notes

### 架构约定

- **命名映射链**：表 `pet_profiles`(复数 snake_case)、列 `owner_id`/`card_token`/`created_at`(timestamptz UTC)；实体 `PetProfile`、DTO `PetProfileCreateRequest/Response`(record)、provider `petProfileProvider`。JSON camelCase、时间 ISO-8601 UTC、null 省略。
- **对外标识**：`card_token` 不可枚举（SecureRandom，≥128bit），**绝不暴露顺序 id 于对外名片路径**（2.6 用）。已授权资源响应可带数字 `id`。
- **错误**：重复创建 409、校验 422、未登录 401、ProblemDetail 统一信封。
- **头像媒体**：复用 Story 2.1——公开桶（头像对外可见于名片/档案）+ CDN URL；EXIF 已在 2.1 客户端剥离。

### 强制护栏

- **禁 MQ/缓存层/新中间件**；Redis 仅限流/幂等。
- `ddl-auto=validate`——schema 只由本 Story 的 Flyway 脚本管，**禁 Hibernate 自动建表**。
- 凭证 env 注入；owner_id 来自 JWT 不信任客户端。
- 删除级联路径设计前置（注销时连带 OSS 头像）。
- 日志不落 PII（名字/介绍属用户内容，按需 INFO 可记业务事件 id，**不记健康数据**——本表无健康字段）。

### 范围边界（不做）

- ❌ 不做时间线/记录展示（2.4）、名片 H5（2.6）、分享 FAB（2.7）、编辑（2.8）、多宠物。
- ❌ 不做 content_posts（2.3）。
- ✅ 只做：`pet_profiles` 表 + 创建/`/me` 查询端点 + 单宠物 409 + 不可枚举 token + 创建表单 + 已有档案直达 + 头像复用 2.1。

### Project Structure Notes

- 后端 `com.petgo.profile.{web/ProfileApiController, service/ProfileService, domain/PetProfile, repository/PetProfileRepository, dto/...}`；迁移 `resources/db/migration/V<n>__init_profile.sql`（与架构示例 `V2__init_profile.sql` 对应）。
- 前端 `lib/features/profile/{data,domain,presentation}`，头像复用 `lib/shared/utils`(2.1) 与 `core/media`。

### References

- [Source: architecture.md#Data Architecture] — 内部 bigint 主键 + 对外不可枚举 token；JSONB/弹性字段（本表无需 JSONB）。
- [Source: architecture.md#Naming Patterns] — 表/列/实体/DTO/provider 命名、Flyway 脚本名。
- [Source: architecture.md#Authentication & Security / PDP 基础] — 删除级联设计前置（FR-20）。
- [Source: architecture.md#Requirements to Structure Mapping] — FR-11/14~16 → `profile/` + `features/profile/`。
- [Source: epics.md#Story 2.2] — 两条原始 AC。
- [Source: epics.md#Story 1.7] — 状态 A 档案创建引导入口（本 Story 表单的上游）。

## Dev Agent Record

### Agent Model Used

云端 dev agent（R1，headless L0 绿）；claude-opus-4-8 (1M context)（F6 + R2 增量）。

### Debug Log References

- 后端：`./mvnw -B -Dtest='ProfileServiceTest,CardTokenGeneratorTest,ProfileApiControllerTest,MeServiceTest' test` 绿；`./mvnw -B -DskipTests package` 绿。
- 前端：`flutter analyze` → No issues；`flutter test` → 62 passed（含 profile/media）。

### Completion Notes List

**L0 状态（云端已验收）：**
- 后端：`pet_profiles` 表迁移（V3__init_profile）+ 创建/`/me` 端点 + 单宠物 409 + 不可枚举 card_token；单测覆盖创建、重复 409、token base62/无碰撞、owner 取自 JWT、getMe 404。编译/打包绿。
- 前端：创建表单（头像复用 2.1、名字≤20/介绍≤30 计数、生日 picker）+ ProfileRepository + petProfileProvider + 已有档案直达守卫 + 409→直达档案。analyze 零问题，widget/model 测试绿。
- 兑现 1.7 契约：MeService.hasPetProfile 改为 `petProfiles.existsByOwnerId`（接入真实档案）。

**Flyway 序号**：V3__init_profile（R1 建表）；V25__add_pet_type（F6 加列）。

#### F6 + R2 增量（pet_type 必选不可改 + 字段必填/生日完整 · 2026-06-09）

- **断档定位**：R1（云端 dev，早于 F6/R2 PRD 修订）建表与端点**不含 `pet_type`**，且 `birthday` 可空——F6 与 R2/AC3 均未落地。本轮补齐。
- **F6 pet_type（必选 + 创建后不可改）**：
  - 新 `PetType` 枚举（CAT/DOG/OTHER）；`PetProfile` 加 `petType`（`@Enumerated(STRING)`，**无 setter**，`create` 工厂注入）——「创建后不可改」由实体无 setter + `PetProfileUpdateRequest` 不含 petType **双重保证**（update 路径无任何变更入口）。
  - Flyway **V25**（不改已应用的 V3）：加列→回填 OTHER→SET NOT NULL→CHECK CAT/DOG/OTHER。
  - `PetProfileCreateRequest.petType @NotBlank` + service `parsePetType`（枚举合法，非法 422）；`PetProfileResponse` 回显 petType。
  - 前端：`PetStatusSelector` 风格的 ChoiceChip 三选一（必选）；`PetProfile`/repository/mock 四处同步 petType（C4/C5）。
- **R2/AC3 字段必填 + 生日完整年月日**：`birthday @NotNull`（完整 `LocalDate`，只月日/非法反序列化即拒）；必填=类型/名字/生日，选填=头像（缺省占位）/品种/介绍。前端 `_canSubmit` 三必填齐全方启用 + `*` 必填标记 + date picker 完整年月日。
- **L0 全绿**：后端 `ProfileServiceTest` 10/10（+类型空/非法）、`ProfileApiControllerTest` 5/5、`CardTokenGeneratorTest` 2/2 + `test-compile` 通过；前端 `flutter analyze` 0 + `flutter test` **253**（pet_profile_create +R2 必填/占位）。

**⚠️ 待本地验收（L1/L2）：**
- **L1（Docker postgres+redis）**：V25 迁移真跑；`ProfileApiControllerEndpointTest` 含 R2 用例（缺类型/缺生日/非法类型 → 422、选填缺省成功 201、petType 落库回显）；J1 创建闭环、J2 单宠物 409、J3 直达。
- **L2**：头像真实 OSS 直传已在 2.1 覆盖；档案创建表单（类型选择/计数/日期/提交）视觉真机走查。

### File List

**后端**：`db/migration/V3__init_profile.sql`、`profile/domain/PetProfile.java`、`profile/repository/PetProfileRepository.java`、`profile/dto/PetProfileCreateRequest.java`、`PetProfileResponse.java`、`profile/service/ProfileService.java`、`CardTokenGenerator.java`、`profile/web/ProfileApiController.java`、`shared/error/ErrorTypes.java`+`AppException.java`（profile-exists）、`auth/service/MeService.java`（接 hasPetProfile）；测试 `ProfileServiceTest`、`CardTokenGeneratorTest`、`ProfileApiControllerTest`、`MeServiceTest`（+mock）。

**前端**：`core/network/api_paths.dart`（+pet-profiles）、`features/profile/domain/pet_profile.dart`、`features/profile/data/profile_repository.dart`、`features/profile/presentation/pet_profile_create_page.dart`、`core/router/app_router.dart`（+/profile/create）、`features/profile/presentation/profile_onboarding_page.dart`（立即创建→表单）、`l10n/app_en.arb`+`app_id.arb`；测试 `test/profile/pet_profile_model_test.dart`、`pet_profile_create_test.dart`。

#### F6 + R2 File List（本轮）

**后端 — 新增**：`profile/domain/PetType.java`、`db/migration/V25__add_pet_type.sql`
**后端 — 修改**：`profile/domain/PetProfile.java`（+petType，无 setter）、`profile/dto/PetProfileCreateRequest.java`（+petType @NotBlank, birthday @NotNull）、`PetProfileResponse.java`（+petType）、`profile/service/ProfileService.java`（parsePetType）；测试 `ProfileServiceTest`、`ProfileApiControllerTest`、`ProfileApiControllerEndpointTest`（+R2 用例）、`CardPageControllerTest`、`HealthEventControllerEndpointTest`、`content/web/ContentFeedControllerEndpointTest`（create 签名同步）
**前端 — 修改**：`features/profile/domain/pet_profile.dart`（+petType）、`data/profile_repository.dart`（create +petType/birthday 必填）、`presentation/pet_profile_create_page.dart`（类型选择器+必填标记）、`core/mock/mock_backend.dart`（petType 同步，仅该行）、`l10n/app_en.arb`+`app_id.arb`（petType 文案）；测试 `test/profile/pet_profile_create_test.dart`、`pet_profile_edit_test.dart`（fake repo 签名）

### Change Log

- 2026-06-09 · F6 + R2（AC1 改 + AC3）：宠物档案加必选不可改 `pet_type`（CAT/DOG/OTHER，Flyway V25 加列 + 实体无 setter + update DTO 不含=双重不可改）+ 字段必填规则（类型/名字/生日必填，生日完整年月日 @NotNull，头像/品种/介绍选填）。后端枚举/DTO/service/校验 + 前端选择器/必填标记/repository + 四处契约同步。L0 全绿（后端 ProfileServiceTest 10/10 等 + test-compile；前端 analyze 0 + test 253）。V25 迁移真跑 + 端点 422 用例 L1 待本地。
