# Story 2.8: 宠物档案编辑

Status: ready-for-dev

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a **宠物主**,
I want **随时修改宠物档案信息**,
so that **档案与名片始终保持准确最新**。

> 本 Story 收口 Epic 2：编辑头像/名字/品种/生日/介绍，两入口（档案 Tab 信息卡 + 「我的」Tab）复用同一编辑页，修改即时生效并联动名片 H5（触发 2.6 OG 图重渲染）。**前后端均衡**。
>
> **依赖前序**：2.1（头像上传）、2.2（pet_profiles + 创建端点/DTO 复用）、2.4（档案 Tab 信息卡编辑入口）、2.6（名片重渲染 service，编辑后调用）。「我的」Tab 入口由 Epic 7 提供，本 Story 提供编辑页 + 入口对接 contract。

## Acceptance Criteria

> **验证层标注**：**L0 静态** · **L1 集成/运行时**（Docker+postgres+redis）· **L2 端到端/外部凭证**（真实 OSS 头像、名片 H5 联动）。核心落 **L0/L1**；名片即时同步实时读库已在 2.6 覆盖（L1），头像直传真实 OSS 在 2.1 覆盖（L2）。

### AC1 — 两入口复用同一编辑页，改五字段

**Given** 已创建档案用户
**When** 从成长档案 Tab 信息卡右上角「编辑」或「我的」Tab「编辑宠物档案」入口进入
**Then** 两处入口复用同一编辑页，可改头像/名字（≤20字）/品种/生日/一句话介绍（≤30字）（FR-39）
**And** 修改即时生效，宠物名片 H5（FR-14）同步更新，V1 不限编辑次数
> 验证层：**L1**（`PATCH /pet-profiles/me` 更新写库 + 编辑后访问 `/p/{cardToken}` 即最新 + OG 图重渲染触发，需 postgres）+ **L0**（同一 `pet_profile_edit_page` 被两入口路由复用、名字 ≤20/介绍 ≤30 校验、表单预填既有值 — widget/MockMvc 测试）。

---

## Tasks / Subtasks

> 三段组织。后端编辑端点 + 名片重渲染联动；前端编辑页（复用创建页表单）+ 两入口接入。建议：后端 → 前端 → 联调。

### 🟦 后端子任务（petgo-backend / `com.petgo.profile`）

- [ ] **B1. 编辑端点** (AC: 1)
  - [ ] `ProfileApiController` `PATCH /api/v1/pet-profiles/me`（JWT），body `PetProfileUpdateRequest{avatarUrl?, name?(≤20), breed?, birthday?, intro?(≤30)}`（部分更新，record + 校验）。owner 取自 JWT，**仅能改自己档案**。
  - [ ] `ProfileService.update(ownerId, req)`：更新 `pet_profiles` 对应列 + `updated_at`；**cardToken 不变**（编辑不重生成 token）。不限次数。
- [ ] **B2. 名片联动重渲染** (AC: 1)
  - [ ] 更新成功后触发 **2.6 的 OG 静态图重渲染** service（头像/名字/品种变更需重渲染预览大图）；名片正文实时读库无需额外动作（2.6 每请求实时读）。重渲染异步 `@Async`（**不引入 MQ**）。
- [ ] **B3. 头像变更旧图处理** (AC: 1)
  - [ ] 头像更新为新公开桶 URL；旧头像对象按需清理（或留待注销级联，记录策略）。

### 🟩 前端子任务（petgo_app / `lib/features/profile`）

- [ ] **F1. 编辑页（复用创建表单）** (AC: 1)
  - [ ] `features/profile/presentation/pet_profile_edit_page.dart`：**复用 2.2 创建表单组件**，预填既有头像/名字/品种/生日/介绍；头像点击走 2.1 上传换图；名字 ≤20、介绍 ≤30 实时计数。
  - [ ] 提交调 `ProfileRepository.update(...)`（PATCH）；成功后刷新 `petProfileProvider` → 档案 Tab/信息卡即时更新。
- [ ] **F2. 两入口路由复用** (AC: 1)
  - [ ] 档案 Tab 信息卡右上角「编辑」（2.4 留入口）+「我的」Tab「编辑宠物档案」（Epic 7 入口，本 Story 对接 route）→ **go_router 指向同一** `pet_profile_edit_page`。二级页隐藏 Tab Bar。
- [ ] **F3. 校验/错误** (AC: 1)
  - [ ] 名字 >20 / 介绍 >30 → 422 字段级提示；未登录 401；i18n 双套，无写死串。

### 🟨 联调验收子任务

- [ ] **J1. 编辑写库（L1）** (AC: 1) — PATCH 改五字段→DB 更新、cardToken 不变、updated_at 刷新。
- [ ] **J2. 两入口复用（L0）** (AC: 1) — 档案 Tab 与「我的」两入口进同一编辑页、预填正确。
- [ ] **J3. 名片即时同步（L1）** (AC: 1) — 编辑后访问 `/p/{cardToken}` 即最新；OG 图重渲染触发。
- [ ] **J4. 头像换图（L2）** (AC: 1) — 真机换头像直传公开桶→档案与名片均更新。

---

## Dev Notes

### 架构约定

- **命名/复用**：复用 2.2 的 `PetProfile`/Repository；DTO `PetProfileUpdateRequest`(record，可空部分更新)；`PATCH` 语义（200 读/改成功）。owner 自 JWT。
- **名片联动**：FR-39 编辑联动——名片正文 2.6 实时读库自动最新；OG 预渲染图需主动重渲染（异步 `@Async`，无 MQ）。
- **token 不变**：编辑不重生成 cardToken（已分享链接保持有效）。
- **头像**：复用 2.1 公开桶 + EXIF 剥离。

### 强制护栏

- **禁 MQ/缓存层/新中间件**；重渲染用 `@Async`。
- `ddl-auto=validate`（本 Story 无新表，复用 2.2/2.6 表/列）。
- owner 自 JWT，仅改自己档案（越权 403/404）。
- 校验 422、ProblemDetail 统一。
- 日志记编辑事件 id 可，不落用户正文做检索；无健康数据。
- 删除级联/旧头像清理策略记录（与注销 FR-20 一致）。

### 范围边界（不做）

- ❌ 不做创建（2.2）、时间线（2.4）、名片 H5 渲染本体（2.6，仅触发其重渲染）、分享 FAB（2.7）。
- ❌ 不做「我的」Tab 本体（Epic 7，仅对接编辑入口 route）。
- ❌ 不做多宠物/删除档案（V1 范围；删除走注销级联 Epic 7）。
- ✅ 只做：`PATCH /pet-profiles/me` 编辑端点 + 名片 OG 重渲染联动 + 复用创建表单的编辑页 + 两入口同页复用 + 校验。

### Project Structure Notes

- 后端 `com.petgo.profile.{web/ProfileApiController(加 PATCH), service/ProfileService(update + 触发 2.6 重渲染), dto/PetProfileUpdateRequest}`。
- 前端 `lib/features/profile/presentation/pet_profile_edit_page.dart`（复用 2.2 创建表单组件）；go_router 两入口指同一页。

### References

- [Source: architecture.md#Naming Patterns / Format Patterns] — PATCH 语义、record DTO、字段命名、422 校验。
- [Source: architecture.md#Communication Patterns] — `@Async` 重渲染，无 MQ。
- [Source: architecture.md#Requirements to Structure Mapping] — FR-39 编辑联动 → profile + 名片 H5。
- [Source: architecture.md#Enforcement Guidelines] — owner 自 JWT、不可枚举 token 不变、删除级联设计。
- [Source: epics.md#Story 2.8] — 原始 AC（FR-39）。
- [Source: epics.md#Story 2.6] — 名片 H5 同步联动（OG 重渲染目标）。
- [Source: epics.md#Story 2.2] — 创建表单（编辑页复用来源）。

## Dev Agent Record

### Agent Model Used

{{agent_model_name_version}}

### Debug Log References

### Completion Notes List

- 记录旧头像对象清理策略（即时删 vs 注销级联删）。

### File List
