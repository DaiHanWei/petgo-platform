---
baseline_commit: d9353457
---

# Story 7.4: 落地分流收口——矩阵变更与超时兜底的迟到纠正

Status: in-progress

> **code-review 2026-08-04 收尾状态**：31 条待修项（含 5 条决策落地）**全部修完**，回归全绿
> —— `flutter analyze` 零问题 / `flutter test` **701** / 后端 `mvn test` **1515**（后端本轮零改动，
> 因改了共享的 `AuthController` 而复跑确认无牵连）。
> 仍是 `in-progress` 而非 `done`，卡在三类**需要人**的事上：
> ① **L2 真机验收**：Epic 7 大半是视觉/动效，加上本轮新增的两处真机待验
>    （Android 11 及以下的白闪是否真闭合、mark 交接是否还看得出跳变）；
> ② **AC5 的 opsz 12 已确认不可达**（决策 D5 接受该偏差，非缺陷）；
> ③ **3 条新待办**待你定（迟到纠正漏的那类弹层场景、字体子集补码位、开源许可页）—— 见
>    下方 Review Findings 末尾。

> **所属**：V1.1.2 **Epic 7 第四个（收尾）Story** —— **纯前端 · 零 schema · 零后端改动 · 零新依赖**。
> 交付两件事：① **FR-78 落地矩阵变更**（状态 A·未建档由 Diary 改落 Discovery）；② **FR-91 超时兜底的迟到纠正**（新增行为）。
>
> 🔴 **本 Story 含两条安全攸关约束**（见 AC4 / AC3⑦），**不可被无监管流程降级或跳过**。
> ⚠️ **依赖 Story 7.3**：迟到纠正建立在它确立的 5s 兜底路径之上。
> ⚠️ **与 Story 2-4 的关系**：矩阵那两行代码是 2-4 已交付实现的**返工**，**由本 Story 负责，2-4 不再动** —— 2-4 的 code-review 可按旧矩阵放行，不必等本 Story。

## Story

As a **用网络很慢的、还没建档的用户或计划养宠的用户**,
I want **就算开屏等超时了，最后也别把我丢在一个我用不了的页面上**,
so that **我不用自己去猜该点哪个标签（FR-78 / FR-91）**。

## Acceptance Criteria

### AC1 — 落地矩阵变更：A·未建档改落 Discovery（L0 · FR-78 变更 · 明确划归本 Story）

**Given** 2026-08-04 产品拍板，口径收敛为「**只有真正建了宠物档案的人才落 Diary，游客是唯一例外**」
**When** 实现完成
**Then** `petgo_app/lib/features/auth/domain/user_state.dart` 的 `landingLocation` 中 `AppUserState.ownerWithoutProfile` 由 `'/profile'` 改为 **`'/home'`**（现状在 `:45`）
**And** `petgo_app/test/shared/diary_gating_and_landing_test.dart` 的断言 `expect(AppUserState.ownerWithoutProfile.landingLocation, '/profile')` 同改为 `'/home'`（现状在 `:247`）
**And** 同文件的注释「状态 A 一律落 Diary：已建档看真实成长本，未建档看建档引导」（现状 `:43`）须**同步改写**，否则注释与代码矛盾
**And** ⚠️ **这两处是 Story 2-4 已交付实现的返工** —— 由本 Story 负责，2-4 不再动

### AC2 — 六态矩阵完整锁定（L0 · FR-78）

**Given** `AppUserState` 是落地分流与埋点 `user_state` 的**唯一判定源**（两处同源，避免看板与实际落点对不上）
**When** 测试锁定
**Then** 六态断言为：游客 → `'/profile'`（Diary 游客态）｜A·已建档 → `'/profile'`｜**A·未建档 → `'/home'`**｜B(planning) → `'/home'`｜C(enthusiast) → `'/home'`｜兽医 → `'/vet/workbench'`
**And** **不持久化「上次落在哪」**（FR-78 明确禁止；这也是慢网必须等 `/me`、进而产生 D-2「最坏 5s」的根本成因）
**And** 状态切换时点：改宠物状态那一刻**落地页不变**，须**建档完成**（转为 A·已建档）后落地 Tab 才变 Diary

### AC3 — 迟到纠正：七条硬约束逐条验收（L0/L1 · FR-91 核心）

**Given** `ensureRestored()` 的 `.timeout()` **只停止等待、不取消底层请求** —— 恢复完成后仍会写回 `AuthState` 并触发重渲染。**故超时兜底态是会变化的中间态，不是终态**
**And** 超时时用户被当作游客处理、一律落 Diary，且 Diary 渲染的是 **FR-80 游客种草页**（`resolveDiaryUserState` 的输入是 `isLoggedIn`，未恢复即视为未登录）
**When** 落地页由超时兜底产生、且会话恢复随后完成
**Then** 按真实状态**重判一次**落地目标，与当前位置不一致则跳转
**And** **① 仅当本次冷启动落地由超时兜底产生时启用** —— 正常路径（恢复在动效结束前完成）不涉及本机制
**And** **② 触发时机为恢复 future 完成**（成功或失败都算完成），**不是固定延时** —— 固定延时要么早于恢复（白跳）要么晚于恢复（用户已在操作）
**And** **③ 仅当用户仍停在兜底送去的那个落地页上才纠正**（详见 AC4，此条为安全攸关）
**And** **④ 只纠正一次**，此后本次冷启动内不再干预，防止与其它跳转逻辑互相拉扯
**And** **⑤ 本次冷启动若由深链唤起 → 不纠正**（与 AD-8 分流优先级 pending 深链 > 落地矩阵一致）
**And** **⑥ 恢复失败（确实是游客）→ 重判结果仍为 Diary → 不产生跳转**，真游客不应因本机制被打扰
**And** **⑦ 🔴 兽医角色隔离守卫（`app_router.dart` 的 `if (auth.isVet)` 分支）保留不动**，作为双保险 —— 那是**用户/兽医路由互斥的安全边界**，继承「安全规则只升不降不可绕过」，**不得以「统一由 FR-91 处理」为名弱化或替换它**

### AC4 — 🔴 安全攸关：不打扰已离开的用户，优先级高于纠正（L0 · NFR-15）

**Given** 「把正在浏览的用户拽走」比「落错页」严重得多
**When** 用户在纠正触发前已**自行导航离开**兜底落地页
**Then** **一律不纠正**
**And** 🔴 **本约束优先级高于「纠正到正确页」** —— 两者冲突时以本条为准。**这是本 Story 的安全攸关点之一，不可被降级或跳过**
**And** `[工程注意]` 判定「用户是否仍停在兜底落地页」须**记录兜底跳转时的目标路径并与当前路由比对**；**不得**使用「路由栈深度」之类的间接信号 —— 用户在同一页内打开弹层 / 详情再返回，栈深会变但人没走
**And** 须有测试锁定：兜底落地后用户主动切到别的 Tab / 打开详情 → 恢复完成 → **不发生任何跳转**

### AC5 — 纠正后终态逐类锁定（L0 · UX-DR31）

**Given** 超时兜底一律落 Diary 种草页
**When** 纠正完成
**Then** 逐类终态为：

| 用户 | 超时落 | 纠正后 | 用户感知 |
|---|---|---|---|
| 游客 | Diary 种草页 | 不跳 | 无感知 |
| A · 已建档 | Diary 种草页 | 不跳（位置已对） | 页面内容由种草页变为真实档案 |
| **A · 未建档** | Diary 种草页 | **跳 `/home`** | 一次跳变 |
| **B / C** | Diary 种草页 | **跳 `/home`** | 一次跳变，**终态正确** |
| 兽医 | Diary 种草页 | **跳 `/vet/workbench`** | 一次跳变（与现状一致） |

**And** 须有测试覆盖**主诉求路径**：「B/C 慢网超时 → 最终落 Discovery」，不停留在「有宠专属」功能拒绝页
**And** **过场表现**：视觉稿**未画此过渡**。产品已接受「一次可见跳变优于停在无效页」，本 Story 按「**无特殊过场、等同普通路由跳转**」实现；若后续 UX 认为需要过渡动画，另行提出，不在本 Story 范围

### AC6 — 埋点区分标记（L0 · 关联 PRD §3 T-1）

**Given** 超时兜底态与真实游客态在 T-1 里**都上报 `user_state=guest`**，混在一起则「兜底发生率」无法统计
**When** 上报 T-1 `app_launch_landed_on_tab`
**Then** 兜底落地与纠正后落地**各上报一次**
**And** 兜底那次带 `restore_timeout: true`；纠正那次带 `corrected_from: <原目标>`
**And** ✅ 该口径使「超时兜底实际发生率」成为**可观测指标**（PRD OQ-23 因此闭合，不再作为开放问题）
**And** 属性名 **snake_case**，**不引入任何新埋点 SDK 或中间件**
**And** ⚠️ 事件名与既有 T-1 一致，**不新造事件名** —— 否则 Story 6-1 已交付的看板口径会断

### AC7 — 弱网真机端到端（L2 · **必须本地**）

**Given** 本 Story 的主诉求场景只在慢网出现
**When** 用真机 + 弱网限速（使会话恢复超过 5s）冷启动
**Then** **B/C 账号**：先见 Diary 种草页 → 恢复完成 → **自动落到 Discovery**，不停留在「有宠专属」拒绝页
**And** **兽医账号**：最终落兽医工作台
**And** **A·未建档账号**：最终落 Discovery
**And** **快网回归**：正常网络下无任何多余跳转
**And** **AC4 回归**：兜底落地后立刻手动切 Tab → 恢复完成 → 不被拽走
**And** 云端 headless 无法执行本条，须在 Completion Notes 标注「L2 待本地验收」

### AC8 — Epic 7 收尾：回写架构（L0）

**Given** `architecture-v1.1.2-delta.md` 的 16 条 AD 产出于 2026-07-31，**早于 splash 设计**
**When** 本 Story 完成
**Then** **回写 AD-8** —— 其第 2 条矩阵描述「游客 / 状态 A 已建档 / 状态 A 未建档 → Diary」已过期（A·未建档已改 Discovery）
**And** **补一条 AD-17** 记录 FR-91 的架构口径：**兜底标记的承载位置**、「用户是否仍停在兜底落地页」的**判定方式**、以及 `onComplete` 与 redirect 的**协作边界**
**And** ⚠️ 不回写会导致下个版本有人照旧 AD-8 实现而**再次出错** —— 本条是 Epic 级收尾动作，不可省

### AC9 — 严守本 Story 边界（L0）

**Given** 视觉与时序已由 7.2 / 7.3 定稿
**When** 本 Story 实现
**Then** **不改视觉规格**（尺寸 / 字体 / 动效三拍）
**And** **不改等待反馈**（进度线 / 慢网提示的时点与样式）
**And** **不把 `.timeout()` 改成可取消** —— 本 Story 的迟到纠正**正是建立在「超时不取消请求、恢复晚到会写回状态」这一既有行为之上**
**And** `flutter analyze` 零警告、`flutter test` 全绿、`flutter build apk --debug` 通过

## Tasks / Subtasks

### 🟩 前端子任务（petgo_app / Flutter）

- [x] **T1 矩阵变更**（AC1 / AC2）
  - [x] `user_state.dart`：`ownerWithoutProfile => '/home'`（原 `'/profile'`，`:45`）
  - [x] 同文件 `:43` 注释同步改写
  - [x] `test/shared/diary_gating_and_landing_test.dart:247` 断言改 `'/home'`
  - [x] 补/核实六态完整断言（AC2 表）
- [x] **T2 兜底标记**（AC3①②）
  - [x] 在超时兜底跳转时记录「本次落地是兜底产物」+ **兜底跳转的目标路径**
  - [x] 正常路径不设该标记
- [x] **T3 迟到纠正触发**（AC3②④⑤⑥）
  - [x] 以**恢复 future 完成**为时机（非固定延时）
  - [x] 深链唤起 → 不纠正
  - [x] 恢复失败（真游客）→ 重判结果不变 → 不跳
  - [x] 只纠正一次（用一次性标志，纠正后清除）
- [x] **T4 🔴 安全攸关：位置比对**（AC3③ / AC4）
  - [x] **比对当前路由路径与 T2 记录的兜底目标路径**；不一致 → 一律不纠正（`router.routerDelegate.currentConfiguration.uri.path` vs `armed`）
  - [x] **禁用路由栈深度等间接信号**
  - [x] 补测试：兜底后手动切 Tab / 开详情 → 恢复完成 → 无跳转
- [x] **T5 🔴 安全攸关：兽医守卫原样保留**（AC3⑦）
  - [x] `app_router.dart` 的 `if (auth.isVet)` 分支**一行不动**
  - [x] 补测试锁定：非兽医不得进 `/vet/*`；兽医出现在用户侧路由被收口回工作台
- [x] **T6 埋点区分标记**（AC6）
  - [x] 兜底落地上报 T-1 带 `restore_timeout: true`
  - [x] 纠正落地上报 T-1 带 `corrected_from: <原目标>`
  - [x] 事件名沿用既有 `app_launch_landed_on_tab`，属性名 snake_case
- [x] **T7 测试**（AC2 / AC4 / AC5）
  - [x] 六态矩阵断言
  - [x] 主诉求路径：B/C 慢网超时 → 最终 Discovery
  - [x] AC4 反例：用户已离开 → 不纠正
  - [x] 快网回归：无多余跳转
- [x] **T8 L0 绿灯**（AC9）
  - [x] `flutter analyze` 零警告 / `flutter test` 全绿 / `flutter build apk --debug` 通过

### 🟨 联调验收子任务（**必须本地真机 + 弱网，云端无法执行**）

- [ ] **T9 B/C 弱网**：先见种草页 → 自动落 Discovery（AC7 主诉求）　⚠️ **未完成**：需真机 + 弱网限速。**判定逻辑已由单测锁定**（`resolveLateCorrection` 对 planning/enthusiast 均返回 correct 且目标为 `/home`）
- [ ] **T10 兽医弱网**：最终落工作台（AC7）　⚠️ **未完成**：同上。单测已锁：兽医重判目标为 `/vet/workbench`，与角色隔离守卫结果一致（互为双保险）
- [ ] **T11 A·未建档弱网**：最终落 Discovery（AC7）　⚠️ **未完成**：同上。单测已锁：`ownerWithoutProfile.landingLocation == /home` 且判定为 correct
- [ ] **T12 快网回归**：无多余跳转（AC7）　⚠️ **未完成**：需真机。**逻辑已锁**：正常路径不武装迟到纠正（约束①），故不可能产生多余跳转
- [ ] **T13 AC4 真机回归**：兜底后立刻切 Tab → 不被拽走（AC7 / AC4）　⚠️ **未完成**：需真机。**这条是安全红线的验收点**；单测已锁「用户已离开 → userMovedOn 不纠正」，含"即便本该纠正也必须放手"的反例

### 🟦 架构回写子任务（Epic 7 收尾）

- [x] **T14 回写 AD-8**：矩阵描述改为新口径（AC8）
- [x] **T15 补 AD-17**：记录 FR-91 的兜底标记承载位置、位置判定方式、`onComplete` 与 redirect 协作边界（AC8）

### 🟦 后端子任务

- [x] **无。本 Story 零后端改动、零 schema 变更、零迁移。**

### Review Findings（Epic 7 整体 · code-review 2026-08-04 第二轮）

> 靶子 `07c70b9b..HEAD`（11 提交 / 代码与工程文档 75 文件 +1564/-432）。三层并行（盲审 / 边界猎手 / AC 审计）+ 主审复核。
> **本块是 Epic 7（7-1~7-4）的统一 findings 台账**，7-1/7-2/7-3 各自文件只留指针。
> 排除本轮范围：Story 6.1 的 AC1 后台实测与 J1/J2（需真机 + 生产 key，用户明确延后）。
> L0 实测：`flutter analyze` 零问题、`flutter test` **690 全绿**（story 记 680，差额来自其后的改名回归用例，可解释，不计缺陷）。

**决策项（2026-08-04 已由用户逐条拍板，转为下方待修项）**

- [x] [Review][Decision] **360dp 窄屏上底部 Tab 标签仍被截断** → **决定：接受窄屏截断，只把测试参数化**（图标承担辨识；不再动刚定稿的文案，也不改底栏布局）。落到 patch：`referenceDevice` 改成 360+411 两档循环，并把「360dp ×1.15 起 Health / Sosial / Social 会截断」写成显式的已知取舍，免得下一轮又被误判为「已解决」。原始发现： — 主审探针实测：411dp 三档（1.0/1.15/1.3）四个标签全部完整，**你这轮的 `Sosial`/`Social` 修复在参照机型上是成立的**；但 **360dp 从 ×1.15 起 `Health` 与 `Sosial`/`Social` 双双被省略号截断**（两种语言都是），`Health` 还是这轮没动的那个词。新测试 `tab_bar_max_text_scale_test.dart` 的 `referenceDevice` 写死 `Size(411,900)`，所以看不到。360dp 是印尼市场常见宽度（Redmi / 三星 A 系）。三条路：再缩文案 / 给标签区更多横向空间（缩「＋」占位或不等宽）/ 接受窄屏截断但把测试参数化成 360+411 两档以免再被误判为「已解决」。
- [x] [Review][Decision] **原生启动屏的可视 mark 是固定 192dp** → **决定：Flutter 首帧改用固定 192 逻辑像素与原生对齐，再在 B1 里过渡到 42% 相对尺寸**（真正做到无缝，代价是动效多一段宽度插值）。落到 patch：首帧宽度 = 192，B1 期间插值到 `size.width * markWidthRatio`；457dp 屏上两者相等故无变化，窄屏表现为「起始略大再收到相对尺寸」，与既有的 1→0.7 缩小是同向的。原始发现：，Flutter 首帧是屏宽 42% —— 两者只在屏宽 457dp 时相等** — 盲审按资产实测反推：`splash_mark_white.png` alpha bbox 占画布 66.67%，Android 12+ 画布 288dp、Android<12 mdpi 288px、iOS storyboard `contentMode=center` 1x 288pt ⇒ 原生侧恒为 **192**。Flutter 侧 411dp 机型 172.6dp（小 10%）、iPhone 15 165pt（小 14%）、iPhone SE **134pt（小 30%）**。AC0 的「同尺寸交接」在任何常见机型上都不成立。要么首帧改用固定 192 逻辑像素再过渡到相对尺寸，要么承认交接有尺寸变化并把它写进动效设计。属设计口径，不擅自定。
- [x] [Review][Decision] **登录后回跳仍写死 `/profile`** → **决定：回跳也改 `/home`，口径统一**（未建档用户一律先看内容流，与本 Story 的立论一致）。落到 patch：改 `app_shell.dart` 的 `RouteIntent` 与那条已过期的注释。原始发现：，与本轮改的落地矩阵冲突** — `app_shell.dart:145` 的 `RouteIntent(location: '/profile')` 及其注释「登录后回跳落 Diary（FR-78 连带调整①）」是照旧矩阵写的。本轮把 A·未建档从 `/profile` 改成 `/home`，而新注册用户正是 A·未建档 ⇒ **游客点「＋」→ 注册成功 → 回跳空的建档引导页；同一个人下次冷启动却落 `/home`**。同一状态两个落地口径，而本 Story 的立论正是「档案未建时 Diary 是空引导页、内容流即时价值更高」。要么回跳也改 `/home`，要么明确「注册后首次刻意去建档引导」并把矛盾写进注释。
- [x] [Review][Decision] **`AuthStatus.newUserPendingOnboarding`（已注册未走完引导）落到 `guest` 分支** → **决定：落地行为保持现状（仍走 guest 分支），只把埋点口径分开**（看板上不再与真游客混为一谈；引导流本身很短、中途杀进程的人少，不值得为它加一条落地分支）。落到 patch：给 `AppUserState` 一个可区分的 wire 值供 T-1 上报，`landingLocation` 不变，并同步埋点文档词表与测试。原始发现： — `isLoggedIn` 只在 `authenticated` 时为真，所以这类用户冷启动落 `/profile` 的游客种草页，点主 CTA 又被弹一次引导；T-1 的 `user_state=guest` 把「真游客」和「注册未完成」混成一档（与本轮刻意用 `restore_timeout` 区分兜底态的动机相反）。本轮重订了落地矩阵却没给 AuthStatus 的第三个取值留分支。是否要补一态属产品口径。
- [x] [Review][Decision] **慢网提示的 `opsz 12` 不可达** → **决定：不重做字体**（主审判断：为一行 12.5px 的提示重新产一份字体，成本远大于收益）。落到 patch：订正 pubspec 与 splash 两处互相矛盾的 opsz 注释，并在 7-3 AC5 明确记录「opsz 12 不可达、实际以标语的光学尺寸渲染」这一偏差。原始发现： — 实测 `Fraunces-subset-SOFT-WONK.ttf` 的 `fvar` 只剩 `SOFT`/`WONK` 两轴，`opsz` 已被固化成单一实例；`pubspec.yaml:181` 说烘死的是 **opsz 16**、`splash_page.dart:481` 的注释说「opsz 12 已烘入」—— 同一个文件不可能两者都真。7-3 AC5 明写提示用 opsz 12，当前实际以标语的光学尺寸渲染。订正注释是 patch（下面已列），但**要不要为一行 12.5px 的提示重新产一份字体**是取舍，需你定。

**待修补（修法明确）**

> ✅ **第一批（3 条红线）已于 2026-08-04 修完并回归**：`flutter analyze` 零问题、`flutter test` **698 全绿**。
> 三点实施说明（与 findings 里写的修法有出入的地方）：
> 1. **双 5s 没有按「算剩余预算」修，而是把等待预算整体收归 splash**：路由层不再二次 `await`，
>    改为同步读新增的 `AuthController.isRestored`（恢复结束即置位）。原因是「算剩余时间」要用
>    `DateTime.now()`，那是**真实时钟**，与 Timer 的假时钟在测试里根本不同步 —— 我照那个思路写的
>    第一版回归测试对两种实现都通过（即又是一条恒真断言），换成单一时间源后才既正确又可测。
> 2. **mark 居中的回归测试用 reduce-motion 钉在终态断言**，而不是按「首帧 t=0」：首帧之后动画
>    立刻推进，按 t=0 断言会随 pump 时长漂移（第一版就因此误报）。已双向验证：把抵扣改回 `w/2`
>    这条会红（实测中心 357.4 vs 应为 372.4，正是 15dp）。
> 3. **顺带闭合了「grep 式测试」里的一条**：`late_landing_correction_test.dart` 原有一条断言
>    `app_router.dart` 源码里必须出现 `.timeout(` 字面量 —— 它守的是实现细节，本次把等待挪走后
>    约束依然成立却让它变红。已改为行为级测试（`splash_landing_budget_test.dart`：恢复晚到仍写回
>    ⇒ 迟到纠正照常把 A·未建档 从 Diary 纠正到 Social）。**同文件另外两条 grep 式断言仍在**，
>    归入下方「FR-91 接线零覆盖」那条待修项一并处理。
> 新增回归测试：`test/shared/splash_landing_budget_test.dart`（2 例）、`splash_test.dart` 的
> mark 居中 1 例 + 等待槽「360/411dp × 三档字号」6 例。

- [x] [Review][Patch] ✅**已修（2026-08-04 第一批：红线）** 🔴 **两个 5s 兜底是串联的，最坏在启动屏停 ~10 秒；且 5–10s 区间 FR-91 整条机制不生效** [petgo_app/lib/features/onboarding/presentation/splash_page.dart:87,239 + petgo_app/lib/core/router/app_router.dart:306-312] — splash 的 `readyDeadline`(5000ms) 到点 → `_maybeComplete(force:true)` → `onComplete` **此刻才**开始 `restore.timeout(5s)`。dio 的 receiveTimeout 是 30s、connectTimeout 15s，完全够挂住。三重后果：① 慢网冷启动实际停留最坏 ≈10s，慢网提示可读时间是 7.7s 而不是注释算的 2710ms，与决策 D-2「5s 兜底放行」和 7-3 AC2 都不符；② 恢复若在 5–10s 之间完成，`timedOut` 保持 false ⇒ **不带 `restore_timeout` 标记**（AC6 的「兜底发生率可观测」不成立、OQ-23 并未闭合）；③ 同一个 `timedOut` 还是 `lateCorrection.arm()` 的唯一条件 ⇒ **FR-91 迟到纠正只在恢复超过 10s 时才会武装**，7-4 的核心机制在最常见的慢网区间接近死代码。`app_router.dart:301` 的注释「此处通常已完成、立即返回」恰恰在唯一需要兜底的场景不成立。修法：改成剩余预算（`readyDeadline - 已耗时`），或 splash 已 force 放行时把这一段 timeout 降为 0。
- [x] [Review][Patch] ✅**已修（2026-08-04 第一批：红线）** 🔴 **首帧 mark 的垂直居中用了宽度的一半，实际比 50% 线高约 15dp** [petgo_app/lib/features/onboarding/presentation/splash_page.dart:396] — `top: (...) - w / 2` 里 `w` 是宽度，而 mark 的 viewBox 是 `389.32×321.81`（宽高比 1.2098）⇒ 渲染高度 = `0.827w`，抵扣应为 `h/2 = 0.413w`。差值 `0.087w`：411dp 机型 `w=172.6` ⇒ **高 15.0dp**。同文件的字标（`:312` 用 `wordmarkH/2`）与光晕都是对的，只有 mark 用了宽度 ⇒ 是笔误。后果：文件头声称的「Flutter 首帧接住**同位**的 mark」不成立，交接处会跳位 —— **正是 Epic 7 存在的理由**。一行修。
- [x] [Review][Patch] ✅**已修（2026-08-04 第一批：红线）** 🔴 **splash 底部等待槽固定 56px，在窄屏 + 印尼语下默认字号就溢出** [petgo_app/lib/features/onboarding/presentation/splash_page.dart:72,457-499] — 主审探针实测（慢网提示可见后）：**360dp + id + 默认字号 ×1.0 溢出 14px**、360dp×1.15 20px、360dp×1.3 29px、411dp+id×1.15 20px、411dp×1.3 29px、411dp+en×1.3 6px；只有 411dp 的 1.0 两语言安全。提示 `Text` 恒在树中（靠 opacity 显隐以保证 AC4 无位移），所以**高度参与布局与是否可见无关 —— 快网用户同样溢出**。56 这个值按 12.5×1.4 单行算出，没给 1.3 缩放（NFR-13 支持态）和窄屏换行留任何余量。现有 AC4 用例跑在 flutter_test 默认 800×600 且默认字号，永远绿。修法：`SizedBox` 改 `ConstrainedBox(minHeight:)` 或给提示 `maxLines: 2 + ellipsis`，并把回归用例参数化到 360dp × 三档字号。（注：widget test 用等宽测试字体，真机换行点更晚、绝对像素会小；但 360dp 默认字号已零余量，方向确定。）
- [x] [Review][Patch] ✅**已修（第二批：中危）** **迟到纠正会把用户从他自己刚选定的 Tab 上拽走**（违反 AC4 红线）[petgo_app/lib/core/router/app_router.dart:170,231] — 判定只比对 `currentConfiguration.uri.path`，分不清「一直没走」与「走了又回来」。边界猎手探针实测 `goBranch` 会真实改写 path：兜底落 `/profile` → 用户点 Social → 再点回 Diary（path 回到 `/profile`）→ 恢复完成 → 判 `correct` → `ctx.go('/home')`。AC4/NFR-15「用户已自行导航就一律不纠正」被绕过。修法：arm 时记下导航序号/上一次 location，任何一次用户发起的导航即 `disarm()`，而不是只看最终 path 是否相等。
- [x] [Review][Patch] ✅**已修（第二批：中危）** **迟到纠正会强关用户正在看的页内详情或正在走的登录弹层**（同一条 AC4 红线）[petgo_app/lib/core/router/app_router.dart:229-233,258] — 兜底落地页恒为 `/profile`（超时时状态判为 guest），该页游客态的示例详情走 `Navigator.push`（`diary_guest_page.dart:205`，刻意不新增受控路由）**不改变 router 的 path**；`showModalBottomSheet` 的软登录浮层与强登录窗同理。于是页内推入与弹层一律被判成「人没走」，纠正一跳把它们无声关掉。修法：correct 前补 `ModalRoute.of(ctx)?.isCurrent != true → disarm()`，或检查分支 Navigator 的 `canPop`。
- [x] [Review][Patch] ✅**已修（第二批：中危）** **T-1 的两个新属性没进埋点文档，也没进测试的契约清单** [docs/analytics-posthog-tracking.md:183,196-205 · petgo_app/test/analytics/v112_events_test.dart:259-269] — `restore_timeout` / `corrected_from` 在整个 `docs/` 下 0 命中，而它们存在的唯一理由就是「配看板的人要能统计兜底发生率」。事件名侧是对的（沿用 `app_launch_landed_on_tab` 未新造，AC6 满足），改名台账也写得很细，紧挨着的新属性反而漏了。
- [x] [Review][Patch] ✅**已修（第二批：中危）** **`corrected_from` 送的是路由路径原文，违反本 Story 自己的护栏** [petgo_app/lib/core/router/app_router.dart:255] — 同一次 capture 里 `tab` 已刻意经 `_landingTabNames` 转成产品名（注释明写「路径本身不适合直接送埋点：产品看不出它是 Diary」），`corrected_from` 却回退成 `/profile`。7-4 Dev Notes 的强制护栏原文是「不得把用户标识或路径原文塞进属性」。看板上两个属性无法互相对齐。修法：同样过 `_landingTabNames`。
- [x] [Review][Patch] ✅**已修（第二批：中危）** **FR-91 的运行时接线零行为覆盖，3 条测试退化成读源码字符串** [petgo_app/test/shared/late_landing_correction_test.dart:139,162,172] — 195 行新测试全落在纯函数与额度类上；真正会坏的那段（`arm` → `ctx.go` → `restore.then` → 比对 `currentConfiguration` → `consume`）一条测试都没有。三条改成读文件：`File('lib/core/router/app_router.dart')` 用相对路径（从 monorepo 根跑就挂）、`src.contains("if (isVetRoute && loc != '/vet/login') return '/home';")` 逐字匹配一行代码（`dart format` 折一次行就红而行为没变）、`RegExp(...).allMatches(src).length == 2` 把「上报两处」写死成源码计数。盲审已用探针补跑端到端（B/C 慢网超时 → 兜底 `/profile` → 恢复 → 自动纠正 `/home`，含 1/5/20/50/100/300ms 六档竞态）**当前实现是通的**，所以这是回归风险不是当下缺陷 —— 但这是被标为「不可降级」的红线代码，靠 grep 守不住。修法：补真实接线的端到端用例，替掉三条 grep 式断言。
- [x] [Review][Patch] ✅**已修（第二批：中危）** **两条恒真断言测试** — ① [petgo_app/test/onboarding/splash_test.dart:66-85] helper 叫 `markWidthAt`、注释说「取其渲染宽度」，实际返回 `tester.getSize(find.byType(SplashPage)).width` 即**整页宽**，于是 `expect(await markWidthAt(390), 390)` 恒真，把 mark 改回硬编码 `width: 108` 也照样绿 —— 7-2 T12「两种屏宽验证相对尺寸无漂移」因此不成立，也正因如此没拦住上面那条 mark 居中缺陷。② [petgo_app/test/shared/diary_gating_and_landing_test.dart:255] `expect(s.landingLocation, s.landingLocation)` —— 后人把它改成读 prefs 也照样通过，AC2「不持久化」的锁定意图落空。
- [x] [Review][Patch] ✅**已修（第二批：中危）** **深链落地缺位置守卫，且兜底窗口从 4.5s 收窄到 1.72s** [petgo_app/lib/core/router/app_router.dart:313-325 · petgo_app/lib/app.dart:61-76] — `onComplete` 只读一次 `pendingDeepLinkProvider`，读到 null 就无条件 `ctx.go(target)`，**不检查当前位置是否仍在 `/splash`**；而 `app.dart:73` 的 `uriLinkStream` 分支会直接 `router.go(loc)`，若它在 splash 期间命中，落地矩阵随后会把深链目标覆盖掉。同时交棒时机由 4500ms 压到 1720ms，`getInitialLink()` 这条异步平台通道若晚于交棒到达，pending 之后再无人读 ⇒ **静默丢弃**，用户落默认 Tab 而不是分享的名片页；窗口缩了 2.6 倍。修法：落地前加 `currentConfiguration.uri.path != '/splash' → return`，并让 pending 在 splash 之后仍可被消费一次。真机需测低端机冷启动首链到达时延。
- [x] [Review][Patch] ✅**已修（第二批：中危）** **Android 12 以下的白闪大概率没真正闭合** [petgo_app/android/app/src/main/res/values/styles.xml · values-night/styles.xml] — 本轮只改了 `windowSplashScreenBackground`(v31) 与 launch drawable，**`NormalTheme` 的 `android:windowBackground` 仍是 `?android:colorBackground`**（Light 主题下是白、Black 下是黑），而 `AndroidManifest` 已把 `NormalTheme` 指向它、FlutterActivity 在 `onCreate` 就切过去、Flutter 首帧在那之后。API 31+ 有系统 SplashScreen 盖住不受影响；**API <12 设备（印尼市场占比不低）在原生启动屏与紫色首帧之间仍会闪一次白（深色下黑）**。`pubspec.yaml:206` 的注释把病因全归给 splash 底色，漏了这一层。修法：给 NormalTheme 显式设紫底 windowBackground。⏳ 需 Android 11 及以下真机 + 浅色/深色各录一次冷启动确认。
- [x] [Review][Patch] ✅**已修（第二批：中危）** **旧品牌资产仍被登录页引用，而 7-2 声称「引用已全部移除」** [petgo_app/lib/features/auth/presentation/login_page.dart:348-350] — `mark_dog.svg` / `mark_cat.svg` 仍在用（`wordmark.svg` 侧确已无引用）。7-2 的 File List 写「引用已全部移除，文件本身保留」，并把「物理删除旧资产」推给 Epic 收尾 —— **照那个描述去删，登录页会缺资产**。修法：订正 File List 并在资产旁标注「登录页仍在用，不可删」。
- [x] [Review][Patch] ✅**已修（第二批：中危）** **字标资产加载失败 → 整个启动屏不出现品牌名，且线上无任何可见告警** [petgo_app/lib/features/onboarding/presentation/splash_page.dart:174-195,417-418] — `_loadWordmark` 的 catch 只 `debugPrint`，`_wordmarkBeats` 保持 null，而 `_wordmark()` 在 null 时返回**空 SizedBox**；终态 mark 的 opacity 已是 0 ⇒ 屏幕上只剩一行标语。文件注释声称的「失败则整块一次性显示，不阻断启动」并未实现。同一处 `RegExp(r'<svg[^>]*>').firstMatch(raw)!` 的 `!` 是直接触发点之一；另有一个静默变体：若资产改成非自闭合 `<path …></path>` 写法，正则零命中会走 else 分支生成一个没有任何路径的空 SVG，既不进 catch 也不打日志。修法：catch 与 `paths.isEmpty` 两条路径都退化为「整块一次性显示」（`_wordmarkBeats = [raw]`），并去掉 `firstMatch` 的 `!`。
- [x] [Review][Patch] ✅**已修（第二批：中危）** **后台运行时 Timer 照常触发 ⇒ 启动埋点在用户看不见时上报、入场动效整段被跳过** [petgo_app/lib/features/onboarding/presentation/splash_page.dart:222-242,261-265] — 全套 Timer 与 `_maybeComplete` 没有任何 `AppLifecycleState` 门控。后台时 Dart Timer 照走而 ticker 因无帧停滞 ⇒ `_holdTimer` 到点即交棒、上报 T-1 与 `Analytics.screen` 并 `ctx.go`，全发生在不可见时；回前台时 `_master` 因单帧巨大 elapsed 直接跳终态。修法：`AppLifecycleListener` 在 paused 时暂停三个反馈时点与交棒判定，resumed 后按剩余预算重排。⏳ 真机行为需 L2 确认。
- [x] [Review][Patch] ✅**已修（第二批：中危）** **快网路径上进度线动画恒转、每帧 relayout** [petgo_app/lib/features/onboarding/presentation/splash_page.dart:469,549-597] — `_IndeterminateLine` 无条件构造，控制器在字段初始化就 `..repeat()`，只靠外层 `AnimatedOpacity` 遮住；`AnimatedBuilder` 包 `LayoutBuilder` ⇒ 每帧触发一次 layout 失效，而快网用户 100% 命中此路径（AC6 明确这两个指示恒不出现）。开销正压在冷启动最吃紧的头 1.7s。修法：`if (_showProgressLine)` 才构造，或首次可见时才 `repeat()`。
- [x] [Review][Patch] ✅**已修（第二批：中危）** **`prepareSession` 同步抛出会打死启动屏且 5s 兜底随之失效** [petgo_app/lib/features/onboarding/presentation/splash_page.dart:214-216] — `(widget.prepareSession?.call() ?? Future.value()).then(...).catchError(...)` 只接得住 future 内的错误；`ref.read(authControllerProvider.notifier)` 若在 notifier 构建期抛错，异常从 `_start()` 冒出 `didChangeDependencies` ⇒ 红屏/黑屏，`_onReady` 永不被调、兜底也不生效。修法：包进 `Future(() => widget.prepareSession!())`。
- [x] [Review][Patch] ✅**已修（第三批：低危）** **PRD OQ-20 未回写**（7-1 AC5 明文要求且已勾 [x]）[_bmad-output/planning-artifacts/v1.1.2/PRD-v1.1.2.md:764] — 仍写「待真机复核…可先按屏宽 42% 实现」，实测的 180dp / 43.70% / 中心 y=50.0% 只进了 Completion Notes。AC5 原文是「记录进 Completion Notes **并回写 PRD OQ-20**，不得跳过」。
- [x] [Review][Patch] ✅**已修（第三批：低危）** **PRD §3 埋点表仍写 `discovery`** [_bmad-output/planning-artifacts/v1.1.2/PRD-v1.1.2.md:711] — 代码侧改名已彻底（`bottom_tab_bar.dart:21`、`app_router.dart:105`、`login_guide_controller.dart` 三处、`Analytics.screen('social_page')`，全仓 `discovery_*` 字面量 0 命中），`docs/analytics-posthog-tracking.md` 也更新并留了台账，唯 PRD 那一行是旧值。
- [x] [Review][Patch] ✅**已修（第三批：低危）** **注释里仍有约 25 处 `Discovery`，与改名的立论自相矛盾** [petgo_app/lib/shared/widgets/bottom_tab_bar.dart:12,13,32,33,59,72,75,110,211 · app_shell.dart:37,106,114,144 · user_state.dart:47,52 · home_page.dart:30,204 等] — 而 `docs/analytics-posthog-tracking.md:134` 给出的改名理由恰恰是「避免代码里叫 discovery、界面上叫 Social 的长期错位」。埋点标识与用户文案都改干净了，注释层原样留着。
- [x] [Review][Patch] ✅**已修（第三批：低危）** **`prefs.dart` 残留一条描述已删功能的孤立注释；旧 key 在存量安装里无清理** [petgo_app/lib/core/storage/prefs.dart:30] — 注释还在说「Splash 当天是否已播放过完整动画（之后只显静止终态）」，而这个行为本轮已整条废除、其下方已无任何成员，会让人以为门控仍在（7-3 AC3 要求「不留死代码」）。另 `petgo.splash_last_shown_date` 在老装机里永久残留（无害，但可在 `AppPrefs.create` 后一次性 remove）。
- [x] [Review][Patch] ✅**已修（第三批：低危）** **Quicksand 124KB 仍随包分发但已无任何代码引用，pubspec 注释仍写「splash 标语用」** [petgo_app/pubspec.yaml:166-169] — AC6 的验收判据（splash 代码里不再出现 Quicksand/Poppins）确已通过，但产物层多带死资产且注释描述的用途已不存在。
- [x] [Review][Patch] ✅**已修（第三批：低危）** **OFL 合规声明与实际不符：`OFL.txt` 从不随 app 分发** [petgo_app/pubspec.yaml:177-178] — 注释写「许可证随字体同目录 assets/fonts/OFL.txt —— OFL 要求随分发附带」，但 `assets/fonts/` 不在 pubspec 的 assets 列表里（字体靠 `fonts:` 段打包、只进 ttf），`lib/` 里也没有 `LicenseRegistry` 或开源许可页 ⇒ OFL §2「每份分发副本须含版权声明与本许可证」未被满足，而注释宣称已闭合。修法：把 `assets/fonts/OFL.txt` 加入 assets，或用 `LicenseRegistry.addLicense` 注册。（Poppins/Quicksand/Rubik 连许可证文件都没有，属既有欠账，见 deferred-work。）
- [x] [Review][Patch] ✅**已修（第二批：中危）** **字体子集注释与实测不符，且两处 opsz 说法互相矛盾** [petgo_app/pubspec.yaml:181,187-193 · petgo_app/lib/features/onboarding/presentation/splash_page.dart:481] — 实测 cmap 共 **97** 码位（U+0020–U+007E + U+2019 + U+2026），注释写「+ 省略号 + 弯引号，共 98 码位」：**弯双引号 U+2018/U+201C/U+201D 一个都没有**，项目两份 ARB 里高频的破折号 U+2014 也没有；两处 `TextStyle` 均未设 `fontFamilyFallback` ⇒ 一旦文案带入这些字符，该字符掉到系统字体、同一行两种字形（违反同处注释宣称的 NFR-16「splash 内只允许一款字体」），系统无字形时为豆腐块。当前四条文案全在范围内，所以是**下一次改文案就会踩**。另 pubspec 说烘死 opsz 16、splash 注释说 opsz 12（见上方决策项）。修法：子集补 U+2014/U+2013/U+2018/U+201C/U+201D，注释改成实测码位数，并加一条用 ARB 实际字符串逐字符校验 cmap 的测试。
- [x] [Review][Patch] ✅**已修（第三批：低危）** **`prepareSession` 的注释立论不成立** [petgo_app/lib/features/onboarding/presentation/splash_page.dart:35-42 · app_router.dart:294-298] — 注释称「改前是串行：动效播完（4.32s）才发请求」。实际 `AuthController.build()` 就已 `_restoreFuture = _restoreSession()` fire-and-forget，且 `app.dart` 在首次 build 时（比 SplashPage 存在更早）就初始化了它 ⇒ 改前的 `ensureRestored()` 只是 await 一条早已在飞的共享 future，「动效与取数并行」本来就成立。`prepareSession` 真正新增的是「观测何时完成」这一能力（确实必要）。按现注释理解代码的人会以为 splash 拥有 `/me` 的触发权，进而可能把它挪走或复用。
- [x] [Review][Patch] ✅**已修（第三批：低危）** **两处 `.catchError` 是死路径，且 `lateCorrect` 结构上可能被调两次** [petgo_app/lib/core/router/app_router.dart:348 · splash_page.dart:216] — `_restoreSession()` 内部已 catch-all 吞掉一切，这条 future 永不 error ⇒ `catchError` 永不执行；且它挂在 `.then` 的结果上，若 `lateCorrect()` 自己抛错会**再调一次**（有 `consume()` 兜底所以当前无害，但结构是错的）。
- [x] [Review][Patch] ✅**已修（第三批：低危）** **ARB 模板缺 `@` 描述，OQ-22 的重要警告写在工具链读不到的地方** [petgo_app/lib/l10n/app_en.arb:789 · app_id.arb:2014] — `splashSlowNetworkHint` 在模板 ARB（`l10n.yaml` 指定 `app_en.arb`）里没有 `@` 描述，而「印尼语为设计侧占位、待母语同事审核（OQ-22）」这条警告写在了非模板文件里 —— gen-l10n 不读非模板文件的元数据，也最不容易被搜到。
- [x] [Review][Patch] ✅**已修（第三批：低危）** **低端机上字标可能「半途整段冒出」而非逐拍写出** [petgo_app/lib/features/onboarding/presentation/splash_page.dart:164,170-192] — `_loadWordmark` 与 `_master.forward()` 同帧发起、互不等待。若 `loadString` 落地时 `_master.value` 已越过 480/1540 的 Interval，前几拍的 t 已接近 1 ⇒ AC2 的「9 拍逐组写出」在慢机上不成立（reduce-motion 下 `value=1` 反而无此问题）。修法：`_loadWordmark` 完成后再 `forward()`，资产失败则直接置 `value=1`。⏳ 实际耗时需真机确认。

**三批修补的实施说明（与 findings 原写修法有出入之处）**

> **第二批（中危 14 条）**：
> - **AC4 两条红线**都补了行为级端到端测试（`splash_landing_budget_test.dart`），并**双向验证**：
>   把修复退回，两条测试确实变红。「切走又切回」改为监听路由变化、离开过就永久 disarm；
>   「弹层开着」改为纠正前检查根 Navigator 是否可 pop。
> - ⚠️ **仍有一个未覆盖的子场景**：`showModalBottomSheet`（默认 `useRootNavigator: false`）
>   与游客 Diary 页内 `Navigator.push` 走的是**分支 Navigator**，根 Navigator 的 `canPop` 看不到它们。
>   要覆盖需把分支 Navigator 的 key 暴露到 router 层（改动 shell 构建），或改成「任何用户交互即
>   disarm」（那会让 FR-91 在实际使用中几乎不触发，是产品取舍）。**已记入下方待办，不在本轮范围。**
> - `corrected_from` 改送产品名后，`late_landing_correction_test.dart` 里两条逐字匹配源码的断言
>   随之失效 —— 已改为行为断言（用 `Analytics.debugCaptureSink` 直接看真实上报）。
>   第三条（兽医隔离守卫）保留源码断言但改为**分要素 + 对格式化不敏感**，因为它守的是
>   「不出现某个写法」，行为测试天生看不见。
>
> **第三批（低危 10 条）**：
> - Quicksand **只停止打包**（注释掉 pubspec 声明，省 124KB），字体文件保留 —— 物理删除与旧品牌
>   资产一起留 Epic 收尾，避免多次改动 assets 目录。
> - OFL 许可证已加入 assets 列表（真正随包分发）。⚠️ 仅打包不等于「用户可见」，完全合规还需一个
>   开源许可页，已记 deferred-work。
> - 字体子集**没有补码位**（需重跑子集脚本，非本轮范围）：只订正了注释里不实的码位描述与
>   两处互相矛盾的 opsz 说法，并给两处 `TextStyle` 加了 `fontFamilyFallback` 作缺字兜底。
> - 「低端机字标突现」改为**等资产就位或 260ms 宽限期到点才起拍**；资产迟到或失败都照常起拍
>   （退化为整块显示），绝不让启动屏被一个资产卡住。
> - `prefs.dart` 的孤立注释已换成「已废弃键」说明，并在 `AppPrefs.create()` 里一次性清理存量脏键。
> - 代码注释里 20 处 `Discovery` → `Social`；余 3 处**刻意保留**（都在讲历史沿革，如
>   「原首页，V1.1.2 曾短暂叫 Discovery」），保留反而防止后来人以为改名从未发生。

**修补过程中新增的待办（需你后续定，非本轮范围）**

- [ ] [Review][Open] **迟到纠正的「免打扰」仍漏一类场景**：非根 Navigator 的弹层与页内推入
  （`showModalBottomSheet` 默认值、游客 Diary 的示例详情）不改变路由 path，也不出现在根
  Navigator 的 `canPop` 里 ⇒ 用户正看着它们时纠正仍会跳页并关掉它们。两条路：
  ① 把分支 Navigator 的 key 暴露到 router 层做统一判定（改动 shell 构建）；
  ② 「兜底落地后只要用户有任何交互就 disarm」（最安全，但 FR-91 在实际使用中会几乎不触发 ——
  这是产品取舍，AC4 的优先级原则支持它）。
- [ ] [Review][Open] **Fraunces 子集需补码位**：实测仅 97 码位，缺破折号 U+2014/U+2013 与
  弯引号 U+2018/U+201C/U+201D。当前四条文案全在范围内，但**下次改启动屏文案就可能踩**。
  需重跑子集脚本（fonttools）并把 pubspec 的码位说明同步为实测值。
- [ ] [Review][Open] **开源许可页**：OFL.txt 已随包分发，但用户无处查看。Flutter 内置
  `LicenseRegistry` / `AboutDialog` 即可承载；顺带把 Poppins / Rubik（以及保留的 Quicksand）
  的许可证一并登记 —— 见 deferred-work 的「开源许可证收口」。

**已延后（既有欠账或已知取舍，非本轮引入）**

- [x] [Review][Defer] iOS 原生 mark 固定 192pt 与 Android 180dp 差 6.7% — deferred：7-1 自己已记「V1 不上 iOS，待 iOS 上架前处理」。
- [x] [Review][Defer] `LaunchScreen.storyboard:39` 的 `<image width="1152" height="1152"/>` 与 1x 资产实测 288×288 不符 — deferred：生成物，仅影响 Xcode 画布不影响运行时；手改会被下次生成覆盖。
- [x] [Review][Defer] Poppins / Quicksand / Rubik 三款字体连许可证文件都没有 — deferred, pre-existing：本轮只有 Fraunces 明确声称了合规，其余属既有欠账，需单独一轮许可证收口。

**已判为噪声丢弃（记录理由，避免下轮重开）**

- AC 审计报的「`arm` 与 `ctx.go` 之间存在竞态、可能白白放弃一次纠正」—— **被边界猎手的探针证伪**：`ctx.go()` 会**同步**更新 `currentConfiguration`，探针在超时后 1/5/20/50/100/300ms 六档落地全部正确纠正，不存在「刚 go 完路径还是 `/splash` 被误判成 userMovedOn」。
- 「7-4 记 680 tests 而实测 690」—— 差额来自其后的 `discovery→social` 改名回归用例，可解释，非缺陷。

## Dev Notes

### 🔴 两条安全攸关约束（不可降级、不可跳过）

**一、兽医角色隔离守卫必须原样保留**（AC3⑦）
`app_router.dart` 的 `redirect` 里有：
- `if (auth.isVet) { if (!isVetRoute || loc == '/vet/login') return '/vet/workbench'; return null; }`
- `if (isVetRoute && loc != '/vet/login') return '/home';`

这是**用户侧与兽医侧路由互斥的安全边界**，不是「顺手做的纠正」。FR-91 新增机制时容易产生「都统一走迟到纠正就行了」的想法 —— **不行**。守卫继承「安全规则只升不降不可绕过」，必须留作双保险。

**二、不打扰已离开的用户，优先级高于纠正**（AC4）
用户可能在兜底落地后 1 秒内就点走了。此时恢复完成触发纠正，会把正在浏览的人**强行拽走** —— 这比落错页严重得多。**冲突时以「不打扰」为准。**

判定必须**比对路径**。用「路由栈深度」判断会误判：用户在同一页内开弹层 / 进详情再返回，栈深变了但人没走。

### 为什么需要 FR-91（技术根因）

`ensureRestored()` 返回的是共享 future（`auth_state.dart:44`），而 `.timeout(5s)` **只停止等待、不取消底层请求**。恢复请求继续跑，完成后写回 `AuthState`；路由挂了 `refreshListenable`（`app_router.dart:147` / `:153` 的 `ref.listen` + `authRefresh`），状态变化会**触发 redirect 重跑**、页面重渲染。

后果：
- **兽医**能自愈 —— 因为 redirect 里有角色隔离守卫，会把它收口回工作台
- **A·未建档 与 B/C 不能自愈** —— 它们是**已登录**用户，而 `/profile` 的门控条件是 `!auth.isLoggedIn && controlled`（`app_router.dart`），已登录不命中；redirect 里**没有任何分支**会把已登录用户从 Diary 移到 Discovery

所以 B/C 的实际体验是：**先看到 FR-80 游客种草页 → 恢复回来后翻成「有宠专属」功能拒绝页 → 卡住**。FR-91 补的就是「兜底跳转之后没人再纠正一次」这一环。

### 现状核实结果（2026-08-04，实施基线）

**`petgo_app/lib/features/auth/domain/user_state.dart`**：
- `landingLocation`（`:39-49`）为覆盖六态的 `switch`：`guest => '/profile'`、`vet => '/vet/workbench'`、`ownerWithProfile => '/profile'`、**`ownerWithoutProfile => '/profile'`（`:45`，本 Story 改 `'/home'`）**、`planning => '/home'`、`enthusiast => '/home'`
- `:43` 注释「状态 A 一律落 Diary」← 本 Story 须同步改写
- 文件头注释已写明：本枚举是**落地 Tab 与埋点 `user_state` 的唯一判定源**；`DiaryUserState` 由它派生（`resolveDiaryUserState` 复用 `resolveAppUserState`），**判定顺序只写一处**

**`petgo_app/lib/core/router/app_router.dart`**：
- `redirect`：`/splash` 直接放过 → 兽医隔离守卫 → 非兽医不进 `/vet/*` → 「默认受控 + 精确例外」门控（`!auth.isLoggedIn && controlled`）
- `refreshListenable: authRefresh`（`:153`）+ `ref.listen(authControllerProvider, ...)`（`:147`）—— 状态变化触发 redirect 重跑
- `onComplete`：`await ensureRestored().timeout(3s)`（7.3 改 5s）→ `pendingDeepLinkProvider` 优先 → `appUserStateOf(auth).landingLocation` → T-1 埋点（`app_launch_landed_on_tab`，属性 `tab` + `user_state`）→ `ctx.go(target)`
- `_landingTabNames` 把路径映射为产品名（diary / discovery / vet_workbench），埋点用产品名不用路由路径

**`petgo_app/test/shared/diary_gating_and_landing_test.dart`**：
- `:236` `expect(resolve(petStatus: 'HAS_PET'), AppUserState.ownerWithoutProfile)`
- `:247` `expect(AppUserState.ownerWithoutProfile.landingLocation, '/profile')` ← **本 Story 改 `'/home'`**

**`petgo_app/lib/features/profile/presentation/growth_archive_page.dart`**：
- `enum DiaryUserState`（`:34-46`）：`guest` / `nonOwner`（B/C）/ `ownerWithoutProfile` / `ownerWithProfile`
- `resolveDiaryUserState`（`:56`）吃 `isLoggedIn` / `petStatus` / `hasPetProfile`，**复用** `resolveAppUserState` 再折叠为本页四分支 —— **这就是「未恢复 → 视为未登录 → 渲染游客种草页」的机制来源**

### 必须保留的既有行为（不得破坏）

- **分流优先级**：pending 深链 > 落地矩阵（AD-8）。FR-91 的第⑤条正是它的延伸
- **落地判定不持久化**（FR-78 硬约束）—— 不得为「修慢网」而引入「记住上次落地页」
- **`AppUserState` 单一判定源**：落地分流、埋点 `user_state`、Diary 页分支三处共用一套顺序。**改矩阵只改 `landingLocation` 一处**，不要在别处再写一份判定
- **T-1 事件名不变**（Story 6-1 已交付看板口径依赖它）

### 强制护栏（违反即返工）

- 安全规则**只升不降不可绕过**（兽医守卫 + 门控默认受控）
- 不引入新依赖、新中间件；不新增埋点 SDK
- 不改 `ddl-auto`、不加 Flyway 迁移（本 Story 零 schema）
- 日志/埋点**严禁记录 PII**；`user_state` 与 `tab` 均为受控字面量，不得把用户标识或路径原文塞进属性

### Project Structure Notes

- 矩阵改动：`lib/features/auth/domain/user_state.dart`（一行 + 注释）
- 迟到纠正：`lib/core/router/app_router.dart`（`onComplete` 与兜底标记）；若需承载启动期状态，优先用既有 Provider 模式，**不新建全局单例**
- 测试：`test/shared/diary_gating_and_landing_test.dart`（改断言 + 补六态）；纠正行为的测试就近落 `test/shared/` 或 `test/onboarding/`
- 架构回写：`_bmad-output/planning-artifacts/architecture-v1.1.2-delta.md`（AD-8 + 新增 AD-17）

### References

- [Source: _bmad-output/planning-artifacts/epics-v1.1.2.md#Story 7.4]（AC1~AC8 原文）
- [Source: _bmad-output/planning-artifacts/v1.1.2/PRD-v1.1.2.md#2.6]（FR-91 全部 + FR-89 超时兜底表 + 五类用户后果）
- [Source: _bmad-output/planning-artifacts/v1.1.2/PRD-v1.1.2.md#2.1]（FR-78 落地矩阵及 2026-08-04 订正、状态切换时点澄清）
- [Source: PRD §5 A-8]（兜底态是中间态，五类后果逐类）
- [Source: PRD §4 OQ-23]（已闭合，由本 Story 的 FR-91 解决）／[OQ-24]（A·未建档改落 Discovery 后的建档转化率，上线后观测）
- [Source: _bmad-output/planning-artifacts/architecture-v1.1.2-delta.md#AD-8]（**矩阵描述已过期，本 Story AC8 负责回写**）
- [Source: _bmad-output/implementation-artifacts/v1.1.2/7-3-启动期数据获取与等待反馈.md]（前序：5s 兜底路径、`.timeout()` 不取消请求的既有行为）
- [Source: _bmad-output/implementation-artifacts/v1.1.2/2-4-登录门控解除与落地-tab-矩阵.md]（旧矩阵实现；其 AC4 已标「矩阵部分被本 Story 取代」）

## Dev Agent Record

### Agent Model Used

Claude Opus 5 (1M context) · claude-opus-5[1m]

### Debug Log References

- `flutter analyze` → `No issues found!`
- `flutter test` → `All tests passed!`（**680 tests**，7-3 后 665 + 本 Story 新增 15）
- `flutter build apk --debug` → 通过；已安装至模拟器

### Completion Notes List

**AC1~AC6、AC8、AC9 已满足。AC7 为弱网真机端到端，待人工（判定逻辑已由单测逐条锁定）。**

#### ① FR-78 落地矩阵变更（Story 2-4 的返工，由本 Story 负责）

`user_state.dart` 的 `landingLocation`：`ownerWithoutProfile` 由 `'/profile'` 改 **`'/home'`**。
同文件注释「状态 A 一律落 Diary」已改写为新口径（**只有真正建了档案的人才落 Diary，游客是唯一例外**），并写明代价与 OQ-24。
`test/shared/diary_gating_and_landing_test.dart` 的六态断言同改，并新增一条锁定「`landingLocation` 是纯函数、不持久化」——防后人为"修慢网"而引入「记住上次落在哪」（FR-78 硬约束；慢网正解是 FR-91）。

#### ② FR-91 迟到纠正（新增行为）

**为什么需要它**（技术根因，写进 AD-17 免得日后被当成多余补丁）：
`ensureRestored()` 的 `.timeout()` **只停止等待、不取消底层请求** —— 恢复继续跑、完成后写回 `AuthState`。
所以超时兜底送去的落地页**是会变化的中间态，不是终态**：

- **兽医能自愈**：`redirect` 有角色隔离守卫（`if (auth.isVet)`）会收口回工作台
- **已登录的 A·未建档与 B/C 不能**：门控条件是 `!auth.isLoggedIn && controlled`，已登录不命中；`redirect` 里**没有任何分支**会把已登录用户从 Diary 移到 Discovery

**实现结构**（刻意拆成三块，让 7 条约束可被逐条单测）：

| 部件 | 职责 |
|---|---|
| `LateLandingCorrection` | 一次性额度状态机：`arm` / `armed` / `disarm`（放弃本次但不耗额度）/ `consume`（耗额度，此后不再干预） |
| `resolveLateCorrection()` | **纯判定函数**，入参只有 `armed` / `currentPath` / `recomputedTarget`，返回四种结果之一。无副作用 ⇒ 7 条约束可精确断言 |
| `lateCorrect()`（provider 内闭包） | 取当前路径与真实状态 → 调纯函数 → 按结果 return / disarm / 跳转 + 上报 |

**七条约束逐条落点**：

| # | 约束 | 落点 |
|---|---|---|
| ① | 仅兜底路径启用 | `onComplete` 内 `if (timedOut) lateCorrection.arm(target)`；正常路径不武装 |
| ② | 以**恢复 future 完成**为时机（非固定延时） | `restore.then(...).catchError(...)` —— 用的是 `onComplete` 里捕获的那条 future 本身，失败也算完成（走⑥） |
| ③ | 仅当用户仍停在兜底落地页才纠正 | `resolveLateCorrection` 的 `currentPath != armed → userMovedOn` |
| ④ | 只纠正一次 | `consume()` 置 `_used`，此后 `armed` 恒为 null，且**再次 `arm` 无效** |
| ⑤ | 深链唤起不纠正 | pending 深链分支内**不武装**即 return |
| ⑥ | 恢复失败（真游客）→ 不跳 | `recomputedTarget == armed → targetUnchanged` |
| ⑦ | 兽医守卫原样保留 | **`redirect` 一行未动**（已用 `git diff` 核实：diff 里只有我新增的注释文字命中 `isVet`/`redirect`/`controlled` 关键词，代码零改动） |

#### 🔴 两条安全红线的处置

**一、兽医角色隔离守卫原样保留（AC3⑦）。** 做 FR-91 时最容易产生「都统一走迟到纠正就行了」的想法 —— 不行。那是**用户/兽医路由互斥的安全边界**，继承「安全规则只升不降不可绕过」。两者算出同一目标（`/vet/workbench`），一次性额度保证只跳一次。已加**源码级锁定测试**：断言 `if (auth.isVet)`、正/反向隔离、以及门控的 `!auth.isLoggedIn && controlled` 三者仍在。

**二、「不打扰已离开的用户」优先级高于「纠正到正确页」（AC4 / NFR-15）。** 已写成测试里最显眼的一条反例：`armed=/profile` 但 `currentPath=/triage` 且 `recomputedTarget=/home`（即**本该纠正**）时，判定仍必须是 `userMovedOn`。
判定**只看路径** —— `resolveLateCorrection` 的入参里**根本没有栈深度这类信号**，从签名上就排除了误用。工程注意已就地注释：同页开弹层/进详情再返回，栈深会变但人没走。

#### ③ AC6 埋点

兜底落地与纠正后落地**各上报一次既有 T-1** `app_launch_landed_on_tab`，**事件名不新造**（否则 Story 6.1 已交付的看板口径会断），分别带 `restore_timeout: true` 与 `corrected_from: <原目标>`。
已加源码级断言：该事件名恰好出现 2 处上报、两个标记都在、属性名 snake_case、路由层不直调 PostHog SDK（统一走 `Analytics` 门面）。
**这使「超时兜底实际发生率」成为可观测指标 ⇒ PRD OQ-23 闭合**（原先只能推测）。

#### ④ AC8 架构回写（Epic 7 收尾）

- **AD-8 第 2 条已订正**：矩阵改为「只有已建档者落 Diary，游客例外」，并补上实现落点、状态切换时点、以及 OQ-24 的代价说明。概览表那一行也加了订正标记。
- **新增 AD-17**：记录 FR-91 的完整口径 —— 技术根因、7 条 Rule、两条安全红线、可观测性、已接受的代价（一次可见跳变）、实现落点，以及**反向约束「`.timeout` 不得改成可取消」**（否则本机制永不触发）。
- 连带把三处「16 条 AD」的引用改为 17 条，并把 sprint 文件头部「AD-8 过期 / FR-91 无 AD」的警示改为完成态。

#### 一处实现选择说明

把判定抽成 `resolveLateCorrection()` 纯函数、并把 `LateLandingCorrection` 从私有类改为公开，**唯一目的是让 7 条约束与额度机制可被单测锁定**。行为与内联实现完全一致；两条安全红线若只写在闭包里，将无法用测试防止后人改坏。

#### ⏳ 待人工验收（AC7，弱网真机）

| 任务 | 已由单测锁定的部分 |
|---|---|
| **T9** B/C 弱网 → 最终落 Discovery（主诉求） | `resolveLateCorrection` 对 planning/enthusiast 均返回 `correct`，目标 `/home` |
| **T10** 兽医弱网 → 落工作台 | 重判目标 `/vet/workbench`，与守卫结果一致 |
| **T11** A·未建档弱网 → 落 Discovery | `ownerWithoutProfile.landingLocation == '/home'` 且判定 `correct` |
| **T12** 快网回归无多余跳转 | 正常路径不武装（约束①）⇒ 不可能产生跳转 |
| **T13** 兜底后切 Tab → 不被拽走 | 「用户已离开 → `userMovedOn`」含"即便本该纠正也必须放手"的反例 |

缺的只是**端到端目视 + 弱网限速**；本会话无弱网注入手段，且 debug 包在模拟器上启动慢、动效帧抓不稳（同 7-2/7-3 的情况）。

### File List

**新增（1）**
- `petgo_app/test/shared/late_landing_correction_test.dart` —— FR-91 的 14 条测试（7 条约束 + 额度机制 + 埋点口径 + 两条安全红线的源码级锁定 + `.timeout` 不可取消的反向约束）

**修改（4）**
- `petgo_app/lib/features/auth/domain/user_state.dart` —— `ownerWithoutProfile` 落地目标 `'/profile'` → `'/home'`，注释改写为新口径
- `petgo_app/lib/core/router/app_router.dart` —— 新增 `LateCorrectionOutcome` / `resolveLateCorrection()` / `LateLandingCorrection`；`onComplete` 记录 `timedOut` 并武装迟到纠正；两处 T-1 上报加区分标记。**`redirect` 块（含兽医守卫与门控）一行未动**
- `petgo_app/test/shared/diary_gating_and_landing_test.dart` —— 六态断言更新 + 新增「不持久化」锁定
- `_bmad-output/planning-artifacts/architecture-v1.1.2-delta.md` —— AD-8 回写 + 新增 AD-17（AC8）

**连带更新（3，文档一致性）**
- `_bmad-output/planning-artifacts/epics-v1.1.2.md` —— 「16 条 AD」→ 17 条，并标注架构缺口已由本 Story 收尾
- `_bmad-output/implementation-artifacts/sprint-status-v1.1.2.yaml` —— AD 数量与架构警示改完成态

### Change Log

| 日期 | 内容 |
|---|---|
| 2026-08-04 | 实施 AC1~AC6/AC8/AC9：FR-78 矩阵变更（A·未建档 → Discovery，Story 2-4 的返工）+ FR-91 迟到纠正（7 条硬约束，判定抽为纯函数以便逐条单测）+ T-1 埋点加 `restore_timeout`/`corrected_from` 区分（闭合 OQ-23）。两条安全红线均有源码级锁定测试。测试 665→680 通过，analyze 零警告，apk 构建通过。 |
| 2026-08-04 | AC8 收尾：回写架构 AD-8 的矩阵描述，新增 AD-17 记录 FR-91 口径（含"`.timeout` 不得改成可取消"的反向约束）；同步三处「16 条 AD」引用与 sprint 头部警示。**Epic 7 的架构缺口至此闭合。** |
