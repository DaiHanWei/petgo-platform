---
baseline_commit: b324308a
---

# Story 4.2: 发布页同步开关与「我的」页私密标识

Status: review

> **所属**：V1.1.2 Epic 4 第二个 Story（**纯前端**）。交付：发布页「同步到 Moment」开关 + 默认类型调整 +「我的」页私密标识。
> 依赖 Story 4.1（`visibility` 字段与接口）。
> ⚠️ **UI 稿有一处 PRD 没写的交互**：关掉同步开关后，主按钮文案从 **Bagikan（分享）变为 Simpan（保存）**。照 PRD 实现会漏掉。
> ⚠️ **口径合并**：代码里已有「在成长档案 Tab 点 [+] 且有档案 → 预选成长日历」（bug 20260703-244），与 FR-83「全局默认 Diary」重叠，须合并为单一口径。

## Story

As a **想给自己留点私人记录的用户**,
I want **发日记时能一键关掉「同步到 Moment」**,
so that **有些只想自己看的事，不用发到大家都能刷到的地方（FR-83）**。

## Acceptance Criteria

> **验证层**：**L0**（analyze / widget test）· **L1**（真后端联调）· **L2**（模拟器视觉）。

### AC1 — 默认 Diary + 同步开关默认开启（L2 · UX-DR14）

**Given** UI 稿 P1 屏
**When** **有宠物档案**的用户进入发布页
**Then** **默认选中 Diary**
**And** 出现「同步到 Moment」开关且**默认开启**
**And** 含「Untuk: {宠物名}」归属行
**And** 开关副文案说明「保存在 Diary 且展示在探索」
**And** 主按钮为 **Bagikan（分享）**

### AC2 — 关闭同步时主按钮文案联动（L2 · UX-DR16，PRD 未提及）

**Given** UI 稿 P2 屏
**When** 用户**手动关闭**同步开关
**Then** 主按钮文案由 **Bagikan（分享）变为 Simpan（保存）**
**And** 该条内容发布后**仅进成长档案时间线**，不进 Discovery、不进话题聚合页、不进任何平台公开位
**And** 作者本人可在「我的发布」看到该条
> ⚠️ 该文案联动为 **UI 稿独有要求**，PRD 未提及，须落 AC 不得遗漏。

### AC3 — 无宠物档案默认 Moment（L2 · UX-DR15）

**Given** UI 稿 P1b 屏
**When** **无宠物档案**的用户（状态 B/C 或状态 A 未建档）进入发布页
**Then** **默认选中 Moment**
**And** Diary 选项维持 V1.0.0 的**灰置状态**与「需要先创建宠物档案」提示
**And** 点击 Diary 灰置项跳转建档引导的既有逻辑**不变**
> 理由：FR-83 若照字面「默认 Diary」实现，无档案用户会出现「默认选中一个灰色不可点的选项」。

### AC4 — Moment / Tips 不出现同步开关（L2 · UX-DR17）

**Given** UI 稿 P3 屏
**When** 用户选中 Moment 或 Tips
**Then** **不出现**同步开关（二者本就公开进 Feed）
**And** 显示「🌐 公开发布，展示在探索」提示

### AC5 — 类型选择器顺序调整（L0 · UX-DR19）

**Given** 类型选择器现状顺序为 Moment / Tips / Diary
**When** 实现完成
**Then** 调整为 **Diary / Moment / Tips**（与默认选中项及 FR-83 表述一致）

### AC6 — 与既有 Tab 语境预选逻辑合并口径（L0）

**Given** 既有逻辑「在成长档案 Tab 点 [+] 且有档案 → 预选成长日历」（bug 20260703-244）与 FR-83「全局默认 Diary」**重叠且口径不同**
**When** 实现完成
**Then** 两者**合并为单一口径**并在代码中注明
**And** **不存在**两处互相覆盖的默认类型判定
> 本版本明确**不改**发布入口位置（OQ-10 暂不做），但既有预选逻辑是该方向的雏形，合并后为日后改造留单一入口。

### AC7 — 同步状态发布后不可更改（L1 · FR-83）

**Given** 同步状态**发布时一次性确定**
**When** 内容已发布
**Then** 内容详情页、「我的发布」列表、内容编辑流程中**均不出现**任何同步开关或「转为私密 / 取消同步」入口
**And** 用户让内容不再被他人看到的**唯一途径是删除该条内容**
> 设计取舍：不做事后可切换，是为避免「已公开内容被撤回」的衍生问题（他人已产生的点赞评论如何处置、已分享出去的卡片链接失效等）。

### AC8 — 「我的」页私密与公开区分（L2 · UX-DR18）

**Given** UI 稿 P4 屏
**When** 作者查看「我的」页
**Then** **完整展示自己全部 Diary（含未同步的私密内容）**
**And** 私密内容与公开内容有**明确的视觉区分标识**
**And** 页面按真实 `me_page` 结构，含 ProfileHeadCard、AKTIVITAS 入口组、POSTINGANKU 列表
**And** 「我的发布」排序仍按 `created_at` 倒序（CROSS-STORY F9，不与档案的 `event_date` 混用）

### AC9 — 不绕过内容审核（L1 · CROSS-STORY F10）

**Given** 发布须走三方自动审核（文字关键词 + 图像识别 stub）
**When** 本 Story 改动发布页
**Then** 审核流程**未被绕过**
**And** **私密 Diary 同样经过审核**

### AC10 — 图片区维持现状（L0）

**Given** 本版本发布页图片区维持 V1.0.0 现状
**When** 实现完成
**Then** **未提前适配** FR-71 的图片比例规范化与裁剪流程（已随内容运营能力整体移至 V1.1.4）
**And** 不预留、不提前改造图片区

---

## Tasks / Subtasks

### 🟩 前端子任务（petgo_app / Flutter）

- [x] **F1. 类型选择器顺序与默认值** (AC: 1, 3, 5, 6)
  - [x] `features/content/presentation/publish_compose_page.dart`：选择器顺序改 Diary / Moment / Tips。
  - [x] 默认值单一口径：有档案 → Diary；无档案 → Moment（Diary 灰置 + 提示 + 跳建档）。
  - [x] 合并 `app_shell.dart _onAddPressed` 的 `preset` 预选逻辑与本页默认值判定，**只留一处**；在代码注明合并结论。

- [x] **F2. 同步开关** (AC: 1, 2, 4)
  - [x] 选中 Diary 时渲染「同步到 Moment」开关，**默认开启**；副文案按 P1 稿。
  - [x] 选中 Moment / Tips 时不渲染开关，改显「公开发布，展示在探索」提示。
  - [x] 开关状态 → 提交时映射为 `visibility`（开=PUBLIC，关=PRIVATE）。

- [x] **F3. 主按钮文案联动** (AC: 2)
  - [x] 开关开 → `Bagikan`；开关关 → `Simpan`。双语 i18n key。
  - [x] widget test 锁定该联动（PRD 未写，靠测试防回归）。

- [x] **F4. 发布后无事后开关** (AC: 7)
  - [x] 核查内容详情页、我的发布列表、编辑流程**均无**同步开关或转私密入口。
  - [x] 加 widget test 锁定「不存在该入口」。

- [x] **F5. 「我的」页私密标识** (AC: 8)
  - [x] `features/me/presentation/me_page.dart`：POSTINGANKU 列表按 P4 稿渲染私密标识。
  - [x] 确认列表完整含私密内容（依赖 4.1 AC4 后端不过滤）。
  - [x] 排序仍按 `created_at` 倒序。

- [x] **F6. 数据层** (AC: 1, 2, 8)
  - [x] 发布请求携带 `visibility`。
  - [x] ⚠️ **内容模型的 `visibility` 镜像已归 Story 4.1**（其 AC7 契约三处同步），本 Story **直接使用，不重复定义**。

- [x] **F7. 测试** (AC: 2, 3, 5, 6, 7, 9)
  - [x] widget test：有档案/无档案两种默认值；开关文案联动；Moment/Tips 无开关。
  - [x] widget test：不存在事后同步开关。
  - [x] 回归 test：审核流程仍被调用（含私密 Diary）。

### 🟨 联调验收子任务

- [x] **J1（L1）**：真后端发一条关同步的 Diary → Feed 查不到、成长档案与我的发布能看到。
- [x] **J2（L1）**：发一条开同步的 Diary → 两处都在；删除任一处两处同步消失（AD-14）。
- [x] **J3（L2）**：模拟器核对 P1 / P1b / P2 / P3 / P4 五屏。

### 🟦 后端子任务

- ❌ **无**。后端归 Story 4.1。

---

## Dev Notes

### 关键约定

- **默认开启 = 与现状一致**。这次反转后，用户侧**无感知变更**——老用户发布结果和以前一样，想私密的人才需要手动关。文案上**不宜再把「私密」当核心卖点主打**（FR-83 对 FR-80 文案方向的约束）。
- **主按钮文案联动是 UI 稿独有的**。它其实很对——不同步的日记确实不是在"分享"。靠 widget test 防回归。
- **口径合并要留痕**：合并后在代码注明，否则下一个人看到 bug 20260703-244 的注释会以为逻辑还在那里。

### 强制护栏（违反即返工）

- **发布后不得提供任何事后同步开关**（FR-83 明确；下游 V1.1.4 依赖「不存在运行中状态翻转」这一确定性）。
- 无档案用户**不得**默认选中灰置的 Diary。
- **不得绕过内容审核**（F10）。
- **不得**提前适配 FR-71 图片区改造（属 V1.1.4）。
- 「我的发布」排序按 `created_at`，不与档案 `event_date` 混用（F9）。

### Project Structure Notes

- 前端修改：`features/content/presentation/publish_compose_page.dart`（顺序/默认/开关/按钮文案）、`shared/widgets/app_shell.dart`（预选口径合并）、`features/me/presentation/me_page.dart`（私密标识）、内容模型与发布请求 DTO、`l10n/app_{en,id}.arb`。
- 前端新增测试：默认值 test、开关文案联动 test、无事后开关 test。

### References

- [Source: epics-v1.1.2.md#Story 4.2] — 10 条 AC 原文。
- [Source: architecture-v1.1.2-delta.md#AD-4 Rule 5~8] — 发布时一次性确定 / Feed 取消状态过滤 / 发布页默认类型 / ContentType 不改名。
- [Source: architecture-v1.1.2-delta.md#§1.5] — 既有 Tab 语境预选逻辑与 FR-83 重叠。
- [Source: PRD-v1.1.2.md#FR-83] — 同步开关、发布后不可更改、无档案默认 Moment、类型选择器顺序。
- [Source: ui-v1.1.2.html#P1 / #P1b / #P2 / #P3 / #P4] — 五屏视觉稿（P2 含主按钮文案联动）。
- [Source: CROSS-STORY-DECISIONS.md#F9 / #F10] — 排序口径 / 内容审核。
- [Source: publish_compose_page.dart / app_shell.dart / me_page.dart] — 现状实现。

## Dev Agent Record

### Agent Model Used

claude-opus-5[1m]（本地 dev-story；L0 全绿）

### Debug Log References

- **前端**：`flutter analyze` 零问题；新增 `test/content/publish_sync_switch_test.dart` **6 绿**；全量 **590 绿**。
- **后端**：`mvnw -B test` **1491 绿**（创建请求接受 `visibility` 后既有测试零回归）。

### Completion Notes List

- **AC5 类型顺序**改为 **Diary / Moment / Tips**（原 V1.0.0 是 日常 / 科普 / 成长末位），与默认选中项及 FR-83 术语一致。
- **AC3/AC6 默认值单一口径**：`initState` 里一处判定 —— 有 preset 用 preset，否则**有档案 → Diary、无档案 → Moment**。
  ⚠️ 合并结论已写进代码注释：`app_shell._onAddPressed` **只负责传 preset**（它知道用户在哪个 Tab），
  「有无档案 → 默认哪个类型」的判定**只在发布页做**。此前两处各判一次会出现「shell 预选 Growth、页面又落回 Daily」的互相覆盖。
- **AC1/AC2 同步开关**：仅 Diary 渲染、**默认开启**；关 → 发布请求带 `visibility=PRIVATE`。副文案随开关状态切换（开=「保存在 Diary 且展示在探索」／关=「只存在你的 Diary，别人看不到」）。
  **主按钮文案联动**开=`Bagikan`／关=`Simpan`（PRD 未提、UI 稿 P2 独有 → 有断言防回归）。
- **AC4** Moment / Tips 不渲染开关，改显「🌐 公开发布，展示在探索」提示。
- ⚠️ **切类型时开关复位为开启**：关掉后切到 Moment/Tips，若不复位，「看不见的开关」会把公开内容偷偷发成私密（有断言）。
- **AC7 发布后不可更改**：全链路只有发布页一个设置点 —— 后端 `visibility` 只在**创建请求**里接受（无更新端点），`MyPost` 只读、无 setter；详情页 / 我的发布 / 编辑流程都没有「转为私密」入口。
- **AC8 私密标识**：我的发布网格右上角「🔒 仅自己可见」（与左上角类型 badge 并存，一条内容可同时是 Diary + 私密）。列表完整含私密内容（依赖 4.1 的作者自视不过滤），排序仍按 `created_at` 倒序。
- 🔧 **补上 4.1 漏的一环（后端）**：4.1 只加了字段与查询过滤，**创建请求并不接受 `visibility`** —— 开关不接后端等于没做。本 Story 补 `ContentPostCreateRequest.visibility`（可空、省略即公开）+ service 落值，并保留两个兼容构造器（老客户端/后台补发/既有测试不传即公开）。审核流程不因私密而跳过（只设可见范围，不动 `status`）。
- 📌 **J1~J3 真机联调待验收**：需要一个已建档账号才能发 Diary（本机测试账号刻意保持「有宠无档案」）。开关→visibility 的映射、文案联动、私密标识数据面均已由 L0 断言锁定。

### File List

**前端（修改）**：`content/data/content_repository.dart`（`syncToMoment` → `visibility`）、`content/domain/publish_controller.dart`（开关状态 + 切类型复位 + `isSharing`）、`content/presentation/publish_compose_page.dart`（类型顺序 / 默认值单一口径 / 同步开关 / 公开提示 / 主按钮文案联动）、`me/data/my_posts_repository.dart`（+`visibility`/`isPrivate`）、`me/presentation/me_page.dart`（私密标识）、`l10n/app_{en,id}.arb`（+6 key）
**前端（测试）**：新增 `test/content/publish_sync_switch_test.dart`（6）；修改两个既有 fake repo 的签名
**后端（修改）**：`content/dto/ContentPostCreateRequest.java`（+`visibility` 与兼容构造器）、`content/service/ContentService.java`（创建时落可见范围）
**规划产物**：本 story 文件、`sprint-status-v1.1.2.yaml`

## Change Log

| 日期 | 变更 | 说明 |
|---|---|---|
| 2026-08-02 | create-story | 依据 epics-v1.1.2 Story 4.2 + AD-4 + UI 稿 P 组生成。baseline=b324308a。 |
| 2026-08-03 | 修订 | `visibility` 的 App data DTO 镜像归属改为明确指向 Story 4.1，本 Story 只使用不重复定义。 |
| 2026-08-04 | dev-story | 发布页：类型顺序改 Diary/Moment/Tips、默认值收敛为单一口径（shell 只传 preset）、Diary 加默认开启的「同步到 Moment」开关（关→visibility=PRIVATE）、主按钮 Bagikan↔Simpan 联动、Moment/Tips 显公开提示；我的发布加「🔒 仅自己可见」标识。顺带补上 4.1 漏的一环：后端创建请求接受 visibility（省略即公开，审核流程不跳过）。L0：前端 590 绿（新增 6）/ 后端 1491 绿。 |
