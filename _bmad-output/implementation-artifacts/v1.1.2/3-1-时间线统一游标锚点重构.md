---
baseline_commit: b324308a
---

# Story 3.1: 时间线统一游标锚点重构（修复现网翻页缺陷）

Status: ready-for-dev

> **所属**：V1.1.2 Epic 3 第一个 Story（**纯后端 · 本版本最大技术风险**）。交付：把时间线的排序键与游标键统一为同一把尺子。
> ⚠️ **这不是新功能，是修一个现网缺陷**。`TimelineService.getTimeline` 用 `createdAt` 作游标拉取，却用 `effectiveDate`（快乐时刻取 `eventDate`、健康事件取 `createdAt`）排序——**两把尺子**。用户补记一篇旧日期的日记（`eventDate` 旧、`createdAt` 新），翻页时该条会丢失或重复。
> ⚠️ **必须先于 Story 3.2**。游标不修好就接新数据源，等于在坏地基上盖楼——四源会把出错概率显著放大。
> ⚠️ **改的是已上线的查询路径**，回归测试不到位会伤到现有用户的时间线。

## Story

As a **会补记旧日期日记的用户**,
I want **往下翻时间线时条目不会莫名消失或重复出现**,
so that **我能相信这本册子记全了我家宠物的事（FR-82 多数据源合并分页口径）**。

## Acceptance Criteria

> **验证层**：**L0**（service 单测，纯逻辑归并/截断）· **L1**（Docker pg：真数据跨页拉取、回归断言）。本 Story 无 L2（纯后端）。

### AC1 — 统一游标锚点（L1 · AD-1 Rule 1）

**Given** 现状排序键为 `effectiveDate`、游标键为 `createdAt`，两者不一致
**When** 重构完成
**Then** 游标锚点**唯一**，取「**事件日期 + 同日排序键**」复合值
**And** 各数据源用**同一锚点**取数，**不使用各源自己的主键或 `createdAt` 游标**
**And** 游标对外不可枚举、不暴露内部主键（继承对外标识护栏）

### AC2 — 归并后再截断（L0/L1 · AD-1 Rule 2）

**Given** 多源归并
**When** 取一页数据
**Then** 顺序为：按锚点从各源各取一批 → **归并排序** → **统一截断**到页大小 → 以截断处锚点作为下一页游标
**And** **不存在**「各源先截断再归并」的实现
> 验证层：**L0**（纯逻辑单测：构造多源乱序输入，断言归并与截断顺序）。

### AC3 — 同日条目不可跨页拆分（L1 · AD-1 Rule 3）

**Given** 同日条目不可跨页拆分
**When** 某日条目数超过单页容量
**Then** 该日**整体作为一页返回**（允许超出默认页大小）
**And** 页边界**始终落在日期分界上**
**And** 不因同日排序键相同而产生重复或丢失
> 本版本**不设**单日硬上限（架构 §7 Deferred：真实数据下单日条目数极少超过个位数）。

### AC4 — 跨页无丢失无重复回归断言（L1 · NFR-1，本 Story 核心验收）

**Given** 用户补记一篇旧日期的日记（`eventDate` 为上月、`createdAt` 为今天）
**When** 逐页拉完整条时间线
**Then** 该条**恰好出现一次**，位置在其**事件日期**对应的时序位置
**And** 全量条目集合与**不分页查询**的结果集合**完全相等**（无丢失、无重复）
**And** 该断言作为**回归测试常驻**，不得删除
> 建议同时覆盖：同日多条、跨月边界、页大小恰好等于某日条目数三种边界。

### AC5 — 排序口径不混用（L0 · CROSS-STORY F9）

**Given** F9 规定 `event_date` 与 `created_at` 分离
**When** 实现完成
**Then** 成长档案时间线 / 日历 / 当天详情按 **`event_date`**；Feed 与「我的发布」仍按 **`created_at` 倒序**
**And** 两套口径**未被混用**
**And** 同日排序键沿用 FR-37 既有「按发布/完成时间正序」口径

### AC6 — 既有行为零回归（L1）

**Given** 时间线已上线
**When** 重构完成
**Then** 现有两类条目（快乐时刻 + 问诊健康事件）的展示内容、字段、交互与改版前一致
**And** `profile` 域既有测试全绿
**And** 对外响应字段集未变（本 Story 不新增字段，`itemType` 归 Story 3.2）

---

## Tasks / Subtasks

> **纯后端**。顺序：抽锚点模型 → 改各源取数签名 → 归并/截断重写 → 回归测试。

### 🟦 后端子任务（petgo-backend / Spring Boot）

- [ ] **B1. 复合锚点模型** (AC: 1)
  - [ ] `profile/service/`：定义时间线游标锚点（事件日期 + 同日排序键）的编解码，对外为不可枚举串。
  - [ ] 解析失败 → 422 ProblemDetail（沿用现有 `parseCursor` 的错误语义）。

- [ ] **B2. 各源取数改用统一锚点** (AC: 1, 2)
  - [ ] `TimelineService.getTimeline`：`contentService.findGrowthMoments` 与 `HealthEventTimelineSource.recentHealthEvents` 的取数条件由 `before: Instant` 改为按复合锚点。
  - [ ] 各源仍**多取一批**用于归并稳健性，但**不在源内截断**。

- [ ] **B3. 归并 + 日界截断重写** (AC: 2, 3)
  - [ ] 归并排序键 = 事件日期倒序 + 同日排序键正序（FR-37 口径）。
  - [ ] 截断落在日期分界：从页大小位置向后延伸到当日末尾；该日整体返回。
  - [ ] `nextCursor` 取截断处锚点。

- [ ] **B4. 测试** (AC: 2, 3, 4, 6)
  - [ ] **L0** 纯逻辑单测：多源乱序输入 → 归并顺序正确；截断不切断同日；页大小恰等于某日条目数的边界。
  - [ ] **L1** 回归断言（AC4，常驻）：种子含「补记旧日期日记」，逐页拉全 vs 不分页查询，集合完全相等。
  - [ ] **L1** 边界：同日多条、跨月边界。
  - [ ] **L1** 回归：`com.tailtopia.profile.**` 既有测试全绿。

### 🟩 前端子任务

- ❌ **无**。游标格式变化对前端透明（前端只透传 `nextCursor`）。若前端对游标做了格式假设，本 Story 须一并核实并移除该假设。

### 🟨 联调验收子任务

- [ ] **J1（L1）**：Docker pg + Redis 起真库，跑 AC4 全量断言。
- [ ] **J2（L1）**：App 连真后端翻时间线，确认现有条目展示与翻页体验无回归。

---

## Dev Notes

### 关键约定

- **这是修 bug，不是加功能**。story 的成败看 AC4 那条回归断言，不看新增了什么。
- **不新增对外字段**。`itemType` 属 Story 3.2，本 Story 只动取数与分页，响应字段集保持不变（降低回归面）。
- **同日整页返回是有意为之**：宁可某一页略大，也不能让同日条目被拆到两页后因排序键相同而重复或丢失。

### 为什么现网就是坏的（估工必读）

`getTimeline` 里：游标 `parseCursor(cursor)` 得到 `Instant`，各源按 `before`(createdAt) 取数；归并后却 `sort(effectiveDate → date)`。`effectiveDate` 对快乐时刻取 `eventDate`。于是「eventDate 旧、createdAt 新」的补记日记，在排序上落到旧位置、在翻页上被当作新记录 → 跨页丢失或重复。FR-82 把源从 2 增至 4 只是**放大**该缺陷。

### 强制护栏（违反即返工）

- 游标对外不可枚举、不暴露内部主键。
- 禁止各源先截断再归并。
- AC4 回归断言常驻，不得在后续 story 中删除或弱化。
- 不引入新中间件；`ddl-auto=validate`（本 Story 无迁移）。

### Project Structure Notes

- 后端修改：`profile/service/TimelineService.java`（锚点/取数/归并/截断）、`content/service/ContentService.java` 与 `profile/service/HealthEventTimelineSource.java` 的取数签名（按锚点）。
- 后端新增测试：`TimelineCursorMergeTest`（L0）、`TimelinePaginationRegressionTest`（L1，AC4 常驻）。
- **无 Flyway 迁移。**

### References

- [Source: epics-v1.1.2.md#Story 3.1] — 6 条 AC 原文。
- [Source: architecture-v1.1.2-delta.md#AD-1] — 统一游标锚点五条 Rule + 验收硬性要求。
- [Source: architecture-v1.1.2-delta.md#§1.1] — 现网缺陷的代码事实与复现路径。
- [Source: PRD-v1.1.2.md#FR-82「多数据源合并分页口径」] — 统一游标锚点 / 同日不跨页 / 归并后再截断。
- [Source: CROSS-STORY-DECISIONS.md#F9] — event_date 与 created_at 的排序口径分离。
- [Source: TimelineService.java] — 现状实现（缺陷所在）。

## Dev Agent Record

### Agent Model Used

_(待填)_

### Debug Log References

_(待填)_

### Completion Notes List

_(待填。须记录 AC4 回归断言的测试类名与覆盖的边界场景。)_

### File List

_(待填)_

## Change Log

| 日期 | 变更 | 说明 |
|---|---|---|
| 2026-08-02 | create-story | 依据 epics-v1.1.2 Story 3.1 + AD-1 生成；修现网缺陷。baseline=b324308a。 |
