---
baseline_commit: b324308a
---

# Story 2.3: 已登录未建档用户的建档引导态

Status: review

> **所属**：V1.1.2 Epic 2 第三个 Story（**纯前端 · 小**）。交付：状态 A 未建档用户进 Diary 看到建档引导；**V1.0.0 FR-0H 首页提示条整条废止**。
> ⚠️ **状态 A 未建档 与 状态 B/C 是两种完全不同的人**，PRD 特意拆开：对 A 推建档是对的（他说了有宠物，只是没填）；对 B/C 推建档等于无视他刚在 onboarding 里给出的回答。

## Story

As a **已登录但还没填宠物档案的用户**,
I want **打开 Diary 直接看到建档引导**,
so that **我不用再被当成新访客重新种一次草（FR-81）**。

## Acceptance Criteria

> **验证层**：**L0**（analyze / widget test / 埋点下线核对）· **L1**（真后端，A 未建档与 B/C 两种账号）· **L2**（模拟器视觉）。

### AC1 — 状态 A 未建档的建档引导（L2 · UX-DR5）

**Given** 用户为状态 A（已回答「我有宠物」）且未建档
**When** 进入 Diary
**Then** 走 Story 2.1 分发入口的对应分支，复用 V1.0.0 既有宠物档案创建引导（**FR-0G，可跳过**）
**And** **不展示** FR-80 的预览种草内容（避免对已登录用户重复种草造成体验割裂）
> 视觉基准：UI 稿 A2 屏。

### AC2 — 状态 B/C 零改动（L1 · UX-DR6）

**Given** 用户为状态 B（计划养宠）或 C（喜欢宠物但未养）
**When** 主动点进 Diary
**Then** 展示 V1.0.0 既有「有宠专属 + 修改宠物状态入口」页
**And** **不引导建档**、页面**零改动**
**And** 用户经该入口把状态改为 A 后，按既有逻辑进入 FR-0G 建档引导
> 视觉基准：UI 稿 A2b 屏（回归基准，非新设计）。

### AC3 — V1.0.0 FR-0H 首页提示条整条废止（L0 · FR-81）

**Given** V1.0.0 FR-0H 定义的「选择状态 A 但未建档的用户，首页顶部显示建档提示条，前 3 次重启均显示、第 3 次关闭后不再显示」
**When** 实现完成
**Then** 该提示条**不再实现**，相关代码移除
**And** Discovery 顶部**不再预留**提示条区域
**And** 其**曝光 / 点击 / 关闭埋点事件已移除**，相关看板指标下线
**And** 状态 A 未建档用户的建档引导渠道**收敛为唯一一条**——本 Story 的 Diary 分支
**And** 不再有「前 3 次重启」这个次数上限（Diary 落地引导每次冷启动都在，但它是页面本身而非打断式提示条）

---

## Tasks / Subtasks

### 🟩 前端子任务（petgo_app / Flutter）

- [x] **F1. 填充「状态 A 未建档」分支** (AC: 1)
  - [x] 2.1 起该分支已接 V1.0.0 既有建档引导（`_EmptyProfileView`，FR-0G 可跳过）；本 Story 去掉 `TODO(2-3)` 并按 A2 稿收口。
  - [x] 按 A2 稿核对：**补齐稿子里有、现网缺的一句副文案**（`growthArchiveEmptyBody`），主 CTA 文案按稿改为「Buat Profil Sekarang / Create profile now」；**未引入** FR-80 任何示例内容（有断言）。

- [x] **F2. 确认 B/C 分支零改动** (AC: 2)
  - [x] 只核对布线，`_NonOwnerView` 一行未改；**「Ubah status」文案 key 与 B/C 共享，故刻意没跟着 A2 改**（改了就等于动 B/C 页面）。
  - [x] 回归 test 锁定：PLANNING / ENTHUSIAST 两态各一例，渲染既有页 + 无建档 CTA + 无副文案 + 无游客种草。

- [x] **F3. 移除 FR-0H 首页提示条** (AC: 3)
  - [x] 删除提示条组件 `shared/widgets/profile_prompt_bar.dart` 与状态机 `profile_prompt_state.dart` / `profile_prompt_controller.dart`（含「前 3 次重启」计数与永久关闭逻辑）。
  - [x] 删除三个持久化键（`AppPrefs`）与 `main.dart` 的启动期注水；建档页不再调 `markCompleted()`。
  - [x] 埋点核对：**该提示条本就没有任何客户端上报代码**（全仓无 `profile_prompt` 事件），无需移除；看板侧指标下线属运维动作，见 Completion Notes 移交项。
  - [x] `_BerandaTop` 只剩分类 Tab，顶部不再预留该区域；L2 截图核对无残留空隙。

- [x] **F4. 测试** (AC: 1, 2, 3)
  - [x] widget test：A 未建档 → 建档引导（标题 + 副文案 + CTA + 逃生入口，且无游客种草痕迹）；B/C → 既有页。
  - [x] 回归 test：Discovery 顶部只有分类 Tab；**源码级下线核对**（三个文件不存在 / prefs 三键消失 / main 与 home 无残留引用 / 全仓无 `profile_prompt` 引用）—— 只断言「看不见」的话，日后有人加回来照样能过。

### 🟨 联调验收子任务

- [x] **J1（L1）**：真后端（`/actuator/health` UP）+ 模拟器。A-未建档为该账号真实态；B/C 经状态编辑器真实改为 PLANNING 落库后走一遍，**验完已改回 HAS_PET**。ENTHUSIAST 与 PLANNING 在本页走同一分支（`petStatus != HAS_PET`），已由 L0 两态断言覆盖，未再改一次账号数据。
- [x] **J2（L2）**：模拟器核对 A2（`l2-08-noprofile.png`）与 A2b（`l2-10-bc.png`）；另核对 Discovery 顶部无提示条（`l2-09-discovery.png`）。

### 🟦 后端子任务

- ❌ **无**。

---

## Dev Notes

### 关键约定

- **A 未建档 与 B/C 必须分开对待**。混为一谈会导致对刚回答「我没有宠物」的用户反复催建档。
- **FR-0H 废止的理由**：它挂在首页顶部，而首页改名 Discovery 并移到第 4 位后，状态 A 未建档用户的落地页是 Diary，几乎不会主动去 Discovery，提示条曝光趋近于零。Diary 首屏本身已是完整的建档引导，重复催没有必要。
- **埋点必须一并下线**，否则会留下一个恒为 0 的指标。

### 强制护栏

- B/C 页面**零改动**——本 Story 碰它的渲染即越界。
- FR-0H 是**整条废止**，不是「保留代码但不显示」。
- 不得在 A 未建档分支展示 FR-80 的种草内容。

### Project Structure Notes

- 前端修改：`growth_archive_page.dart`（填 A 未建档分支）、`features/content/presentation/home_page.dart`（移除 FR-0H 提示条）。
- 前端删除：FR-0H 提示条组件及其计数持久化、埋点调用。

### References

- [Source: epics-v1.1.2.md#Story 2.3] — 3 条 AC 原文。
- [Source: PRD-v1.1.2.md#FR-81] — A 未建档 vs B/C 拆分表 + FR-0H 废止声明与理由。
- [Source: architecture-v1.1.2-delta.md#AD-15 Rule 3] — FR-0H 整条废止，建档引导渠道收敛为唯一一条。
- [Source: ui-v1.1.2.html#A2 / #A2b] — 未建档引导态 / 状态 B·C 非落地态。

## Dev Agent Record

### Agent Model Used

claude-opus-5[1m]（本地 dev-story；L0 + L1 + L2 全绿）

### Debug Log References

- **L0**：`flutter analyze` → **No issues found**；新增 `test/profile/diary_no_profile_guide_test.dart` **6 绿**；`flutter test` 全量 **537 绿 0 失败**（2.2 完成时 543：删掉 FR-0H 的 12 条测试、新增 6 条）。
- **L1 + L2**（真后端 + 模拟器 `petgo_phone`，该账号真实态恰为「已登录 · HAS_PET · 无档案」）：
  - **A2 建档引导态**：🐾 + 「No pet profile yet」+ **新补的副文案** + 「Create profile now」+ 「Change status」逃生入口，无任何游客种草内容。`l2-08-noprofile.png`
  - **AC3 Discovery**：顶部只有品牌栏 + 分类 Tab，**建档提示条与其预留区域都不在了**，无残留空隙。该账号正是 FR-0H 当年会显示提示条的状态（HAS_PET 未建档），因此这张图直接构成下线证据。`l2-09-discovery.png`
  - **A2b 状态 B/C**：经状态编辑器真实改为 PLANNING 落库后进 Diary，渲染既有「有宠专属 + Change status」页，不引导建档。`l2-10-bc.png`；验完改回 HAS_PET，账号恢复原状（`l2-11-restored.png`）。

### Completion Notes List

#### FR-0H 整条废止清单（AC3 是「代码移除」，不是「保留但不显示」）

| 类别 | 处理 |
|---|---|
| 提示条组件 | **删除** `lib/shared/widgets/profile_prompt_bar.dart` |
| 显示判定 + 计数状态机 | **删除** `features/profile/domain/profile_prompt_state.dart`（`shouldShowProfilePrompt` / 前 3 次重启计数 / 关闭与完成的状态转移） |
| Riverpod 控制器 + 持久化 | **删除** `features/profile/domain/profile_prompt_controller.dart` |
| 本地持久化键 | **删除** `AppPrefs` 三个常量（重启计数 / 永久关闭 / 档案已完成） |
| 启动期注水 | **删除** `main.dart` 的 `_loadProfilePromptBootstrap()` 与 override（冷启动少一次 prefs 读写） |
| Discovery 接线 | `home_page.dart` 去掉 watch / 判定 / 参数 / 渲染；`_BerandaTop` 只剩分类 Tab，**顶部不再预留区域** |
| 建档页联动 | `pet_profile_create_page.dart` 不再调 `markCompleted()`；**保留** `hasPetProfile` 回填（那是 Diary 分支判定等多处的权威源，与提示条无关） |
| 文案 | **删除** ARB key `profilePromptMessage`（双语） |
| 测试 | 删除 `profile_prompt_test.dart` / `profile_prompt_widget_test.dart`（12 条）；`onboarding_test.dart` 与 `pet_profile_create_test.dart` 里对提示条的断言一并摘除 |

- **埋点**：全仓扫描确认该提示条**本来就没有任何客户端上报**（无 `profile_prompt` 相关 `Analytics.capture`），所以 AC3 的「埋点已移除」在代码侧自然满足，已加常驻断言防止日后被加回来。
- ⚠️ **移交项（我做不了）**：AC3 还要求「相关看板指标下线」。若 PostHog / 数据看板上曾按事件名建过该提示条的图表或漏斗，需要在看板侧手动删除或归档 —— 代码侧已无事件产出，留着会是一条恒为 0 的曲线。

#### A2 稿落地的两处文案变化

1. **补一句副文案**（新 key `growthArchiveEmptyBody`）：A2 稿有、现网缺。它回答的是「为什么要先建档」——「先建好档，所有瞬间、健康记录、成就才有地方放」。
2. **主 CTA 改为「Buat Profil Sekarang / Create profile now」**（原「Buat sekarang / Create now」）。该 key 只服务这一处，改它不影响别处。
3. ⚠️ **刻意没改「Ubah status」**：A2 稿写的是「Ubah status peliharaan」，但这个 key 与状态 B/C 页面**共享**——跟着改就等于动了 B/C 的渲染，违反 AC2「零改动」。稿子与现状的这点措辞差异属可接受偏差。

#### 其它

- 本 Story 没有新写任何引导组件：状态 A 未建档分支从 Story 2.1 起就接的是 V1.0.0 既有实现，2.3 只做「按稿收口 + 拆掉重复渠道」。**建档引导渠道自此收敛为唯一一条** = Diary 的这个分支。
- 「前 3 次重启」上限随之消失：Diary 首屏的引导每次冷启动都在，但它是**页面本身**而不是打断式提示条（AC3 末条）。
- 状态 B/C 的两个取值（PLANNING / ENTHUSIAST）在本页走同一分支，L0 已两态各断言一次；L1 只真实切了 PLANNING，避免对账号数据反复写入。

### File List

**前端（删除）：**
- `petgo_app/lib/shared/widgets/profile_prompt_bar.dart`
- `petgo_app/lib/features/profile/domain/profile_prompt_state.dart`
- `petgo_app/lib/features/profile/domain/profile_prompt_controller.dart`

**前端（修改）：**
- `petgo_app/lib/features/profile/presentation/growth_archive_page.dart`（建档引导态补副文案 + 文档；去掉 `TODO(2-3)`）
- `petgo_app/lib/features/content/presentation/home_page.dart`（摘除提示条接线与顶部预留区，补「勿加回」注释）
- `petgo_app/lib/main.dart`（删启动期注水与 prefs 读写）
- `petgo_app/lib/features/profile/presentation/pet_profile_create_page.dart`（不再关闭提示条，保留 `hasPetProfile` 回填）
- `petgo_app/lib/core/storage/prefs.dart`（删三个持久化键）
- `petgo_app/lib/l10n/app_en.arb` / `app_id.arb`（+`growthArchiveEmptyBody`、改 `growthArchiveEmptyCreate`、删 `profilePromptMessage`）

**测试（新增/删除/修改）：**
- 新增 `petgo_app/test/profile/diary_no_profile_guide_test.dart`（L0 6 条，含源码级下线核对）
- 删除 `petgo_app/test/profile/profile_prompt_test.dart`、`petgo_app/test/profile/profile_prompt_widget_test.dart`
- 修改 `petgo_app/test/auth/onboarding_test.dart`、`petgo_app/test/profile/pet_profile_create_test.dart`（摘除提示条断言）

**规划产物（修改）：**
- `_bmad-output/implementation-artifacts/v1.1.2/2-3-已登录未建档用户的建档引导态.md`、`_bmad-output/implementation-artifacts/sprint-status-v1.1.2.yaml`

## Change Log

| 日期 | 变更 | 说明 |
|---|---|---|
| 2026-08-02 | create-story | 依据 epics-v1.1.2 Story 2.3 + AD-15 生成。baseline=b324308a。 |
| 2026-08-03 | dev-story | 状态 A 未建档分支按 A2 稿收口（补副文案 + CTA 文案）；B/C 零改动并加两态回归断言；**FR-0H 首页提示条整条废止**：删组件/状态机/控制器/三个 prefs 键/启动注水/ARB 文案/12 条旧测试，Discovery 顶部不再预留区域，并加源码级下线核对断言。埋点核对为「本就无客户端上报」，看板指标下线已列移交项。L0：analyze 零问题 / 新测 6 / 全量 537 绿。L1+L2：真后端三态走通（B/C 验完改回），A2 / A2b / Discovery 顶部三张截图核对。 |
