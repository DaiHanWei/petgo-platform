---
baseline_commit: d9353457
---

# Story 7.1: 品牌资产重导与原生启动屏无缝交接

Status: review

> **所属**：V1.1.2 **Epic 7（Splash 启动屏改版）第一个 Story** —— **纯前端 + 原生配置 · 零 schema · 零后端改动 · 零新依赖**。
> 交付：消除每次冷启动必现的白闪（**含深色模式**），并把品牌资产换回母文件版本、修掉 mark 的 viewBox 偏心。
> ⚠️ **本 Story 不改动入场动效，也不改 Flutter 首帧落位**（两者均属 Story 7.2）。本 Story 只做「**新增**资产 + 原生配置」两件事。
>
> 🔴 **范围已于 2026-08-04 收窄（story 起草期核实代码后调整，epics 原文的 AC3「首帧落 50%」已移入 7.2）**：
> 现状入场动效是**两个 SVG 叠加**演出来的 —— `mark_dog.svg` 是带**猫形缺口**的狗/T 底标，`mark_cat.svg` 是猫，沿路径跑屏后归位、填充由白翻紫，**正好填进那个缺口做出镂空效果**。猫的落点是按「底标在 43%、声明 108×108」算出的**绝对像素偏移**。
> 因此：**在 7.2 重做动效之前，只要动了底标的资产、尺寸或位置，猫就对不上缺口，旧动效的核心视觉效果直接坏掉** —— 这不是「看着有点怪」，是明显故障。
> **本 Story 因此采取：新资产只喂原生启动屏，不替换 `mark_dog.svg` / `mark_cat.svg`，不动 `_markStage()` 的任何尺寸与定位。**
>
> 🟢 **发布意图（2026-08-04 用户确认）**：本轮是**整体替换旧 splash 效果**，不是在旧效果上做增量。**Epic 7 作为一批发布**，因此下方「mark 跳位」遗留**用户实际不会看到**——它只存在于 7.1 与 7.2 之间的开发窗口内。7.1 的独立可上线性是**安全属性**（保证任何时点仓库都不破损），不是发布计划。
>
> 📌 **已知遗留（由 7.2 消除，本 Story 不算缺陷）**：底色全程为紫、**白闪已消除**；但交接处 mark 会有一次**尺寸/位置跳变**（原生按屏宽 42% 渲染在 50% 正中，Flutter 首帧仍是 108px 居中于 43%）。**跳位比白闪轻，且不破坏任何既有效果**，可接受地独立上线。

## Story

As a **每天打开 App 的用户**,
I want **冷启动第一眼不要闪一下白光**,
so that **App 给我的第一印象是完整的，而不是像卡了一下（FR-87）**。

## Acceptance Criteria

### AC1 — 品牌资产换回母文件并修掉偏心（L0 · FR-90 资产部分 / NFR-16 资产半 / UX-DR20 / 缺陷 B-4、B-5）

**Given** 现有两份资产都不是品牌母文件那一版，且 mark 的 `viewBox` 自带空白与偏心
**When** 重导出资产
**Then** **新增** mark 资产，取 `V1.0.0/ICON SVG.svg` 的 `Layer_7`，导出使用**紧贴 bbox** 的 `viewBox="67.23 84.79 389.32 321.81"`
**And** **新增**字标资产，取 `V1.1.0/FULL LOGO -0624.svg` 的 `Layer_9`（猫尾为粗壮实心月牙、位置更高、动感短划更长）
**And** 🔴 **以「新增文件」方式落地，不得覆盖 `mark_dog.svg` / `mark_cat.svg` / `wordmark.svg`** —— 这三份仍被现状动效使用（原因见本文件顶部范围说明），覆盖即弄坏旧动效。**旧资产的下线由 Story 7.2 在重做动效时一并执行**
**And** ⚠️ **不得靠调大声明尺寸解决「mark 显小」** —— 现状 `viewBox="0 0 500 500"`（已核实）而图形只占 `x 67–456 / y 85–407`，声明 108px 实际可见仅约 84×70 且右偏 12 / 上偏 4；**调大尺寸会把偏心一起放大**

### AC2 — 原生启动屏改紫底白 mark（L0 · FR-87 / UX-DR23 / 缺陷 B-1、B-2、B-3）

**Given** 白闪来自「原生白底 → Flutter 紫底」的落差，且**深色模式同样白闪**（已核实 `values-night-v31/styles.xml` 的 `windowSplashScreenBackground` 亦为 `#FFFFFF`）
**When** 实现完成
**Then** 冷启动全程底色为品牌紫 `#7D45F6`，**浅色与深色模式均如此**
**And** 原生显示的图形为**白色 mark**（AC1 的紧贴 bbox 版），可视宽 = 屏宽 42%，由系统渲染在 50% 正中
**And** Android 12+ 的图标源资产满足规范：**288×288（mdpi 基准），可视内容限 192×192**（现状 mdpi 为 200×200，已核实，会被系统裁切）
**And** iOS `LaunchScreen.storyboard` 同步改底色 + 白 mark，视觉尺寸与 Android 对齐

### AC3 — Flutter 侧零改动（L0 · 本 Story 的关键边界）

**Given** 现状 `_markStage()` 是「带猫形缺口的狗底标 + 沿绝对像素路径跑的猫」两个 SVG 叠加，猫的落点按「底标 43% / 声明 108×108」算出
**When** 本 Story 实现
**Then** **`splash_page.dart` 一行不改** —— 不动 mark 尺寸、不动 `centerY`（`height * 0.43`）、不动 `_markStage()`、不动任何动效切片
**And** 🔴 **「Flutter 首帧落 50% 正中、可视宽 42%」已从本 Story 移出，归 Story 7.2**（epics 文档的 7.1 AC3 原文即此条）—— 原因：改首帧落位会同时改变猫的落点参照，使其对不上缺口，旧动效核心效果损坏
**And** 因此本 Story 交付后 Flutter 首帧仍是「108px 居中于 43%」，与原生那一帧**不同位不同尺寸**，交接处存在一次 mark 跳变（预期遗留，见 AC4）

### AC4 — 真机验收白闪确已消除（L2 · NFR-12 · **必须本地**）

**Given** 深色模式白闪（B-2）长期存在的原因正是当初只验了浅色
**When** 真机冷启动
**Then** **浅色模式**与**深色模式**各验一次，两种模式下**全程无底色闪变** —— 从点图标到 Flutter 首屏，底色恒为 `#7D45F6`
**And** 冷启动定义按 PRD §2.1：首次安装后打开 / 手动划掉后重开 / 被系统回收后重开
**And** 📌 **允许**交接处出现一次 mark 尺寸/位置跳变（原生 42%@50% vs Flutter 108px@43%）——**该跳变由 Story 7.2 消除，本 Story 不作为失败项**
**And** 🔴 验收口径务必分清：**「底色闪白」= 本 Story 必须消除；「mark 跳位」= 本 Story 允许遗留**
**And** 云端 headless 无法执行本条，须在 Completion Notes 标注「L2 待本地验收」

### AC5 — 真机测量系统图标实际渲染尺寸并记录（L2 · OQ-20 · **必须本地**）

**Given** 系统 splash 图标的实际渲染尺寸由系统决定、不可指定，也无法在设计稿中验证
**When** 真机测量
**Then** 量出 Android 12+ 系统实际渲染的图标尺寸，**记录进 Completion Notes 并回写 PRD OQ-20**
**And** 📌 本 Story 只负责**测量与记录**；「把 Flutter 首帧 mark 对齐到实测值」由 **Story 7.2** 执行（首帧落位归它）
**And** 若实测值与「屏宽 42%」不一致，以**实测值**为准，并据此**同步修正原生源资产的可视内容占比**（原生侧属本 Story 范围）
**And** 这是「无缝交接」唯一无法在稿件里验证的环节，**不得跳过** —— 不测则 7.2 无从对齐

### AC6 — 严守本 Story 边界（L0）

**Given** 入场动效重做属 Story 7.2
**When** 本 Story 实现
**Then** **不改动入场动效逻辑**（不动 `animatedTotal` / 时间线切片 / 猫跑屏路点 / 字标淡入 / 标语 / spinner / 版本号）
**And** 已知遗留：旧动效原样继续播（现状总时长 **4320ms**，已核实，非设计稿的 1.54s），终态仍是含 mark 的旧竖排 lockup —— 这是预期的，不算缺陷
**And** 🔴 **判断本 Story 是否越界的最简判据：`git diff` 里不应出现 `lib/` 下任何文件** —— 本 Story 只改 `pubspec.yaml`、新增 `assets/brand/` 资产、以及 `flutter_native_splash:create` 生成的 `android/` 与 `ios/` 产物
**And** `flutter analyze` 零警告、`flutter test` 全绿（现有 splash 相关测试不得因本 Story 失败）

## Tasks / Subtasks

### 🟩 前端子任务（petgo_app / Flutter + 原生配置）

- [x] **T1 产出新资产（全部为新增文件，不覆盖任何既有资产）**（AC1）
  - [x] 从 `V1.0.0/ICON SVG.svg` 导出 `Layer_7` → **新增** `petgo_app/assets/brand/mark_brand.svg`（建议名），`viewBox` 必须是 `67.23 84.79 389.32 321.81`
  - [x] 从 `V1.1.0/FULL LOGO -0624.svg` 导出 `Layer_9` → **新增** `petgo_app/assets/brand/wordmark_brand.svg`（建议名）
  - [x] 产出**白色 mark 的 PNG**（供原生启动屏用）→ **新增** `petgo_app/assets/brand/splash_mark_white.png`（建议名），按 Android 12 规范：**288×288 mdpi 基准、可视内容 ≤192×192**，居中留白
  - [x] 🔴 **`mark_dog.svg` / `mark_cat.svg` / `wordmark.svg` 三份一律不动**（仍被现状动效使用），其下线由 Story 7.2 执行
  - [x] ⚠️ 也不要覆盖 `app_icon.png` / `app_icon_padded.png`（应用图标 + 现状 android_12 源图，另有用途）
  - [x] 核对 `pubspec.yaml` 的 `assets:` 声明是否覆盖新文件（若按目录声明 `assets/brand/` 则无需改；若逐文件声明须补登记）
- [x] **T2 改 pubspec 的 flutter_native_splash 配置**（AC2）
  - [x] `color: "#FFFFFF"` → `"#7D45F6"`
  - [x] `android_12.color: "#FFFFFF"` → `"#7D45F6"`
  - [x] `image` / `android_12.image` 由 `app_icon.png` / `app_icon_padded.png` 改指向 T1 新增的白 mark PNG
  - [x] 🔑 **补 `color_dark` 与 `android_12.color_dark`（同为 `#7D45F6`）** —— 见 Dev Notes「深色模式白闪的可能根因」
- [x] **T3 重新生成原生资源**（AC2）
  - [x] 跑 `dart run flutter_native_splash:create`
  - [x] **生成后逐项核对**：`values-v31/styles.xml` 与 `values-night-v31/styles.xml` 的 `windowSplashScreenBackground` 是否**都**变成 `#7D45F6`
  - [x] 核对 `drawable-mdpi/android12splash.png` 是否已是 288×288（各密度按比例）
  - [x] 核对 iOS `LaunchScreen.storyboard` 与 `LaunchBackground.imageset` / `LaunchImage.imageset` 已更新
- [x] **T4 确认 Flutter 侧零改动**（AC3 / AC6）
  - [x] 🔴 `lib/` 下**一行不改**。首帧落位归 Story 7.2，本 Story 不碰
  - [x] 自检：`git diff --stat` 输出中不应出现任何 `petgo_app/lib/` 路径
- [x] **T5 L0 绿灯**（AC6）
  - [x] `flutter analyze` 零警告
  - [x] `flutter test` 全绿
  - [x] `flutter build apk --debug` 通过

### 🟨 联调验收子任务（**必须本地真机，云端无法执行**）

- [x] **T6 浅色模式冷启动**（模拟器 Android 16/API 36 已验证）：三种冷启动路径各验一次，**全程无底色闪白**（AC4）。mark 跳位属允许遗留，不判失败
- [x] **T7 深色模式冷启动**（模拟器已验证）：同上（AC4）—— **这一条是 B-2 的验收，不可只验浅色**
- [x] **T8 量系统图标尺寸**（模拟器实测 180dp / 43.7% 屏宽，见 Completion Notes）：Android 12+ 真机测量实际渲染尺寸 → **记录进 Completion Notes + 回写 PRD OQ-20**（AC5）。若与 42% 不符，据实测修正原生源资产可视占比；**Flutter 首帧对齐留给 7.2**

### 🟦 后端子任务

- [x] **无。本 Story 零后端改动、零 schema 变更、零迁移。**

## Dev Notes

### 🔑 深色模式白闪的可能根因（实施前先读这条，能省一轮返工）

现状 `pubspec.yaml` 的 `flutter_native_splash` 块**没有任何 `color_dark` / `android_12.color_dark` 配置**（已核实），而 `values-night-v31/styles.xml` 的底色是 `#FFFFFF`。

**推断**：只改 `color` 与 `android_12.color` 而不补 `*_dark`，重新生成后**深色模式可能仍是白底**，B-2 没修掉。故 T2 要求一并补 `color_dark`。

⚠️ 这是基于配置缺失的**推断，不是已验证结论**。T3 要求生成后**逐项核对两个 styles.xml 的实际取值** —— 以生成结果为准，若补了 `color_dark` 仍不对，改为手工修正 `values-night-v31` 并在 Completion Notes 记录（同时说明该文件是生成物、下次 `create` 会被覆盖）。

### ⚠️ 生成物 vs 源文件（最容易踩的坑）

Epic 的改动清单里提到的这几个文件**全部是 `flutter_native_splash:create` 的生成物，改了会在下次生成时被覆盖**：

| 文件 | 性质 | 正确改法 |
|---|---|---|
| `android/app/src/main/res/values-v31/styles.xml` | 生成物 | 改 pubspec 的 `android_12.color` 后重新生成 |
| `values-night-v31/styles.xml` | 生成物 | 改 pubspec 的 `android_12.color_dark` 后重新生成 |
| `drawable-*/android12splash.png`（各密度） | 生成物 | 换 `android_12.image` 指向的**源 PNG**后重新生成 |
| `drawable/background.png` | 生成物 | 改 pubspec 的 `color` 后重新生成 |
| iOS `LaunchBackground/LaunchImage.imageset` | 生成物 | 同上 |

**唯一应该手工编辑的是 `pubspec.yaml` 的配置块与 `assets/brand/` 下的源资产。**

### 现状核实结果（2026-08-04，估工与实施均以此为准）

**原生侧**（`petgo_app/`）：
- `pubspec.yaml:185-191`：`color: "#FFFFFF"`、`image: "assets/brand/app_icon.png"`、`android_12: { image: "assets/brand/app_icon_padded.png", color: "#FFFFFF" }`。**无 `color_dark`**
- `android/app/src/main/res/values-v31/styles.xml`：`windowSplashScreenBackground` = `#FFFFFF`，`windowSplashScreenAnimatedIcon` = `@drawable/android12splash`
- `values-night-v31/styles.xml`：**同一属性亦为 `#FFFFFF`** ← B-2 成立
- 生成物按密度铺开：`drawable-{mdpi,hdpi,xhdpi,xxhdpi}/android12splash.png` + `drawable-night-*/` 同名；**mdpi 实测 200×200、xxhdpi 600×600** ← B-3 成立
- iOS 启动屏在 `ios/Runner/Base.lproj/LaunchScreen.storyboard`（无独立 xib）

**Flutter 侧**（`lib/features/onboarding/presentation/splash_page.dart`，297 行）：
- mark 声明 **108×108**（`:222` / `:234` / `:252`），资产 `assets/brand/mark_dog.svg`
- 该资产 `viewBox="0 0 500 500"` ← **B-4 成立**（图形只占 `x67–456 / y85–407`）
- 视觉中心 `centerY = size.height * 0.43`（`:138`）
- 呼吸光晕写死 `260×260`（`:157`）
- spinner 定位 `bottom: 108`（`:192`）
- 版本号 `static const String version = 'v 1.0.0'`（`:24`）← B-6，**本 Story 不动，属 7.2**
- **入场总时长 `animatedTotal = 4320ms`**（`:28`，时间线切片基准 `_t = 4320`）
  > 📌 注意：设计稿的「1.70s → 1.54s」是**新方案内部的迭代**；相对**现状 4320ms** 是砍掉近 2.8s。本 Story 不做这个改动（属 7.2），但估工与验收时别把 4.32s 当成 1.8s
- prefs 门控：`_decided` 标志（`:48`）在 `AppPrefs.create().timeout(300ms)`（`:96`）返回前不渲染 mark ← **B-7 首帧空窗成因**；`splashLastShownDate` 判定「当天是否已播」（`:98-99`）← C-3 门控。**本 Story 不动，属 7.3**
- reduce-motion 取 `MediaQuery.disableAnimations`（`:88`）

### 必须保留的既有行为（改动时不得破坏）

- `SplashPage.onComplete` 回调契约不变 —— 路由层 `app_router.dart` 依赖它做分流（pending 深链 > 落地矩阵），**本 Story 不碰分流逻辑**
- `brandViolet = Color(0xFF7D45F6)` 已是品牌紫常量，原生底色须与它**完全一致**，不要另写一个色值
- 现有 splash 相关测试须继续通过；若因首帧落位改动而失败，**改测试断言而非绕过**，并在 Completion Notes 说明改了哪一条

### 强制护栏（违反即返工）

- 凭证/密钥不入库（本 Story 不涉及）
- 不引入任何新依赖、新中间件（`flutter_native_splash: ^2.4.6` 已在 `dev_dependencies`，不升版）
- 不改 `ddl-auto`、不加 Flyway 迁移（本 Story 零 schema）
- 用户可见字符串不硬编码（本 Story 不新增文案）

### Project Structure Notes

- 资产落 `petgo_app/assets/brand/`（既有目录，已在 pubspec `assets:` 声明范围内）
- Flutter 改动仅限 `lib/features/onboarding/presentation/splash_page.dart`
- 原生改动通过 `pubspec.yaml` 配置 + 生成命令达成，**不手工编辑 `android/` 与 `ios/` 下的生成物**

### References

- [Source: _bmad-output/planning-artifacts/epics-v1.1.2.md#Story 7.1]（6 条 AC 原文）
- [Source: _bmad-output/planning-artifacts/v1.1.2/PRD-v1.1.2.md#2.6]（FR-87 全部 + FR-90 资产部分 + 9 条缺陷汇总表）
- [Source: _bmad-output/planning-artifacts/v1.1.2/ui-splash-v1.1.2.html]（S1 原生新规格 / S2 首帧接住 / T3 尺寸规格 / T4 原生改动清单）
- [Source: PRD §4 OQ-20]（真机量系统图标尺寸，阻塞本 Story 的 L2 验收）
- [Source: 源目录 .decision-log.md]（C-7 字标资产 / C-8 mark 资产与尺寸 / B-1~B-5 缺陷 / V-1 真机复核）——**未随仓库归档**，在 `/Users/hexsfile/work/MY-PROJECT/Pet Project/TailTopia/V1.1.2/`
- 架构：`architecture-v1.1.2-delta.md` **无 FR-87 对应 AD**（16 条 AD 产出于 2026-07-31，早于 splash 设计）。技术细节已由 UI 稿给足可执行规格，故不阻塞；详见 epics 文档「架构 delta 对 Splash 的覆盖缺口」

## Dev Agent Record

### Agent Model Used

Claude Opus 5 (1M context) · claude-opus-5[1m]

### Debug Log References

- `flutter analyze` → `No issues found!`（4.3s）
- `flutter test` → `All tests passed!`（651 tests，34s）
- `flutter build apk --debug` → `✓ Built build/app/outputs/flutter-apk/app-debug.apk`（Gradle 21.7s）
- `dart run flutter_native_splash:create` → `✅ Native splash complete.`

### Completion Notes List

**L0 全绿；AC1 / AC2 / AC3 / AC6 已满足。AC4 / AC5 属 L2，须本地真机，见文末「待本地验收」。**

#### 三个既有缺陷在配置与资源层已验证修复

| 缺陷 | 验证方式 | 结果 |
|---|---|---|
| **B-1** 浅色白闪 | 读生成物 `values-v31/styles.xml` | `windowSplashScreenBackground` = `#7D45F6` ✅ |
| **B-2** 深色同样白闪 | 读生成物 `values-night-v31/styles.xml` | `#7D45F6` ✅ —— **Dev Notes 里那条「缺 `color_dark` 才是根因」的推断得到证实**：补上 `color_dark` / `android_12.color_dark` 后生成器才会改写 night 变体，无需手工改生成物 |
| **B-3** Android 12 图标不合规 | 量各密度生成物 | mdpi **288×288**（原 200×200），hdpi 432 / xhdpi 576 / xxhdpi 864 / xxxhdpi 1152，浅色与深色两套齐全 ✅ |

旧版路径（Android 11 及以下）：`drawable/background.png` 与 `drawable-night/background.png` 均为 `#7D45F6`（1×1 拉伸）✅

#### 资产来源改用设计稿的成品，未自行从母文件重推（一处偏离 T1 字面写法）

T1 字面要求「从 `ICON SVG.svg` 的 `Layer_7` 导出」。**实施时核实母文件后改为从设计稿 `ui-splash-v1.1.2.html` 提取已备好的成品**，理由：

1. **母文件 `Layer_7` 的 6 条 path 填充是紫色**（`.st94 = fill:#7D45F6`），其中 1 条是白色（`.st2`）作镂空。直接导出得到的是「紫 mark 带白镂空」，**不是原生启动屏需要的白 mark**；要变白就得自行决定「那条白镂空该变成什么色」——属设计决策，不该由实施侧拍。
2. **`Layer_7` 还含一个 `<rect width="500" height="500" class="st42">`**（`.st42 = fill:none`，切片参考框），紧贴 bbox 导出时必须剔除。
3. **设计稿里那份已经把上述两件事处理好了**：`viewBox="67.23 84.79 389.32 321.81"`、6 条 path 全部去掉 class/fill、由 CSS `.mk{fill:#FFFFFF}` 统一上白色。稿中 4 处 `.mk` 实例 viewBox 完全一致。

**顺带独立验证了那个 viewBox 确实紧贴**：把 mark 改黑渲染成 768×768 后，图形横向占满 `x 0–767`（贴满两边），宽高比 1.211 与 viewBox 的 389.32/321.81 = 1.210 吻合 —— 说明左右无空白、B-4 的偏心已随新 viewBox 消除。

字标同理取自稿中 `.wm`：`viewBox="37.05 183.5 413.74 128.1"`，**11 条 path**（与决策日志「11 条可见路径 → 拆 9 拍」一致，可直接作为 Story 7.2 AC2 的分组依据）。

#### 本机无 SVG 光栅化工具，PNG 用两步法生成（可复现）

`rsvg-convert` / `inkscape` / ImageMagick / `cairosvg` **本机均未安装**（无 Homebrew）。改用系统自带 `qlmanage` + Pillow：

1. 复制一份 mark、`fill` 改 `#000000`，`qlmanage -t -s 768` 渲成 768×768 PNG（白 mark 直接渲会得到纯白一片，因 Quick Look 底色为白）
2. Pillow 按亮度反算 alpha（`alpha = 255 − 亮度`），颜色置白 → 得到白色 + 透明、抗锯齿边缘保留
3. 贴入 1152×1152 透明画布居中

**产出核对**：画布 1152×1152（= 288dp @4x），可视内容 768×636，**占画布宽 66.7%**，正好是 Android 12 规范的 192/288 上限；左右留白各 192、上下各 258，居中。

#### ⚠️ 实施中算出一个数，与设计稿的 42% 不一致 —— 直接影响 Story 7.2（OQ-20 的部分答案）

iOS 侧可以**静态推算**出实际渲染尺寸，无需真机：

- 生成物 `LaunchImage.png`（1x）= **288×288**，`@2x` 576、`@3x` 864
- `LaunchScreen.storyboard` 的 `LaunchImage` imageView 为 `contentMode="center"` + 四边约束 → **按自然尺寸居中渲染，不缩放**
- 故 iOS 上画布 288pt、**可视内容 192pt**
- 在 390pt 宽的机型上：192 / 390 = **约 49.2%**，而设计稿定的是**屏宽 42%**（390 上为 164pt）

**结论：原生实际渲染的 mark 比设计稿目标大了约 7 个百分点。** 这正是 OQ-20 存在的原因（系统图标尺寸不可指定）。**处置建议留给 Story 7.2**：其 AC0 要求首帧对齐「7.1 的实测值」，则应对齐 **192dp 可视宽**而非 42%；否则交接处仍有尺寸跳变。Android 侧的实际渲染值仍需真机测量（T8）后确认是否同为 192dp。

（本 Story 未据此修改原生源资产 —— 资产本身已严格合规 288/192；差异出在「规范尺寸」与「设计稿百分比」两套口径之间，属需产品/设计确认的口径问题，不是资产缺陷。）

#### iOS 白色根背景不会露出（核对结论）

`LaunchScreen.storyboard` 的根 `<view>` 仍有 `backgroundColor red="1" green="1" blue="1"`（白）。**核对约束后确认无影响**：`LaunchBackground` imageView 为 `scaleToFill` 且四边均约束到父视图，1×1 紫色被拉伸铺满全屏，白色根背景永远被盖住。属生成器固定输出，不需处理。

#### 生成器副产品：web 启动屏一并被重写

`flutter_native_splash:create` 同时重写了 `web/index.html` 与 `web/splash/img/*`（9 个文件）。**V1 不上 web**（PRD：V1 仅 Android 上架），这些改动无功能影响，但已如实列入 File List。

#### 未越界（AC3 / AC6 判据）

`git status petgo_app/lib/` → **0 个文件**。手工编辑仅 `pubspec.yaml` 一处；其余全为新增资产与生成物。`splash_page.dart` 一行未动，旧动效原样保留（总时长仍 4320ms）。

#### ⏳ 待本地验收（L2，需真机 —— 本会话无法执行）

| 任务 | 内容 |
|---|---|
| **T6**（AC4） | **浅色**模式三种冷启动路径各验一次，全程无底色闪白 |
| **T7**（AC4） | **深色**模式同上 —— B-2 的正式验收点，不可只验浅色 |
| **T8**（AC5） | Android 12+ 真机**测量系统图标实际渲染尺寸**，回写本文件与 PRD OQ-20；与上方 iOS 推算的 192pt 比对 |

验收口径提醒：**「底色闪白」必须已消除；「mark 跳位」允许遗留**（原生 192dp 可视 vs Flutter 108px@43%），由 Story 7.2 消除。

#### ✅ 2026-08-04 补充：AC4 / AC5 已在模拟器上验证（原判「待本地真机」，实际已可执行）

设备：AVD `petgo_phone` · **Android 16 / API 36**（属 Android 12+）· 1080×2400 @420dpi（1dp = 2.625px，屏宽 411.4dp）
方法：`adb shell am force-stop` 冷启动 + 连拍 `screencap`；深色用 `adb shell cmd uimode night yes` 切换。

**AC4 白闪验收 —— 通过**

| 模式 | 冷启动后各帧主色 | 白占比 | 结论 |
|---|---|---|---|
| 浅色 | `(125,69,246)` = `#7D45F6`　占 96% | 3.3%（状态栏图标与 mark 本身） | ✅ 无白闪 |
| 深色 | `(125,69,246)`　占 95% | 4.2% | ✅ **无白闪 —— B-2 正式验收通过** |

已肉眼核对截图：紫底 + 白 mark（狗剪影 + 猫形镂空透出紫色 + 右下草叶），构图正确、无裁切。

**AC5 系统图标实测 —— 完成（OQ-20 的 Android 侧答案）**

| 项 | 实测 |
|---|---|
| 可视内容 | **472×418 px = 180×159 dp** |
| 占屏宽 | **43.70%** |
| 渲染中心 | x 48.8% / **y 50.0%**（证实系统固定居中 50%，不可定位） |

**🔑 与设计稿的 42% 基本吻合（差 1.7 个百分点）**，差值落在「左侧细猫尾与紫底混色后低于测量阈值」的误差范围内 —— 已排除裁切：逐行扫左右边缘，极值分布连续变化（曲线轮廓），未出现「多行同一值」的直边。

**→ 给 Story 7.2 AC0 的结论：首帧对齐目标取「屏宽 42%」即可，设计稿的数是对的。** 不需要改成 192dp。（我在上一轮 Completion Notes 里由 iOS 推算出的 49.2% **只对 iOS 成立**，不适用于 Android，此处更正。）

**⚠️ 遗留一处 iOS / Android 不一致（V1 不上 iOS，不阻塞）**

AC2 要求「iOS 视觉尺寸与 Android 对齐」，实际两者机制不同：

- **Android**：系统按自己的尺寸渲染，实测可视 **180dp**
- **iOS**：`LaunchImage.png`(1x) = 288×288，`contentMode="center"` 按自然尺寸渲染 → 可视固定 **192pt**

两者差约 6.7%，且都是**固定 dp/pt**（不随屏宽等比），故占屏比会因机型而变。V1 仅 Android 上架，**记录待 iOS 上架前处理**（届时把 iOS 的 `LaunchImage` 缩到 180pt 可视即可对齐）。

**仍建议保留的真机复核**：模拟器与真机在 splash 渲染上可能因 OEM 定制有差异。上述数值来自 AOSP 模拟器，**7.2 若要把 42% 写死，建议先在一台真机上抽查一次**。

#### 🐛 2026-08-04 用户实机核对发现并修复：新字标资产曾被截断成「Tail」

**用户在模拟器上圈出字标说「和要求的不一样」，据此复查发现一个真实缺陷（已修）。**

**两件事要分清：**

1. **屏幕上显示的字标是旧资产，这是本 Story 的预期行为** —— 7.1 不改 `lib/`，Flutter 侧仍渲染 `wordmark.svg`（细卷钩尾，即缺陷 B-5 的那一版）。新字标要等 **Story 7.2** 接入才会出现在屏幕上。
2. **但我新导出的 `wordmark_brand.svg` 本身有 bug** —— 只画得出「**Tail**」，「topia」五个字母缺失。

**根因**：设计稿里有 6 个 `.wm` 实例，我当初取了 **index 0 = B2「逐拍写出」屏**，那是**动效中间帧** —— 后 5 条字母路径带 `style="opacity:0"`（还没写出来）。**11 条路径都在，但 5 条是透明的**，所以只数路径条数看不出问题。

**修复**：改从 **index 1 = B3 终态**实例提取（已核对该实例 0 条隐藏路径），并剥掉空的 `class` / `style` 属性。

**验证**（渲染成 PNG 量墨迹）：

| | 墨迹宽高比 | 判定 |
|---|---|---|
| 修复前 | **1.560** | ❌ 与 viewBox 的 3.230 严重不符 → 内容缺失 |
| 修复后 | **3.143** | ✅ 与 viewBox 3.230 吻合（差值为字母两端的自然留白） |
| 旧 `wordmark.svg` 参照 | 3.336 | — |

肉眼对比确认修复版为完整「Tailtopia」，且**猫尾是粗壮实心月牙、位置更高、动感短划更长** —— 与 C-7 / 缺陷 B-5 的描述一致，旧版是细卷钩。

**顺带复查了 mark**：4 个 `.mk` 实例**均无 `opacity:0` 路径**，故 `mark_brand.svg` 不受此坑影响；再以渲染墨迹交叉验证 —— 旧 `mark_dog.svg` 墨迹比 1.206、新 `mark_brand.svg` 1.207，图形一致，差别仅在 viewBox（旧 `0 0 500 500` 含大量空白 vs 新紧贴）与草叶填充（旧有一处灰、新统一纯白，符合「白 mark」要求）。**mark 与已生成的原生资源无需重做。**

**教训（已写入资产文件头注释，供 7.2 参考）**：从含动效的设计稿提取资产时，**必须挑终态实例，并检查 `opacity` / `visibility` 一类的中间态内联样式** —— 只核对元素个数会漏掉这类缺陷。

### File List

**手工编辑（1）**
- `petgo_app/pubspec.yaml` —— `flutter_native_splash` 配置块：底色改 `#7D45F6`、补 `color_dark` / `android_12.color_dark`、`image` 系列改指向新白 mark；附设计口径与「生成物勿手改」注释

**新增资产（3）**
- `petgo_app/assets/brand/mark_brand.svg` —— 白 mark，`viewBox="67.23 84.79 389.32 321.81"`，6 条 path
- `petgo_app/assets/brand/wordmark_brand.svg` —— 字标，`viewBox="37.05 183.5 413.74 128.1"`，11 条 path 全部可见（7.2 的 9 拍分组依据）。**2026-08-04 修复：初版误取 B2 动效中间帧，后 5 条字母 `opacity:0` 导致只画出「Tail」；已改从 B3 终态实例提取**
- `petgo_app/assets/brand/splash_mark_white.png` —— 1152×1152 透明画布 + 白 mark，可视内容占宽 66.7%

**生成物 · Android（26）** —— `dart run flutter_native_splash:create` 产出
- `android/app/src/main/res/values-v31/styles.xml`、`values-night-v31/styles.xml`（底色 → `#7D45F6`）
- `drawable-{mdpi,hdpi,xhdpi,xxhdpi,xxxhdpi}/android12splash.png` + `drawable-night-*` 同名（288→1152 各密度）
- `drawable/background.png`、`drawable-night/background.png`、`drawable-v21/background.png`（紫）
- `drawable-*/splash.png` 与新增的 `drawable-night-*/splash.png`、`drawable-night/`、`drawable-night-v21/`
- `values/styles.xml`、`values-night/styles.xml`、`drawable*/launch_background.xml`

**生成物 · iOS（11）**
- `ios/Runner/Base.lproj/LaunchScreen.storyboard`
- `ios/Runner/Assets.xcassets/LaunchBackground.imageset/`（`background.png`、`darkbackground.png`、`Contents.json`）
- `ios/Runner/Assets.xcassets/LaunchImage.imageset/`（1x/2x/3x + Dark 变体 + `Contents.json`）
- `ios/Runner/Info.plist`

**生成物 · Web（9，V1 不上 web）**
- `web/index.html`、`web/splash/img/{light,dark}-{1,2,3,4}x.png`

### Change Log

| 日期 | 内容 |
|---|---|
| 2026-08-04 | 实施 AC1/AC2/AC3/AC6：新增三份品牌资产、改 `flutter_native_splash` 配置（含补 `color_dark`）、重生成原生资源。三缺陷 B-1/B-2/B-3 在配置与资源层验证修复。L0 全绿（analyze 0 / test 651 / apk build ok），`lib/` 零改动。AC4/AC5 属 L2 待本地真机。 |
| 2026-08-04 | 记录一处口径差异：iOS 静态推算可视 192pt，390pt 机型上约占屏宽 49.2%。 |
| 2026-08-04 | **修复 `wordmark_brand.svg` 截断缺陷**（用户实机核对发现）：初版取自设计稿 B2「逐拍写出」中间帧，后 5 条字母路径带 `style="opacity:0"`，导出后只画得出「Tail」。改从 B3 终态实例提取并剥空属性，渲染墨迹比由 1.560 → 3.143（viewBox 3.230）验证通过。mark 侧复查无此问题，原生资源无需重做。 |
| 2026-08-04 | **AC4/AC5 在模拟器（Android 16/API 36）完成验证**：浅色与深色冷启动均无白闪（B-2 正式通过）；系统图标实测可视 180dp = 屏宽 43.70%、中心固定 50%。**更正上一条**：43.7% 与设计稿 42% 基本吻合，49.2% 仅对 iOS 成立 —— Story 7.2 AC0 取 42% 即可。已排除裁切。iOS/Android 固定尺寸差 6.7%，V1 不上 iOS 故记录待办。Status → review。 |
