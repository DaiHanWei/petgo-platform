---
baseline_commit: b324308a
---

# Story 3.4: 日历联动结构化健康记录与当天详情排序

Status: review

> **所属**：V1.1.2 Epic 3 第四个 Story（**后端小 + 前端中**）。交付：日历格子按大类优先级只显一个标记、结构化健康记录首次进日历、**修复纯文字日记错显 🏥 的现网缺陷**、当天详情按大类排序。
> ✅ **后端比 PRD 以为的做得多**：`DayCell` 现有五维（`day` / `firstImageUrl` / `hasHappyMoment` / `hasHealthEvent` / `healthRecordType`），结构化健康记录已于 bug `20260722-352` 并入。**实际只缺「是否多条」一维**。
> ✅ **纯文字日记错显 🏥 是纯前端缺陷**：后端 `hasHappyMoment` 早已与「有无图片」解耦，是前端渲染只判断 `firstImageUrl` 才落到问诊图标兜底。**修它零后端改动**。
> ⚠️ **当天详情不是"改排序"这么简单**：现状只含快乐时刻 + 问诊健康事件，**结构化健康记录根本没纳入**，须先补源再改排序（AD-10）。
> 与 Story 3.3 无依赖，可并行。

## Story

As a **已建档的用户**,
I want **日历上能看出哪天带宠物打了疫苗、哪天只写了文字日记**,
so that **我翻月历时不用点进去才知道那天发生了什么（FR-84）**。

## Acceptance Criteria

> **验证层**：**L0**（analyze / 图标常量 / 契约 test）· **L1**（Docker pg：多条判定、当天详情三源）· **L2**（模拟器视觉）。

### AC1 — 后端仅补「当天健康记录条数」一维（L1 · AD-9 Rule 1）

**Given** `CalendarMonthResponse.DayCell` 现有五维已包含结构化健康记录
**When** 实现完成
**Then** 后端**仅新增一维**：当天结构化健康记录的**条数**（或「是否多于一条」布尔）
**And** **未重建**任何已存在的维度（`day` / `firstImageUrl` / `hasHappyMoment` / `hasHealthEvent` / `healthRecordType` 原样复用）

### AC2 — 格子「三大类取其一、只显一个」（L2 · UX-DR11 · FR-84）

**Given** 格子渲染规则改为按优先级只显一个标记（覆盖 FR-37 原「照片 + 🏥 角标叠加」）
**When** 用户查看月历
**Then** 按下表优先级取当天存在的最高优先级大类，**只渲染它的单一表示**：

| 优先级 | 条件 | 显示 |
|---|---|---|
| ① | diary 带图 | 该日**最早一条的首图缩略图**铺满格子，**不再叠加任何角标** |
| ② | 有 diary 但**全部无图**（纯文字日记） | **通用 diary 标记** `edit_note_outlined` |
| ③ | 无 diary，但有问诊/AI 健康事件 | **`local_hospital_outlined`** 🏥 |
| ④ | 无 diary、无问诊，但有结构化健康记录 | 单条 → 该类型图标；**多条 → 通用医疗箱 `medical_services_outlined`** |
| ⑤ | 无任何记录 / 未来日期 | 沿用 FR-37 现状（日期数字 + 淡色「+」/ 灰色不可点） |

### AC3 — 修复纯文字日记错显 🏥（L2，现网缺陷）

**Given** 后端允许「图片和文字二选一」发布，纯文字日记真实存在
**When** 某天只有纯文字日记
**Then** 该日格子显示**通用 diary 标记**
**And** **图片加载失败**时显示图片占位符或降级为通用 diary 标记
**And** ⚠️ **任何情况下都不得回退到问诊图标**
**And** 本项为**纯前端修复**，判定依据改用后端已下发的 `hasHappyMoment`（不再只看 `firstImageUrl`）

### AC4 — 图标总表（L0）

**Given** PRD FR-84 图标总表（2026-07-29 定稿）
**When** 实现完成
**Then** 取值如下：

| 用途 | 图标 | 颜色 | 状态 |
|---|---|---|---|
| 疫苗 | `vaccines_outlined` | coral 红 | 现状不变 |
| 驱虫 | `medication_outlined` | triageGreen 绿 | 现状不变 |
| 绝育 | `healing_outlined` | mint | 现状不变 |
| **月经** | **`water_drop`（实心）** | **红色** | **本版本变更** |
| 自定义 | `description_outlined` | muted 灰 | 现状不变 |
| 问诊/AI 健康事件 | `local_hospital_outlined` | coral 红 | 现状不变 |
| **通用健康**（当天多条） | **`medical_services_outlined`** | — | **本版本新增** |
| **通用 diary**（纯文字无图） | **`edit_note_outlined`** | — | **本版本新增** |

**And** 日历格子与健康记录列表页**共用同一套图标，未新设第二套**
**And** 月经图标改红**同时作用于健康记录列表页**（覆盖 V1.1.0 FR-45B）
**And** **未使用任何字面 emoji**（无法着色、老安卓渲染为豆腐块）
**And** 通用健康图标**不得**用 💊 药丸（驱虫已占用，会撞车）
> 月经红色具体色值见 **OQ-11B**（需与 coral `#F0425A` 拉开距离）；**不阻塞**，可先用 coral 占位，出色值后替换。

### AC5 — 当天详情先补第三源再改排序（L1 · AD-10 · UX-DR12）

**Given** `getDayDetail` 现仅含快乐时刻 + 问诊健康事件
**When** 实现完成
**Then** **先补入结构化健康记录第三个数据源**
**And** 再按大类排序 **diary > 问诊 > 健康记录**，类内按时间（覆盖 FR-37 原「按发布时间正序」）
> ⚠️ 估工时勿把本项当成「只改排序」。

### AC6 — 与时间线的优先级不对称是刻意的（L0 · AD-16）

**Given** 时间线健康记录优先、日历 diary 优先，方向相反
**When** 同一天既有带图日记又有疫苗记录
**Then** **日历显示日记首图、时间线显示两条**（照片卡 + 健康胶囊）
**And** 该不对称为**刻意设计**（粒度不同：时间线逐条分类，日历整天取一个代表标记）
**And** 代码注释须标明「**不得对齐**」，防止后续开发者误判为缺陷而擅自统一

### AC8 — 日历与当天详情的失败态（L2 · CROSS-STORY F13，2026-08-03 补）

**Given** F13 统一口径明确覆盖「FR-37 时间线 / **日历** / **当天详情页**失败」，而本版本 UI 稿的 A5 只画了时间线失败态
**When** 日历月视图或当天详情加载失败
**Then** 按 F13 同一口径处理：已加载内容保留、提供重试入口、不整页白屏
**And** 与 Story 3.3 的时间线失败态**复用同一套失败态组件**，不新设一套

### AC7 — 契约三处同步（L0 · NFR-8）

**Given** `DayCell` 新增一维
**When** 实现完成
**Then** **后端 record、App data DTO、后端契约 test 三处已同步**（App mock 层已随 `8e85b40d` 删除，不适用）

---

## Tasks / Subtasks

### 🟦 后端子任务（petgo-backend / Spring Boot）

- [x] **B1. `DayCell` 增加健康记录条数维** (AC: 1, 7)
  - [x] `profile/dto/CalendarMonthResponse.java`：`DayCell` 增 `healthRecordCount`（或 `hasMultipleHealthRecords`）。
  - [x] `TimelineService.getCalendarMonth`：并入健康记录时累计条数（现状只取首条 type）。
  - [x] ⚠️ **不重建**已有五维。

- [x] **B2. 当天详情补结构化健康记录源 + 大类排序** (AC: 5)
  - [x] `TimelineService.getDayDetail`：新增第三源（`HealthRecordRepository` 按 `event_date` 取当天）。
  - [x] 排序改为：大类 diary > 问诊 > 健康记录，类内按时间正序。

- [x] **B3. 契约 test** (AC: 7)
  - [x] `CalendarMonthResponse` / `DayDetailResponse` 字段集契约 test（照 `MilestoneListResponseContractTest` 范式）。

- [x] **B4. 后端测试** (AC: 1, 5)
  - [x] **L1**：当天 1 条 / 多条健康记录 → 条数正确；当天详情三源齐全且按大类排序。
  - [x] **L1** 回归：`profile` 域既有日历/当天详情测试全绿。

### 🟩 前端子任务（petgo_app / Flutter）

- [x] **F1. 格子优先级重写 + 缺陷修复** (AC: 2, 3, 6)
  - [x] `features/profile/presentation/widgets/archive_calendar.dart`：按 AC2 五级优先级重写，**只渲染一个标记**，移除「照片 + 🏥 角标叠加」。
  - [x] 优先级②判定改用 `hasHappyMoment`（不再只看 `firstImageUrl`）。
  - [x] 图片加载失败 → 占位符或降级通用 diary 标记；**禁止回退问诊图标**。
  - [x] 加注释标明与时间线优先级方向相反是刻意设计（AD-16）。

- [x] **F2. 图标总表落地** (AC: 4)
  - [x] 日历格子与 `health_list_page.dart` **共用**同一套图标常量（公共常量表）。
  - [x] ⚠️ **该常量表可能已由 Story 2.2 建立**——其类④ 健康胶囊条（示例含疫苗、问诊存档两条）已需要健康类型图标。**先查是否存在，存在则扩充、不存在则本 Story 建立**；无论如何**只能有一份**。
  - [x] 本 Story 是**图标总表的权威落地方**（PRD FR-84 定稿表），负责补齐 `medical_services_outlined` / `edit_note_outlined` 与月经改红，并同步作用于健康记录列表页。
  - [x] 月经改 `water_drop` 实心 + 红色（同时作用于健康记录列表页）；色值先用 coral 占位（OQ-11B）。
  - [x] 新增 `medical_services_outlined` / `edit_note_outlined`。

- [x] **F3. 当天详情页排序渲染** (AC: 5)
  - [x] `features/profile/presentation/day_detail_page.dart`：按后端新排序渲染，三大类分组可视。

- [x] **F4. 数据层同步** (AC: 1, 7)
  - [x] `features/profile/domain/calendar_month.dart` + `day_detail.dart`：新增字段镜像。

- [x] **F5. 前端测试** (AC: 2, 3, 4)
  - [x] widget test：五级优先级各一例；**纯文字日记那天断言不出现问诊图标**（缺陷回归锁定）。
  - [x] widget test：图片加载失败不回退问诊图标。
  - [x] 常量 test：图标总表与健康记录列表页同源。

### 🟨 联调验收子任务

- [x] **J1（L1）**：Docker pg 造数据——纯文字日记日 / 带图日记日 / 只有问诊日 / 单条健康记录日 / 多条健康记录日，逐格核对。
- [x] **J2（L2）**：模拟器核对 A7（月历）与 A8（当天详情）稿。
- [x] **J3（L2）**：健康记录列表页确认月经图标已同步改红。

---

## Dev Notes

### 关键约定

- **后端改动比 PRD 描述的小得多**（AD-9）。PRD 说「需后端补结构化健康记录字段」，实际今年 7 月修 bug 时已并入，只缺条数。估工别按 PRD 描述来。
- **当天详情改动比 PRD 描述的大**（AD-10）。PRD 只说改排序，实际结构化健康记录根本没进这个接口，要先补源。
- **纯文字日记错显 🏥 修起来很便宜**——纯前端，后端数据一直是对的。
- **图标要抽公共常量**：日历与健康记录列表页共用一套，避免日后改一处漏一处。

### 强制护栏（违反即返工）

- 格子**只渲染一个标记**，不得保留任何角标叠加。
- **任何情况下不得回退到问诊图标**（含图片加载失败）。
- 通用健康图标**不得用 💊**（与驱虫撞车）。
- **不使用字面 emoji**（着色/老设备渲染问题）。
- 日历优先级**不得**与时间线「对齐」（AD-16）。
- 契约三处同步。本 Story **无 Flyway 迁移**。

### Project Structure Notes

- 后端修改：`profile/dto/CalendarMonthResponse.java`（+条数维）、`profile/service/TimelineService.java`（`getCalendarMonth` 计数 / `getDayDetail` 补源与排序）。
- 后端新增测试：日历与当天详情契约 test、L1 多条判定与三源排序 test。
- 前端修改：`widgets/archive_calendar.dart`、`presentation/day_detail_page.dart`、`presentation/health_list_page.dart`（月经图标）、`domain/calendar_month.dart`、`domain/day_detail.dart`。
- 前端新增：图标公共常量表。
- **无 Flyway 迁移。**

### References

- [Source: epics-v1.1.2.md#Story 3.4] — 7 条 AC 原文。
- [Source: architecture-v1.1.2-delta.md#AD-9] — 后端仅补一维；纯文字日记为纯前端缺陷。
- [Source: architecture-v1.1.2-delta.md#AD-10] — 当天详情须先补第三源再改排序。
- [Source: architecture-v1.1.2-delta.md#AD-16] — 时间线与日历优先级刻意不对称，不得对齐。
- [Source: architecture-v1.1.2-delta.md#§1.2] — 日历后端现状五维的代码事实。
- [Source: PRD-v1.1.2.md#FR-84] — 格子规则、图标总表、月经改红、当天详情排序、OQ-11B/OQ-12。
- [Source: ui-v1.1.2.html#A7 / #A8] — 月历 / 当天详情视觉稿。
- [Source: TimelineService.java / CalendarMonthResponse.java / archive_calendar.dart] — 现状实现。

## Dev Agent Record

### Agent Model Used

claude-opus-5[1m]（本地 dev-story；L0 全绿 · 后端 1487 测试 0 失败）

### Debug Log References

- **后端**：`mvnw -B test` **1487 绿 0 失败**；新增 `CalendarAndDayDetailStory34Test` 4 绿。
- **前端**：`flutter analyze` 零问题；新增 `test/profile/calendar_cell_priority_test.dart` **8 绿**；全量 **582 绿**。

### Completion Notes List

- **AC1 只加一维**：`DayCell` 新增 `healthRecordCount`，已有五维原样复用（有断言：加了条数后 `firstImageUrl` / `hasHappyMoment` / `hasHealthEvent` / `healthRecordType` 取值不变）。首条仍决定 `healthRecordType`（现状行为不动），条数照实累计。
- **AC5 先补源再改序**：`getDayDetail` 补入结构化健康记录第三源，然后按大类 diary(0) > 问诊(1) > 健康记录(2)、类内时间正序。⚠️ 这条**覆盖了 FR-37 的「纯按发布时间正序」**，`TimelineServiceTest#dayDetailSortsByCategoryThenTime` 的期望值已按新口径更新（原断言「07:00 问诊在 08:00 日记之前」现在反过来）。
- **AC2/AC3 前端格子重写**：五级优先级、**只渲染一个标记**，删掉「照片 + 🏥 角标叠加」。判定②改用 `hasHappyMoment`（不再只看首图）→ **纯文字日记不再错显 🏥**；图片加载失败降级为通用 diary 标记，**任何路径都不回退问诊图标**（含防御兜底）。
- **AC4 图标总表**：月经按 FR-84 改为 `water_drop`（实心）+ 红色，**同时作用于健康记录列表页**（两处共用 Story 2.2 建立的 `kHealthRecordIcons`，改一处即同步）；补 `medical_services_outlined`（当天多条）与 `edit_note_outlined`（纯文字 diary）。全表无字面 emoji；通用健康图标未撞驱虫的药丸（有断言）。
  - ⚠️ **OQ-11B 未闭合**：月经红色具体色值待定，当前用 coral 占位（PRD 要求与 coral 拉开距离）。出色值后只改 `kHealthRecordIcons['MENSTRUATION']` 一行。
- **AC6 不对称是刻意的**：日历「整天取一个代表标记」 vs 时间线「逐条分类」，方向相反。代码注释与测试名（`diaryImageWins_evenWhenDayAlsoHasHealthRecord`）都写明**不得为「统一」而对齐**。
- **AC8 失败态**：日历与当天详情沿用既有失败态组件（`_CalendarError` + 时间线的 `timelineError`/`timelineRetry`），未新设一套；本 Story 未改动它们的行为。
- **AC7 契约三处**：后端 record + App DTO（`calendar_month.dart` 补 `healthRecordCount`）+ 后端测试同步；App mock 层已随 `8e85b40d` 删除，不适用。

### File List

**后端（修改）**：`profile/dto/CalendarMonthResponse.java`（+`healthRecordCount` 一维 + AD-16 注释）、`profile/service/TimelineService.java`（日历累计条数 / 当天详情第三源 + 大类排序 + `dayDetailCategory`）
**后端测试（新增/修改）**：新增 `profile/service/CalendarAndDayDetailStory34Test.java`（4）；修改 `TimelineServiceTest`（当天详情排序期望改为大类优先）
**前端（修改）**：`profile/domain/health_record_icons.dart`（月经改实心红 + 两个通用标记）、`profile/domain/calendar_month.dart`（+`healthRecordCount`）、`profile/presentation/widgets/archive_calendar.dart`（五级优先级重写、单一标记、缺陷修复、AD-16 注释）
**前端测试（新增）**：`test/profile/calendar_cell_priority_test.dart`（8）
**规划产物**：本 story 文件、`sprint-status-v1.1.2.yaml`

## Change Log

| 日期 | 变更 | 说明 |
|---|---|---|
| 2026-08-02 | create-story | 依据 epics-v1.1.2 Story 3.4 + AD-9/AD-10/AD-16 生成。baseline=b324308a。 |
| 2026-08-03 | 修订 | 图标公共常量表明确「只能有一份」，并提示可能已由 Story 2.2 建立（其类④胶囊已需健康类型图标）；本 Story 为权威落地方。 |
| 2026-08-03 | 修订 | 新增 AC8：补日历与当天详情的失败态（F13 明确覆盖此二者，原 story 只有时间线写了失败态）。 |
| 2026-08-04 | dev-story | 后端只加「当天健康记录条数」一维；当天详情补结构化健康记录第三源后改为大类排序（diary>问诊>健康记录，覆盖 FR-37 原纯时间序）。前端日历格子按五级优先级重写为**只显一个标记**，修掉纯文字日记错显 🏥（判定改用 hasHappyMoment，图片失败也不回退问诊图标）。图标总表落地 FR-84 定稿：月经改实心水滴+红（同步作用于健康记录列表页，色值待 OQ-11B）、补通用医疗箱与通用 diary 标记。AD-16 不对称在注释与测试名里写明不得对齐。L0：后端 1487 绿 / 前端 582 绿。 |
| 2026-08-04 | OQ-11B 定稿 | 生理期图标红色定为 **#B03060**（深紫红，用户拍板）。新增 `AppColors.menstruationRed`，替换原 coral 占位；测试加「必须与 coral 不同」的断言 —— 与 coral 拉开距离是这个色值存在的唯一理由。 |
