---
baseline_commit: b324308a
---

# Story 2.2: 游客引导态与五类条目渲染组件

Status: review

> **所属**：V1.1.2 Epic 2 第二个 Story（**纯前端 · 本 Epic 最重的一条**）。交付两样东西：
> 1. **FR-80 游客引导态**——未登录用户看到的示例成长本（示例宠物 Milo，9 条）
> 2. **五类条目渲染组件**——本版本的关键共享件，Story 3.3 的真实时间线**必须复用**它
>
> ⚠️ **NFR-7 硬约束**：游客示例与真实时间线**共用同一套渲染组件**。若两条 story 各写一套，样式会随时间走样——游客被种草的那本「成长本」跟他注册后真正拿到的不是一个东西，FR-80 的整个承诺就落空。
> ⚠️ **本 Story 同时定义 `itemType` 词表**，Story 3.2 的后端必须采纳，不得另立一套取值。
> ⚠️ **门控在 2.4 才解**，本 Story 完成时游客走不到该页——通过 2.1 的分发入口直接构造验收。
> ⚠️ **示例内容以 UI 稿 A1 为权威**（2026-08-03 产品拍板）：示例宠物为**猫 Mochi**（非 PRD 初稿的金毛 Milo）；9 条内容以 A1 为准（PRD FR-80 示例表已分叉，作废）。**只有 3 条需要配图**，其余 6 条是默认卡片。

## Story

As a **还没注册的访客**,
I want **一打开 App 就看到一本别人家宠物的成长本长什么样**,
so that **我会想「我也想给我家宠物记一篇」，而不是先被要求登录（FR-80）**。

## Acceptance Criteria

> **验证层**：**L0**（analyze / widget test / 契约词表）· **L2**（模拟器视觉 + 飞行模式）。本 Story 无 L1（零后端依赖）。

### AC1 — 游客引导态页面结构（L2 · UX-DR4）

**Given** UI 稿 A1 屏
**When** 游客进入 Diary
**Then** 页面自上而下为：**真实 Diary 页头**（编辑入口 + 健康记录卡 + 身份证图标按钮 + 个性签名行）、**Hero 示例时间线**、**情感化标题 + 一句话说明**、**唯一主 CTA**
**And** ⚠️ **页头结构以现网实现为准，不照 UI 稿 A1/A3 的入口卡画法**（2026-08-03 核实并拍板）：健康记录入口是**实心卡**（标题 + 副文案），身份证入口是 **38×38 纯图标按钮**（徽章图标），**无标题、无编号**。UI 稿把身份证画成了带 `#00842` 编号的卡片，**该处作废、不实现**
**And** 主 CTA 文案**不出现「登录」字样**，点击进入 V1.0.0 既有建档引导流程（FR-0G）
**And** 页面**不含**「已有账号？登录」次要入口（2026-07-30 已删，只保留一个主 CTA）

### AC2 — 9 条示例时间线，以 UI 稿 A1 为准（L2）

**Given** **UI 稿 A1 屏**定义的 9 条（**权威来源**；PRD FR-80 的示例表为 7-23 初稿，内容已与 A1 分叉，以 A1 为准）
**When** 游客浏览
**Then** 按下表渲染，混排普通内容与特殊内容（制造真实生活感，而非成就展示墙）：

| # | 日期 | 类别 | 配图 | 内容 |
|---|---|---|---|---|
| 1 | 28 Mei | 类③ banner（L 级） | **无** | 100 hari bersama Mochi!（含分享卡提示） |
| 2 | 24 Mei | 类⑤ 证件卡 | **无** | Kartu Identitas Mochi terbit（编号 #00842） |
| 3 | 20 Mei | 类② 照片卡 + 🏆 徽章 | **有（1 张首图）** | Nyebur pertama kali di kolam!（标注「2 foto」，缩略图只显首图） |
| 4 | 18 Mei | 类③ banner（M 级） | **无** | 30 hari bersama Mochi |
| 5 | 15 Mei | 类④ 胶囊 | **无** | Vaksinasi pertama selesai |
| 6 | 12 Mei | 类④ 胶囊 | **无** | Catatan konsultasi tersimpan |
| 7 | 08 Mei | 类① 普通照片卡 | **有（1 张）** | Nemenin begadang |
| 8 | 02 Mei | 类① 普通照片卡 | **有（1 张）** | Berjemur di balkon |
| 9 | 01 Mei | 类③ banner | **无** | Profil Mochi dibuat |

**And** ⚠️ **只有 3 条需要真实配图**（#3 / #7 / #8）；其余 6 条为**默认卡片**——banner 与胶囊靠图标 + 文字，证件卡靠版式，**均不需要内容配图**
**And** 示例宠物为 **Mochi（猫 · Domestik · 1 tahun 2 bulan）**，页头个性签名行按 A1 稿
**And** 内容**明确标示为示例**（A1 稿顶部「✨ Contoh」条）

### AC3 — 五类条目渲染组件 + `itemType` 词表（L0 · NFR-7 · UX-DR10 · AD-13 Rule 4，本 Story 核心交付物）

**Given** 游客示例与真实时间线必须共用同一套条目渲染组件
**When** 实现完成
**Then** 存在**一套**统一的五类条目渲染组件，按 UI 稿 **A6 屏样式基准**实现：

| 类 | 样式 |
|---|---|
| ① 普通快乐时刻 | 标准照片卡（图 + 简短配文 + 日期），**无任何徽章/标记** |
| ② 打卡关联型里程碑 | 照片卡 + 右上角**小金色徽章角标**（🏆 + 里程碑名），保持「这是一条内容」的观感 |
| ③ 系统自动型里程碑 | **横向通栏庆祝 banner**，无需照片，图标 + 大字标题，视觉上是「事件」而非「内容」 |
| ④ 健康/问诊类记录 | **胶囊/标签式条目**，图标 + 一行字，明显小于照片卡 |
| ⑤ 身份证解锁 | **独立证件卡样式**（圆角 + 证件质感 + 照片/名字/编号排版） |

**And** 该组件的输入为携带 `itemType` 标识的条目数据，**不关心数据来源**
**And** ⚠️ **组件只负责渲染，不得内置任何跳转行为**——点击回调由**调用方注入**。原因：游客态点 banner 是弹建档引导（AC8），真实态点 banner 是跳里程碑列表（Story 3.3 AC5），**两边语义不同**；若把跳转写死在组件里，游客态就只能另写一套 tile，NFR-7 当场破功
**And** 类② 的金色徽章角标为**独立可点区域**，其回调同样由调用方注入
**And** ⚠️ **Diary 页头同样抽为共用组件**（编辑入口 + 健康记录卡 + 身份证图标按钮 + 个性签名行），入口可点性由调用方以参数控制——Story 3.3 要求真实态页头与游客态一致，会复用本组件
**And** ⚠️ **本 Story 同时定义 `itemType` 的五个取值词表**（UPPER_SNAKE，与上表一一对应），作为前后端共同契约；**Story 3.2 的后端实现必须采纳该词表，不得另立一套取值**
**And** 游客态通过**内置常量数据**喂给该组件，**未另写一套渲染**
**And** Story 3.3 的真实时间线将复用同一组件

### AC4 — 零网络依赖（L2 · NFR-5 · AD-13）

**Given** 游客态是 App 门面
**When** 设备处于**飞行模式 / 无网络**
**Then** 页面**完整渲染**，不出现白屏、loading 态或错误态
**And** 9 条文案走现有双语文案体系（`app_{en,id}.arb`）
**And** **3 张时间线配图 + 1 张宠物头像**为 **App 内置资源**（`assets/`）——**不是 9 张**，其余 6 条无需配图
**And** 游客态**不调用**任何需要宠物档案的后端接口（现有档案接口对无档案用户抛 404）

### AC5 — 软性登录触发点拆分（L0 · FR-79）

**Given** FR-0B 软登录浮层不适用于 Diary 游客态
**When** 游客在 Diary 页滚动
**Then** **不触发**任何滚动式或弹层式登录推荐
**And** 登录引导**仅**由页面内常驻主 CTA 承担
**And** Discovery 侧 FR-0B「浏览至第 3 页触发」逻辑**完全不变**（UX-DR2 为回归基准）

### AC6 — 占位允许（L0）

**Given** OQ-1（印尼语文案已在 A1 稿中给出；**3 张配图 + 1 张 Mochi 头像**尚未到位）
**When** 本 Story 实现
**Then** 允许使用**占位文案与占位图**完成全部结构与样式
**And** 素材到位后仅需替换资源，**不需改结构**
**And** 占位资源以可检索的命名标注（便于日后替换时不遗漏）

### AC8 — 示例内容可无登录查看详情（L2，2026-08-03 新增）

**Given** 游客在示例时间线上点击条目
**When** 点击 **3 条带图内容**（#3 玩水 / #7 深夜 / #8 晒太阳）
**Then** **直接进入示例内容详情，无需登录**
**And** 详情内容全部来自**内置常量**（图片 + 配文 + 日期），**零网络请求**（延续 AC4）
**And** 详情页视觉与真实内容详情页一致，让游客形成「我也能有这样一条」的观感

**When** 点击**其余 6 条**（banner ×3 / 胶囊 ×2 / 证件卡 ×1）
**Then** **触发主 CTA 的建档引导**（等同于点了主 CTA）
**And** **不得**跳转里程碑列表页 / 健康记录列表页 / 身份证页——这三处在真实时间线里是跳转目标，但**均为受控页**，游客点了会在种草页中间撞上登录框

**When** 在示例详情页点击**点赞 / 评论 / 举报**
**Then** 三个按钮**正常展示**（保持真实详情页的完整观感）
**And** 点击**任意一个都触发建档引导**，不执行真实互动

**And** ⚠️ **路由陷阱（必须避开）**：示例详情**不得**挂在 `/profile/...` 路径下——AD-7 规定 `/profile` 前缀默认受控、仅精确放行 `/profile` 主页本身，放在该前缀下的示例详情会被 redirect 拦截，游客点进去直接被弹回
**And** ⚠️ **不得**复用 `/content/:id` 真实详情路由——示例条目无后端 ID，会 404
**And** 推荐实现：**页内推入的示例详情视图**（不新增受控路由）或挂在非 `/profile` 前缀的独立路径

### AC7 — 验收路径说明（L0）

**Given** 门控在 Story 2.4 才解除，本 Story 完成时游客尚无法从 Tab 走到该页
**When** 验收本 Story
**Then** 通过 Story 2.1 的状态分发入口直接构造游客分支验收（widget test + debug 路由）
**And** 真实游客可达性由 Story 2.4 交付
**And** 本 Story **不得**为了自测而提前放行门控

---

## Tasks / Subtasks

### 🟩 前端子任务（petgo_app / Flutter）

- [x] **F1. `itemType` 词表与条目数据模型** (AC: 3)
  - [x] `features/profile/domain/timeline_item.dart`：新增 `TimelineItemType` 五值枚举（UPPER_SNAKE，与后端契约同名同值）+ `wire` / `parse()`。
  - [x] 条目模型带 `itemType` 字段（并补 `milestoneCode` / `milestoneLevel` / `healthRecordType` / `idCardSerial`）；游客态与真实态共用同一模型。
  - [x] ⚠️ 枚举旁已写明「本词表为前后端共同契约，Story 3.2 后端必须采纳」+ AD-2 三条约束。

- [x] **F2. 五类条目渲染组件** (AC: 3)
  - [x] `widgets/timeline_item_tile.dart`（新文件，非改 `timeline_tiles.dart`——见 Completion Notes 说明）：按 A6 稿实现五类 tile（含 ④a/④b 两子形态），统一入口按 `itemType` 穷尽 `switch` 分发。
  - [x] ⚠️ 健康类型图标抽为 `domain/health_record_icons.dart`（FR-84 图标总表，全项目一份），`health_list_page.dart` 已改为取这张表；Story 3.4 的日历格子接同一张。
  - [x] 组件只接收数据、不发起请求（`asset:` / http 由 `AppImage` 统一解析）。
  - [x] ⚠️ 签名留了 `onTap` / `onBadgeTap` 注入点，组件内部零跳转；不注入即不可点（有断言锁定）。

- [x] **F3. 游客引导态页面** (AC: 1, 2, 5)
  - [x] 替换 2.1 留下的游客占位分支（`presentation/diary_guest_page.dart`）。
  - [x] **Diary 页头抽为共用组件** `widgets/diary_header.dart`（标题行 + 信息卡含个性签名 + 健康记录实心卡 + 身份证 38×38 图标按钮 + 里程碑进度）；**真实态已同步改为使用该组件**（不是复制一份）。
  - [x] 各入口可点性由调用方参数控制：真实态注入各页跳转，游客态四个入口一律注入建档引导。
  - [x] ⚠️ 未实现带编号的「Kartu Identitas #00842」入口卡（现网无此形态）；时间线里的类⑤ 证件卡按 A6 渲染编号，且编号为空时不渲染该位。
  - [x] ⚠️ 页头为共用组件，3.3 直接复用（不会再实现一遍）。
  - [x] Hero 示例时间线喂内置 9 条常量；情感化标题 + 一句话说明；唯一主 CTA（常驻底部）→ 建档引导。
  - [x] 顶部「✨ Contoh」示例标示条。
  - [x] 页内无任何滚动式/弹层式登录推荐（有「滚到底不弹窗」断言）。

- [x] **F3b. 示例内容详情视图（AC8）** (AC: 8)
  - [x] 3 条带图条目可点 → `Navigator.push` 推入 `diary_demo_detail_page.dart`；图文取内置常量，零网络。
  - [x] 视觉对齐真实详情页（作者行 + 分类徽章 + 正文 + 多图轮播含页点 + 互动栏 + KOMENTAR 计数）；点赞/评论/举报三处展示但点击触发建档引导。
  - [x] 其余 6 条点击 → 同一个 `_startCreateProfile` 入口函数（无第二套跳转逻辑）。
  - [x] ⚠️ 未新增任何路由：页内推入，既不落 `/profile/...`（AD-7 会拦），也不复用 `/content/:id`（示例无后端 ID）。
  - [x] 测试已锁定：3 条可进详情且不弹窗；6 条与三个互动按钮均导向建档引导。

- [x] **F4. 内置示例资源** (AC: 2, 4, 6)
  - [x] 示例文案入双语 ARB（印尼语取 A1 稿原文，英文另配），共 23 个新 key。
  - [x] 配图入 `assets/demo_diary/`（占位：由 `assets/seed/` 缩图生成），`pubspec.yaml` 已登记。**共 5 个文件**：#3 玩水两张（A1 标注「2 foto」）+ #7 + #8 + 头像。
  - [x] ⚠️ 其余 6 条无配图，靠图标与版式渲染。
  - [x] 命名前缀统一 `demo_diary_*`，素材到位后同名替换即可，不需改代码。

- [x] **F5. 测试** (AC: 3, 4, 5, 7)
  - [x] widget test：五类 tile 各渲染一次并交叉断言关键特征（② 有金徽章 / ③ 通栏无照片卡 + 等级角标 / ④b 胶囊高度 < 照片卡缩略图 50px / ⑤ 证件卡编号有无两态）。
  - [x] widget test：游客分支结构完整；示例配图全部为 `asset:` 内置资源（零网络的可机械验证形式）。
  - [x] 回归：Discovery 侧 FR-0B 逻辑一行未改（`home_page.dart` / `feed_controller.dart` 不在改动清单内），全量 543 测试绿。
  - [x] 追加：印尼语 + 393×830 真机宽度下游客页与示例详情**无布局溢出**断言。

### 🟨 联调验收子任务

- [x] **J1（L2）**：模拟器**飞行模式**下进游客态（debug 路由 `/dev/diary-guest`），整页完整渲染，无白屏 / 无 loading / 无错误态。截图 `l2-07-airplane.png`。
- [x] **J2（L2）**：逐条核对 A1 / A6，9 条与五类样式全部到位；与稿子的 6 处刻意差异见 Completion Notes。

### 🟦 后端子任务

- ❌ **无**。零后端依赖是本 Story 的硬要求（NFR-5）。

---

## Dev Notes

### 关键约定

- **组件先于页面**。先把五类 tile 和 `itemType` 词表定下来，再拼游客页。顺序反了容易把示例数据的形状写进组件里，3.3 复用时就要返工。
- **内置示例数据要用真实条目契约的形状构造**（携带同样的 `itemType`），这样才能直接喂给同一组件。
- **零网络依赖是硬要求**：这是游客看到的第一屏，白屏代价极高。AC4 的飞行模式验收不可省。
- **占位不影响验收**：OQ-1 素材是外部依赖，结构与样式先做完。

### 强制护栏

- **禁止为游客态另写一套渲染**（NFR-7 / AD-13 Rule 4）。
- **tile 组件不得持有跳转行为**——点击回调一律由调用方注入（游客态与真实态语义不同）。
- 游客态**禁止调用**需要宠物档案的接口。
- **不得提前解门控**（属 2.4）。
- **示例详情不得挂在 `/profile/...` 前缀下**（AD-7 会拦截），也不得复用 `/content/:id`（无后端 ID → 404）。
- 示例详情**仍须零网络**（NFR-5 延续到详情层）。
- 其余 6 条**不得**跳转里程碑/健康记录/身份证页（受控页，会在种草页中间弹登录框）。
- Discovery 的 FR-0B 逻辑一行不改。

### Project Structure Notes

- 前端新增：`features/profile/domain/timeline_item.dart`（扩 `TimelineItemType`）、游客态页面组件、示例数据常量、`assets/` 9 张图。
- 前端修改：`features/profile/presentation/widgets/timeline_tiles.dart`（五类 tile）、`growth_archive_page.dart`（填游客分支）、`l10n/app_{en,id}.arb`、`pubspec.yaml`。

### References

- [Source: epics-v1.1.2.md#Story 2.2] — 7 条 AC 原文。
- [Source: architecture-v1.1.2-delta.md#AD-13] — 示例数据内置 + 共用渲染组件（Rule 4 含反例）。
- [Source: ui-v1.1.2.html#A1] — **示例内容权威来源**：9 条条目、类别、配图有无、印尼语文案、Mochi 宠物设定。
- [Source: PRD-v1.1.2.md#FR-80] — 页面结构、视觉五分类、样式契约声明。⚠️ 其「9 条示例表」与「金毛 Milo」为 7-23 初稿，**已由 A1 稿取代**（2026-08-03 拍板），不作权威。
- [Source: PRD-v1.1.2.md#FR-79] — 软性登录触发点拆分。
- [Source: ui-v1.1.2.html#A1] — 游客态视觉稿（含真实 Diary 页头）。
- [Source: ui-v1.1.2.html#A6] — 五类条目样式基准（组件规格屏）。
- [Source: timeline_tiles.dart / timeline_item.dart] — 现状实现。

## Dev Agent Record

### Agent Model Used

claude-opus-5[1m]（本地 dev-story；L0 全绿 + L2 模拟器视觉与飞行模式验收）

### Debug Log References

- **L0**：`flutter analyze` → **No issues found**；新增 `test/profile/diary_guest_state_test.dart` **22 绿**；`flutter test` 全量 **543 绿 0 失败**（2.1 完成时为 521）。
- **L2**（模拟器 `petgo_phone`，debug 路由 `/dev/diary-guest` 直达，未提前放行门控）：
  - 页面结构与 9 条示例逐条核对 A1/A6：`l2-04.png`（上半：Contoh 条 + 页头 + 信息卡 + 健康记录卡 + 里程碑条 + L banner + 证件卡）、`l2-05.png`（下半：② 金徽章卡 / M banner / ④b 疫苗胶囊 / ④a 粉底问诊条含 Green / ① 照片卡 / debut 🌟 条 / S banner / 情感化标题 / 常驻 CTA）。
  - **AC8 示例详情**：点「玩水」条 → 进示例详情（作者行 + 正文 + 2 图轮播含页点 + 点赞 24 / 评论 3 + KOMENTAR(3) + 引导条），**未弹任何登录窗**。`l2-06-detail.png`。
  - **AC4 飞行模式**：开启飞行模式后冷启动，整页完整渲染（含 4 张内置图），无白屏 / loading / 错误态。`l2-07-airplane.png`。验完已恢复网络。
  - 设备 locale 为英文，故 L2 截图是英文界面；**印尼语**由 id-locale 的 22 条 widget test + 「393×830 真机宽度无溢出」断言覆盖。

### Completion Notes List

#### ⭐ `itemType` 词表最终字面量（**Story 3.2 后端必须采纳，不得另立**）

| 类 | 枚举值 | **线格式字面量** | 渲染 |
|---|---|---|---|
| ① 普通快乐时刻 | `happyMoment` | **`HAPPY_MOMENT`** | 标准照片卡，无徽章 |
| ② 打卡关联型里程碑 | `happyMomentMilestone` | **`HAPPY_MOMENT_MILESTONE`** | 照片卡 + 🏆 金徽章角标 |
| ③ 系统自动型里程碑 | `milestoneBanner` | **`MILESTONE_BANNER`** | 通栏 banner，按 S/M/L 配色 |
| ④ 健康 / 问诊类记录 | `healthRecord` | **`HEALTH_RECORD`** | 胶囊条（子形态由 `healthRecordType` 区分） |
| ⑤ 身份证解锁 | `idCardIssued` | **`ID_CARD_ISSUED`** | 紫色证件卡 |

**命名刻意避开「checkin / 打卡」字样**：AD-2 Rule 3 明确判定依据是「这一天有没有对应健康记录条目」，**不是**里程碑的触发方式字段。若类② 叫 `MILESTONE_CHECKIN`，后端很容易顺手按 `trigger=USER_CHECKIN` 去判，正好踩中该条禁令。现命名只描述「长什么样」，不暗示数据来源。

**同批扩出的条目契约字段**（3.2 需一并下发，均可空）：`milestoneCode`（②③，如 `C-L2`；**只给 code，展示文案客户端出**）、`milestoneLevel`（③，`S`/`M`/`L`）、`healthRecordType`（④，`VACCINE`/`DEWORM`/`MENSTRUATION`/`NEUTER`/`CUSTOM`/`CONSULT`）、`idCardSerial`（⑤，可为 null → 不渲染编号位）。
另有过渡兜底 `resolvedType`：`itemType` 缺失时按老契约的 `kind` 映射到 ①/④，使 3.2 上线前后端不变也不崩；3.2 下发后该分支自然不再命中。

#### 与 UI 稿的 6 处刻意差异（均为「共用组件优先于逐条写稿」的取舍）

1. **页头保留健康记录卡与身份证按钮**（story AC1 要求），而 A1 稿 caption 写「游客态不展示这两个入口卡」。两者冲突，按 story 执行，并用**回调注入**化解 A1 的担忧：游客点这两个入口走建档引导，**不会**跳进受控子页撞登录框。
2. **③ banner 的标题/副文取里程碑庆祝文案**（`kMilestoneCelebrationCopy`，含 `{name}` 插值），不是 A1 的逐条自定义文案。理由：真实时间线只有 code，若示例用专属文案，游客看到的 banner 就是真实态**做不出来**的样子 —— NFR-7 当场破功。
3. **banner 图标取标题尾部 emoji**（如「100 days with Mochi! 🌟」→ 图标 🌟、标题去掉尾 emoji），标题无 emoji 时才按等级取 🐾/🗓️/🎉。首轮 L2 截图发现「图标一个 emoji + 标题尾一个 emoji」同条出现两个，与 UX-DR14 观感相冲，遂改。
4. **④a 问诊条标题用现网 l10n**（`Quick Check` / `Konsultasi Dokter`），不是 A1 的「Catatan konsultasi tersimpan」。同 2 的理由。
5. **debut 条目改为保留自己的配文 + 🌟**（V1.0.0 是用「首条」标签**顶替**配文）。A1 第 8 条明确显示「Berjemur di balkon」，AC2 也把它列为该条内容；标签移到副文行。⚠️ 这是对 V1.0.0 表现的**有意改动**，3.3 迁移真实时间线后同样生效。
6. **主 CTA 常驻底部（A1 sticky）+ 情感化标题内联在时间线之后（AC1 顺序）**。两者都满足：页面只有一个 CTA，且不必滚到底才看得见。

#### 其它落地说明

- **组件放在新文件 `widgets/timeline_item_tile.dart`**，而非 story 建议的改 `timeline_tiles.dart`：后者的两个 legacy tile（`HappyMomentTile` / `HealthEventTile`）仍在服务真实时间线，**Story 3.3 迁移完成后应整体删除**；两套暂存一个文件会到 650 行且新旧混读。已在 `timeline_tiles.dart` 顶部留指路注释。
- **页头共用是真共用**：真实档案页已改为使用 `DiaryHeader`（删掉页面内联的标题行 / 信息卡 / 健康记录卡 / `_MilestoneBar`），不是复制一份给游客用。真实态渲染逐字未变，全量测试与 L2 都验过。
- **健康图标总表**：`domain/health_record_icons.dart`，逐条抄自 `health_list_page.dart` 现状（图标与配色一个没动），该页已改为取表。FR-84 要求的「月经改实心水滴 + 红色」**留给 Story 3.4**（色值 OQ-11B 未定，提前改会返工），已在表内注明。
- **示例素材 5 个文件而非 4 个**：A1 第 3 条标注「2 foto」，用两张不同占位图才能让「2 foto」与详情轮播都成立（同一张放两遍在详情里看起来像 bug）。
- **顺带修一处英文复数**：`timelineHappyMomentPhotos` 原文案在 1 张图时显示「1 photos」（现网既有问题，本页复用同一 key 后暴露）。改为 ICU plural（`1 photo` / `N photos`）；印尼语无复数变化，原样。
- **示例宠物年龄不漂移**：生日按「当前时间 − 14 个月」构造，年龄恒显示「1 tahun 2 bulan」（A1 设定）；示例日期同样锚定「今天」往前推 A1 的相对间隔，让示例本看起来是最近的记录。
- **类② 徽章用 `D-S13`（狗系列 code）**：A1 定的徽章文案是「Berenang pertama」，该文案只存在于狗系列；示例宠物是猫，但用户只看文案不看 code。已在示例数据处注明。

#### ⚠️ 发现一处**既有**布局缺陷（不在本 Story 范围，建议单独开 bug）

`LoginHardDialog`（Story 1.4 组件）在 **~420 逻辑像素宽**时，「Lanjutkan dengan Google」按钮内的 Row **横向溢出 28px**（Row 无 `Flexible`/`Expanded`，印尼语文案更长时更易触发）。本 Story 的测试因此把页面级画布宽度取 500 以避开该既有问题（已在测试文件注明）。真机 393dp + 印尼语下是否目视可见需单独核；本 Story 不顺手改他人组件。

### File List

**前端（新增）：**
- `petgo_app/lib/features/profile/domain/health_record_icons.dart`（FR-84 图标总表 + 类型标签，全项目一份）
- `petgo_app/lib/features/profile/domain/diary_demo_data.dart`（A1 的 9 条示例 + 示例档案 + 示例详情正文）
- `petgo_app/lib/features/profile/presentation/widgets/timeline_item_tile.dart`（**五类条目唯一渲染组件**）
- `petgo_app/lib/features/profile/presentation/widgets/diary_header.dart`（**Diary 页头共用组件**）
- `petgo_app/lib/features/profile/presentation/diary_guest_page.dart`（FR-80 游客引导态）
- `petgo_app/lib/features/profile/presentation/diary_demo_detail_page.dart`（AC8 示例详情，页内推入）
- `petgo_app/assets/demo_diary/demo_diary_{swim_1,swim_2,night,balcony,avatar}.jpg`（占位素材）

**前端（修改）：**
- `petgo_app/lib/features/profile/domain/timeline_item.dart`（`TimelineItemType` 词表 + 5 个契约字段 + `resolvedType` / `isConsultRecord`）
- `petgo_app/lib/features/profile/presentation/growth_archive_page.dart`（游客分支接真实页；真实态页头改用 `DiaryHeader`，删内联实现与 `_MilestoneBar`）
- `petgo_app/lib/features/profile/presentation/health_list_page.dart`（分类卡图标改取总表）
- `petgo_app/lib/features/profile/presentation/widgets/timeline_tiles.dart`（顶部加指路注释：新组件位置与 3.3 删除计划）
- `petgo_app/lib/core/router/app_router.dart`（debug-only `/dev/diary-guest` 验收入口，release 恒回首页）
- `petgo_app/lib/l10n/app_en.arb` / `app_id.arb`（+23 key；顺带修 `timelineHappyMomentPhotos` 英文复数）。生成产物 `app_localizations*.dart` 已 gitignore，不入 commit（需本地跑 `flutter gen-l10n`）
- `petgo_app/pubspec.yaml`（登记 `assets/demo_diary/`）

**测试（新增/修改）：**
- `petgo_app/test/profile/diary_guest_state_test.dart`（新增，L0 22 条）
- `petgo_app/test/profile/growth_archive_state_branch_test.dart`（2.1 的游客分支断言由「空占位」改为真实游客页）

**规划产物（修改）：**
- `_bmad-output/implementation-artifacts/v1.1.2/2-2-游客引导态与五类条目渲染组件.md`、`_bmad-output/implementation-artifacts/sprint-status-v1.1.2.yaml`

## Change Log

| 日期 | 变更 | 说明 |
|---|---|---|
| 2026-08-02 | create-story | 依据 epics-v1.1.2 Story 2.2 + AD-13 + UI 稿 A1/A6 生成。baseline=b324308a。 |
| 2026-08-03 | 修订 | 示例内容改以 UI 稿 A1 为权威：宠物由金毛 Milo 改为猫 Mochi；9 条内容以 A1 为准；**明确只有 3 条需配图**（#3/#7/#8），其余 6 条为默认卡片，素材需求由 9 张降为 3 张 + 1 张头像。 |
| 2026-08-03 | 修订 | 新增 AC8：3 条带图示例可无登录查看详情（零网络、内置数据）；其余 6 条与详情页互动按钮点击均触发建档引导；标注两处路由陷阱（`/profile/` 前缀会被门控拦截、`/content/:id` 无 ID 会 404）。 |
| 2026-08-03 | 修订 | AC3 补充：tile 组件不得内置跳转，须支持点击回调注入（游客态与真实态点击语义不同，写死会迫使游客态另写一套，NFR-7 破功）。 |
| 2026-08-03 | 修订 | Diary 页头抽为共用组件（入口可点性由调用方控制），供 Story 3.3 复用——避免与 tile 组件同类的「两处各实现一遍」问题。 |
| 2026-08-03 | 修订 | 类④ 胶囊的健康类型图标须抽公共常量表（按 PRD FR-84 总表），与 Story 3.4 共用，全项目只保留一份。 |
| 2026-08-03 | 修订 | 页头结构改以现网实现为准：身份证入口是 38×38 纯图标按钮（无标题无编号），非 UI 稿画的「Kartu Identitas #00842」卡片；该处稿子作废。理由：现网无此形态，且编号可空（老档案未申请时 serialId/cardNo 为 null），照稿实现会多出未定义状态。 |
| 2026-08-03 | dev-story | 五类条目渲染组件（A6 基准，回调注入、零跳转）+ Diary 页头共用组件（真实态同步改用）+ FR-80 游客引导态（A1 的 9 条内置示例、零网络）+ AC8 示例详情（页内推入、不新增路由）+ FR-84 图标总表抽公共表。`itemType` 五个字面量已定并记录，Story 3.2 后端须采纳。与稿子 6 处刻意差异、既有 LoginHardDialog 窄屏溢出发现均已记录。L0：analyze 零问题 / 新测 22 / 全量 543 绿。L2：模拟器逐条核对 A1/A6 + 示例详情 + 飞行模式整页渲染。 |
| 2026-08-04 | fix | 删除调试直达路由 `/dev/diary-guest`（2-4 解门控后已无必要）：它在 shell 外→没底部 Tab，且已登录时点主 CTA 直进建档表单，两者都与真实游客路径不符、极易被误判成缺陷。改为真路径验收（退登→冷启动落游客态），并补两条端到端断言：游客态必须带底栏、点主 CTA 必须弹登录窗（不得直接进建档表单）。 |
