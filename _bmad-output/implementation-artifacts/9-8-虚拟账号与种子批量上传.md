---
baseline_commit: c307613
---

# Story 9.8: 虚拟账号与种子批量上传

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

> **所属**：V1.1 Epic 9 第八个 Story（**后端 + admin 模板**）。承接 9-1 权限码 `virtual_account.manage`。两块：① **虚拟账号管理**（A-6，本次实现）——`users` 加 `account_type`(REAL/VIRTUAL) + 虚拟账号字段，无登录，复用 `content_posts.author_id`，与已上线 id1-20 种子作者一致；② **种子批量上传**（AB-1.1-02，zip+Excel 指纹，**方案待定**——见 Dev Notes「Part 2 取舍」）。
> **关键事实**：`users.google_sub` **NOT NULL UNIQUE** → 虚拟账号用合成 google_sub（`virtual-{token}`，无真实 Google 身份）满足约束；无登录靠 `account_type=VIRTUAL`（真实 Google idToken 不会映射到合成 sub，天然不可登录）。生产已灌 20 虚拟作者（`google_sub` 前缀 `seed-tailtopia-`，见记忆），迁移回填为 VIRTUAL。
> ⚠️ **Flyway 撞号**：9-7（标识审核）在**其他分支**实现（已告知，本 story 不做 9-7），其迁移号未知；本 story 用 V82（本分支当前 max V81），**merge 时按当时全局 max 复核顺延**（决策 E2 移动靶）。

## Story

As a **运营**,
I want **管理虚拟账号、批量上传种子内容**,
so that **冷启动内容可运营（AB-1.1-02A/02）**。

## Acceptance Criteria

> **验证层**：**L0**（工厂/校验单测、`mvn compile`）· **L1**（迁移 + 回填、建虚拟账号、启停、schema validate）。无 L2。

### AC1 — `users.account_type` + 虚拟账号字段（Flyway V82）+ 回填

**Given** `users` ALTER 加 `account_type`(REAL/VIRTUAL 默认 REAL) + `created_by`/`enabled`/`published_count`
**When** 上下文启动
**Then** `ddl-auto=validate` 一致；存量 `seed-tailtopia-%` 作者回填 `VIRTUAL`；REAL 账号不变
> 验证层：**L1**（迁移后种子作者 account_type=VIRTUAL、普通用户 REAL；validate 过）+ **L0**（枚举/实体编译）。

### AC2 — 建虚拟账号（无登录）+ 列表 + 启停

**Given** `virtual_account.manage` 门控
**When** 运营建虚拟账号（昵称 + 选填头像）/ 启停
**Then** 建成 VIRTUAL 账号（合成 google_sub、无密码、无 google 身份）；**不可登录**（真实 idToken 不映射）；复用 `content_posts.author_id` 可发种子；列表/启停 + 审计
> 验证层：**L1**（建账号 account_type=VIRTUAL + 无 password_hash + 不可 Google 登录；启停落库 + 审计）+ **L0**（`newVirtual` 工厂单测）。

### AC3 — 种子批量上传（Part 2，方案待定）

**Given** zip+Excel 指纹 + 图片命名规范
**When** 批量上传
**Then** 指纹比对去重、命名规范校验、逐条以虚拟账号发布
> 验证层：**L1**。**⚠️ 本 AC 方案待用户定**（见 Dev Notes）。

---

## Tasks / Subtasks

### 🟦 后端子任务（Part 1 · 本次）

- [x] **B1. Flyway `V82__alter_users_account_type.sql`**（AC: 1）——ALTER users 加 4 列 + CHECK + 回填 seed-tailtopia-% → VIRTUAL。
- [x] **B2. `User` 域**（AC: 1,2）：`AccountType{REAL,VIRTUAL}` 枚举 + 字段 + `newVirtual(nickname,avatar,createdBy)` 工厂 + getters/enabled setter。
- [x] **B3. `AdminVirtualAccountService`**（AC: 2）：`create`/`list`/`setEnabled` + 审计。合成 google_sub。
- [x] **B4. 控制器 + 模板 + 导航 + i18n**：`AdminVirtualAccountController`（virtual_account.manage）+ `virtual-accounts.html` + 导航 + i18n。
- [x] **B5. 测试**：L0 `newVirtual` 工厂 + service 校验；L1 迁移回填 + 建账号 + 启停 + 审计。

### 🟩 Part 2（种子批量上传）—— 轻量方案（用户 2026-07-14 定选 b）

- [x] **B6. Flyway `V83__init_seed_content_hashes.sql`**——内容 hash 去重锚（跨批防重发）。
- [x] **B7. `AdminSeedBatchService`**：选虚拟账号逐条发 DAILY（复用 `ContentService.publish`）+ sha256 内容去重 + `publishedCount++` + 审计。
- [x] **B8. `AdminSeedBatchController` + `seed-batch.html` + 导航 + i18n**（virtual_account.manage）。
- [x] **B9. L0 `AdminSeedBatchServiceTest`**（逐条发/图解析/去重跳过/非 VIRTUAL 拒/停用拒/空拒）。

## Dev Notes

- **Flyway**：V82（本分支 max V81）；⚠️ merge 时与 9-7 分支复核撞号。
- **虚拟账号无登录**：合成 google_sub（`virtual-{token}`）+ 无 password_hash；真实 Google idToken 不会验出合成 sub，天然不可登录。防御性可在登录跃迁点加 `account_type!=VIRTUAL` 断言（如有中心点）。
- **Part 2 取舍**：AC3「zip+Excel 指纹批量上传」需 **Apache POI（Excel 解析，新依赖）+ zip 解压 + 图像指纹 + multipart + OSS 直传**——重量级。生产种子已由 `scripts/local` 脚本灌入（记忆）。**建议二选一**：(a) 全量实现 zip+Excel 指纹管线（新依赖，重）；(b) 复用既有 seed-post 发布 + 虚拟账号（轻，逐条/小批），指纹去重用内容 hash。**待用户定，本次先交 Part 1**。

## Completion Notes

**Part 1（虚拟账号管理）实现完成（2026-07-14）· L0 全绿；L1 待本地。Part 2 待用户定方案。**

- **Flyway V82** `alter_users_account_type`(本分支 max V81;⚠️merge 与 9-7 分支复核撞号):ALTER users 加 account_type(REAL/VIRTUAL 默认 REAL)+created_by/enabled/published_count+CHECK;回填 `seed-tailtopia-%` 作者→VIRTUAL。
- `AccountType{REAL,VIRTUAL}` 枚举 + User 加字段 + `newVirtual(syntheticSub,nickname,avatar,createdBy)` 工厂(合成 google_sub=`virtual:<uuid>`,无密码,accountType=VIRTUAL) + getters/enabled setter。
- `AdminVirtualAccountService`(create[昵称≤20校验+合成sub+审计]/list/setEnabled[仅VIRTUAL可,防误改真实用户+审计])+ `AdminVirtualAccountController`(virtual_account.manage,9-1已加)+ `virtual-accounts.html`(建号表单+列表+启停)+导航+i18n。
- 无登录:合成 google_sub 真实 idToken 不映射 + 无 password_hash → 天然不可登录。
- 测试:L0 `AdminVirtualAccountServiceTest`(7:工厂 VIRTUAL 无密码、建号校验、仅 VIRTUAL 启停、无变更 no-op);L1 `AdminVirtualAccountIntegrationTest`(REAL 默认 + 建 VIRTUAL 无密码 + 启停,待本地)。`mvn test-compile` 通过。

### Part 2（种子批量上传）— **轻量方案实现完成**（用户 2026-07-14 定选 b）
不引新依赖。以选定虚拟账号为作者逐条发 DAILY 种子（复用 `ContentService.publish`），**内容 hash 跨批去重防重发**（**Flyway V83** `seed_content_hashes`）。每行一条:`文本` 或 `文本 ||| url1, url2`（≤9 图,逗号分隔）。
- `AdminSeedBatchService.publishBatch`(校验 VIRTUAL+enabled → 逐行解析 → sha256(type|text|sorted images) 去重 → publish → 记 hash + publishedCount++ + 审计,返回 published/skipped)。
- `AdminSeedBatchController`(`/admin/seed-batch`,virtual_account.manage)+ `seed-batch.html`(选账号 + 多行 textarea)+ 导航「批量种子」+ i18n。
- L0 `AdminSeedBatchServiceTest`(5:逐条发含图解析、hash 去重跳过、非 VIRTUAL 拒、停用拒、空拒)全绿。
- **AC3 字面 zip+Excel 未做**(重依赖,与用户确认走轻量);全量管线如需再起。

- 未提交(等用户)。
