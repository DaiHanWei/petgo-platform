---
baseline_commit: 1c002e2
---

# Story 8.6: L 级里程碑达成推送与分享卡

Status: review

> 后端 L 级完成 → `MILESTONE_NODE` 推送（接 6-1 `NotificationService`，复用 6-7 切片）+ 前端 L 级分享卡
> （复用 2-6 名片分享通道，一键 WhatsApp/Instagram）。连带 6-6 铃铛里程碑条改真数据。

## Story
As a **用户**, I want **重大里程碑达成时收到通知并能一键分享**, so that **不错过重大节点并向亲友炫耀宠物成长**。

## Acceptance Criteria
### AC1 — L 级达成推送 + 通知中心真数据（FR-34/FR-38）
L 级完成（任何触发方式）→ 经 6-1 `NotificationService` 下发 `MILESTONE_NODE` 至通知中心 + 已授权用户收系统推送；
点击跳成长档案 Tab → 里程碑列表页；连带 6-6 铃铛改真数据。**L0**（notify 监听器：L 推 / S·M 不推）。
### AC2 — L 级分享卡
L 完成后自动生成可分享卡，一键分享 WhatsApp/Instagram（复用 2-6 名片分享通道）。**L0**（onShare 注入）/ **L2** 视觉。
### AC3 — 「系统推送 + 当天发布」L 节点
生日 L1 / 满 100 天 L2 / 满 365 天 L3：6-7 定时切片提醒；用户当天发布成长日历 → 自动完成。**L0**（发布回填日期门）。

## Tasks / Subtasks
### 🟦 后端
- [x] `MilestoneCompletedEvent`（profile）：每次新完成发布；`MilestoneCompletionService.complete` 发布。
- [x] `notify/MilestoneNotifyListener`：@Async + @TransactionalEventListener 订阅，**仅 L 级** → send
  MILESTONE_NODE（接 6-1 NotificationService，targetRef=code，深链 → 里程碑列表页）。
- [x] `completeDateGatedLNodesOnPublish`：成长日历发布回填 L1（生日当天）/L2（≥100 天）/L3（≥365 天），
  接 ContentPublished 监听器（8-3）。source=PUBLISH。
### 🟩 前端
- [x] L 级 Duolingo 开宝箱后经 `onShare` 自动弹分享（`shareServiceProvider` → Share.share，复用 2-6 通道，
  系统面板含 WhatsApp/Instagram）；l10n milestoneShareText。
- [x] 6-6 铃铛：MILESTONE_NODE 既已 icon/label/深链支持（6-6 落地），随后端真推送即真数据；mock 加一条
  MILESTONE_NODE 通知演示；契约测 legal type 集补 6-7/8-6 三类。

### 🔗 联调
- L1（Docker）：真 L 完成 → notifications 落库 + 角标 + 铃铛展示；发布回填 L1/L2/L3。**待本地**。
- L2：真机系统推送收达成通知 + 点击深链直达；分享卡真机 WhatsApp/Instagram 视觉。**待本地**。

## Dev Agent Record
### Completion Notes
- L0 绿：`MilestoneNotifyListenerTest`（L 推 / S·M 不推）+ `MilestoneCompletionServiceTest`（日期门 L1/L2）
  + 前端契约/列表/打卡/庆祝测试全绿 + analyze 0 issue。
- 复用 6-1 NotificationService + MILESTONE_NODE 枚举 + 深链路由（6-1/6-6/6-7 已建），未新增推送基建（护栏）。
- 分享卡 V1 = 系统分享面板分享里程碑文案（复用 2-6 Share 通道）；真·图片分享卡渲染是 L2 本地增强。
- 「生日若已过手动补录」：L1 为 PUSH_PUBLISH，8.4 打卡当前仅放行 USER_CHECKIN 类——已过生日补录需放行
  L1 的 check-in，记入 deferred（小改）；当年生日当天发布的自动完成已覆盖。
- **L2 待本地**：真机推送/深链/分享卡视觉。
