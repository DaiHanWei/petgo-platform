---
baseline_commit: 4bdeb0ca
---

# Story 7.2: 软引导与设置页常驻入口

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a 运营，
I want 在用户第 3 天打开 App 时轻轻问一次手机号，且「我的」页永远有一个可以填的入口，
so that 愿意留联系方式的人有地方留，不愿意的人被问一次就不再被打扰。

---

> **本 story 今天（2026-08-20）才解锁。** 它被挡了 8 天，卡点不是技术 ——
> 是**「什么时候提示」这件事此前无人定义**（OA-5 收窄后唯一剩下的硬阻塞）。
>
> 原 PRD 写的是「复用推送权限的双时机（首次问诊后 / 建档后取最早）」，
> 而第二代「首启即申请」2026-08-07 上线后置位了**与旧门控共用的那个标记键** ⇒
> 旧双时机在线上**已成死代码**（决策 X-20）。产品 2026-08-20 给出新口径（决策 **X-21**）：
> **用户第 3 天打开 App 时提示。**
>
> **交付**：时机判定 + 软引导浮层 + 「我的」页常驻入口 + 两处埋点。
> **Story 7-1 已交付**字段、保存、清空、脱敏与编辑抽屉本体 —— 但它**没有任何入口**
> （7-1 自己如实标注了）。本 story 就是给它接上两条路。

---

## Acceptance Criteria

### AC1 — 第 3 天起首次打开时提示一次（L1）

**Given** 用户注册满 3 天、且尚未填写手机号、且本设备从未提示过
**When** 用户打开 App
**Then** 展示一次手机号软引导浮层 `[L1]`
**And** 判定口径四条（决策 X-21 / AD-14 Rule 8），**每条都要有对应用例** `[L1]`：
- **第 1 天 = 注册当天** ⇒ 第 3 天 = 注册日 **+2 个自然日**
- 自然日按 **WIB（`Asia/Jakarta`）** 划界
- 🔴 **第 3 天起（含）首次打开即触发，错过第 3 天不作废**
- 已填手机号者**永不提示**

> 🔴 **写成「仅第 3 天当天才提示」即为违规**：那天没打开 App 的用户会**永远**收不到，
> 与本 FR 要解决的问题（拿到愿意留号码的人的联系方式）恰好相反。
> 这条是最容易被"照字面实现"做错的地方 —— 产品说的是"第 3 天打开时"，
> 落地必须是"第 3 天**及以后**首次打开时"。

### AC2 — 只问一次（L1）

**Given** 软引导已经展示过
**When** 用户填了、或跳过了、或直接划走
**Then** 🛡 **本设备再也不主动弹这个引导** `[L1]`
**And** 🛡 标记位与 FR-85 推送权限的四个键**物理隔离**，一个都不共用（AD-14 Rule 2）`[L0]`
**And** 🛡 **不得读写既有 `petgo.push_permission_asked`** —— 它归第二代推送权限，
本 story 只读不写、不迁移、不删（AD-14 Rule 3）`[L0]`

> ⚠️ **「全局仅一次」在按设备存储下只能是「本设备仅一次」**（AD-14 Rule 1 已定标记位按设备存）。
> 换设备会再问一次 —— 这是既定代价、不是缺陷；已填手机号者不受影响，所以真实影响面很小。
> **不要**为了"真全局"去新建服务端标记，那是另一笔工、且 AD-14 已经拍过。

### AC3 — 与推送权限串行，不叠同屏（L1 / L2）

**Given** 同一次打开同时命中推送权限引导与手机号软引导
**When** 两者都要出现
**Then** 🛡 **先推送权限、后手机号**，串行处理，**不叠加同屏** `[L1]`（AD-14 Rule 7）

> 两个索取型弹层同屏会双双掉转化，且用户容易误点。

### AC4 — 「我的」页常驻入口（L1 / L2）

**Given** 不论用户是否跳过过软引导
**When** 打开「我的」页
**Then** AKUN 分组始终有一行手机号入口，点击进入 7-1 已交付的编辑抽屉 `[L1]`
**And** 未填写时右值显示"未填写"占位；**已填写时按脱敏展示**（保留国家码/前缀与末四位，中间遮蔽，如 `+62 812-••••-7890`）`[L1]`
**And** 点进抽屉后展示**完整号码**供修改 `[L2]`

> 脱敏规则 PRD §2.2 有明文（对齐 UI 稿 05b 屏）。⚠️ **脱敏只是展示层**：
> 抽屉里必须是完整号码，否则用户改不了自己的号。

### AC5 — 埋点（L1）

**Given** 需要判断"这次问值不值得"
**When** 用户对软引导做出响应
**Then** 上报 `phone_prompt_responded`，带 `action`（填 / 跳过 / 划走各自可分）`[L1]`
**And** 🔴 **事件名用改名后的新名** —— 旧名 `phone_prompt_result` 已作废
（`result` 是名词，不满足"动作在词尾且须是动词"；OA-7 已闭合）`[L0]`
**And** 7-1 留下的那条「保存成功带入口来源」的埋点在本 story 补齐 —— 入口属本 story，
7-1 已显式标注留到这里做 `[L1]`

### AC6 — 后端补下发注册时间（L1）

**Given** 「第 3 天」必须靠注册时间算，而 🔴 **`/me` 目前不下发它**（实读确认）
**When** 客户端要判定时机
**Then** `/me` 响应新增注册时间字段 `[L1]`
**And** 🛡 **不新增迁移** —— `users.created_at` 早就有，本条只是把已有列下发 `[L0]`

> ⚠️ 这是本 story 唯一的后端改动，别漏。发现它靠的是实读 `UserProfileResponse`：
> 里面没有任何 `createdAt`。**不要**改用"首次启动时本地记一个日期"来绕开 ——
> 那样存量用户（注册已久）会被当成新用户、第 3 天永远不到。

### AC7 — 不扩散到本 story 范围外（L0）

**Then** 🛡 以下**明确不做** `[L0]`：
- **后台 AB-11A**（手机号查看 + 批量催填）—— 运营后台，独立排期
- **OTP / 验证 / 手机号登录** —— PRD 明确 Non-goal
- **第二次、第三次自动提醒** —— PRD 明确：后续提醒改为运营后台手动触发
- **填写入口的同意勾选 / 用途说明文案** —— 用途告知由用户协议承接（PRD §2.2）
- **FR-85 的四个触发点与其引导** —— 归 8-2 / 8-3

---

## Tasks / Subtasks

- [x] **T1 · 后端下发注册时间**（AC6）
  - [x] `UserProfileResponse` 加注册时间字段；`MeService` 填值（`users.created_at` 已有，**不建迁移**）
  - [x] ⚠️ 时间一律 UTC ISO-8601 下发（既有约定），**时区换算在客户端做**
  - [x] L1：`/me` 返回该字段

- [x] **T2 · 时机判定（抽成一处，可单测）**（AC1、AC2）
  - [x] 新建一个纯逻辑判定：入参 = 注册时间 + 当前时间 + 是否已填手机号 + 本设备是否已提示过
  - [x] 🔴 **WIB 换算集中在这一处**。这是项目里**第一处「时区参与判定」** ——
        既有三处 `Asia/Jakarta` 都只做展示格式化。散开写两遍，两处迟早不一致
  - [x] 判定：`已填 → false`；`已提示过 → false`；`WIB 日历日差 >= 2 → true`
  - [x] ⚠️ 用**日历日差**，不是"小时数 / 24"：注册当天 23:00 与次日 01:00 只差 2 小时，
        但那是两个自然日；按小时算会把第 3 天推迟近一天
  - [x] 一次性标记键**新建独立键**（如 `petgo.phone_prompt_shown`），与推送权限四键物理隔离

- [x] **T3 · 软引导浮层**（AC1、AC3、AC5）
  - [x] 浮层内容轻量：一句说明 + 输入 + 「保存」/「以后再说」；**不加合规文案层**（PRD §2.2）
  - [x] 复用 7-1 的保存链路（`phone_edit_sheet.dart` 的校验与保存已就绪，**不要重写校验**）
  - [x] 🛡 与推送权限串行：命中两者时先推送权限、后手机号，不同屏（AD-14 Rule 7）
  - [x] 展示后**立即置位**标记（不等用户作答）—— 否则用户划走后下次打开又弹

- [x] **T4 · 「我的」页常驻入口**（AC4）
  - [x] AKUN 分组加一行，点击打开 7-1 的编辑抽屉
  - [x] 未填写 → 占位文案；已填写 → **脱敏**右值；抽屉内**完整号码**
  - [x] 两份 ARB 同步补文案（严禁源码硬编码用户可见字符串）

- [x] **T5 · 埋点**（AC5）
  - [x] `phone_prompt_responded` + `action`（填 / 跳过 / 划走三态可分）
  - [x] 补 7-1 留下的「保存成功带入口来源」——区分来自软引导还是「我的」页
  - [x] ⚠️ 事件名与属性都要过 `test/analytics/v112_events_test.dart` 的命名契约
        （前缀白名单目前**没有** `phone_`，很可能需要按 8-1 的先例扩表并写明理由）

- [x] **T6 · 测试**
  - [x] L1：第 3 天判定四条口径各一条用例，**含"第 5 天才打开也要提示"**（钉住 AC1 的"错过不作废"）
  - [x] L1：已填手机号 → 不提示；已提示过 → 不提示
  - [x] L1：展示后立即置位（划走也不再弹）
  - [x] L1：同时命中推送权限时的先后顺序
  - [x] L1：脱敏展示 / 抽屉内完整号码
  - [x] L1：两条埋点的事件名与属性

---

## Dev Notes

### 🔴 「第 3 天」的三个坑，按顺序踩

**① 错过第 3 天不作废** —— 见 AC1。产品的话是"第 3 天打开时"，落地必须是"第 3 天**及以后**首次打开时"。

**② 用日历日差，不是小时差。** 注册当天 23:00 与次日 01:00 相差 2 小时，却是两个自然日。
按 `(now - createdAt).inDays >= 2` 算会把第 3 天推迟将近一天，边界上的用户体验不一致。
正确做法：两边都先换算到 WIB 的**日期**（年月日），再求日历日差。

**③ WIB 换算只写一处。** 项目里既有三处 `Asia/Jakarta` **全都只做展示格式化**，
本 story 是**第一次让时区参与判定**。两处各写一遍换算，迟早不一致（而这种不一致极难发现：
只在跨日边界的几小时里表现出来）。

### 🔴 `/me` 不下发注册时间 —— 这是本 story 唯一的后端改动

实读 `auth/dto/UserProfileResponse.java`：**没有任何 `createdAt`**。
客户端算不出"第几天"，所以必须补下发（`users.created_at` 列早就有，**不需要迁移**）。

⚠️ **不要用"首次启动时本地记一个日期"绕开。** 那样对**存量用户**是错的 ——
他们注册已久，本地记的却是今天，第 3 天要再等两天；更糟的是重装 App 会重新开始计时。

### Story 7-1 已经交付了什么（别重造）

7-1 status = review，已交付：

| 已有 | 位置 |
|---|---|
| 可空列 + 归一入库 + 「不传 = 不动 / 传空 = 清空」 | `V112__add_user_phone.sql`、`MeService`、`UpdateMeRequest` |
| 印尼手机号校验（**刻意放宽**） | `auth/domain/IndonesianPhone.java`（18 条测试） |
| 日志脱敏（**字段名即护栏**，3 条测试守着） | `PhoneNeverLoggedTest` |
| 编辑抽屉 + 三种提示（成功 / 格式错 / 网络错） | `features/me/presentation/phone_edit_sheet.dart`（8 条测试） |

**本 story 只加入口与时机**，校验、保存、清空一律复用 —— 特别是**不要重写校验**
（7-1 写了一组"印尼人真实会怎么写"的用例，宁松勿紧是 AC 明写的）。

7-1 还显式留了一条给本 story：**「保存成功」埋点要带入口来源**，而入口属 7-2，
所以那条埋点留到这里补（7-1 的 Completion Notes 里写明了，避免被当成漏做）。

### 一次性标记键：新建独立键

`AppPrefs` 现有 `petgo.push_permission_asked`（第二代推送权限用）。
🛡 **本 story 新建自己的键**，与推送权限的四个键物理隔离（AD-14 Rule 2），
且**不读写** `push_permission_asked`（AD-14 Rule 3：那个键归第二代，不迁移不删）。

⚠️ **展示后立即置位**，不等用户作答 —— 否则用户划走浮层，下次打开又弹，
就变成"反复索要个人信息"，正是 PRD 明确要避免的。

### 埋点命名可能需要扩契约白名单

`test/analytics/v112_events_test.dart` 有一条契约测试守事件命名，两张白名单
（模块前缀 / 词尾动作）。目前前缀白名单里**没有 `phone_`**。
Story 8.1 刚遇到同样情况（`push_` + `_reported`），处理方式是**扩白名单并写明理由**、
而不是改产品定的名字（改名会推翻已闭合的 OA-7）。本 story 照同一先例。

### 不在本 story 范围

- **后台 AB-11A**（手机号查看 + 批量催填）—— 运营后台，独立排期
- **OTP / 验证 / 手机号登录** —— PRD 明确 Non-goal
- **第二次及以后的自动提醒** —— PRD 明确改为运营后台手动触发
- **同意勾选 / 用途说明文案** —— 用途告知由用户协议承接（PRD §2.2）
- **FR-85 四个触发点** —— 8-2 / 8-3

### ⚠️ 上线前的非技术闸门（OA-2，已闭合但要点一句）

用途告知由用户协议承接。OA-2 已于 2026-08-20 闭合（产品确认已覆盖、另有新版条款文档），
但**新版条款文档的路径尚未登记在架构 delta 的 OA-2 行**上 —— 上线前补上，
否则日后无从追溯"依据哪一版条款"。

### References

- [Source: _bmad-output/planning-artifacts/v1.1.6/决策日志.md#X-21] 新时机与四条判定口径
- [Source: _bmad-output/planning-artifacts/architecture-v1.1.6-delta.md#AD-14] Rule 7（串行：先推送权限后手机号）、Rule 8（软引导全局仅一次 + 新时机四点）、Rule 1/2/3（按设备存储、四键物理隔离、不动旧标记）
- [Source: _bmad-output/planning-artifacts/v1.1.6/PRD-v1.1.6.md#2.2] FR-70 全文：软引导可跳过、「我的」页常驻入口与脱敏展示、清空、Non-goal、同意口径
- [Source: _bmad-output/planning-artifacts/epics-v1.1.6.md#Epic-7] 阻塞全部解除（2026-08-20）
- [Source: _bmad-output/planning-artifacts/v1.1.6/埋点清单v116.md#E-5] `phone_prompt_responded` + `action`
- [Source: _bmad-output/implementation-artifacts/v1.1.6/7-1-手机号字段与保存清空.md] 已交付件与"入口留给 7-2"的标注
- [Source: petgo-backend/src/main/java/com/tailtopia/auth/dto/UserProfileResponse.java] 实读：无 createdAt
- [Source: petgo_app/lib/core/storage/prefs.dart] 现有键与 `petgo.push_permission_asked`

---

## Dev Agent Record

### Context Reference

- 决策 **X-21**（新时机四条判定）；架构 delta AD-14 Rule 1/2/3/7；PRD §2.2 FR-70 全文；埋点清单 E-5

### Agent Model Used

Claude Opus 5 (1M context)

### Debug Log References

- `flutter analyze`：零问题；`flutter test`：**980 条全过**（本 story 新增 27 条）
- 后端 `mvn test`：**1752 条零失败**
- ⚠️ 后端首次全量曾红 7 条（ConsultSuspension / AiUnlock / NotificationController），
  **重跑全量零失败**、这 4 个类单独跑也全绿（41 条）。判为跨用例偶发，与本 story 无交集
  （它们都不碰 `/me` 的响应形状；真被加字段牵动会**稳定**红）。
  🔴 **如实记录：那次失败的具体信息没能取到** —— 单独跑那一轮把 surefire 报告覆盖了。
  故此结论依据是「重跑全绿 + 单类全绿 + 失败类与改动无交集」三点，**不是铁证**。
  这三个类在本仓反复以此方式冒头，值得单独查一次（不属本 story）。

### Completion Notes List

#### 1. 后端只加了一个字段，没有迁移

`/me` 补下发 `createdAt`（`users.created_at` 列早就有）。先写了一条失败测试
（`meReturnsCreatedAtForPhonePromptTiming`）确认它此前确实没下发，再补的。

🔴 **为什么不能用「首次启动时本地记一个日期」绕开**（story 里也写了，这里再钉一次）：
存量用户注册已久却会被当成新人、还要再等两天；重装 App 更是重新计时。
**注册时间只有服务端知道。**

#### 2. 时机判定：两处最容易写错的地方各有正面用例

**① 必须用日历日差，不是小时差。** 注册当天 23:00 与次日 01:00 只相隔 **2 小时**，
却是**两个自然日**。用 `(now - registeredAt).inDays >= 2` 会把第 3 天推迟将近一天。
用例正反两面都造了：相隔 25.5 小时**应该**提示；相隔近 47 小时**不应该**。

**② `>=` 不是 `==`。** 写成「只在第 3 天当天提示」是违规的 —— 那天没打开 App 的人
**永远**收不到。用例里有「第 300 天才打开仍然提示」。

**③ 按 WIB 折算，不用设备时区。** 用户可能在别的时区、也可能手改了时区，
而这条需求里的「天」是**印尼当地的天**。这是本项目**第一处让时区参与判定**
（既有三处 `Asia/Jakarta` 全是展示格式化），故换算**集中在 `PhonePromptTiming` 一处**。

#### 3. ⚠️ 脱敏的测试期望值是**我先写错的**（如实记录）

初版按"点的数量"随手写成 `+62 812•••••7890`，实现出来是 4 个点。
查 UI 稿 05b 屏写的是 `+62 812-••••-7890` ⇒ **前缀保留 5 位数字**（国家码 2 + 运营商 3）。
**以 UI 稿为准改了测试期望**，不是改实现去迁就测试。

另加了一条边界：号码短到「遮不动一位」时**原样返回** ——
输出 `+62 8121234` 这种"看着像脱敏其实没遮"的结果比不遮更糟。

#### 4. 注册位置从 `main.dart` 挪到 `app.dart`（初版放错）

按惯例先放进 `main.dart` 的启动回调，但那里在 **`ProviderScope` 之外**、拿不到 `ref` ——
而判定要读当前用户档案（注册时间、是否已填号码）。`app.dart` 的 `initState` 才有 ref。

注册时所有取值都传**函数**而非值：注册在启动期，而判定所需的数据要到**触发那一刻**
才读（用户可能刚在设置页填过号码）。

#### 5. 只注册钩子，**没有另写一套判定**

8-2 已建好 `PushPermissionPrompt.phonePromptHook`。本 story 只赋值 ——
AD-14 Rule 7 的「先推送权限、后手机号」由那一处保证。
有测试钉住顺序（`order == ['push', 'phone']`）。

#### 6. ⚠️ 一处刻意的取舍：「跳过」与「划走」合并计入 `skipped`

7-1 的编辑抽屉返回的是**一个布尔**（是否保存成功），所以「点跳过」和「划走关闭」
在埋点里都算 `skipped`。要分开得改 7-1 的接口回传更细的结果 ——
**那不在本 story 范围**，故合并计入并在代码注释里标注。

#### 6b. 软引导**复用 7-1 的抽屉**，没有另做一个浮层（子任务措辞的偏离）

T3 的措辞是「浮层内容轻量：一句说明 + 输入 + 保存 / 以后再说」，读起来像要**新建**一个浮层。
实际做法是**直接用 7-1 的 `PhoneEditSheet`**（以 `entry: 'soft_prompt'` 打开）——
实读确认它本来就具备软引导需要的全部形态：输入 + 保存 + **「取消」按钮**（`phoneCancel`）+ 可划走。

理由：另做一个浮层 = 两套界面、两套校验入口、两份双语文案，而 PRD 对软引导的要求
（轻量、可跳过、不加合规文案层）7-1 的抽屉已经满足。**少一套要维护的 UI 是好事。**
⚠️ 代价是「点取消」与「划走」都表现为"未保存"，故埋点合并计入 `skipped`（见上一条）。

#### 7. 复用而非重造（story 明令）

- 校验与保存：全部走 7-1 的 `PhoneEditSheet`，**一行校验都没重写**
  （7-1 那组"印尼人真实会怎么写"的用例是宁松勿紧的依据）
- 入口来源埋点：7-1 的抽屉本就带 `entry` 参数并预留了 `soft_prompt` / `me_page` 两个取值 ——
  正是留给本 story 的，直接用
- 命名白名单加 `phone_` 前缀（按 8-1/8-2 的先例写明理由）；`_responded` 后缀 8-2 已加

#### L2 说明（如实）

L2 项（真机上第 3 天的浮层、设置页脱敏右值的视觉、抽屉内完整号码）**未做**，
需真机/模拟器，留本地人工验收。
⚠️ 验第 3 天那条要改设备日期或造数，L2 时记得连"第 5 天才打开仍会提示"一起看。

### File List

**后端 · 修改**
- `petgo-backend/src/main/java/com/tailtopia/auth/dto/UserProfileResponse.java` —— 加 `createdAt`（无迁移）
- `petgo-backend/src/test/java/com/tailtopia/auth/web/MeControllerEndpointTest.java` —— 1 条

**前端 · 新增**
- `petgo_app/lib/features/me/domain/phone_prompt_timing.dart` —— 第 3 天判定（WIB 换算唯一处）
- `petgo_app/lib/features/me/domain/phone_soft_prompt.dart` —— 编排 + 注册排队钩子
- `petgo_app/lib/features/me/domain/phone_mask.dart` —— 脱敏（显示层）
- `petgo_app/test/me/phone_prompt_timing_test.dart` —— 11 条
- `petgo_app/test/me/phone_soft_prompt_test.dart` —— 10 条
- `petgo_app/test/me/phone_mask_test.dart` —— 6 条

**前端 · 修改**
- `petgo_app/lib/features/auth/domain/login_response.dart` —— 模型加 `createdAt`
- `petgo_app/lib/core/storage/prefs.dart` —— 第四把键 `kPhonePromptShown`（四键物理隔离）
- `petgo_app/lib/app.dart` —— 启动期注册软引导钩子
- `petgo_app/lib/features/me/presentation/settings_page.dart` —— 账号分组常驻入口 + 脱敏右值
- `petgo_app/lib/l10n/app_en.arb` / `app_id.arb` —— `phoneNotSet` 占位文案
- `petgo_app/test/analytics/v112_events_test.dart` —— 命名白名单加 `phone_` 前缀
- `_bmad-output/implementation-artifacts/sprint-status-v1.1.6.yaml` —— 状态流转

---

## Change Log

| 日期 | 变更 |
|---|---|
| 2026-08-20 | 由 create-story 生成，status = ready-for-dev。本 story 随 X-21 定稿解锁 |
| 2026-08-21 | dev-story 实现完成：/me 补 createdAt + 第 3 天判定 + 软引导 + 常驻入口与脱敏 + 埋点，27 条新增测试。Flutter 980 条 / 后端 1752 条全过。status = review |
