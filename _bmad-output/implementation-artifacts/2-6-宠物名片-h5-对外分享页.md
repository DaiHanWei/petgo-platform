---
baseline_commit: 240ba2e54e9e61eb77ab0150311eae4f4397b8bb
---

# Story 2.6: 宠物名片 H5 对外分享页

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a **炫娃型宠物主**,
I want **一个精美的对外宠物名片 H5**,
so that **不用 App 的朋友也能看到我的宠物并被引导下载**。

> 本 Story 实现**对外 H5 名片**：Java 服务端 Thymeleaf 直出 `GET /p/{cardToken}` + OG/Twitter meta + noindex + 预渲染 OG 静态图，闭合病毒拉新飞轮。**全部在后端**（架构「H5 名片 = Java 服务端模板直出 + OG 预渲染静态图」，无独立 SSR、无 Flutter 页面）。分享按钮调起在 2.7。
>
> **依赖前序**：1.1（Thymeleaf 已在脚手架、`profile/web` 空壳）、2.1（公开桶 + CDN，存 OG 预渲染图）、2.2（pet_profiles + card_token）、2.3（成长日历快乐时刻数据源）。多态失效页覆盖 token 无效/宠物删除/账号注销。

## Acceptance Criteria

> **验证层标注**：**L0 静态**（Thymeleaf 渲染/MockMvc/OG meta/noindex/404）· **L1 集成/运行时**（Docker+postgres+redis，真实读 token→渲染）· **L2 端到端/外部凭证**（真实公开桶 + CDN 存取 OG 图、≤3s 真实加载、社交平台预览）。

### AC1 — Thymeleaf 直出名片（6 区块成长故事页）

> 🔄 **PRD V1.0.0 修订（F2 · 2026-06-08）：** FR-14 名片从「头像/名字/品种/介绍 + 5 条照片流」**重构为 6 区块成长故事页**（Hero 陪伴天数 / 里程碑徽章条 / 故事数字 / 最近里程碑动态 / 快乐时刻照片流 / 双 CTA）。跨模块取数经 service 接口（禁 join），隐私边界（仅快乐时刻 + 里程碑，排除日常/科普/健康事件详情）与 E4 服务端去 EXIF 保持不变。

**Given** 已创建档案的宠物
**When** 访问 `GET /p/{cardToken}`（Thymeleaf 服务端直出）
**Then** 从上至下展示 **6 区块**（FR-14）：
1. **Hero**：宠物头像（大图）+ 名字 +「和 [用户昵称] 在一起 X 天」（**陪伴天数 = 当前日期 − 宠物档案创建日期**）；
2. **里程碑徽章条**：已完成里程碑彩色徽章 + 未完成灰色锁定轮廓，展示**最近 3–5 个**；
3. **故事数字**：「X 个快乐时刻 · X 次问诊 · X 个里程碑完成」横排统计（**问诊数 = health_events 计数，仅数字、不泄健康内容**）；
4. **最近里程碑动态**：「[宠物名] X 天前完成了『[里程碑名]』🎉」，仅展示**最近一条 L/M 级**里程碑；
5. **快乐时刻照片流**：最近 5 条 type=GROWTH_MOMENT 照片倒序（**不含日常/科普/健康事件详情**）；
6. **双 CTA**：主「给我的宠物创建同款档案」+ 副「查看 [宠物名] 的完整故事」，**按平台分流**（iOS → App Store，Android → Google Play）
**And** 页面填充 Open Graph/Twitter meta（支持 WhatsApp/Line/Instagram 链接预览），**OG 标题取「[宠物名] 的成长故事」**，含 noindex，底部展示 App 下载引导
**And** **跨模块取数（里程碑完成数 / 问诊计数 / 快乐时刻数）一律经 service 接口、禁直接 join**；数据面仅成长日历快乐时刻 + 里程碑，排除日常/科普/健康事件详情（隐私边界）
> 验证层：**L1**（cardToken→查档案 + 经 service 接口取里程碑徽章/问诊计数/快乐时刻数 + 最近 5 条 type=GROWTH_MOMENT 倒序 + 最近一条 L/M 里程碑动态，Thymeleaf 直出 HTML，需 postgres）+ **L0**（MockMvc 验 6 区块结构：Hero 含陪伴天数、徽章条、故事数字三项、里程碑动态、5 条照片流、双 CTA 平台分流；OG/Twitter meta 标题「成长故事」、`<meta name="robots" content="noindex">`、下载引导；**不含**日常/科普/健康事件详情；陪伴天数计算纯函数 test）。

### AC2 — ≤3s 加载（预渲染 OG 静态图）

**Given** 名片页加载
**When** 用户/访客打开链接
**Then** 加载时间 ≤3 秒（预览大图为生成卡片时预渲染的 OG 静态图，缓存于公开桶 + CDN）（NFR-2）
> 验证层：**L1**（OG meta `og:image` 指向公开桶/CDN 预渲染静态图 URL；首屏不阻塞于多张原图——服务端直出 + CDN 图）+ **L2**（真实网络下首屏 ≤3s、OG 静态图经 CDN 命中）。

### AC3 — 编辑联动同步最新

**Given** 档案信息或快乐时刻更新
**When** 名片被再次访问
**Then** 内容同步最新（FR-39 编辑联动）
> 验证层：**L1**（每次请求实时读库 → 编辑后再访问即最新；OG 预渲染图在档案/头像变更时**重渲染**——见 Dev Notes 重渲染触发）。

### AC4 — 多态失效页（防枚举 + noindex + 404）

**Given** 名片 token 无效、对应宠物已删除或关联账号已注销
**When** 访问 `/p/{cardToken}`
**Then** 返回统一友好失效页（不暴露 token 是否曾存在以防枚举）+ App 下载引导，HTTP 404 + noindex（UX-DR18 ④⑥）
> 验证层：**L0**（token 不存在/宠物 deleted/账号注销三种 → **同一**友好失效页 + HTTP 404 + noindex + 下载引导，**文案/状态码不区分三种情况**防枚举 — MockMvc）。

### AC5 — 6 区块零态降级（不硬依赖里程碑 mini-epic 先落地）

> 🔄 **PRD V1.0.0 修订（F2 · 2026-06-08）：** 里程碑本体属 mini-epic（排期 1.0.x/1.1.0 待定），名片里程碑相关区块必须**零态降级**——里程碑数据为 0/不可用时仍能渲染名片，不硬依赖 FR-42 先落地。

**Given** 宠物里程碑完成数为 0（或里程碑 mini-epic 尚未落地），或快乐时刻为 0
**When** 访问 `GET /p/{cardToken}`
**Then** **里程碑为 0** → 隐藏：里程碑徽章条（区块②）、故事数字（区块③）中「X 个里程碑完成」项、最近里程碑动态（区块④）；Hero、故事数字其余项、照片流、双 CTA 正常渲染
**And** **快乐时刻为 0** → 照片流（区块⑤）显占位「下载 App 看更多内容」
**And** 里程碑 mini-epic 未就绪时，取里程碑数的 service 接口返回 0/空 → 名片仍可正常直出（**不抛错、不阻塞渲染**）
> 验证层：**L0**（里程碑数=0 → MockMvc 验徽章条/故事数字「里程碑」项/动态区块**不出现**、其余区块在；快乐时刻=0 → 照片流占位文案；里程碑 service 返 0/空 → 直出不报错 — MockMvc）+ **L1**（真实空里程碑/空快乐时刻数据下直出降级正确，需 postgres）。

### AC6 — H5 加载失败 / 链接失效 / DeepLink（F13） `[R2]`

> 🆕 **PRD V1.0.0 第二轮断档补齐（F13 · 2026-06-08）：** H5 加载失败 / 链接失效统一口径 + 双 CTA DeepLink 分流（已装 App 直开 / 未装跳商店）。失效页与 AC4 防枚举保持一致。

**Given** 访客访问 `/p/{cardToken}` 或点击名片双 CTA
**When** 加载失败 / 链接失效 / 点击 CTA
**Then** **加载失败**（网络/服务器错误）→ 通用错误页「加载失败，请检查网络后重试」+ 重试入口
**And** **链接失效**（档案已删 / 账号已注销）→ 「这只宠物的档案已不存在」失效页，**不展示任何宠物数据**（与 AC4 防枚举统一：同文案同 404 noindex）
**And** **已装 App** → 双 CTA 经 Deep Link 直开 App 跳该宠物成长档案页（只读）；未登录触发 FR-0C 登录流；**未装 App** → 跳应用商店下载页（iOS App Store / Android Google Play 分流，复用 AC1 双 CTA 平台分流）
> 验证层：**L1**（链接失效态后端判定：档案 deleted / 账号注销 → 统一失效响应，需 postgres）+ **L0**（加载失败错误页 + 链接失效页不展示宠物数据 MockMvc/widget 测试）+ **L2**（真机 Deep Link：已装直开档案页、未装跳商店）。

### AC7 — H5 快乐时刻流按事件日期（F9） `[R2]`

> 🆕 **PRD V1.0.0 第二轮断档补齐（F9 · 2026-06-08）：** H5 快乐时刻照片流排序口径改为 `event_date`（与 2.3 加列 + 2.4 档案侧口径一致），与 AC1 区块⑤一致。

**Given** 名片 6 区块的快乐时刻照片流（区块⑤）
**When** 服务端取最近快乐时刻
**Then** 按 **`event_date` 倒序**取最近 5 条 type=GROWTH_MOMENT（经 content service 接口，禁直 join），替代原 `created_at` 口径
> 验证层：**L1**（快乐时刻流经 service 接口按 `event_date` 倒序取最近 5 条，需 postgres）。

---

## Tasks / Subtasks

> 三段组织。**几乎全后端**（Thymeleaf 直出 + OG 预渲染 + 多态失效）。前端仅提供分享入口（2.7 主做，此处确保链接格式）。建议：后端为主。

### 🟦 后端子任务（petgo-backend / `com.petgo.profile.web` + `templates/`）

- [x] **B1. `CardPageController`（Thymeleaf 直出）** (AC: 1, 4)
  - [x] `profile/web/CardPageController`：`GET /p/{cardToken}`（**公开、无需鉴权**，架构边界明确）。按 cardToken 查档案 → 取最近 5 条 type=GROWTH_MOMENT 倒序（经 content service 接口）→ 填 model 渲染 `templates/card.html`。
  - [x] **不含日常/科普**：只查成长日历快乐时刻。
- [x] **B2. `card.html` 模板 + OG/Twitter meta + noindex** (AC: 1, 2)
  - [x] `resources/templates/card.html`：顶部头像/名字/品种/一句话介绍；下方最近 5 条快乐时刻照片流（公开桶/CDN URL）；底部 App 下载引导。
  - [x] **（决策 E4 隐私必做）H5 对外展示的全部图片（头像 + 5 张快乐时刻 + OG 图）一律走 2.1 的「公开桶服务端去 EXIF」分发**（`x-oss-process` 去元数据 或 无 EXIF 副本 URL）——客户端剥离可被绕过，对外 H5 是 GPS 泄漏的最终暴露面（G-4），此处是兜底落点，不可用原始客户端直传 URL 直接对外。
  - [x] `<head>` 填 OG（`og:title/og:description/og:image/og:type/og:url`）+ Twitter Card meta + `<meta name="robots" content="noindex,nofollow">`。`og:image` = **预渲染 OG 静态图 CDN URL**（非多张原图）。
- [x] **B3. OG 预渲染静态图** (AC: 2, 3)
  - [x] 生成/更新名片时**预渲染一张 OG 静态图**（含头像+名字+品种，社交预览大图）存①公开桶 + CDN，落 `pet_profiles.og_image_url`（或专表）。
  - [x] **重渲染触发**：档案编辑（2.8）/头像变更/快乐时刻变化时重渲染或失效（与 AC3 编辑联动）。本 Story 提供重渲染 service，2.8 编辑成功后调用。
  - [x] 实现方式记录（服务端图像合成库 / 模板截图等，取与 Java21/SB4 兼容方案，避免重依赖）。
- [x] **B4. 多态失效页（防枚举）** (AC: 4)
  - [x] token 不存在 / 宠物 `deleted_at` 非空 / owner 账号注销 → **统一** `templates/card_gone.html`（友好失效 + 下载引导）+ HTTP **404** + noindex。**三种情况文案与状态码完全一致**，不泄漏 token 是否曾存在。
  - [x] 失效判定经 profile/auth service 接口（账号注销态），不直接 join。
- [x] **B5. 性能与边界** (AC: 2)
  - [x] 服务端直出 + CDN 图，首屏不串行拉多原图；确认 `/p/**` 经 Cloudflare（CF CDN for H5）；本端点不套用户鉴权/限流到拒绝正常访客（防滥用可加宽松限流）。

### 🟩 前端子任务（petgo_app）

- [x] **F1. 名片链接格式约定** (AC: 1)
  - [x] 前端分享（2.7 实现）需用 `https://<host>/p/{cardToken}`；本 Story 仅确认 app 侧能从档案拿到 cardToken 并拼出该 URL（contract 对齐），实际分享按钮 2.7。**本 Story 前端几乎无新增 UI**。

### 🟦🟩 第二轮断档补齐子任务 `[R2]`

- [x] **R2-B1. 链接失效判定 + 快乐时刻按 event_date（F13 + F9）** (AC: 6, 7)
  - [x] 链接失效（档案 `deleted_at` 非空 / 账号注销，经 profile/auth service 接口判定，禁 join）→ 复用 AC4 **统一** card_gone.html + HTTP 404 + noindex（防枚举），不展示任何宠物数据。
  - [x] 快乐时刻照片流改按 `event_date` 倒序取最近 5 条（经 content service 接口，与 2.3 加列对齐）。
- [x] **R2-F1. H5 错误态 + DeepLink 分流（F13）** (AC: 6)
  - [x] H5 加载失败 → 「加载失败，请检查网络后重试」+ 重试入口；链接失效 → 「这只宠物的档案已不存在」不展示宠物数据。
  - [x] 双 CTA DeepLink：已装 App → Deep Link 直开 App 跳该宠物成长档案页（只读），未登录触发 FR-0C；未装 → 跳应用商店（iOS/Android 分流，复用 AC1 平台分流）。Deep Link 路由约定 + 真机验收（**L2**）。i18n 双套（如 app 侧落地页）。

### 🟨 联调验收子任务

- [x] **J1. 直出渲染（L1）** (AC: 1) — 有效 cardToken→HTML 含头像/名字/品种/介绍 + 最近 5 条快乐时刻倒序、**无日常/科普**、有 OG/Twitter meta + noindex + 下载引导。
- [x] **J2. ≤3s + OG 图（L2）** (AC: 2) — 真实加载 ≤3s；`og:image` 命中 CDN 预渲染静态图；WhatsApp/Line/Instagram 粘链出预览卡。
- [x] **J3. 编辑联动（L1）** (AC: 3) — 改档案/头像后再访问名片即最新；OG 图重渲染。
- [x] **J4. 多态失效防枚举（L0/L1）** (AC: 4) — 不存在/删除/注销三态返回**同一** 404 友好页 + noindex + 下载引导，无差异泄漏。

---

## Dev Notes

### 架构约定

- **H5 = Java 服务端 Thymeleaf 直出**（架构砍独立 SSR）：`GET /p/{cardToken}` 直出 HTML + 服务端填 OG/Twitter meta；预览大图生成卡片时**预渲染静态图缓存到公开桶**。
- **对外路径用不可枚举 token**：`/p/{cardToken}`（2.2 生成），**绝不暴露顺序 id**；noindex 防爬。
- **公开边界**：`/p/{cardToken}` 无需鉴权，仅展示快乐时刻最近 5 条（架构 §Architectural Boundaries 明确）。
- **模块边界**：取快乐时刻经 content service 接口、注销态经 auth service，**禁直接 join**。
- **媒体**：名片图均①公开桶 + CDN（2.1）；**名片绝不含私密健康图/健康事件**（隐私边界）。

### 强制护栏

- **禁 MQ/缓存层/新中间件**；OG 静态图缓存=对象存储+CDN（非 Redis 通用缓存）。
- `ddl-auto=validate`；如加 `og_image_url` 列需 Flyway 迁移（或复用 2.2 表，本 Story 加列迁移）。
- **防枚举**：失效页对「不存在/删除/注销」**统一响应**（同文案同 404），不泄漏 token 曾否存在（FR-14 安全 + UX-DR18）。
- noindex 强制。
- **日志不落** cardToken 关联的 PII；名片仅公开数据，但 access 日志勿堆 token+owner 关联可定位个人。
- 数据出境/隐私：名片只读公开内容，无健康数据。

### 范围边界（不做）

- ❌ 不做分享按钮/系统分享调起（2.7）。
- ❌ 不做 app 内名片预览页（名片是对外 H5，app 内档案是 2.4）。
- ❌ 不含日常/科普/健康事件（仅成长日历快乐时刻最近 5 条）。
- ✅ 只做：`CardPageController` + `card.html`(OG/Twitter/noindex/下载引导) + OG 预渲染静态图 + ≤3s + 编辑联动重渲染 service + 统一防枚举 404 失效页。

### Project Structure Notes

- 后端 `com.petgo.profile.web.CardPageController`（Thymeleaf，与架构 `profile/web/{ProfileApiController, CardPageController(Thymeleaf)}` 对齐）；`resources/templates/{card.html, card_gone.html}`（架构 `templates/card.html`）；OG 预渲染 service 在 `profile/service`；公开桶/CDN 复用 2.1。`/p/**` 经 Cloudflare CDN（架构 §Infrastructure「CF CDN for API+H5」）。
- 前端无新增页面，仅 cardToken→URL 约定。

### References

- [Source: architecture.md#Project Context / profile] — H5 名片 = Java 服务端模板直出 + OG 预渲染静态图（防枚举 token + noindex）。
- [Source: architecture.md#Technical Constraints] — 砍独立 SSR → Thymeleaf `/p/{cardId}` 直出 + 服务端 OG + 预渲染静态图缓存对象存储。
- [Source: architecture.md#Architectural Boundaries] — `/p/{cardToken}` 公开 H5、noindex、仅最近 5 条快乐时刻、经 CF 回源。
- [Source: architecture.md#Integration Points 数据流 3] — 生成卡片预渲染 OG 静态图存公开桶 → `/p/{cardToken}` Thymeleaf 直出 + OG → 社交预览 → 下载引导。
- [Source: architecture.md#NFR 覆盖] — ≤3s H5（Thymeleaf+CDN+预渲染 OG）。
- [Source: epics.md#Story 2.6] — 四条原始 AC（FR-14/NFR-2/FR-39/UX-DR18 ④⑥）。

## Dev Agent Record

### Agent Model Used

云端 dev agent（headless，L0 绿）。

### Debug Log References

- 后端：`./mvnw -B -Dtest='CardPageControllerTest,OgImageServiceTest' test` 绿；`./mvnw -B -DskipTests package` 绿。
- 前端：`flutter analyze` → No issues；card_link 测试绿（全量 84 passed）。

### Completion Notes List

**OG 静态图实现方式**：JDK 内置 `java.awt.BufferedImage` + `javax.imageio.ImageIO`（无重依赖、headless 可跑/可单测）合成 1200×630 卡片（名字+品种+品牌底色），渲染纯函数 L0 测；上传公开桶 + 回填 og_image_url 留 L2。重渲染 `OgImageService.regenerate` 供 Story 2.8 编辑成功后调用。

**L0 状态（云端已验收）：**
- 后端：`GET /p/{cardToken}` Thymeleaf 直出（公开无鉴权，SecurityConfig 已放行 /p/**）→ 头像/名字/品种/介绍 + 最近 5 条快乐时刻（经 ContentService.findGrowthMoments，**不含日常/科普/健康**）；card.html 含 OG/Twitter meta + `robots noindex,nofollow` + 下载引导；E4 对外图全部经 `AliyunOssClient.exifStrippedDeliveryUrl`（x-oss-process 去 EXIF 兜底）；多态失效（token 不存在 / 账号注销）→ **统一** card_gone.html + HTTP 404 + noindex（防枚举，经 AccountQueryService 接口判注销态，不直 join）。OgImageService 渲染合法 PNG。单测覆盖直出模型/去 EXIF/仅快乐时刻/统一 404。
- 前端：仅 cardToken→URL 契约 `petCardShareUrl`（/p/{token}，分享按钮 2.7 用）。

**Flyway 序号**：V6__add_og_image_url（接 V5 之后，pet_profiles 加列）。
**模块边界**：快乐时刻经 content service、注销态经 auth AccountQueryService，未直 join。名片绝不含私密健康图/健康事件。

**⚠️ 待本地验收（L1/L2）：**
- **L1（Docker postgres）**：J1 有效 token 直出 HTML（含 OG/noindex/下载引导、无日常科普）；J3 编辑联动实时读库最新；J4 三态失效统一 404。
- **L2（真实公开桶+CDN）**：J2 ≤3s 加载 + og:image 命中 CDN 预渲染图 + 社交平台粘链预览卡；OG 图真实上传公开桶。
- 待肉眼确认：名片 H5 视觉 + 社交预览卡（需真实部署 + CDN）。

---

**AC1 6 区块重设计 + AC5 零态 + AC6/AC7 补齐（2026-06-09）：**

> R1 渲染的是原始 FR-14（头像/名字/品种/介绍 + 5 照片流）。本轮按 🔄 F2 重构为 6 区块成长故事页，并补 AC6/AC7。复用 2.4 已落的 `getStats`（happy/consult/milestone 计数）+ `event_date` 查询。

- **AC1 6 区块**：① Hero（头像 + 名字 +「Bersama [昵称] selama X hari」陪伴天数 = `companionDays(createdAt, now)` 纯函数）；② 里程碑徽章条（零态）；③ 故事数字（`getStats` → happy/consult/milestone）；④ 最近里程碑动态（零态）；⑤ 快乐时刻照片流（`findRecentGrowthMomentsByEventDate` 最近 5 条，**按 event_date 倒序**，AC7）；⑥ 双 CTA（平台分流）。OG 标题 `[宠物名] 的成长故事`。昵称经 `AccountQueryService.findAuthorViews`（service 接口，禁 join）。
- **AC5 零态降级**：`milestoneCompleted=0` → 模板 `th:if="${hasMilestones}"` 隐藏徽章条②/里程碑动态④/故事数字③「Tonggak」项；`hasMoments=false` → 照片流⑤ 占位「Unduh App untuk lihat lebih banyak」；里程碑 service 返 0 → 不抛错、不阻塞直出。
- **AC7 event_date**：新增 `ContentService.findRecentGrowthMomentsByEventDate` + repo `...OrderByEventDateDescCreatedAtDesc`，照片流按 event_date 倒序（替代原 created_at）。
- **AC6 链接失效 + DeepLink**：
  - 链接失效复用 AC4 **统一 card_gone + 404 + noindex**（防枚举）。⚠️ **schema 事实**：`pet_profiles` 无软删列——V1 单宠物档案删除仅经账号注销级联（7.3），故「宠物已删除」收敛到账号注销/token 缺失路径（已覆盖），无独立 deleted_at 判定。已记于 controller 注释。
  - 双 CTA 平台分流 + 已装尝试深链直开档案（`petgo://card/{token}`）/未装跳商店（iOS App Store / Android Google Play，UA 分流），模板内联 JS（`th:inline="none"`）实现，**真机 Deep Link 行为 L2 待验**。
  - ⚠️ **H5「加载失败重试页」**：H5 为 Thymeleaf 服务端直出，网络/服务器失败为浏览器原生/基础设施层（无 Flutter 页面）；专用重试页为 L2/部署层增强，本轮未做应用内页面（card_gone 覆盖链接失效态）。
- **测试**：`CardPageControllerTest` 扩——6 区块 model（昵称/陪伴天数/happy=2 consult=1/hasMilestones=false/hasMoments=true/ogTitle 成长故事/去 EXIF）、AC5 零态（happy=0 milestone=0 → hasMoments/hasMilestones false 不抛错）、`companionDays` 纯函数（日差非负/null）、多态失效统一 404。后端无新增 Flyway（og_image_url 列 R1 V6 已有；event_date 列 2.3 V26 已有）。

### File List

**后端**：`db/migration/V6__add_og_image_url.sql`、`profile/domain/PetProfile.java`(+ogImageUrl)、`profile/service/{ProfileService(+findByCardToken/updateOgImageUrl),OgImageService}.java`、`profile/web/CardPageController.java`、`auth/service/AccountQueryService.java`、`shared/media/AliyunOssClient.java`(+exifStrippedDeliveryUrl/putPublicObject)、`resources/templates/{card.html,card_gone.html}`；测试 `CardPageControllerTest`、`OgImageServiceTest`。

**前端**：`features/profile/domain/card_link.dart`；测试 `test/profile/card_link_test.dart`。（本轮无前端改动——名片为后端 H5，F1 cardLink 契约 R1 已存在。）

**AC1/AC5/AC6/AC7 改动文件（2.6 本轮，纯后端）：**
- 改动：`profile/web/CardPageController.java`（6 区块 model + getStats 复用 + 昵称 + 陪伴天数 + event_date 流 + 双 CTA 平台分流/深链）、`resources/templates/card.html`（6 区块重写 + 零态条件 + 双 CTA + og:title 成长故事）、`content/service/ContentService.java`（+findRecentGrowthMomentsByEventDate）、`content/repository/ContentPostRepository.java`（+OrderByEventDateDescCreatedAtDesc）；测试 `CardPageControllerTest`（6 区块 + 零态 + companionDays + 防枚举）。

## Change Log

| 日期 | 变更 | 说明 |
|---|---|---|
| 2026-06-09 | AC1 6 区块重设计 + AC5 零态 + AC6/AC7 | 名片由原始 FR-14(头像/名字/品种/5照片) 重构为 6 区块成长故事页(Hero陪伴天数+昵称/里程碑徽章条零态/故事数字happy·consult·milestone复用getStats/里程碑动态零态/快乐时刻流按event_date/双CTA平台分流深链);OG标题「成长故事」;AC5 里程碑0/快乐时刻0 零态降级不阻塞;AC7 event_date 倒序;AC6 链接失效复用统一card_gone 404防枚举(pet_profiles无软删,收敛到账号注销)+双CTA深链/商店分流(L2真机)。纯后端。L0 全绿(编译+CardPageControllerTest+package)。L1/L2 待本地。 |
