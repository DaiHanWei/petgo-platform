---
baseline_commit: a240e6ea583aa6ed65ebf14e26a3e1f30c871a1c
---
# Story 1.1: 支付网关基础设施与幂等地基

Status: review

> V1.1 增量首个 story。Epic 1（PawCoin 钱包与充值 / 资金地基）。**brownfield**：V1.0 已上线，Flyway 停在 V46，本 story 用 **V60**。
> 源：`_bmad-output/planning-artifacts/epics-v1.1.md` Story 1.1 · 架构：`architecture-v1.1-delta.md` §2/§3.1/§4 · 排期：`sprint-status-v1.1.yaml`（V60=init_payment_intents 已固化）。
> **范围边界**：本 story 只建**支付网关抽象 + 幂等 + payment_intents 意图状态机 + 回调去重**。**不做**充值下单闭环（Story 1.3）、**不做**双分录总账/PawCoin 钱包（Story 1.2）。本 story 用 `mode=stub` 即可跑通 L0/L1，`mode=live` 才打真实 Midtrans（L2）。

## Story

As a 平台,
I want 一套抽象的支付网关（Midtrans 收款）+ 幂等锚 + 回调/轮询双通道去重,
so that 后续一切收费场景（AI 解锁、兽医咨询、PawCoin 充值、身份证高清）都建在同一个可靠、可替换、防重复入账的支付基座上（FR-43 / FR-NFR-2）。

## Acceptance Criteria

1. **模块与网关抽象就位（L0）**
   **Given** 新增 `shared/pay`（基础设施）与 `pay`（业务模块）
   **When** 定义 `PaymentGateway` 接口 + `StubPaymentGateway`（默认）+ `MidtransGateway`（`mode=live`）+ `PayConfig`/`PayProperties` + `PayException`
   **Then** `mvn -B compile` 通过；`PayConfig` 按 `petgo.pay.mode` 选 bean（stub/live），无凭证也能装配启动

2. **payment_intents 表与实体（L0/L1）**
   **Given** Flyway `V60__init_payment_intents.sql` + `PaymentIntent` 实体 + `PaymentIntentRepository`
   **Then** schema `snake_case`、枚举 `varchar`+CHECK、`timestamptz` UTC、`public_token` 唯一、`gateway_ref` 唯一（回调去重库级兜底）、`@Version` 乐观锁（**L0** 编译 + 单测）
   **And** `mvn -B package` 绿灯，`ddl-auto=validate` 下实体与 V60 schema 契约一致（**L1**）

3. **意图状态机幂等（L0/L1）**
   **Given** `PaymentIntentService.createIntent(purpose, amount, channel, idempotencyKey)`
   **When** 同一 `Idempotency-Key` 并发/重放创建
   **Then** 复用既有 `shared/ratelimit/IdempotencyService` 命中即返回既有 intent、不重复建单（**L1**）
   **And** 状态迁移 `PENDING → PAID/FAILED/EXPIRED` 由实体方法守卫、非法迁移抛 `AppException.conflict`（**L0**）

4. **回调验签 + 双通道去重（L1）**
   **Given** `PayCallbackController`（`POST /pay/callback`，内网/白名单，不带 `/api/v1`）
   **When** 同一笔支付经回调 + 轮询两通道各到一次
   **Then** 仅推进一次意图状态、二次入账被幂等拒绝（Redis 前置 + `gateway_ref` 唯一约束权威）（**L1**）
   **And** Midtrans `signature_key`（SHA-512）验签失败即拒（`AppException.forbidden`）、密钥 env 注入不入库、**日志绝不打印 body/凭证/签名**（**L1**）

5. **配置与凭证接线（L0/L1 + L2 前置）**
   **Given** `application.yml` 加 `petgo.pay` 块 + `.env.example` 加占位块
   **Then** `mode=stub` 默认免凭证、`MIDTRANS_SERVER_KEY`/`MIDTRANS_CALLBACK_TOKEN` 留空占位（**L0/L1**）
   **And** `mode=live` 用真实 Midtrans **sandbox** 凭证跑通一笔 QRIS 收款回调（**L2**，留本地/带凭证环境，Completion Notes 标注）

## Tasks / Subtasks

> 纪律：后端单侧 story（无前端）。先接口/配置基建 → 实体/迁移 → service/回调 → 测试。每个子任务标验证层级。

- [x] **T1 网关抽象与配置基建**（AC1、AC5）
  - [x] `shared/pay/PaymentGateway.java`：接口方法 `createCharge(...)`（返回网关订单号 `gatewayRef` + 二维码/deeplink 载荷）、`verifyCallback(Map<String,Object> body): boolean`、`parseCallback(...)`（→ 内部支付结果 `PaymentCallback`）。**L0** ✅
  - [x] `shared/pay/StubPaymentGateway.java`：无凭证确定性实现（`createCharge` 返回伪 `gatewayRef=stub-<orderId>`；`verifyCallback` 校验固定 `callbackToken`，未配则 dev 放行）。默认 bean。**L0** ✅
  - [x] `shared/pay/MidtransGateway.java`：`RestClient`（照 `shared/ai/GeminiDeveloperApiClient` 骨架，`SimpleClientHttpRequestFactory` + timeout；`Authorization: Basic base64(serverKey:)` header）；验签用纯 JDK `MessageDigest` SHA-512(`order_id+status_code+gross_amount+serverKey`)，`MessageDigest.isEqual` 常量时间比对。**L0**（验签确定性单测）✅
  - [x] `shared/pay/PayConfig.java` + `PayProperties.java`（`@ConfigurationProperties("petgo.pay")`，`mode` 默认 `stub`）：照 `shared/ai/AiConfig`+`GeminiProperties`。**L0** ✅
  - [x] `shared/pay/PayException.java`：照 `shared/ai/GeminiException`（异常只 log 类名，不带 body/凭证）。**L0** ✅
  - [x] 附带基础设施 DTO：`ChargeRequest`/`ChargeResult`/`PaymentCallback`(record) + `GatewayStatus`(enum，网关四态，解耦业务领域) + `CallbackParser`(桩/实共用归一化+脱敏 `signature_key`) + `package-info`。**L0** ✅
  - [x] `application.yml` 加 `petgo.pay` 块（im 块后）；`.env.example` 加带注释占位块（凭证留空 + 「stub 默认免凭证 / 真实 key 绝不入库不落日志」）。**L0** ✅
- [x] **T2 payment_intents 表与实体**（AC2）
  - [x] `src/main/resources/db/migration/V60__init_payment_intents.sql`（Dev Notes 完整 SQL 照抄；顶部注释写清 story + 号段推导 V46+1 + 护栏）。**L1**（scratch 库验证 ✅，本地 dev 库 validate 见 Completion Notes）
  - [x] `pay/domain/PaymentIntent.java`（`@Entity`；`Long id` IDENTITY；`publicToken` 由 service 用 `CardTokenGenerator` 生成传入；`@Enumerated(STRING) length=16` 的 `PaymentStatus`；`purpose`/`channel` 枚举；`amount long`；`gatewayMeta` JSONB `@JdbcTypeCode(SqlTypes.JSON)`；`@Version long`；`Instant createdAt/updatedAt` + `@PrePersist/@PreUpdate`；状态迁移方法内 `requireStatus` 守卫）。**L0** ✅
  - [x] `pay/domain/PaymentStatus.java`(`PENDING/PAID/FAILED/EXPIRED`+`isTerminal()`)、`PaymentPurpose.java`(`VET_CONSULT/PAWCOIN_TOPUP/AI_UNLOCK/ID_HD`)、`PayChannel.java`(`QRIS/DANA/PAWCOIN`)。**L0** ✅
  - [x] `pay/repository/PaymentIntentRepository.java`（`extends JpaRepository`；`findByPublicToken`、`findByGatewayRef`）。**L0** ✅
  - [x] `pay/dto/PaymentIntentResponse.java`（record + 静态 `of(entity)`，**只暴露 publicToken 不暴露 id/gatewayRef/快照**）。**L0** ✅
- [x] **T3 意图状态机 service 与回调**（AC3、AC4）
  - [x] `pay/service/PaymentIntentService.java`：`createIntent(...)`（复用 `IdempotencyService.findResourceId/store` 幂等 + `RedisRateLimiter` 写限流）、`applyCallback(PaymentCallback)`（回调/轮询单一收口，Redis 前置 + 「已终态即返回」+ `gateway_ref` 唯一去重；到账 `markPaid` 后**同事务**发 `PaymentIntentPaidEvent` hook）。**L1** ✅（逻辑 L0 mock 覆盖）
  - [x] `pay/web/PayCallbackController.java`（`POST /pay/callback`，**不带 `/api/v1`**；`gateway.verifyCallback` 失败→`AppException.forbidden`；去重后推进状态；应答 `{status:OK}`）。照 `shared/im/web/ImCallbackController` + SecurityConfig permitAll。**L1** ✅
  - [x] `pay/event/PaymentIntentPaidEvent.java`：到账领域事件（本 story 无消费者，仅落发布点 hook 供 1.2/1.3 同事务入账；注释禁 AFTER_COMMIT 异步）。**L0** ✅
- [x] **T4 测试**（AC1-5）
  - [x] L0 单测：`MidtransGatewayTest`（验签确定性 + 脱敏 + 桩确定性 + 状态映射）、`PaymentIntentTest`（状态迁移守卫）、`PaymentIntentServiceTest`（mock `IdempotencyService`/repo/redis/events 验幂等建单 + 回调去重 + 发事件）。照 `vet/service/VetPresenceServiceTest`。**L0** ✅ 17/17 绿
  - [x] L1 集成：`PaymentIntentIntegrationTest extends ApiIntegrationTest`（真 postgres/redis dev profile；验 Flyway V60 + validate；回调双通道去重 + 终态守卫 + 未知 order 忽略）。照 `admin/anomaly/ConsultAnomalyIntegrationTest`。**L1**（已编译；本地 dev 库污染阻断，见 Completion Notes）
  - [x] 云端 headless 只跑 **L0 绿灯**；L1/L2 留本地，Completion Notes 标注「L1（docker pg+redis）/L2（Midtrans sandbox 凭证）待本地验收」。

### Review Findings（跨 1.1-1.3 联审 2026-07-11，三层对抗式）

- [x] [Review][Patch] ✅已修 **【安全·必修】网关 verifyCallback fail-open**：PayConfig 启动即 fail-closed（prod 禁 stub / live 缺 serverKey 或 callbackToken 即拒启动）+ MidtransGateway.verifyCallback serverKey 空即拒。回归测试 `PayConfigTest`(5) 锁死。
- [ ] [Review][Patch] **[D1 已定论·待实现] 乱序回调 late-PAID 被终态守卫静默吞** [PaymentIntentService.java:107-113]：处置 = **记对账异常 + 告警，人工处理**（不自动改账）。迟到 PAID 命中非-PAID 终态时落一条对账异常记录 + WARN 告警，由人工核实后手动入账/退款。本轮未实现（补丁范围仅 P1/P2/P3），下批做。
- [ ] [Review][Patch] applyCallback 未核对回调 `gross_amount` 与意图 `amount` → 少付/部分结算仍发满额 koin [PaymentIntentService.java:120-127]
- [ ] [Review][Patch] 【低·L2】`capture` 无条件判 PAID，忽略 `fraud_status`（V1 仅 QRIS/DANA 走 settlement，不可达；卡支付启用即隐患）[GatewayStatus.java:26]
- [ ] [Review][Patch] 【trivial】javadoc 称 BEFORE_COMMIT 与实际 `@EventListener` 同步内联不符，仅文档措辞 [PaymentIntentService.java:35]

## Dev Notes

> 以下范式来自对现有 V1.0 代码的实测勘探，**照抄不另起炉灶**（brownfield 铁律）。

### 技术栈实测确认
- Spring Boot **4.0.6** / Java **21**；HTTP 客户端 = Spring **`RestClient`**（同步，Boot 4 内置，**不用 WebClient**）。
- **Jackson 3**：生产代码内部自建 `ObjectMapper` 时注意；**测试基类注入的是 `tools.jackson.databind.ObjectMapper`（Jackson 3），别用 `com.fasterxml`**。
- **无 Testcontainers**：L1 打**本地 docker-compose 的真 postgres+redis**（dev profile）。

### 外部 HTTP 客户端范式（照 `shared/ai/GeminiDeveloperApiClient.java` + `shared/im/LiveTencentImClient.java`）
```java
public MidtransGateway(PayProperties props) {
    Duration t = Duration.ofSeconds(props.getTimeoutSeconds());
    SimpleClientHttpRequestFactory rf = new SimpleClientHttpRequestFactory();
    rf.setConnectTimeout(t); rf.setReadTimeout(t);
    this.client = RestClient.builder().baseUrl(props.getBaseUrl()).requestFactory(rf).build();
}
// 密钥走 header（Authorization: Basic base64(serverKey:)），绝不入 URL query（避免落日志）
```
异常转译护栏：`catch (RuntimeException e){ log.warn("支付网关调用失败: {}", e.getClass().getSimpleName()); throw new PayException("支付网关调用失败"); }` —— **只 log 异常类名，绝不打印 body/凭证/签名 URL/上游堆栈**。验签用纯 JDK（照 `LiveTencentImClient.hmacSha256`/`verifyCallback`，常量时间比对）。

### 幂等（复用，勿另造）——`shared/ratelimit/IdempotencyService.java`
```java
Optional<Long> findResourceId(String idempotencyKey)   // 命中即取回既有资源 id
void store(String idempotencyKey, long resourceId)      // TTL 24h, PREFIX="idem:"
```
用法照 `ContentService.publish`（入口先 `findResourceId` 命中返回、落库后 `store`）。**回调去重必须库级唯一约束兜底**（`gateway_ref` UNIQUE，照 `consult_anomalies.session_id` 去重范式）——因回调可能超 24h Redis TTL，Redis 仅作前置、DB 唯一约束作权威。写端点加 `RedisRateLimiter.check(...)`（超限 `AppException.rateLimited`→429）。

### config 范式（照 `shared/ai/AiConfig` 的 mode 选 bean）
```java
@Configuration @EnableConfigurationProperties(PayProperties.class)
public class PayConfig {
  @Bean PaymentGateway paymentGateway(PayProperties p){
    return "live".equalsIgnoreCase(p.getMode()) ? new MidtransGateway(p) : new StubPaymentGateway(p);
  }
}
```
`application.yml`（加在 `petgo:` 下、`im:` 块后）：
```yaml
  pay:
    mode: ${PAY_MODE:stub}
    server-key: ${MIDTRANS_SERVER_KEY:}
    base-url: ${MIDTRANS_BASE_URL:https://api.sandbox.midtrans.com}
    callback-token: ${MIDTRANS_CALLBACK_TOKEN:}
    timeout-seconds: ${PAY_TIMEOUT_SECONDS:10}
```

### 实体范式（照 `consult/domain/ConsultSession.java`）
- 主键 `Long id` IDENTITY；对外只暴露 `public_token`（`profile/service/CardTokenGenerator` = `SecureRandom` base62，绝不由顺序 id 派生）。
- 枚举 `@Enumerated(STRING)` + `length=16`（**禁 length=1**，Hibernate6 映 CHAR(1)→validate 全红）。
- `Instant createdAt/updatedAt` + `@PrePersist/@PreUpdate` 赋值（实体层管，迁移层也给 `DEFAULT now()`，并存）。
- JSONB：`@JdbcTypeCode(SqlTypes.JSON) Map<String,Object> gatewayMeta`。
- `@Version long version` 乐观锁（回调并发/状态迁移 CAS）。
- 状态迁移封装实体方法内、`requireStatus()` 守卫抛 `AppException.conflict`。

### 回调 Controller（照 `shared/im/web/ImCallbackController.java`）
`POST /pay/callback`（**不带 `/api/v1`**，内网/白名单语义同 `/im/callback`）；`gateway.verifyCallback(body)` 失败→`AppException.forbidden`；面向 App 的**创建**端点才走 `/api/v1`（JWT + 写限流 + `Idempotency-Key` 头）。错误统一 `AppException.*` → 全局 RFC 9457 ProblemDetail（`shared/error/AppException.java`）。

### V60 迁移完整 SQL（照 `V44__init_consult_anomalies.sql` 风格）
```sql
-- Story 1.1（V1.1 资金地基）：payment_intents 支付意图表。原占 V47，因 feat/content-moderation 已在 stag 占 V47-56、stag 到 V59，V1.1 让号从 V60 起（决策 E2 跨分支撞号重排）。
-- 凭证/网关正文绝不入库；金额用最小币种单位整型(IDR 无小数)；status 枚举 varchar + CHECK；
-- gateway_ref 唯一 = 回调/轮询双通道去重的库级权威兜底（Redis 仅前置）。
CREATE TABLE payment_intents (
    id            BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    public_token  VARCHAR(32)  NOT NULL,
    user_id       BIGINT       NOT NULL,
    purpose       VARCHAR(16)  NOT NULL,   -- VET_CONSULT | PAWCOIN_TOPUP | AI_UNLOCK | ID_HD
    channel       VARCHAR(16)  NOT NULL,   -- QRIS | DANA | PAWCOIN
    amount        BIGINT       NOT NULL,
    currency      VARCHAR(8)   NOT NULL DEFAULT 'IDR',
    status        VARCHAR(16)  NOT NULL,   -- PENDING | PAID | FAILED | EXPIRED
    gateway_ref   VARCHAR(128),            -- 网关订单号，回调去重
    gateway_meta  JSONB,                   -- 原始快照（脱敏）
    version       BIGINT       NOT NULL DEFAULT 0,
    created_at    TIMESTAMPTZ  NOT NULL DEFAULT now(),
    updated_at    TIMESTAMPTZ  NOT NULL DEFAULT now(),
    CONSTRAINT ck_payment_intents_status  CHECK (status  IN ('PENDING','PAID','FAILED','EXPIRED')),
    CONSTRAINT ck_payment_intents_purpose CHECK (purpose IN ('VET_CONSULT','PAWCOIN_TOPUP','AI_UNLOCK','ID_HD')),
    CONSTRAINT ck_payment_intents_channel CHECK (channel IN ('QRIS','DANA','PAWCOIN'))
);
CREATE UNIQUE INDEX uq_payment_intents_token       ON payment_intents (public_token);
CREATE UNIQUE INDEX uq_payment_intents_gateway_ref ON payment_intents (gateway_ref);
CREATE INDEX        idx_payment_intents_user       ON payment_intents (user_id);
CREATE INDEX        idx_payment_intents_status     ON payment_intents (status);
```
命名约定：`uq_<表>_<列>` / `idx_<表>_<列>` / `ck_<表>_<语义>`。**决策 E2：已提交迁移冻结，加列一律新起 ALTER**。

### 测试范式
- **L0**：`@ExtendWith(MockitoExtension.class)` + `@Mock` + AssertJ（照 `vet/service/VetPresenceServiceTest`）。验签用固定输入断言确定性输出（照 `LiveTencentImClient` 的 `Clock` 注入思路）。
- **L1**：`extends ApiIntegrationTest`（`@SpringBootTest(webEnvironment=MOCK) @AutoConfigureMockMvc @ActiveProfiles("dev")`，真 pg+redis，`SEQ.incrementAndGet()` 造数避撞；`userBearer(id)` JWT）。上下文启动即验 Flyway V60 + `validate`。
- **CLAUDE.md 纪律**：云端 headless 只跑 L0；L1/L2 留本地并在 Completion Notes 标注。

### 资金四不变式在本 story 的落点（FR-NFR-2 幂等；1/3/4 在 1.2/后续）
本 story 只兑现 **FR-NFR-2 幂等**（支付创建 + 回调双通道去重）。**双分录总账（NFR-1）、PawCoin 非负（NFR-3）、审计哈希链（NFR-4）不在本 story**，属 Story 1.2 / Epic 4/9。本 story 的 intent **不直接改余额**——`markPaid` 仅推进意图状态并发领域事件/预留 hook 供 1.2/1.3 消费。

### Project Structure Notes
- 新增 `com.tailtopia.pay/{domain,repository,dto,service,web}` + `com.tailtopia.shared.pay/*`，与现有 8+ 模块（auth/triage/consult/content/profile/notify/moderation/vet/account/admin）并列，边界一致。
- 模块间只经 service/领域事件通信、禁跨模块直访 repository（延续 V1.0 护栏）。
- 无前端改动（纯后端 story）。

### References
- [Source: _bmad-output/planning-artifacts/epics-v1.1.md#Story 1.1]
- [Source: _bmad-output/planning-artifacts/architecture-v1.1-delta.md#§2 支付聚合商][#§3.1 资金核心][#§4 资金正确性地基]
- [Source: _bmad-output/implementation-artifacts/sprint-status-v1.1.yaml#FLYWAY V60]
- 代码范式：`shared/ai/GeminiDeveloperApiClient.java`、`shared/ai/AiConfig.java`、`shared/im/LiveTencentImClient.java`、`shared/im/web/ImCallbackController.java`、`shared/ratelimit/IdempotencyService.java`、`shared/ratelimit/RedisRateLimiter.java`、`shared/error/AppException.java`、`consult/domain/ConsultSession.java`、`profile/service/CardTokenGenerator.java`、`db/migration/V44__init_consult_anomalies.sql`、`test/.../support/ApiIntegrationTest.java`、`test/.../vet/service/VetPresenceServiceTest.java`

### 待澄清（不阻塞本 story，L2 前收敛）
- [OPEN] Midtrans 商务合同 + sandbox 凭证获取（AC5 的 L2 前置；整条资金脊柱 lead time，建议即刻并行推进）。
- [OPEN] Midtrans 具体 API（Snap vs Core/Charge）与 QRIS 轮询超时时长——本 story 网关接口设计需容纳，实现细节可在拿到凭证后微调 `MidtransGateway`。

## Dev Agent Record

### Agent Model Used

claude-opus-4-8（bmad-dev-story 流程，本地 darwin）。

### Debug Log References

- `mvnw -B test -Dtest=MidtransGatewayTest,PaymentIntentTest,PaymentIntentServiceTest` → **17/17 绿**（L0）。
- L1 `PaymentIntentIntegrationTest` 已编译（172 test sources 全通过 test-compile），运行时因**本地 dev 库污染**阻断（见下）。
- V60 迁移正确性用一次性 scratch 库真验：单独 apply V60 → 表/列/索引契约与实体逐一吻合；全量 V1→V60 按序 apply → 28 表全建、无冲突（零副作用，scratch 库跑完即销毁）。

### Completion Notes List

- **L1 已本地清账（2026-07-11）**：在专用干净库 `petgo_l1`（避开被 content-moderation 污染的共享 `petgo` 库）+ `mvn clean`（清 target 陈旧迁移）跑通。Flyway V1→V46→V60→V61 全链干净有序 + `ddl-auto=validate` 通过；`PaymentIntentIntegrationTest` 3/3（幂等建单 + 回调双通道去重 + 终态守卫 + 未知 order 忽略）。L2（Midtrans sandbox 凭证）仍待商务合同。

- **L0 全绿（云端可复现的验收基线）**：网关抽象/验签/桩确定性、意图状态机守卫、service 幂等（建单 + 回调双通道去重 + 到账发事件）三类 17 用例通过。`mvn -B compile` 通过。
- **资金四不变式落点**：本 story 只兑现 **FR-NFR-2 幂等**——`createIntent` 复用 `IdempotencyService`（同 Idempotency-Key 重放取回既有意图）；`applyCallback` 三闸去重（Redis 前置 + 「已终态即返回」+ `gateway_ref` 唯一约束库级权威），回调/轮询双通道**只推进一次**。意图**不直接改余额**——`markPaid` 仅推进状态并在**同 `@Transactional`** 内发 `PaymentIntentPaidEvent`（本 story 无消费者，入账 hook 留 1.2/1.3；注释显式禁 AFTER_COMMIT 异步，规避 1.3 Dev Notes 的静默吞写血泪）。
- **安全护栏落实**：Server Key 走 `Authorization: Basic base64(serverKey:)` header（绝不入 URL query）；验签 SHA-512 + `MessageDigest.isEqual` 常量时间比对；`signature_key` 在 `CallbackParser`/charge 快照里被剔除，**绝不落 gateway_meta / 不漂日志**；异常只 log 类名。`server-key`/`callback-token` 全 env 注入，`.env.example` 仅占位。`/pay/callback` 已在 SecurityConfig permitAll（内网/白名单语义，验签兜底）。
- **schema 契约（AC2 L1）已 scratch 库真验**：V60 产出 `enum→varchar(16)`、`gateway_meta→jsonb`、`version→bigint default 0`、时间戳→`timestamptz default now()`，无 CHAR(1) 陷阱；`uq_*/idx_*/ck_*` 命名合约一致 —— 与 `PaymentIntent` 实体的 Hibernate `validate` 期望逐列吻合。
- **⚠️ L1（docker pg+redis）/ L2（Midtrans sandbox 凭证）待本地验收**：
  - L1 完整 Spring context validate + 回调去重回归**被本地 dev 库污染阻断**——本地 `flyway_schema_history` 存有 V60/V61/V49 = 一套 **moderation 迁移**（`init_moderation_keyword_rules` 等），**不在本仓库/本分支**（本分支基线 V46），系此前某实验分支在本地库的残留，与 checksum 冲突致 `Validate failed`。**非本 story 代码缺陷**（迁移与契约已 scratch 库证明正确）。用户已决策「仅 L0，L1 留后续」——不重置本地 dev 库。跑完整 L1 需在纯净 dev 库上执行（会同时解锁 1.2/1.3 的 L1）。
  - L2：Midtrans 商务合同 + sandbox 凭证到位后，`PAY_MODE=live` 跑通一笔 QRIS 收款回调（`MidtransGateway.createCharge` 的 Core API 字段/QRIS 轮询超时可届时微调，接口已容纳）。

### File List

**新增（后端 main）**
- `petgo-backend/src/main/java/com/tailtopia/shared/pay/package-info.java`
- `petgo-backend/src/main/java/com/tailtopia/shared/pay/PaymentGateway.java`
- `petgo-backend/src/main/java/com/tailtopia/shared/pay/StubPaymentGateway.java`
- `petgo-backend/src/main/java/com/tailtopia/shared/pay/MidtransGateway.java`
- `petgo-backend/src/main/java/com/tailtopia/shared/pay/PayConfig.java`
- `petgo-backend/src/main/java/com/tailtopia/shared/pay/PayProperties.java`
- `petgo-backend/src/main/java/com/tailtopia/shared/pay/PayException.java`
- `petgo-backend/src/main/java/com/tailtopia/shared/pay/GatewayStatus.java`
- `petgo-backend/src/main/java/com/tailtopia/shared/pay/ChargeRequest.java`
- `petgo-backend/src/main/java/com/tailtopia/shared/pay/ChargeResult.java`
- `petgo-backend/src/main/java/com/tailtopia/shared/pay/PaymentCallback.java`
- `petgo-backend/src/main/java/com/tailtopia/shared/pay/CallbackParser.java`
- `petgo-backend/src/main/java/com/tailtopia/pay/domain/PaymentIntent.java`
- `petgo-backend/src/main/java/com/tailtopia/pay/domain/PaymentStatus.java`
- `petgo-backend/src/main/java/com/tailtopia/pay/domain/PaymentPurpose.java`
- `petgo-backend/src/main/java/com/tailtopia/pay/domain/PayChannel.java`
- `petgo-backend/src/main/java/com/tailtopia/pay/repository/PaymentIntentRepository.java`
- `petgo-backend/src/main/java/com/tailtopia/pay/dto/PaymentIntentResponse.java`
- `petgo-backend/src/main/java/com/tailtopia/pay/service/PaymentIntentService.java`
- `petgo-backend/src/main/java/com/tailtopia/pay/web/PayCallbackController.java`
- `petgo-backend/src/main/java/com/tailtopia/pay/event/PaymentIntentPaidEvent.java`
- `petgo-backend/src/main/resources/db/migration/V60__init_payment_intents.sql`

**新增（后端 test）**
- `petgo-backend/src/test/java/com/tailtopia/shared/pay/MidtransGatewayTest.java`
- `petgo-backend/src/test/java/com/tailtopia/pay/domain/PaymentIntentTest.java`
- `petgo-backend/src/test/java/com/tailtopia/pay/service/PaymentIntentServiceTest.java`
- `petgo-backend/src/test/java/com/tailtopia/pay/PaymentIntentIntegrationTest.java`

**修改**
- `petgo-backend/src/main/resources/application.yml`（加 `petgo.pay` 块）
- `petgo-backend/.env.example`（加 Midtrans 占位块）
- `petgo-backend/src/main/java/com/tailtopia/shared/security/SecurityConfig.java`（`/pay/callback` permitAll）

### Change Log

- 2026-07-11：实现 Story 1.1 支付网关基础设施与幂等地基（网关抽象 + payment_intents 意图状态机 + 幂等 + 回调/轮询双通道去重 + 到账事件 hook）。L0 17/17 绿；V60 迁移 scratch 库真验；L1/L2 待本地（dev 库污染，用户决策仅 L0）。
