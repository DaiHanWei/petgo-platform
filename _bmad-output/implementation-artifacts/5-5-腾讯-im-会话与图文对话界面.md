# Story 5.5: 腾讯 IM 会话与图文对话界面

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a **用户与兽医**,
I want **实时图文/视频对话并附专业辅助**,
so that **我能获得专业指引、兽医能高效判断**。

> 本 Story 是 Epic 5 的**核心实时段**：兽医在「待接单」接单 → 后端经腾讯 IM 建会话、状态 `WAITING→IN_PROGRESS`、双方进入图文/视频（≤60s）对话界面、切 Tab 后台保持连接消息连续（NFR-5）、兽医侧 FR-5 辅助工具（AI 参考回复 + 历史摘要冷启动）、用户侧免责提示。
>
> 做：接单流程（待接单列表 → 接受 → IM 建会话 → 状态迁移 → 双端进入对话）、腾讯 IM Flutter SDK 直连对话界面（文字/图片/视频 ≤60s）、后台保连消息连续、兽医 FR-5 辅助（AI 参考回复，历史摘要冷启动空库仅给 AI 参考）、用户侧免责提示、接单时兽医置 BUSY。
> 不做：结束会话 / PENDING_CLOSE / 评分（5.6）、封禁中断（5.7）、IM→OSS 存档（5.6/Epic 2）、推送本身（Epic 6，本 Story 用 IM 系统消息 + 留推送事件）。
>
> 依赖前序：5.2（在线态 / 待接单队列基础 + BUSY 枚举位）、5.3（`consult_sessions` WAITING + 队列 + `ConsultAcceptedEvent` 占位 + 「同时仅 1 个」）、5.4（AI 上下文卡数据，挂本 Story 对话顶部）。本 Story **首次引入腾讯 IM**——架构明确「后端经 IM REST/回调建会话、发系统消息，后端不持长连接；客户端腾讯 IM Flutter SDK 直连」。**含 L2**（真实腾讯 IM appid + UserSig + 真机）。

## Acceptance Criteria

> **验证层标注**：**L0**（状态迁移单元 / DTO / lint） · **L1**（需 Docker + pg + redis：接单迁移 WAITING→IN_PROGRESS、出队、BUSY 置位、辅助工具数据） · **L2**（需**真实腾讯 IM appid/SecretKey、UserSig 签发、真机**：建 IM 会话、收发文字/图片/视频、后台保连消息连续、IM 系统消息）。
> 本 Story 实时对话**主体在 L2**（真实 IM 不可在纯 Docker 验）；状态机迁移与队列在 L1；辅助工具数据 L1，渲染 L2。

### AC1 — 接单 → IM 建会话 + WAITING→IN_PROGRESS

**Given** 兽医在「待接单」点击「接受」
**When** 会话创建
**Then** 后端经腾讯 IM 建会话、状态迁移 WAITING→IN_PROGRESS、自动移入「进行中」，用户侧收到「兽医已接受你的问诊，点击开始对话」（FR-30 接单流程）
> 验证层：状态迁移 WAITING→IN_PROGRESS + 出待接单队列 + 兽医置 BUSY = **L1**；腾讯 IM 建会话（后端 REST 建会话/填 im_conversation_id）+ 用户侧「兽医已接受」IM 系统消息 = **L2**（真实 IM）。**并发抢单**：多兽医同时点「接受」→ 仅一人成功（DB 乐观锁/状态 CAS），其余得「已被接走」= **L1**。

### AC2 — 进行中图文/视频对话 + 后台保连消息连续

**Given** 进行中会话
**When** 双方对话
**Then** 支持文字/图片/视频（腾讯 IM 传输，视频 ≤60 秒）；用户切其他 Tab 时会话后台保持连接不中断、切回恢复消息连续（NFR-5）
> 验证层：**L2**（真实 IM 收发文字/图片/视频 ≤60s；切 Tab 后台保连、切回消息连续）。视频 ≤60s 客户端校验 = **L0/L2**；图片客户端压缩（≤10MB）= **L2**。聊天媒体由腾讯 IM 托管（架构媒体三层③），**不**经 OSS、**不**经后端中转。

### AC3 — 兽医 FR-5 辅助工具 + 用户侧免责提示

**Given** 兽医接单后
**When** 查看辅助工具
**Then** 展示 FR-5 辅助：按症状关键词自动匹配历史判断摘要（冷启动数据库为空时仅展示 AI 参考回复）、用户侧自动展示免责提示「本次建议为参考，最终决策权在您」（FR-5、NFR-9）
> 验证层：AI 参考回复 + 历史摘要匹配（冷启动空库 → 仅 AI 参考回复）数据 = **L1**（G-2：冷启动仅 AI 参考回复，历史匹配为后置增强）；兽医侧辅助面板渲染 + 用户侧免责提示展示 = **L2**。免责提示文案双语、克制（UX-DR14：免责声明克制）。

---

## Tasks / Subtasks

> 三段组织。后端：接单状态迁移 + IM 建会话编排 + UserSig 签发 + BUSY + 辅助工具数据 + IM 回调。前端：兽医待接单列表/接单 + 双端 IM SDK 对话界面 + 后台保连 + 辅助面板 + 免责提示。建议顺序：后端接单/IM 编排 → 前端对话界面 → 联调（含真实 IM L2）。

### 🟦 后端子任务（petgo-backend / Spring Boot）

- [ ] **B1. 接单状态迁移（含并发抢单）** (AC: 1)
  - [ ] `consult.service.ConsultSessionService.accept(vetId, sessionId)`：CAS 迁移 `WAITING→IN_PROGRESS`（DB 乐观锁/`UPDATE ... WHERE status='WAITING'`），成功者填 `vet_id`、出待接单队列、置兽医 BUSY（经 5.2 `VetPresenceService`，ONLINE→BUSY）；失败者 409「已被接走」。
  - [ ] 接单成功发 `ConsultAcceptedEvent`（5.3 定义）→ notify（Epic 6）推送用户「兽医已接受」；本 Story 同时经 IM 发系统消息保证即时（推送是离线补充）。
- [ ] **B2. 腾讯 IM 会话编排（后端不持长连接）** (AC: 1,2)
  - [ ] `shared/im/TencentImClient`（架构指定）：后端经 IM REST 建会话（或确保双方 IM 账号就绪 + 建 C2C/Group 会话），回 `im_conversation_id` 写入 `consult_sessions`。
  - [ ] **UserSig 签发**：后端用 IM SecretKey 为 user/vet 签发 UserSig（短时），客户端 SDK 用其登录 IM。端点 `GET /api/v1/im/usersig`（按 role 签自身 IM 账号 UserSig）。SecretKey env 注入、绝不下发客户端、绝不入日志。
  - [ ] IM 账号映射：user/vet 的 IM userId 与平台 id 的映射规则（如 `u_{userId}` / `v_{vetId}`），首次接入时确保 IM 账号已导入/自动注册。
  - [ ] `shared/im/ImCallbackController`：`/im/callback` 接收腾讯 IM 服务端回调（签名校验、内网/白名单），用于消息事件/存档触发（存档在 5.6）。本 Story 落回调骨架 + 签名校验，存档桥接 5.6 接。
- [ ] **B3. 进行中会话查询** (AC: 1,2)
  - [ ] `GET /api/v1/consult-sessions/{id}`：返回 session（status、im_conversation_id、对端信息、AI 上下文卡数据 via 5.4）；用户侧/兽医侧各取自身视角，role 门控。
  - [ ] 兽医「进行中」Tab 列表、用户侧「进行中会话卡」(5.8) 数据源。
- [ ] **B4. FR-5 辅助工具数据** (AC: 3)
  - [ ] AI 参考回复：兽医侧请求 → 基于会话上下文（AI 评级/描述 via 5.4 + 已有对话）生成参考回复（可调 `shared/ai/GeminiClient` 或既有分诊上下文，**仅供兽医参考、不自动发给用户**）。
  - [ ] 历史判断摘要：按症状关键词匹配历史问诊库（G-2：冷启动空库 → 返回空，前端仅展示 AI 参考回复）。本 Story 实现「查询接口 + 冷启动返回空」，历史匹配算法为后置增强。
  - [ ] 用户侧免责提示文案（双语，「本次建议为参考，最终决策权在您」）由前端 l10n 提供，后端不强制；若需服务端开关，留配置。

### 🟩 前端子任务（petgo_app / Flutter）

- [ ] **F1. 兽医待接单列表 + 接单** (AC: 1)
  - [ ] `features/vet/presentation/VetInboxPage`（5.2 壳）填内容：待接单请求列表（含 5.4 AI 上下文摘要 / DIRECT 无摘要）；「接受」按钮 → 调 accept；成功 → 进对话界面、自动移入「进行中」Tab；失败「已被接走」提示。
- [ ] **F2. 腾讯 IM SDK 对话界面（双端共用）** (AC: 2)
  - [ ] `features/consult/presentation`（用户侧）+ `features/vet/presentation`（兽医侧）对话界面：集成腾讯 IM Flutter SDK，用 UserSig 登录 IM、加载 `im_conversation_id` 会话、收发文字/图片/视频。
  - [ ] 图片客户端压缩（≤10MB）；视频 **≤60s** 客户端校验（超时拒绝/提示）；媒体经 IM 托管，不传 OSS。
  - [ ] **后台保连消息连续（NFR-5）**：切 Tab/退后台时 IM 连接保持，切回恢复消息历史连续不丢（依赖 IM SDK 后台能力 + 正确生命周期管理）。
  - [ ] 对话顶部挂 5.4 的 **AI 上下文卡**（AI_UPGRADE session 显示评级/描述/图片；DIRECT 不显示）。
- [ ] **F3. 兽医辅助面板 + 用户免责提示** (AC: 3)
  - [ ] 兽医侧辅助面板：AI 参考回复（点「采用」填入输入框供编辑后发送，**不自动发**）+ 历史摘要（冷启动空 → 仅显示 AI 参考回复区）。
  - [ ] 用户侧对话界面自动展示免责提示「本次建议为参考，最终决策权在您」（双语、克制、置顶或显著位）。

### 🟨 联调验收子任务（含真实 IM L2）

- [ ] **J1. 接单闭环 + 并发抢单** (AC: 1 / **L1+L2**)
  - [ ] 兽医接受 → WAITING→IN_PROGRESS、出队、BUSY、im_conversation_id 落库、用户收「兽医已接受」系统消息。两兽医抢同一单 → 仅一人成功。
- [ ] **J2. 实时图文视频 + 后台保连** (AC: 2 / **L2** 真实 IM + 真机)
  - [ ] 双端收发文字/图片/视频 ≤60s；用户切 Tab/退后台再回，消息连续不丢；媒体经 IM 托管。
- [ ] **J3. 辅助工具 + 免责** (AC: 3 / **L1+L2**)
  - [ ] 兽医侧 AI 参考回复可生成（冷启动历史摘要空）；用户侧免责提示常驻。
- [ ] **J4. IM UserSig / 回调安全** (AC: 1,2 / **L2**)
  - [ ] UserSig 短时有效、SecretKey 不下发不入日志；`/im/callback` 签名校验通过、非法回调拒绝。

---

## Dev Notes

### 关键架构约定（本 Story 必须落实）

- **后端编排 IM、不持长连接**：建会话/系统消息/封禁中断在 Java 侧经 IM REST/回调；**实时收发由客户端腾讯 IM Flutter SDK 直连**（架构 §API & Communication / Frontend / Integration Points）。后端只编排，不做消息中转。
- **聊天媒体留 IM（媒体三层③）**：聊天图/视频由腾讯 IM 托管，**不经 OSS、不经后端**（架构 §Data Architecture 媒体三层）。V1 不做 VOD/转码，视频 ≤60s 由 IM 托管。存档时（5.6）才复制必要图到私密桶。
- **会话状态机 WAITING→IN_PROGRESS 在本 Story**：接单是这一迁移的唯一触发；CAS 防并发抢单（架构状态机在 Java 侧）。BUSY 子态（5.2 留位）在接单时置位、结束/中断时回 ONLINE/OFFLINE。
- **FR-5 辅助冷启动（G-2）**：历史判断摘要库冷启动为空，V1 仅给 AI 参考回复，历史匹配为 consult 内查询后置增强。AI 参考回复仅供兽医参考、不自动发用户（NFR-9 专业辅助不替代判断）。
- **免责提示（NFR-9 / UX-DR14）**：用户侧常驻「本次建议为参考，最终决策权在您」，克制、双语。

### 会话状态机专项

> 本 Story 是状态机的**实时核心段**：`WAITING →(接单,本Story) IN_PROGRESS`。接单：CAS 迁移 + 填 vet_id + 出队 + IM 建会话 + 兽医 BUSY + 系统消息 + `ConsultAcceptedEvent`。进行中：双端 IM SDK 直连对话。**结束（IN_PROGRESS→PENDING_CLOSE）在 5.6**、**封禁中断（IN_PROGRESS→INTERRUPTED）在 5.7**、**存档（CLOSED→OSS 复制）在 5.6/Epic 2**——本 Story 均不做，但 `ImCallbackController` 骨架与 `im_conversation_id` 为它们铺好基础。

### 强制护栏（架构 §Enforcement —— 违反即返工）

- **后端不持 IM 长连接 / 不中转聊天媒体**；聊天媒体留 IM，不落 OSS、不落后端、不入日志。
- **禁 MQ / 通用缓存 / 新中间件**；状态迁移用 DB CAS；BUSY 用 5.2 Redis 在线态（不新增 Redis 用途）。
- **IM SecretKey / UserSig 签发**：SecretKey env 注入、绝不下发客户端、绝不入日志；UserSig 短时。
- **角色门控**：接单 `role=vet`；对话双方各自 role；`/im/callback` 签名校验 + 内网/白名单。
- **AI 参考回复不自动发**、免责前置——专业辅助不替代兽医判断（NFR-9）。

### 范围边界（防 scope creep —— 本 Story 明确不做）

- ❌ 不做结束会话 / PENDING_CLOSE / 30min 评分门 / 评分（Story 5.6）。
- ❌ 不做封禁 → INTERRUPTED 中断（Story 5.7，本 Story 仅 `ImCallbackController` 骨架）。
- ❌ 不做 IM→OSS 存档复制（5.6 / Epic 2 `ImToOssArchiver`）。
- ❌ 不做推送本身（Epic 6 / 6.2，本 Story 发 `ConsultAcceptedEvent` + IM 系统消息）。
- ❌ 不做历史摘要匹配算法（G-2 后置增强，本 Story 冷启动返回空）。
- ❌ 不做 VOD/视频转码（架构 V1 不做，视频 ≤60s 留 IM）。
- ✅ 只做：接单(CAS+IM 建会话+BUSY)+ WAITING→IN_PROGRESS + 双端 IM SDK 图文视频对话 + 后台保连消息连续 + UserSig 签发 + IM 回调骨架 + FR-5 辅助(AI 参考回复 + 冷启动空历史)+ 用户免责提示。

### Project Structure Notes

- 后端：`com.petgo.consult.service.ConsultSessionService`(accept/CAS)、`com.petgo.consult.web`(接单/会话查询/辅助工具端点)、`shared/im/{TencentImClient, ImCallbackController}`(建会话/系统消息/UserSig/回调骨架)、`shared/ai/GeminiClient`(AI 参考回复，复用)、`shared/media/SignedUrlService`(AI 上下文私密图，via 5.4)。`im_conversation_id` 落 `consult_sessions`。
- 前端：`lib/features/consult/presentation`(用户侧对话界面 + 免责 + AI 上下文卡)、`lib/features/vet/presentation`(待接单列表 + 接单 + 兽医侧对话 + 辅助面板)；腾讯 IM Flutter SDK 接入 `features/consult`/`features/vet` 共用聊天组件（可抽 `shared/widgets` 或 consult 内共享）。
- 用户侧咨询 `features/consult`、兽医工作台 `features/vet`——本 Story 两侧共用 IM 对话核心但各自界面壳。

### References

- [Source: architecture.md#API & Communication Patterns] — 后端经 IM REST/回调建会话/系统消息/封禁中断，后端不持长连接；状态机/超时/评分门在 Java；兽医在线态/队列在 Redis。
- [Source: architecture.md#Frontend Architecture] — 腾讯 IM Flutter SDK 直连；图片客户端压缩 ≤10MB STS 直传(此处聊天媒体留 IM)；portrait-only。
- [Source: architecture.md#Data Architecture 媒体三层] — ③腾讯 IM 托管聊天图/视频；V1 不做 VOD；存档时复制图到私密桶②(5.6)。
- [Source: architecture.md#Integration Points] — `shared/im/{TencentImClient, ImCallbackController}` + 离线推送；IM 账号/UserSig。
- [Source: architecture.md#Gap Analysis (G-2)] — FR-5 历史摘要冷启动空，V1 仅 AI 参考回复，历史匹配后置增强。
- [Source: architecture.md#Communication Patterns] — 会话状态机 WAITING→IN_PROGRESS；@Async + DB 状态机、禁 MQ。
- [Source: epics.md#Story 5.5] — 三条原始 AC（接单建会话迁移 / 图文视频 + 后台保连 NFR-5 / FR-5 辅助 + 免责 NFR-9）。
- [Source: epics.md#Story 5.3 / 5.4 / 5.6 / 5.7] — 队列衔接 / AI 上下文卡 / 结束评分 / 封禁中断。
- [Memory: spec-page-state-completeness] — 显式覆盖并发抢单(已被接走)、DIRECT 无 AI 卡、冷启动历史空、后台保连等多态。

## Dev Agent Record

### Agent Model Used

云端 dev agent（Epic 5 批量，凭证攸关 story：IM 用 stub 跑绿 L0，真实 IM 待本地）

### Debug Log References

### Completion Notes List

**⚠️ 凭证攸关**：本 Story 含腾讯 IM SDK/SecretKey。按批跑铁律——实现完整编排代码 + **stub IM 客户端**把 L0 跑绿，**禁止伪造凭证**；真实 IM（建会话/收发/UserSig/回调/后台保连）属 **L2 待本地**。

**L0 绿（云端已验）**：后端 `package` 通过 + 新增 `ConsultAcceptServiceTest`(3) + `StubTencentImClientTest`(4) + `VetPresenceServiceTest`(9，含 BUSY) 绿；前端 `flutter analyze` 零问题 + `flutter test`(164) 绿（新增 `test/vet/vet_inbox_test.dart`）。

**关键决策（按 Completion Notes 要求记录）**：
- **并发抢单 CAS**：JPA `@Version` 乐观锁（consult_sessions 加 `version` 列 V16）。`accept` 先查 WAITING，`markInProgress` + `saveAndFlush` 触发版本 CAS，败者 `ObjectOptimisticLockingFailureException` → 409「已被接走」。胜者再建 IM 会话 + 绑 `im_conversation_id` + 出队 + 置 BUSY + 发 `ConsultAcceptedEvent` + IM 系统消息。
- **IM userId 映射规则**：用户 `u_<userId>`、兽医 `v_<vetId>`（`ImAccountMapper`）。
- **UserSig 签发与 TTL**：`GET /api/v1/im/usersig` 按 JWT role 为自身 IM 账号签 UserSig；TTL `IM_USERSIG_TTL_SECONDS` 默认 86400s。SecretKey **仅服务端持有、绝不下发客户端、绝不入日志**；`UserSig` record 不含 SecretKey。
- **C2C vs Group 选型**：接口 `createConversation(userImId, vetImId)` 抽象，stub 返回本地会话 id；真实选型（建议 C2C 单聊，一对一咨询）在 live 实现时落定（L2）。
- **IM 模式开关**：`petgo.im.mode=stub`（默认，免凭证验状态机/接单 CAS/BUSY/查询）/ `live`（真实 IM，L2）。**live 未实现 → `ImConfig` 启动即抛错**，避免用桩冒充真实 IM 或伪造凭证。
- **BUSY 子态**：`VetPresenceService` 加 `vet:busy` SET，`goBusy`/`goAvailable`/`statusOf`（BUSY 优先于 ONLINE）；接单 goBusy，结束/中断（5.6/5.7）goAvailable，goOffline 清 busy。
- **FR-5 辅助冷启动（G-2）**：`assist` 返回基于会话 AI 上下文的**模板化** AI 参考回复（确定性，免凭证可验）+ **空历史摘要**。真实 Gemini 生成更自然参考回复属 L2 增强。AI 参考回复**不自动发**（前端「采用」填输入框，NFR-9）。
- **聊天媒体留 IM**：后端不持长连接、不中转聊天媒体、不落 OSS/日志；实时收发由客户端腾讯 IM Flutter SDK 直连（占位 `ImChatPlaceholder`，真机接入）。
- **IM 回调骨架**：`POST /im/callback`（permitAll + `verifyCallback` token 校验），存档(5.6)/中断(5.7) 事件分发在此接。

**待本地验收（L2，需真机 + 真实 SDKAppID/SecretKey）**：
- 真实腾讯 IM 建会话、UserSig 登录、收发文字/图片(≤10MB 压缩)/视频(≤60s)、切 Tab 后台保连消息连续（NFR-5）——需实现 `LiveTencentImClient`（TLSSigAPIv2 UserSig + REST 建会话）+ 集成腾讯 IM Flutter SDK 替换 `ImChatPlaceholder`。
- IM 系统消息「兽医已接受你的问诊」用户侧可见；`/im/callback` 真实签名校验。
**待本地验收（L1，Docker+pg+redis）**：J1 接单迁移 WAITING→IN_PROGRESS + 出队 + BUSY + im_conversation_id 落库；并发两兽医抢单仅一人成功；待接单列表/辅助数据。

### File List

**后端（新增）**：`db/migration/V16__add_consult_session_version.sql`、`shared/im/{ImProperties,ImAccountMapper,UserSig,TencentImClient,StubTencentImClient,ImConfig}.java`、`shared/im/web/{ImUserSigController,ImCallbackController}.java`、`consult/service/{ConsultAcceptService,VetConsultService}.java`、`consult/dto/{VetInboxItem,VetSessionView,ConsultAssistResponse}.java`、`consult/web/VetConsultController.java`、`test/.../consult/service/ConsultAcceptServiceTest.java`、`test/.../shared/im/StubTencentImClientTest.java`
**后端（修改）**：`consult/domain/ConsultSession.java`（@Version + markInProgress/attachImConversation）、`vet/service/VetPresenceService.java`（BUSY 集合）、`shared/security/SecurityConfig.java`（/im/callback permitAll）、`application.yml`、`.env.example`、`test/.../vet/service/VetPresenceServiceTest.java`
**前端（新增）**：`features/vet/domain/vet_inbox_item.dart`、`features/vet/presentation/vet_conversation_page.dart`、`features/consult/presentation/{im_chat_placeholder,consult_conversation_page}.dart`、`test/vet/vet_inbox_test.dart`
**前端（修改）**：`features/vet/presentation/vet_inbox_page.dart`（空壳→列表+接单）、`features/vet/data/vet_repository.dart`（+waiting/accept/session/assist）、`features/consult/presentation/consult_waiting_page.dart`（IN_PROGRESS→对话）、`core/network/api_paths.dart`、`core/router/app_router.dart`、`l10n/app_en.arb`、`l10n/app_id.arb`
