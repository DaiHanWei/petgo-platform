---
baseline_commit: 2bc770495356fcedf949135653dd6974ac99f824
---

# Story 1.3: 操作审计日志（哈希链 append-only）与查询

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

> **范围域**：管理后台 V1.0.0 · Epic 1（后台地基）· Story 1.3。**建立在已完成的 Story 1.1/1.2 之上**（admin_accounts + 服务端会话 + 紧急账密 + Lark OAuth + adminFilterChain）。
> **产物归属**：仅后端 `petgo-backend`（**无 Flutter 侧**）。brownfield 扩 `com.tailtopia.admin`。
> **横切定位**：本故事交付的 `AdminAuditService.record(...)` 是 **Epic 2~6 所有后台写操作都要调用的横切件**（架构 §Communication Patterns「审计写入是强制副作用」），务必做成统一、易复用、难绕过的单一入口。

## Story

As a **后台账号**,
I want **所有后台写操作被不可篡改地记录，并能按维度筛查审计日志**,
so that **每个运营决策可追溯、且任何篡改可被检出（AM-C1 操作日志完整性零漏记录）**。

## Acceptance Criteria

> 验证层：**L0** 编译/单测/MockMvc 切片（mock repo，无需 DB）· **L1** 集成（Docker postgres+redis，验迁移/哈希链/append-only/门控真跑）· **L2** 外部凭证（本故事无 L2）。

1. **AC1（建表，L0 编译 + L1 迁移）**：新增 Flyway `V32__init_admin_audit_logs.sql` 建 `admin_audit_logs`（`id` / `actor_account_id` / `action_type`(varchar UPPER_SNAKE) / `target_type`(varchar) / `target_id`(varchar，外露用不可枚举值或业务 id 字符串) / `summary` / `created_at`(timestamptz UTC) / `prev_hash` / `row_hash`）；JPA 实体 `@Column` 与列精确匹配（`ddl-auto=validate` 启动不报错）。

2. **AC2（统一审计入口 + 哈希链，L0 + L1）**：提供统一 `AdminAuditService.record(actorAccountId, actionType, targetType, targetId, summary)`；每次写入：取链尾 `row_hash` 作为本行 `prev_hash`（首行 `prev_hash` 为约定创世值），`row_hash = SHA-256(canonical(prev_hash, actorAccountId, actionType, targetType, targetId, summary, createdAt))`；同一事务内随业务写操作落库。**各控制器/服务禁止自拼 SQL 或绕过此入口**（架构 Enforcement）。

3. **AC3（并发串行化，L1，AG-2）**：审计链写入串行化——取链尾时对链尾加锁（悲观行锁 / `SELECT ... FOR UPDATE` 链尾、或数据库 advisory lock 单写入路径），保证并发写入下 `prev_hash→row_hash` 链不断裂、无竞态空洞（后台写并发极低，正确性优先）。

4. **AC4（append-only 不可篡改，L1，A6）**：任何对 `admin_audit_logs` 的 UPDATE/DELETE 被拒——以 **DB 触发器**在 `BEFORE UPDATE OR DELETE` 抛异常阻断（迁移内建），且审计仓库接口**不暴露** `delete*`/改写方法（窄接口，仅 append + 查询）；链可被独立校验器逐行重算 `row_hash` 验证未被篡改。

5. **AC5（查询页 + 权限门控，L0 门控 + L1）**：`/admin/audit-logs` 审计查询页**仅 `SUPER_ADMIN` 或持 `admin.view_logs` 权限的账号可见**；无此权限的后台账号访问 → 403。〔需求修正 2026-06-29：AB-1B 由「全员可查」改为权限受限。STAFF 的 `permission_code` authority 由 Story 1.5 装载，本故事先以 `hasRole('SUPER_ADMIN') or hasAuthority('admin.view_logs')` 表达式门控——1.5 落地前实际仅超管可见，前向兼容、无前向依赖。〕

6. **AC6（筛选 + 永久保留，L1）**：查询页支持按**日期范围 / 操作人（actor account）/ 操作类型（action_type）**筛选；筛选提交走 HTMX 局部刷新结果区（`HX-Request` 返片段，整页请求返完整视图）；结果按 `created_at` 倒序分页；**永久保留不设年限**（无清理任务）。

7. **AC7（接入 1.1 紧急登录 hook，L1）**：Story 1.1 预留的「紧急账密成功登录 → 写审计 + 告警全体超管」在本故事接入——紧急账密（formLogin）登录成功时写一条 `EMERGENCY_LOGIN_SUCCEEDED` 审计，并向全体 `SUPER_ADMIN` 发后台告警（复用既有 notify/后台预警机制，不引入新中间件）；**审计/告警内容绝不含密码/令牌/签名 URL**（架构「一次性敏感值」护栏）。〔Lark 登录成功不属紧急入口，不在本 AC；会话登录/登出/过期审计归 Story 1.4。〕

8. **AC8（回归绿，L0/L1）**：`mvn -B compile` + L0 全绿；本地 L1 全量回归绿；既有端点（含 1.1/1.2 登录路径、种子发帖、举报处理）不回归。

## Tasks / Subtasks

- [x] **T1 数据模型与迁移（AC1/AC4）**
  - [x] 新建 `admin/audit/domain/AdminAuditLog.java`（`@Entity @Table(name="admin_audit_logs")`）：`id(bigint pk)` / `actorAccountId(Long)` / `actionType(String, varchar UPPER_SNAKE)` / `targetType(String)` / `targetId(String, nullable)` / `summary(String)` / `createdAt(Instant, timestamptz UTC)` / `prevHash(String)` / `rowHash(String)`。无 setter / 无 `@PreUpdate`（构造即定值，append-only）
  - [x] `V32__init_admin_audit_logs.sql`（Flyway，**V32**，决策 E2 顺延）：建表 + 索引（`created_at` DESC、`actor_account_id`、`action_type`）+ **BEFORE UPDATE OR DELETE 触发器**抛异常阻断（append-only DB 强制）+ `row_hash` 唯一约束
  - [x] `admin/audit/repository/AdminAuditLogRepository`：**窄接口**（继承基础 `Repository` 非 `JpaRepository`，仅 `save` + `findTopByOrderByIdDesc` 链尾 + `findAllByOrderByIdAsc` 校验 + 自定义 fragment 筛选分页；不暴露任何 `deleteX`/改写）。筛选用 `AdminAuditLogRepositoryCustom`/`Impl`（Criteria 动态拼条件——规避无类型 null 参数报错，且避开会带 `delete(Specification)` 的 `JpaSpecificationExecutor`）
- [x] **T2 审计服务 + 哈希链（AC2/AC3）**
  - [x] `admin/audit/service/AdminAuditService.record(Long actorAccountId, String actionType, String targetType, String targetId, String summary)`：`@Transactional`（随调用方事务），取链尾 `row_hash`（advisory 锁串行化，AG-2）→ 计算 `prev_hash`/`row_hash`（SHA-256，规范化拼接，字段间用 U+001F 分隔、null 用 U+0000 哨兵）→ append。`createdAt` truncatedTo(MICROS) 与列精度对齐，保证回读可复算
  - [x] 创世值约定（首行 `prev_hash` = 64 个 '0'）写入常量 `AuditHashing.GENESIS_HASH` 并文档化
  - [x] `admin/audit/service/AuditChainVerifier`：逐行重算 + 校验链接，定位首个断裂行（供测试 + 运维 `verifyAll()`）
  - [x] `action_type` 常量集中定义（`AuditActions`，UPPER_SNAKE 过去式）：`EMERGENCY_LOGIN_SUCCEEDED` 本故事用；`VET_BANNED`/`VET_UNBANNED`/`CONTENT_TAKEN_DOWN`/`USER_DEACTIVATED`/`ACCOUNT_CREATED`/`PERMISSION_GRANTED` 为 Epic 2~6 预留（仅定义常量）
- [x] **T3 查询页 + 门控 + 筛选（AC5/AC6）**
  - [x] `admin/audit/web/AuditLogAdminController`：`GET /admin/audit-logs`（整页 + HTMX 片段，依 `HX-Request` 头区分）；`@PreAuthorize("hasRole('SUPER_ADMIN') or hasAuthority('admin.view_logs')")`
  - [x] `templates/admin/audit-logs.html`：筛选条（日期范围 / 操作人账号 id 输入 / 操作类型下拉）+ 结果表 + 分页；`th:fragment="resultsFragment"` 供 HTMX 局部刷新（hx-get → #results）
  - [x] `/admin/audit-logs` 落在 admin 链 `anyRequest().hasRole("ADMIN")` 之内（方法级 `@PreAuthorize` 再细化到 view_logs）；`layout.html` 加审计导航入口（`sec:authorize` 仅对有权者显示）
- [x] **T4 接入 1.1 紧急登录 hook（AC7）**
  - [x] 在 `SecurityConfig.adminFilterChain` 的 **formLogin successHandler**（紧急账密路径）接入：从 `AdminUserDetails` 取 accountId/email → `AdminAuditService.record(accountId, "EMERGENCY_LOGIN_SUCCEEDED", "ADMIN_ACCOUNT", accountId, summary)` + 向全体在职 SUPER_ADMIN 发后台告警；依赖经 bean 方法参数注入（避免循环依赖）
  - [x] 告警 = `AdminAlertService` 经 logback JSON 结构化 WARN 安全日志（既有运维告警管道，不新增中间件）；告警只记 account id/数量，审计 summary 含邮箱语义但**绝不含**密码/令牌
- [x] **T5 方法级安全开启（AC5）**
  - [x] `SecurityConfig` 加 `@EnableMethodSecurity`（确认对无 `@PreAuthorize` 的 api/admin 链无影响）；最小单测 `AuditLogAccessControlTest` 验证 `@PreAuthorize` 生效（无权限 → AccessDenied）
- [x] **T6 测试 + 回归（AC2~AC8）**
  - [x] L0：`AuditHashingTest`（创世/确定性/不可碰撞分隔/null 哨兵）、`AdminAuditServiceTest`（首行创世值、链接续、锁、micros 截断）、`AuditChainVerifierTest`（完整/改内容/改 rowHash 检出）、`AuditLogAdminControllerTest`（HTMX 片段 vs 整页、日期→UTC 半开区间、空筛选→null）、`AuditLogAccessControlTest`（@PreAuthorize 403/放行）、`AdminAlertServiceTest`
  - [x] L1：`AdminAuditIntegrationTest`——迁移 validate 绿；append-only 触发器真拒 UPDATE/DELETE；并发 12 写链不断裂 + prevHash 全局唯一；紧急登录成功写审计且 summary 不含密码；全量回归 654 通过
  - [x] 本地 Docker PG 已真跑 L1 全绿（云端 headless 仅需 L0）

## Dev Notes

### 架构约束（必须遵守）
- 后端 = Spring Boot 4 / Java 21 / Maven；包 `com.tailtopia.admin.audit.{web,service,domain,repository,dto}`。[Source: admin-backend/architecture.md#Project Structure → admin/audit]
- 后台 = SSR MVC + HTMX，路由 `/admin/**`，**返回视图/HTML 片段不返 JSON**；CSRF 开启；错误用友好页/内联条非 ProblemDetail。[Source: architecture.md#API & Communication Patterns / Format Patterns]
- **审计写入是强制副作用**：每个后台写操作必须在同一事务内经 `AdminAuditService.record(...)` 写一条审计（哈希链 `prev_hash→row_hash`）；`action_type` UPPER_SNAKE **过去式**；统一入口，**禁各控制器自拼**。[Source: architecture.md#Communication Patterns / Enforcement]
- **A6 不可篡改**：append-only + 应用无 UPDATE/DELETE 权（窄仓库接口）+ DB 触发器 + 行间哈希链（篡改可检）。[Source: architecture.md#Authentication & Security A6 / Data Architecture]
- 时间 `timestamptz` UTC；枚举/动作 varchar UPPER_SNAKE；新表 snake_case 复数 `admin_` 前缀。[Source: architecture.md#Naming Patterns]
- 日志/审计 **绝不含**一次性密码、令牌、签名 URL、健康/PII。[Source: architecture.md#Enforcement / Format Patterns「一次性敏感值」]

### 既有代码基线（READ，1.1/1.2 产出）
- `SecurityConfig.adminFilterChain`（shared/security）：session + form + CSRF + `hasRole("ADMIN")` + `AdminLoginThrottleFilter` + `AdminSessionGuardFilter` + 失败/成功处理器。**本故事在 formLogin successHandler 接入紧急登录审计（AC7）**；⚠️ 该文件含仓库既有「法律页」未提交 WIP，提交时**仅纳入 admin 审计增量**。[Source: SecurityConfig.java:57-99]
- `AdminUserDetails`（admin/service）：principal，携 `adminAccountId`/`operatorUserId`/`accountType`，authorities=`ROLE_ADMIN`(+`ROLE_SUPER_ADMIN`)。**细粒度 `permission_code` authority 尚未装载（Story 1.5 做）**——故本故事审计页门控用 `hasRole('SUPER_ADMIN') or hasAuthority('admin.view_logs')`，1.5 前实际仅超管命中。[Source: AdminUserDetails.java]
- `AdminAccountRepository`（admin/account）：`findByLarkEmail` / `findById` / `countByAccountType`。需「全体 SUPER_ADMIN」列表用于 AC7 告警 → 可加 `findByAccountType(SUPER_ADMIN)` 查询方法。[Source: AdminAccountRepository.java]
- 当前 Flyway 最大 = `V31`（admin_account_permissions）；本故事新表从 **V32** 起。[来源：`db/migration/` 实测]
- 既有后台告警/notify：复用 `notify` 模块或既有后台预警路径（READ `com.tailtopia.notify` 现状后择最小侵入方式）；**不引入 MQ/缓存层**。

### 关键边界 / 防坑
- **窄仓库接口防误删**：`JpaRepository` 默认暴露 `delete*`——审计仓库应改继承 `org.springframework.data.repository.Repository<AdminAuditLog, Long>` 并只声明 `save` + 查询，或显式不调用任何删除；DB 触发器是最后防线（即便代码误删也被 DB 拒）。
- **哈希链串行化（AG-2）**：取链尾必须加锁，否则并发两写可能读到同一 `prev_hash` → 分叉。后台写并发极低，用悲观锁/advisory lock 即可，勿引入队列中间件（F5）。
- **同事务**：`record(...)` 须随业务写操作在**同一事务**提交——业务回滚则审计回滚（避免悬挂审计）；本故事自身的紧急登录审计无业务事务，独立提交即可。
- **successHandler 取 bean**：Lambda successHandler 内需 `AdminAuditService`/`AdminAccountRepository`/告警服务——经方法参数注入或 `http.getSharedObject(ApplicationContext.class)` 取，避免循环依赖。
- **summary 脱敏**：紧急登录 summary 只记「邮箱 + 时间 + 来源」语义，**绝不**记密码/失败明文；其他动作的 summary 同样禁敏感值。
- **HTMX 片段 vs 整页**：依 `HX-Request` 头区分；筛选/分页返 `audit-logs :: rows`，直接访问返完整视图（`layout :: page`）。

### 范围边界（不做 / 避免前向依赖）
- **本故事不做**：会话登录/登出/过期审计落库（→ Story 1.4 `admin_session_logs`）；STAFF `permission_code` authority 装载与账号管理 UI、超管≤5、SUPER_ADMIN 隐式全权 PermissionEvaluator（→ Story 1.5——本故事门控表达式已前向兼容）；Epic 2~6 各业务写操作对 `AdminAuditService` 的实际调用（仅**定义** `record` 入口 + 预留 action_type 常量，调用在各业务故事）；双语外化（→ 1.6，新页文案先中文）。
- 本故事**只**交付：审计表/实体（V32）+ 哈希链 append-only 服务 + 查询页（权限门控 + 筛选 HTMX）+ 接入 1.1 紧急登录 hook。

### Flyway
- 本故事新增 **`V32__init_admin_audit_logs.sql`**（含表 + 索引 + append-only 触发器）。序号严格 V32（决策 E2 顺延，勿照搬 architecture §Data Architecture 的指示性示例号）。

### 测试标准
- L0：哈希链正确性（创世首行、续接、篡改检出）、`record` 入口、控制器权限门控 403 / 渲染 / HTMX 筛选片段（mock repo）。沿用 `AdminWebControllerTest` MockMvc/Mockito 范式。
- L1（本地，需 Docker PG）：迁移 validate 绿、触发器真拒 UPDATE/DELETE、并发写链不断裂、紧急登录审计+告警、全量回归绿。**云端 headless 只跑 L0；L1 标注待本地验收**（CLAUDE.md 云端约定）。

### Project Structure Notes
- 新增包 `com.tailtopia.admin.audit.{domain,repository,service,web}`；模板 `templates/admin/audit-logs.html`（+ 复用 `fragments/`，若 1.6 尚未抽则本故事先内置最小片段）。
- 与架构目录树一致。[Source: architecture.md#Project Structure → admin/audit/{AdminAuditLog, AdminAuditService}]

### References
- [Source: admin-backend/epics.md#Story 1.3 操作审计日志（哈希链 append-only）与查询]
- [Source: admin-backend/architecture.md#Data Architecture（admin_audit_logs 字段 + 哈希链）]
- [Source: admin-backend/architecture.md#Authentication & Security A6（审计不可篡改）/ AG-2（并发写串行化）]
- [Source: admin-backend/architecture.md#Implementation Patterns（审计强制副作用 / action_type UPPER_SNAKE 过去式 / 统一 AdminAuditService）]
- [Source: admin-backend/PRD.md#AB-1B 操作审计日志（2026-06-29 修订：权限受限 admin.view_logs，永久保留）]
- [Source: 1-1-后台账号模型与认证重构.md（AC4 紧急登录「审计+告警超管」留 1.3；T4 hook 占位）]
- [Source: SecurityConfig.java:57-99（adminFilterChain successHandler 接入点）]
- [Memory: [[hibernate6-char1-and-cloud-l0-gap]]（列长度别用 VARCHAR(1) / 云端 L0 盲区）]
- [Memory: [[prd-r2-rework-delta-and-frozen-flyway]]（冻结迁移、新表顺延 E2）]

## Dev Agent Record

### Agent Model Used

claude-opus-4-8[1m]（Claude Opus 4.8, 1M context）

### Debug Log References

- **Postgres 无类型 null 参数报错（real bug，非仅测试问题）**：初版用 `@Query("... WHERE (:from IS NULL OR a.createdAt >= :from) ...")`，空筛选时 Postgres 报 `ERROR: could not determine data type of parameter $1`（参数仅出现在 `IS NULL` 上下文无法推断类型）。生产空筛选会 500。改为 `AdminAuditLogRepositoryCustom` + `Impl`（Criteria 动态拼条件，仅为非 null 项加谓词），保持窄接口（不引入会带 `delete(Specification)` 的 `JpaSpecificationExecutor`）。
- L1 紧急登录用例初版用 `redirectedUrlPattern("**/admin/dashboard")` 不匹配精确 `/admin/dashboard`，改 `redirectedUrl`。

### Completion Notes List

- **AC3 串行化选型**：用 Postgres 事务级 `pg_advisory_xact_lock`（`AuditChainLock`）覆盖整个「取链尾→算哈希→append」临界区，而非 story 备选的 `SELECT ... ORDER BY id DESC LIMIT 1 FOR UPDATE`——后者在 READ COMMITTED 下 `LIMIT + FOR UPDATE` 可能返回过期链尾导致两行取到同一 `prevHash`（链分叉）。advisory 锁随事务提交自动释放、不引入中间件（F5）。L1 并发 12 写实测链不断裂、prevHash 全局唯一。
- **哈希可复算关键**：`createdAt` 入库前 `truncatedTo(MICROS)`（Postgres timestamptz 仅微秒精度），否则回读重算哈希对不上。`AuditChainVerifier.verifyAll()` L1 实测对真实落库链复算通过。
- **AC7 告警落地决策**：未走 App `notify` 模块——`notifications` 表带 `type IN (...)` CHECK 约束且面向 App 用户端，塞后台安全告警既需加枚举+迁移、语义也不符。改为 `AdminAlertService` 经 logback JSON 结构化 WARN 安全日志（CLAUDE.md 既定的运维告警管道），显式查询「全体在职 SUPER_ADMIN」作为受众并记数量，**只记 account id/数量不外泄邮箱/密码/PII**；留作未来接真实告警通道的扩展点。审计行 summary 含 actor 邮箱语义（安全审计的核心信息，非禁止的 PII 类目），但绝不含密码（L1 已断言）。
- **窄接口三道防线**：① 仓库继承基础 `Repository` 不暴露 delete ② 行间哈希链篡改可检 ③ V32 DB 触发器 `BEFORE UPDATE OR DELETE` 硬拒（L1 实测 JdbcTemplate 直连 UPDATE/DELETE 均抛 `DataAccessException`，行未被改）。
- **范围边界**：仅交付审计地基 + 紧急登录 hook；Epic 2~6 各业务写对 `record(...)` 的实际调用、STAFF `admin.view_logs` authority 装载（1.5）、会话审计（1.4）、双语外化（1.6）均不在本故事——门控表达式 `hasRole('SUPER_ADMIN') or hasAuthority('admin.view_logs')` 已前向兼容（1.5 前实际仅超管命中）。
- **验证**：L0 19 测试全绿（云端 headless 可跑）；L1 `AdminAuditIntegrationTest` 4 用例全绿（本机 Docker PG）；全量回归 **654 tests, 0 failures, 0 errors, 6 skipped, BUILD SUCCESS**。本故事纯后端、无 L2。
- **依赖说明**：审计查询页用 HTMX（CDN `unpkg.com/htmx.org@1.9.12`）做局部刷新；未加载时表单退化为整页 GET 提交（仍可用）。未新增任何 Maven 依赖 / 中间件。

### File List

**新增（main）**
- petgo-backend/src/main/java/com/tailtopia/admin/audit/domain/AdminAuditLog.java
- petgo-backend/src/main/java/com/tailtopia/admin/audit/domain/AuditHashing.java
- petgo-backend/src/main/java/com/tailtopia/admin/audit/repository/AdminAuditLogRepository.java
- petgo-backend/src/main/java/com/tailtopia/admin/audit/repository/AdminAuditLogRepositoryCustom.java
- petgo-backend/src/main/java/com/tailtopia/admin/audit/repository/AdminAuditLogRepositoryImpl.java
- petgo-backend/src/main/java/com/tailtopia/admin/audit/service/AdminAuditService.java
- petgo-backend/src/main/java/com/tailtopia/admin/audit/service/AuditChainLock.java
- petgo-backend/src/main/java/com/tailtopia/admin/audit/service/AuditChainVerifier.java
- petgo-backend/src/main/java/com/tailtopia/admin/audit/service/AuditActions.java
- petgo-backend/src/main/java/com/tailtopia/admin/audit/service/AdminAlertService.java
- petgo-backend/src/main/java/com/tailtopia/admin/audit/web/AuditLogAdminController.java
- petgo-backend/src/main/resources/db/migration/V32__init_admin_audit_logs.sql
- petgo-backend/src/main/resources/templates/admin/audit-logs.html

**修改（main）**
- petgo-backend/src/main/java/com/tailtopia/shared/security/SecurityConfig.java（`@EnableMethodSecurity` + formLogin successHandler 紧急登录审计/告警 hook + bean 注入）
- petgo-backend/src/main/java/com/tailtopia/admin/account/repository/AdminAccountRepository.java（`findByAccountTypeAndStatus`）
- petgo-backend/src/main/resources/templates/admin/layout.html（审计导航入口 + sec namespace）

**新增（test）**
- petgo-backend/src/test/java/com/tailtopia/admin/audit/domain/AuditHashingTest.java
- petgo-backend/src/test/java/com/tailtopia/admin/audit/service/AdminAuditServiceTest.java
- petgo-backend/src/test/java/com/tailtopia/admin/audit/service/AuditChainVerifierTest.java
- petgo-backend/src/test/java/com/tailtopia/admin/audit/service/AdminAlertServiceTest.java
- petgo-backend/src/test/java/com/tailtopia/admin/audit/web/AuditLogAdminControllerTest.java
- petgo-backend/src/test/java/com/tailtopia/admin/audit/web/AuditLogAccessControlTest.java
- petgo-backend/src/test/java/com/tailtopia/admin/audit/AdminAuditIntegrationTest.java

### Change Log

- 2026-06-29：实现 Story 1.3 操作审计日志（哈希链 append-only + 查询）。新增 `admin.audit` 包（实体/哈希算法/窄仓库+Criteria 筛选/统一审计服务+advisory 串行化/链校验器/动作常量/安全告警/查询控制器）、V32 迁移（表+索引+append-only 触发器）、审计查询页（HTMX 筛选+分页+权限门控）；开启方法级安全；接入 1.1 紧急账密登录成功审计+超管告警 hook。L0 19 + L1 4 测试全绿，全量回归 654 通过。
