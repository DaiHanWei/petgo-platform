# Story 6.2: 单个兽医评分详情

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

> **范围域**：管理后台 V1.0.0 · Epic 6（兽医评分质量看板）· Story 6.2（AB-6B）。**纯只读详情**，与 6.1 互不依赖（无前向依赖）。
> **产物归属**：仅后端 `petgo-backend`（**无 Flutter 侧**）。brownfield **扩/规整既有页**。
> **brownfield 现状**：既有 `GET /admin/vets/{id}/ratings` → `admin/vet-ratings.html`（Story 5.6）**已上线**，展示该兽医「均分 + 已评历史（星级/评价/时间）」。**本故事的增量 = 补「未评问诊单列 + 标注原因」**（既有页只有已评列表，没有未评区）。
> **隐私边界**：V1.0.0 评分**不对 App 用户/兽医公开**，仅运营可见（沿用 `ConsultRatingQueryService`「仅运营可见」语义）。

## Story

As a **运营成员**,
I want **打开某个兽医的评分详情页，既看到全部已评问诊（日期/1-5 星/文字评价），也看到未评问诊并标明原因**,
so that **我能深入分析该兽医的服务表现，并区分「真没评」与「评分门超时」等情况（FR-33 / AB-6B）**。

## Acceptance Criteria

> 验证层：**L0** 编译/单测/MockMvc 切片（mock service，无需 DB）· **L1** 集成（Docker postgres+redis，验已评/未评分区与原因标注真跑）· **L2**（本故事无）。

1. **AC1（已评历史完整展示，L0/L1）**：详情页展示该兽医**已评问诊完整历史**：日期、1-5 星、文字评价（无评价显示「—」），按时间倒序。沿用既有 `vet-ratings.html` 的已评列表与均分摘要，**不回退**。
   **Given** 某兽医有若干评分
   **When** 打开 `/admin/vets/{id}/ratings`
   **Then** 列出全部已评条目（日期/星级/评价），均分摘要与底层一致
2. **AC2（未评问诊单列 + 原因，L0/L1）**：**新增**「未评问诊」区，单独列出该兽医未获评分的问诊，每条**标注原因**；原因取自会话终态，映射见 Dev Notes「未评原因映射」。
   **Given** 某兽医有未评问诊（CLOSED 无评分 / 中断等）
   **When** 打开详情页
   **Then** 未评区逐条列出（含会话标识/日期 + 原因标签），与已评区分离清晰
3. **AC3（原因如实映射，不杜撰，L0/L1）**：未评原因按**实际数据模型**标注，**不得**编造数据中不存在的类别。当前模型唯一的「无评分自动关闭」终态是 `closed_reason=UNRATED`（30min 评分门超时）；中断为 `INTERRUPTED`。映射与「用户未评/30 分钟超时」的对应关系见 Dev Notes，差异需在 Completion Notes 记录。
   **Given** UNRATED 关闭且无补评分的会话 / INTERRUPTED 会话
   **When** 渲染未评区
   **Then** 前者标「30 分钟超时自动关闭（用户未在评分窗口内评分）」，后者标「会话中断未评（兽医封禁中断）」或按 Dev Notes 约定
4. **AC4（纯只读，L0）**：详情页**无任何运营操作功能**（无编辑/删除/隐藏/回复/导出按钮），仅查看（PRD AB-6B「纯只读」）。
   **Given** 详情页
   **When** 渲染
   **Then** 页面不含任何对评分/会话产生写副作用的控件
5. **AC5（隐私与门控，L0/L1）**：端点经 `@PreAuthorize("hasAuthority('rating.view')")` 门控（横切依赖 Story 1.5）；评分/未评数据**绝不**出现在任何 App 用户/兽医可达接口。
   **Given** 无 `rating.view` 权限的后台账号
   **When** 访问该详情页
   **Then** 方法级拒绝 403（前端隐藏非安全边界）
6. **AC6（跨模块边界，L0）**：补充的未评数据**只经既有/扩展的 consult `service` 接口**取得，`com.tailtopia.admin`（含 rating/既有 AdminVetService）**绝不**直接注入 `consult.repository` / `vet.repository`（禁跨 repo）。
7. **AC7（回归绿，L0/L1）**：既有 5.6 单兽医评分页行为不回归（已评列表 + 均分仍正常）；`mvn -B compile` + L0 全绿；本地 L1 全量回归绿。

## Tasks / Subtasks

- [x] **T1 consult 侧只读未评读口（AC2/AC3/AC6）**
  - [x] 复用 6.1 的 `ConsultQualityQueryService` 加 `unratedConsults(vetId)`：取该兽医终态会话（`findByVetIdAndStatusInOrderByCreatedAtDesc(vetId,[CLOSED,INTERRUPTED])`），剔除 `existsBySessionId` 已评者，其余按 status 推导原因。仅 consult.service 内访问 consult repo；不新建表/不写库
  - [x] 新增 DTO `VetUnratedConsult(sessionId, terminalAt, reason)` + 枚举 `UnratedReason(TIMEOUT_UNRATED, INTERRUPTED)`
- [x] **T2 admin 侧装配进详情视图（AC1/AC2/AC4/AC5）**
  - [x] `AdminVetService` 旁新增 `unratedConsults(vetId)`（注入 `ConsultQualityQueryService` 委托）；保持 admin→consult.service 边界
  - [x] `AdminWebController#vetRatings`：模型追加 `unrated`；加 `@PreAuthorize("hasRole('SUPER_ADMIN') or hasAuthority('rating.view')")`（既有仅 ROLE_ADMIN 链 → 补细粒度门控）
- [x] **T3 模板增量（AC1/AC2/AC4）**
  - [x] `templates/admin/vet-ratings.html`：保留均分摘要 + 已评表格；新增「未评问诊」表格（会话/时间/原因标签）+ 空态；页面**无写控件**
  - [x] i18n 外化 `admin.ratings.unrated.*`（zh_CN + en）
- [x] **T4 测试 + 回归（AC1~AC7）**
  - [x] L0：`ConsultQualityQueryServiceTest` +未评读口（RATED 排除 / UNRATED→TIMEOUT / INTERRUPTED→INTERRUPTED）；`AdminVetRatingsAccessControlTest`（rating.view 门控 + 超管）；3 个 `AdminVetServiceXxxTest` 构造同步（+ConsultQualityQueryService mock）
  - [x] L1：`VetRatingDetailIntegrationTest`（造 RATED 排除 + UNRATED→TIMEOUT + INTERRUPTED→中断 真跑经 PG）；全量 **852 测试绿**，既有 5.6 已评页不回归
  - [x] L2：无

## Dev Notes

### 架构约束（必须遵守）
- 后端 = Spring Boot 4 / Java 21 / Maven；本故事**扩既有** `admin/web/AdminWebController#vetRatings` + `admin/service/AdminVetService` + `templates/admin/vet-ratings.html`，未评读口在 consult 模块扩展（架构允许 admin/rating 或既有 admin slice 读 consult 评分）。[Source: admin-backend/architecture.md#Backend Module Map「AB-6 评分看板 → vet-ratings」；§Project Structure（admin/rating，读 consult 评分）]
- 路由保持既有 `/admin/vets/{id}/ratings`（名词 + 子资源，`/admin/**`，绝不落 `/api/v1`）。[Source: architecture.md#Routing]
- 跨模块**只经既有业务 service，禁跨 repo**：admin 侧取未评数据须经 consult.service，不得注入 consult/vet repository。[Source: architecture.md#Project Structure 边界]
- 授权 = 方法级 `@PreAuthorize("hasAuthority('rating.view')")`（A5，权威）；模板隐藏仅体验；`SUPER_ADMIN` 隐式全权（1.5）。[Source: architecture.md#Authentication & Security A5；§Routing rating.view]
- **纯只读 → 本 Epic 不建表、不加 Flyway、不写操作审计**（无写操作即无 `AdminAuditService.record(...)`）；仅靠 `rating.view` 门控。当前 Flyway 最大 = `V31`（Story 1.1）；本故事不应新增任何迁移。

### 既有代码现状（READ 过，brownfield 基线 — 现状 / 改什么 / 不可破坏）
- `admin/web/AdminWebController#vetRatings`（`GET /admin/vets/{id}/ratings`）：**现状**——`model` 放 `vet`（`AdminVetService.view(id)`）+ `ratings`（`AdminVetService.ratings(id)` → `VetRatingsView`），渲染 `admin/vet-ratings.html`，门控仅靠 `/admin/**` 的 `ROLE_ADMIN` 链。**改什么**——追加 `unrated` 模型属性 + 加 `@PreAuthorize("hasAuthority('rating.view')")`。**不可破坏**——已评列表与均分仍正常。
- `admin/service/AdminVetService#ratings(vetId)` → `ConsultRatingQueryService#forVet(vetId)` → `VetRatingsView(average, count, items[stars,comment,createdAt])`：**现状=已评数据来源，保留**；本故事新增「未评」取数（新方法或经 6.1 服务），不改 `ratings`。
- `admin/service/AdminVetService#view(vetId)` → `VetAdminView(id, username, displayName, status, createdAt)`：兽医表头来源，保留。
- `templates/admin/vet-ratings.html`：**现状**——`back` 链 + 标题 + `summary`（均分/条数）+ 已评 `table`（星级/评价/时间）+ 空态「暂无评分」。内联 `<style>`、蓝色 `#2d6cdf`（与 1.6 令牌不一致，待 1.6 收编）。**改什么**——在已评表格后**新增未评表格**；**不可破坏**——既有已评区结构/空态保留。
- `consult/service/ConsultRatingQueryService`：仅注入 `ConsultRatingRepository`。**改什么**——加未评读口（需 `ConsultSessionRepository`，可在同模块新服务里注入两个 consult repo；同模块内不破边界）。
- `consult/repository/ConsultSessionRepository#findByVetIdAndStatusInOrderByCreatedAtDesc(vetId, [CLOSED,INTERRUPTED])`：**现状=兽医终态会话读法（工作台历史 Tab 用），可复用**；注释写明「仅 consult.service 直接访问」——保持。
- `consult/repository/ConsultRatingRepository#existsBySessionId / findBySessionId`：判定某会话是否已评。
- `consult/domain/ConsultSession`：`closeRated()`→`CLOSED`+`RATED`；`closeUnrated()`→`CLOSED`+`UNRATED`（30min 评分门超时，置 `rating_prompt_state=PENDING` 供补弹）；`interrupt(...)`→`INTERRUPTED`（封禁中断，**不评分**）。`terminalAt()` 为历史展示/排序的单一时间口径。
- `consult/domain/ClosedReason`：`RATED` / `UNRATED`（30min 超时未评自动关闭）。

### 🟡 未评原因映射（关键，AC3 —— 不杜撰）
PRD AB-6B 文案列了两类原因：「用户未评分 / 30 分钟超时自动关闭」。**但当前数据模型里二者同源**——会话从 `PENDING_CLOSE` 只有两条出口：`closeRated()`（已评）或 `closeUnrated()`（30min 评分门超时、用户未在窗口内评分）。**不存在**独立的「用户主动拒评」终态。建议映射：
- **`status=CLOSED` 且 `closed_reason=UNRATED` 且无评分行** → 标「30 分钟超时自动关闭（用户未在评分窗口内评分）」。这一类同时覆盖 PRD 所说的「用户未评分」与「30 分钟超时」（实现上是同一事件）。
- **`status=INTERRUPTED`** → 标「会话中断未评（兽医封禁中断，不进入评分）」。这是 PRD 文案未单列、但数据中真实存在的第三类未评会话；**如实呈现**，不要塞进上面两类。
- **`closed_reason=RATED` 或存在评分行** → 属已评区，不进未评区（注意 `UNRATED` 后**补评分**的会话有评分行 → 归已评，**以「是否存在评分行」为权威**，不以 `closed_reason`）。
- ⚠️ 实现**不得**编造「用户主动拒评」这类数据里没有的标签。PRD 文案与实现的差异在 Completion Notes 记录，若产品坚持二分需回填 PRD / 排后续故事补数据字段。

### 关键边界 / 防坑
- **已评判定以评分行为权威**：`UNRATED` 关闭后补评分的会话有评分行 → 算已评、不进未评，避免重复计入两区。
- **隐私**：未评/已评均仅运营可见，切勿泄露到任何 `/api/v1` 或 App 可达响应。
- **纯只读**：模板不得出现任何写控件（含「下架/隐藏评价」之类未来想象功能）；AC4 要求页面零写副作用。
- **门控就位**：`@PreAuthorize("hasAuthority('rating.view')")` 依赖 Story 1.5 的 `@EnableMethodSecurity` 与 permission authority 装配；若 1.5 未落地，注解先就位（待生效），且**不弱于**既有 `ROLE_ADMIN` 链门控；在 Completion Notes 标注生效层级。
- **会话标识展示**：未评区列「会话标识」用内部 `sessionId` 即可（运营内部工具）；不展示 IM 正文/AI 上下文/用户媒体（NFR5 数据边界——本页本就只取元数据 + 评分）。

### 范围边界（不做 / 避免过度实现）
- **本故事不做**：全兽医评分总览（→ Story 6.1）、任何评分/会话写操作（编辑/删除/回复/导出）、向 App 公开评分、未评问诊的跟进/工单化（中断工单归 Epic 5）。
- **无新表、无 Flyway、无操作审计**（纯只读，本 Epic 统一约定）。
- 双语外化（`#{admin.*}`）统一在 1.6 收口，本故事新文案先中文。

### 测试标准
- L0：consult 未评读口单测（mock `ConsultSessionRepository` + `ConsultRatingRepository`：RATED 不入 / UNRATED-无补评 入 / UNRATED-有补评 不入 / INTERRUPTED 按约定入并标中断原因）；`AdminWebControllerTest#vetRatings` MockMvc 验已评+未评同页、原因标签、无 `rating.view` → 403、无写控件。沿用既有 `AdminWebControllerTest` 范式。
- L1（本地，需 Docker PG/Redis）：`docker compose up -d postgres redis` + `mvn -B test`；造上述四类会话真跑验未评分区与原因标注。**云端 headless 只跑到 L0；L1 标注待本地验收**（CLAUDE.md 云端约定）。

### Project Structure Notes
- 修改：`admin/web/AdminWebController#vetRatings`（+ unrated 模型 + `@PreAuthorize`）、`admin/service/AdminVetService`（+ 未评取数，或经 `AdminRatingService`）、`templates/admin/vet-ratings.html`（+ 未评表格）。
- 新增/扩展（consult 侧只读）：`consult/service/ConsultRatingQueryService`（或共用 6.1 的 `ConsultQualityQueryService`）+ 未评 DTO；`ConsultSessionRepository`/`ConsultRatingRepository` 复用现有读法（必要时加只读方法）。
- 与架构目录树一致。[Source: architecture.md#Backend Module Map（AB-6 → vet-ratings）]

### References
- [Source: admin-backend/epics.md#Story 6.2 单个兽医评分详情]
- [Source: admin-backend/PRD.md#4.7 模块 6 / AB-6B 单个兽医评分详情（已评完整历史 + 未评单列标原因；纯只读；仅运营、不对 App 公开）]
- [Source: admin-backend/architecture.md#Backend Module Map「AB-6 评分看板 → vet-ratings」/ §Project Structure（admin/rating 读 consult 评分）/ §Authentication & Security A5（@PreAuthorize）/「禁跨 repo」边界]
- [Source: petgo-backend/.../admin/web/AdminWebController.java#vetRatings（既有单兽医页，本故事扩 unrated + 门控）]
- [Source: petgo-backend/.../admin/service/AdminVetService.java#ratings/view（已评 + 表头来源，保留）]
- [Source: petgo-backend/.../consult/service/ConsultRatingQueryService.java（仅运营可见，待扩未评读口）]
- [Source: petgo-backend/.../consult/repository/{ConsultSessionRepository#findByVetIdAndStatusInOrderByCreatedAtDesc, ConsultRatingRepository#existsBySessionId}.java]
- [Source: petgo-backend/.../consult/domain/{ConsultSession,ClosedReason}.java（RATED/UNRATED/INTERRUPTED 终态、terminalAt 口径）]
- [Source: petgo-backend/.../templates/admin/vet-ratings.html（既有页结构，本故事增量未评区）]
- [Source: 1-1-后台账号模型与认证重构.md（Flyway 现状 V31；admin slice 基线）]

## Dev Agent Record

### Agent Model Used

claude-opus-4-8[1m]

### Debug Log References

- `mvn -B test`：852 通过 / 0 失败 / 6 跳过（新增 5：consult 未评读口 1 + vetRatings 门控 3 + 集成 1）。
- 顺手修了一处**与本故事无关的 TZ 边界 flaky**：`VetConsultControllerEndpointTest` 三处宠物生日用系统时区 `LocalDate.now()`，而生产 `PetProfileQueryService.ageMonths` 用 `LocalDate.now(ZoneOffset.UTC)`；跨日窗口（本地 06-30 / UTC 仍 06-29）导致 petAgeMonths 差 1 失败。改测试生日基准为 UTC 对齐生产，消除 flaky。
- 同时修了两处既有 i18n **重复键**（本会话前序故事引入）：`admin.ratings.title/empty`（6.1 总览与 5.6 单兽医页撞 → 6.1 改用 `admin.ratingOverview.*` 命名空间）、`admin.anomalies.note`（5.1 页副标题与「内部备注」标签撞 → 标签改 `admin.anomalies.internalNote`）。zh/en 键集仍一一对应（parity 测试绿）。

### Completion Notes List

- **未评原因映射（AC3，不杜撰）**：以「**是否存在评分行**」为已评权威（UNRATED 后补评者归已评、不入未评）；未评仅两类——`CLOSED` 无评分 → `TIMEOUT_UNRATED`（30 分钟超时，覆盖 PRD「用户未评/30 分钟超时」同源）、`INTERRUPTED` → `INTERRUPTED`（封禁中断，PRD 未单列但数据真实存在，如实呈现）。**未编造「用户主动拒评」**（数据模型无此终态）。
- **跨模块边界（AC6）**：未评读口在 consult 模块内（复用 6.1 `ConsultQualityQueryService`），admin 经 `AdminVetService.unratedConsults` → consult.service，不跨 repo。
- **门控补强（AC5）**：既有 `/admin/vets/{id}/ratings` 仅靠 `/admin/**` 的 ROLE_ADMIN 链，本故事补方法级 `@PreAuthorize(rating.view)`（超管隐式全权）。
- **纯只读（AC4）**：未评区无任何写控件；页面零写副作用；无新表/无 Flyway/无审计。
- **构造同步**：`AdminVetService` 加 `ConsultQualityQueryService` 依赖 → 同步更新 3 个 `AdminVetServiceXxxTest` 构造（+mock），既有行为不回归。
- **云端备注**：本机 Docker 在线，L0+L1 本地实测绿；云端 headless 只跑 L0。

### File List

新增（consult 模块，只读）：
- `petgo-backend/src/main/java/com/tailtopia/consult/dto/UnratedReason.java`
- `petgo-backend/src/main/java/com/tailtopia/consult/dto/VetUnratedConsult.java`

新增（测试）：
- `petgo-backend/src/test/java/com/tailtopia/admin/web/AdminVetRatingsAccessControlTest.java`
- `petgo-backend/src/test/java/com/tailtopia/admin/rating/VetRatingDetailIntegrationTest.java`

修改：
- `petgo-backend/src/main/java/com/tailtopia/consult/service/ConsultQualityQueryService.java`（+`unratedConsults(vetId)`）
- `petgo-backend/src/main/java/com/tailtopia/admin/service/AdminVetService.java`（+`ConsultQualityQueryService` 依赖 + `unratedConsults`）
- `petgo-backend/src/main/java/com/tailtopia/admin/web/AdminWebController.java`（vetRatings +unrated 模型 + `@PreAuthorize(rating.view)`）
- `petgo-backend/src/main/resources/templates/admin/vet-ratings.html`（+未评问诊表格）
- `petgo-backend/src/main/resources/i18n/messages_zh_CN.properties` / `messages_en.properties`（+`admin.ratings.unrated.*`；并修 6.1/5.1 重复键 → `admin.ratingOverview.*` / `admin.anomalies.internalNote`）
- `petgo-backend/src/main/resources/templates/admin/ratings.html`（6.1 页改用 `admin.ratingOverview.*` 键）
- `petgo-backend/src/main/resources/templates/admin/anomaly-detail.html`（内部备注标签改 `admin.anomalies.internalNote`）
- `petgo-backend/src/test/java/com/tailtopia/consult/service/ConsultQualityQueryServiceTest.java`（+未评读口单测）
- `petgo-backend/src/test/java/com/tailtopia/admin/service/{AdminVetServiceCreateTest,AdminVetServiceEditTest,AdminVetServiceListTest}.java`（构造 +mock）
- `petgo-backend/src/test/java/com/tailtopia/consult/web/VetConsultControllerEndpointTest.java`（修 TZ 边界 flaky：生日基准改 UTC）

### Change Log

- 2026-06-30：实现 Story 6.2 全量（T1~T4）——单兽医评分详情补「未评问诊单列 + 原因标注」（超时/中断如实映射）+ 既有页补 rating.view 门控 + 双语 i18n；纯只读无表无审计。顺带修 1 处 TZ flaky + 2 处 i18n 重复键。+5 测试，全量 852 通过。Status → review。
