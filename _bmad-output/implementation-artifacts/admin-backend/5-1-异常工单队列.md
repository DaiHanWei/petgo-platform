# Story 5.1: 异常工单队列

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

> **范围域**：管理后台 V1.0.0 · Epic 5（问诊异常与会话查询）· Story 5.1（AB-4A）。**建立在 Epic 1 地基（admin_accounts / 服务端会话 / `AdminAuditService` 哈希链审计 / permission authority 门控 / i18n）与 Epic 2 封禁流（Story 2.5 发 `ConsultAnomalyRaisedEvent`）之上**。
> **产物归属**：仅后端 `petgo-backend`（**无 Flutter 侧**）。brownfield 扩 `com.tailtopia.admin`，**新建 `com.tailtopia.admin.anomaly` 包**。SSR（Thymeleaf）+ HTMX，走 `/admin/**`，**不返 JSON**。
> **数据边界（NFR5，硬约束）**：问诊消息正文 / AI 上下文 / 用户媒体储存于第三方腾讯 IM，本故事**绝不跨系统拉取**，工单只承载 TailTopia 系统内的会话元数据。

## Story

As a **运营成员**,
I want **处理因兽医被封禁而自动生成的问诊异常工单（仅元数据 + 内部备注 + 处理图 + 归档）**,
so that **能在 48h 内（AM-2）追溯并处置受影响的问诊，且全程不触达第三方消息正文**。

## Acceptance Criteria

> 验证层：**L0** 编译/单测/MockMvc 切片（mock service/repo，无需 DB）· **L1** 集成（Docker postgres+redis，验事件落工单/去重/门控/审计/迁移）· **L2** 外部凭证（处理图上传走 OSS 私密桶 + 签名 URL，留本地/真凭证手测）。

1. **AC1（建表，L0 编译 + L1 迁移）**：新增 Flyway `V38__init_consult_anomalies.sql` 建 `consult_anomalies`；对应 JPA 实体字段/列**精确匹配**（`ddl-auto=validate` 启动不报错）。枚举落库 `varchar` + UPPER_SNAKE；时间 `timestamptz` UTC；`session_id` 上唯一索引（一会话至多一工单，AC8 去重的库级兜底）。
2. **AC2（事件落工单，L1 + L0 listener 单测）**：`ConsultAnomalyService`（`admin.anomaly.service`）以 `@Async @EventListener` 订阅 Story 2.5 封禁流发出的 `ConsultAnomalyRaisedEvent`；每个事件落一条工单，**仅写元数据**（`session_id` / `user_id` / `vet_id` / `session_started_at` / `session_ended_at` / `session_status` / `anomaly_type` = `VET_BANNED`），`status=OPEN`。
3. **AC3（仅元数据，绝不读第三方，L0/L1）**：工单实体/DTO/模板**不含**任何 IM 正文、AI 分诊上下文、用户媒体字段；列表与详情仅展示会话元数据；不调用任何 IM / AI 拉取接口（NFR5）。
4. **AC4（内部备注，L1 + 审计）**：运营在工单详情可添加内部文字备注（用户不可见）；写操作经 `AdminAuditService.record(ANOMALY_NOTE_ADDED, anomalyId, summary)` 同事务落审计哈希链。
5. **AC5（标记已处理 + 处理图 + 归档，L1/L2）**：运营可「标记已处理」（选填上传处理过程图）→ 工单 `status=RESOLVED`、记 `resolved_by`/`resolved_at`、移入归档区；**已归档工单仍可被搜索/查看**；处理图存 OSS 私密桶（短签名 URL + EXIF 剥离，复用 `shared/media`），**库内只存对象 key，绝不存签名 URL**；写操作经 `AdminAuditService.record(ANOMALY_RESOLVED, anomalyId, summary)`（summary **不含**签名 URL/对象 key 之外的敏感内容）。
6. **AC6（工单不可删，L1）**：无任何删除工单的端点/UI；应用层不提供 DELETE 路径（与审计「只可追加」一致的姿态，AB-4A「工单记录不可删除」）。
7. **AC7（双入口可达 + 筛选，L0 视图 + L1）**：异常工单队列经**模块 4 导航**与**模块 1（兽医管理）**均可访问（两处侧栏入口指向同一 `/admin/anomalies`）；可按工单状态（待处理 OPEN / 已归档 RESOLVED）筛选，HTMX 片段返回匹配行；归档区可搜。
8. **AC8（去重幂等，L1）**：同一 `session_id` 重复事件（重放/并发）**不产生重复工单**（按 `session_id` upsert/存在即跳过 + 唯一索引兜底）；落工单失败有结构化日志（脱敏，不记 PII/健康数据）。
9. **AC9（权限门控，L0/L1）**：查看队列/详情 `@PreAuthorize("hasAuthority('consult.view_anomalies')")`；加备注/标记已处理 `@PreAuthorize("hasAuthority('consult.handle')")`；无对应 authority 访问返回 403（前端隐藏仅体验，A5）。
10. **AC10（回归绿，L0/L1）**：`mvn -B compile` + 既有 admin/consult 单测全绿；新增 listener/service/controller 单测；本地 L1 全量回归绿，新 bean 注入 `@SpringBootTest` 上下文无误。

## Tasks / Subtasks

- [x] **T1 数据模型（AC1）**
  - [x] `admin/anomaly/domain/ConsultAnomaly`（`@Table("consult_anomalies")`）：全字段含 `sessionId(uniq)` / `userId(nullable)` / `vetId` / 会话快照 / `anomalyType` / `status` / `internalNote(text)` / `resolutionImageKey` / `resolvedBy/At` / 时间戳；`open()` 工厂 + `setInternalNote()`/`resolve()` 状态变更
  - [x] 枚举 `AnomalyType(VET_BANNED)`、`AnomalyStatus(OPEN/RESOLVED)`
  - [x] `ConsultAnomalyRepository`：`existsBySessionId` / `findBySessionId` / `findByStatusOrderByCreatedAtDesc` / `findAllByOrderByCreatedAtDesc`（**无删除方法**，AC6）
  - [x] **`V42__init_consult_anomalies.sql`** ⚠️ 实测顺延：story 指示 V38 已被上游占用（实测最大 V41），按决策 E2 取 **V42**；session_id 唯一索引 + status/type CHECK（列长度无 VARCHAR(1)）
- [x] **T2 事件订阅落工单（AC2/AC8）**
  - [x] `ConsultAnomalyService`：`@Async @EventListener onAnomalyRaised` 薄委托 → 公开 `recordTicket(event)`（@Transactional，便于集成测试同步驱动）；映射事件 → `ConsultAnomaly`（status=OPEN、type=VET_BANNED、sessionStatus=INTERRUPTED）
  - [x] 去重：`existsBySessionId` 首道 + 捕获 `DataIntegrityViolationException`（唯一索引并发兜底）当作已存在吞掉
  - [x] 失败结构化日志（脱敏，不记 PII/health）
  - [x] 〔依赖确认〕`ConsultAnomalyRaisedEvent` 已由 **Story 2.5** 在 `ConsultInterruptService:53` 封禁中断流发出（本故事仅消费，未改 2.5）
- [x] **T3 队列/详情控制器（AC3/AC6/AC7/AC9）**
  - [x] `AdminAnomalyController`：`GET /admin/anomalies`（状态筛选 + HX-Request 返 `:: rows` 片段）、`GET /admin/anomalies/{id}`（详情：仅元数据 + 备注 + 处理图现签 URL）；`consult.view_anomalies` 门控；**无删除端点**（AC6）
- [x] **T4 处理动作（AC4/AC5）**
  - [x] `POST /admin/anomalies/{id}/note` → `internalNote` + 审计 `ANOMALY_NOTE_ADDED`
  - [x] `POST /admin/anomalies/{id}/resolve`（选填 `resolutionImageKey`，对象 key 由客户端预签上传——与 2.7 资质图同范式）→ `status=RESOLVED`/`resolvedBy`/`resolvedAt`/`resolutionImageKey` + 审计 `ANOMALY_RESOLVED`（summary 不含 key/签名 URL）
  - [x] 二者 `consult.handle` 门控；详情页 `SignedUrlService.sign(key)` 现签（**库内只存对象 key**）
- [x] **T5 模板 + 双入口导航（AC7）**
  - [x] `templates/admin/anomalies.html`（列表 + `:: rows` 片段 + 状态筛选）+ `admin/anomaly-detail.html`（元数据只读 + 备注/标记已处理表单）
  - [x] `layout.html`：模块 4「问诊异常」分组 + 兽医分组下各一条入口（**双入口同页** `/admin/anomalies`，均 `consult.view_anomalies` 门控）；i18n 外化 `admin.anomalies.*` / `admin.nav.anomalies`（zh_CN + en）
- [x] **T6 测试 + 回归（AC2~AC10）**
  - [x] L0：`ConsultAnomalyServiceTest`（新事件落库 / 已存在跳过 / 唯一冲突吞掉 / 备注（含空校验）/ 标记已处理 + 审计 mock）；`AdminAnomalyAccessControlTest`（查看 view_anomalies、备注/处理 handle、超管隐式全权）；`AdminPagesRenderSmokeTest` 加 `/admin/anomalies`（中英渲染无缺键）
  - [x] L1：`ConsultAnomalyIntegrationTest`（V42 validate 经上下文启动；recordTicket 落库 + session_id 去重幂等；备注/标记已处理 + 审计哈希链各一条）；全量 **829 测试绿**
  - [ ] L2（留本地/真凭证手测）：处理图上传 OSS 私密桶 + 详情签名 URL + EXIF 剥离

## Dev Notes

### 架构约束（必须遵守）
- 后端 = Spring Boot 4 / Java 21 / Maven；新建包 `com.tailtopia.admin.anomaly.{web,service,domain,repository,dto}`。[Source: admin-backend/architecture.md#Project Structure → `anomaly/ {web,service,domain,repository,dto}`；line 250]
- 后台一律 `/admin/**` + HTML/HTMX 片段，**不返 JSON**；服务端会话 + CSRF（adminFilterChain，A1，Epic 1 已立）。[Source: architecture.md#AI Agent Guidelines line 367]
- **审计是强制副作用**：每个后台**写操作**同事务经 `AdminAuditService.record(...)` 写 `admin_audit_logs`（哈希链）；`action_type` UPPER_SNAKE 过去式。本故事新增动作码：`ANOMALY_NOTE_ADDED` / `ANOMALY_RESOLVED`。[Source: architecture.md#Implementation Patterns line 200；Story 1.3]
- **跨模块只经既有 service / 领域事件，禁跨 repo**：本故事消费 `consult` 模块发出的 `ConsultAnomalyRaisedEvent`，**不**直访 `consult` 任何 repository；兽医/用户展示名如需解析，经既有 `VetAccountService` / 用户侧 service（V1.0.0 工单展示以 ID 为主，名称解析可选）。[Source: architecture.md line 188/201/294；CLAUDE.md 强制护栏]
- 异步只用 `@Async`（+ `@EventListener`），**禁引入 MQ / 通用缓存 / 新中间件**。[Source: CLAUDE.md 强制护栏；architecture.md line 367]
- 权限 authority：`consult.view_anomalies`（查看）/ `consult.handle`（处理）；方法级 `@PreAuthorize`，前端隐藏仅体验（A5）。[Source: architecture.md line 182；Story 1.5]
- 敏感数据：处理图属高敏，私密桶 + 短签名 URL + EXIF 剥离 + 日志不落；审计/日志**绝不**含签名 URL / PII / 健康数据。[Source: architecture.md#NFR7 line 214；CLAUDE.md 命名映射链]

### 事件契约（与 Story 2.5 的反向依赖 —— 关键）
- `ConsultAnomalyRaisedEvent` **由 Story 2.5（封禁/解封兽医）产出**：封禁流复用既有 `ConsultInterruptService.interruptByVetBan(vetId)`（`IN_PROGRESS/PENDING_CLOSE → INTERRUPTED`），在**每个被中断会话**处，除既有 `ConsultInterruptedEvent` 外**再发一条** `ConsultAnomalyRaisedEvent`。建议事件载荷（record，置于 `consult.event`）：`sessionId / userId / vetId / sessionStartedAt(=createdAt) / sessionEndedAt(=interruptedAt/terminalAt) / sessionStatus(=INTERRUPTED) / anomalyType(=VET_BANNED)`。
- **执行顺序合规**：Epic 2 在 Epic 5 之前实现，故 5.1 编码时 2.5 已发布该事件（反向依赖在时间线上成立）。**若实现 5.1 时事件尚未就绪**，dev 须先在 `consult.event` 定义该事件并在 `ConsultInterruptService` 补发（属 2.5 的产出物，标注在 Completion Notes，勿改其它语义）。
- 本故事订阅用 `@Async @EventListener`（架构明示）；注意 `interruptByVetBan` 为 `@Transactional`，`@Async` 监听在独立线程消费 —— 工单落库与封禁事务**最终一致**即可（48h SLA，非强一致）；若需「封禁回滚则不建工单」更强保证，可改 `@TransactionalEventListener(phase=AFTER_COMMIT)` + `@Async`，dev 视实现权衡并在 Completion Notes 记录。[Source: ConsultInterruptService.java:36-51]

### 既有代码基线（READ —— 现状 / 改什么 / 不可破坏）
- `consult/service/ConsultInterruptService.interruptByVetBan`（**现状**）：批量中断 + 发 `ConsultInterruptedEvent`。**改什么**：归 Story 2.5 —— 增发 `ConsultAnomalyRaisedEvent`（本故事不动其它逻辑）。**不可破坏**：既有中断/IM 系统消息/`ConsultInterruptedEvent` 推送链路。[Source: consult/service/ConsultInterruptService.java]
- `consult/domain/ConsultSession`（**现状**）：会话状态机 + `terminalAt()`（终态时间单一口径：`interruptedAt` 优先）+ `interruptedReason/interruptedAt`。本故事**只读语义**（经事件取快照，不直访实体）。[Source: consult/domain/ConsultSession.java:256-281]
- `admin/web/AdminWebController` + `templates/admin/layout.html`（**现状**）：SSR 外壳，侧栏已有「概览/内容/兽医（Epic 5）」分组；`th:replace="~{admin/layout :: page(~{::content})}"`。**改什么**：layout 增「问诊异常」分组 + 兽医分组下加工单入口（双入口）。**不可破坏**：既有导航/页面渲染。[Source: templates/admin/layout.html:29-50]
- `shared/media`（**现状**）：`PresignedUploadService` / `SignedUrlService` / EXIF 剥离链（兽医资质 2.7 复用同设施）。本故事处理图复用之。[Source: shared/media/{PresignedUploadService,SignedUrlService}.java]
- `AdminAuditService`（Story 1.3 产出）：`record(actionType, targetId, summary, ...)` 统一入口，哈希链 append-only。本故事写操作必调。[Source: architecture.md line 200/238；Story 1.3]
- `@PreAuthorize` + permission authority（Story 1.5 产出）：authority 字符串即 `permission_code`。[Source: Story 1.5；architecture.md line 182]

### 关键边界 / 防坑
- **NFR5 红线**：实体/DTO/模板**零** IM 正文、AI 上下文、用户媒体字段；评审时 grep 确认无任何 `imClient`/AI 拉取调用。
- **签名 URL 绝不入库/审计**：`consult_anomalies` 只存 `resolution_image_key`（对象 key）；展示时 `SignedUrlService` 现签；审计 summary 不含签名 URL。
- **去重**：`session_id` 唯一索引是兜底，service 层 `existsBySessionId` 是首道；`@Async` 并发可能撞唯一约束 → 捕获 `DataIntegrityViolationException` 当作「已存在」吞掉，不抛。
- **不可删**：不写任何 `@DeleteMapping`/删除 service 方法（AC6），归档=状态切 `RESOLVED` 而非删行。
- **双入口同页**：两个侧栏链接指向**同一** `/admin/anomalies`，避免重复实现/路由分叉。
- **用户匿名化兼容**：注销（D1）会把会话 `user_id` 置空（`ConsultSession.anonymize`），故工单 `user_id` 可空，展示需容空。[Source: consult/domain/ConsultSession.java:167-171]

### Flyway（V38 —— 顺延说明）
- 架构早期示意把 anomaly 表归在「V30+ 指示性序号」，**但序号按 sprint 实际执行顺序单调分配（决策 E2，勿照搬示例号）**：Story 1.1 已占用 **V30/V31**；后续 **V32/V33** 归 1.3/1.4（审计/会话日志），**V34** 归 2.1（兽医资质），**V35** 归 2.9（失败请求），**V36/V37** 归 4.3（人工审核队列 + admin_settings）。故本故事 anomaly 表实际顺延为 **V38**。实现时以 `db/migration/` 实测最大号 +1 为准（若前序 story 的实际占号有出入，按单调顺延取下一个空号，并在 Completion Notes 记录）。[Source: CLAUDE.md 实现纪律 §4；Memory: prd-r2-rework-delta-and-frozen-flyway]
- DDL 草案（列长度勿用 `VARCHAR(1)`，Hibernate6 会映 CHAR(1) 触发 validate 全红）：
```sql
-- V38__init_consult_anomalies.sql
CREATE TABLE consult_anomalies (
  id                   BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  session_id           BIGINT       NOT NULL,
  user_id              BIGINT,                          -- 注销匿名化后可空
  vet_id               BIGINT       NOT NULL,
  session_started_at   TIMESTAMPTZ,
  session_ended_at     TIMESTAMPTZ,
  session_status       VARCHAR(16)  NOT NULL,           -- 中断时会话状态快照（INTERRUPTED）
  anomaly_type         VARCHAR(32)  NOT NULL,           -- VET_BANNED
  status               VARCHAR(16)  NOT NULL,           -- OPEN | RESOLVED
  internal_note        TEXT,                            -- 内部备注（用户不可见）
  resolution_image_key VARCHAR(255),                    -- OSS 对象 key（绝不存签名 URL）
  resolved_by          BIGINT,                          -- admin_accounts.id
  resolved_at          TIMESTAMPTZ,
  created_at           TIMESTAMPTZ  NOT NULL DEFAULT now(),
  updated_at           TIMESTAMPTZ  NOT NULL DEFAULT now(),
  CONSTRAINT ck_consult_anomalies_status CHECK (status IN ('OPEN','RESOLVED')),
  CONSTRAINT ck_consult_anomalies_type   CHECK (anomaly_type IN ('VET_BANNED'))
);
CREATE UNIQUE INDEX uq_consult_anomalies_session ON consult_anomalies (session_id);
CREATE INDEX idx_consult_anomalies_status ON consult_anomalies (status);
```
- 实体 `@Column` 名/可空性/长度必须与 DDL 精确一致（`ddl-auto=validate`）；主键生成策略与既有 admin 迁移（V30）保持一致。

### 范围边界（避免前向依赖 / 过度实现）
- **本故事不做**：会话元数据查询页（→ Story 5.2）；`ConsultAnomalyRaisedEvent` 的**发布**（→ Story 2.5，本故事仅消费，必要时补定义）；评分看板（→ Epic 6）；任何 IM/AI 正文读取（NFR5 永不做）。
- 本故事**只**交付：anomaly 表/实体（V38）+ 事件订阅落工单（去重）+ 队列/详情页（双入口）+ 内部备注 + 标记已处理（处理图 + 归档）+ 权限门控 + 审计 + 不可删。

### 测试标准
- L0：`ConsultAnomalyService`（mock repo：新事件落库 / 已存在跳过 / 唯一冲突吞掉）；`AdminAnomalyController`（视图名 + 筛选片段 + 写操作调 service & `AdminAuditService`，mock）；NFR5 静态走查（无 IM/AI 字段与调用）。沿用既有 `AdminWebControllerTest` 的 MockMvc/Mockito 范式。
- L1（本地，需 Docker PG）：迁移 validate 绿；发事件 → 工单落库（@Async）；重复事件不重复落；`consult.view_anomalies`/`consult.handle` 门控 403；审计哈希链每写操作各一条。**云端 headless 只跑到 L0；L1/L2 标注待本地验收**（CLAUDE.md 云端约定）。
- L2（留手测）：处理图上传 OSS 私密桶 + 详情签名 URL + EXIF 剥离。

### Project Structure Notes
- 新增：`admin/anomaly/{domain/ConsultAnomaly,AnomalyType,AnomalyStatus, repository/ConsultAnomalyRepository, service/ConsultAnomalyService, web/AdminAnomalyController, dto/*}`；`templates/admin/anomalies.html`；`V38__init_consult_anomalies.sql`。
- 修改：`templates/admin/layout.html`（双入口导航）；（归 2.5）`consult/event/ConsultAnomalyRaisedEvent` + `ConsultInterruptService` 增发。
- 与架构目录树一致：[Source: architecture.md#Project Structure line 250-251 `anomaly/ … service/{ConsultAnomalyService} 订阅 ConsultAnomalyRaisedEvent`]

### References
- [Source: admin-backend/epics.md#Story 5.1 异常工单队列]
- [Source: admin-backend/PRD.md#AB-4A 异常工单队列（line 445-465）；NFR5 数据边界（line 50）]
- [Source: admin-backend/architecture.md#模块4 问诊异常（line 34）；#Implementation Patterns 审计/跨模块事件（line 188/200/201/294）；#Project Structure anomaly（line 250-251）；#permission_code（line 182）；#NFR7（line 214）]
- [Source: 1-1-后台账号模型与认证重构.md（admin_accounts / Flyway V30/V31 基线 / Hibernate6 CHAR(1) 坑）]
- [Source: consult/service/ConsultInterruptService.java（封禁中断流，2.5 在此增发事件）]
- [Source: consult/domain/ConsultSession.java（terminalAt / interruptedAt / anonymize / 状态机）]
- [Source: shared/media/{PresignedUploadService,SignedUrlService}.java（私密桶 + 签名 URL + EXIF）]
- [Memory: [[hibernate6-char1-and-cloud-l0-gap]]（CHAR(1) 坑 + 云端 L0 盲区）]
- [Memory: [[prd-r2-rework-delta-and-frozen-flyway]]（冻结迁移、新表顺延 E2）]

## Dev Agent Record

### Agent Model Used

claude-opus-4-8[1m]

### Debug Log References

- `mvn -B test`：829 通过 / 0 失败 / 6 跳过（新增 15：service 6 + 门控 7 + 集成 2）。
- `ConsultAnomalyIntegrationTest` 上下文启动 ＝ V42 迁移 `ddl-auto=validate` 通过。

### Completion Notes List

- **事件依赖已就位**：`ConsultAnomalyRaisedEvent` 由 Story 2.5 在 `ConsultInterruptService:53` 封禁中断流发出（字段 `sessionId/userId/vetId/startedAt/endedAt/anomalyType`），本故事仅消费、未改 2.5。事件不携 sessionStatus → 服务侧固定写 `INTERRUPTED`（VET_BANNED 恒为中断态）。
- **@Async 与可测性**：`@Async @EventListener onAnomalyRaised` 薄委托到公开 `recordTicket`，集成测试同步调 `recordTicket` 验持久化 + 去重（避开 @Async 时序 flaky）；生产仍异步消费（48h SLA 最终一致即可）。
- **去重双保险**：service `existsBySessionId` 首道 + `session_id` 唯一索引兜底（捕获 `DataIntegrityViolationException` 当已存在吞掉）。
- **NFR5 红线**：实体/DTO/模板**零** IM 正文/AI 上下文/用户媒体字段，无任何 IM/AI 拉取调用；工单只承载会话元数据。
- **处理图**：与 2.7 资质图同范式——库内只存 OSS 对象 key（客户端预签上传得 key 后提交，实际上传属 L2 手测），详情页 `SignedUrlService` 现签短 TTL URL；审计 summary 不含 key/签名 URL。
- **不可删（AC6）**：repository 无删除方法、controller 无 `@DeleteMapping`；归档＝置 `RESOLVED`。
- **双入口（AC7）**：layout 在「兽医」分组 + 新「问诊异常」分组各放一条入口，均指向同一 `/admin/anomalies`，避免路由分叉。
- **用户匿名化兼容**：`user_id` 可空，列表/详情容空显示「已匿名化」。
- **Flyway 实测顺延（E2）**：story 指示 V38 过时，实测最大 V41 → 取 **V42**。
- **云端备注**：本机 Docker 在线，L0+L1 本地实测绿；L2（OSS 处理图）待本地/真凭证手测；云端 headless 只跑到 L0。

### File List

新增（admin.anomaly 模块）：
- `petgo-backend/src/main/java/com/tailtopia/admin/anomaly/domain/AnomalyType.java`
- `petgo-backend/src/main/java/com/tailtopia/admin/anomaly/domain/AnomalyStatus.java`
- `petgo-backend/src/main/java/com/tailtopia/admin/anomaly/domain/ConsultAnomaly.java`
- `petgo-backend/src/main/java/com/tailtopia/admin/anomaly/repository/ConsultAnomalyRepository.java`
- `petgo-backend/src/main/java/com/tailtopia/admin/anomaly/service/ConsultAnomalyService.java`
- `petgo-backend/src/main/java/com/tailtopia/admin/anomaly/web/AdminAnomalyController.java`
- `petgo-backend/src/main/resources/templates/admin/anomalies.html`
- `petgo-backend/src/main/resources/templates/admin/anomaly-detail.html`
- `petgo-backend/src/main/resources/db/migration/V42__init_consult_anomalies.sql`

新增（测试）：
- `petgo-backend/src/test/java/com/tailtopia/admin/anomaly/service/ConsultAnomalyServiceTest.java`
- `petgo-backend/src/test/java/com/tailtopia/admin/anomaly/web/AdminAnomalyAccessControlTest.java`
- `petgo-backend/src/test/java/com/tailtopia/admin/anomaly/ConsultAnomalyIntegrationTest.java`

修改：
- `petgo-backend/src/main/java/com/tailtopia/admin/audit/service/AuditActions.java`（+ANOMALY_NOTE_ADDED / ANOMALY_RESOLVED）
- `petgo-backend/src/main/resources/templates/admin/layout.html`（双入口导航）
- `petgo-backend/src/main/resources/i18n/messages_zh_CN.properties` / `messages_en.properties`（+`admin.nav.anomalies` / `admin.nav.group.consult` / `admin.anomalies.*`）
- `petgo-backend/src/test/java/com/tailtopia/admin/web/AdminPagesRenderSmokeTest.java`（路径表加 `/admin/anomalies`）

### Change Log

- 2026-06-29：实现 Story 5.1 全量（T1~T6 / L2 留手测）——异常工单 @Async 事件落库（去重幂等）+ 队列/详情双入口页 + 内部备注 + 标记已处理（处理图 key + 归档）+ 审计 + 门控分层 + 不可删 + 双语 i18n；NFR5 仅元数据。Flyway V42。+15 测试，全量 829 通过。Status → review。
