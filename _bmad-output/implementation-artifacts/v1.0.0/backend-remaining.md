# 后端剩余工作清单

> 生成日期：2026-06-15
> 范围：`petgo-backend/`。
> 基线事实：全部 54 个 story 均为 `review` 态（L0 单测绿，代码已实现），无 `backlog`/`ready-for-dev`。下列为"代码层已写完之外"真正未收口的部分。

---

## 🔴 Tier 1 — 还缺的后端代码（需要写）

### 1. 内容图像审核（Story 2-3，决策 F10）
- **现状**：`ContentModerationService` 为 V1 stub。
  - 文字审核：应用内关键词黑名单，占位词取印尼语/英语（赌博/毒品/色情/诈骗类），**非真实敏感词库**。
  - 图像审核：接口占位，**默认放行**（仅靠特殊标记 URL 触发可测分支）。
- **待办**：
  - [ ] 接真实三方图像审核（违规图拦截、不落库、返回 422）。
  - [ ] 文字关键词黑名单替换为真实敏感词库。
- **护栏**：真实接入仅替换 `ContentModerationService` 内部，调用方 `ContentService` 不变；**禁引入 MQ/缓存/新中间件**（CLAUDE.md）。

---

## 🟡 Tier 2 — 代码已写完，只差切真凭证 / 配置（开开关，非写代码）

| 能力 | 实现类（已就绪） | 需要补的配置 | 相关 Story |
|---|---|---|---|
| Gemini AI 分诊 | `GeminiDeveloperApiClient`（真 HTTP 已写） | `GEMINI_API_KEY` + `petgo.ai.gemini.mode=live` | 4-1 |
| 阿里云 OSS/STS 媒体直传 | OSS/STS service（含 EXIF 剥离） | 真 bucket + STS AccessKey/Role 凭证 | 2-1 |
| Google 登录校验 | `NimbusGoogleTokenVerifier` | 真 Google client id + 联网 JWKS（L2） | 1-3 |

- [ ] 配齐上述 env 注入（遵守 CLAUDE.md：凭证全部 env 注入、绝不入库、`.env.example` 只放占位）。
- [ ] 各项 `mode` 从 `stub` 切到 `live` 后做一次真实联通验证。

---

## 🟢 Tier 3 — 验收缺口（代码绿，但从未真跑过）

### L1 集成验收（需 Docker postgres + redis）
- [ ] **54 个 story 全部标注「L1 待本地」——没有任何一个在真库上验证过。** 重点核验：
  - [ ] Flyway 迁移 V1→V27 真跑无错。
  - [ ] `spring.jpa.hibernate.ddl-auto=validate` 启动不报红（schema 与实体一致，注意 Hibernate6 CHAR(1) 坑）。
  - [ ] 关键端点真实状态码（200 / 401 / 403 / 422 / 404 不可枚举）。
  - [ ] `/actuator/health` = UP。
- ⚠️ 安全攸关节点单独验：5-3 抢单原子写入（先到先得）/ 等待退出取消（WAITING→CANCELLED）。

### L2 端到端验收
- [ ] 真三方凭证联通（Gemini / OSS / Google，见 Tier 2）。
- [ ] 媒体：真阿里 OSS 直传双桶 + 越权拒 + 签名 URL 403/200/过期 + EXIF 端到端 + 真机相册/相机/HEIC。
- [ ] 分诊：真 Gemini 三态结果（绿/黄/红）。

---

## 🔵 Tier 4 — 上线前待确定（运营 / 产品 / 可观测性，非纯写代码）

> 这些不是"功能没写"，而是上线前必须**敲定内容、补埋点、定终值**的收口项；漏掉会导致功能在、但发出去是空壳 / 看不到数据 / 参数是测试值。部分跨前后端，但触发点与数据源在后端。

### 4.1 推送文案与触发内容（Epic 6 / 8）
- [ ] **每类推送的 title/body 双语终稿（id / en）**——目前是占位/示意文案，需产品定稿：
  - [ ] 兽医回复（6-2）
  - [ ] 内容互动：被点赞 / 被评论（6-3）
  - [ ] App 版本更新提醒（6-5）
  - [ ] 定时类系统推送：生日 / 纪念日 / 里程碑节点（6-7）
  - [ ] L 级里程碑达成（8-6）
- [ ] **触发时机与频控**：定时类几点发、同日多事件是否合并、防打扰窗口。
- [ ] **深链落地**：每个 `deepLinkType` → 落地页映射确认 + 失效/兜底文案（链接已失效/内容已删除）。

### 4.2 埋点 / 可观测性（上线前必埋）
- [ ] **业务埋点位置**确定并落点（关键转化漏斗）：
  - [ ] 游客 → 登录 → 建档 转化
  - [ ] 内容发布 / Feed 曝光 / 点击 / 点赞 / 评论
  - [ ] 问诊提交 → 三态结果（绿/黄/红）→ 升级兽医
  - [ ] 名片分享（FAB / H5）
  - [ ] 里程碑达成 / 打卡
  - [ ] 推送 送达 → 点击 → 深链落地
- [ ] **监控告警**：关键指标 dashboard + 告警阈值（接口错误率、p95 延迟、`@Async` 任务积压/失败、分诊处理时长、Flyway 启动失败）。
- [ ] **链路追踪**：ProblemDetail 的 `traceId` 全链路贯通（已知 1-5 处 401/403 未注入 traceId，待补）。
- [ ] **审计可溯**：安全攸关动作（注销级联删除 / 兽医封禁 / 安全规则升红）有迹可查，且日志不含 PII / 健康数据 / 令牌（CLAUDE.md）。

### 4.3 运营内容与文案（待填真内容）
- [ ] **冷启动种子内容**（3-1）：Feed 首屏真实运营内容，避免上线空列表。
- [ ] **敏感词库终稿**：与 Tier 1 图像审核同源，替换占位词。
- [ ] **里程碑清单文案**（8-1）：猫30 / 狗30 / 通用15 的标题+描述双语终稿（印尼语初稿待复核）。
- [ ] **双语文案收口**（7-2）：印尼语翻译初稿全量复核。
- [ ] **法务文案**：隐私政策 / 用户协议 / 注销告知（7-3，D1/D2 合规对外口径）。

### 4.4 配置终值与运维
- [ ] **限流阈值**生产终值确认（`RedisRateLimiter` 当前是否测试值）。
- [ ] **令牌/URL 时效**终值：JWT 过期、签名 URL TTL、UserSig/会话短时凭证有效期。
- [ ] **部署与数据**：德国单机部署环境 + 数据备份 / 保留策略。

---

## 已知遗留（非阻断，来自评审）
- 问诊严重度胶囊 YELLOW(#E0A458)+白字对比度可能 < AA 4.5:1，后续可改浅底深字并补 token（见 `deferred-work.md`）。

---

## 一句话总结
> "写代码"层面只剩 **内容图像审核接真三方**一项；其余分三块——**切真凭证**（Gemini/OSS/Google）、**全量 L1/L2 验收**、以及**上线前待确定**（推送文案、埋点位置、运营内容、配置终值）。前两块本就留在本地/真凭证阶段，第三块需产品/运营拍板。
