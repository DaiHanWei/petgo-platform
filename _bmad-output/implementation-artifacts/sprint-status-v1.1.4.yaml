# generated: 2026-08-15
# last_updated: 2026-08-15
# project: TailTopia V1.1.4
# project_key: NOKEY
# tracking_system: file-system
# story_location: {project-root}/_bmad-output/implementation-artifacts/v1.1.4
# source_epics: {project-root}/_bmad-output/planning-artifacts/epics-v1.1.4.md (4 Epic / 15 Story)
#               2026-08-15 已含 IR-1 ~ IR-10 修补回写 + IR-7R 撤销（AC 增补与切分，无设计变更）
# architecture: {project-root}/_bmad-output/planning-artifacts/architecture-v1.1.4-delta.md (AD-1 ~ AD-20, 权威)
#               冲突序：架构 delta > epics > PRD > 决策日志 > CROSS-STORY-DECISIONS > V1.0/V1.1/V1.1.2 基线
# prd:          {project-root}/_bmad-output/planning-artifacts/v1.1.4/PRD-v1.1.4.md (含 2026-08-15 校验订正)
# ux:           {project-root}/_bmad-output/planning-artifacts/v1.1.4/ui-v1.1.4.html (28 屏 / 4 泳道)
# readiness:    {project-root}/_bmad-output/planning-artifacts/implementation-readiness-report-2026-08-15-v1.1.4.md
#               结论 READY —— 17 条发现（0 严重 / 2 主要 / 15 次要），9 条已回写 epics
#
# NOTE: 本文件是 V1.1.4 增量 sprint 跟踪，与 V1.0 的 sprint-status.yaml、V1.1 的
#       sprint-status-v1.1.yaml、V1.1.2 的 sprint-status-v1.1.2.yaml 并存，均不覆盖。
#       story 文件另放子目录 v1.1.4/，避免与前几版的 1-1/2-1 编号混淆。
#
# ⚠️ 版本号说明：本「1.1.4」不是历史上那个 1.1.4。该号 2026-08-11 被社区管控这批需求取走，
#    原 1.1.4（内容运营与增长）顺延为 1.1.6。判断能力归属请用 FR / AB 编号，不要用版本号。
#    本版本与 1.1.6 零依赖、可完全并线开发、任意顺序发版。

# STATUS DEFINITIONS:
# ==================
# Epic Status:   backlog -> in-progress -> done
# Story Status:  backlog -> ready-for-dev -> in-progress -> review -> done
# Retrospective: optional <-> done
# Action Item:   open -> in-progress -> done
#
# WORKFLOW NOTES:
# ===============
# - Epic 首个 story 创建(create-story)时自动 -> in-progress
# - 每 story 三段推进：后端 -> 前端 -> 联调（CLAUDE.md 纪律）
# - 每条 AC 已标 L0(静态) / L1(集成) / L2(端到端) 层级
# - 云端 headless 只跑到 L0 绿灯；L1/L2 留本地，在 Completion Notes 标注
# - Dev 完成后转 review，再跑 code-review（建议新开 context）
#
# ===== 依赖链（全后向，无前向依赖 —— IR 已逐条验证） =====
# E1 拉黑闭环（含隐藏关系地基）
#    ├──> E2 举报闭环 ──> E3 运营后台 + 处置 + FR-51
#    └────────────┴──────────┴──> E4 埋点收尾
#
# - E1 完成即完整可用：用户能拉黑、能解除、能管黑名单，不依赖 E2 任何东西 —— **可独立上线**
# - E2 只是往 E1 建好的同一张表写一条 source=REPORT，六处过滤自动生效
# - ⚠️ E3 必须与 E2 同版本上线（PRD 硬约束）：AB-3D 不到位 = 用户能提交举报、运营无处处理，上线即空转
# - 跨 Epic 的两处组件交接（均为「预留 → 渲染」，不是返工）：
#     2-2 追加迷你卡「⋯」的举报项  <- 1-2 建菜单与拉黑项
#     2-4 渲染黑名单页行内「⋯」    <- 1-5 只预留布局、本身不渲染（否则是点开空菜单的死按钮）
#
# ===== Epic 内部顺序（刻意为之，勿调换） =====
# E1: 1-1 是唯一硬串行项（表 + 社区关系模块 + 只读端口 + 接口 + 首页过滤 + 主页拦截）
#     其后 1-3 / 1-4 两条后端 与 1-2 / 1-5 / 1-6 三条前端可并行铺开
#     （1-2 只需 1-1 的接口契约先定即可动工，不必等 1-1 全部完成）
#     ⚠️ 「三条前端可并行」指**实现**可并行，**验收有先后**：
#        1-6 的 AC4（三处入口一致性）里「已拉黑 / 网络失败」两分支由 1-2 AC3 交付，
#        1-6 单独验收只验 AC1~AC3，AC4 待 1-2 完成后回归（epics 已加时序说明，IR-9）
# E2: 2-1 后端地基（表 + 举报即隐藏同事务）-> 2-2 前端表单 -> 2-3 乐观移除 -> 2-4 黑名单页入口
#     2-2 需 1-2 的菜单已建；2-3 是 2-2 成功态的收尾行为（UI 稿画不出来，单独成 story）；
#     2-4 需 1-5 的行内「⋯」布局已预留
# E3: 3-1 统一工单视图（建 account_disposals + 三表联合 + 排序公式）-> 3-2 单条处置 -> 3-3 批量处置
#     3-1 在前：它是 3-2/3-3 的操作载体；建表在 3-1 是因为工单列表要**读**它（历史处置次数），
#              此时表为空、次数显示 0，属逻辑完整状态，**写入**由 3-2 接入
#     3-3 在 3-2 后：批量是单条的扩展（AC4 要求每条走 3-2 AC2 的「先写记录再触发后果」同一事务口径）
#     3-4 FR-51 文案变更与 3-1~3-3 无依赖，**可任意时点插入**
# E4: 4-1 排最后 —— 沿用 V1.1.2 既定做法，埋点在功能逻辑全部理清后集中实现，
#     避免边做边改、口径散在各处（四个入口的参数已在各自 story 预留）
#
# ===== 推荐执行顺序 =====
# 风险前置：1-1 既是唯一硬串行项，又承载三个最高风险点中的两个（R1 主页拦截只认主动拉黑、
#           安全规则层通用声明），必须最先做且做扎实。
# 0) ⚡ **与开发同时启动、不占开发工时**：OQ-A4 四条文案送法务/运营（详见下方开放问题）
# 1) 1-1（V101 + 模块 + 只读端口 + 拉黑/解除接口 + 首页过滤 + 主页拦截）
# 2) 1-3 / 1-4 后端并行；1-2 前端（1-1 契约定后即可动工）
# 3) 1-5 / 1-6 前端（1-6 的 AC4 待 1-2 完成后回归）
#    → 此时 **E1 完整可用，可独立上线**（拉黑闭环不依赖举报）
# 4) 2-1（V102）-> 2-2 -> 2-3 -> 2-4
# 5) 3-1（V103）-> 3-2 -> 3-3；3-4 任意时点插入
#    ⚠️ E3 与 E2 必须同版本上线，不得只发 App 端
# 6) 4-1 埋点收尾
# 7) 🚩 **上线前**：产品必须先量成功度量基线（4-1 AC7）—— 上线后才想起来就没有基线可对了
#
# ===== FLYWAY 号 =====
# 本分支实测已有 V100（v1.1.4/README 原记的「只到 V99」有误，架构 §4 已核实订正）
# 本版本从 V101 起单调顺延（占位 V<n>__，实际号按执行顺序分配 — 决策 E2）
#   V101  user_hide_relations                        <- 1-1  唯一键三元 (holder_id, target_id, source)
#   V102  account_reports + account_report_entries   <- 2-1  工单粒度 = 被举报账号，target_user_id 唯一
#   V103  account_disposals                          <- 3-1  警告 + 封号共用
# **三张全新表，零回填、零数据迁移**；既有 content_reports 一字不动（AD-9）
# ⚠️ 3-2 另需一个 DROP+ADD 迁移新增封号/警告通知 type（实证：NotificationType 无封号类，V1 封号不发站内通知），
#    **必须基于 V97 的完整 19 值追加**，否则重蹈 V72 覆盖审核线三值的覆辙
# ⚠️ 须回写 CROSS-STORY-DECISIONS.md「表归属总表」：追加这三张表及其创建 story（每张表恰好一个创建者）
#
# ===== 安全攸关节点（AC 含硬约束，不可被无监管流程降级/跳过） =====
# 架构 §3 列了五个「改动既有代码的高风险点」R1~R5，全部已逐条进 AC：
# - 1-1 AC7  【R1】主页拦截**只认 source=BLOCK**。写成「存在隐藏关系即拦」会一并把举报隐藏拦掉 ——
#            「已举报」状态无处显示、重复举报无入口，**FR-58 闭环当场作废**
# - 1-3 AC2  【R2】R2 影子评论判的是**内容作者**不是当前查看者。做成按 viewer 过滤 -> 第三方仍然看得到，
#            **等于没做**。验收必须三个视角（A / B / C）各看一遍
# - 1-4 AC3  【R3】通知抑制**判据有两条**：①接收者隐藏了 actor ②**该内容的作者隐藏了 actor**。
#            只做①会让未拉黑任何人的第三方收到点进去空白的通知，一对比就能推断出屏蔽机制存在
#            （PRD §3.2 第 6 条列的三种场景收通知的人全是拉黑发起方本人，第三方这一支是 PRD 的空白）
# - 1-2 AC3  【R4】迷你卡拉取失败**必须改原来那个 catch (_) { return; } 分支**，不是只加新分支
# - 1-5 AC8  【R5】**封号 ≠ 注销**，而代码里命名几乎一样、本版本要求**正好相反**：
#   3-2 AC6       封号（UserStatus.DEACTIVATED = 运营停用）-> 黑名单里**照常展示、不做标记、仍可被拉黑**
#                 注销（User.deleted / AuthorView.deleted）-> **匿名态 + 去点击态 + 条目保留**（NFR-8）
#                 ⚠️ 以中文定义为准，**不要凭字段名推断语义**
# 另有三条继承自 architecture Enforcement：
# - 1-1 AC6  主页拦截**必须服务端校验** —— 纯前端拦可被推送深链 / 通知中心历史 / 外部分享链接绕过
# - 1-1 AC9  隐藏过滤属**安全规则层，只升不降、不可绕过**；凡新增「展示他人内容」的位置默认套用，
#            不做逐场景例外（本版本平台尚无运营位与用户搜索，规则不触发，但**不得因此省略声明**）
# - 2-1 AC12 举报的「其他」补充说明是**用户自由文本，禁止进入任何日志**（日志禁 PII 红线）
#
# ===== 开工前 / 上线前须闭合的开放问题 =====
# - ✅ **OQ-A4 法务已闭环（2026-08-16，C-101）—— 本版本不再有上线阻塞项**
#      封号通知措辞经法务审核通过：「引导联系客服反馈异议」不构成承诺正式申诉结果的风险。
#      封号功能可随版本正常发布，3-2 AC9 已同步。
#      ⚠️ 仍有**两条印尼语待运营产出**（警告通知 · FR-51 改后的回告），**已降级为不阻塞发版的普通待办**，
#         上线前收口即可。（「提交后无法撤销」与「解除拉黑第二句」UI 稿已有草稿，非待办 ——
#         PRD A-A28 原写「四处待产出」不准确，实际两条，已订正）
# - ✅ **第三类工单的数据源已定案（2026-08-15，用户拍板「按产品意图来」）**
#      架构 AD-7 原认定第三类是 `manual_review_queue`，实证不成立：该表 `content_type` CHECK 只允许
#      CONTENT_POST / COMMENT，且 V51 迁移注释明写「不复用帖子 manual_review_queue」。
#      **按 PRD §4.1 产品意图，第三类 = 账号标识字段 = `name_moderation_records`(V50) + `avatar_reviews`(V51)。**
#      ⇒ 三个业务类别落在**四张表**上；`manual_review_queue` 不进本视图。
#      已同步：架构 AD-7 + AD-17 + §4 + 依赖图 · epics 3-1 AC2/AC6 · story 3-1 · OQ-CA1 重定 + 新增 OQ-CA2。
#      ⚠️ 联合时须处理第三类的三处不同构：① 只取 status='MANUAL_PENDING'（两表都有部分索引）
#         ② 终态映射（name 有 RESOLVED_PASS/RESOLVED_VIOLATION 两个、avatar 只有 RESOLVED 靠 verdict 区分）
#         ③ 时间锚点字段名不同（name=submitted_at / avatar=created_at），各自 AS 成同名列
#      ⚠️ PII 红线：name 的 submitted_value 与 avatar 的 avatar_url 可展示、**严禁进日志**（迁移注释均标明）
# - ✅ **OQ-CA1 已确认（2026-08-16，C-102）**：第三类工单优先级映射取 **HIGH -> 10 / NORMAL -> 2**，
#      锚点取自公式自身的两个例子。⚠️ NORMAL 不得映射成 0（默认值，映射成 0 会让这类工单几乎全部永远沉底）。
#      **规格已回写产品文档 PRD §4.1 正文**；架构 / epics / story 3-1 已同步。实现时抽成两个常量。
# - ✅ **OQ-CA2 已确认（2026-08-16，C-103）**：分类逻辑按「有没有对被审对象采取动作」不变
#      （违规终态 -> 已处理；通过终态 -> 第三档），但**第三档标签由「已驳回」改为中性词「无需处置」**
#      （Tidak Perlu Tindakan / No Action Needed）—— 「已驳回」只对举报成立（驳的是举报人的主张），
#      而第三类工单没有请求方，运营判「这昵称没问题」时驳的是谁？
#      ⚠️ **只改状态筛选档的标签，不改「驳回」这个动作按钮**（对举报而言那个词是准确的）。
#      ⚠️ **不新增第四档「已通过」** —— 举报类在该档永远为空，违反本版本自己定的「不保留空分类」。
#      **规格已回写产品文档 PRD §4.1 正文**；架构 / epics / story 3-1 已同步。
# - 印尼语文案：**2 条**尚无译文（警告通知 · FR-51 回告，见 OQ-A4）+ 约 25 条新写待运营复核措辞
#      —— 不阻塞开发，上线前收口。（原记「4 条」不准确，PRD A-A28 已订正）
# - IR-6 / IR-7 两条为 **epics 的暂行约定**（架构 delta 未覆盖）：
#      IR-6 social 取用户展示投影的路径与「禁直连用户侧仓储」-> 1-5 AC9
#      ~~IR-7 响应须含 total~~ -> **已由 IR-7R 撤销（2026-08-15 用户拍板）**：全仓无 total 先例，
#           小列表惯例是裸 List<T> 全量返回，total 与 items.length 冗余。计数由列表长度派生。
#           同批订正：端点统一为 /api/v1/me/blocked-users（原 /me/blocks）。架构 §3 端点表已同步。
#      **若架构 delta 后续补入对应 AD，以架构为准**（冲突序：架构 delta > epics）
# - ~~OQ-A5 隐藏关系的存储与读取方案~~ ✅ 已闭合（架构 AD-1 / AD-3 / AD-4，据代码实证）
# - ~~OQ-A6 Feed 分页是否需要产品层兜底~~ ✅ 已闭合（AD-5：实测是「过滤后凑够 N」，
#      **页恒满、不会出现空页，原前提不成立**，产品层无需任何兜底）
#
# ===== 已留痕的冲突裁决（CS 生成 story 时须抄进 Dev Notes） =====
# 1) **AD-9 不采纳 PRD §3.2 生效范围第 1 条**那句「实现上应合并为一次过滤，不要新起一层」：
#    既有「排除本人已举报的帖」子查询**原样不动**，账号级隐藏**另加一条**并列 AND。
#    ⚠️ 实现者若拿着 PRD 对照，会以为 1-1 AC5 做反了 —— 理由留痕在 AD-9 正文
# 2) **决策日志 C-50 已被 PRD 正文取代**：它建议「一对(举报人,账号)只存一行 + 次数累加」，
#    但 PRD 重复举报要求第 3 条要留存**每一次**的类型与说明，一行存不下 -> 架构 AD-2 取 PRD
# 3) **决策日志有 5 条已失效**（C-32 / C-33 / C-36 / C-64 / C-78），照旧行实现会做错。
#    失效清单见 v1.1.4/README.md §2 与决策日志正文顶部（两边一致）

generated: 2026-08-15
last_updated: 2026-08-16   # Story 3.1 实现完成，转 review
project: TailTopia V1.1.4
project_key: NOKEY
tracking_system: file-system
story_location: '{project-root}/_bmad-output/implementation-artifacts/v1.1.4'

development_status:
  # ===== Epic 1: 我能让某个人从我眼前彻底消失 =====
  # FR-94 全部 + NFR-8（黑名单与迷你卡的注销分支）。含隐藏关系地基（AD-1 / AD-8），E2 直接复用。
  # **完成即完整可用、可独立上线** —— 用户能拉黑、能解除、能管黑名单，不依赖 E2 任何东西。
  epic-1: in-progress
  1-1-拉黑一个人他的帖子立刻从我首页消失服务端: review          # 唯一硬串行项。V101 + social 模块 + 只读端口(须两种查询) + 拉黑/解除接口 + 首页过滤 + 主页拦截。含 R1 与安全规则层声明。AC8 已按 IR-1 拆为 AC8a/8b/8c（AD-20 单向屏蔽边界，须各自签字不得合并勾选）。✅ CS 阶段代码实证新增 AC10（内容详情页同样不可达），已回写 epics（IR-10）
  1-2-在他的卡片上点两下就能把他拉黑界面: review                # A 泳道 4 屏(A1/A1b/A4/A5) + C 泳道 4 屏(C1-C4)。含 R4「必须改原 catch 分支」。只接通拉黑项，举报项由 2-2 接入（在此之前不渲染，避免死菜单项）。IR-5 已补「本人的卡「⋯」整体不渲染」。⚠️ CS 阶段实证订正：UI 稿 A2 说明写的「菜单向下弹」与决策日志 C-76 相反，定案**向上弹**；另两条实现决策已定（showConfirmSheet 加可选 leading / 不改 toast 共享件）
  1-3-评论区里再也看不到他他也没法在我地盘上冲别人喊话: review         # 含 R2「R2 判内容作者非查看者」。四个评论查询方法全部已带 viewerId（架构实证），R1 基本是加一个条件；R2 需 service 传内容作者 id、不 join
  1-4-他来点赞评论我这边一点动静都没有: review                        # 含 R3「抑制判据有两条」。是「不发」不是「发了再隐藏」（未读角标不 +1）；系统通知一律不接入
  1-5-我能看到自己拉黑了谁随时放人出来: review                  # D 泳道 7 屏(D1-D7)+D7b+D8。含 R5「封号≠注销」。行内「⋯」只预留布局不渲染（随 2-4 出现）。IR-4 排序钉死 BLOCK.created_at / IR-6 新增 AC9 模块边界 / IR-8 原 AC0 改为不带编号的验收时序说明。✅ IR-7 已由 IR-7R 撤销、端点已统一为 blocked-users（epics/架构/三个 story 全部同步）。⚠️ 仍有一处待产品确认（不阻塞）：「已注销用户」文案 —— 既有键 feedDeletedUser="Pengguna terhapus" vs UI 稿 D8="Pengguna nonaktif"，建议复用既有键；若取 UI 稿说法则 Feed/详情/评论三处须一并改
  1-6-只在评论区骚扰从不发帖的人我也能就地处理: review                 # 本版本最大闭环缺口的补丁：影子评论/R1/R2/通知抑制全为评论区骚扰设计，而入口原先在评论区根本不存在。IR-9 已补验收时序说明（AC4 待 1-2 完成后回归）
  epic-1-retrospective: optional

  # ===== Epic 2: 我能把一个账号交给平台，并且立刻眼前清净 =====
  # FR-58 用户侧全部。复用 E1 建好的隐藏关系机制：只是往同一张表写一条 source=REPORT，
  # 六处过滤自动生效，**唯一例外是主页访问仍可进入**（1-1 AC7 已保证）。
  epic-2: in-progress
  2-1-举报一个账号他的东西立刻从我眼前消失: review                    # V102 两张表 + 举报即隐藏同事务 + 五类新枚举(禁复用 ReportReason) + 重复举报四条要求。⚠️ 不得照抄内容举报的「幂等吞掉」。IR-2 新增 AC11 服务端防重复提交(原 AC11 顺延 AC12) / IR-3 AC10 增「不设频率限制」/ IR-4 AC5 增「写 REPORT 行不得改 BLOCK 行」
  2-2-举报的每一步都清楚而且动手前就知道收不回来: review               # B 泳道 7 屏。需 1-2 的菜单已建（纯加一项，非返工）。「提交后无法撤销」须出现在**六个表单态全部**(B1-B4/B6/B7)，比 UI 稿待改清单多两个
  2-3-举报完抬头一看他的内容已经不在了: review                        # 「立即」兑现在当前这一屏：点关闭时移除该作者在当前列表的**全部**卡片（非只移一条）；详情页入口须**先退回上一级列表**再移除；**静默、不给任何提示**（提示会泄露「举报会隐藏内容」）。UI 稿画不出来，故单独成 story
  2-4-已经拉黑的人我照样能举报他: review                              # 需 1-5 的行内「⋯」布局已预留。存在理由：拉黑禁止进对方主页、而迷你主页是举报唯一入口，两条一叠 = 拉黑等于永久放弃举报该账号 —— 而骚扰者恰恰是最容易先被拉黑的那类
  epic-2-retrospective: optional

  # ===== Epic 3: 运营能在一个队列里把这些举报真正处理掉 =====
  # AB-3D 全部 + FR-58 处置侧 + FR-51 文案变更。
  # ⚠️ **硬约束：必须与 Epic 2 同版本上线**，否则用户能提交举报、运营无处处理，功能上线即空转。
  # ⚠️ 本 Epic 无 UI 稿（产品明确 AB-3D 不出图），却有 9 条 L2 级 AC 无视觉基准 ——
  #    建议 3-1 落地前先出一版低保真让产品过目，避免返工。
  epic-3: in-progress
  3-1-三类工单终于在一个队列里还知道先看哪条: review                  # V103 + 三表读时联合(不建索引表/不双写/存量零回填) + 优先级公式(AC 内嵌 PRD 四个算例) + 筛选检索 + 完全替代 AB-3A。AC6 含 OQ-CA1 的默认映射
  3-2-运营能给出警告或封号用户也真的收得到: ready-for-dev             # 含 R5 服务端侧。处置**必先落记录且同一事务**（否则会出现「用户登不上了但工单页显示从没处置过」）。AC9 是 🚩 OQ-A4 上线阻塞的落点
  3-3-一批同类的工单能一次处理完但不会一不小心封掉几百个人: ready-for-dev  # 四项边界：上限 50 / 跨类型置灰 / 批量封号须二次确认且逐条列出 / 部分失败逐条回报不整批回滚。逐条独立事务
  3-4-举报处理完用户收到的那句话终于统一了: ready-for-dev             # ⚠️ **独立交付项，不是文档订正**：改的是内容举报与账号举报**共用**的同一条通知，会连带改变**已上线的内容举报回告** —— 有意为之，AC3 要求必须作为独立可追溯改动出现，不得混在别的改动里悄悄改。与 3-1~3-3 无依赖，可任意时点插入
  epic-3-retrospective: optional

  # ===== Epic 4: 这一版的效果可度量 =====
  # FR-94 / FR-58 埋点要求 + PRD §4.3 成功度量所需数据。排最后（沿用 V1.1.2 做法）。
  # 上线后要能回答：举报量降了还是升了（这是本版本立项赌注的方向，两种结果都说得通）、
  # 主动拉黑的真实使用量、黑名单页那个举报入口到底有没有人用（长期为 0 就该撤掉）。
  epic-4: in-progress
  4-1-v114-埋点收尾: ready-for-dev                                    # 四个入口参数已在各自 story 预留(mini_profile->1-2 / blocklist->1-5,2-4 / comment->1-6 / report_flow->2-1)，本 story 统一收口与校验。⚠️ AC7「上线前必须先量基线」是**产品任务非代码任务**，漏了整节度量作废
  epic-4-retrospective: optional
