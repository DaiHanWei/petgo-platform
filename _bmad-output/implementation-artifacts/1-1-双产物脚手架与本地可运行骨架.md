---
baseline_commit: b5d76f4b92089cc8a6a20df0c01b2e01f36cf075
---

# Story 1.1: 双产物脚手架与本地可运行骨架

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a **开发者**,
I want **一套可运行的 Flutter 移动端 + Spring Boot 后端脚手架与本地编排**,
so that **团队能在统一约定（命名/分层/错误规范）上"改一行就跑起来"，为后续所有功能提供地基**。

> 这是整个实现阶段的**第一个 Story**（架构文档 §Implementation Handoff 明确指定）。本 Story 不实现任何业务功能（无登录、无 Feed、无分诊），只交付"双产物能本地跑起来 + 统一约定就位 + CI 绿灯"的地基。auth/triage 等业务从 Story 1.3 起在此骨架上叠加。

## Acceptance Criteria

> **验证层标注**：每条 AC 末尾标注验证层级与所需本地环境——
> **L0 静态**（编译/lint，无需 DB） · **L1 集成/运行时**（需 Docker daemon + postgres + redis） · **L2 端到端/外部凭证**（需真实第三方或真机模拟器）。
> 本 Story 全部 AC 落在 **L0 / L1**，无 L2——这是它适合作为首个自动化验收节点的原因（不依赖任何外部凭证）。

### AC1 — 双产物脚手架与目录骨架

**Given** 干净的开发环境
**When** 执行架构文档指定的两条 init 命令（`flutter create --org com.tailtopia --project-name petgo petgo_app` + Riverpod/go_router/dio/intl/flutter_lints；`spring init` Maven/SB4.0.x/Java21 含 web,validation,data-jpa,postgresql,data-redis,oauth2-resource-server,security,thymeleaf,actuator,flyway,lombok）
**Then** 生成的两套工程均能本地启动：`flutter run` 出空白首页、后端 `mvn spring-boot:run` 启动成功
**And** 后端按 8 模块 + shared 的 package-by-feature→layer 目录骨架建立，前端按 core/shared/features 骨架建立
> 验证层：**L0**（目录结构、`flutter analyze`、`mvn compile` 可纯静态验证）+ **L1**（`mvn spring-boot:run` 实际起进程需 postgres/redis 在跑，见 AC2；`flutter run` 需模拟器但出空白页不依赖后端）。

### AC2 — 本地编排、迁移基线、健康检查与错误/文档规范

**Given** 后端工程
**When** 启动 `docker-compose up`（app + PostgreSQL + Redis，德国单机配置占位）
**Then** Flyway 执行基线迁移成功、`GET /actuator/health` 返回 `UP`、Redis 连接就绪
**And** `@RestControllerAdvice` 全局异常处理统一输出 RFC 9457 ProblemDetail，springdoc 暴露 `/v3/api-docs`（OpenAPI 3.1）
> 验证层：**L1**（必须 Docker daemon + postgres + redis 真实在跑才能验 health=UP / Flyway 迁移 / Redis 就绪）。ProblemDetail 输出可用一个临时 `/api/v1/_ping-error` 端点或 MockMvc 测试在 **L0** 验证信封结构。

### AC3 — CI 骨架双绿

**Given** 代码仓库
**When** 推送触发 GitHub Actions CI 骨架
**Then** 后端 `mvn package` 出 jar、前端 `flutter build` 通过，CI 绿灯（部署步骤可占位）
> 验证层：**L0**（CI 里 `mvn package` 与 `flutter build` 均为编译/打包级，CI runner 用 service container 起 postgres 供测试阶段；部署步骤本 Story 仅占位 `echo`，不接德国机凭证 = 不引入 L2）。

---

## Tasks / Subtasks

> **按"后端子任务 / 前端子任务 / 联调验收"三段组织**（TailTopia 每个 Story 跨 Flutter + Spring Boot 双代码库，分段让 dev agent 一次只碰一侧、最后联调）。建议执行顺序：后端 → 前端 → 联调。

### 🟦 后端子任务（petgo-backend / Spring Boot）

- [x] **B1. 生成后端骨架** (AC: 1)
  - [x] 执行 `spring init`（见 Dev Notes「初始化命令」原文，**逐字使用**，勿改依赖清单/版本）。artifact-id=`petgo-backend`，package=`com.tailtopia`。
  - [x] 确认 `pom.xml`：Spring Boot `4.0.6`、Java `21`、Maven；依赖含 web,validation,data-jpa,postgresql,data-redis,oauth2-resource-server,security,thymeleaf,actuator,flyway,lombok。
- [x] **B2. 建立 8 模块 + shared 包骨架（空壳，不写业务）** (AC: 1)
  - [x] `com.tailtopia.<module>/{web,service,domain,repository,dto,event}`，module ∈ `auth,triage,consult,content,profile,notify,moderation,vet`（**8 个**，每个建空包 + 一个 `package-info.java` 占位即可，不建 Controller）。
  - [x] `com.tailtopia.shared/{config,security,error,async,media,im,ai,ratelimit}`（仅本 Story 需要的 `config`/`error` 落实现，其余建空包占位）。
  - [x] `src/main/resources/{db/migration, templates, safety}` 三个资源目录建好（`safety/` 留空待 Epic 4）。
- [x] **B3. Docker Compose 本地编排** (AC: 2)
  - [x] `docker-compose.yml`：`app`(Spring Boot) + `postgres` + `redis` 三服务；postgres 库名 `petgo`、用户/密码走 env；redis 无密码本地即可。
  - [x] `Dockerfile`（多阶段：`maven` 构建 → `eclipse-temurin:21-jre` 运行 jar）。
  - [x] `.env.example`：列出 `DB_*`、`REDIS_*` 占位 + 注释「OAuth/Gemini/IM/OSS 凭证后续 Story 才需要」。**真实凭证绝不入库**。
- [x] **B4. 配置 profiles 与 Spring Data 连接** (AC: 2)
  - [x] `application.yml` + `application-dev.yml` + `application-prod.yml`；datasource/redis 从 env 读取；`spring.jpa.hibernate.ddl-auto=validate`（**schema 由 Flyway 管，禁用 Hibernate 自动建表**）。
  - [x] Jackson 配置（`shared/config/JacksonConfig`）：`NON_NULL`（null 字段省略）、时间 ISO-8601 UTC 字符串、camelCase。
- [x] **B5. Flyway 基线迁移** (AC: 2)
  - [x] `src/main/resources/db/migration/V1__baseline.sql`——本 Story 不建业务表，放一张可验证迁移生效的最小表（如 `schema_meta(key varchar primary key, value varchar)` 插一行 `('baseline','1.1')`），或空 `flyway_schema_history` 基线。命名严格 `V<序号>__<描述>.sql`。
- [x] **B6. 全局错误规范（ProblemDetail RFC 9457）** (AC: 2)
  - [x] `shared/error/GlobalExceptionHandler`（`@RestControllerAdvice`）→ 所有异常转 `ProblemDetail`，字段含 type/title/status/detail/instance/traceId；**绝不外泄堆栈**。
  - [x] `shared/error/AppException` + `ErrorTypes`（type URI 常量）。
  - [x] HTTP 状态码语义就位（见 Dev Notes 状态码表）；提供最小验证手段（MockMvc 测试或临时受 `@Profile("dev")` 的 `/api/v1/_ping-error` 抛 AppException 验信封）。
- [x] **B7. OpenAPI 文档** (AC: 2)
  - [x] 加 `springdoc-openapi-starter-webmvc-ui`（最新 SB4 兼容版，见 Dev Notes 版本）；`shared/config/OpenApiConfig`；确认 `/v3/api-docs` 出 OpenAPI **3.1**、`/swagger-ui.html` 可访问。**不引入 HATEOAS**（规避 springdoc/Jackson3 兼容坑）。
- [x] **B8. Actuator 健康检查** (AC: 2)
  - [x] 暴露 `health`（+ 基础 `info`）；确认 `GET /actuator/health` 含 db & redis 组件且整体 `UP`。`/actuator/**` 仅内网语义（本地不强制鉴权，写注释标注 prod 由 CF/防火墙限制）。

### 🟩 前端子任务（petgo_app / Flutter）

- [x] **F1. 生成前端骨架** (AC: 1)
  - [x] 执行 `flutter create --org com.tailtopia --project-name petgo petgo_app`，随后 `flutter pub add`（见 Dev Notes「初始化命令」原文，**逐条照搬**：flutter_riverpod, riverpod_annotation, dev:riverpod_generator, dev:build_runner, dev:custom_lint, dev:riverpod_lint, intl, flutter_localizations(sdk), go_router, dev:flutter_lints）。
  - [x] portrait-only（`SystemChrome.setPreferredOrientations` 锁竖屏）；V1 仅浅色模式。
- [x] **F2. feature-first 目录骨架（空壳）** (AC: 1)
  - [x] `lib/core/{network,router,theme,l10n,storage}`、`lib/shared/{widgets,utils}`、`lib/features/<feature>/{data,domain,presentation}`（feature ∈ `auth,triage,consult,content,profile,notify,me,vet`——注意前端用 `me` 而非后端的 `moderation`，moderation 是后端 Admin slice 无前端 feature）。
  - [x] `lib/main.dart` + `lib/app.dart`：`ProviderScope` + `MaterialApp.router`（go_router 空路由表 → 一个空白 HomePage 占位）。
- [x] **F3. i18n 脚手架（id/en，Day-1）** (AC: 1; 支撑 NFR-11)
  - [x] `l10n.yaml`（`generate: true`）+ `lib/l10n/app_en.arb` / `app_id.arb`（各放 1 条占位 key 如 `appTitle`）。
  - [x] 确认 `flutter gen-l10n` 生成成功、`MaterialApp` 接入 `localizationsDelegates` + `supportedLocales`，跟随设备语言、其他语言回退 en。**无写死字符串**。
  - [x] ⚠️ 视觉 token / Tab Bar 凸起+弧形缺口属于 **Story 1.2**，本 Story 不做——此处只要"能切语言的空壳"。
- [x] **F4. 严格 lint** (AC: 1, 3)
  - [x] `analysis_options.yaml` 接入 `flutter_lints` + `riverpod_lint` + `custom_lint`；`flutter analyze` 零警告。

### 🟨 联调验收子任务（端到端跑起来 + CI）

- [x] **J1. 后端本地起栈验收** (AC: 2 / **L1**)
  - [x] `docker compose up -d` 起 postgres+redis → `mvn spring-boot:run` → 验：`curl localhost:8080/actuator/health` 返回 `{"status":"UP"}` 含 db/redis；启动日志显示 Flyway `V1` 迁移成功；`curl localhost:8080/v3/api-docs` 返回 OpenAPI 3.1 JSON。
  - [x] 触发一次错误验 ProblemDetail 信封（含 traceId、无堆栈）。
- [x] **J2. 前端本地起验收** (AC: 1 / L0+模拟器)
  - [x] `flutter run` 出空白首页且不崩；切换设备语言 id↔en，占位文案随之切换；`flutter analyze` 零警告。
- [x] **J3. CI 骨架双绿** (AC: 3 / **L0**)
  - [x] `petgo-backend/.github/workflows/ci.yml`：job 用 postgres+redis service container → `mvn -B package`（含测试）→ 上传 jar artifact；部署步骤占位（`echo "deploy placeholder"`）。
  - [x] `petgo_app/.github/workflows/ci.yml`：`flutter pub get` → `flutter analyze` → `flutter test`（若无测试则跳过）→ `flutter build apk --debug`。
  - [x] 推送后两个 workflow 均绿灯。
- [x] **J4. README 双产物起步说明**
  - [x] 根/各子项目 `README.md`：列本地起步命令（`docker compose up` / `mvn spring-boot:run` / `flutter run`）与 `.env` 配置说明。

---

## Dev Notes

### ⚠️ 本地环境前置（2026-06-01 探测，dev agent 必读）

执行前确认本机具备，否则对应 AC 无法验收：

| 工具 | 架构要求 | 本机现状 | 处置 |
|---|---|---|---|
| **Docker daemon** | 起 postgres+redis | 已装 v29，**daemon 常未启动** | 先 `open -a Docker` 等就绪，再做 L1 验收 |
| **Java** | **21 LTS**（pom 写死 21；2026-06-02 由 25 调整为 21） | 本机有 21（jenv 21.0.11） | `jenv local 21` 钉本项目。Boot 4 官方支持 Java 17+，21 完全兼容 |
| **Flutter** | 3.44.x / Dart 3.12 | **未安装** | 装 Flutter SDK + iOS 模拟器(Xcode) 或 Android SDK，否则前端 AC 全部跑不了 |
| Maven | 3.9 | ✅ 3.9.12 | — |
| Node | 工具链 | ✅ v20 | — |

仓库当前只有 `_bmad-output/`、`docs/`、`.claude/`；**没有任何 `pom.xml` / `pubspec.yaml` / `docker-compose.yml`**——本 Story 从零生成它们。两套工程放仓库根下：`petgo-backend/` 与 `petgo_app/`。

### 初始化命令（架构 §Selected Starter，逐字使用——勿擅改版本/依赖）

**后端：**
```bash
spring init \
  --type=maven-project \
  --boot-version=4.0.6 \
  --java-version=21 \
  --group-id=com.tailtopia \
  --artifact-id=petgo-backend \
  --name=petgo-backend \
  --package-name=com.tailtopia \
  --dependencies=web,validation,data-jpa,postgresql,data-redis,oauth2-resource-server,security,thymeleaf,actuator,flyway,lombok \
  petgo-backend
```
> 若无 Spring Boot CLI，用 start.spring.io 网页勾选**完全相同**的依赖与版本生成。

**前端：**
```bash
flutter create --org com.tailtopia --project-name petgo petgo_app
cd petgo_app
flutter pub add flutter_riverpod riverpod_annotation
flutter pub add dev:riverpod_generator dev:build_runner dev:custom_lint dev:riverpod_lint
flutter pub add intl
flutter pub add flutter_localizations --sdk=flutter
flutter pub add go_router
flutter pub add dev:flutter_lints
```

### 版本基线（架构已联网核实，2026-06；作为权威，dev agent 勿用旧版）

- Flutter **3.44.x** / Dart **3.12** · Spring Boot **4.0.6** · Java **21 LTS**。
- springdoc-openapi：取**最新 SB4 兼容版**（`springdoc-openapi-starter-webmvc-ui`）；**不用 HATEOAS**（规避 springdoc/Jackson3 已知坑）。
- ⚠️ 这些版本晚于 LLM 知识截止（2026-01）。**以架构文档与 `start.spring.io`/`pub.dev` 实际可解析版本为准**；若 `4.0.6` 在执行时不可用，取 4.0.x 最新 patch 并在 Completion Notes 记录实际版本，勿降级到 SB3/Java21。

### 关键架构约定（本 Story 必须落实，后续 Story 全程沿用）

**目录/分层**（架构 §Project Structure）：
- 后端 **package-by-feature → layer**：`com.tailtopia.<module>/{web,service,domain,repository,dto,event}` + `com.tailtopia.shared/{config,security,error,async,media,im,ai,ratelimit}`。
- 前端 **feature-first**：`lib/core/{network,router,theme,l10n,storage}` + `lib/shared/{widgets,utils}` + `lib/features/<feature>/{data,domain,presentation}`。
- 模块间只经 service 接口或领域事件通信，**禁跨模块直接访问对方 repository**；`shared/` 只放跨模块基础设施，不放业务。

**命名映射链**（核心，架构 §Naming）：DB `snake_case` ↔ Java/Dart `camelCase` ↔ JSON `camelCase`，由 JPA + Jackson 自动桥接。
- DB 表复数 snake_case；主键 `id`(bigint)；外键 `<单数>_id`；时间戳 `created_at`/`updated_at`(`timestamptz`, **一律 UTC**)；枚举落库 `varchar` + UPPER_SNAKE；Flyway 脚本 `V<序号>__<描述>.sql`。
- API：`/api/v1`，资源路径小写复数连字符（`/api/v1/pet-profiles`），查询参 camelCase，自定义头 `Idempotency-Key`/`Accept-Language`。
- Java：`XxxController/Service/Repository`、实体 `Xxx`、DTO `XxxRequest`/`XxxResponse`(record)、事件 `XxxEvent`(过去式)。
- Dart：文件 snake_case、类 PascalCase、provider `xxxProvider`、状态 `XxxState`(不可变 copyWith/freezed)。

**错误响应（统一 RFC 9457 ProblemDetail）** 示例：
```json
{ "type":"https://petgo/errors/validation", "title":"Validation Failed",
  "status":422, "detail":"...", "instance":"/api/v1/...", "traceId":"...",
  "errors":[{"field":"nickname","message":"≤20"}] }
```
**HTTP 状态码语义表**：200 读/改 · 201 创建 · 202 异步受理 · 400/422 校验 · 401 未认证 · 403 越权 · 404 不存在 · 409 冲突/重复 · 429 限流 · 5xx 服务端。

**成功响应格式**：不包通用 wrapper，直接返回资源；列表用游标分页 `{items,nextCursor,hasMore}`；camelCase；时间 ISO-8601 UTC；null 字段省略（Jackson NON_NULL）。

**日志**：SLF4J + logback **JSON**；MDC 带 traceId/userId；**严禁记录 PII/健康数据/令牌/签名 URL**。

### 强制护栏（架构 §Enforcement —— 违反即返工）

- 异步只用 `@Async` + DB 状态机，**禁止擅自引入 MQ / 通用缓存层 / 新中间件**（违背 V1 轻量姿态）。本 Story 不写异步，但**勿顺手加 Kafka/Caffeine/RabbitMQ**。
- `ddl-auto` 必须 `validate`（schema 归 Flyway），**禁用 `update`/`create`**。
- 新增对外暴露标识一律用不可枚举 token（本 Story 无对外标识，记住即可）。
- 凭证全部 env 注入，**绝不入库**。
- 红色态零变现 / 安全规则层"只升不降" —— 本 Story 不涉及，但写代码时勿埋违反点。

### 范围边界（防 scope creep —— 本 Story 明确不做）

- ❌ 不实现登录/OAuth/JWT（Story 1.3）——虽然 `oauth2-resource-server`/`security` 依赖已引入，本 Story 只让它们存在且不拦截，**SecurityConfig 先放行全部**（或 permitAll），避免脚手架阶段被 401 挡住 health/docs。
- ❌ 不做设计 token / Tab Bar 视觉 / 凸起+弧形缺口（Story 1.2）。
- ❌ 不做任何业务表（users 在 1.3 建、pet_profiles 在 2.2 建……）。
- ❌ 不接 Cloudflare/德国机真实部署（CI 部署步骤仅占位）。
- ✅ 只做：两套工程生成 + 目录骨架 + 本地编排起栈 + Flyway 基线 + ProblemDetail + OpenAPI + Actuator + i18n 空壳 + CI 双绿。

### Project Structure Notes

- 双产物并列仓库根：`petgo-backend/`、`petgo_app/`。与架构 §Complete Project Directory Structure 一致。
- 前端 feature 命名差异：架构前端树用 `features/me/`（个人中心），无 `moderation` feature（moderation 是后端 Admin slice）。后端建 8 模块含 `moderation`+`vet`；前端建 8 feature 含 `me`+`vet`，无 `moderation`。
- `vet` 在前后端都要建空壳（Epic 5 才填充）。

### References

- [Source: architecture.md#Selected Starter A/B] — 两条 init 命令（逐字）。
- [Source: architecture.md#版本基线] — Flutter 3.44/Dart 3.12/SB 4.0.6/Java 21 LTS。
- [Source: architecture.md#Implementation Patterns & Consistency Rules] — 命名/结构/格式/通信/过程五类约定 + 正反例。
- [Source: architecture.md#Complete Project Directory Structure] — 后端 8 模块+shared / 前端 feature-first 完整树。
- [Source: architecture.md#API & Communication Patterns] — `/api/v1`、ProblemDetail、springdoc 3.1、不用 HATEOAS。
- [Source: architecture.md#Infrastructure & Deployment] — Docker Compose(app+pg+redis)、profiles、Actuator、logback JSON。
- [Source: architecture.md#Enforcement Guidelines] — 禁 MQ/缓存层、ddl-auto=validate 意涵、env 注入。
- [Source: epics.md#Story 1.1] — 三条原始 AC（Given/When/Then）。
- [Source: epics.md#Epic 1] — Epic 目标与本 Story 在 1.2~1.7 中的定位（1.1 只做地基，登录/引导/门控在 1.3~1.7）。
- [Memory: story-acceptance-layering] — 前后端分段 + AC 标 L0/L1/L2 的本项目约定来源。

## Dev Agent Record

### Agent Model Used

claude-opus-4-8 (1M context) — 2026-06-02

### Debug Log References

- 后端启动一度失败：`spring.jackson.serialization.write-dates-as-timestamps` 在 Jackson 3（Boot 4）下绑定失败——该特性已从 `SerializationFeature` 移至 `DateTimeFeature`，且 Jackson 3 默认即 ISO-8601 字符串输出。处置：删除该行（见下 D2）。
- `flutter build apk --debug` 本地失败：`zip END header not found`——Gradle 发行包下载被本机受限网络截断（127MB/~230MB），非代码缺陷（见下 D5）。

### Completion Notes List

**实际使用版本：** Spring Boot **4.0.6**、Java **21 LTS**（jenv `.java-version=21`；初版曾用 25.0.3，2026-06-02 决策降为 21，见 Change Log）、Maven 3.9.16(wrapper)、Flutter **3.44.1**(stable)/Dart 3.12、springdoc-openapi **3.0.3**(Boot4/Spring7 兼容线)、Jackson **3.1.2**、analyzer 12.1.0。

**验收结果：**
- **L0**：后端 `mvn compile`/`package` 出可执行 jar（81MB）；`GlobalExceptionHandlerTest`(standalone MockMvc，ProblemDetail 信封)通过；前端 `flutter analyze` 零警告、`flutter test` 冒烟通过。
- **L1**（docker compose postgres+redis + `mvn spring-boot:run`，实测）：`/actuator/health`=**UP** 含 db(PostgreSQL)+redis(7.4.9)；Flyway **V1__baseline** 迁移成功（`schema_meta.baseline=1.1`、history success=t）；`/v3/api-docs`=**OpenAPI 3.1.0**；`/api/v1/_ping-error`→HTTP **422** `application/problem+json`，type/title/status/detail/instance/traceId 齐备、无堆栈。`TailtopiaBackendApplicationTests` contextLoads 在 DB 起栈下通过。
- **L2**（前端视觉 `flutter run` + 切语言、apk 真机/CI 构建）：**留待本地模拟器视觉验收 + CI**，见下。

**与 story 的偏差（均有据，已落实）：**
- **D1 — 依赖路由**：本机全局 `~/.m2/settings.xml` 强制走私有 Nexus（当前 502 宕机）。新增项目级 `petgo-backend/.mvn/settings.xml`（直连 Maven Central）+ `.mvn/maven.config`（`-s` 自动生效），不动全局配置、不影响其它项目。
- **D2 — JacksonConfig 取消**：Jackson 3 移除了 `Jackson2ObjectMapperBuilderCustomizer`，自定义 bean 易踩 Jackson3 builder API 坑且与 `spring.jackson.*` 冗余。三条规则（NON_NULL / ISO-8601 UTC / camelCase）收口到 `application.yml`（Boot 的 `StandardJsonMapperBuilderCustomizer` 自动应用），删除 `shared/config/JacksonConfig.java`。
- **D3 — `spring init` 用 start.spring.io API**：本机无 spring CLI，按 Dev Notes 指引用 `start.spring.io/starter.zip` 生成；其返回的 `bootVersion=4.0.6.RELEASE` 为元数据 id，实际 Maven artifact 为 `4.0.6`（已修正 pom）。
- **D4 — `riverpod_lint` + `custom_lint` 暂缓**：`custom_lint` 最高仅支持 analyzer ^8，而 Flutter 3.44 / riverpod 3.x 实际解析到 analyzer 12 → 生态当前无解。保留 `flutter_lints` 强基线，`analysis_options.yaml` 留 TODO，待生态跟进。`riverpod_generator`+`build_runner` 已装。
- **D5 — CI 工作流置于仓库根**：story 写的 `petgo-backend/.github/...`、`petgo_app/.github/...` GitHub Actions **不识别**（monorepo 工作流必须在根 `.github/workflows/`）。改为根目录 `backend-ci.yml` + `frontend-ci.yml` + `paths:` 过滤，行为等价。
- **D6 — Gradle 发行包改 `-bin`**：`flutter create` 默认 `-all`（含源码/文档，~230MB），改 `-bin` 更小更快；本机网络仍截断（环境问题），CI 干净网络正常。
- **D7 — 生成的 l10n 代码不入库**：`lib/l10n/app_localizations*.dart` 由 `flutter gen-l10n` 现生成（已加入 SessionStart hook 与前端 CI），`.gitignore` 忽略，仅 `.arb` 源入库。

**待后续验收（非阻塞）：**
1. 前端视觉验收：本地模拟器 `flutter run`，确认空白首页渲染 + 设备语言 id↔en 切换。
2. CI 双绿：首次 push 后确认 `backend-ci` / `frontend-ci` 两个 workflow 绿灯（apk 构建在 CI 干净网络验证）。

### File List

> 共约 250 个文件（含 Flutter 多平台脚手架）。下列为本 Story 关键交付，非脚手架自动生成的样板。

**仓库根**
- `.github/workflows/backend-ci.yml`、`.github/workflows/frontend-ci.yml`（新增，D5）
- `.java-version`（jenv 钉 JDK 21）
- `.gitignore`、`README.md`（修改）

**petgo-backend（Spring Boot 4 / Java 21）**
- `pom.xml`（Boot 4.0.6 / Java 21 + springdoc 3.0.3）
- `.mvn/settings.xml` + `.mvn/maven.config`（项目级 Central 路由，D1）
- `src/main/resources/application.yml` + `application-dev.yml` + `application-prod.yml`
- `src/main/resources/db/migration/V1__baseline.sql`
- `src/main/java/com/tailtopia/shared/error/{GlobalExceptionHandler,AppException,ErrorTypes,PingErrorController}.java`
- `src/main/java/com/tailtopia/shared/security/SecurityConfig.java`
- `src/main/java/com/tailtopia/shared/config/OpenApiConfig.java`
- 8 模块 × {web,service,domain,repository,dto,event} + shared × 8 的 `package-info.java`（56 个）
- `src/test/java/com/tailtopia/shared/error/GlobalExceptionHandlerTest.java`
- `docker-compose.yml`、`Dockerfile`、`.env.example`、`README.md`

**petgo_app（Flutter 3.44）**
- `lib/main.dart`、`lib/app.dart`
- `lib/core/router/app_router.dart`、`lib/core/theme/app_theme.dart`
- `lib/shared/widgets/home_page.dart`
- `lib/l10n/app_en.arb`、`lib/l10n/app_id.arb`、`l10n.yaml`、`pubspec.yaml`(generate:true)
- `analysis_options.yaml`、`test/widget_test.dart`
- feature-first 目录骨架 `lib/features/{auth,triage,consult,content,profile,notify,me,vet}/{data,domain,presentation}`
- `android/gradle/wrapper/gradle-wrapper.properties`（`-bin`，D6）

## Change Log

| 日期 | 变更 | 作者 |
|---|---|---|
| 2026-06-02 | Story 1.1 实现：双产物脚手架（Spring Boot 4/Java 21 + Flutter 3.44）、本地编排、Flyway 基线、RFC 9457 ProblemDetail、OpenAPI 3.1、Actuator、i18n 空壳、CI 骨架；L0+L1 实测通过。偏差 D1–D7 见 Completion Notes。Status → review。 | Amelia (dev agent) |
| 2026-06-02 | **Java 基线 25 → 21**：经核查全项目无任何功能需求依赖 Java 22~25；改 21 因云端/CI 自带 21、生态成熟、Temurin 21 LTS 至 ~2029、消除前沿摩擦（云端 JDK 安装/JAVA_HOME/TLS 等坑），契合 V1 轻量姿态。改动 pom/Dockerfile/.java-version/CI/CLAUDE.md/README/architecture/epics/2-1/2-6 + 云端 setup script；本地 Java 21 实测 `mvn package` BUILD SUCCESS（contextLoads + ProblemDetail 测试通过）。 | Dai（决策）+ dev agent |
