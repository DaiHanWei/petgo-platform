---
baseline_commit: 071247d9
epic: 1
story: 1.3
version: V1.1.4
source_epics: _bmad-output/planning-artifacts/epics-v1.1.4.md
architecture: _bmad-output/planning-artifacts/architecture-v1.1.4-delta.md
depends_on: Story 1.1（表 + 只读端口）
---

# Story 1.3: 评论区里再也看不到他，他也没法在我地盘上冲别人喊话

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

> **所属**：V1.1.4 Epic 1 第三个 Story（**纯后端**）。可与 Story 1.4 并行，两者都只依赖 Story 1.1 的表与端口。
> **交付**：评论查询的两条过滤 **R1（按查看者）+ R2（影子评论，按内容作者）** + 评论数口径。
> ⚠️ **本 story 含高风险点 R2**：判据是「**内容作者**」不是「当前查看者」。做成按 viewer 过滤 → 第三方仍然看得到，**等于没做**。
> ⚠️ **本 story 有三处「写法差一点就全错」**，见 Dev Notes 开头。R1 与 R2 的「作者自视豁免」**处理方式不一样**，这是最容易做反的地方。

## Story

As a **拉黑了某人的用户**,
I want **他的评论从我看到的所有评论区消失，而且他在我内容下的评论对所有人都不显示**,
so that **我不用再看见他，也不会出现「全平台都看得见、唯独我看不见因而删不掉」的攻击性评论**。

## Acceptance Criteria

> **验证层**：**L0**（`mvn -B package`）· **L1**（Docker postgres 真跑）。本 story 无 L2。
> 云端只跑 L0；**L1 留本地**，在 Completion Notes 标注。

### AC1 — R1 按查看者隐藏（L1）

**Given** 查看者 A 已隐藏 B（**主动拉黑或举报，两种来源都算**，不区分 `source`）
**When** A 请求任何内容下的评论列表
**Then** B 的评论**对 A 不展示**
**And** 隐藏方式是**直接不展示该条**，不占位、不显示「该内容已隐藏」占位符（A-A5）
**And** 对其他人的可见性**无影响**
**And** 该条件写在**四个评论查询方法**的 JPQL 内：`findTopLevel` / `findReplies` / `findRepliesForParents` / `countVisibleForViewer`
**And** ⚠️ 新条件与既有的「作者自视」括号**并列 `AND`**，**绝不塞进那个 `OR` 括号里**（见 Dev Notes 陷阱 ①）

### AC2 — ⚠️ R2 影子评论判的是内容作者（高风险点 R2）（L1）

**Given** 内容作者 A 已隐藏 B，第三方 C 与谁都无隐藏关系
**When** B 在 A 的内容下发表评论
**Then** **A 看不到、C 也看不到**，**只有 B 自己看得到**
**And** R2 的判据是 `holder = 该内容的作者`，**不是 `holder = 当前查看者`**
**And** 内容作者 id **由 service 作为参数传入查询**，不在 SQL 里 join 内容表（AD-3）
**And** R1 与 R2 两条件**独立并列**，任一命中即隐藏

> ⚠️ 做成按查看者过滤 → 第三方仍然看得到，**等于没做**。验收必须**三个视角各看一遍**（A / B / C）。

### AC3 — 影子评论对评论者本人无感知（L1）

**Given** B 的评论已被 R2 影子
**When** B 自己查看该内容的评论区
**Then** 他看到**自己那条评论正常展示**，与发布成功的状态完全一致
**And** **无任何失败提示、无视觉差异、无标记**
**And** B **能看到他人对他那条的回复**

> ⚠️ **这一条决定了 R2 的 SQL 写法与 R1 不同**：R2 的 `NOT EXISTS` 必须**放过 `c.authorId = :viewerId` 的行**（即评论作者本人自视豁免），而 R1 不需要这层豁免（R1 是「我不看」，作者本人天然不受影响）。**两条的豁免处理不对称，是本 story 最容易做反的点。** 见 Dev Notes 陷阱 ②。

### AC4 — 回复串随父一并隐藏（L1）

**Given** B 的一条评论被 R2 影子，C 回复了 B 那条
**When** A 或第三方查看评论区
**Then** **B 那条与 C 的回复一并不展示**，不论回复者是谁
**And** R1 同理：被 R1 隐藏的评论，其回复串对该查看者一并隐藏
**And** 不出现「回复了某条看不见的评论」的孤儿回复

### AC5 — 评论数口径（L1）

**Given** 某内容下有 5 条评论，其中 1 条被 R2 影子
**When** 第三方查看该内容
**Then** 对外展示的评论数为 **4**，与实际渲染出的条数一致
**And** 被 R2 影子的评论**及其回复串不计入对外评论数与互动量统计**
**And** **被 R1 隐藏的评论照常计入评论数**（R1 只是「我不看」，该评论对平台真实存在且公开可见）
**And** 评论作者本人视角下数字比他人多 1（影子机制固有特性，可接受）
**And** ⚠️ **两条计数口径都要处理**：详情页的 `commentCount`（`countVisibleForViewer`）与一级评论的 `replyCount`（`findRepliesForParents` 的返回条数）—— 见 Dev Notes「两条独立的计数口径」
**And** ⚠️ **运营看板的 `countByRealAuthor()` 不套 R1/R2**（那是平台口径，与 viewer 无关）

### AC6 — 被拉黑方仍可「评论成功」（L1）

**Given** B 被 A 拉黑
**When** B 在 A 的内容下提交评论
**Then** 接口**返回成功**，界面表现与正常评论完全一致
**And** 评论**正常落库**，只是读取时被 R2 过滤
**And** 写路径（`CommentService`）**一行不改**

### AC7 — 下游口径同步（L0）

**Given** 本 story 确立的评论数口径
**When** 后续任何依赖评论数或互动量的功能被开发
**Then** 该口径已在代码注释与 `CROSS-STORY-DECISIONS.md` 中留痕，避免影子评论被当作真实互动信号

### AC8 — 不做的（L0）

**Given** 本 story 范围
**Then** **不做** Instagram Restrict 的「查看 / 批准」二次操作——内容作者不需要能看到、也不需要能放行被影子的评论
**And** **不新增任何 DTO 字段** —— R1/R2 是纯隐藏（行不返回）。⚠️ 契约金标测试用 `isEqualTo(Set.of(...))` 精确锁死了 `CommentResponse` 的 10 个键与 `ContentDetailResponse` 的 13 个键，**加字段会立刻变红且需三方同步**

### AC9 — 护栏（L0）

**Given** 本 story 的实现
**Then** 过滤**全部写在 JPQL `WHERE` 内**，禁止取回后在 Java 侧 filter（AD-5）
**And** 未引入 MQ / 缓存 / 新中间件
**And** 日志中不出现评论正文等 PII
**And** `mvn -B package` 全绿

---

## Tasks / Subtasks

### 🟦 后端子任务（petgo-backend）

- [x] **T1 · 改 `CommentQueryService`，取出 `postAuthorId`**（AC2）
  - [x] ⚠️ **`requireVisiblePost` 目前是 `private void`，把查出的 `ContentPost` 直接扔了**（`.orElseThrow(...)` 的返回值没接）
        ➜ 改签名为 `private ContentPost requireVisiblePost(long postId)` 并 `return`，**零额外查询**
  - [x] ⚠️ **`replies(parentId, cursor, viewerId)` 分支当前完全没有 post / parent 查询** —— 它拿到 `parentId` 直接调 `findReplies`
        ➜ **需新增查询拿 `postAuthorId`**：`comments.findById(parentId)` 取 `postId` → `posts.findById(postId)` 取 `authorId`（或新增一个按 parentId 直取 postAuthorId 的投影查询，减少一次往返）
        ⚠️ **架构 AD-3 写的「`replies(parentId)` 分支经 `Comment.postId` 反查一次」是「要新增的动作」，不是「已现成的」** —— 见 Dev Notes「对架构 AD-3 的一处订正」
  - [x] 注入 `social.read` 只读端口（AD-8）—— ⚠️ 但**过滤本身走 JPQL，不用端口**，见 T2 说明

- [x] **T2 · 四个查询方法加 R1 / R2**（AC1 / AC2 / AC4 / AC5）
  - [x] `findTopLevel` / `findReplies` / `findRepliesForParents` / `countVisibleForViewer` 各加两条 `NOT EXISTS`
  - [x] ⚠️ **与既有的「作者自视」`OR` 括号并列 `AND`，不要塞进括号内**（陷阱 ①）
  - [x] ⚠️ **R2 要放过 `c.authorId = :viewerId`（作者本人自视豁免），R1 不放**（陷阱 ②）
  - [x] 游客分支照抄 `findFeed` 的 **`:hasViewer` 布尔标志门控**，不用裸 `:viewerId IS NULL`（42P18 陷阱）
  - [x] 子查询写**实体名** `UserHideRelation h`，不写表名 —— 与 `findFeed` 引用 `ContentReport r` 的既有做法一致
  - [x] **回复串随父隐藏**（AC4）：`findRepliesForParents` / `findReplies` 的过滤须同时作用于「回复自身的作者」与「父评论的作者」，或在 service 层裁掉父被隐藏的整串。**实现方式由 dev 定，但两条路径都要覆盖**

- [x] **T3 · 两条计数口径**（AC5）
  - [x] `countVisibleForViewer`（详情页 `commentCount`）套 R1 + R2
  - [x] `replyCount`（`findRepliesForParents` 返回条数在 service 里 `replies.size()`）**天然随过滤生效** —— 但要验证
  - [x] ⚠️ **`countByRealAuthor()` 不动**（运营看板平台口径）

- [x] **T4 · 修既有测试**（AC9）
  - [x] `CommentQueryServiceTest`：**4 处 `when(...)` stub + 1 处构造器**会编译失败（见 Dev Notes 清单）
  - [x] `ContentDetailServiceTest`：**2 处**（`countVisibleForViewer` stub + 可能的构造器）
  - [x] 契约金标 `ContentDetailAndCommentContractTest` **应保持绿**（不加字段）

- [x] **T5 · 新增测试**（AC1–AC6）
  - [x] ⚠️ **仓库里没有任何测试覆盖 viewer 维度的可见性过滤**（评论侧为零）—— 全部从零补
  - [x] R1 命中 / R2 命中 / 两者叠加
  - [x] **被 R2 影子的作者本人仍可自视**（AC3，最易做反）
  - [x] **三视角验证**（A 内容作者 / B 被隐藏者 / C 第三方）
  - [x] 回复串随父隐藏
  - [x] 计数口径：R2 不计入、R1 计入
  - [x] 被拉黑方评论**接口仍返回成功**（AC6）

- [x] **T6 · 口径留痕**（AC7）
  - [x] 代码注释写明「R2 影子评论不计入对外评论数与互动量」
  - [x] 回写 `CROSS-STORY-DECISIONS.md`

---

## Dev Notes

> ⚠️ 本节代码事实来自 CS 阶段对 `petgo-backend` 的实证扫描。

### 🔴 三处「写法差一点就全错」

#### 陷阱 ① — 新条件塞错位置，会让「作者本人」绕过拉黑过滤

既有过滤是一条 **`OR`（放行式）** 条件，四条 JPQL 里**逐字相同**：

```sql
AND (c.moderationStatus = com.tailtopia.content.domain.CommentModerationStatus.VISIBLE
     OR (:viewerId IS NOT NULL AND c.authorId = :viewerId))
```

语义是「**公开可见 或 是我自己写的**」。

**➜ 新的 R1/R2 必须写成与整个括号并列的 `AND NOT EXISTS`：**

```
AND ( 既有的 VISIBLE OR 自己 括号 )      ← 原样不动
AND NOT EXISTS ( R1 … holder = :viewerId … )
AND NOT EXISTS ( R2 … holder = :postAuthorId … )
```

**一旦塞进那个 `OR` 括号里面，「作者本人」这条 OR 支路就会绕过拉黑过滤** —— 编译通过、大部分场景也正常，只在「被隐藏者恰好是评论作者本人」时暴露。

#### 陷阱 ② — R1 与 R2 的「作者自视豁免」处理**不一样**

| | R1 | R2 |
|---|---|---|
| 判据 | `holder = :viewerId` | `holder = :postAuthorId` |
| 语义 | 「我不看」 | 「让我的地盘干净」 |
| **评论作者本人自视** | **不需要豁免** —— 我不会拉黑我自己，天然不受影响 | ⚠️ **必须豁免** —— 被 R2 影子的人**要能看见自己那条**（AC3「无感知」的前提） |

**➜ R2 的 `NOT EXISTS` 需要放过 `c.authorId = :viewerId` 的行。**
写成对称的两条 `NOT EXISTS` 会让被影子者自己也看不见自己的评论 → **直接破坏「无感知」，对方立刻知道被屏蔽了**。

#### 陷阱 ③ — 42P18：游客分支不能裸判空

照抄 `findFeed` 的既有形状（`ContentPostRepository`）：

```java
AND (:hasViewer = false
     OR NOT EXISTS (SELECT 1 FROM ContentReport r
                    WHERE r.postId = p.id AND r.reporterId = :viewerId))
```

方法 Javadoc 写明原因：裸 `:x IS NULL` 会让 PG 推不出 NULL 参数类型（**42P18 could not determine data type**）。

⚠️ 注意评论侧既有条件用的是 `:viewerId IS NOT NULL`（不是裸 `IS NULL`），它能定型是因为紧接着与 `c.authorId`（bigint 列）比较。**新增的 `NOT EXISTS` 若把 `:viewerId` 放在没有列比较的位置，仍会踩 42P18** —— 稳妥做法是沿用 `:hasViewer` 布尔标志。

---

### 📌 对架构 AD-3 的一处订正（实证）

> **AD-3 原文**：「`:postAuthorId` 由 service 传入：`CommentQueryService.requireVisiblePost(postId)` 已 `findById(postId)`，作者是现成的；`replies(parentId)` 分支经 `Comment.postId` 反查一次。」

| 半句 | 实证结论 |
|---|---|
| 「`requireVisiblePost` 已 `findById(postId)`，作者是现成的」 | **半真**。确实调了 `posts.findById(postId)`，但它是 **`private void`，把查出的 `ContentPost` 直接扔了**（`.orElseThrow(...)` 返回值没接）。要拿 `authorId` 必须改签名返回 —— **一行改动、零额外查询**。 |
| 「`replies(parentId)` 分支经 `Comment.postId` 反查一次」 | **目前不成立**。`replies(parentId, cursor, viewerId)` **完全没有任何 post / parent 查询**，拿到 `parentId` 直接调 `comments.findReplies(...)`。这是**要新增的动作**，不是「已有的」。该分支要**新增 1~2 次查询**才拿得到 `postAuthorId`。 |

**➜ 写工时评估时不要照抄「已现成」的措辞。** `topLevel` 分支几乎零成本，`replies` 分支要新增查询。

**顺带发现的既有缺口（范围外，需 PO 确认）**：`replies()` **不校验帖子可见性** —— 帖被下架/软删后，只要知道 `parentId` 仍能拉到回复。若为 R2 加了反查，可以顺手补齐，但这属于**既有行为变更**，不在本 story 范围内，**不要擅自修**。

---

### 📌 `content` 在 JPQL 里引用别的模块的实体：已有先例，不违反 AD-8

表面上 AD-8 说「`content` 只依赖 `social.read` 端口、禁止直读其仓储」，而 AD-5 说「过滤必须写进 SQL WHERE」——两者看似冲突。

**实证：既有代码已经给出了答案。** `ContentPostRepository.findFeed` 的 JPQL 里已经跨模块引用了 `moderation` 的实体：

```sql
AND (:hasViewer = false
     OR NOT EXISTS (SELECT 1 FROM ContentReport r WHERE r.postId = p.id AND r.reporterId = :viewerId))
```

**Java 侧不 import，只在 JPQL 字符串里写实体名。** 这是本仓既定破例。

**➜ R1/R2 照此执行**：JPQL 里写 `UserHideRelation h`，Java 侧不 import `social` 的仓储。
⚠️ **不要为了「不跨模块」而去做 Java 侧二次过滤** —— 那会直接违反 AD-5，并破坏分页契约。

---

### 两条独立的计数口径，都要同步（AC5）

| 口径 | 来源 | 展示位置 |
|---|---|---|
| `commentCount` | `countVisibleForViewer(postId, viewerId)` | **仅详情页两处**：`content_detail_page.dart:135`（「评论 (N)」小节标题）与 `:237`（底部互动条） |
| `replyCount` | `findRepliesForParents` 返回条数，service 里 `replies.size()` | 一级评论下的「查看全部 X 条回复」 |

⚠️ **两处都必须同步套 R1/R2**，否则会出现「显示 5 条评论却只数得出 4 条」的穿帮。
✅ **好消息**：`findRepliesForParents` **无分页**（一次取全部，service 端裁前 3），所以 R2 过滤写进它的 WHERE 后，`replies.size()` **天然就是过滤后的 replyCount** —— 正好满足 AD-13。

**Feed 卡片没有评论数** —— `FeedItemResponse` 的类注释明写「PRD-642：点赞数显示在内容卡片上；**commentCount 仍不在卡片**」。所以本 story 不涉及 Feed 卡片。

⚠️ **`countByRealAuthor()` 不套 R1/R2** —— 它服务运营看板总量统计（`AdminDashboardService`），是**平台口径**、与 viewer 无关。

---

### 四个查询方法的现状（全部已带 `viewerId`）

| 方法 | 已带 `viewerId`？ | 类型 | 备注 |
|---|---|---|---|
| `countVisibleForViewer(long postId, Long viewerId)` | **是**（第 2 参） | JPQL | 详情页 commentCount |
| `findTopLevel(postId, hasCursor, cursorTs, cursorId, viewerId, Pageable)` | **是**（第 5 参） | JPQL | 一级评论，时间**正序**游标 |
| `findReplies(parentId, hasCursor, cursorTs, cursorId, viewerId, Pageable)` | **是**（第 5 参） | JPQL | 二级回复展开 |
| `findRepliesForParents(List<Long> parentIds, Long viewerId)` | **是**（第 2 参） | JPQL | **无游标无 Pageable**，一次取全 |

**四条全是 JPQL，无一条原生 SQL。** `viewerId` 当前**唯一**用途是审核态自视，**与拉黑无关** —— R1 是在这个既有条件**旁边**再并列一条，不是复用它。

---

### `comments` 表结构

**建表** `V9__init_comments.sql`：`id BIGSERIAL PK` / `post_id` / `parent_id`（**自引用 FK**，NULL=一级、非空=二级）/ `author_id` / `body VARCHAR(1000)` / `deleted_at` / `created_at` / `updated_at`。

**三次 ALTER**：`V49` 加 `moderation_status`（默认 VISIBLE）+ `content_version`；`V56` 扩 CHECK 容纳 `AUTHOR_DEACTIVATED`；`V94` 把 `moderation_status` 加宽到 `VARCHAR(32)`（`AUTHOR_DEACTIVATED` 18 字符溢出 varchar(16) 的线上 bug 20260724-360）。

**三级由 service 层强制归并**，DB 无约束阻止。

**索引**：`idx_comments_post_toplevel (post_id, created_at, id) WHERE parent_id IS NULL` · `idx_comments_parent (parent_id, created_at, id)` · `idx_comments_moderation (post_id, moderation_status)`。

⚠️ **`author_id` 上无索引**（PG 不为 FK 自动建索引）。但 R1/R2 的 `NOT EXISTS` 是拿 `comments.author_id` 当参数去打**新隐藏关系表**的索引，所以**关键索引在新表上**（Story 1.1 已建 `idx_user_hide_relations_holder_target`），`comments.author_id` 侧**无需新增索引**。

**本 story 无 Flyway 迁移** —— 不改表结构。

---

### DTO 不加字段（AC8）

`CommentResponse` 是 record，作者投影**摊平**为 `authorNickname / authorAvatarUrl / authorDeleted`（不嵌套 `AuthorView`）：

```java
public record CommentResponse(
        Long id, long authorId, String authorNickname, String authorAvatarUrl,
        boolean authorDeleted, String body, Instant createdAt,
        Integer replyCount, List<CommentResponse> replies, String moderationStatus) {
```

⚠️ **契约金标 `ContentDetailAndCommentContractTest` 用 `assertThat(wire(dto).keySet()).isEqualTo(Set.of(...))` 精确锁死** `CommentResponse` 的 10 键与 `ContentDetailResponse` 的 13 键。R1/R2 是**纯隐藏、不加字段**，所以它保持绿；**任何 DTO 加字段都会立刻变红**，且需三方同步（App `comment.dart` / `content_detail.dart` + `mock_backend.dart`）。

---

### 会被打破的既有测试

**必定编译失败（改方法签名后）：**

| 文件 | 行 | 原因 |
|---|---|---|
| `content/service/CommentQueryServiceTest.java` | 100 / 117 | `when(comments.findTopLevel(eq(1L), anyBoolean(), any(), any(), any(), any(Pageable.class)))` —— 当前 6 实参 |
| 同上 | 102 / 123 | `when(comments.findRepliesForParents(anyList(), any()))` —— 当前 2 实参 |
| 同上 | 138 | `when(comments.findReplies(eq(1L), anyBoolean(), any(), any(), any(), any(Pageable.class)))` —— 当前 6 实参 |
| 同上 | 44 | `new CommentQueryService(comments, posts, accounts)` —— **注入 social 端口后这行必红** |
| `content/service/ContentDetailServiceTest.java` | 45-46 | `when(comments.countVisibleForViewer(anyLong(), any()))` |
| 同上 | 44 | `new ContentDetailService(posts, comments, likes, accounts, reportService)` —— 若也注入端口 |

**行为回归护栏（L1，需 Docker，留本地）：**
`ContentDetailControllerEndpointTest` · `CommentControllerEndpointTest` —— 走真实 Spring + 落库跑真 SQL，**新 JPQL 若有语法或参数类型问题（如 42P18）会在这里炸**。这是本 story 最有价值的护栏。

---

### ⚠️ 测试缺口：评论侧的 viewer 过滤，零覆盖

现有 `CommentQueryServiceTest` 的四个用例（分页 10/hasMore、内嵌前 3、回复展开、帖不可见 404）**全部以 `viewerId = null`（游客）调用**。全仓 `grep viewer src/test` 只命中 `ContentDetailServiceTest` / `FeedServiceTest` / `ContentFeedControllerEndpointTest` / `ReportTriageIntegrationTest` —— **评论侧为零**。

R1/R2 是**安全攸关过滤**，测试必须从零补齐（见 T5 清单）。

### Project Structure Notes

**修改（UPDATE，改前先读全文）**：
```
content/repository/CommentRepository.java     四个方法各加两条 NOT EXISTS（既有条件原样不动）
content/service/CommentQueryService.java      requireVisiblePost 改为返回 ContentPost；replies 分支新增反查；注入 social 端口
content/service/ContentDetailService.java     countVisibleForViewer 调用处传 postAuthorId
test/.../CommentQueryServiceTest.java         4 处 stub + 构造器
test/.../ContentDetailServiceTest.java        2 处
_bmad-output/implementation-artifacts/CROSS-STORY-DECISIONS.md   评论数口径留痕
```

**⚠️ 不动**：`CommentService`（写路径，AC6 明确一行不改）· `CommentRepository.countByRealAuthor`（平台口径）· `CommentResponse` / `ContentDetailResponse`（不加字段）· `comments` 表（无迁移）。

### 依赖与时序

- **依赖 Story 1.1** 的表与只读端口。**可与 Story 1.4 并行**（两者都只依赖 1.1）。
- 本 story 交付后，**Story 2.1 的举报隐藏自动复用这两条过滤**（举报只是往同一张表写 `source=REPORT`）。

### References

- [Source: _bmad-output/planning-artifacts/epics-v1.1.4.md#Story-1.3]
- [Source: _bmad-output/planning-artifacts/architecture-v1.1.4-delta.md#AD-3] R2 传参不 join（⚠️ 本 story 订正其第二句，见上）
- [Source: _bmad-output/planning-artifacts/architecture-v1.1.4-delta.md#AD-5] 过滤一律下沉 SQL
- [Source: _bmad-output/planning-artifacts/architecture-v1.1.4-delta.md#AD-13] 评论可见性与评论数口径
- [Source: _bmad-output/planning-artifacts/v1.1.4/PRD-v1.1.4.md#§3.2] FR-94 生效范围第 3 条 R1/R2 与三场景表
- [Source: _bmad-output/implementation-artifacts/v1.1.4/1-1-拉黑一个人他的帖子立刻从我首页消失服务端.md] 表结构与端口契约

---

## Dev Agent Record

### Agent Model Used

claude-opus-5[1m]（Claude Code，本地 macOS · Temurin JDK 21 · Docker postgres+redis 在跑）

### Debug Log References

- `./mvnw -B -o test-compile` → 绿（改签名后的既有测试已全部修好）
- `./mvnw -B -o test -Dtest=CommentHideFilterIntegrationTest` → **15/15 通过（L1，真 PostgreSQL）**
- `./mvnw -B -o test -Dtest=CommentQueryServiceTest,ContentDetailServiceTest` → 20/20
- `./mvnw -B -o package`（全量）→ **1581 通过 / 0 失败 / 6 skipped**，含契约金标 `ContentDetailAndCommentContractTest`（未加 DTO 字段，保持绿）

### Completion Notes List

#### 交付内容（L0 + L1 全绿，本 story 无 L2）

- **AC1 R1**：四个查询方法各加一条 `NOT EXISTS`（holder = 查看者），**不区分来源**——举报产生的隐藏关系同样过滤（Story 2.1 靠这条自动复用，已有专门用例钉住）。
- **AC2 R2**：判据是 `holder = :postAuthorId`（**内容作者**），`postAuthorId` 由 service 传参，SQL 里不 join 内容表。每条 R2 用例都**三视角 + 游客各看一遍**——写成按查看者判，第三方与游客那两条断言会立刻红。
- **AC3 无感知**：R2 的 `NOT EXISTS` 放过 `c.authorId = :viewerId`；被影子者看得到自己那条（正文、`moderationStatus` 一字不差），也看得到别人对他的回复。
- **AC4 回复串随父**：两条路径都覆盖，但**手段不同**（见下）。
- **AC5 两条计数口径**：`commentCount` 与 `replyCount` 都随过滤走，数字与渲染条数一致。
- **AC6 写路径一行未改**：`CommentService` 零改动，被拉黑者提交评论照常 201 并落库。
- **AC7 口径留痕**：`CROSS-STORY-DECISIONS.md` 新增一节 + 两处代码 Javadoc。
- **AC8/AC9 护栏**：无 DTO 字段变更、无新中间件、过滤全在 JPQL `WHERE` 内、无 Java 侧行过滤。

#### ⚠️ 一处文档自相矛盾，我做了裁决，产品可以推翻（成本极低）

**AD-13 的「评论数」那一条把两句互斥的话写在了同一句里**：「`countVisibleForViewer` 同步套用 R1 + R2」与「R1 隐藏的评论照常计入」。AC5 照抄了后半句。

裁决依据是**该条自己列的 Prevents 首项**——「防『显示 5 条评论却只数得出 4 条』的穿帮」。这个穿帮**只在 R1 场景发生**（R2 对第三方完全不存在，不会有落差；被影子者本人 +1 是文档明确接受的）。所以：

- **面向查看者的 `countVisibleForViewer` 套 R1 + R2** —— 数字恒等于他实际能看到的条数。
- **「R1 照常计入」属于平台口径 / 互动量那一层**（`countByRealAuthor` 及后续任何热度指标）：我不看不代表它不存在。R2 影子的则在那一层也必须排除。

两层分工已写进 `CROSS-STORY-DECISIONS.md`、`countVisibleForViewer` 的 Javadoc、以及 `ac5_r1HiddenCommentAlsoKeepsCountAndRenderedRowsInSync` 用例的注释。**若产品判另一种读法**，要动的是 `countVisibleForViewer` 里 R1 那两行 WHERE + 那条用例，其余一概不受影响。

#### AC4「回复串随父隐藏」为什么两条路径用了不同手段

| 路径 | 手段 | 理由 |
|---|---|---|
| 首屏内嵌回复（`findRepliesForParents`） | **什么都不用加** | `parentIds` 取自 `findTopLevel` **已过滤后**的那一页，被隐藏的父根本进不来，它的回复自然查不到。再补一层父判定是纯冗余，还会让这条 SQL 无谓变慢（已写进 Javadoc，防后人「补全」） |
| 展开端点（`findReplies`） | **service 层用只读端口判一次父，命中直接返回空页** | 该分支的父只有一个，在 SQL 里为每一行重复判定同一个父纯属浪费。story T2 明确允许这种做法（「实现方式由 dev 定」） |

R1 与 R2 的父判定在 Java 侧的 `isHiddenForViewer`，**自视豁免的不对称与 JPQL 那两段严格一致**（R2 豁免评论作者本人，R1 不豁免）。

#### 对架构 AD-3 第二句的实证订正已兑现

`replies(parentId, ...)` 原先**一次 post / parent 查询都没有**（AD-3 写的「经 `Comment.postId` 反查一次」是要新增的动作，不是既有的）。现在它反查两跳：`comments.findById(parentId)` → `posts.findById(parent.postId)`。`topLevel` 那侧则是零额外查询——只把 `requireVisiblePost` 从 `private void` 改成返回 `ContentPost`，原来查出来的实体本来就被扔了。

#### 刻意没做的事

- **没有给 `replies()` 补「帖子是否仍可见」的校验**。该分支原本就不校验（帖被下架/软删后，凭 `parentId` 仍能拉到回复）。这是一个**既有缺口**，story 明确写了「不要擅自修」——修它属于既有行为变更。现在为了 R2 已经把帖子查出来了，**补齐的成本已降到一行 filter**，等 PO 确认后随时可加。已在代码里就地留了注释说明。
- `countByRealAuthor()` 未动（平台口径），并加了 Javadoc 挡住后人顺手给它加过滤。
- 未加任何 DTO 字段、未建任何索引（R1/R2 打的是 Story 1.1 已建的 `idx_user_hide_relations_holder_target`，`comments.author_id` 侧不需要索引）。

#### 测试从零补齐（本 story 之前评论侧 viewer 过滤是零覆盖）

- **L1 `CommentHideFilterIntegrationTest`（15 例）**：R1 两种来源 / R2 三视角 + 游客 / R2 只作用于该作者自己的帖 / 无感知两条 / 回复串随父三条（R2、R1、回复自身被影子）/ 计数两条 / 被拉黑者仍可评论成功 / R1+R2 叠加 / 解除后立即恢复 / 解除拉黑不放行举报隐藏。
- **L0 `CommentQueryServiceTest` 新增 6 例**：`postAuthorId` 与 `hasViewer` 确实被传进查询、游客 `hasViewer=false`、父被 R1/R2 挡住返回空页且**不查库**、**被影子者自己展开自己那条仍拿得到回复**、父不存在返回空页。
- 游客分支（`hasViewer=false`）由 L1 里所有不带 `Authorization` 头的请求真打了一遍 —— **42P18 若复发会在那里当场炸**。

### File List

**新增**
```
petgo-backend/src/test/java/com/tailtopia/social/CommentHideFilterIntegrationTest.java   L1 · 15 例
```

**修改**
```
petgo-backend/src/main/java/com/tailtopia/content/repository/CommentRepository.java   四个查询各加 R1/R2 + 口径 Javadoc
petgo-backend/src/main/java/com/tailtopia/content/service/CommentQueryService.java    requireVisiblePost 返回实体 · replies 反查两跳 · 父闸门 · 注入只读端口
petgo-backend/src/main/java/com/tailtopia/content/service/ContentDetailService.java   commentCount 传 postAuthorId
petgo-backend/src/test/java/com/tailtopia/content/service/CommentQueryServiceTest.java  4 处 stub + 构造器 + 新增 6 例
petgo-backend/src/test/java/com/tailtopia/content/service/ContentDetailServiceTest.java 1 处 stub
_bmad-output/implementation-artifacts/CROSS-STORY-DECISIONS.md                        评论数与互动量口径留痕
```

**未动（刻意）**：`CommentService`（写路径）· `CommentRepository.countByRealAuthor`（平台口径）· `CommentResponse` / `ContentDetailResponse`（不加字段）· `comments` 表（无 Flyway 迁移）

---

## Change Log

| 日期 | 变更 |
|---|---|
| 2026-08-16 | 实现完成（`bmad-dev-story`）：T1–T6 全部完成。L0 全量 1581 通过；**L1 `CommentHideFilterIntegrationTest` 15/15 真打 PostgreSQL 通过**（Docker 在跑，本轮已验，非「待本地」）。⚠️ **裁决了 AD-13「评论数」那一条的字面矛盾**（面向查看者套 R1+R2；「R1 照常计入」归平台口径层），已回写 CROSS-STORY-DECISIONS。Status → review。 |
| 2026-08-15 | 由 `bmad-create-story` 生成。AC 承接 epics-v1.1.4 Story 1.3，补 AC9 护栏。**Dev Notes 订正架构 AD-3 第二句**（`replies` 分支的反查是要新增的、非现成）。**补出 R1/R2「作者自视豁免」不对称这一最易做反点**（AC3 下方说明 + 陷阱 ②），epics 原文未涉及 SQL 层处理。 |
