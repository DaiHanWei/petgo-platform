---
baseline_commit: 071247d9
epic: 3
story: 3.2
version: V1.1.4
source_epics: _bmad-output/planning-artifacts/epics-v1.1.4.md
architecture: _bmad-output/planning-artifacts/architecture-v1.1.4-delta.md
depends_on: Story 3.1（account_disposals 表 + 工单页）
---

# Story 3.2: 运营能给出警告或封号，用户也真的收得到

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

> **所属**：V1.1.4 Epic 3 第二个 Story（**后端 + 运营后台**）。
> **交付**：警告 / 封号两档处置 · 处置记录落库 · 用户侧通知。
> ✅ **AC9 的法务阻塞已解除（2026-08-16，C-101）** —— 封号通知措辞经法务审核通过。**仍有两条印尼语待运营产出**（警告通知 · FR-51 回告），但**不再阻塞发版**。
> ⚠️ **含高风险点 R5 的服务端侧**：封号 ≠ 注销，两者命名几乎一样而要求正好相反。
> ⚠️ **实证发现两处 epics 未预料的工作量**，见 Dev Notes：① **封号通知 type 当前不存在**，要新增枚举值 + 写 Flyway 迁移；② **停用不作废 access token**，「即时无法登录」有既有缺口。

## Story

As a **运营人员**,
I want **对一条工单执行警告或封号，并留下可追溯的处置记录**,
so that **违规的人受到实际处置，而我下次再遇到这个人时一眼能看到他已经被警告过几次**。

## Acceptance Criteria

> **验证层**：**L0**（`mvn -B package` / 门控测试）· **L1**（Docker postgres 真跑）· **L2**（后台页面，⚠️ 无 UI 稿基准）。

### AC1 — 两档处置动作（L1）

**Given** 一条待处理工单
**When** 运营执行处置
**Then** 可选**警告**或**封号**，两者**可组合使用**
**And** **不提供「限流曝光」选项，也不为它预留 UI**（该动作依赖推荐算法打分链路，随 FR-95 移至 1.1.8）

### AC2 — 处置必先落记录，且同一事务（L1）

**Given** 运营点击警告或封号
**When** 执行
**Then** **先写入 `account_disposals` 一行**（被处置账号 / 类型 / 处置人 / 关联工单 / 时间），**再触发用户侧后果**
**And** 写记录与触发后果**在同一事务内**

> ⚠️ 不同事务会出现「**用户登不上了，但工单页显示从没处置过**」—— 既有停用链路自带审计留痕，处置记录必须与它同生共死。
> ✅ **既有停用链路本就是同事务的**（`AdminUserService.deactivate` 上有 `@Transactional`，内部依次调 `authService.deactivateUser` → `consultInterrupt.interruptByUser` → `auditService.record`）。本 story 只需把 `account_disposals` 的写入插进这个事务。

### AC3 — 警告的完整定义（L1）

**Given** 运营对某账号执行警告
**When** 处理完成
**Then** 用户收到**一条系统通知**，中文语义为「你的账号因违反 TailTopia 社区规范收到一次警告，请遵守社区规范」
**And** 通知**不告知**是谁举报的、因哪条内容、也不告知这是第几次警告
**And** 警告**不影响用户使用** —— 能登录、能发内容、内容可见性不变
**And** 警告**不需要异议渠道**（不引导联系客服，与封号不同）

### AC4 — 警告可重复、不合并、不自动升级（L1）

**Given** 同一账号被多次警告
**When** 每次执行
**Then** **每次都发一条独立通知，不做合并、不做去重**
**And** **每次都写入一行处置记录**，工单页的「历史处置次数」相应递增
**And** 警告累计到**任何次数都不会自动触发封号、也不会自动置顶工单**
**And** 是否升级**完全由运营看到历史记录后人工判断**

### AC5 — 封号的后果与告知（L1）

**Given** 运营对某账号执行封号
**When** 处理完成
**Then** 该账号状态置为运营停用，**即时无法登录**，refresh token 一并撤销
**And** 用户收到系统通知，中文参照「你的账号因违反 TailTopia 社区规范已被停用。如有异议，请联系客服团队。」
**And** 异议走 **FR-52 通用客服工单**，由客服按个案人工判断
**And** **不建立独立的申诉审核工作流或 UI** —— 客服只是唯一的异议反馈渠道
**And** ⚠️ **「即时无法登录」的既有实现有一处缺口**：停用只删 refresh 行 + 置状态位，**已签发的 access JWT 在其自然过期前仍可用**（无黑名单）。**本 story 沿用既有行为**，但须在 Completion Notes 记录该取舍

### AC6 — ⚠️ 封号不等于注销（高风险点 R5 的服务端侧）（L2）

**Given** 被封号的账号
**When** 检查其在各处的表现
**Then** 其**存量内容不因封号而全部删除**
**And** 该账号在**其他用户的黑名单里照常展示、不做特殊标记、仍可被拉黑**
**And** 封号**不触发**任何注销才有的匿名化处理

> ⚠️ 代码里「运营停用」（`UserStatus.DEACTIVATED`）与迷你卡投影的「已注销」（`User.deletedAt` → `AuthorView.deleted`）命名几乎一样，而本版本对两者要求**正好相反**。**以中文定义为准，不要凭字段名推断语义。**
> ✅ **实证：后端 `AuthorView` 只有 `deleted` 一个字段，根本不下发「是否被封号」** —— 所以「黑名单里照常展示」**前端什么都不做就自动满足**。本 AC 的价值在于**阻止有人「顺手」加标记**。

### AC7 — 工单状态流转（L1）

**Given** 工单被处置或驳回
**When** 状态更新
**Then** 工单状态相应流转（已处理 / 已驳回），记录处理人与处理时间

> ⚠️ **数据层与展示层要分清（C-103）**：**工单内部状态名与「驳回」这个动作一律不改** —— 对举报而言「驳回」是准确的（驳的是举报人的主张），既有 `ReportStatus.DISMISSED` 枚举、「驳回」按钮、「批量驳回」按钮**全部保持原样**。
> **改的只有统一视图的状态筛选档标签**：第三档展示为**「无需处置」**而不是「已驳回」，因为那一档现在同时装着「举报不成立」与「账号标识字段审核通过」两类，后者没有请求方可驳。
**And** 若该账号**之后又被举报**，同一条工单**翻回待处理**（Story 2.1 AC9），而**历史处置记录不受影响、照常累计**

### AC8 — 零自动处置的回归确认（L1）

**Given** 任意数量的用户举报同一账号
**When** 举报累积
**Then** **系统不会自动执行警告或封号**，也**不会自动挂起该账号的内容**
**And** 所有处置**均需运营确认**

### AC9 — 文案（法务已闭环，两条印尼语待运营）（L0/L2）

**Given** 警告通知与封号通知的印尼语措辞
**When** 实现
**Then** ✅ **封号通知的印尼语已经法务审核通过（2026-08-16，C-101）**，可直接落 ARB：<br>`Akun Anda telah dinonaktifkan karena melanggar Pedoman Komunitas TailTopia. Jika Anda ingin mengajukan keberatan, silakan hubungi tim dukungan pelanggan kami.`
**And** ⚠️ **警告通知的印尼语尚未产出**（待运营），代码可先完成、文案位留出，**上线前收口**
**And** 该待办**不阻塞发版**（法务阻塞已解除）

### AC10 — 护栏（L0）

**Given** 本 story 的实现
**Then** ⚠️ **新增通知 type 的 Flyway 迁移必须基于 `V97` 的完整 19 值追加**（见 Dev Notes）
**And** 未引入 MQ / 缓存 / 新中间件
**And** 处置记录与既有审计链路**双留痕**（`account_disposals` + `admin_audit_logs`）
**And** 新权限码已补三份 i18n 并加进 `AdminPermissions.GROUPS`
**And** `mvn -B package` 全绿

---

## Tasks / Subtasks

### 🟦 后端子任务（petgo-backend）

- [x] **T1 · 新增通知类型 + Flyway 迁移**（AC3 / AC5 / AC10）
  - [x] ⚠️ **实证：`NotificationType` 里没有封号/警告类型** —— V1 封号**不发站内通知**。本 story 要新增
  - [x] `NotificationType` 加值（如 `ACCOUNT_WARNED` / `ACCOUNT_SUSPENDED`）
  - [x] ⚠️ **写一个 `DROP CONSTRAINT ck_notifications_type` + `ADD CONSTRAINT` 迁移**（照 V40/V43/V72/V97 范式）
  - [x] ⚠️ **必须基于 `V97` 的完整 19 值追加**，否则会重蹈 V72 覆盖审核线三值的覆辙
  - [x] 迁移号接 Story 3.1 之后顺延

- [x] **T2 · 处置服务**（AC1–AC5 / AC7）
  - [x] 新建处置 service，**先写 `account_disposals` 再触发后果，同一事务**
  - [x] 警告：调 `NotificationService.send(userId, ACCOUNT_WARNED, ...)`
  - [x] 封号：调既有 `AuthService.deactivateUser(userId)`（置状态 + 删 refresh）+ 发通知
  - [x] ⚠️ 若复用 `AdminUserService.deactivate`，注意它还会 `consultInterrupt.interruptByUser(userId)` **强关进行中问诊会话** —— 这条副作用会一并发生，需确认产品是否接受
  - [x] 工单状态流转 + 记录处理人/时间（AC7）
  - [x] 处理人 id 取法照抄既有 `handlerId(AdminUserDetails admin)`：`admin.hasOperatorUserId() ? admin.getUserId() : admin.getAdminAccountId()`

- [x] **T3 · 后台页面处置动作**（AC1 / AC6）
  - [x] 在 Story 3.1 的工单页加「警告」「封号」两个动作按钮
  - [x] 危险操作用 `data-confirm`（`admin.js` 既有能力：表单带 `data-confirm="提示文案"` 时提交前弹 `confirm`）
  - [x] ⚠️ CSRF 开着，必须真表单 POST
  - [x] **不为「限流曝光」预留任何 UI**

- [x] **T4 · 审计**（AC10）
  - [x] 新动作码加进 `AuditActions`（UPPER_SNAKE 过去式，如 `ACCOUNT_WARNED` / `ACCOUNT_SUSPENDED`）
  - [x] 经 `AdminAuditService.record(actorAccountId, actionType, targetType, targetId, summary)` 写
  - [x] ⚠️ summary 里**严禁 PII / 健康数据 / 内容原文 / 令牌**
  - [x] 若希望在审计查询页的下拉出现，还要加进 `AuditLogAdminController.ACTION_TYPES`（**手维护的短白名单，不是全集**）

- [x] **T5 · 测试**（AC1–AC10）
  - [x] **L0 门控**：`*AccessControlTest` 写法
  - [x] **L1**：`extends ApiIntegrationTest`
  - [x] AC2 同事务：制造通知发送失败 → `account_disposals` 也回滚
  - [x] AC4：多次警告 → 多条独立记录 + 多条独立通知，**无合并去重**
  - [x] AC6：封号用户在黑名单接口中**照常返回昵称头像**、无特殊标记
  - [x] AC8：大量举报后**内容仍 PUBLISHED、账号仍 ACTIVE**

---

## Dev Notes

> ⚠️ 本节代码事实来自 CS 阶段实证扫描。

### 🔴 两处 epics 未预料的工作量

#### ① 封号通知 type 当前**不存在** —— 要新增枚举 + 写 Flyway 迁移

**实证**：`NotificationType` 的 19 个值里**没有任何封号/警告类**；`admin` 包内 ban/suspend 逻辑与 notify **无交集**。
**➜ V1 的封号处置根本不发站内通知。** AC3/AC5 要求发通知，**是新增能力**。

`notifications` 表自 `V19` 建表后**没有任何 ADD COLUMN 型 ALTER** —— 后续 6 个迁移全是 `DROP CONSTRAINT ck_notifications_type` + `ADD CONSTRAINT` 重建白名单：

| 迁移 | 新增值 |
|---|---|
| `V21` | `PET_BIRTHDAY`, `COMPANION_ANNIVERSARY`, `MILESTONE_NODE` |
| `V40` | `CONTENT_REMOVED`（补漏）, `REPORT_REVIEWED` |
| `V43` | `CONTENT_REVIEW_APPROVED`, `CONTENT_REVIEW_REJECTED` |
| `V53` | `NAME_RESET`, `AVATAR_RESET`, `CONTENT_REVIEW_TIMED_OUT` |
| `V72` | `REFUND_REJECTED`, `TICKET_RESOLVED`, `CSAT_SURVEY`, `IDENTITY_REQUIRE_MODIFY` |
| **`V97`** | **两条工作线并集重建，为当前权威的 19 值** |

⚠️ **必须基于 V97 的完整 19 值追加**，否则会重蹈 **V72 覆盖审核线三值**的覆辙。

#### ② 「即时无法登录」有既有缺口 —— access token 不作废

`AuthService.deactivateUser`：

```java
@Transactional
public void deactivateUser(long userId) {
    User user = users.findById(userId).orElseThrow(() -> AppException.notFound("用户不存在"));
    user.deactivate();                                              // 置 UserStatus.DEACTIVATED
    users.save(user);
    refreshTokens.deleteByUserIdAndSubjectType(userId, SubjectType.USER);  // 撤销点：物理删 refresh 行
}
```

刷新端有二次门控：

```java
if (!user.isActiveStatus()) {
    throw AppException.forbidden(DEACTIVATED_MESSAGE);
}
```

**➜ 只删 refresh 行 + 置状态位，已签发的 access JWT 在其自然过期前仍可用（无黑名单）。**
本 story **沿用既有行为**（不新建 token 黑名单，那是独立议题），但 **AC5 要求在 Completion Notes 记录这个取舍** —— 否则验收时「即时无法登录」这句话会被当成没做到。

---

### 既有停用链路（可复用，但注意一处副作用）

```java
@Transactional
public void deactivate(long userId, String reason, long actorAccountId) {
    if (reason == null || reason.isBlank()) {
        throw AppException.validation("停用原因不能为空");     // ⚠️ 原因必填
    }
    accountQuery.findUserById(userId).orElseThrow(() -> AppException.notFound("用户不存在"));
    authService.deactivateUser(userId);
    consultInterrupt.interruptByUser(userId);                  // ⚠️ 强关进行中问诊会话
    auditService.record(actorAccountId, AuditActions.USER_DEACTIVATED, "USER",
            String.valueOf(userId), "停用用户（原因：" + reason.trim() + "）");
}
```

**三点注意**：
1. ⚠️ **原因必填**（空白直接 400）—— 从工单页封号时要带上原因，或另开一个不校验原因的路径
2. ⚠️ **会强关进行中问诊会话** —— 若从举报工单封号也走这条，这个副作用会一并发生。**需确认产品是否接受**
3. ✅ **已是 `@Transactional`** —— `account_disposals` 的写入插进这个事务即可满足 AC2

---

### `violation_counts` 为什么不复用（架构 AD-10 的判断是对的）

```sql
CREATE TABLE violation_counts (
    account_id          BIGINT      NOT NULL,
    violation_type      VARCHAR(16) NOT NULL,           -- POST / COMMENT / NAME / AVATAR
    violation_count     INTEGER     NOT NULL DEFAULT 0,
    first_violation_at  TIMESTAMPTZ,
    last_violation_at   TIMESTAMPTZ,
    ...
    CONSTRAINT uq_violation_counts_account_type UNIQUE (account_id, violation_type)
);
```

**它是纯聚合计数快照** —— 没有单条处置记录、没有时间线、**没有处置类型/操作人/关联工单**。实体 javadoc 明写「**仅记录、不处置** —— 不进任何审核/发布/风控判定链」。

**➜ 天然承载不了「上次什么时候因什么警告的」这类信息**，`account_disposals` 是必要的新表。
⚠️ 若产品另要求账号举报判定成立也计入违规计数，那需要扩它的 `ck_violation_counts_type` —— **需产品明确表态，不要自作主张**。

---

### 审计现状

`admin_audit_logs` 是 **append-only 哈希链**（`prev_hash` + `row_hash`，DB 触发器阻断 UPDATE/DELETE）。写入唯一入口 `AdminAuditService.record`，`@Transactional(REQUIRED)`（业务回滚则审计一并回滚，无悬挂审计）。

⚠️ **`record` 内有 `chainLock.acquire()`（Postgres advisory 锁）串行化** —— 单条处置无所谓，但 **Story 3.3 的批量处置会是 N 次取锁 + N 次链尾查询**，那里要注意规模成本。

**账号级处置目前的留痕只有 `admin_audit_logs` 里的一行中文 detail**（`USER_DEACTIVATED`，detail 含运营填的原因），**无结构化字段** —— 这正是 `account_disposals` 的立项理由。

---

### 后台动作按钮的既有能力

`admin.js`（全文仅 61 行，原生 JS + 事件委托，**无框架**）提供：
- **`data-confirm`**：表单带该属性时提交前弹 `window.confirm`，取消则阻止提交 ← **危险操作二次确认直接用它**
- **`data-open-dialog` / `data-close-dialog`**：原生 `<dialog>` 开关（人工审核的拒绝弹窗就是范例）← **若封号要填原因，用这个**
- `data-lightbox` 图片灯箱 · `.toast` 自动消失

⚠️ **没有全选/选中计数/表格排序/AJAX 提交** —— 那些是 Story 3.3 要新写的。

---

### Project Structure Notes

**新增（NEW）**：
```
moderation/service/AccountDisposalService.java
admin/moderation/web/（处置动作端点，或并入 Story 3.1 的 controller）
db/migration/V1xx__extend_notification_types_account_disposal.sql   ⚠️ 基于 V97 全量追加
```

**修改（UPDATE）**：
```
notify/domain/NotificationType.java              加 ACCOUNT_WARNED / ACCOUNT_SUSPENDED
admin/audit/service/AuditActions.java            加处置动作码
admin/audit/web/AuditLogAdminController.java     ACTION_TYPES 白名单（若需在下拉出现）
admin/account/domain/AdminPermissions.java       新权限码 + GROUPS
resources/i18n/messages_{zh_CN,en,id}.properties 页面文案 + perm.<code>
resources/templates/admin/<工单页>.html           处置动作按钮
```

**⚠️ 不动**：`violation_counts` 表 · `NotificationService.send`（含 `REQUIRES_NEW`）· 既有 `AuthService.deactivateUser` 的行为（沿用，含 access token 缺口）。

### 依赖与时序

- **依赖 Story 3.1** 的 `account_disposals` 表与工单页。
- **Story 3.3 依赖本 story** 的单条处置口径（批量要走同一条「先写记录再触发后果」的路径）。
- ✅ **AC9 的法务阻塞已于 2026-08-16 解除（C-101）**；仅剩警告通知的印尼语待运营产出，上线前收口即可。

### References

- [Source: _bmad-output/planning-artifacts/epics-v1.1.4.md#Story-3.2]
- [Source: _bmad-output/planning-artifacts/architecture-v1.1.4-delta.md#AD-10] 新建账号处置记录表 / 先写记录再触发后果 / 同一事务
- [Source: _bmad-output/planning-artifacts/architecture-v1.1.4-delta.md#§3] 安全攸关 S3「封号 ≠ 注销」
- [Source: _bmad-output/planning-artifacts/v1.1.4/PRD-v1.1.4.md#§3.1] 处置动作两档 / 警告完整定义 / 封号告知
- [Source: _bmad-output/implementation-artifacts/v1.1.4/3-1-三类工单终于在一个队列里还知道先看哪条.md] 表与页面
- [Source: _bmad-output/implementation-artifacts/v1.1.4/1-4-他来点赞评论我这边一点动静都没有.md] 通知 type 迁移范式

---

## Dev Agent Record

### Agent Model Used

claude-opus-5[1m]（Claude Code，本地 macOS · Temurin JDK 21 · Flutter 3.44.8 · Docker postgres+redis 在跑）

### Debug Log References

- 后端：`AccountDisposalIntegrationTest` **9/9（L1）** · `UnifiedTicketAccessControlTest` **7/7（L0）** · 全量 `package` → **1658 通过 / 0 失败 / 6 skipped**
- 前端：`flutter analyze` → **No issues found**；`flutter test` → **815 通过**（通知中心新增 2 例）

### Completion Notes List

#### 交付内容

- **AC1**：警告 / 封号两档，**没有「限流曝光」，也没有为它留任何 UI 位**（点了没反应的按钮比没有按钮更糟）。
- **AC2**：**先写 `account_disposals` 再触发后果，同一事务**。
- **AC3/AC4**：警告发通知、**不影响使用**；三次警告 = **三条记录 + 三条通知**，不合并不去重；**累计任何次数都不自动封号**（有用例断言三次警告后账号仍 ACTIVE）。
- **AC5**：封号置停用（可逆）+ 撤销 refresh + 发通知；异议走 FR-52 客服工单，**没有独立申诉工作流**。
- **AC6**：封号**不删存量内容、不匿名化**，有用例逐项断言（`deletedAt` 仍 null / 昵称还在 / 帖子仍 PUBLISHED）。
- **AC7**：工单流转到已处理/无需处置 + 记处理人与时间；**再被举报翻回待处理时，历史处置记录一条不减**（专门一条用例）。
- **AC8**：15 人举报后**零自动处置**（无记录、无通知、账号 ACTIVE、内容 PUBLISHED）。
- **AC9**：封号印尼语按法务定稿原文落 ARB；**警告印尼语是草稿**（见下）。
- **AC10**：V104 基于 V97 全量追加 · 双留痕（`account_disposals` + `admin_audit_logs`）· 新权限码四处齐。

#### ⚠️ 一处产品决定，我选了保守的一边，需要你确认

story 明确提示：复用 `AdminUserService.deactivate` 会**顺带强关该用户进行中的问诊会话**，「需确认产品是否接受」。

**我没有复用它**，而是直接调更底层的 `AuthService.deactivateUser`（置状态位 + 撤销 refresh 句柄）+ 自己写审计。理由：**问诊是付费的，强关牵扯退款语义** —— 从一条社区举报工单顺手触发一次涉及金钱的副作用，风险远大于收益。

**代价**：一个刚被封号的人，如果此刻正在跟兽医对话，那个会话**不会被立刻掐断**（但他的 access token 过期后就登不回来了）。若产品认为「封号必须立刻停掉一切」，改动是在 `AccountDisposalService.suspend` 里加一行 `consultInterrupt.interruptByUser(...)` —— **但要先把退款口径定清楚**。

> 顺带：`AdminUserService.deactivate` 还要求**停用原因必填**（空白直接 400）。工单侧的封号没有原因输入框（PRD 没要求），这也是不复用它的原因之一。

#### ⚠️ AC5「即时无法登录」的既有缺口（沿用未改，story 要求记录）

`AuthService.deactivateUser` 只做两件事：置 `UserStatus.DEACTIVATED` + **物理删 refresh 行**。刷新端有二次门控（状态不对就 403），所以他**续不了期**；但**已经签发出去的那张 access JWT 在自然过期前仍然可用**（系统没有 token 黑名单）。

→ 「即时无法登录」的准确表述是「**立即无法续期，当前 access token 的剩余寿命内仍可请求**」。建 token 黑名单是独立议题，不在本 story 范围。**验收时别把这条当成没做到。**

#### 通知类型是新增能力，不是接线

实证确认 **V1 的封号根本不发站内通知** —— `NotificationType` 的 19 个值里没有任何封号/警告类，admin 侧的停用逻辑与 notify 毫无交集。所以本 story 新增了两个枚举值 + `V104` 迁移。

⚠️ **V104 是 DROP + ADD 全量重建，19 个既有值一个不落地照抄了 V97**。V97 本身就是为修 V72 的事故而存在的（两条工作线各自重建 CHECK，后跑的那条漏了另一条的三个值 → 审核通知全断）。**在这里漏抄任何一个值都会重演那次事故**，迁移文件里已就地写明。

#### 前端也要改：App 不渲染后端下发的通知文案

通知中心是**按 type 本地化**的（`_typeLabel` / `_typeBody` 两张 switch 表），后端下发的 title/body 根本不显示。所以新增两类必须同步加 ARB 键 + 两处 switch 分支，否则会落到「系统通知」兜底 —— 用户会看到两条一模一样的卡片，而**这两件事的严重程度完全不同**（一个不影响使用，一个已经登不上了）。图标也分开：警告用警示琥珀、封号用危险红。有用例断言「不得落兜底」。

#### 文案状态

| | 状态 |
|---|---|
| 封号（印尼语） | ✅ **法务已审核通过**（2026-08-16，C-101），按定稿原文落 ARB。**改词前必须回法务**，ARB 的 description 里已写明 |
| 警告（印尼语） | ⚠️ **草稿**，待运营产出定稿。已在 ARB description 标注。**不阻塞发版**，上线前收口 |

#### 权限：封号比警告多要一个码

新增 `content.dispose_account`（警告 / 无需处置用它）。**封号那一档额外还要 `user.deactivate`**（端点上是 and 关系，页面按钮也同条件隐藏）：停用账号本来就是一项受管能力，不能因为「他能处理工单」就顺带把停用权也给了。有专门用例断言「只有处置权 → 封号仍 403」。

#### ⚠️ 本 Epic 无 UI 稿

处置按钮直接放在工单行的操作列（警告 / 封号 / 无需处置三个真表单 POST，危险动作走既有 `data-confirm`）。**只对「用户举报」类工单、且只在待处理时出现** —— 内容举报走既有的下架/驳回，账号标识字段走它自己的审核页，三类工单的处置动作不是同一套东西。这一处分工建议与产品确认。

### File List

**新增**
```
petgo-backend/src/main/resources/db/migration/V104__extend_notification_types_account_disposal.sql
petgo-backend/src/main/java/com/tailtopia/moderation/service/AccountDisposalService.java
petgo-backend/src/test/java/com/tailtopia/moderation/AccountDisposalIntegrationTest.java   L1 · 9 例
```

**修改**
```
petgo-backend/src/main/java/com/tailtopia/notify/domain/NotificationType.java        加两值
petgo-backend/src/main/java/com/tailtopia/moderation/domain/AccountReport.java       加 handleBy
petgo-backend/src/main/java/com/tailtopia/admin/audit/service/AuditActions.java      加三个动作码
petgo-backend/src/main/java/com/tailtopia/admin/account/domain/AdminPermissions.java content.dispose_account
petgo-backend/src/main/java/com/tailtopia/admin/moderation/web/UnifiedTicketController.java  三个处置端点
petgo-backend/src/main/resources/templates/admin/tickets.html                        处置按钮（data-confirm）
petgo-backend/src/main/resources/i18n/messages_{zh_CN,en,id}.properties               6 键 × 3
petgo-backend/src/test/java/.../UnifiedTicketAccessControlTest.java                   加 2 例
petgo-backend/src/test/java/.../AdminPermissionsTest.java                             44 → 45
petgo_app/lib/features/notify/presentation/notification_center_page.dart              两类的图标/标题/正文
petgo_app/lib/l10n/app_en.arb · app_id.arb                                            4 键
petgo_app/test/notify/notification_center_test.dart                                   加 2 例
```

**未动（刻意）**：`violation_counts` 表（纯计数快照，承载不了「上次因什么警告的」）· `NotificationService.send`（含 `REQUIRES_NEW`）· `AuthService.deactivateUser` 的行为（沿用，含 access token 缺口）· `AdminUserService.deactivate`（那条路径的强关问诊副作用不引入）

---

## Change Log

| 日期 | 变更 |
|---|---|
| 2026-08-16 | 实现完成（`bmad-dev-story`）：T1–T5 全部完成。**Flyway V104**（基于 V97 全量追加两值）。后端全量 1658 通过（L1 9 例 + 门控 7 例）；前端 815 通过。⚠️ **封号刻意不复用 `AdminUserService.deactivate`**（避免顺带强关付费问诊会话），需产品确认；**access token 缺口沿用未改**，已记录取舍。前端通知中心按 type 本地化，故同步加了 ARB 与两处 switch。封号印尼语按法务定稿落地，**警告印尼语仍是草稿待运营**。Status → review。 |
| 2026-08-15 | 由 `bmad-create-story` 生成。AC 承接 epics-v1.1.4 Story 3.2，补 AC10 护栏。**Dev Notes 实证补出两处 epics 未预料的工作量**：① 封号/警告通知 type 当前不存在，须新增枚举 + 基于 V97 全量写 DROP+ADD 迁移；② 停用不作废 access token，「即时无法登录」有既有缺口（沿用但须记录取舍）。另提示：既有 `deactivate` 原因必填 + 会强关问诊会话，复用前需确认。 |
