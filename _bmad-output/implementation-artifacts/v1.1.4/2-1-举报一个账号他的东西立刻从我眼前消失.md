---
baseline_commit: 071247d9
epic: 2
story: 2.1
version: V1.1.4
source_epics: _bmad-output/planning-artifacts/epics-v1.1.4.md
architecture: _bmad-output/planning-artifacts/architecture-v1.1.4-delta.md
depends_on: Story 1.1（表 + 只读端口 + 写服务）
flyway_expected: V102
---

# Story 2.1: 举报一个账号，他的东西立刻从我眼前消失

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

> **所属**：V1.1.4 Epic 2 第一个 Story（**纯后端**）。Epic 2 的地基。
> **交付**：`account_reports` + `account_report_entries` 两张表（V102）· 账号举报提交接口 · **举报即隐藏（同事务）** · 五类新枚举 · 「已举报」状态。
> ⚠️ **本 story 最大的风险是「照抄既有内容举报」**。既有 `ReportService.submit` 用两处裸 `return` 把重复举报**幂等吞掉、什么都不写**；账号举报**必须反过来**——每次都留一行明细。
> ⚠️ 举报隐藏一旦写入**永不删除**，且六处过滤自动生效——**唯一例外是主页访问仍可进入**（Story 1.1 AC7 已保证）。

## Story

As a **遇到骚扰、仿冒或恶意账号的用户**,
I want **举报这个账号并说明原因**,
so that **平台能整体处置他，同时我不用再逐条举报他的每一篇内容**。

## Acceptance Criteria

> **验证层**：**L0**（`mvn -B package`）· **L1**（Docker postgres 真跑）。本 story 无 L2。

### AC1 — 工单与明细两张表（L1）

**Given** 数据库已执行本 story 的迁移（**接 Story 1.1 之后的号，实际序号按执行顺序顺延**；预期 `V102`）
**When** 检查表结构
**Then** `account_reports`（工单）的 `target_user_id` **唯一**，含 `status` / `first_reported_at` / `handled_by` / `handled_at` / 时间戳
**And** `account_report_entries`（明细）含 `report_id`(FK) / `reporter_id` / `reason` / `detail` / `created_at`，索引 `(report_id, reporter_id)` 供优先级公式聚合
**And** 表归属已回写 `CROSS-STORY-DECISIONS.md` 的「表归属总表」

### AC2 — 工单粒度 = 被举报账号（L1）

**Given** 同一个被举报账号 B 被 12 个不同用户举报，其中甲报了 7 次
**When** 查询工单
**Then** **只存在一条工单**（`target_user_id = B`）
**And** 明细表有 **18 行**（12 人中 11 人各 1 次 + 甲 7 次）
**And** 「同一举报人对同一账号只占一条工单」这条要求由本粒度**天然满足**

### AC3 — 每一次举报的类型与说明都要留（L1）

**Given** 甲第一次选「持续骚扰」，第二次选「仿冒他人」并填了补充说明
**When** 查询该工单的明细
**Then** **两次的类型都在**，第二次的说明**追加保存、不覆盖**
**And** 后台可按时间倒序取出全部明细

> ⚠️ **不得照抄既有内容举报的做法** —— `ReportService.submit` 有**两处裸 `return`**：
> ```java
> if (reports.existsByPostIdAndReporterId(postId, reporterId)) {
>     return; // 幂等：已举报过
> }
> try { reports.save(...); }
> catch (DataIntegrityViolationException e) { return; } // 并发撞唯一约束，幂等吞掉
> ```
> 照抄会让**次数、类型变化、补充说明全部丢失**，PRD 重复举报要求的第 2、3 条直接落空。
> ⚠️ 决策日志 **C-50** 曾建议「一对(举报人,账号)只存一行 + 次数累加」，**该建议已被 PRD 正文取代**（一行存不下每次的类型与说明）。

### AC4 — 五类账号维度理由（L1）

**Given** 举报提交接口
**When** 传入理由
**Then** 只接受**账号维度五类**：垃圾信息/恶意营销 · 仿冒他人 · 持续骚扰 · 发布违规内容 · 其他
**And** 落库为 `varchar` + `UPPER_SNAKE`（`AccountReportReason{SPAM, IMPERSONATION, HARASSMENT, VIOLATING_CONTENT, OTHER}`）
**And** **是新枚举、未复用内容维度的 `ReportReason`**
**And** 选「其他」时 `detail` **必填**，上限 **200 字**，不填则返回校验错误
**And** 其余四类**不接受也不要求** `detail`

> ⚠️ 既有 `ReportReason` 是内容维度五类（`ILLEGAL / MISINFO / INAPPROPRIATE / HARASSMENT / OTHER`），**与账号维度只有「骚扰」和「其他」能对上**。研发极易直接套用。

### AC5 — 举报即隐藏，同一事务（L1）

**Given** 用户 A 举报 B
**When** 提交成功
**Then** 写入工单/明细 **与** 写入一条 `source=REPORT` 的隐藏关系**在同一事务内完成**
**And** 任一失败则整体回滚，不出现「有工单没隐藏」或「有隐藏没工单」
**And** ⚠️ **若该 `(holder, target)` 已存在 `source=BLOCK` 行（先拉黑、后举报），写入 `REPORT` 行不得修改 `BLOCK` 行的任何字段**（含时间戳）—— 两条隐藏关系是彼此独立的行

> ⚠️ **这条与 Story 1.5 AC2 的排序约束是一对**：读侧钉死「排序取 `BLOCK.created_at`」，写侧钉死「举报不碰 `BLOCK` 行」，**两边都做才保证先拉黑后举报不会把条目顶到黑名单最前面**。只做一边，另一边的实现细节一变就会重新穿帮。
> ⚠️ **调用方向**：`moderation` 调 `social` 的写服务建 `REPORT` 行，**不反向依赖**（AD-8）。

### AC6 — 举报隐藏的生效范围（复用 Epic 1 机制的回归验证）（L1）

**Given** A 举报了 B
**When** 检查各处
**Then** B 的内容**不出现在 A 的首页**
**And** B 的评论**对 A 不展示**（R1）
**And** B 在 **A 的内容下**的评论**对所有人不展示**（R2，影子评论**同样因举报触发**）
**And** 来自 B 的互动通知**不再发给 A**
**And** B 的帖子详情页**对 A 返回 404**（Story 1.1 AC10）
**And** **但 A 仍可进入 B 的迷你主页**（唯一例外，闭环需要它）

### AC7 — 举报隐藏不可解除、不进黑名单页（L1）

**Given** 存在 `source=REPORT` 的隐藏关系
**When** 尝试解除或查询黑名单
**Then** **不存在任何解除该关系的接口或入口**
**And** 该关系**不出现在黑名单页**（黑名单页只收录含 `source=BLOCK` 的条目）

### AC8 — 「已举报」状态服务端持久化（L1）

**Given** A 举报过 B
**When** A 重启 App 后再查询 B 的迷你主页
**Then** 返回中带「已举报」标记，该标记**由 `source=REPORT` 行是否存在派生**，不是前端会话态
**And** 黑名单页中若该用户也在（含 BLOCK 层），同样带该标记

### AC9 — 工单被处理后再收到举报（L1）

**Given** 某工单已被运营处理（`status` 非待处理）
**When** 又有用户举报该账号
**Then** **同一条工单翻回待处理**，**不新建工单**
**And** 新的明细行照常追加
**And** 历史处置留痕**不因翻回而丢失**（留痕在 Epic 3 的处置记录表）

### AC10 — 边界（L1）

**Given** 举报提交接口
**Then** **用户不可举报自己**（目标为本人时返回校验错误）
**And** 举报行为**对被举报人完全不可见**（不发通知、不产生任何可感知变化）
**And** **不设任何自动预处置** —— 无论多少人举报、什么理由，都不会自动挂起该账号的内容
**And** 既有帖子级举报的两条自动通道**保持不变、未被改动**
**And** ⚠️ **不设任何频率限制或冷却窗口** —— 同一举报人对同一账号**可无限次举报**，每次照常落一行明细

> ⚠️ **「不设冷却」是一条主动决定，不是遗漏**（A-A23）：单个举报人对优先级分的贡献**已由公式从结构上封顶 2 分**，加冷却只会多一处逻辑、并多一处需要向用户解释的静默失败。
> **为什么必须写进 AC**：AC3 要求「每次都留一行」，研发看到「同一人可无限次写库」时，**防刷直觉几乎必然驱使他自行加一道冷却**。
> ⚠️ **与 AC11 的短窗去重不矛盾**：AC11 防的是**误触**，本条禁的是**限制用户意图**。
> ✅ **实证：既有内容举报的两条自动通道是** `ReportService.maybeApplyP0PreDisposal` —— ① `reason == ILLEGAL` 单次即触发；② `countByPostId >= P0_REPORT_COUNT_THRESHOLD`（**= 10，硬编码在 Java 里，运营改不了**）。**本 story 一行不改它们。**

### AC11 — ⚠️ 服务端防重复提交（L1）

**Given** 用户在举报抽屉点击提交
**When** 因双击穿透、或提交中的网络重试，同一举报人对同一账号在**极短窗口内**（建议 5 秒，具体值实现时定并在 Dev Notes 记录）发生重复提交
**Then** 该重复提交**视为同一次举报，不新增 `account_report_entries` 明细行**，接口返回成功（幂等）
**And** **服务端不依赖前端防连点**——Story 2.2 AC5 的「提交中禁用」照做，但服务端自身必须成立
**And** 该去重**不改变** AC3 的要求：超出窗口后的再次举报**照常追加一行**，每次的举报类型与「其他」补充说明**照常独立留存**

> ⚠️ **为什么需要这条**：拉黑侧 Story 1.1 AC3 已明写「服务端不依赖前端防连点」，**举报侧原先没有对应条款**。
> **后果不是数据错误，是污染两个直接给运营看的数字**：① AB-3D 工单展示的「12 人 / 27 次」；② **≥5 次高频阈值判定**（Story 3.1 AC4）—— 一个举报 3 次的人被双击刷到 5 次就会被计入高频加成。

### AC12 — 日志与护栏（L0）

**Given** 举报的「其他」补充说明是**用户自由文本**
**When** 记录日志
**Then** **该文本不得进入任何日志**（继承日志禁 PII 红线）
**And** 未引入任何 MQ / 缓存 / 新中间件；举报写入为同步小写
**And** `mvn -B package` 全绿

---

## Tasks / Subtasks

### 🟦 后端子任务（petgo-backend）

- [x] **T1 · Flyway 迁移（预期 `V102`）**（AC1）
  - [x] 接 Story 1.1 之后的号（1.1 取 V101 ⇒ 本 story **V102**；实际按执行顺序顺延）
  - [x] `account_reports`：主键 `BIGINT GENERATED BY DEFAULT AS IDENTITY` · `target_user_id BIGINT NOT NULL` **UNIQUE** · `status VARCHAR(16)` + CHECK · `first_reported_at TIMESTAMPTZ` · `handled_by BIGINT` · `handled_at TIMESTAMPTZ` · `created_at` / `updated_at TIMESTAMPTZ NOT NULL DEFAULT now()`
  - [x] `account_report_entries`：`report_id BIGINT NOT NULL` FK → `account_reports(id)` · `reporter_id BIGINT NOT NULL` · `reason VARCHAR(24) NOT NULL` + CHECK（五值）· `detail VARCHAR(200)` · `created_at TIMESTAMPTZ NOT NULL DEFAULT now()`
  - [x] 索引 `idx_account_report_entries_report_reporter (report_id, reporter_id)`（供优先级公式聚合）
  - [x] 约束命名 `uq_` / `ck_` / `fk_`，索引 `idx_`（仓内多数派）
  - [x] 文件头中文块注释：story 号 + 「工单粒度 = 被举报账号」+「明细每次追加不覆盖」+「ddl-auto=validate，schema 归 Flyway。UTC。」
  - [x] **零回填**

- [x] **T2 · 实体与仓储**（AC1–AC4）
  - [x] `moderation/domain/AccountReport.java` + `AccountReportEntry.java`（照 `ContentReport` 的形制：**无 BaseEntity、无 Auditing、无 Lombok**；`protected` 无参构造 + 静态工厂；`@PrePersist`/`@PreUpdate` + `Instant.now()`；外键存裸 `Long`）
  - [x] `moderation/domain/AccountReportReason.java`：`SPAM / IMPERSONATION / HARASSMENT / VIOLATING_CONTENT / OTHER`，`@Enumerated(EnumType.STRING)` + 显式 `length`
  - [x] 仓储 `extends JpaRepository<T, Long>`，优先派生查询

- [x] **T3 · 提交接口**（AC4 / AC5 / AC9 / AC10 / AC11）
  - [x] `POST /api/v1/account-reports`（架构 §3 端点表）
  - [x] 请求 DTO：`reason`（必填枚举）+ `detail`（选 OTHER 时必填，≤200）+ 目标用户 id
  - [x] 当前用户取法照抄 `MeController.currentUserId(Jwt)` 那 4 行样板
  - [x] **工单 upsert**：`target_user_id` 存在则复用、不存在则建；已处理状态**翻回待处理**（AC9）
  - [x] **明细 append**（AC3），⚠️ **不要照抄 `exists` 前置短路**
  - [x] **同事务调 `social` 写服务建 `REPORT` 隐藏行**（AC5）
  - [x] 短窗去重（AC11）—— 窗口值写进 Completion Notes
  - [x] 不可举报自己（AC10）
  - [x] ⚠️ 参考既有 `ReportController` 返回 **202 ACCEPTED**，但本接口前端需要成功态，**返回体形态由 dev 定**（建议 200 + 空体或含「已举报」标记）

- [x] **T4 · 「已举报」标记下发**（AC8）
  - [x] 迷你主页接口（Story 1.1 已加可选 viewer）补一个「已举报」布尔
  - [x] ⚠️ **注意 `MiniProfileResponse` 是 record + 全局 Jackson `NON_NULL`** —— 新增 primitive `boolean` 会**永远出现在 JSON 里**，改变游客响应体的 key 集合。**须用装箱 `Boolean` 且游客时置 null**（靠 NON_NULL 省略），或另想不改游客契约的办法
  - [x] 既有 L1 测试 `MiniProfileControllerEndpointTest` 的 3 条游客断言**必须继续绿**

- [x] **T5 · 不动既有举报链路**（AC10）
  - [x] ⚠️ `moderation/service/ReportService.java` **一行不改**（含 `P0_REPORT_COUNT_THRESHOLD = 10` 与 `maybeApplyP0PreDisposal`）
  - [x] ⚠️ `content_reports` 表、`ReportReason` / `ReportStatus` 枚举**一律不动**

- [x] **T6 · 测试**（AC1–AC12）
  - [x] L1 继承 `ApiIntegrationTest`（`newUser()` / `userBearer(id)`，真本地 postgres）
  - [x] AC2 场景：12 人 / 甲 7 次 → 1 条工单 + 18 行明细
  - [x] AC3：两次不同类型 + 第二次带说明 → 两行都在、说明不覆盖
  - [x] **AC5 同事务**：制造隐藏写入失败 → 工单也回滚
  - [x] **AC5 交叉**：已存在 `BLOCK` 行时举报 → `BLOCK` 行字段（含时间戳）**逐一比对未变**
  - [x] AC9：工单已处理后再举报 → 翻回待处理、不新建
  - [x] AC10：不可举报自己 · **无自动预处置**（大量举报后内容仍 PUBLISHED）
  - [x] AC11：短窗内两次提交 → 只 1 行明细；超窗后 → 2 行
  - [x] 无 token → 401

- [x] **T7 · 台账回写**（AC1）
  - [x] `CROSS-STORY-DECISIONS.md` 表归属总表追加两张表，创建者 = 本 story

---

## Dev Notes

> ⚠️ 本节代码事实来自 CS 阶段实证扫描。

### 🔴 最大的风险：照抄既有内容举报

**既有 `ReportService.submit` 全文（要反着做的地方）：**

```java
@Transactional
public void submit(long postId, long reporterId, ReportReason reason) {
    if (!contentService.isVisible(postId)) {
        throw AppException.notFound("内容不存在");
    }
    if (reports.existsByPostIdAndReporterId(postId, reporterId)) {
        return; // 幂等：已举报过（隐藏已成立，计数/预处置状态不变）
    }
    try {
        reports.save(ContentReport.create(postId, reporterId, reason));
    } catch (DataIntegrityViolationException e) {
        return; // 并发撞唯一约束：本次未新增，预处置交由胜出线程处理，幂等吞掉。
    }
    maybeApplyP0PreDisposal(postId, reason);
}
```

**两处裸 `return` 的后果**：同一举报人换个原因再报 = **无效**；端点仍返回 202，**用户侧完全感知不到「你已经举报过」**，前端照样弹成功态。

**➜ 账号举报必须反过来**：每次都追加一行明细、保留每次的类型与说明。**AC11 的短窗去重是唯一允许的「吞掉」，且只针对秒级误触。**

---

### 既有的两条自动预处置（本 story 一行不改）

```java
static final long P0_REPORT_COUNT_THRESHOLD = 10;   // ⚠️ 硬编码在 Java，不在配置表，运营改不了

private void maybeApplyP0PreDisposal(long postId, ReportReason justReported) {
    boolean legalReport = justReported == ReportReason.ILLEGAL
            || reports.existsByPostIdAndReasonType(postId, ReportReason.ILLEGAL);
    long count = reports.countByPostId(postId);
    if (legalReport || count >= P0_REPORT_COUNT_THRESHOLD) {
        contentService.applyReportHoldIfPublished(postId);
    }
}
```

① `ILLEGAL` **单次即触发**；② 唯一举报人数 ≥ 10。**或**关系。命中 → `PUBLISHED → UNDER_REVIEW` + 写 `report_hidden_at`，**内容不删**。

**➜ 账号举报零自动预处置（AC10）**，且**这两条继续只作用于内容举报**。

---

### 既有 `ReportController` 的形制（可参考，但返回码要重定）

```java
@PostMapping("/api/v1/content-posts/{postId}/reports")
@ResponseStatus(HttpStatus.ACCEPTED)   // 202，空 body
public void report(@AuthenticationPrincipal Jwt jwt, @PathVariable long postId,
        @Valid @RequestBody ReportRequest req) {
    reportService.submit(postId, currentUserId(jwt), req.reasonType());
}
```

请求 DTO 只有一个字段：`record ReportRequest(@NotNull ReportReason reasonType)` —— **无补充说明、无 idempotency key**。
本 story 的 DTO 要多两个字段（目标用户 + 可选 detail）。

---

### ⚠️ `MiniProfileResponse` 加「已举报」字段的陷阱（AC8）

```java
public record MiniProfileResponse(
        String nickname, String avatarUrl, String signature,
        long postCount, boolean isDeactivated) {}
```

全局 `spring.jackson.default-property-inclusion: non_null`。
**新增一个 primitive `boolean` 会永远出现在 JSON 里**（primitive 不为 null）→ **游客响应体的 key 集合就变了**，而 Story 1.1 AC6 要求「游客行为一字不变」，既有 L1 测试 `MiniProfileControllerEndpointTest` 有 3 条断言守着。

**➜ 必须用装箱 `Boolean` 且游客时置 null**（靠 NON_NULL 省略），或另想办法。

---

### 实体与迁移惯例（照抄 `ContentReport` / `V91__init_id_cards.sql`）

- **无 `BaseEntity`、无 `@MappedSuperclass`、无 `@EnableJpaAuditing`**（全仓零命中）
- 时间戳自写 `@PrePersist` / `@PreUpdate` + `Instant.now()`，配 DB 侧 `DEFAULT now()`
- **无 Lombok**（手写 getter）· `protected` 无参构造 + 静态工厂 `create(...)` · 外键存裸 `Long xxxId`（**不用 `@ManyToOne`**）
- 枚举 `@Enumerated(EnumType.STRING)` + 显式 `length`；时间用 `java.time.Instant`
- 迁移：`TIMESTAMPTZ` 显式写 · 主键取 `BIGINT GENERATED BY DEFAULT AS IDENTITY`（较新惯例）· 约束前缀 `ck_`/`uq_`/`fk_`、索引 `idx_` · 全中文 `--` 注释 + 文件头块注释
- `Instant` → JSON **ISO-8601 字符串**

---

### 参考：既有举报队列的 N+1（账号举报别踩）

`AdminModerationService.queue(status)` 在循环内逐条调 `contentService.findSummary` + `reportService.countForPost` —— **典型 N+1**，靠 50 条硬上限勉强撑住。Epic 3 的统一视图若照抄会更糟（三表联合 + 优先级实时计算）。**本 story 的明细索引 `(report_id, reporter_id)` 就是为让 Epic 3 能一次聚合出「人数 / 次数 / 高频人数」而建的。**

### Project Structure Notes

**新增（NEW）**：
```
moderation/domain/AccountReport.java · AccountReportEntry.java · AccountReportReason.java
moderation/repository/AccountReportRepository.java · AccountReportEntryRepository.java
moderation/service/AccountReportService.java
moderation/web/AccountReportController.java          POST /api/v1/account-reports
moderation/dto/AccountReportRequest.java
db/migration/V102__init_account_reports.sql
```

**修改（UPDATE）**：
```
auth/web/MiniProfileController.java + MiniProfileResponse.java   加「已举报」标记（⚠️ 装箱 Boolean）
_bmad-output/implementation-artifacts/CROSS-STORY-DECISIONS.md   表归属回写
```

**⚠️ 不动**：`moderation/service/ReportService.java` · `content_reports` 表 · `ReportReason` / `ReportStatus` · 既有两条自动预处置。

### 依赖与时序

- **依赖 Story 1.1** 的表、只读端口与写服务。
- **本 story 交付后，Epic 1 建的六处过滤自动对举报隐藏生效**（AC6 是回归验证，不是新实现）。
- **Story 2.2 / 2.3 / 2.4 依赖本 story 的接口契约。**
- **Epic 3 依赖本 story 的两张表。**

### References

- [Source: _bmad-output/planning-artifacts/epics-v1.1.4.md#Story-2.1]
- [Source: _bmad-output/planning-artifacts/architecture-v1.1.4-delta.md#AD-1] 隐藏关系单表双来源 / 同事务
- [Source: _bmad-output/planning-artifacts/architecture-v1.1.4-delta.md#AD-2] 工单粒度 = 被举报账号
- [Source: _bmad-output/planning-artifacts/architecture-v1.1.4-delta.md#AD-12] 账号维度五类枚举
- [Source: _bmad-output/planning-artifacts/architecture-v1.1.4-delta.md#AD-17] 零自动预处置
- [Source: _bmad-output/planning-artifacts/v1.1.4/PRD-v1.1.4.md#§3.1] FR-58 全文
- [Source: _bmad-output/implementation-artifacts/v1.1.4/1-1-拉黑一个人他的帖子立刻从我首页消失服务端.md] 表结构与写服务契约

---

## Dev Agent Record

### Agent Model Used

claude-opus-5[1m]（Claude Code，本地 macOS · Temurin JDK 21 · Docker postgres+redis 在跑）

### Debug Log References

- `./mvnw -B -o test -Dtest=AccountReportIntegrationTest` → **21/21（L1，真 PostgreSQL）**
- `./mvnw -B -o test -Dtest=AccountReportTransactionIntegrationTest` → **1/1**（事务回滚，单独一个上下文）
- `./mvnw -B -o package`（全量）→ **1630 通过 / 0 失败 / 6 skipped**，含既有 `MiniProfileControllerEndpointTest` 的游客断言

### Completion Notes List

#### 交付内容（L0 + L1 全绿，本 story 无 L2）

- **AC1**：`V102__init_account_reports.sql` —— 工单表 `target_user_id` 唯一 + 明细表 + `idx_account_report_entries_report_reporter`。**零回填。**
- **AC2/AC3**：工单粒度 = 被举报账号；明细**每次追加、绝不覆盖**，**明细表刻意没有任何唯一约束**。
- **AC4**：`AccountReportReason` 新枚举五类（未复用内容维度的 `ReportReason`）；`OTHER` 必填 detail（≤200）。
- **AC5**：工单 upsert + 明细追加 + `source=REPORT` 隐藏行**同一事务**，有专门的回滚用例。**先拉黑后举报不碰 `BLOCK` 行**（id / created_at / updated_at 逐一比对未变）。
- **AC6**：举报后帖子详情对举报人 404、对他人照常 200；**迷你主页仍可进入**（唯一例外）。
- **AC7**：`DELETE /me/blocked-users/{id}` 之后 REPORT 行仍在；黑名单列表返回 `[]`。
- **AC8**：「已举报」由 REPORT 行派生，登录者 true/false、**游客那个键整个不存在**。
- **AC9**：已处置工单翻回 PENDING、不新建、`first_reported_at` 不刷新、处置人/时刻清空。
- **AC10**：不可举报自己；**15 个人举报后内容仍 PUBLISHED**（零自动预处置）；`ReportService` 一行未改。
- **AC11**：**去重窗口 = 5 秒**（`AccountReportService.DUPLICATE_WINDOW`）。
- **AC12**：`detail` 不进任何日志，controller 里就地写了警告注释；无新中间件。

#### 关键实现决定（review 时值得看）

1. **`AccountReportStatus` 是新枚举，没复用内容侧的 `ReportStatus`。** 账号侧多一条内容侧没有的路径（已处置 → 又被举报 → 翻回待处置），共用一个枚举迟早互相绑架。三个值同名（PENDING/RESOLVED/DISMISSED），**展示层第三档叫「无需处置」**（C-103），数据层值不改。
2. **端口加了第三个方法 `isReported`**（只认 REPORT），而不是让 `auth` 去读 moderation 的表。AC8 明写「该标记由 `source=REPORT` 行是否存在派生」——那行归 `social`，所以出口就该在 `social.read`。端口 Javadoc 已写明三个方法的口径互不替代。
3. **`MiniProfileResponse.reported` 用装箱 `Boolean`、游客置 null**（story 已预警的陷阱）。有一条用例断言游客响应体里**这个键根本不存在**；写成 primitive 会当场红。
4. **非 OTHER 的 `detail` 静默丢弃，不报 422**。AC4 写的是「不接受也不要求」；前端只在选了「其他」时才展示输入框，为一个用户根本看不到的字段回一个校验错误，除了制造线上噪声没有别的用处。有用例钉住「传了也不落库」。
5. **命中 5 秒去重时，隐藏关系那一步照样走一遍**（幂等）。否则会有这么一条缝：第一次提交写隐藏失败被整体回滚、用户 2 秒内重试，结果因去重跳过了明细**也**跳过了隐藏。

#### ⚠️ 测试里「把时间往前拨」不是绕过被测逻辑

AC2 要求「甲报 7 次 → 7 行明细」，AC11 要求「5 秒内重复提交只算一次」——**这两条在测试里直接打架**：连着提交 7 次会被去重合成 1 次。

处理办法是每次提交前用 `UPDATE ... created_at - interval '1 minute'` 把**已有**明细的时间前拨，模拟「这几次发生在不同时刻」。**没有为了测试给生产代码开配置口子**（窗口仍是写死的 5 秒常量），去重窗口本身另有三条用例正面验证（窗口内只 1 行 / 出窗口后追加 / 不同举报人互不影响）。

#### 事务回滚用例为什么单独一个类

`@MockitoSpyBean` 会让 Spring 另起一个上下文。放进主用例集会拖累那 21 条的启动，所以拆成 `AccountReportTransactionIntegrationTest`。它验的是：让隐藏关系那一步抛异常 → **工单也回滚**，不出现「有工单没隐藏」（用户举报完还天天看见他）或「有隐藏没工单」（内容消失了但运营永远看不到）。

#### 给下游的接口契约

```
POST /api/v1/account-reports        → 204 No Content，无响应体
body: { "targetUserId": <long>, "reason": <五类之一>, "detail": <仅 OTHER 必填，≤200> }
错误：422 校验（举报自己 / OTHER 未填说明 / 说明超长）· 400（理由不在五类里）· 401（未登录）
```
**不下发任何工单信息**——举报对被举报人完全不可见，对举报人也不回显「他已被几人举报」这类内部数据。
迷你主页新增 `reported`（登录者 true/false，游客无此键）。**Story 2.2 / 2.3 / 2.4 按这个契约接前端。**

#### 未动（AC10 / T5 的硬要求，已核实）

`moderation/service/ReportService.java` 一行未改（含 `P0_REPORT_COUNT_THRESHOLD = 10` 与 `maybeApplyP0PreDisposal` 两条自动通道）· `content_reports` 表 · `ReportReason` / `ReportStatus` 枚举。

### File List

**新增**
```
petgo-backend/src/main/resources/db/migration/V102__init_account_reports.sql
petgo-backend/src/main/java/com/tailtopia/moderation/domain/AccountReport.java
petgo-backend/src/main/java/com/tailtopia/moderation/domain/AccountReportEntry.java
petgo-backend/src/main/java/com/tailtopia/moderation/domain/AccountReportReason.java
petgo-backend/src/main/java/com/tailtopia/moderation/domain/AccountReportStatus.java
petgo-backend/src/main/java/com/tailtopia/moderation/repository/AccountReportRepository.java
petgo-backend/src/main/java/com/tailtopia/moderation/repository/AccountReportEntryRepository.java
petgo-backend/src/main/java/com/tailtopia/moderation/service/AccountReportService.java
petgo-backend/src/main/java/com/tailtopia/moderation/web/AccountReportController.java
petgo-backend/src/main/java/com/tailtopia/moderation/dto/AccountReportRequest.java
petgo-backend/src/test/java/com/tailtopia/moderation/AccountReportIntegrationTest.java              L1 · 21 例
petgo-backend/src/test/java/com/tailtopia/moderation/AccountReportTransactionIntegrationTest.java   L1 · 1 例
```

**修改**
```
petgo-backend/src/main/java/com/tailtopia/social/read/UserHideRelationReader.java      加 isReported（只认 REPORT）
petgo-backend/src/main/java/com/tailtopia/social/read/UserHideRelationReaderImpl.java
petgo-backend/src/main/java/com/tailtopia/auth/dto/MiniProfileResponse.java            加装箱 Boolean reported
petgo-backend/src/main/java/com/tailtopia/auth/web/MiniProfileController.java          游客置 null
_bmad-output/implementation-artifacts/CROSS-STORY-DECISIONS.md                          两张表归属 + 两套举报的对照表
```

---

## Change Log

| 日期 | 变更 |
|---|---|
| 2026-08-16 | 实现完成（`bmad-dev-story`）：T1–T7 全部完成。**Flyway 实际取 V102**（与预期一致，V101 之后顺延）。L0+L1 全绿：全量 1630 通过，`AccountReportIntegrationTest` 21/21 + 事务回滚 1/1 真打 PostgreSQL。**去重窗口定为 5 秒**。「已举报」经 `social.read` 端口新增的 `isReported` 派生；`MiniProfileResponse.reported` 用装箱 Boolean，游客键不存在（有用例钉死）。Status → review。 |
| 2026-08-15 | 由 `bmad-create-story` 生成。AC 承接 epics-v1.1.4 Story 2.1（含 IR-2 新增 AC11 防重复提交、IR-3 AC10 不设频率限制、IR-4 AC5 写入侧约束）。Dev Notes 实证补出：既有 `submit` 两处裸 return 的精确代码 · `P0_REPORT_COUNT_THRESHOLD=10` 硬编码不可配 · **`MiniProfileResponse` 加 boolean 会破坏游客契约（须用装箱 Boolean）**。 |
