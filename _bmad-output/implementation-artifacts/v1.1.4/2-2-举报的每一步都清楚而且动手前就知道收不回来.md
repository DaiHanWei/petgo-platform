---
baseline_commit: 071247d9
epic: 2
story: 2.2
version: V1.1.4
source_epics: _bmad-output/planning-artifacts/epics-v1.1.4.md
architecture: _bmad-output/planning-artifacts/architecture-v1.1.4-delta.md
depends_on: Story 1.2（「⋯」菜单已建）· Story 2.1（接口契约）
---

# Story 2.2: 举报的每一步都清楚，而且动手前就知道收不回来

Status: ready-for-dev

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

> **所属**：V1.1.4 Epic 2 第二个 Story（**纯前端 Flutter**）。**B 泳道 7 屏（B1–B7）+ A 泳道 1 屏（A3 已举报态）**。
> **交付**：账号举报抽屉全流程 · 五类账号维度选项 · 「提交后无法撤销」· 六个表单态。
> ⚠️ **本 story 的最大诱惑是复用 `report_sheet.dart`** —— 视觉规格该抄，**但枚举必须新建、失败处理必须重写**。
> ⚠️ **既有 `report_sheet.dart` 的失败分支只把按钮解禁、不给任何提示**，用户看到的就是「按了一下，按钮又能按了」。**这是本 story 明确不该照抄的地方。**

## Story

As a **准备举报某个账号的用户**,
I want **看到专为账号设计的举报理由、必要时能补充说明、并在提交前知道这不可撤销**,
so that **我选得准、说得清，也不会因为三次点击就造成自己后悔的结果**。

## Acceptance Criteria

> **验证层**：**L0**（`flutter analyze` / `flutter test`）· **L2**（模拟器视觉，本 story 主战场）。
> 云端只跑 L0；**L2 留本地**。

### AC1 — 菜单举报项接通（UX-DR1，A2）（L2）

**Given** Story 1.2 已在迷你卡建好「⋯」菜单
**When** 用户点击菜单里的「举报」
**Then** 弹出账号举报抽屉（**纯接通已有菜单项，不改菜单结构**）
**And** 「举报」与「拉黑」仍**并列、不分主次**，各自副标题按 PRD 定稿口径

### AC2 — 已举报态菜单（UX-DR3，A3）（L2）

**Given** 用户已举报过该账号
**When** 打开「⋯」菜单
**Then** 举报项显示为「已举报 / 点击可再次举报」（紫色，**明示可再点**，不能被误认为禁用态）
**And** **拉黑项此时仍可点、不置灰**

> ⚠️ 举报后再拉黑**不是多余操作** —— 拉黑带来一个举报没有的效果：**禁止再进入对方主页**。不得以「已举报」为由置灰或隐藏拉黑项。
> ✅ 「已举报」状态来自 Story 2.1 AC8 下发的服务端标记，**不是前端会话态**。

### AC3 — 举报表单（UX-DR6/7，B1/B2/B3）（L2）

**Given** 举报抽屉打开
**When** 渲染
**Then** 标题为**「举报这个账号」**（对象是账号不是内容，**与既有 `reportTitle`「Laporkan konten ini」是不同的键**）
**And** 展示**账号维度五类**选项，副标题**复用既有 `reportSubtitle`**「选择最合适的理由」
**And** **未选中任何类型时提交按钮禁用**
**And** 仅选中「其他」才展开必填文本框，**其余四类不展开**，文本框右下角显示 **0 / 200** 计数
**And** 选中态 = **紫边 `#845EC9` + 紫浅底 `#F8F2FF` + 实心 radio**，逐项取自既有实现

> ⚠️ **`AppColors.mint` 实际是品牌紫 `#845EC9`，不是绿色**（历史遗留命名）。选中底色是 `AppColors.cream2` = `#F8F2FF`。

### AC4 — ⚠️「提交后无法撤销」出现在全部表单态（UX-DR8）（L2）

**Given** 举报抽屉处于任一表单态
**When** 渲染提交按钮上方
**Then** **B1 未选 / B2 其他展开 / B3 已选 / B4 提交中 / B6 失败 / B7 重复举报——六个态全部**显示一行小字「提交后无法撤销」
**And** 该文案**只说不可撤销、不说会隐藏**（不破坏「成功态不提隐藏」那条取舍，A-A27）

> ⚠️ UI 稿的待改清单原文只列了 B1/B2/B3，**但 B4 提交中与 B6 失败态同样是表单态**，实际稿件已六个态全加。**按六个走。**

### AC5 — 提交中（UX-DR9，B4）（L2）

**Given** 用户点击提交
**When** 请求进行中
**Then** **选项与取消按钮一并禁用**（防重复提交），抽屉**不关闭**，提交按钮内显示转圈

> ✅ 既有 `report_sheet.dart` 的做法是 `_submitting = true` → 两个按钮 `onPressed: null`（第 108 / 117 行），**但没有 loading 指示器**。本 story 要求加转圈。
> ⚠️ 前端禁用是第一道防线，**服务端还有短窗去重**（Story 2.1 AC11）—— 两者都要有。

### AC6 — 成功态（UX-DR10，B5）（L2）

**Given** 提交成功
**When** 抽屉切换
**Then** **在抽屉内原地切换为成功态**（✅ + 「已收到你的举报」+「我们会尽快处理」+「关闭」），**不用 Toast、不自动收起**
**And** 点「关闭」→ **两层一并收起**（迷你卡 + 确认抽屉），回到点开迷你卡之前的页面
**And** 中途取消（点「取消」未提交）**只收起第二层**，回到迷你卡本身
**And** 成功态**刻意不提「内容将被隐藏」**

> **已知不一致，产品复核后接受**：既有内容举报的 `reportHiddenToast`「Kamu tidak akan melihat konten ini lagi」（你将不再看到这条内容）是**会告知**的 —— **举报帖子告知、举报账号不告知**。这是实锤差异，不是疏漏。
> ✅ 成功态文案可**复用既有键** `reportDoneTitle`（Laporan diterima）+ `reportDoneBody`（Kami akan segera memprosesnya.）—— 与 PRD 定稿完全对应。

### AC7 — 失败态（UX-DR11，B6）（L2）

**Given** 提交失败
**When** 处理错误
**Then** **抽屉不关闭、不清空已选类型与已填说明**，用户可直接重试
**And** 弹 Toast「举报失败，请刷新网络状态重试或联系客服」，Toast **放在顶部**避开抽屉

> ⚠️ **既有 `report_sheet.dart` 的 catch 分支只把提交中状态复位、不给任何提示**：
> ```dart
> } catch (_) {
>   if (mounted) setState(() => _submitting = false);
> }
> ```
> 用户看到的就是「按了一下，按钮又能按了」。**本 story 必须补上提示，不要照抄。**
> ⚠️ **「Toast 放顶部」全项目无既有实现** —— `showAppToast` 位置硬编码在底部（`Positioned(bottom: mq.padding.bottom + mq.viewInsets.bottom + 90)`），且是全站唯一 toast 实现、改它影响 30+ 处调用。**处理方式见 Dev Notes「Toast 位置的取舍」。**

### AC8 — 重复举报（UX-DR12，B7）（L2）

**Given** 用户已举报过该账号，再次进入举报抽屉
**When** 渲染
**Then** 副标题替换为紫底说明块「你已举报过该用户，再次举报会让我们知道问题仍在持续，我们会加快处理」
**And** 类型**重新选、不预填上次的选择**（每次的类型都要独立留存，第一次选骚扰、第二次选仿冒正是问题升级的证据）

### AC9 — 文案与视觉规格（L0/L2）

**Given** 本 story 的全部用户可见文案
**Then** 能复用既有 `app_id.arb` 键的一律复用：`commonClose`（Tutup）· `commonCancel`（Batal）· `reportSubtitle` · `reportDoneTitle` + `reportDoneBody` · `reportSubmit`（Kirim Laporan）
**And** 新增键按 UI 稿文案清单落地，**「提交后无法撤销」已有印尼语草稿**（`Laporan tidak dapat dibatalkan setelah dikirim`）可直接用
**And** 抽屉视觉规格逐项照搬既有实现：内边距 22/12/22/28 · 手柄 36×4 · 原因卡圆角 13 / 边框 1.5px / 内距 14·12 / 间距 10 · 提交按钮圆角 14 / 垂直内距 14 · 取消按钮描边 1.5px
**And** ⚠️ 新增键**不放在 `commonClose` 那段重复区附近**（该键在两份 ARB 里各重复定义两次）
**And** 不命中 `microcopy_rules_test.dart` 的 `_bannedWords`
**And** `flutter analyze` / `flutter test` 全绿

---

## Tasks / Subtasks

### 🟩 前端子任务（petgo_app / Flutter）

- [ ] **T1 · 账号举报理由枚举**（AC3）
  - [ ] 新建 `features/social/domain/account_report_reason.dart`（或同域既有分层位置）
  - [ ] 五值 + `wire` 字段，照抄既有 `features/content/domain/report_reason.dart` 的形制：
        `spam('SPAM')` / `impersonation('IMPERSONATION')` / `harassment('HARASSMENT')` / `violatingContent('VIOLATING_CONTENT')` / `other('OTHER')`
  - [ ] ⚠️ **不要复用 `ReportReason`**（那是内容维度五类 `ILLEGAL/MISINFO/INAPPROPRIATE/HARASSMENT/OTHER`，只有「骚扰」和「其他」能对上）

- [ ] **T2 · 账号举报抽屉**（AC3–AC8）
  - [ ] 新建组件，**视觉规格逐项照抄 `report_sheet.dart`**（数值见 AC9）
  - [ ] 六个表单态全部带「提交后无法撤销」（AC4）
  - [ ] 「其他」展开必填框 + `0 / 200` 计数（AC3）
  - [ ] 提交中：选项 + 取消一并禁用 + **按钮内转圈**（AC5，比既有多一个转圈）
  - [ ] 成功态原地切换（AC6）—— 照抄既有 `_done = true` 切视图的做法
  - [ ] 失败：**不清空 + Toast**（AC7）—— ⚠️ **不要照抄既有的静默 catch**
  - [ ] 重复举报副标题替换 + 类型不预填（AC8）
  - [ ] 入口签名照抄 `openReport(context, ref, postId, {VoidCallback? onReported})` 的形态，成功态关闭时 `Navigator.pop(true)` → 触发回调

- [ ] **T3 · 接通迷你卡菜单**（AC1 / AC2）
  - [ ] Story 1.2 已建的「⋯」菜单里，把「举报」项从不渲染改为渲染并接通
  - [ ] 已举报态：菜单项文案换成「已举报 / 点击可再次举报」（紫色，**非禁用态**）
  - [ ] ⚠️ **拉黑项不置灰、不隐藏**

- [ ] **T4 · 数据层**（AC6 / AC7）
  - [ ] `api_paths.dart` 注册 `accountReports = '$base/account-reports'`
  - [ ] repository 调 `POST /api/v1/account-reports`（Story 2.1 契约）
  - [ ] 401 由 `AuthInterceptor` 处理，repository 不自理；错误以 `DioException` 抛给控制器（照既有 repository 文件头纪律）

- [ ] **T5 · i18n**（AC9）
  - [ ] 新增键补 `app_id.arb` + `app_en.arb`（模板是 `app_en.arb`）
  - [ ] 印尼语取 UI 稿文案清单（五类理由 / 标题 / placeholder / 重复举报说明 / 失败文案 / 不可撤销）
  - [ ] 复用既有键：`commonClose` `commonCancel` `reportSubtitle` `reportDoneTitle` `reportDoneBody` `reportSubmit`
  - [ ] ⚠️ 新增键别放 `commonClose` 重复区附近

- [ ] **T6 · 测试**（AC9）
  - [ ] widget 测试：六个表单态都有「提交后无法撤销」
  - [ ] widget 测试：仅「其他」展开文本框；未选中时提交禁用
  - [ ] widget 测试：失败**不清空已选**
  - [ ] widget 测试：已举报态菜单项文案 + **拉黑项仍可点**
  - [ ] `flutter analyze` / `flutter test` 全绿

---

## Dev Notes

> ⚠️ 本节代码事实来自 CS 阶段实证扫描。

### 🔴 三处「能复用」与「不能复用」的界线

| 项 | 复用？ |
|---|---|
| **视觉规格**（内边距 / 手柄 / 圆角 / 边框 / 按钮） | ✅ **逐项照抄** `report_sheet.dart` |
| **成功态切换机制**（`_done = true` 原地切视图 + `Navigator.pop(true)` 触发回调） | ✅ 照抄 |
| **提交中禁用**（两个按钮 `onPressed: null`） | ✅ 照抄，但**要加转圈**（既有没有） |
| **理由枚举** | ❌ **必须新建** —— 既有是内容维度 |
| **失败处理** | ❌ **必须重写** —— 既有是静默 catch |
| **标题键** | ❌ 既有 `reportTitle` = 「Laporkan konten ini」（内容），账号要新键 |

---

### 既有 `report_sheet.dart` 的三态处理（照抄前先看清）

```dart
Future<void> _submit() async {
  if (_selected == null || _submitting) return;
  setState(() => _submitting = true);
  try {
    await widget.ref.read(detailRepositoryProvider).submitReport(widget.postId, _selected!.wire);
    if (mounted) setState(() => _done = true); // 切 sheet 内成功态（不再 toast）
  } catch (_) {
    if (mounted) setState(() => _submitting = false);   // ⚠️ 只解禁，不提示、不区分错误
  }
}
```

**提交中**：`_submitting = true` → 提交按钮（108 行）与取消按钮（117 行）都 `onPressed: null`，**无 loading 指示器**。
**成功**：`_done = true` → sheet 内切成功态，**不弹 toast**；成功态关闭按钮 `Navigator.pop(true)` → `openReport` 触发 `onReported`。
**失败**：⚠️ 见上，**这是本 story 不该照抄的地方**。

---

### 视觉规格（逐项照搬，禁自创数值 —— UX-DR22）

| 项 | 值 | `report_sheet.dart` 行 |
|---|---|---|
| 抽屉内边距 | `EdgeInsets.fromLTRB(22, 12, 22, 28)` | 78 |
| 拖拽手柄 | `36 × 4`，`margin bottom 16`，`AppColors.line`，`radius 9999` | 63–71 |
| 标题 | `16 / w700 / AppColors.ink` | 91 |
| 标题→副标题 | `SizedBox(height: 4)` | 92 |
| 副标题 | `12 / AppColors.textSecondary` | 94 |
| 副标题→卡组 | `SizedBox(height: 16)` | 95 |
| 原因卡圆角 | `BorderRadius.circular(13)` | 138 / 143 |
| 原因卡内距 | `EdgeInsets.symmetric(horizontal: 14, vertical: 12)` | 140 |
| 原因卡间距 | `bottom: 10` | 133 |
| 原因卡边框 | `1.5px`，选中 `AppColors.mint` / 未选 `AppColors.line` | 144–145 |
| 选中态底色 | `AppColors.cream2` | 142 |
| 单选点 | `Icon(radio_button_checked/unchecked, size: 20)` + `SizedBox(width: 10)` | 149–151 |
| 原因文字 | `13 / AppColors.ink` | 154 |
| 提交按钮 | `FilledButton`，`AppColors.mint`，`vertical 14`，`radius 14`，文字 `14/w700` | 100–110 |
| 提交↔取消间距 | `SizedBox(height: 8)` | 113 |
| 取消按钮 | `OutlinedButton`，`side 1.5px AppColors.line`，`vertical 13`，`radius 14` | 116–123 |
| 成功态排版 | 手柄 → 8 → `Text('✅', fontSize: 40)` → 10 → 标题 `15/w700` → 6 → 正文 `13` → 20 → 按钮 | 164–193 |

⚠️ **`AppColors.mint = Color(0xFF845EC9)`（品牌紫，非绿）** · `cream2 = 0xFFF8F2FF` · `popRed = 0xFFF0425A` · `line = 0xFFE6E6E6` · `ink = 0xFF2E2A45`。
`colors.dart` 文件头有强制护栏：**业务/组件文件禁止硬编码 `Color(0x...)`**。

---

### ⚠️ Toast 位置的取舍（AC7）

**实证**：`shared/widgets/app_toast.dart` 的 `showAppToast` 挂 root Overlay，**位置硬编码在底部**：

```dart
final bottom = mq.padding.bottom + mq.viewInsets.bottom + 90;
return Positioned(left: 24, right: 24, bottom: bottom, child: IgnorePointer(...));
```

**全仓只有这一个 toast 实现，无顶部变体、无位置参数**，改它会影响 30+ 处调用。

**➜ 本 story 的处理**：
- **优先方案**：给 `showAppToast` **加一个可选的位置参数**（如 `bool top = false`），默认 false 时行为与现在完全一致 —— 既满足 AC7 的「放顶部避开抽屉」，又不影响既有 30+ 处调用。
- **若不加参数**：用底部 toast，但**必须在 L2 验收时确认它不与抽屉视觉打架**（toast 挂 root Overlay 所以 z 序在抽屉之上，不会被遮挡，但位置可能叠在抽屉上）。
- ⚠️ **无论选哪个，都要在 Completion Notes 记录**，因为 Story 1.2 的 C4（拉黑失败）面临同样问题，两处口径要一致。

---

### 既有 l10n 键（可直接复用）

| key | `app_id.arb` | `app_en.arb` |
|---|---|---|
| `commonClose` | `Tutup` | `Close` |
| `commonCancel` | `Batal` | `Cancel` |
| `reportSubtitle` | `Pilih alasan yang paling sesuai` | `Choose the most relevant reason` |
| `reportSubmit` | `Kirim Laporan` | `Submit report` |
| `reportDoneTitle` | `Laporan diterima` | `Report received` |
| `reportDoneBody` | `Kami akan segera memprosesnya.` | `We'll process it shortly.` |
| `reportTitle` | `Laporkan konten ini` ← **内容维度，账号要新键** | `Report this content` |
| `reportHiddenToast` | `Kamu tidak akan melihat konten ini lagi` ← **账号举报刻意不用** | — |

⚠️ **`commonClose` 在两份 ARB 里各重复定义了两次**（值相同故未暴雷）—— **新键别放在第二段重复区附近**，以免被误删。
引用方式 **`AppLocalizations.of(context).xxx`（无 `!`）**；模板是 `app_en.arb`；生成物在 analyzer.exclude，**不要手改**。

---

### 既有 `ReportReason` 的形制（新枚举照抄这个结构）

`features/content/domain/report_reason.dart`：5 个值 `illegal('ILLEGAL')` / `misinfo('MISINFO')` / `inappropriate('INAPPROPRIATE')` / `harassment('HARASSMENT')` / `other('OTHER')`，字段 `final String wire`。

**账号维度五值**（与后端 `AccountReportReason` 对齐）：`SPAM` / `IMPERSONATION` / `HARASSMENT` / `VIOLATING_CONTENT` / `OTHER`。

---

### Project Structure Notes

**新增（NEW）**：
```
features/social/domain/account_report_reason.dart
features/social/data/account_report_repository.dart
features/social/presentation/account_report_sheet.dart
core/network/api_paths.dart                          注册 accountReports
```

**修改（UPDATE）**：
```
shared/widgets/mini_profile_sheet.dart（或 1.2 建的菜单组件）  举报项由不渲染改为渲染并接通 + 已举报态
lib/l10n/app_id.arb · app_en.arb                              新增文案键
shared/widgets/app_toast.dart                                 ⚠️ 仅当采「加可选位置参数」方案时
```

**⚠️ 不动**：`features/content/presentation/report_sheet.dart`（内容举报，一行不改）· `features/content/domain/report_reason.dart`。

### 依赖与时序

- **依赖 Story 1.2** 的「⋯」菜单（本 story 是**纯加一项，非返工**）。
- **依赖 Story 2.1** 的接口契约与「已举报」标记。
- **Story 2.3 接本 story 成功态的收尾行为**（点关闭后移除该作者全部卡片）。
- **Story 2.4 复用本 story 的举报抽屉**（黑名单页行内「⋯」走同一套）。

### References

- [Source: _bmad-output/planning-artifacts/epics-v1.1.4.md#Story-2.2]
- [Source: _bmad-output/planning-artifacts/architecture-v1.1.4-delta.md#AD-12] 账号维度五类枚举
- [Source: _bmad-output/planning-artifacts/v1.1.4/ui-v1.1.4.html] B1–B7 · A3
- [Source: _bmad-output/planning-artifacts/v1.1.4/PRD-v1.1.4.md#§3.1] 举报类型 / 二次确认 / 成功失败态
- [Source: _bmad-output/implementation-artifacts/v1.1.4/2-1-举报一个账号他的东西立刻从我眼前消失.md] 接口契约
- [Source: _bmad-output/implementation-artifacts/v1.1.4/1-2-在他的卡片上点两下就能把他拉黑界面.md] 「⋯」菜单与 toast 取舍

---

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

---

## Change Log

| 日期 | 变更 |
|---|---|
| 2026-08-15 | 由 `bmad-create-story` 生成。AC 承接 epics-v1.1.4 Story 2.2。Dev Notes 实证补出：既有 `_submit()` 三态的精确代码（点明失败分支不该照抄）· 完整视觉规格行号表 · **Toast 顶部无既有实现的取舍方案**（与 Story 1.2 C4 同一问题，口径须一致）· `commonClose` 重复定义陷阱。 |
