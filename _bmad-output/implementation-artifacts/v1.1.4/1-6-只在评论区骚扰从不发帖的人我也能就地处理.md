---
baseline_commit: 071247d9
epic: 1
story: 1.6
version: V1.1.4
source_epics: _bmad-output/planning-artifacts/epics-v1.1.4.md
architecture: _bmad-output/planning-artifacts/architecture-v1.1.4-delta.md
depends_on: Story 1.2（AC4 的三分支一致性回归待其完成；实现本身可并行）
---

# Story 1.6: 只在评论区骚扰、从不发帖的人，我也能就地处理

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

> **所属**：V1.1.4 Epic 1 第六个 Story（**纯前端 Flutter**）。
> **交付**：评论区（一级评论 + 二级回复）的头像/昵称接入 `showMiniProfile`（**UI 稿 A6，1 屏**）。
> ⚠️ **本 story 是本版本最大闭环缺口的补丁** —— 影子评论 / R1 / R2 / 通知抑制**全部为评论区骚扰设计**，而举报与拉黑的发起入口原先在评论区**根本不存在**。
> ⚠️ **代码量极小（挂一个回调），但它决定了整个版本的主场景能不能用。**

## Story

As a **在评论区被某人反复骚扰的用户**,
I want **直接点评论里的头像就能弹出那张卡**,
so that **我不用等他哪天发一条帖子、再去首页把他找出来才能拉黑他**。

## Acceptance Criteria

> **验证层**：**L0**（`flutter analyze` / `flutter test`）· **L2**（模拟器视觉）。
> 云端只跑 L0；**L2 留本地**。

### AC1 — 评论区接入迷你卡（UX-DR5，A6）（L2）

**Given** 用户在内容详情页的评论区
**When** 点击**一级评论或二级回复**的头像或昵称
**Then** 弹出迷你主页卡，与首页卡片作者、详情页作者入口**完全一致**（同一张卡、同一套「⋯」菜单）
**And** **不在评论行内另设「⋯」，不使用任何滑动手势**

> **为什么这条最要紧：** 本版本的影子评论、R1、R2、通知抑制**全部是为「评论区骚扰」设计的**，而发起入口原先在评论区**根本不存在** —— 实证确认 `showMiniProfile` 在全 App 只有 `home_page.dart:190`（Feed 卡片作者）与 `content_detail_page.dart:213`（详情页作者）两个触发点。**一个只在评论区骚扰、从不发帖的账号，用户既举报不了也拉黑不了。**

### AC2 — 已注销分支复核（L2）

**Given** 评论作者已注销
**When** 用户点击其头像
**Then** **不弹卡**（NFR-8），与首页侧行为一致
**And** 不给任何 Toast（与「已拉黑」「网络失败」两种有提示的情况区分开）

> ⚠️ 这个判断此前**只在首页侧考虑过**（`masonry_card.dart:91` 有 `onTap: (item.authorDeleted || onAuthorTap == null) ? null : onAuthorTap` 的前置拦截）。**评论列表与迷你卡是两条独立的读路径**，须显式验证。
> ✅ **实证：`Comment` 模型已有 `authorDeleted` 字段**（`comment.dart:47`，`(json['authorDeleted'] ?? false) as bool`），前端有足够信息做判断，**无需后端补字段**。

### AC3 — 已隐藏用户不构成新分支但仍需判断（L2）

**Given** 评论作者已被当前用户隐藏
**When** 评论列表渲染
**Then** 其评论已被 R1 隐藏、头像根本不会出现
**And** 实现时**不因此省掉判断** —— 两条读路径独立，不得假设另一条一定生效

### AC4 — 三处入口行为一致性回归（L2）

**Given** 首页卡片作者、详情页作者、评论区头像三处入口
**When** 分别点击同一个用户
**Then** 弹出的卡片内容、菜单项、以及**已拉黑/已注销/网络失败三种不弹卡分支**表现完全一致

> **⚠️ 验收时序说明（非验收条件，不占 AC 编号）**
> **AC4 里的「已拉黑」与「网络失败」两个分支由 Story 1.2 AC3 交付**，本 story 只负责让评论区头像调用 `showMiniProfile`。
> → 本 story 的**实现**可与 1.2 完全并行（它只是给评论行挂上调用），但 **AC4 的三分支一致性回归必须待 1.2 完成后进行**。
> → **本 story 单独验收时只验 AC1 / AC2 / AC3 三条**；AC4 标记为「待 1.2 完成后回归」。**这不是本 story 的缺失。**
> ⚠️ epics「Epic 依赖关系」写的「1.2 / 1.5 / 1.6 三条前端可并行」指的是**实现可并行**，不代表**验收无先后** —— 本条即是那个例外。

### AC5 — 护栏（L0）

**Given** 本 story 的实现
**Then** 未新增第三方依赖、未新增 l10n 键（复用既有）
**And** 未改 `showMiniProfile` 本身（那归 Story 1.2）
**And** `flutter analyze` / `flutter test` 全绿

---

## Tasks / Subtasks

### 🟩 前端子任务（petgo_app / Flutter）

- [x] **T1 · 评论行接入 `showMiniProfile`**（AC1 / AC2）
  - [x] 改 `features/content/presentation/comment_section.dart` 的 `_CommentTile`（一级）与回复行（二级）
  - [x] 头像与昵称包 `GestureDetector`，`onTap: () => showMiniProfile(context, ref, comment.authorId)`
  - [x] ⚠️ **已注销时整体去掉点击手势**（照抄 `content_detail_page.dart:211-215` 的 `if (detail.authorDeleted) return row;` 模式，或 `masonry_card.dart:91` 的 `onTap: null` 模式）
  - [x] ⚠️ **不在评论行内另设「⋯」、不加任何滑动手势**（UI 稿 A6 明确，且全项目零 `Dismissible`/`Slidable`）
  - [x] 埋点入口参数标记为 `comment`（Story 4.1 AC2 的四个入口之一；**本 story 只保证参数传递，实际上报在 4.1 收口**）

- [x] **T2 · 一致性核对**（AC4）
  - [x] 与 `home_page.dart:190`、`content_detail_page.dart:213` 两处调用形态对齐
  - [x] ⚠️ 若 Story 1.2 已把 `showMiniProfile` 签名改为带 `{VoidCallback? onBlocked}`，**本处也要接上**（评论区拉黑成功后的列表刷新行为需与产品确认，见 Dev Notes「一处待确认」）

- [x] **T3 · 测试**（AC5）
  - [x] widget 测试：点评论头像弹卡
  - [x] widget 测试：已注销评论作者**不弹卡且无 Toast**
  - [x] `flutter analyze` / `flutter test` 全绿

---

## Dev Notes

> ⚠️ 本节代码事实来自 CS 阶段实证扫描。

### 实证：评论区确实不弹卡，且模型字段已够用

**`showMiniProfile` 的全部生产调用点（2 处）：**

| 文件 | 行 | 上下文 |
|---|---|---|
| `features/content/presentation/home_page.dart` | **190** | `onAuthorTap: (item) => showMiniProfile(context, ref, item.authorId)` |
| `features/content/presentation/content_detail_page.dart` | **213** | `GestureDetector(key: ValueKey('detailAuthorRow'), onTap: () => showMiniProfile(context, ref, detail.authorId), child: row)` |

**评论列表未接入** —— `comment.dart` 已有 `authorId` / `authorDeleted` 字段但从未调用 `showMiniProfile`。**与 PRD §3.1 的核查结论一致。**

**➜ 本 story 就是把这个调用挂上去，代码量极小。**

---

### 已注销用户的渲染惯例（照抄这三步）

`content_detail_page.dart:175-215`（最完整）：

```dart
final name = detail.authorDeleted ? l10n.feedDeletedUser : (detail.authorNickname ?? l10n.feedDeletedUser);
...
LetterAvatar(
  url: detail.authorDeleted ? null : detail.authorAvatarUrl,
  name: name,
  deleted: detail.authorDeleted,
  size: 36,
),
...
// 作者点击触发迷你卡（Story 3.8）：注销作者不可点（NFR-8）。
if (detail.authorDeleted) return row;
return GestureDetector(
  key: const ValueKey('detailAuthorRow'),
  onTap: () => showMiniProfile(context, ref, detail.authorId),
  child: row,
);
```

`LetterAvatar` 的 `deleted: true` 渲染默认 person 图标而非首字母（`letter_avatar.dart:38-46`）。

**评论侧已有同款处理**：`comment_section.dart:228` 有一行同样的 `name` 三元兜底，且 `_canDelete` 里 `!c.authorDeleted` 参与判定（152-157 行）。**➜ 本 story 只需补「点击手势」这一步。**

l10n 键 **`feedDeletedUser`** 已存在（id: `Pengguna terhapus` / en: `Deleted user`），**复用即可，不新增键**。

---

### `showMiniProfile` 内部还有第二道防线

```dart
if (profile.isDeactivated) return; // 已注销：不触发迷你卡
```

所以即使前端漏判，服务端返回 `isDeactivated=true` 时也不会弹卡。**但不要因此省掉 AC2 的前端判断** —— 漏判会让用户点了之后经历一次无谓的网络往返再什么都不发生，体验上与「网络失败」不可区分。

---

### ⚠️ 一处待确认：评论区拉黑成功后，列表怎么刷新

Story 1.2 会把 `showMiniProfile` 改为带 `{VoidCallback? onBlocked}` 回调，Feed 与详情页两处分别做「移除该作者卡片」。

**但评论区拉黑成功后该怎么办，PRD / UI 稿 / epics 都没写**：
- 被拉黑者的评论会被 R1 隐藏（Story 1.3 服务端已保证），但**那是下次拉取时**；
- 当前这一屏的评论列表**不会自动消失**。

**候选处理**：① 刷新评论列表（`ref.invalidate` 评论 provider）；② 乐观移除该作者的评论行；③ 什么都不做，等用户自己下拉。

**本 story 倾向 ①（刷新评论列表）** —— 与 Story 2.3「举报后立即兑现在当前屏」的产品取向一致，且实现最简单、无乐观状态一致性风险。
⚠️ **但这属于 PRD 未定义的行为，实现前请与产品确认一次。** 若产品选 ③，也要在 story 的 Completion Notes 里记下来，避免日后当 bug 报。

---

### Project Structure Notes

**修改（UPDATE，改前先读全文）**：
```
features/content/presentation/comment_section.dart    一级评论行 + 二级回复行的头像/昵称接入 showMiniProfile
```

**⚠️ 不动**：`shared/widgets/mini_profile_sheet.dart`（改造归 Story 1.2）· `letter_avatar.dart` · l10n（复用 `feedDeletedUser`）。

**新增**：仅 widget 测试。

### 依赖与时序

- **实现可与 Story 1.2 完全并行**（本 story 只是挂调用）。
- **AC4 的验收须待 Story 1.2 完成**（三分支中的两个由 1.2 交付）。
- 若 1.2 先改了 `showMiniProfile` 签名，本处需同步接上 `onBlocked`。

### References

- [Source: _bmad-output/planning-artifacts/epics-v1.1.4.md#Story-1.6]
- [Source: _bmad-output/planning-artifacts/architecture-v1.1.4-delta.md#AD-15] 迷你卡三处既有行为改动（第 1 条即本 story）
- [Source: _bmad-output/planning-artifacts/v1.1.4/ui-v1.1.4.html] A6 评论区入口
- [Source: _bmad-output/planning-artifacts/v1.1.4/PRD-v1.1.4.md#§3.1] 2026-08-15 校验订正：评论区必须接入迷你卡
- [Source: _bmad-output/implementation-artifacts/v1.1.4/1-2-在他的卡片上点两下就能把他拉黑界面.md] `showMiniProfile` 改造与三分支

---

## Dev Agent Record

### Agent Model Used

claude-opus-5[1m]（Claude Code，本地 macOS · Flutter 3.44.8）

### Debug Log References

- `flutter analyze` → **No issues found**
- `flutter test test/social/comment_mini_profile_entry_test.dart` → **5/5**
- `flutter test`（全量）→ **786 通过**

### Completion Notes List

#### 交付内容

- **AC1**：一级评论与二级回复的**作者名**接入 `showMiniProfile` —— 与首页卡片作者、详情页作者是同一张卡、同一套「⋯」菜单。评论行内**没有另设「⋯」、没有任何滑动手势**。
- **AC2**：已注销作者 → 传 `null` 去掉点击手势，**不弹卡、不给任何 Toast**。有用例断言此时**连一次网络请求都不发**（`showMiniProfile` 内部虽有第二道防线，但那要先走一次往返，用户看到的「点了没反应」与网络失败无法区分）。
- **AC3**：判断没有省——两条读路径独立，不假设 R1 一定已经把他的评论过滤掉了。
- **AC5**：无新依赖、无新 l10n 键（复用 `feedDeletedUser`）、未改 `showMiniProfile` 本身。

#### ✅ 评论行的头像已补上（2026-08-16 用户拍板）

产品决定：**补**。评论行现在是「30px 头像 + 右侧文字块」（UI 稿 A6 的形状），头像与作者名
**共用同一个手势**（热区连成一片，不再只有一行 12.5px 的小字可点）；已注销时头像走
`LetterAvatar(deleted: true)` 的默认 person 态且同样不可点。数据取本就存在却从未被渲染过的
`Comment.authorAvatarUrl`，**没有新增任何字段或接口**。

同时确认：**「评论区拉黑成功后重拉评论列表」按方案①保留**（下方那条待确认已结）。

下面这段保留为记录 ——

#### ⚠️（已解决）一处 CS 阶段扫描没发现的事实：评论行**原本根本没有头像**

story 的 T1 写的是「头像与昵称包 `GestureDetector`」，UI 稿 A6 画的评论行也带 30px 头像。**但实际代码里的评论行从来没有头像** —— `_CommentTile` 只渲染「作者名 + 正文 + 回复/删除」三段，`Comment` 模型虽有 `authorAvatarUrl` 字段却从未被渲染过。

第一版只挂了入口（作者名可点），把「要不要补头像」作为产品决定提出 —— **同日拍板：补**，已实现（见上）。
留这段是为了记住一件事：**UI 稿画了的东西不等于代码里有**，A6 这张图是照着「理想中的评论行」画的。

#### ✅（已拍板）一处 PRD 未定义的行为：评论区拉黑成功后怎么刷新

**「评论区拉黑成功后，当前这一屏的评论列表怎么办」PRD / UI 稿 / epics 都没写。** 三个候选：① 重拉评论列表 ② 乐观移除该作者的评论行 ③ 什么都不做，等用户自己下拉。

**本 story 取 ①**（`onBlocked: _reload`，复用既有的「静默重拉首屏、不闪骨架」方法）。理由：与 Story 2.3「举报后立即兑现在当前屏」的产品取向一致；没有乐观移除的一致性风险；实现是一行。有端到端用例断言「拉黑成功后确实又拉了一次」。

**2026-08-16 用户拍板：就用①「重拉一次评论列表」。** 此条已定，不再是待确认项。

#### 两个手势的优先级（容易被后人改坏）

评论行**整行**的既有 `onTap` 是「回复这条评论」（用户反馈后加的）。作者名上新挂的 `GestureDetector` 嵌在它内部，**命中测试内层优先** —— 点名字弹卡、点正文回复，互不打架。有专门用例钉住「点正文不会误弹卡」，谁把内层挪出去或改成 `behavior: deferToChild` 都会红。

#### AC4 待回归（story 已写明，非本 story 缺失）

AC4 的「已拉黑 / 网络失败」两个分支由 **Story 1.2** 交付，本 story 只负责让评论区调用 `showMiniProfile`。三处入口现在调用的是**同一个函数**，一致性在实现上已经成立；**L2 逐屏对比待本地**（三处入口 × 三种不弹卡分支）。

#### ⚠️ L2 待本地模拟器验收

| 项 | 要看什么 |
|---|---|
| **A6** | 评论区新补的 30px 头像与右侧文字的对齐/间距；一级评论与二级回复（左缩进）两种缩进下的观感；点头像与点名字弹卡都要试 |
| **三入口一致性** | 首页卡片作者 / 详情页作者 / 评论区作者，同一个人弹出的卡片、菜单、三种不弹卡分支表现是否一致 |
| **拉黑后** | 从评论区拉黑成功 → 两层弹层收起 → 评论列表重拉，观感是否突兀（列表会整体刷一下） |

### File List

**新增**
```
petgo_app/test/social/comment_mini_profile_entry_test.dart   5 例
```

**修改**
```
petgo_app/lib/features/content/presentation/comment_section.dart   作者名接入 showMiniProfile（已注销去手势）+ 拉黑成功重拉列表
```

**未动（刻意）**：`shared/widgets/mini_profile_sheet.dart`（改造归 Story 1.2）· `letter_avatar.dart`（直接复用，未改）· l10n（复用 `feedDeletedUser`，零新增键）· `Comment` 模型（`authorAvatarUrl` 本就有，只是此前没渲染）

---

## Change Log

| 日期 | 变更 |
|---|---|
| 2026-08-16 | 用户拍板两条：**① 补评论行头像**（30px，UI 稿 A6 形状，与作者名共用手势；已注销走默认 person 态且不可点）**② 拉黑后重拉评论列表**（维持方案①）。两条待确认项均已结。全量 `flutter test` 788 通过。 |
| 2026-08-16 | **L2 视觉验收完成**（见 `L2-视觉验收报告.md`）：**A6 通过**——评论行为「左头像 + 右列（昵称 / 正文 / 操作行）」，与 UI 稿 A6 一致（头像已按产品决定补上）；自己的评论只有「回复 + 删除」，不出现举报入口。⚠️ 「已举报 / 已拉黑」两个不弹卡分支在真实产品里**无路径可达**（隐藏会把评论一并抹掉），详见报告。 |
| 2026-08-16 | 实现完成（`bmad-dev-story`）：T1–T3 完成。L0 全绿（analyze 0 · 全量 786 通过 · 新增 5 例）。⚠️ **实证订正 story 的一处前提：评论行根本没有头像**（UI 稿 A6 画了、代码里从来没有），本 story 只挂入口未补头像，是否补留给产品定。⚠️ 「评论区拉黑成功后列表怎么办」PRD 未定义，取 Dev Notes 倾向的①重拉列表。**L2 待本地**。Status → review。 |
| 2026-08-15 | 由 `bmad-create-story` 生成。AC 承接 epics-v1.1.4 Story 1.6（含 IR-9 验收时序说明），补 AC5 护栏。**Dev Notes 提出一处 PRD 未定义的行为待产品确认**：评论区拉黑成功后当前屏评论列表如何刷新。 |
