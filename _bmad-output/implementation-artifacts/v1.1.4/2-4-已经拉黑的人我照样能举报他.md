---
baseline_commit: 071247d9
epic: 2
story: 2.4
version: V1.1.4
source_epics: _bmad-output/planning-artifacts/epics-v1.1.4.md
architecture: _bmad-output/planning-artifacts/architecture-v1.1.4-delta.md
depends_on: Story 1.5（行内「⋯」布局位已预留）· Story 2.2（举报抽屉）
---

# Story 2.4: 已经拉黑的人，我照样能举报他

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

> **所属**：V1.1.4 Epic 2 第四个 Story（**纯前端 Flutter**）。**D 泳道 1 屏（D9）**。
> **交付**：黑名单页每行的行内「⋯」→ 举报入口（在 Story 1.5 预留的布局位上渲染并接通）。
> ⚠️ **本 story 代码量很小，但它堵的是一个真实的死路**：拉黑后进不去对方迷你主页，**而那是举报的唯一入口** —— 两条一叠，拉黑就等于永久放弃举报该账号的能力。

## Story

As a **先拉黑了骚扰者、冷静下来想举报他的用户**,
I want **在黑名单页那一行直接举报他**,
so that **我不会因为「拉黑后进不去他主页」而永远失去举报他的机会**。

## Acceptance Criteria

> **验证层**：**L0**（`flutter analyze` / `flutter test`）· **L1**（后果一致性）· **L2**（模拟器视觉）。

### AC1 — 行内「⋯」举报入口（UX-DR21，D9）（L2）

**Given** Story 1.5 已在黑名单页每行建好「⋯」按钮位（**布局已预留、按钮未渲染**）
**When** 用户点击某行的「⋯」
**Then** 弹出菜单，含「举报」一项
**And** 走 **Story 2.2 的同一套举报抽屉**（同样的五类选项、同样的二次确认、同样的「提交后无法撤销」）
**And** **不使用任何滑动手势**（右滑撞系统返回、左滑会误触「解除拉黑」）
**And** 「解除拉黑」仍是**常驻按钮**，两个热区分开、互不误触

> **为什么必须有这个入口**：FR-94 生效范围第 4 条禁止发起方再进入被拉黑用户的迷你主页，**而迷你主页是举报的唯一入口**。两条一叠，拉黑就等于永久放弃举报该账号的能力 —— 而**骚扰者恰恰是最容易先被拉黑的那类账号**（用户被骚扰的第一反应是拉黑图清净，冷静下来想举报时路已经没了），结果是**越恶劣的账号越不容易被举报到运营手里**，与 FR-58 的立项理由直接冲突。
> ✅ **实证支持不用手势**：全项目 `Dismissible` / `Slidable` / `flutter_slidable` **零命中**，站内本就没有任何滑动删除的列表行。

### AC2 — 已举报状态同样展示（L2）

**Given** 该用户已被举报过
**When** 黑名单页渲染该行
**Then** 昵称后带「已举报」小标签（服务端持久化状态，来自 Story 2.1 AC8）
**And** 菜单里的举报项**仍可点**，允许重复举报

### AC3 — 从这里发起的举报后果一致（L1）

**Given** 用户从黑名单页举报了某人
**When** 提交成功
**Then** **同样产生一条不可解除的 `source=REPORT` 隐藏关系**
**And** 因该用户本就在黑名单中、内容已不可见，**用户感知不到差异**
**And** 该行**仍留在黑名单页**（它含 `BLOCK` 层），解除拉黑后才移除

> ⚠️ **不要在这里做「举报后移除该行」** —— 该行是因为 `BLOCK` 才在这个页面上的，举报不改变 `BLOCK`（Story 2.1 AC5 已钉死写入侧不碰 `BLOCK` 行）。

### AC4 — 埋点来源预留（L0）

**Given** 从黑名单页发起的举报
**When** 调用举报接口与前端埋点
**Then** **操作入口标记为 `blocklist`**（实际打点上报在 Epic 4 收口，本 story 只保证来源参数被正确传递）

> 这个数字直接验证「拉黑后仍需举报入口」这个设计判断：**长期为 0 说明该入口可以撤掉**；量不小则说明原来的设计确实堵死了一条重要路径。

### AC5 — 护栏（L0）

**Given** 本 story 的实现
**Then** 未新增第三方依赖
**And** 举报抽屉**完全复用 Story 2.2 的组件**，不另写一套
**And** 「举报」菜单项文案**复用 Story 2.2 已建的键**（与 A2 迷你卡菜单同一文案）
**And** `flutter analyze` / `flutter test` 全绿

---

## Tasks / Subtasks

### 🟩 前端子任务（petgo_app / Flutter）

- [x] **T1 · 渲染行内「⋯」按钮**（AC1）
  - [x] 在 Story 1.5 预留的布局位上渲染按钮（1.5 只留了位置、未渲染）
  - [x] ⚠️ **不要调整行内间距** —— 1.5 已按有「⋯」的布局预留好了
  - [x] 点击弹菜单，含「举报」一项

- [x] **T2 · 菜单样式**（AC1）
  - [x] 可照抄 `content_detail_page.dart:247-323` 的 `_showMoreSheet`：`showModalBottomSheet` + `shape: RoundedRectangleBorder(borderRadius: BorderRadius.vertical(top: Radius.circular(24)))` + `padding fromLTRB(20, 12, 20, 16)` + 手搓手柄（36×4、margin bottom 14）+ 一行 `InkWell`（emoji + `SizedBox(width: 10)` + 文字 `14/w600`，`Container(padding: vertical 14, 下边框 AppColors.line2)`）+ 左对齐 `TextButton(commonCancel)`
  - [x] ⚠️ **本处是底部抽屉即可**，与迷你卡「⋯」的向上弹浮层不同 —— 那是因为迷你卡本身是底部抽屉、向下弹会溢屏（C-76），黑名单页是普通列表页，没有这个约束

- [x] **T3 · 接通举报抽屉**（AC1 / AC3 / AC4）
  - [x] 调 Story 2.2 建好的账号举报抽屉组件，传入目标用户 id
  - [x] 传入来源标记 `blocklist`（AC4）
  - [x] 成功后**不移除该行**（AC3）—— 只需刷新该行的「已举报」标签

- [x] **T4 · 已举报标签**（AC2）
  - [x] 该标签由 Story 1.5 的列表接口下发（`GET /api/v1/me/blocked-users` 已含），本 story 只需在举报成功后刷新
  - [x] 菜单里的举报项**不置灰**

- [x] **T5 · 测试**（AC5）
  - [x] widget 测试：点「⋯」弹菜单、含举报项
  - [x] widget 测试：举报成功后该行**仍在列表中**
  - [x] widget 测试：**无任何滑动手势**
  - [x] `flutter analyze` / `flutter test` 全绿

---

## Dev Notes

> ⚠️ 本节代码事实来自 CS 阶段实证扫描。

### 与 Story 1.5 的交接

Story 1.5 AC3 明确：**「行布局为右侧「⋯」预留位置，但在本 story 交付时该按钮不渲染」** —— 理由是 1.5 独立交付时「⋯」尚无任何动作可执行，渲染出来就是一个点开是空菜单的死按钮。

**➜ 本 story 就是把它渲染出来并接通。** ⚠️ **布局间距已经按「有「⋯」」预留好了，不要再调**。

---

### 菜单形态：底部抽屉即可，不必照搬迷你卡的向上弹

| | 迷你卡「⋯」（Story 1.2） | 黑名单行内「⋯」（本 story） |
|---|---|---|
| 宿主 | **底部抽屉**（`showModalBottomSheet`） | 普通列表页 |
| 弹出方向 | ⚠️ **必须向上弹**（向下会溢屏底并盖住头像，C-76） | **底部抽屉即可**，无此约束 |

**可照抄的样板**：`content_detail_page.dart:247-323` 的 `_showMoreSheet` —— 全仓唯一的「⋯」菜单实现（`IconButton(Icons.more_horiz)` → `showModalBottomSheet` → 手写 `InkWell` 行）。

⚠️ **全项目 `PopupMenuButton` / `PopupMenuItem` 零命中** —— 不要引入这个 Material 组件，与站内风格不一致。

---

### 站内的「行内次要动作」惯例

`comment_section.dart:296-304`（`_CommentTile`）—— 每条评论右下角一个 14px 删除图标：

```dart
if (canDelete) ...[
  const SizedBox(width: AppSpacing.md),
  GestureDetector(
    key: ValueKey('deleteComment_${comment.id}'),
    onTap: onDelete,
    child: Icon(Icons.delete_outline_rounded, size: 14, color: AppColors.textTertiary),
  ),
],
```

即：**行内单一动作 = 小图标按钮 + `showConfirmSheet` 二次确认；多动作才用底部抽屉。**
本 story 是「一个动作（举报）」，但产品明确要「⋯」形态（与 A2 迷你卡同一交互模式，用户学一次即可 —— C-79），**按产品定的走**。

---

### 为什么不用滑动手势（C-79 原文）

> **右滑撞系统返回手势** —— iOS 从左边缘右滑 = pop 返回，Android 10+ 手势导航同理，而黑名单页是从设置 push 进来的；
> **左滑会误触解除** —— 滑动区正好压在「Buka Blokir」上，同一片像素滑得短是点击（解除）、滑得长才是露出（举报）。
> **选「⋯」的决定性理由是与 A2 迷你卡的「⋯」是同一个交互模式** —— 用户学一次就够，且两个热区分开、无误触，可发现性也比隐藏手势强。

✅ **实证支持**：`Dismissible` / `Slidable` / `flutter_slidable` 在 `lib/` 与 `pubspec.yaml` 中**零命中**（只匹到 `barrierDismissible` / `isDismissible` 这类 dialog/sheet 参数）。**站内没有任何滑动删除的列表行** —— 引入会是新依赖 + 新交互范式。

---

### 为什么举报不移除该行（AC3 的关键）

该行出现在黑名单页是因为它含 **`BLOCK`** 层。举报只是**另外**加一条 `REPORT` 行（Story 2.1 AC5 已钉死「写 `REPORT` 行不得修改 `BLOCK` 行的任何字段」）。

**➜ 举报成功后：**
- 该行**仍在列表中** ✅
- 只是昵称后多了「已举报」标签
- **位置也不变**（Story 1.5 AC2 钉死了排序取 `BLOCK.created_at`，且追加 `REPORT` 行不得改变位置）

⚠️ **不要写「举报后移除该行」的逻辑** —— 那会让用户以为拉黑也一起解除了。

---

### Project Structure Notes

**修改（UPDATE）**：
```
features/social/presentation/blocked_users_page.dart   渲染行内「⋯」+ 接通举报抽屉
```

**⚠️ 不动**：Story 1.5 已定的行布局间距 · Story 2.2 的举报抽屉组件（**复用，不改**）· 「解除拉黑」常驻按钮。

**新增**：仅 widget 测试。

### 依赖与时序

- **依赖 Story 1.5** 的行内「⋯」布局位与「已举报」标签数据。
- **依赖 Story 2.2** 的举报抽屉组件。
- **Epic 4（Story 4.1）收口本 story 传递的 `blocklist` 来源参数。**

### References

- [Source: _bmad-output/planning-artifacts/epics-v1.1.4.md#Story-2.4]
- [Source: _bmad-output/planning-artifacts/v1.1.4/ui-v1.1.4.html] D9 行内 ⋯ → 举报入口
- [Source: _bmad-output/planning-artifacts/v1.1.4/决策日志.md#C-79] 行内「⋯」不用滑动手势（推翻 C-78 左滑方案）
- [Source: _bmad-output/planning-artifacts/v1.1.4/PRD-v1.1.4.md#§3.2] 黑名单管理 · 为什么必须有举报入口
- [Source: _bmad-output/implementation-artifacts/v1.1.4/1-5-我能看到自己拉黑了谁随时放人出来.md] 布局预留与排序约束
- [Source: _bmad-output/implementation-artifacts/v1.1.4/2-2-举报的每一步都清楚而且动手前就知道收不回来.md] 举报抽屉组件

---

## Dev Agent Record

### Agent Model Used

claude-opus-5[1m]（Claude Code，本地 macOS · Flutter 3.44.8）

### Debug Log References

- `flutter analyze` → **No issues found**
- `flutter test test/social/blocked_users_page_test.dart` → **20/20**（既有 15 例 + 本 story 新增 5 例）
- `flutter test`（全量）→ **813 通过**

### Completion Notes List

#### 交付内容（L0 全绿，L2 待本地）

- **AC1**：在 Story 1.5 预留的位置上渲染「⋯」（**行内间距一点没动**），点开是**底部抽屉**菜单，含「举报」一项 → 走 Story 2.2 的**同一套抽屉**（同样五类、同样的「提交后无法撤销」，有用例断言）。**无任何滑动手势**，且有用例断言「⋯」与「解除拉黑」两个热区**矩形不重叠**。
- **AC2**：已举报过 → 昵称后带「已举报」标签；菜单里的举报项**照样可点**，点进去直接是重复举报态。
- **AC3**：举报成功后该行**仍留在列表里**，只是多一个标签 —— 有专门用例钉住。
- **AC4**：来源标记 `blocklist` 已随举报抽屉一路传到发起处。
- **AC5**：无新依赖；举报抽屉完全复用、一行未改；菜单文案复用 Story 2.2 的 `accountReportAction`。

#### 为什么这个入口不是「可有可无的小功能」

拉黑之后就**进不去对方的迷你主页**了（FR-94 生效范围第 4 条），而迷你主页是举报的**唯一**入口。两条一叠，**拉黑等于永久放弃举报他的能力**。

而骚扰者恰恰是最容易先被拉黑的那类账号 —— 用户被骚扰的第一反应是拉黑图清净，冷静下来想举报时路已经没了。结果是**越恶劣的账号越不容易被举报到运营手里**，与 FR-58 的立项理由直接冲突。这个入口堵的就是这条死路。已把这段理由写进代码注释，免得日后有人觉得「黑名单页上一个「⋯」没什么用」把它删了。

#### 三处实现选择

1. **菜单用底部抽屉，没照搬迷你卡的「向上弹浮层」。** 迷你卡那个之所以要向上弹，是因为**它自己就是个底部抽屉**、向下弹会溢出屏底并盖住头像（C-76）。黑名单页是普通列表页，没有这个约束 —— 照抄反而会引入一个没必要的浮层定位逻辑。样板取自 `content_detail_page` 的 `_showMoreSheet`。
2. **「已举报」标签的即时点亮用本地集合，不 `ref.invalidate`。** 后者会把整页打回 loading 闪一次骨架屏，而用户只是给某一行加了个标签。标签本身是服务端持久化的（Story 2.1 AC8），下次进页强制刷新以服务端为准。这与本页解除拉黑那处的处理是同一口径。
3. **新增 `AccountActionEntry` 枚举**（`mini_profile` / `blocklist` / `report_flow` / `comment` 四个值，与 Story 4.1 AC2 的口径一致），顺手也给迷你卡那条路径传上了 `mini_profile`。**本 story 不做任何上报**（4.1 统一收口），只保证参数传到了发起处 —— 参数本身已被抽屉持有，4.1 在提交成功处读它即可。

#### 刻意没做的事

- **没有「举报后移除该行」**。这一行出现在本页是因为它含 `BLOCK`，举报只是**另外**加一条 `REPORT` 关系、碰都不碰 `BLOCK` 行（Story 2.1 AC5 已在写入侧钉死）。移除它会让用户以为拉黑也一起解除了。
- **没调行内间距**（1.5 已按「有「⋯」」的布局预留好）· **没改举报抽屉**（完全复用）· **没动「解除拉黑」常驻按钮**。

#### ⚠️ L2 待本地模拟器验收

| 项 | 要看什么 |
|---|---|
| **D9** | 「⋯」加进来之后整行的疏密：头像 / 昵称+时间 / 解除按钮 / 「⋯」四段的间距是否还舒服，**长昵称时会不会把「⋯」挤出去** |
| **误触** | 「解除拉黑」与「⋯」两个热区实机点一遍（自动化只验了矩形不重叠，手指宽度是另一回事） |
| **菜单** | 底部抽屉弹出的观感与详情页那个「⋯」是否一致（同一套样板） |
| **标签** | 举报成功回到列表的一瞬间「已举报」标签点亮，**行的位置不能动** |

### File List

**新增**
```
petgo_app/lib/features/social/domain/account_action_entry.dart   四个操作入口（Story 4.1 收口用）
```

**修改**
```
petgo_app/lib/features/social/presentation/blocked_users_page.dart      渲染行内「⋯」+ 菜单 + 接通举报抽屉 + 标签即时点亮
petgo_app/lib/features/social/presentation/account_report_sheet.dart    openAccountReport 加 entry 参数
petgo_app/lib/shared/widgets/mini_profile_sheet.dart                    传 entry: miniProfile
petgo_app/test/social/blocked_users_page_test.dart                      新增 5 例
```

**未动（刻意）**：Story 1.5 已定的行布局间距 · Story 2.2 的举报抽屉组件本体 · 「解除拉黑」常驻按钮

---

## Change Log

| 日期 | 变更 |
|---|---|
| 2026-08-16 | **L2 视觉验收通过**（见 `L2-视觉验收报告.md`）：**D9 通过**——行内「⋯」弹出的底部抽屉只有「举报 + 取消」，不重复出现「拉黑」；进去是与迷你卡入口**同一套**举报抽屉（标题、五类理由、不可撤销小字完全一致）。举报成功关闭后该行**仍在列表、位置不变**，只多出紫色「已举报」标签，昵称自动截断给标签让位无溢出。⚠️ 同一路径上暴露出解除拉黑文案只读服务端快照的缺陷，已在 Story 1.5 修复。 |
| 2026-08-16 | 实现完成（`bmad-dev-story`）：T1–T5 全部完成。L0 全绿（analyze 0 · 全量 813 通过 · 新增 5 例）。菜单按 Dev Notes 取**底部抽屉**（不照搬迷你卡的向上弹）。新增 `AccountActionEntry` 枚举承载四个入口，本 story 传 `blocklist`、迷你卡传 `mini_profile`，**上报由 Story 4.1 收口**。**L2 D9 待本地**。Status → review。 |
| 2026-08-15 | 由 `bmad-create-story` 生成。AC 承接 epics-v1.1.4 Story 2.4，补 AC5 护栏。Dev Notes 实证补出：**本处菜单用底部抽屉即可、不必照搬迷你卡的向上弹**（那是宿主是抽屉才有的约束）· 全项目零滑动手势/零 `PopupMenuButton` · AC3「举报不移除该行」的理由链。 |
