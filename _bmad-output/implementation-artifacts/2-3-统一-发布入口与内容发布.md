# Story 2.3: 统一「+」发布入口与内容发布

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a **用户**,
I want **通过单一「+」入口发布日常分享/专业科普/成长日历快乐时刻**,
so that **我能便捷地记录与分享，无需在多个入口间纠结**。

> 本 Story **首次建立 `content_posts` 表**（含 type/danger 等弹性字段），是 Epic 3 Feed/详情/互动的数据根，也是 2.4/2.6 时间线与名片读「成长日历快乐时刻」的来源。本 Story 只做"统一发布入口 + 三类内容发布 + 成长日历绑定宠物 + B/C 灰置 + 仅重传失败件 + Idempotency-Key"，**不做 Feed 流、不做详情、不做评论/点赞**（Epic 3）。
>
> **依赖前序**：1.1（脚手架/content 模块）、1.3（JWT）、1.6（用户状态 A/B/C，决定成长日历可选性）、2.1（图片压缩/直传公开桶/权限工具）、2.2（pet_profiles，成长日历绑定宠物）。

## Acceptance Criteria

> **验证层标注**：**L0 静态** · **L1 集成/运行时**（Docker+postgres+redis）· **L2 端到端/外部凭证**（真实 OSS/STS、真机相册）。
> 核心落 **L0/L1**；图片直传真实 OSS 已在 2.1 的 L2 覆盖，本 Story 用其产出 URL 落库（L1）。

### AC1 — 统一 Publish Compose 单页发布

**Given** 登录用户点击「＋」
**When** 打开 Publish Compose 全屏 bottom sheet（UX-DR16）
**Then** 同页内含内容类型 Segment（全部/日常分享/成长日历/科普）→ 图片上传区 → 文字输入区 → 发布按钮，类型选择不超过一步、无独立页面跳转（FR-12）
**And** 文字上限 1000 字符实时计数（如「剩余 800 字符」），超出禁止发布；图片每条 ≤9 张（单张 ≤10MB），V1 不支持视频
> 验证层：**L0**（bottom sheet 布局/Segment 单步切换/字数实时计数/≤9 张/≤10MB 前端校验 widget 测试；服务端 Bean Validation 1000 字/≤9 张 MockMvc）。「全部」为浏览态语义，发布时须落具体类型（见 Dev Notes）。

### AC2 — 成长日历绑定宠物 + B/C 灰置

**Given** 选择「成长日历快乐时刻」
**When** 状态 A 用户发布
**Then** 需绑定宠物，发布后该条进入宠物档案时间线（写 `content_posts` 表，本故事创建该表，含 type/danger 等弹性字段）
**And** 状态 B/C 用户的「成长日历」选项灰置不可选，悬浮提示「需要先创建宠物档案」+ 跳转创建入口
> 验证层：**L1**（成长日历 post 写库带 `pet_id` 关联 + type=GROWTH_MOMENT，2.4 时间线据此读取，需 postgres）+ **L0**（B/C 用户 Segment 灰置 + 悬浮提示 + 跳创建入口 widget 测试；服务端校验「成长日历必须有档案否则 422/409」）。

### AC3 — 仅重传失败件 + 内存草稿

**Given** 发布上传过程中网络中断/服务器错误
**When** 用户点击「重试」
**Then** 仅重传失败文件，已填文字在当前 session 内存保留；用户退出编辑页则内容清空（V1 无持久草稿）（NFR-10）
> 验证层：**L0**（上传状态机：每图独立 状态(待传/传中/成功/失败)，重试仅作用失败件；退出 sheet 清空内存草稿 — widget/provider 测试，用 mock uploader）。

### AC4 — 公开提示 + Idempotency-Key

**Given** 任意发布
**When** 提交成功
**Then** 内容默认全平台公开，发布确认页提示「发布后内容将对所有用户公开展示」，请求带 Idempotency-Key 去重
> 验证层：**L0**（确认提示文案 i18n + 客户端生成 `Idempotency-Key`）+ **L1**（同 Idempotency-Key 重复提交只落一条 — `IdempotencyService` + Redis）。

---

## Tasks / Subtasks

> 三段组织。**前后端均衡偏前端**（Publish Compose 交互 + 上传状态机为重点），后端建 `content_posts` 表 + 发布端点 + 幂等。建议：后端 → 前端 → 联调。

### 🟦 后端子任务（petgo-backend / `com.petgo.content`）

- [ ] **B1. Flyway 建 `content_posts` 表（含弹性字段）** (AC: 1, 2)
  - [ ] `db/migration/V<n>__init_content.sql`：`content_posts(id bigserial pk, author_id bigint not null, type varchar not null, pet_id bigint null, text varchar(1000), image_urls jsonb, danger_level varchar null, status varchar default 'PUBLISHED', deleted_at timestamptz null, created_at timestamptz default now(), updated_at timestamptz)`。
  - [ ] `type ∈ {DAILY, GROWTH_MOMENT, KNOWLEDGE}`（UPPER_SNAKE）；`danger_level` 为弹性字段（成长日历可关联问诊评级，本 Story 普通发布留 null）；图 URL 列表用 **JSONB**。
  - [ ] 索引：`idx_content_posts_author_id`、`idx_content_posts_pet_id`、`idx_content_posts_type`、`idx_content_posts_created_at`（Feed/时间线倒序读）；`fk_content_posts_author`、`fk_content_posts_pet`(可空)。`deleted_at` 软删（架构命名表要求）。注释标注注销级联路径。
- [ ] **B2. 实体 / DTO / Repository** (AC: 1, 2)
  - [ ] `content/domain/ContentPost`(@Entity, JSONB 映射 image_urls)；`content/repository/ContentPostRepository`。
  - [ ] DTO record：`ContentPostCreateRequest{type, petId?, text(≤1000), imageUrls(≤9)}`、`ContentPostResponse{...}`。Bean Validation：text ≤1000、imageUrls size ≤9、type 枚举。
- [ ] **B3. 发布服务 + 成长日历校验 + 幂等** (AC: 2, 4)
  - [ ] `ContentService.publish(authorId, req)`：type=GROWTH_MOMENT → **必须有 pet_id 且属当前用户档案**（经 profile service 接口校验，**禁直接 join profile 表**），否则 422/409；普通 type 不绑宠物。
  - [ ] `ContentApiController` `POST /api/v1/content-posts`（JWT，201，**`Idempotency-Key` 头 + `IdempotencyService`(Redis) 去重**，写端点限流）。author_id 取自 JWT。
  - [ ] 默认 `status=PUBLISHED`、全平台公开（无可见性字段=公开）。

### 🟩 前端子任务（petgo_app / `lib/features/content` + `shared/widgets/publish_fab`）

- [ ] **F1. 「＋」入口 + Publish Compose bottom sheet** (AC: 1)
  - [ ] `shared/widgets/publish_fab`（Tab Bar 凸起「+」，1.2 已建外壳，此处接发布动作）触发 → 打开全屏 `PublishComposePage`(bottom sheet, UX-DR16)：Segment(全部/日常分享/成长日历/科普) + 图片上传区 + 文字区 + 发布按钮，**单页无跳转**。
- [ ] **F2. 类型 Segment + B/C 灰置** (AC: 1, 2)
  - [ ] 据用户状态：B/C 时「成长日历」Segment **灰置不可选**，长按/点击悬浮提示「需要先创建宠物档案」+ 按钮跳 2.2 创建入口。「全部」为浏览语义——发布前须选定一个可发布类型（默认日常分享）。
- [ ] **F3. 文字实时计数 + 图片区（≤9/≤10MB）** (AC: 1)
  - [ ] 文字 1000 上限「剩余 N 字符」，超出禁用发布；图片区复用 2.1 选图/权限/压缩/直传公开桶，≤9 张、单张压到 ≤10MB；无视频。
- [ ] **F4. 上传状态机：仅重传失败件 + 内存草稿** (AC: 3)
  - [ ] provider 持每图独立状态；发布=直传图→拿 URL→提交 post。网络/服务端错误 → 「重试」**仅重传失败件**，文字内存保留；退出 sheet→provider dispose 清空（无持久草稿）。错误按 UX 上传 toast 3s。
- [ ] **F5. 发布确认 + Idempotency-Key** (AC: 4)
  - [ ] 发布前/确认页提示「发布后内容将对所有用户公开展示」；请求带客户端生成 `Idempotency-Key`；成功后关闭 sheet（成长日历成功→可回档案 Tab）。i18n 双套。

### 🟨 联调验收子任务

- [ ] **J1. 三类发布写库（L1）** (AC: 1, 2) — 日常/科普写 type，成长日历写 type=GROWTH_MOMENT+pet_id；JSONB image_urls 正确。
- [ ] **J2. 成长日历约束（L1）** (AC: 2) — 无档案/B/C 端发成长日历被服务端拒；前端 Segment 灰置+跳创建。
- [ ] **J3. 重试仅失败件（L0）** (AC: 3) — mock 部分图失败→重试只重传失败件，文字保留；退出清空。
- [ ] **J4. 幂等（L1）** (AC: 4) — 同 Idempotency-Key 重复提交只落一条 post。
- [ ] **J5. 校验（L0/L1）** (AC: 1) — text >1000 / 图 >9 → 422。

---

## Dev Notes

### 架构约定

- **命名**：表 `content_posts`(复数 snake_case，软删 `deleted_at`)、枚举 `type`/`danger_level` UPPER_SNAKE 落 varchar、`image_urls` JSONB（架构「分诊结果/档案弹性字段用 JSONB」）；实体 `ContentPost`、DTO record、provider。JSON camelCase / UTC ISO / null 省略。
- **模块边界**：成长日历绑宠物校验**经 profile service 接口或事件，禁 content 直接读 pet_profiles 表**（架构「禁跨模块访问对方 repository」）。
- **幂等**：发布带 `Idempotency-Key` → `shared/ratelimit/IdempotencyService`(Redis 防重锁)。
- **公开桶**：内容图进①公开桶 + CDN（复用 2.1）。
- **错误**：校验 422 / 成长日历无档案 422 或 409 / 限流 429，ProblemDetail；前端 dio→本地化（上传 toast、网络 banner 5s）。

### 强制护栏

- **禁 MQ/缓存层/新中间件**；Redis 仅幂等/限流。
- `ddl-auto=validate`，schema 只由本 Story Flyway。
- **无持久草稿**（NFR-10）：仅内存保留文字、仅重传失败件，**勿引入本地草稿持久化**。
- author_id/pet 归属取自 JWT + service 校验，不信任客户端。
- 日志记业务事件 id 可，**不落用户正文 PII 进结构化字段做检索**；本表无健康数据，但 danger_level 关联问诊时不外泄症状细节到日志。
- 软删 `deleted_at` 为级联删除/Epic3 删除做准备。

### 范围边界（不做）

- ❌ 不做 Feed 流/硬过滤/无限滚动（3.2）、详情（3.3）、点赞（3.4）、评论（3.5）、删除（3.6）、举报（3.7）。
- ❌ 不做时间线渲染（2.4 读本表）、名片读取（2.6 读本表）。
- ❌ 不做视频/收藏/@提及/搜索（V1 范围外）。
- ✅ 只做：`content_posts` 建表(含弹性字段) + 发布端点(幂等/限流) + Publish Compose 单页 + 三类型 + 成长日历绑宠物 + B/C 灰置 + 仅重传失败件 + 内存草稿 + 公开提示。

### Project Structure Notes

- 后端 `com.petgo.content.{web/ContentApiController, service/ContentService, domain/ContentPost, repository/ContentPostRepository, dto/...}`；迁移 `V<n>__init_content.sql`。成长日历归属校验依赖 `com.petgo.profile` 的 service 接口。
- 前端 `lib/features/content/{data,domain,presentation/publish_compose_page}` + `lib/shared/widgets/publish_fab`（凸起「+」，1.2 外壳此处接发布）；上传复用 2.1。

### References

- [Source: architecture.md#Data Architecture] — JSONB 弹性字段、对外 token、内部 id；缓存收窄。
- [Source: architecture.md#Naming Patterns] — content_posts、deleted_at 软删、枚举 UPPER_SNAKE varchar、JSONB。
- [Source: architecture.md#Communication Patterns / Process Patterns] — 幂等 `Idempotency-Key`、仅重传失败件、内存草稿、客户端实时预校验+服务端权威。
- [Source: architecture.md#Architectural Boundaries] — 模块只经 service/事件通信，禁跨 repo join。
- [Source: epics.md#Story 2.3] — 四条原始 AC（UX-DR16 / FR-12 / NFR-10）。
- [Source: epics.md#Story 2.4] — 成长日历快乐时刻进时间线（本表为来源）。

## Dev Agent Record

### Agent Model Used

云端 dev agent（headless，L0 绿）。

### Debug Log References

- 后端：`./mvnw -B -Dtest='ContentServiceTest,ContentApiControllerTest' test` 绿；`./mvnw -B -DskipTests package` 绿。
- 前端：`flutter analyze` → No issues；`flutter test` → 68 passed（+publish_controller）。

### Completion Notes List

**L0 状态（云端已验收）：**
- 后端：content_posts 表（V4__init_content，含 JSONB image_urls、弹性 danger_level、软删 deleted_at）+ 发布端点（JWT/限流/Idempotency-Key）+ 成长日历绑宠物校验（经 ProfileService.ownsPet，不直读 pet_profiles）+ IdempotencyService(Redis) 幂等。单测覆盖三类发布、成长日历无 pet/越权拒、幂等重放、author 取自 JWT。
- 前端：PublishComposePage 全屏 bottom sheet（Segment + 图片区 + 文字 1000 计数 + 公开提示）+ PublishController 上传状态机（每图独立状态、**仅重传失败件**、文字内存保留、autoDispose 清空草稿）+ ContentRepository（Idempotency-Key）+ B/C 灰置（无档案→悬浮提示+跳创建）。「＋」入口接 compose。publish_controller 单测覆盖 ≤9、部分失败、retry 仅失败件、幂等 key、成长日历带 petId、字数禁用。

**模块边界**：content 经 ProfileService 接口校验宠物归属，未直读 pet_profiles 表（架构 Architectural Boundaries）。
**Flyway 序号**：V4__init_content（接 V3 之后，决策 E2）。

**⚠️ 待本地验收（L1/L2）：**
- **L1（Docker postgres+redis）**：J1 三类发布写库（JSONB image_urls 正确、type/pet_id）；J2 成长日历约束（无档案/B/C 发被拒）；J4 幂等（同 Idempotency-Key 只落一条，需 Redis）；J5 校验 text>1000/图>9 → 422。
- **L2**：内容图真实 OSS 直传已在 2.1 覆盖；本 Story 用其产出 URL 落库。
- 待肉眼确认界面：Publish Compose bottom sheet（Segment 单步切换、图片缩略图/上传状态/重试、字数计数、B/C 灰置悬浮提示）真机/模拟器视觉。

### File List

**后端**：`db/migration/V4__init_content.sql`、`content/domain/{ContentPost,ContentType,PostStatus}.java`、`content/repository/ContentPostRepository.java`、`content/dto/{ContentPostCreateRequest,ContentPostResponse}.java`、`content/service/ContentService.java`、`content/web/ContentApiController.java`、`shared/ratelimit/IdempotencyService.java`、`profile/service/ProfileService.java`(+ownsPet)；测试 `ContentServiceTest`、`ContentApiControllerTest`。

**前端**：`core/network/api_paths.dart`(+content-posts)、`features/content/domain/{content_type,publish_controller}.dart`、`features/content/data/content_repository.dart`、`features/content/presentation/publish_compose_page.dart`、`features/media/domain/media_upload_use_case.dart`(+pickAndProcess)、`shared/widgets/app_shell.dart`(+开 compose)、`l10n/*.arb`；测试 `test/content/publish_controller_test.dart`。
