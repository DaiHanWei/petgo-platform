# Story 5.6: 会话结束机制与用户评分门

Status: ready-for-dev

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a **用户**,
I want **兽医结束会话后我有保护窗口并通过评分确认关闭**,
so that **服务质量得到保障，我不被过早切断对话**。

> 本 Story 在 5.5（IN_PROGRESS 进行中会话 + IM 会话）之上，落地**会话状态机的收尾段**：兽医结束 → `PENDING_CLOSE`（非立即关闭）、30min 保护窗口内用户可继续发消息 / 评分立即关闭 / 超时自动关闭记「未评分」、关闭触发 FR-16 存档（IM→OSS 桥接，Epic 2 承接落地档案）+ FR-33 评分（1-5 星必填 + ≤100 字选填，仅运营可见，超时未评下次补弹一次），Admin 评分查看。
>
> 做：兽医「结束会话」二次确认 → PENDING_CLOSE、用户侧评分门（30min 窗口、继续发消息、评分立即 CLOSED、超时自动 CLOSED 记未评分、下次补弹一次）、关闭发 `ConsultClosedEvent`（触发存档桥接 + 评分记录）、IM→OSS 存档桥接（复制聊天媒体到私密桶档案路径）、Admin 评分查看页。
> 不做：封禁中断（5.7，那是 INTERRUPTED 旁路）、问诊历史列表渲染（5.8，本 Story 产出历史/存档数据，列表在 5.8）、档案落地展示（Epic 2 profile 承接 FR-16，本 Story 只发事件 + 复制媒体）、推送本身（Epic 6，本 Story 发事件/IM 系统消息）。
>
> 依赖前序：5.5（IN_PROGRESS + `im_conversation_id` + `ImCallbackController` 骨架）、5.4（AI 上下文私密图，存档一并纳入）、5.1（Admin slice 承评分查看）。本 Story 落 `ConsultClosedEvent`，profile 模块（Epic 2）订阅做 FR-16 存档落地。

## Acceptance Criteria

> **验证层标注**：**L0**（状态迁移 / 评分校验 / lint） · **L1**（需 Docker + pg + redis：PENDING_CLOSE 迁移、30min 定时关闭、评分落库、补弹标记、Admin 查看、存档媒体复制到私密桶） · **L2**（需真实腾讯 IM + 真机：PENDING_CLOSE 期间继续发消息、IM 系统消息、存档实际拉 IM 媒体）。
> 本 Story 状态机收尾与评分主体在 **L1**；30min 窗口内继续对话与 IM 系统消息在 **L2**。存档桥接（IM→OSS 复制）在 **L1/L2**（拉真实 IM 媒体需 L2）。

### AC1 — 兽医结束 → PENDING_CLOSE（非立即关闭）

**Given** 兽医点击「结束会话」并二次确认
**When** 触发结束
**Then** 会话进入 PENDING_CLOSE「待关闭」状态（非立即关闭），用户侧收到「兽医已完成本次问诊，请为本次服务评分」（FR-31）
> 验证层：状态迁移 `IN_PROGRESS→PENDING_CLOSE` + 启动 30min 计时 = **L1**；兽医二次确认 + 用户侧 IM 系统消息「请评分」= **L2**。**注意**：PENDING_CLOSE 非立即关闭——会话仍可发消息（见 AC2）；兽医结束后回 ONLINE（解除 5.5 BUSY）可接新单。

### AC2 — 30min 保护窗口三分支（继续/评分/超时）

**Given** 30 分钟保护窗口内
**When** 用户行为分支
**Then** 用户继续发消息→兽医收推送可继续回复；用户评分（1-5 星必填 + ≤100 字选填）→会话立即 CLOSED 并触发 FR-16 存档；超时未评分→自动 CLOSED 记「未评分」（FR-31、FR-33）
> 验证层：① 继续发消息（PENDING_CLOSE 仍可收发，兽医收推送）= **L2**（IM）；② 评分 1-5 星必填 + ≤100 字选填 → `PENDING_CLOSE→CLOSED` + `ConsultClosedEvent`(触发存档)= **L1**（评分落库/迁移）+ **L2**（存档拉 IM 媒体）；③ 30min 超时无评分 → 自动 `CLOSED` 记 `unrated` = **L1**（DB 状态机定时）。评分校验（星必填、字数 ≤100）服务端权威 = **L1**。

### AC3 — 超时未评分下次补弹一次

**Given** 用户超时未评分
**When** 下次进入问诊页面
**Then** 补弹评分弹窗一次；仍未评分则该次不再要求、记「未评分」（FR-33）
> 验证层：`unrated` 会话标记 `rating_prompt_pending` → 下次进问诊 Tab(5.8)弹一次 → 弹后置 `prompted` 不再弹 = **L1**（标记流转）+ **L2**（弹窗 UI）。**多态**：补弹后用户仍可评（→CLOSED 已存档，仅补评分记录）或仍跳过（永久 unrated，不再弹）。

### AC4 — Admin 评分查看（不对普通用户公开）

**Given** 运营在 Admin 后台
**When** 查看兽医评分
**Then** 可见各兽医历史评分与平均分（V1 评分不对普通用户公开）（FR-33、G-1）
> 验证层：**L1**（Admin `role=ADMIN` 评分查看端点 + 各兽医历史评分/平均分聚合）。评分**仅运营可见**——普通用户/其他兽医 API 不返回他人评分；前端用户侧不展示兽医评分（5.8 历史显示的是「用户自己给的评分」，非兽医公开均分）。

---

## Tasks / Subtasks

> 三段组织。后端：结束迁移 + 30min 评分门状态机 + 评分落库 + 存档桥接事件 + Admin 评分查看。前端：兽医结束确认 + 用户评分弹窗 + 补弹 + Admin Thymeleaf 评分页。建议顺序：后端状态机/评分 → 前端评分门 → 联调（含存档 L2）。

### 🟦 后端子任务（petgo-backend / Spring Boot）

- [ ] **B1. 结束迁移 + 评分门字段** (AC: 1,2,3,4)
  - [ ] Flyway `V<n>__add_consult_rating.sql`：`consult_sessions` 加 `pending_close_started_at`(timestamptz)、`closed_reason`(varchar `RATED|UNRATED|...`)、`rating_prompt_state`(varchar `NONE|PENDING|PROMPTED`)。新建 `consult_ratings` 表（或并入 session）：`session_id`、`vet_id`、`stars`(int 1-5)、`comment`(varchar ≤100 nullable)、`created_at`。索引 `idx_consult_ratings_vet_id`。
  - [ ] `ConsultSessionService.endByVet(vetId, sessionId)`：CAS `IN_PROGRESS→PENDING_CLOSE`、记 `pending_close_started_at`、兽医 BUSY→ONLINE（5.2/5.5）、IM 系统消息「请评分」、发结束提示事件。
- [ ] **B2. 30min 评分门状态机** (AC: 2,3)
  - [ ] 评分提交 `submitRating(userId, sessionId, stars, comment)`：校验 PENDING_CLOSE + stars∈1..5 必填 + comment ≤100 选填 → 写 `consult_ratings` → CAS `PENDING_CLOSE→CLOSED`(closed_reason=RATED) → 发 `ConsultClosedEvent`。
  - [ ] 超时自动关闭：DB 状态机定时扫描（`@Async`/定时，**禁 MQ**）`pending_close_started_at` 超 30min 且仍 PENDING_CLOSE → `CLOSED`(closed_reason=UNRATED)、置 `rating_prompt_state=PENDING`（供 5.8 补弹）→ 发 `ConsultClosedEvent`。
  - [ ] PENDING_CLOSE 期间仍可收发消息（会话未关，IM 会话仍在）——继续发消息不改状态、不重置 30min（窗口是「评分确认窗口」，按 FR-31 语义；若 PRD 要求继续对话重置窗口则记 Dev Notes，默认不重置）。
- [ ] **B3. IM→OSS 存档桥接（FR-16）** (AC: 2)
  - [ ] `shared/media/ImToOssArchiver`（架构指定）：`@Async @EventListener` 消费 `ConsultClosedEvent` → 把会话所需聊天媒体（图/必要视频帧/AI 上下文图）从 IM 复制一份到**私密桶**档案路径，档案只引用**应用自有 URL**（不引用会过期的 IM URL）。
  - [ ] **桥接归属**：本 Story 落「复制媒体到私密桶 + 产出存档数据/事件」；**档案落地展示（FR-16 成长档案）由 profile 模块/Epic 2 订阅 `ConsultClosedEvent` 承接**。遵守「跨模块经事件」——consult 发事件，profile 订阅，不互相直调 repository。
  - [ ] 存档失败重试（DB 状态机 retry ≤3，架构降级），失败不阻断会话 CLOSED。
- [ ] **B4. 补弹标记流转** (AC: 3)
  - [ ] `rating_prompt_state`：UNRATED 关闭 → PENDING；用户下次进问诊 Tab(5.8)查询「待补弹评分」→ 弹后置 PROMPTED；补弹后评分 → 写 rating（会话已 CLOSED，仅补评分记录）；补弹后仍跳过 → 保持 UNRATED 不再弹。
  - [ ] `GET /api/v1/consult/pending-rating`（用户侧，返回需补弹的已关闭会话，供 5.8 进页时弹）。
- [ ] **B5. Admin 评分查看（G-1）** (AC: 4)
  - [ ] `com.petgo.admin`（5.1 建）：`GET /api/v1/admin/vets/{id}/ratings`（历史评分 + 平均分）`role=ADMIN`；经 consult service 接口聚合（不跨 repository）。极简 Thymeleaf 评分页。
  - [ ] 评分**仅 ADMIN 可见**：用户侧/兽医侧 API 不暴露兽医公开均分。

### 🟩 前端子任务（petgo_app / Flutter）

- [ ] **F1. 兽医「结束会话」二次确认** (AC: 1)
  - [ ] `features/vet/presentation` 对话界面：「结束会话」按钮 → 二次确认「确认结束本次问诊？」→ 调 endByVet → 会话移出「进行中」、兽医回在线可接单。
- [ ] **F2. 用户评分门** (AC: 1,2,3)
  - [ ] `features/consult/presentation`：收到 PENDING_CLOSE（IM 系统消息/轮询）→ 展示「兽医已完成本次问诊，请为本次服务评分」+ 评分弹窗（1-5 星必填 + ≤100 字选填）。
  - [ ] 30min 窗口内**仍可继续发消息**（输入框不锁）；评分提交 → 会话 CLOSED、转只读（5.8 只读态）。
  - [ ] 超时未评 → 会话自动 CLOSED；下次进问诊 Tab(5.8)补弹一次（读 `pending-rating`）；补弹跳过则不再弹。
- [ ] **F3. 评分校验与文案** (AC: 2)
  - [ ] 客户端预校验（星必选、≤100 字）仅体验，服务端权威；ProblemDetail 本地化。评分文案双语、克制（UX-DR14）。

### 🟨 联调验收子任务

- [ ] **J1. 结束 → PENDING_CLOSE → 评分 CLOSED + 存档** (AC: 1,2 / **L1+L2**)
  - [ ] 兽医结束(二次确认)→ PENDING_CLOSE + 用户收「请评分」；用户评分 → CLOSED(RATED) + `ConsultClosedEvent` 触发存档媒体复制到私密桶。
- [ ] **J2. 30min 超时自动关闭** (AC: 2,3 / **L1**)
  - [ ] PENDING_CLOSE 超 30min 无评分 → 自动 CLOSED(UNRATED) + rating_prompt_state=PENDING；下次进问诊 Tab 补弹一次；补弹跳过 → 不再弹、永久未评分。
- [ ] **J3. PENDING_CLOSE 期间继续对话** (AC: 2 / **L2**)
  - [ ] PENDING_CLOSE 内用户继续发消息 → 兽医可收（推送 Epic 6）可回复，会话未提前关闭。
- [ ] **J4. Admin 评分可见、用户不可见** (AC: 4 / **L1**)
  - [ ] Admin 看各兽医历史评分 + 平均分；用户/兽医侧 API 不返回兽医公开均分。

---

## Dev Notes

### 关键架构约定（本 Story 必须落实）

- **会话状态机收尾在 Java 侧**：`IN_PROGRESS →(兽医结束) PENDING_CLOSE →(评分/30min超时) CLOSED`（架构 §Communication Patterns）。30min 评分门是**状态迁移触发器**（架构明确「评分门(30min)为状态迁移触发器」），用 DB 状态机定时扫描，**禁 MQ**。
- **IM→OSS 存档桥接（架构关键跨云点）**：`ConsultClosedEvent → profile 订阅做存档桥接`；媒体「问诊存档时把所需图从 IM 复制一份到私密桶，档案只引用应用自有 URL（不引用会过期的 IM URL）」（架构 §Data Architecture 媒体三层桥接规则 / §Decision Impact「存档桥接(IM→OSS)连接 consult→profile」）。本 Story 落 consult 侧事件 + `ImToOssArchiver` 媒体复制；profile/Epic 2 订阅落档案。
- **评分仅运营可见（FR-33 / G-1）**：Admin slice 查看，普通用户/兽医不可见他人/公开均分。用户侧 5.8 历史显示的是「用户本人给的评分」，非兽医均分。
- **跨模块经事件**：consult 发 `ConsultClosedEvent`，profile（存档）、notify（推送）订阅，不互相直调 repository（架构 §模块边界）。

### 会话状态机专项

> 全机收尾：`IN_PROGRESS →(本Story 兽医结束) PENDING_CLOSE`。PENDING_CLOSE 是「待关闭保护窗口」——**非立即关闭**，30min 内可继续对话。三出口：① 用户评分 → `CLOSED`(RATED) + 存档；② 30min 超时 → `CLOSED`(UNRATED) + 补弹标记；③ （封禁在 5.7 是另一条 `IN_PROGRESS→INTERRUPTED` 旁路，不经 PENDING_CLOSE）。
> CLOSED 触发 `ConsultClosedEvent` → 存档桥接（IM→OSS 私密桶复制）+ 评分记录 + 历史可见（5.8）。存档是「额外操作」，问诊历史无论是否存档都保留（5.8）——本 Story 产存档数据，5.8 区分「是否已存档」标记。

### 强制护栏（架构 §Enforcement —— 违反即返工）

- **禁 MQ / 通用缓存 / 新中间件**：30min 超时 + 存档重试用 DB 状态机 + @Async。
- **存档只引用应用自有 URL**：档案不引用会过期的 IM URL；私密图只走签名 URL；日志不落签名 URL/健康图/PII。
- **跨模块经事件**：`ConsultClosedEvent` 发布，profile/notify 订阅；consult 不直调 profile/notify repository。
- **角色门控**：结束 `role=vet`、评分 `role=user`、评分查看 `role=ADMIN`；评分不对普通用户/兽医公开。
- **Redis 收窄**：本 Story 不新增 Redis 用途（BUSY→ONLINE 复用 5.2 在线态）。

### 范围边界（防 scope creep —— 本 Story 明确不做）

- ❌ 不做封禁 → INTERRUPTED（Story 5.7，那是不经 PENDING_CLOSE 的旁路）。
- ❌ 不做问诊历史列表渲染 / 只读会话页（Story 5.8，本 Story 产历史/存档数据 + pending-rating 接口）。
- ❌ 不做成长档案 FR-16 落地展示（Epic 2 profile 订阅 `ConsultClosedEvent` 承接，本 Story 只复制媒体 + 发事件）。
- ❌ 不做推送本身（Epic 6，本 Story 发事件 + IM 系统消息）。
- ✅ 只做：兽医结束(二次确认)→PENDING_CLOSE + 30min 评分门(继续/评分立即关/超时记未评)+ 评分 1-5星必填≤100字选填 + 补弹一次 + `ConsultClosedEvent` + IM→OSS 存档媒体复制 + Admin 评分查看。

### Project Structure Notes

- 后端：`com.petgo.consult.{service,domain,web,event}`——`ConsultSessionService`(endByVet/submitRating/超时关闭)、`event/ConsultClosedEvent`、`domain/RatingGate`(架构指定 `consult/domain/{ConsultSession,SessionStatus,RatingGate}`)、`consult_ratings` 表；`shared/media/ImToOssArchiver`(存档复制)；`com.petgo.admin`(评分查看 + Thymeleaf)。Flyway `V<n>__add_consult_rating.sql`。
- 前端：`lib/features/vet/presentation`(结束确认)、`lib/features/consult/presentation`(评分弹窗 + 补弹 + 只读转换)；补弹入口在问诊 Tab(5.8)。
- 用户侧 `features/consult`、兽医侧 `features/vet`、存档落 profile(Epic 2)。

### References

- [Source: architecture.md#Communication Patterns] — 会话状态机 `IN_PROGRESS→PENDING_CLOSE→CLOSED`、评分门(30min)为迁移触发器、@Async+DB 状态机禁 MQ。
- [Source: architecture.md#Data Architecture 媒体三层桥接] — 存档时把图从 IM 复制到私密桶②，档案引用应用自有 URL 不引用过期 IM URL。
- [Source: architecture.md#Decision Impact Analysis] — 存档桥接(IM→OSS) `ConsultClosedEvent`→profile 连接 consult→profile。
- [Source: architecture.md#Complete Project Directory Structure] — `consult/domain/{ConsultSession,SessionStatus,RatingGate}`、`shared/media/ImToOssArchiver`、`admin` slice。
- [Source: architecture.md#Validation Issues Addressed (G-1)] — Admin 评分查看挂 admin slice。
- [Source: epics.md#Story 5.6] — 四条原始 AC（结束 PENDING_CLOSE / 30min 三分支 / 补弹一次 / Admin 评分查看）。
- [Source: epics.md#Story 5.5 / 5.8] — 进行中会话衔接 / 历史与只读态衔接。
- [Memory: spec-page-state-completeness] — 显式覆盖超时未评(自动关+记未评)、补弹后跳过(永久未评)、存档失败不阻断关闭、评分不公开等多态。

## Dev Agent Record

### Agent Model Used

{{agent_model_name_version}}

### Debug Log References

### Completion Notes List

- 记录 30min 窗口是否因「继续发消息」重置（默认不重置）、存档复制的媒体范围（图/视频策略）、`consult_ratings` 独立表 vs 内联、补弹标记流转细节。

### File List
