---
title: '个人中心 & 设置流核对（⑥）—— 无需改动'
type: 'chore'
created: '2026-06-16'
status: 'done'
---

## 结论：无代码改动（已完整 + 合规）

⑥ 个人中心 & 设置流（P-40 我的 / P-41 设置 / P-43 注销）经调查**无需改动**，是 6 条流里唯一一条「调查即结论」的：

- **i18n 已 100% 干净**：`me_page.dart` / `settings_page.dart` / `language_settings_page.dart` 零硬编码，全部走 arb（me*/deleteAccount*/logout*/language* 共 ~23 键）。前 5 条流的「i18n 收口」在此无活。
- **注销已完整且合规**（story 7.3）：双重确认（邮箱键入 `deleteAccountConfirmPhrase`）+ 调 `DELETE /me` 触发后端**立即**级联删除（个人数据表 + OSS 硬删，UGC/consult 匿名化保留，D1/D2）。登出确认 + 清本地态亦到位。
- **P-40 我的**：用户信息（email 只读）+ 宠物 mini 卡三态 + 发帖网格 + 编辑昵称抽屉，已对齐薄荷绿。
- **P-41 设置**：语言/兽医登录/登出/注销四项，统一 token。

## 原型与 PRD 的冲突（不实现，记差异）

原型 P-43 画的「**30 天冷静期 + 期内重登可撤销**」与 **story 7.3 AC2「立即级联删除」直接冲突**，且 CLAUDE.md 护栏明确「7.3 是安全攸关节点、勿埋违反点」。实现冷静期 = 注销后数据保留 30 天 = 违反 PDP「注销即删」承诺 + 需改后端契约（软删状态机 + 定时硬删 + 撤销）+ 与 V1 轻量姿态冲突。**按「story/后端契约权威、原型不准」原则，不实现。** 详见 deferred-work（待单独后端为主 story 评估是否引入宽限期 + PRD 修订）。

原型 P-41 的「深色模式开关 / 档案公开开关」同样不做：V1 仅浅色（无 dark theme，画了即死按钮违反「不留死按钮」）；档案公开无字段/无后端契约/涉 PII 政策。均记 deferred。

## 决策（用户 2026-06-16 拍板）

走 A：本条按「无需改动」收尾，不写埋合规违反点的代码；冷静期等差异留 deferred 单独评估。
