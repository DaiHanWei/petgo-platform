# generated: 2026-09-15
# last_updated: 2026-09-15  # 24 条 story 文件全部生成
# project: TailTopia V1.3.0 · 主题 batch-b1（场所发现与社交基建包）
# project_key: NOKEY
# tracking_system: file-system
# story_location: {project-root}/_bmad-output/implementation-artifacts/v1.3.0
# source_epics: {project-root}/_bmad-output/planning-artifacts/v1.3.0/epics-v1.3.0-batch-b1.md (4 Epic / 24 Story)
# architecture: {project-root}/_bmad-output/planning-artifacts/v1.3.0/architecture-v1.3.0-batch-b1-delta.md (AD-1~AD-10)
# prd: {project-root}/_bmad-output/planning-artifacts/v1.3.0/PRD-v1.3.0-batch-b1-places-social.md
# 决策日志: {project-root}/_bmad-output/planning-artifacts/v1.3.0/决策日志-batch-b1.md (B1-D1~D13)
# 代码核对报告: {project-root}/_bmad-output/planning-artifacts/v1.3.0/代码核对报告-batch-b1.md
# ui: {project-root}/_bmad-output/planning-artifacts/v1.3.0/ui-v1.3.0-batch-b1-places-social.html (24 屏 / 5 泳道)
#
# 🔴 口径优先级：决策日志 > PRD > UI 稿。
#    PRD 有两处已被推翻 —— 地图选型（B1-D9：OSM → Google，且只做显示+打自己的点）、
#    Diary 推荐覆盖人群（B1-D2：扩到「声明未养宠/计划养宠」态）。
#    UI 稿 2026-09-11 已按代码核对结论修订 4 处，稿内首屏有修订说明表。
#
# NOTE: 同一版本目录下 admin / batch-a / batch-b1 三个主题**各自独立成域**，
#       Epic 号段可以重复。技能按本文件里的**完整 key** 取 story，不按 `<epic>-<n>` 前缀通配。
#       **口头引用 story 必须带主题**（如「batch-b1 的 1-1」）。

# STATUS DEFINITIONS:
# ==================
# Epic Status:   backlog -> in-progress -> done
# Story Status:  backlog -> ready-for-dev -> in-progress -> review -> done
# Retrospective: optional <-> done
#
# WORKFLOW NOTES:
# ===============
# - 2026-09-15：24 条 story 文件**已全部生成**，状态 ready-for-dev，可直接 bmad-dev-story
# - Epic 首个 story 创建(create-story)时自动 -> in-progress
# - 每 story 三段推进：后端 -> 前端 -> 联调（CLAUDE.md 纪律）
# - 每条 AC 已标 L0(静态) / L1(集成) / L2(端到端) 层级
# - ⚠️ 本批次 AC 里 **L2 占比近六成**（四个 epic 全是界面）。云端 headless 只跑到 L0 绿灯，
#   L1/L2 留本地并在 Completion Notes 标注「待本地验收」。
#   **建议分批回本地验收，不要等 24 条全跑完一次性看** —— 否则验收会堆成一座山。
# - Dev 完成后转 review，再跑 code-review（建议新开 context）
#
# ===== Epic 依赖链（全后向，无环） =====
# 零依赖、可立刻开工：Epic 1 · Epic 2（两者之间无硬依赖，可并行）
# Epic 2 -> Epic 3        # @ 的点击落点是 2-1 交付的公开主页
# Epic 2 -> Epic 4        # 宠物卡点击复用 2-3 交付的「宠物档案访客视图（站内入口）」，不新建通道
# Epic 1 -> Epic 4        # 4-4 的宠物横滑行加在 1-1 已落地的场所入口行**之下**
#                         #   （布局已在 UI 稿统一设计，不存在返工；这是顺序约束不是功能依赖）
#
# ===== 跨主题依赖（唯一一条，硬阻塞） =====
# 后台 AB-17A「场所内容管理」—— 归 **admin 主题**，不在本文件的 story 序列里。
#   ✅ 2026-09-14 已确认后台侧在做。
#   ⚠️ 它是 App 端场所功能**上线**的硬依赖：没有它，场所上线后没人能下架违规场所、
#      没人能合并重复条目；而本版**用户不可编辑场所**（2026-09-15 拍板），纠错只能走后台。
#      不阻塞开发，阻塞发版 —— 见发版检查单 RC-4。
#
# ===== 发版检查单（RC-1~RC-4）不在本文件 =====
# 定位权限商店合规 / 地图服务账号 / 冷启动数据 / 后台 AB-17A 四项**零代码零测试、验证全是 L2**，
# 已从 story 序列摘出，放在 epics 文件末尾的「发版检查单」一节，归发版负责人。
# 🔴 **不要**把它们加回本文件 —— 混进 sprint 的唯一后果是有人标成 done 而实际没人去点过那个表单。

development_status:

  # ===== Epic 1: 宠物友好场所发现 · FR-112（①~⑦⑨，不含⑧打卡）=====
  # 零依赖起点。全新模块 place，前后端从零建（全仓现无任何场所/venue 实现）。
  # 引入本批次唯一一处 App 新依赖：地图 SDK（使用边界见 AD-3，越界即计费）。
  epic-1: in-progress
  1-1-场所列表与场所本体建模: review                     # 建表(AD-1) + 列表接口 + 列表页 + 空态 + **首页场所入口**。入口与页面必须同条交付，否则页面不可达、无法验收
                                                        # 2026-09-15 云端 L0 全绿 → review；迁移 V20260915_0409__init_places.sql；L1/L2 待本地验收（清单见 story Completion Notes）
  1-2-按距离排序与定位权限: review                       # AD-2：经纬度范围粗筛 + 直线距离 + 两列索引；禁装地理扩展、禁接三方 LBS；「按距离」与「按最新」是两条独立查询分支
                                                        # 2026-09-15 云端 L0 全绿 → review；迁移 V20260915_0458__add_places_geo_index.sql；新增依赖 geolocator
                                                        # ⚠️ code-review 修掉一条 NFR-4 违反：坐标经 query string 进了落盘 access log（已按键打码）
                                                        # L1/L2 待本地验收（含「贴 EXPLAIN 确认走索引」，清单见 story Completion Notes）
  1-3-标记一个场所: review                               # 表单全集（7 类型 + 6 标签，UI 稿是示意省略）；必填未满时保存灰色禁用；**提交前告知不可修改**，服务端硬拒编辑接口
                                                        # 2026-09-15 云端 L0 全绿 → review；无新迁移；AC5 用反射测试钉住「没有写端点」
                                                        # ⚠️ code-review 修掉 9 条：内联错误死代码 / 兽医 token 能创建场所（已限 hasRole USER）/ 创建无幂等键（已接 Idempotency-Key）
                                                        # L1/L2 待本地验收（清单见 story Completion Notes）
  1-4-地图选点: review                                   # 引入地图 SDK。🔴 只做「显示 + 打自己的点」，禁调搜索/地址解析/路线；地图只出现在选点弹层与详情页小地图两处
                                                        # 2026-09-15 云端 L0 全绿 → review；无新迁移；新增依赖 google_maps_flutter
                                                        # AC1/AC2/AC5 用 test/place/map_usage_boundary_test.dart 扫源码机械守（越界即计费，靠人看守不住）
                                                        # ⚠️ 密钥两套走 env / gitignored 文件注入，仓库内零明文；RC-2（绑卡+申请密钥）未闭合但**不阻塞本 story**
                                                        # L2 待本地验收（需真实密钥；清单见 story Completion Notes）
  1-5-场所详情页: review                                  # 照片流/地址复制/小地图/举报。标记人本条内走**现有迷你卡**（Epic 2 退役时统一改），故不依赖 Epic 2
                                                        # 2026-09-15 云端 L0 全绿 → review；新迁移 V20260915_0656__init_place_reports.sql（AD-8 独立表，不用多态外键）
                                                        # 🔴 下架/不存在同一个 404（刻意不可区分）；AC6 的"没有"由 PlaceDetailResponseContractTest 反向钉死
                                                        # 小地图是地图允许出现的**第二处也是最后一处**（全手势关闭 + liteMode），已进 map_usage_boundary_test 白名单
                                                        # ⚠️ code-review 修掉 4 条：举报并发撞唯一约束 500 / 安卓缺 geo: 包可见性声明（在地图中打开永远只开网页）/ 详情族键把已渲染页面顶掉 / launchUrl 返回值被丢弃
                                                        # L1/L2 待本地验收（清单见 story Completion Notes）
  1-6-灯箱抽成公共组件并接入场所照片: review             # 抽既有私有灯箱成公共件，内容详情页行为一字不变。**不等批次 A**（B1-D4）
                                                        # 2026-09-15 云端 L0 全绿 → review；无迁移、不碰后端
                                                        # 抽成 shared/widgets/photo_lightbox.dart；两条历史 bug（20260727-372 单图翻页 / 20260701-192 点图关闭）各钉一例测试
                                                        # 🔴 刻意不与 case_image_viewer.dart 合并（那是兽医/IM/病例/商品四处在用的另一套）——统一属批次 A FR-115
                                                        # AC3 后续动作以 photo_lightbox.dart 里的 TODO(批次A/FR-115) 记录
                                                        # ⚠️ code-review 修掉 5 条：thumbWidth 死参数 / "原图"注释不实（服务端只发 1080 px，真原图会带 GPS）/ 死链一屏全黑 / 夹取没下沉进组件 / 测试吞掉全部异常
                                                        # L2 待本地验收（清单见 story Completion Notes）
  1-7-场所评论与二元态度: review                         # AD-8 独立建表 place_comments，不改既有 comments、不用多态外键；一级评论 only；**用户可自删自己的评论**（校验本人）
                                                        # 2026-09-15 云端 L0 全绿 → review；新迁移 V20260915_0813__init_place_comments.sql
                                                        # 🔴 表里没有 parent_id = 「只有一级」是结构性保证（实体/DTO/端点三层同样没有回复的位置）
                                                        # 拉黑只做 R1（按查看者），**刻意没有 R2 影子评论**：场所没有主人，标记人连场所本身都改不了
                                                        # 评论数接真值（viewer 维度、批量取）；两个态度计数仍为 0 → Story 1.8
                                                        # ⚠️ code-review 修掉 4 条：兽医 token 被当成 users.id 读到未过审评论（安全）/ 注销级联漏了 place_comments（D1/D2）/ 两个零调用的状态迁移 / 发评论后详情计数不刷
                                                        # 🔴 待 admin 主题：降级/高危评论目前一直挂 UNDER_REVIEW —— 入运营队列需扩 ReviewContentType（塞进现有 COMMENT 会让运营放行 comments 表里同号的别人评论）
                                                        # L1/L2 待本地验收（清单见 story Completion Notes）
  1-8-推荐不推荐计数: review                             # AD-9 Redis 计数器 + DB 回算自愈。三个触发点：创建 / 用户自删 / 运营删下架。本版只展示不参与排序
                                                        # 2026-09-15 云端 L0 全绿 → review；无新迁移
                                                        # 🔴 计数只认 VISIBLE：先发后审之下「创建」那一刻评论还没过审，自增落在转可见那一刻（与 DB 回算口径逐字一致）
                                                        # 触发点实际是四个：转可见 / 用户自删 / 运营下架 / 作者注销；删未过审的不减
                                                        # 整页 MGET 一次取回（AC6）；AC2/AC4 用扫源码的机械检查守（不缓存整页、不参与排序降权警示）
                                                        # ⚠️ code-review 修掉 4 条「丢一次增量就永久偏差」：写失败永不自愈→加 TTL 10min / 事务提交前改计数→afterCommit / 回算写回无条件覆盖→setIfAbsent / 负值被洗成 0→当缺失回算
                                                        # L1/L2 待本地验收（清单见 story Completion Notes；自愈与 TTL 必须真 Redis 验）
  1-9-他人为场所补充照片: review                         # 标注上传者；走既有上传链路 + 公开桶 + EXIF 剥离兜底
                                                        # 2026-09-15 云端 L0 全绿 → review；新迁移 V20260915_0927__init_place_photos.sql
                                                        # 🔴 **动了 Story 1.1 的结构**：照片从 places.photo_urls(JSONB 数组) 搬到独立表 place_photos，**并删掉了那一列**（单一事实源）
                                                        #    理由：数组装不下「每张的上传者 + 每张自己的审核态 + 单张删除」——后两样在数组上做是读改写整个数组，两张同时过审必丢一条
                                                        # ⚠️ 契约变更：详情的 photoUrls(字符串数组) → photos(带上传者的对象数组)，客户端已同步
                                                        # 审核判定三分：PASS→可见 / 确定性违规→REJECTED 终态 / 三方降级→保持挂起（与评论那条有意不同：图审能把「确定」和「不知道」分开）
                                                        # ⚠️ code-review 修掉 8 条：选图数写死 9 导致公开桶孤儿对象 / 上限把不可见行算进去（场所永远加不进新图）/ 被判死的照片一直标「审核中」/ 删除入口点不到 / 无上传中保护 / 可以把照片删到零张（场所不可编辑，删光就回不去了）/ 注销豁免判据按人判且 N+1 → 改 is_original 列 / 注入未用的依赖
                                                        # 🔴 同 1-7 的待办：降级挂起的照片需 admin 主题扩 ReviewContentType 才能进运营队列
                                                        # L1/L2 待本地验收（清单见 story Completion Notes）
  1-10-场所分享到站外: review                             # H5 直出 /place/{随机码} + OG + 唤起链 + 安卓深链声明。🔒 **审核通过前不下发 og:image**（预览图会被平台缓存，不随下架消失）
                                                        # 2026-09-15 云端 L0 全绿 → review；无新迁移（只给 place_photos 加了一列 og_eligible，那个迁移尚未合入 main）
                                                        # /place/{不可枚举 token} 服务端直出；下架/不存在统一 card_gone + 404 + noindex（不可区分）
                                                        # 🔴 OG 预览图是对名片/里程碑「不放图」边界的**有意例外**（场所照片不含个人与宠物隐私）——理由写进代码且有测试钉着（AC4 原文要求）
                                                        # 🔴 AC5 的门控判据是 og_eligible 而非 VISIBLE：标记时那批在三方 RISKY/DEGRADED 下照样对外展示（1.3 口径），但不给 og——预览卡会被平台缓存、下架也撤不回来
                                                        # ⚠️ 新写的 deeplink_manifest_test 当场抓到存量漏网：tailtopia://post（V1.1.6 Story 9.3）从来没进过安卓清单，那条深链在安卓上一直不工作，已补
                                                        # ⚠️ code-review 修掉 4 条：/place/ 没进 H5 错误页白名单（异常回 JSON 给站外访客，/c/ 同样漏了一并补）/ og 门控只判 VISIBLE 挡不住 RISKY-DEGRADED / 品牌图裂图（抄了 App 才有的文件名）/ 跨仓守门测试放在 App 仓但读后端文件，纯后端 PR 上根本不会跑
                                                        # L1/L2 待本地验收（清单见 story Completion Notes）

  # ===== Epic 2: 用户公开主页 · FR-118 =====
  # 零依赖。被 Epic 3（@ 落点）与 Epic 4（访客通道）依赖 —— 是本批次的关键路径，建议优先。
  epic-2: in-progress
  2-1-公开主页他人视角与迷你卡退役: review               # ⚠️ **刻意不拆小**：迷你卡上挂着举报/拉黑两个入口，先切入口后做抽屉会出现用户找不到举报拉黑的功能回退期
                                                        # 2026-09-15 云端 dev-story 完成，L0 全绿（后端 3255 例真实失败 0 / 前端 analyze 0 issue、1675 例）
                                                        # 新端点 GET /api/v1/users/{id}/profile；**无数据库迁移**（加入时间取既有 users.created_at；发帖总数复用既有统计）
                                                        # 🔴 入口是**五处不是三处**：Dev Notes 那三处是 Epic 1 落地之前数的，本批次 1-5/1-7 又添了「场所详情标记人」「场所评论头像」两处（当时主页还不存在，先接的迷你卡）。五处一并收口，有守门测试钉 lib/ 零引用
                                                        # 迷你卡组件与后端 /mini-profile 端点**保留但零引用**（AC2 原文是"不再被任何入口引用"而非删除；两者各挂着一批既有回归用例）。组件头部已标注"已退役"
                                                        # 🔴 「id 不存在」与「已注销」改为**刻意不可区分**（同一个 200 + 空投影）：原先照抄迷你卡的"查不到→404"是**不可达分支**（findAuthorViews 会补齐匿名投影），其单测 stub 了线上到不了的状态属假绿。可区分 = 给了一个按 id 遍历确认谁注册过的枚举口子
                                                        # ⚠️ code-review 修掉 3 条：publicProfileProvider 漏 isAutoDispose（Riverpod 3 family 默认 keep-alive → 同设备换账号 B 读到 A 的 self/reported，与 bug 20260730-421/446 同型）/ 上面那条不可达 404 + 假绿单测 / 自动重试没关（错误态自己横跳，用户点不到重试）
                                                        # 埋点 entry 的 wire 值**仍是 mini_profile**（改版前后是同一条时间序列，换字面量会把曲线断成两截）
                                                        # 内容网格/获赞总数(2-2)、宠物卡(2-3)、编辑资料(2-4)均未做；身份区下**刻意不摆空网格占位**
                                                        # L1/L2 待本地验收（清单见 story Completion Notes）
  2-2-主页内容区与聚合计数: review                        # PUBLIC 混排 + 发帖总数（**已有可复用**）+ 获赞总数（本批次新补）；非 PUBLIC 服务端就不下发
                                                        # 2026-09-15 云端 dev-story 完成，L0 全绿（后端 3271 例真实失败 0 / 前端 analyze 0 issue、1689 例）
                                                        # 新端点 GET /api/v1/users/{id}/posts（游标分页）；**无数据库迁移、无冗余计数列**（AD-6：获赞总数是一条实时 SQL）
                                                        # 🔴 顺带改掉一条既有口径：发帖总数原先没带 visibility，把私密内容也数了进去 —— 而它的两个出口（迷你卡 / 公开主页）都是他人视角，差值即"这人有几篇私密内容"。迷你卡端点一并跟上；作者自视（我的发布）一行未动
                                                        # 网格从 me_page 的私有 _MyPostCard 抽成 shared/widgets/post_grid_tile.dart，两页共用（widget key myPost_* → postGridTile_*）
                                                        # ⚠️ code-review 修掉 3 条：网格漏了 Feed 一直带着的两条隐藏排除（举报过的那一条 / 隐藏过的那个人）→ 举报过某人账号后进他主页，整片网格点进去全是 404；抽组件后留下的孤儿文档注释；注释里写错的测试文件名
                                                        # 🔸 **一条自行判定的口径，建议本地确认**：上面那条隐藏排除只作用于网格、不作用于两个计数（计数说"这个人发了多少"，网格说"你能看到哪些"；访客知道自己隐藏过谁，所以不泄漏也不困惑）。story AC 与决策日志都没写到它
                                                        # 宠物卡(2-3)、编辑资料(2-4)、被拉黑/注销空态(2-5) 均未做
                                                        # L1/L2 待本地验收（清单见 story Completion Notes）
  2-3-宠物档案访客视图站内入口: review                    # 🔴 **共用件，Epic 4 复用**。AD-4：新建 petId 入口 + 复用 v1.1.6 那层只读投影；不下发对方宠物的分享 token；⚠️ 访客视图**没有日历**，别照旧稿补回去
                                                        # 2026-09-15 云端 dev-story 完成，L0 全绿（后端 3285 例真实失败 0 / 前端 analyze 0 issue、1701 例）
                                                        # 新端点 GET /api/v1/pets/{petId}/visitor/{profile,stats,timeline}（仅登录，靠"不写 permitAll"实现，没改安全配置）+ GET /api/v1/users/{id}/pet（宠物卡，游客可读，无档案回 204）
                                                        # **无数据库迁移**。站内入口与分享入口落到同一层 VisitorProjectionService（AD-4 Rule 2），新增的只有一句 findVisibleProfileById
                                                        # 🔴 拉黑守卫在主页/内容区/宠物卡/站内入口**各拦一次** —— 站内入口按 petId 直达，只拦主页等于留绕过口
                                                        # ⚠️ 站内入口**根本没有 calendar/day 端点**，页面也没有切换行（测试断言映射恰好三条）
                                                        # ⚠️ code-review 修掉 4 条：三个访客 provider 漏 isAutoDispose（换账号吃旧缓存 → 服务端拉黑守卫根本不被调用，与 bug 20260730-421/446 同型）/ return null 出去是 200 不是 204 / 为一个 Diary 数算了五条查询含两次健康表计数 / token ?? "" 把 fail-fast 变成畸形 URL
                                                        # 🔸 **一条待产品确认的口径（本 story 按 AD-4 原样保留、未改）**：访客视图的 Diary 时间线含作者设为私密的条目（PRD §2.9 §② 的既有拍板，前提是"作者自己按下分享键"）。站内入口没有这个前提，而 ContentVisibility 把"他人主页"归在按 PUBLIC 过滤那一档 —— 两处口径在这个新入口上第一次撞上。缓解现状：私密条目点不开（openableByVisitor=false），但卡片文字与图片是渲染出来的
                                                        # L1/L2 待本地验收（清单见 story Completion Notes）
  2-4-自己视角的主页: review                             # 同页两视角；「编辑资料」在身份行内与头像同行，不塞 AppBar；「我的」Tab 一行不改
                                                        # 2026-09-15 云端 dev-story 完成，L0 全绿（前端 analyze 0 issue、1709 例；后端未动、3285 例真实失败 0）
                                                        # **纯前端、无迁移**：self 字段 Story 2.1 就在下发了
                                                        # 🔴 AC3 的「跳既有抽屉、不重画」靠**抽取**实现：资料编辑抽屉与 _InitialAvatar 从 me_page.dart 逐行抽成 me/presentation/profile_edit_sheet.dart 与 shared/widgets/initial_avatar.dart（复制一份的话两处细节会各改各的，且没有任何测试会红）。两条 L0 测试钉住「两处调同一个函数」「me_page 里不再有抽屉本体」
                                                        # ⚠️ code-review 修掉 3 条 ref-after-await：主页 invalidate 缺 mounted 守卫（riverpod 3 真抛 StateError，release 也抛）/ 保存成功却不更新本地也不提示（异常被 catch(_) 吞）/ 换头像上传完 PATCH 根本没发。三条在「我的」Tab 上都碰不到（那个 Tab 常驻），但公开主页是可 pop 的路由，从此真实可达
                                                        # 🔸 **一条既有缺陷，按 AC5「行为一字不改」有意保留**：抽屉里昵称清空时整表当 no-op，用户同时改的签名一并丢弃，且无校验提示、保存按钮不置灰。源码已标注，留给产品决定
                                                        # L2 待本地验收（清单见 story Completion Notes；本 story 无 L1 项）
  2-5-被拉黑者视角与已注销用户的主页: review             # 🔴 模糊空态**必须与「真的没有公开内容」复用同一组件同一文案**，否则一对比就能推断出被拉黑；判定在服务端
                                                        # 2026-09-15 云端 dev-story 完成，L0 全绿（后端 3295 例真实失败 0 / 前端 analyze 0 issue、1715 例）。**无迁移**
                                                        # 四条读路径加反向判据（isBlocked 参数掉过来，不新写查询）：/profile 两个计数归零 · /posts 空信封 · /users/{id}/pet 204 · /pets/{petId}/visitor/* 404+同一句 GONE_DETAIL（⚠️ 不是 403，403 等于明说"你被拉黑了"）
                                                        # 🔴 AC2：服务端两种情况**逐字段相等**（测试直接 assertThat(blocked).isEqualTo(empty)），客户端压根没有"被拉黑"这个概念；空态文案按 UI 稿 C4 统一，三条守门测试钉住"不许分第二句"
                                                        # AC6 的例外声明已写进 story Completion Notes：**对 V1.1.4 FR-94「被拉黑方完全无感」的有意例外、仅主页维度；1.1.4 文档不回改**
                                                        # ⚠️ code-review 修掉 4 条：自己视角的空态在说胡话（共用那句"对方的隐私设置"）/ 畸形游标成了问答口（普通访客 422、被拉黑者 200 空页）/ /mini-profile 还在报真实 postCount（与 /profile 打架）/ 被删掉的文案守卫没补回来
                                                        # 🔸 **一条固有限制，记录在案未自行改动**：三个主页端点都是 permitAll（2.1~2.3 的既定口径），去掉 Authorization 头就能看到真实计数与内容。但这不是本 story 新开的口子，也不是最容易那条路 —— AC4 明写 Feed 与评论区不屏蔽，被拉黑者本来就会在自己 Feed 里刷到对方的帖。要堵死得把主页整体改成登录可见（与 2.1~2.3 冲突），属产品取舍
                                                        # L1/L2 待本地验收（清单见 story Completion Notes）

  # ===== Epic 3: @ 提及 · FR-119 =====
  # 依赖 Epic 2（3-3 的跳转落点是 2-1 的公开主页）。
  epic-3: in-progress
  3-1-at候选集: review                                   # AD-10 独立建表。**维护逻辑是本条主体**：写入时机 / 裁剪策略 / 防无界增长，三者必须在 AC 里定死
                                                        # 2026-09-15 云端 dev-story 完成，L0 全绿（后端 3316 例真实失败 0 / 前端 analyze 0 issue、1715 例）。**纯后端**
                                                        # 迁移 V20260915_1402__init_mention_candidates.sql；新模块 com.tailtopia.mention
                                                        # 🔴 AC2 两条口径定死：**写入**=随互动 @Async（消费既有 ContentLikedEvent/ContentCommentedEvent，content 侧零改动、AFTER_COMMIT）；**裁剪**=写入时顺手淘汰、每人留 50。表上界因此是硬的（用户数 × 50，与互动量无关），**没引任何调度中间件、连 @Scheduled 都没用**
                                                        # ⚠️ 方向不是一律双向：评论双向 / 二级回复再多一对（「与我互相回复对话过」唯一落点）/ **点赞单向**（只有"赞过我的"，没有"我赞过的"）
                                                        # 留 50 给 30 是刻意的冗余 —— 拉黑排除在查询时做，正好存 30 的话排掉 3 个就只剩 27
                                                        # AC3 的双向拉黑排除走**既有统一出口**：在 UserHideRelationReader 上加了批量方法 hiddenEitherWay，没在 mention 模块自己写查询（AD-7）
                                                        # AC4：一次取数**恰好三条查询**，与候选人数无关；AC5：端点没有任何查询参数（反射测试钉住），**没有全局用户搜索**
                                                        # 注销级联（D1/D2）已接入 AccountDeletionService，**两个方向都物理删**
                                                        # ⚠️ code-review 修掉 4 条：@Async 乱序到达让排序键倒退（→ GREATEST）/ 二级回复两对关系共用一个容错块，第一对失败会把"对话过"那一对整个跳过 / 为不展示的运营标签多查一次库（→ 新增 findAuthorViewsWithoutTags）/ SecurityConfig 漏 hasRole("USER")，兽医拿到 401 被客户端当成 token 过期强制登出
                                                        # ⚠️ 存量互动**不回溯建表**：新表从上线起积累，再互动一次就有了
                                                        # L1 待本地验收（清单见 story Completion Notes；本 story 无 L2 项）
  3-2-at选择器与插入: ready-for-dev                      # 全新叠加层；仅在候选集内过滤（**不是搜索框**）；上限 5 人；🔴 **存 userId 不存昵称**
  3-3-at的展示与跳转: ready-for-dev                      # 依赖 Epic 2。昵称按 userId 实时查（对方改名自动跟着变）；拉黑关系不高亮
  3-4-被at的通知: ready-for-dev                          # 🔴 新增通知类型必须 **DROP + ADD 重列 CHECK 全集**，取值以代码枚举为唯一权威 + 复查并行分支。**此处已出过四次事故，是验收条件不是提醒**
  3-5-at的存量与删除后边界: ready-for-dev                # 存量不回溯；删除后通知不撤回 → 「内容已删除」统一空态；埋点属性禁含自由文本

  # ===== Epic 4: 逛别人家的毛孩子 · FR-121 + FR-112.4（顶部统一区另一半）=====
  # 依赖 Epic 2（复用 2-3 的访客视图入口，不新建通道）；4-4 需 1-1 的场所入口行已落地。
  epic-4: in-progress
  4-1-推荐规则与宠物卡: ready-for-dev                    # 近 14 天公开 Diary 帖 + 有头像 + 公开记录≥3；**不复用 FR-95 打分**。⚠️ 实时 SQL（不许加缓存）→ 必须给索引方案并贴执行计划；降级只许 @Scheduled 落表
  4-2-diary声明未养宠态也看到推荐: ready-for-dev         # B1-D2 对 PRD 原文的扩展：代码里有**两种**无档案状态，PRD 只写了未建档那一种。游客态不动
  4-3-全屏推荐集合页: ready-for-dev                      # 2 列网格 + 分页 + 空态；**「查看全部」入口由本条一并加上**（它的落点是本条才交付的页面，放 4-1 会成前向依赖）
  4-4-探索tab顶部宠物横滑行: ready-for-dev               # 加在 1-1 已落地的场所入口行**之下**；两模块分层清晰不叠一堆；推荐池为空时整行不渲染
