---
baseline_commit: 4301227a
branch: feat/1.3.0-ops-ui-refactor
epic: 3
story: 3.3
ad: [AD-3]
decisions: [D-15]
depends_on: [3-2]
---

# Story 3.3: 自愈跑批与历史回填（AD-3）

Status: review

> 自包含 story，可本地或云端 L0 执行。与用户沟通用中文。执行纪律见根 `CLAUDE.md`（异步只用 `@Scheduled` + DB 状态机，禁 MQ/调度中间件）。
> 纯后端：1 个 `@Scheduled` 物化器 + 1 个 stag 专用手动触发端点 + 配置项。无迁移、无页面。

## Story

As a 运营，
I want 看板每天自动补齐到昨天的数据，上线当天就能看到 7 月 17 日以来的完整趋势，
so that 不需要任何人手工回填或补跑。

## Acceptance Criteria

**AC1 · 缺日自愈算法**
**Given** `DashboardMaterializer`（`admin/dashboard/service/`）
**When** 运行一次
**Then** 计算 `[petgo.admin.dashboard.backfill-from（默认 2026-07-17）, 昨天(WIB)]` 内 `ops_daily_metrics` 缺失的日期集合（以 `findDistinctReportDates` 差集），按日期升序逐日物化 `[L1]`
**And** 每日 = 18 项 × 口径（9 项双口径 → 27 行）在**一个事务**内写入；任一项失败整日回滚，其余日不受影响 `[L1]`
**And** 已存在的日期不重算、不覆盖（行不可变）`[L1]`

**AC2 · 定时**
**Then** `@Scheduled(cron = "${petgo.admin.dashboard.cron:0 0 1 * * *}", zone = "Asia/Jakarta")`，即每天 WIB 01:00 `[L0]`
**And** 首次部署后第一次运行即完成 D-15 全量回填；模拟「某日失败」后下一次运行自动补齐（OQ-B2）`[L1]`

**AC3 · stag 手动触发**
**Given** `POST /admin/dashboard/materialize`（`@StagOnly`，`hasRole('SUPER_ADMIN')`）
**Then** 同步执行 AC1 一次并返回 `{materializedDates:[…], skipped:n, tookMs}` 的 fragment/JSON（后台内部用，可返 JSON）；生产 profile 下该 Controller **不注册**（Bean 不存在，非 403）`[L1]`

**AC4 · 日志与审计**
**Then** 应用日志只记日期与耗时，不记指标值；不写审计（系统行为）`[L0]`
**And** 单日失败 `log.warn` 含日期与异常类名，不含 SQL 参数值 `[L0]`

**AC5 · 配置**
**Then** `application.yml` 新增 `petgo.admin.dashboard.cron` 与 `petgo.admin.dashboard.backfill-from`（带默认值、可 env 覆盖）；`.env.example` 不需新增（有默认值）`[L0]`

**AC6 · 回归**
**Then** `mvn -B clean package` 通过；既有 4 个 admin 扫描器不受影响 `[L0]`

---

## Tasks / Subtasks

- [x] **T1 · `DashboardMaterializer`**（AC1/AC2）
  - [x] `@Component`，注入 `List<MetricQuery>`（Spring 按类型收集 18 个）、`OpsDailyMetricRepository`、`TransactionTemplate`
  - [x] `materializeMissing()`：`LocalDate today = LocalDate.now(ScheduleWindow.WIB); LocalDate end = today.minusDays(1)`；缺日 = `from..end` − `findDistinctReportDates()`；for each：`tx.execute(status -> materializeDay(d))`，catch 记 warn 继续下一日
  - [x] `materializeDay(d)`：for m in `DashboardMetric.values()`：`ALL` 一行；`m.dualScope()` 再算 `REAL` 一行；`value` NULL → 0（#12 空集）；批量 `saveAll`
  - [x] `@Scheduled(cron=…, zone="Asia/Jakarta")` 的 `run()` 调 `materializeMissing()`，`try/catch` 兜底日志（照 `SipdhExpiryScanner` 范式）
  - [x] 🔴 并发防护：用 `pg_advisory_xact_lock(hashtext('ops_daily_metrics'))` 或 `UNIQUE` 冲突捕获，防手动触发与定时同时跑重复插入
- [x] **T2 · 配置**（AC5）
  - [x] `application.yml` 在 `petgo:` 下加：
    ```yaml
    admin:
      dashboard:
        cron: ${ADMIN_DASHBOARD_CRON:0 0 1 * * *}     # WIB 01:00，跑批物化到昨天
        backfill-from: ${ADMIN_DASHBOARD_BACKFILL_FROM:2026-07-17}   # D-15 全量回填起点
    ```
  - [x] `@ConfigurationProperties("petgo.admin.dashboard")` record `DashboardProperties(String cron, LocalDate backfillFrom)`
- [x] **T3 · `@StagOnly` 与手动端点**（AC3）
  - [x] 若 Story 2.2 尚未落 `admin/shared/StagOnly.java`：本 story 先建（`@Profile("stag")` 的组合注解：`@Target(TYPE) @Retention(RUNTIME) @Profile("stag")`），2.2 复用
  - [x] `AdminDashboardMaterializeController`（`@StagOnly @RestController`）：`POST /admin/dashboard/materialize`，`@PreAuthorize("hasRole('SUPER_ADMIN')")`，返回执行结果 JSON
  - [x] 🔴 现有代码里**没有** `@Profile("stag")` 用法（grep 为零；B12 模拟回调如何门控需 dev 时核实 `AdminPaymentController`），本 story 建立第一处，Javadoc 注明「生产不注册 Bean」
- [x] **T4 · 测试**
  - [x] L0：缺日集合计算纯函数单测（from > end 返回空；中间缺 2 天只补 2 天）
  - [x] L1：`DashboardMaterializerTest extends ApiIntegrationTest`：空表 → 跑一次 → 行数 = 日数 × 27；人为删掉某日 → 再跑只补该日；已有行 value 不变；某 `MetricQuery` 抛异常 → 该日无任何行、其余日正常
  - [x] L1：`@ActiveProfiles("stag")` 下端点 200；默认 profile 下 `context.getBeansOfType(AdminDashboardMaterializeController)` 为空
- [x] **T5 · 云端执行须知**
  - [x] 云端 L0：编译 + 纯函数单测；L1 留本地并标注

---

## Dev Notes

### 🔴 回填与补偿是同一段代码

不写单独回填脚本、不写手工补偿入口。「找缺日 → 逐日物化」这一个循环既完成上线首日的全量回填（起点 2026-07-17 = 参考 SQL 创建日），也在某天跑批失败后于次日自动补上。运维动作为零，这是 AD-3 的核心。

### 🔴 昨天 = WIB 的昨天

`LocalDate.now(ScheduleWindow.WIB).minusDays(1)`。cron 也用 `zone = "Asia/Jakarta"`（既有扫描器多用 UTC，本处必须 WIB：01:00 WIB 才保证「昨天」已完整）。不要用 `LocalDate.now()`（容器 UTC）。

### 🔴 单日一事务，跨日互不影响

18 项里有 LATERAL 全表打分（#11 #12），单日耗时可能数秒；用 `TransactionTemplate` 而非方法级 `@Transactional`，避免整个回填 50+ 天一个巨事务。失败日只记 warn，下次再补。

### 🔴 `@StagOnly` 是本版第一处 profile 门控

架构把 `@StagOnly` 定为 `admin/shared` 横切件（结构树已列）。现有代码没有 `@Profile("stag")` 用法，B12「模拟回调」的门控方式需 dev 时读 `AdminPaymentController` 核实；本 story 以 `@Profile("stag")` 组合注解建立第一处，Story 2.2（STAG 角标）与 8.5（模拟回调改造）复用。stag 环境 profile 名以 `scripts/deploy-backend-stag.sh` 注入的 `SPRING_PROFILES_ACTIVE` 为准，dev 时核对。

### 现状代码要点

| 文件 | 现状 | 本 story |
|---|---|---|
| `admin/vetqual/service/SipdhExpiryScanner.java` | `@Scheduled(cron="${petgo.vet.sipdh-expiry-scan-cron:0 0 3 * * *}", zone="UTC")` + try/catch + `log.info` 计数 | 范式照抄，zone 改 WIB |
| `shared/schedule/ScheduleWindow.java` | `WIB` 常量 | 复用 |
| `application.yml` | `petgo.lifecycle-push.cron` / `petgo.lark-content.sync-cron` 等 cron 配置样式 | 同样式加 `petgo.admin.dashboard.*` |
| `admin/dashboard/metrics/*MetricQuery`（3-2） | `compute(LocalDate, MetricScope)` | 消费 |

### 测试标准

- L1 基类 `ApiIntegrationTest`；跑法 flush Redis DB0 → scratch 库 → `DB_NAME`。
- stag profile 测试用 `@ActiveProfiles("stag")` 单独一个测试类，避免污染其它上下文缓存。

### Project Structure Notes

- `admin/dashboard/service/DashboardMaterializer.java`、`admin/dashboard/web/AdminDashboardMaterializeController.java`、`admin/dashboard/config/DashboardProperties.java`、`admin/shared/StagOnly.java`。

### References

- [Source: architecture-v1.3.0-admin-delta.md#AD-3 / Infrastructure & Deployment]
- [Source: PRD-v1.3.0-admin.md#1 ② 历史回填（D-15）/ §9 OQ-B2 OQ-B3]
- [Source: 决策日志-admin.md D-15]
- [Source: petgo-backend/src/main/java/com/tailtopia/admin/vetqual/service/SipdhExpiryScanner.java]

## Dev Agent Record

### Agent Model Used

claude-fable-5-1（云端 headless session，2026-09-09）

### Debug Log References

- 云端 L0：`./mvnw -B clean package`（排除需真库的 Spring 上下文测试类）→ BUILD SUCCESS，0 failures；`DashboardMaterializerTest`（L0，事务管理器 / 仓储 mock）通过。
- 无迁移、无 i18n 改动；`check-i18n-keys.sh` OK。`application.yml` 新增 `petgo.admin.dashboard.cron` / `backfill-from`（带默认值，env `ADMIN_DASHBOARD_CRON` / `ADMIN_DASHBOARD_BACKFILL_FROM` 可覆盖）；`.env.example` 未动。
- 复审（bmad-code-review，CONFIRMED 6 / PLAUSIBLE 3）已修：③ stag 端点 L1 测试跑完按返回的 `materializedDates` 删行（不再在共享库留下几十天 × 27 行）；④ `ApiIntegrationTest` 基类加 `petgo.admin.dashboard.cron=-`（`CRON_DISABLED`），防定时线程在套件跨 18:00 UTC 时抢先物化导致随机红；⑤ L1 补「落库中途失败（值超出 NUMERIC(18,4)）→ 该日 0 行、其余日正常」用例；⑥ `DashboardProperties.cron` 保留（story T2 要求）但 Javadoc 明写运行时以 `@Scheduled` 占位符为准；⑦ 单项取数失败包成 `MetricFailure`（warn 含 `metric key / scope / 原异常类名`，不含值与 SQL 参数），定位是哪条 SQL 挂了；⑧ `TransactionTemplate` 明示 `PROPAGATION_REQUIRES_NEW`；②「手动触发」补可行调用路径（见 Notes）；① staging profile 阻塞项见 Notes 🔴。

### Completion Notes List

- **L1/L2 待本地验收**：① `DashboardMaterializerIntegrationTest`（真库）：近 3 天空表 → 跑一次 → 每日 27 行；删掉中间一天 → 再跑只补该日、其它日 value 不变；某项在指定日抛异常 → 该日无任何行、其余日正常，下一次自动补齐（OQ-B2）；默认 profile 下 `AdminDashboardMaterializeController` Bean 不存在；② `DashboardMaterializeEndpointStagTest`（`@ActiveProfiles({"dev","stag"})`）：STAFF 403、SUPER_ADMIN 200 且 JSON 含 `materializedDates / skipped / failedDates / tookMs`；③ stag 部署后首次 01:00 WIB（或手动 `POST /admin/dashboard/materialize`）完成 2026-07-17 起全量回填，核对 `ops_daily_metrics` 行数 = 天数 × 27，并看单日耗时（#11 / #12 LATERAL 全表打分）是否可接受。
- **AC1 / AC2 自愈算法**：`DashboardMaterializer.materializeMissing()`：`end = LocalDate.now(WIB) - 1`、`from = backfillFrom`，缺日 = 区间 − `findDistinctReportDates()`，升序逐日 `TransactionTemplate.execute`（单日一事务：18 项 ALL + 9 项 REAL = 27 行 `saveAll`；任一项异常整日回滚，只记 warn 含日期与异常类名，继续下一日）。已存在日期不重算不覆盖。回填与补偿是同一段循环，无独立脚本。`@Scheduled(cron = "${petgo.admin.dashboard.cron:0 0 1 * * *}", zone = "Asia/Jakarta")`，兜底 try/catch 照 `SipdhExpiryScanner`。
- **并发防护**：每日事务内先 `SELECT pg_advisory_xact_lock(hashtext('ops_daily_metrics'))`，再 `existsByReportDate` 复查，被并发跑批抢先的日子计入 `skipped`；`uq_ops_daily_metrics` 唯一约束兜底。
- **构造期校验**：`List<MetricQuery>` 必须恰好覆盖 18 个 key（缺 / 重复 → 启动即 `IllegalStateException`），防止 3-2 新增指标漏了查询类却静默少行。
- **🔴 AC3 在真实 staging 的前提（复审 ①，需运维处理，本 session 不碰部署）**：`docs/runbooks/runbook-staging.md` 与 `~/.env.petgo-stag` 当前给 staging 容器的是 `SPRING_PROFILES_ACTIVE=prod`，而 `@StagOnly` = `@Profile("stag")`。**不把 stag 加进 profile，本端点（以及 Story 2.2 的 STAG 角标 `StagFlag`）在真实 staging 都不会注册。** 处理建议：`~/.env.petgo-stag` 改 `SPRING_PROFILES_ACTIVE=prod,stag`（不要在 `application-prod.yml` 里 include，会把生产也激活），runbook C2 同步；改完 `curl -s https://api-stag.tailtopia.id/actuator/health` 应仍 UP。L1 `DashboardMaterializeEndpointStagTest` 用 `{dev, stag}` 组合，与线上 `prod,stag` 对应关系已写在测试 Javadoc。
- **手动触发的调用路径（复审 ②）**：admin 链 CSRF 开启，纯 curl 会 403。两条路：(a) 看板页底部 stag 专用按钮「立即跑批（stag）」（`th:if="${stag}"` + `hasRole('SUPER_ADMIN')`，htmx `hx-post`，`admin-core.js` 自动注入 CSRF 头，结果 JSON 落在按钮下方 `<pre>`）——随 Story 3.4 的看板页落地；(b) 登录后浏览器 devtools：`const s=document.querySelector('[data-csrf]'); fetch('/admin/dashboard/materialize',{method:'POST',headers:{[s.dataset.csrfHeader]:s.dataset.csrfToken}}).then(r=>r.json()).then(console.log)`。
- **AC3 stag 手动触发**：`AdminDashboardMaterializeController`（`@StagOnly @RestController`，`POST /admin/dashboard/materialize`，`hasRole('SUPER_ADMIN')`）同步跑一次并返回 `Result` JSON。`@StagOnly` 已由 Story 2.2 落在 `admin/shared`（`@Profile("stag")` 组合注解），本 story 复用，生产不注册 Bean。**结果字段比 AC 多一个 `failedDates`**（跑批失败日列表，运维排障必需；`skipped` = 区间内已物化 / 被并发抢先的日数）。
- **AC4 日志**：只记日期、计数、耗时、异常类名；平均类指标空集存 0 时记 `date + metric key + scope`（不记值）。不写审计。
- **依赖 3-1 口径待拍板项**：`countByRealAuthor` 与 `EXCLUDE_WHERE` 的 `role=ADMIN` 分歧（3-1 Notes）若改口径，需清空并重跑长表（行不可变，改口径 = 换数据）。

### File List

- petgo-backend/src/main/java/com/tailtopia/admin/dashboard/service/DashboardMaterializer.java（新增）
- petgo-backend/src/main/java/com/tailtopia/admin/dashboard/web/AdminDashboardMaterializeController.java（新增，`@StagOnly`）
- petgo-backend/src/main/java/com/tailtopia/admin/dashboard/config/{DashboardProperties,DashboardConfig}.java（新增）
- petgo-backend/src/main/resources/application.yml（`petgo.admin.dashboard.*`）
- 测试：admin/dashboard/service/DashboardMaterializerTest（新增 L0）、DashboardMaterializerIntegrationTest（新增 L1）、admin/dashboard/web/DashboardMaterializeEndpointStagTest（新增 L1，stag profile）
