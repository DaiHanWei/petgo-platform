---
baseline_commit: ac3d57b733769b3bbc374e82db59a07082e26522
epic: 5
story: 3
batch: A
verification: [L0, L1]
---

# Story 5.3: 年龄卡分享奖励链路

Status: ready-for-dev

## Story

As a 用户，
I want 分享年龄卡也能拿到 PawCoin，
so that 我有动力把它发出去。

---

> **含一条资金口径的安全攸关约束**：日界必须按 WIB 当地日切，按 UTC 切会让印尼用户在早上 6–7 点领双份。
>
> **本 story 最容易漏的是「还有一整张账本表」** —— 既有 KTP 渠道除两列配置外，另有 `id_card_share_rewards` 表记去重与日限。只加两列不算接通。
>
> 依赖 Story 5.2（分享动作已存在）。5.2 不含奖励也能独立交付，故本条不是 5.2 的前置。

---

## Acceptance Criteria

### AC1 — 渠道配置两列（L1）

**Given** PawCoin 配置表
**When** 迁移执行完毕
**Then** 新增年龄卡渠道两列：单次奖励、单日上限 `[L1]`
**And** 两列默认 0（不发币），含 `>= 0` CHECK `[L1]`
**And** 与既有 `id_card_share_reward` / `id_card_share_daily_cap` 同构 `[L0]`

### AC2 — 🔴 专用账本表（L1）

**Given** 数据库
**When** 迁移执行完毕
**Then** 存在年龄卡分享奖励**账本表**，记录发放与日限 `[L1]`
**And** **不做档案级去重** —— 无 `pet_profile_id` 唯一约束（决策 A-8） `[L0]`
**And** 迁移文件名为时间戳制 `[L0]`

> KTP 的账本以「宠物档案首次」去重，年龄卡没有卡实体、也不该按档案唯一（同一只宠物隔几个月再生成是不同的分享物），**塞不进那张表**。

### AC3 — 🔴 日界按 WIB（资金口径，L1）

**Given** 单日上限的「日」
**When** 判定
**Then** 按 **WIB（`Asia/Jakarta`）当地日**切分 `[L1]`
**And** 复用既有唯一实现（`IdCardShareRewardService.shareDateOf` / `ShareRewardService.periodOf`），**不另写换算** `[L0]`
**And** **不得按 UTC 切** `[L0]`

> 🔴 按 UTC 切会让「今天」在印尼早上 7 点才换。运营配「3 次/日」，用户在 06:00–07:00 能领双份。**这是资金口径错误，不是体验问题。**

### AC4 — 判断顺序（L1）

**Given** 一次领奖请求
**When** 服务端处理
**Then** 判断顺序为：去重命中 → 渠道日上限 → 月度全局额度 `[L1]`
**And** 顺序不可调换（最便宜的判断在前，避免占掉额度又退回） `[L0]`

### AC5 — 共用全局月度上限（L1）

**Given** 月度上限
**Then** 与其他渠道**共用同一个全局月度上限** `[L1]`
**And** 年龄卡渠道**不设**独立月度上限 `[L0]`
**And** 全局总开关关闭时，本渠道一并停发 `[L1]`

### AC6 — 领奖调用不带内容（安全，L0）

**Given** 领奖调用
**Then** 只携带渠道标识与幂等键 `[L0]`
**And** **不携带卡面内容、不上传图片** `[L0]`

> 「年龄卡不落服务端」指的是**卡片图像**；领奖是一次独立的服务端调用，这是已澄清的唯一例外。

### AC7 — 后台配置（L1）

**Given** 运营后台 PawCoin 配置页
**Then** 可编辑年龄卡渠道两列 `[L1]`
**And** 变更写入既有配置变更日志 `[L1]`

### AC8 — 幂等（L1）

**Given** 同一次分享重复上报
**When** 服务端处理
**Then** 只发放一次（幂等键兜底） `[L1]`

---

## Tasks / Subtasks

- [ ] 时间戳迁移①：PawCoin 配置表加两列 + CHECK（AC1）
- [ ] 时间戳迁移②：建年龄卡分享奖励账本表（AC2）
- [ ] 领奖端点：三层判断按序，日界走既有 WIB 实现（AC3/AC4/AC5/AC8）
- [ ] 客户端分享成功后调用领奖（AC6）
- [ ] 后台配置项 + 变更日志（AC7）
- [ ] 测试：WIB 跨日边界（06:00–07:00 那一小时必须覆盖）、幂等、全局开关（AC3/AC5/AC8）

## Dev Notes

### 三层结构（原先误认为两层）

| 层 | 落点 | 年龄卡要做什么 |
|---|---|---|
| 全局 | `share_reward_enabled` / `share_reward_monthly_cap` | **不动**，共用 |
| 渠道配置 | PawCoin 配置表两列 | **新增两列** |
| 渠道账本 | 专用表 | **新建一张** |

### 要改的文件

| 文件 | 改什么 |
|---|---|
| `petgo-backend/src/main/resources/db/migration/V<ts>__*.sql` ×2 | 配置两列 / 账本表 |
| `petgo-backend/.../config/domain/PawCoinConfig.java` | 两列字段 |
| `petgo-backend/.../admin/config/service/AdminConfigService.java` | 变更日志 diff |
| `petgo-backend/.../share/service/IdCardShareRewardService.java` | **只读参照**（三层顺序、WIB 实现、幂等键前缀） |
| `petgo-backend/.../share/service/`（新建） | 年龄卡领奖服务 |

### 🔴 WIB 那行注释是前人踩过坑留下的

`IdCardShareRewardService` 里写着「WIB。日上限按**当地日**切；UTC 切日会让『今天』在 WIB 早上 7 点才换」，并标注为「唯一实现」。**照抄，不要另写。**

### 验证层级

- `[L0]` 编译、无档案级唯一约束、未另写时区换算
- `[L1]` **WIB 跨日边界用例**、三层判断顺序、幂等、全局开关 —— 留本地

### References

- 架构 delta `AD-A20`（三层 + WIB + 去重键）· 决策日志 `A-8`

---

## Dev Agent Record

### Context Reference

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

---

## Change Log

| 日期 | 变更 | 作者 |
|---|---|---|
| 2026-09-10 | 创建 | bmad-create-epics-and-stories |
