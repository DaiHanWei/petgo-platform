---
baseline_commit: 4301227a
branch: feat/1.3.0-ops-ui-refactor
epic: 7
story: 7.2
ad: [AD-9, AD-11]
decisions: [D-8, D-23]
page: B2（页签一）
depends_on: [2-3a, 2-3b]
coordinates_with: [4-1]
---

# Story 7.2: B2 评论巡查页签套模板 B

Status: review

> 自包含 story。与用户沟通用中文。纪律见根 `CLAUDE.md`。
> 依赖 2.3a / 2.3b。**与 Story 4.1（AB-20A「帖子评论分布」页签二）共用 `comments.html` 的页顶页签条**——协作边界见 Dev Notes。端点零变更。

## Story

As a 运营，
I want 评论巡查列表有摘要条、抽屉看评论全文与所属帖子，
so that 与其他列表页一致。

## Acceptance Criteria

**AC1 · 页签条（与 4.1 共享）**
**Given** `/admin/comments`
**Then** 页顶常驻两页签「评论巡查」｜「帖子评论分布」，htmx 局部切换（`hx-get` 页签内容区 `#comments-tab`）；本 story 只实现页签一，页签二占位指向 `GET /admin/comments/distribution`（4.1 实现，未实现前返回「即将上线」fragment）`[L2]`
**And** 页签条 fragment 抽为 `fragments/comments-tabs.html`，4.1 复用不重写 `[L0]`

**AC2 · 筛选与摘要条**
**Then** 筛选：状态（全部 / VISIBLE / UNDER_REVIEW / TAKEN_DOWN / REJECTED / 已删除）· 帖子 ID · 关键词（显式查询钮）；现状 `service.recent()` 无筛选 → 新增分页查询方法（GET 查询增强，非写端点）`[L1]`
**And** 摘要条：总评论数 · 今日新增（WIB）· 已下架，随筛选联动，单条 `COUNT FILTER` `[L1]`

**AC3 · 表格**
**Then** 列 = ID ｜ 所属帖子（摘要 + 带图缩略；点击跳 B1 抽屉 `/admin/content?open=<postId>`）｜ 作者 ｜ 内容 ｜ 状态（色点 + 徽标）｜ 时间（WIB）；默认时间倒序，每页 20 `[L2]`

**AC4 · 详情抽屉**
**When** 点行 → `GET /admin/comments/{id}/drawer`
**Then** 评论全文 + 所属帖子摘要卡（首图预览 + 正文摘要 + 作者，可跳 B1 抽屉）+ 作者卡（昵称 / id / 注销置灰）+ 操作条 `[L2]`

**AC5 · 操作（端点零变更）**
| 按钮 | 端点 | 权限 | 确认 |
|---|---|---|---|
| 下架 | `POST /admin/comments/{id}/takedown` | `content.proactive_takedown` | data-confirm |
| 恢复 | `POST /admin/comments/{id}/restore` | `content.restore` | 无 |
**Then** 按钮显示条件沿现状：仅 `VISIBLE` 可下架；`TAKEN_DOWN` / `REJECTED` 可恢复；用户自删（`deleted`）只读展示删除态 `[L1]`
**And** htmx：200 + 抽屉 fragment + oob 行；422 行内 err；权限不足禁用注明 `[L1]`

**AC6 · 三语与回归**
**Then** 新 key `admin.v130.comments.*` 三包；`AdminCommentModerationIntegrationTest` 全绿；新增 MockMvc 四条 `[L0/L1]`

---

## Tasks / Subtasks

- [x] **T1 · 查询增强**（AC2）
  - [x] `AdminCommentModerationController` 的 `GET /admin/comments` 增加 `status / postId / q / page` 参数；service 新增 `search(filter, pageable)` 与 `summary(filter)`；`recent()` 保留给旧调用方或删除（grep 无其他调用即删）
- [x] **T2 · 页签条与模板 B 壳**（AC1 / AC3）
  - [x] `fragments/comments-tabs.html`（两页签，`aria-selected`，htmx 切换 `#comments-tab`）
  - [x] `comments.html` 套 `tpl-b-list`：filters / summary / table / drawer；页签一内容为默认渲染
  - [x] 页签二占位：`GET /admin/comments/distribution` 若 4.1 未合入，返回 `fragments/coming-soon.html`（三语一句话）；4.1 合入后替换
- [x] **T3 · 抽屉**（AC4 / AC5）
  - [x] `GET /admin/comments/{id}/drawer`（权限同列表 `content.proactive_takedown`）；`fragments/drawer-comment.html`
  - [x] `AdminCommentManageController` 两个 POST 增加 `HxRequest` 分支返回 drawer fragment + oob `<tr id="comment-{id}">`
- [x] **T4 · 三语 + 测试**（AC6）
- [x] **T5 · 云端执行须知**：L0 云端；抽屉与页签切换 L2 留本地

## Dev Notes

### 现状与改动边界

| 文件 | 现状 | 本 story | 保留 |
|---|---|---|---|
| `admin/comment/web/AdminCommentModerationController.java` | `GET /admin/comments`（`content.proactive_takedown`），`service.recent()` 无筛选无分页 | + 筛选/分页参数 + drawer 端点 | 权限表达式 |
| `admin/moderation/web/AdminCommentManageController.java` | `POST {id}/takedown`（`content.proactive_takedown`）、`POST {id}/restore`（`content.restore`） | + htmx 分支 | 路径参数 |
| `templates/admin/comments.html` | 单表：ID / 帖子 / 作者 / 内容 / 状态 / 时间 / 操作（行内两个表单） | 套模板 B + 页签条 + 抽屉 | 按钮显示条件 |

### 🔴 与 Story 4.1 的协作边界
- 本 story 拥有：页签条 fragment、页签一全部、`comments.html` 壳。
- 4.1 拥有：`GET /admin/comments/distribution*`、`fragments/comments-distribution.html`、「去评论」抽屉、`POST /admin/comments/virtual`。
- 4.1 **不得**改页签条以外的页签一内容；本 story **不得**实现分布查询。两者合并冲突点只有 `comments.html` 顶部的页签条 include 一行。
- 评论管理没有独立查看权限码（AB-21A 现状不规整项），本页继续用 `content.proactive_takedown`，不新增码。

### 🔴 「已删除」语义
现状按钮条件：`!c.deleted and moderationStatus=='VISIBLE'` 才能下架；`TAKEN_DOWN|REJECTED` 才能恢复。用户自删（`deleted_at` 非空）任何操作都不给，抽屉只显示删除态。不要把「已下架」与「已删除」混成一个状态。

### 端点零变更清单
`POST /admin/comments/{id}/takedown` · `POST /admin/comments/{id}/restore`。新增 GET：`/admin/comments/{id}/drawer`（查询参数增强不算新写端点）。

### 测试标准
`AdminCommentModerationIntegrationTest` 范式；筛选组合 × 分页；抽屉 fragment；四条 MockMvc。

### Project Structure Notes
改动落 `admin/comment/**`、`admin/moderation/web/AdminCommentManageController.java`、`templates/admin/comments.html`、`fragments/comments-tabs.html`、`fragments/drawer-comment.html`。

### References
- [Source: 后台重构逐页规格.md#B2 · 评论管理（页签一部分）/ 模板 B 通用规格]
- [Source: ui-v1.3.0-admin.html 泳道 2 帧 2-6～2-7]
- [Source: architecture-v1.3.0-admin-delta.md#AD-9 / AD-11]
- [Source: PRD-v1.3.0-admin.md#6 AB-20A（页签结构）· §7 ④（评论无独立查看码）]
- [Source: epics-v1.3.0-admin.md#Story 7.2 / Story 4.1]

## Dev Agent Record

### Agent Model Used

claude-opus-5（云端 headless session，2026-09-10）

### Debug Log References

- 云端 L0：`./mvnw -B clean package`（排除需真库的 Spring 上下文测试类）→ BUILD SUCCESS，1831 tests，0 failures；`AdminTemplateStructureTest`（含 6.5 的两条属性优先级守卫）、`AdminTemplateShellRenderTest`、`AdminMessagesExternalizedTest` / `AdminMessagesParityTest`、`AdminCommentModerationServiceTest`（改测 `detail` 的映射与富化）全绿。**无迁移**；写端点零新增。
- `check-i18n-keys.sh` OK（四包 2713 key 集合相等、无重复；+21 key：`admin.v130.comments.{filter.*, status.*（6 态）, summary.*（3）, pager.total, drawer.title, post.open, post.gone, confirm.takedown, deletedNote}` 与 `admin.err.comment.notFound`、`admin.v130.comments.col.post`；`admin.comments.hint` 四包改词——原文案写「近 200 条评论」，列表改成筛选 + 分页后这句话不再成立）。
- 复审（bmad-code-review）：**CONFIRMED 4 条：3 条已修 + 1 条是流程节点（story 回写本就在提交前做，见本文件）**；PLAUSIBLE 9 条已修、3 条留作记录。
  - ① 🔴 `aria-selected` 是服务端按 `${tab}` 算死的，而页签二是 htmx 局部切换、不整页刷新 —— 切完之后视觉高亮在「帖子评论分布」，DOM 里 `aria-selected="true"` 还钉在「评论巡查」上，读屏用户拿到与视觉相反的状态。改：`comments.html` 的 `htmx:afterSwap` 里连 `aria-selected` 一起同步，`#comments-tab-body` 补 `role="tabpanel"`。
  - ② 🔴 AC1 的测试断言被污染：`comments.html` 的内联脚本里就有 `.comments-tabs`，所以把页签条 include 整行删掉、这条断言照样绿 —— 本 story 的核心动作恰恰是「抽成 fragment」，等于没测。改断 `role="tablist"` / 分布页签的 href / `aria-selected=` 这些页签条真实渲染出来的结构。
  - ③ 🔴 `AdminCommentRow` 随 `recent()` 一起成了死代码（全仓只剩自己的定义行）→ 删除（T1 原话就是「grep 无其它调用即删」）。
  - ④ story 回写与 sprint-status：本会话的流程是「复审 → 修 → 回写 → 提交」，回写就在这一步做（Status→review、Tasks 全勾、Dev Agent Record 三节）。
  - ⑤ 422 落在抽屉顶部而两个按钮在抽屉底部：评论正文最长 1000 字，滚到底提交时顶部的报错早已滚出视口 —— err 槽改放在操作条正上方（与 7.1 复审 C1 同一件事）。
  - ⑥ 处置掉「第 N 页最后一行」后按 `data-page=N` 重拉会落到空页，而空态里没有分页器，运营就此卡住 → `search` 页码越界时回退到最后一页，并把**实际页码**带给分页器。
  - ⑦ `keyword.toLowerCase()` 未带 locale（土耳其语 I/ı 会让 Specification 与摘要 SQL 分岔）→ `Locale.ROOT`。
  - ⑧ `AUTHOR_DEACTIVATED` 原先落在「非 VISIBLE 一律标红」的分支里 —— 它是作者注销的级联隐藏、不是违规态，标红会让运营当成处置对象 → 单独给 `badge-muted` / `dot--done`。
  - ⑨ 「所属帖子」列头复用了 `admin.comments.col.post`（zh 值是「帖子」），与 AC3 / UI 稿措辞不符 → 新增 `admin.v130.comments.col.post`（四包同批）。
  - ⑩ 列表正文列直出全文（最长 1000 字）会把 48px 行撑爆 → 列内 `abbreviate(60)` + `title` 挂全文（抽屉里仍是全文）。
  - ⑪ 「无下架权」的那行提示是死分支（进这个抽屉的前提就是 `content.proactive_takedown`）→ 删；`target="_blank"` 补 `rel="noopener"`；`.cm-drawer` 补一条样式而不是留空 class。
  - ⑫ 两个刷新事件常量原先散在各自控制器里，而 `AdminHxEvents` 的类注释写着「集中定义」→ B1 的两个与 B2 的一个一并收编进 `AdminHxEvents`（字面量不变，模板与测试不受影响）。
  - **留作记录不改**：列表侧 JPA count 与摘要 SQL 的 `COUNT(*)` 条件相同、跑了两次（要真省掉得改成不带 count 的 slice 查询，收益不抵改动面，条件已在两处互相点名要求对齐）；`escapeLike` 在仓库里已有四份逐字相同的拷贝（该收进公共工具，属跨 story 的清理）；AC5 措辞是「按钮禁用注明」而实现是「不渲染表单 + 提示句」（与 7.1 同一风格，L2 视觉验收再定）。

### Completion Notes List

- **L1/L2 待本地验收**：① 新增 `AdminCommentInspectIntegrationTest`（真库 + 真 Thymeleaf）：整页含页签条 + 模板 B 壳 + 摘要条 + `#comments-rows` + `#comment-drawer`，筛选参数 `status / postId / q` 齐、行 `data-drawer-url`、**行内处置表单已消失**、所属帖子链接指向 `/admin/content?open=<postId>`；摘要条三格随 `postId` 筛选联动（总数 2 / 今日 2 / 已下架 1）；按状态筛只剩已下架那条且摘要条随 oob；抽屉四区 + `#comment-drawer-err`，非 htmx 直达 302 `?open=`；htmx 下架 → `HX-Trigger: admin:comment-list-refresh` + 抽屉体 oob + 行 oob + toast，落库为 `TAKEN_DOWN` 且**未软删**（审核线语义）；原因为空 → 422；恢复 → 回 `VISIBLE`；用户自删的评论抽屉只显示删除态、两个处置都不给；无 `content.proactive_takedown` 的账号列表与抽屉都 403。② 既有回归：`AdminCommentModerationIntegrationTest`（`recent()` → `search(...)`）、`AdminCommentDistributionIntegrationTest` / `AdminVirtualCommentIntegrationTest`（4.1 / 4.2 的页签二与「去评论」抽屉未受影响）、`AdminPagesRenderSmokeTest`（`/admin/comments` 双语扫描）。③ L2：本地 stag 对照 UI 稿 2-6 / 2-7：两页签切换（页签一整页、页签二 htmx）、480px 抽屉四区、所属帖子缩略图与跳转、`?open=` 深链（含不在首页那一条）、行高与状态色点。
- **AC1 页签条**：从 `comments.html` 抽出 `fragments/comments-tabs.html`（`tabs` fragment），页签一整页链接、页签二 htmx 切换 `#comments-tab-body`，`aria-selected` 随 `tab` 变；两页签的 `sec:authorize` 与各自控制器门控逐字一致。4.1 与 7.2 的合并冲突点因此只剩 `comments.html` 里的一行 include。
- **AC2 筛选与摘要条**：`GET /admin/comments` 增 `status / postId / q / page`（+ `open` 深链），`AdminCommentModerationService.search(...)` 走 `Specification` + `Pageable`（时间倒序），`summary(...)` 一条 `COUNT(*) FILTER` 聚合（总评论数 · 今日新增按 WIB 自然日 · 已下架）；两处条件在文件里互相点名要求逐条对齐。状态取值非法时**当作不筛**（手改 URL 不出 500）。
- **AC3 表格**：列 = ID / 所属帖子（首图缩略 + 摘要，点进 B1 抽屉 `?open=<postId>`）/ 作者（注销置灰 + `#id`）/ 内容 / 状态（色点 + 徽标）/ 时间(WIB)；时间倒序、每页 20、底部分页。所属帖子与作者昵称**整页一次批量取**（`ContentService.adminRowsByIds` + `AccountQueryService.findAuthorViews`）。
- **AC4 抽屉**：`GET /admin/comments/{id}/drawer`（门控与列表同码）→ `fragments/drawer-comment :: drawer` 四区：评论全文 + 状态徽标 / 所属帖子卡（首图 + 摘要 + 跳 B1）/ 作者卡 / 操作条；非 htmx 直达回列表并自动开该抽屉；`?open=` 找不到行时用 `[data-drawer-deeplink]` 兜底（7.1 加的机制）。
- **AC5 操作**：`POST /admin/comments/{id}/takedown`（`reason`）与 `/restore` **路径与参数逐字不变**，只加 htmx 分支 → `:: afterAction`（抽屉体 oob + 行 oob + toast）+ `HX-Trigger: admin:comment-list-refresh`（列表按当前筛选重拉：状态变了之后这一行在当前筛选下可能整行都不该在，摘要条也全是旧值）。⚠️ 两个表单的 `hx-target` 指**带 id 的** `#comment-drawer-err`（7.1 复审 C1 的同一处教训：htmx 只在目标带 id 时才发 `HX-Target` 头）。按钮显示条件与 9.9 行内表单逐字一致：仅 `VISIBLE` 可下架、`TAKEN_DOWN` / `REJECTED` 可恢复、**用户自删只读**。
- **AC6 三语与回归**：21 个 key 四包同批；`admin.comments.hint` 改词（原文案「近 200 条评论」在分页化之后不再成立）。
- **偏差说明**：① story AC1 写的页签内容区 id 是 `#comments-tab`，实现沿用 4.1 已落地的 `#comments-tab-body` —— 改它等于把 4.1 的页签切换打断，且这个 id 在 4.1 的页内 JS 里还有一处引用。② **D-44 的「页签二返回即将上线占位」条款作废**：Story 4-1 已合入，分布页签是真的；`fragments/coming-soon.html` 与 `admin.v130.comments.distribution.comingSoon` 都不需要了（未创建）。③ `recent()` 与仓储的 `findTop200ByOrderByIdDesc` 一并删除（main 里无其它调用方，story T1 允许）；L0 单测改测 `detail(...)` 的映射与富化（覆盖同样的状态 / 删除态口径，另加所属帖子与作者富化）。④ 状态筛选比 AC 多列一个 `AUTHOR_DEACTIVATED`（可见性态枚举里本就有，漏掉它会让这批评论在「全部」之外无从单独筛出）。⑤ 摘要条的「已下架」只数可见性态 `TAKEN_DOWN` 且未软删 —— 与「已删除」严格分开（Dev Notes 🔴）。
- **端点底账（Epic 11 diff 基线）**：不变 —— `POST /admin/comments/{id}/takedown`、`POST /admin/comments/{id}/restore`；新增只读 `GET /admin/comments/{id}/drawer`；`GET /admin/comments` 仅增查询参数。

### File List

- petgo-backend/src/main/resources/templates/admin/comments.html（页签条改 include）、fragments/comments-tabs.html（新增）、fragments/comments-inspect.html（套模板 B：filters / summary / rows / row + 深链兜底）、fragments/drawer-comment.html（新增：四区 + afterAction）
- petgo-backend/src/main/java/com/tailtopia/admin/comment/service/AdminCommentModerationService.java（+`search` / `summary` / `detail` / 富化；−`recent`）、comment/web/AdminCommentModerationController.java（筛选分页参数 + 抽屉 GET + `LIST_REFRESH`）、comment/dto/{CommentInspectRow,CommentSummary}.java（新增）、moderation/web/AdminCommentManageController.java（两个 POST 加 htmx 分支）、content/repository/CommentRepository.java（+`JpaSpecificationExecutor`；−`findTop200ByOrderByIdDesc`）
- petgo-backend/src/main/resources/static/admin/admin.css（`.comments-table` / `.cm-*`）；i18n/messages{,_zh_CN,_en,_id}.properties（+21 key、1 条改词）；shared/web/AdminHxEvents.java（收编三个列表 / 抽屉刷新事件常量）；**删除** comment/dto/AdminCommentRow.java（随 `recent()` 一并作废）
- 测试：admin/comment/AdminCommentInspectIntegrationTest（新增 L1）、AdminCommentModerationIntegrationTest（`search`）、comment/service/AdminCommentModerationServiceTest（改测 `detail`）

## 拍板回写（2026-09-09）

- **D-44**：页签二在 Story 4-1 合入前返回「即将上线」占位 fragment（三语 key `admin.v130.comments.distribution.comingSoon`），4-1 合入后替换。
