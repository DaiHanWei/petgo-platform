---
baseline_commit: ac3d57b733769b3bbc374e82db59a07082e26522
epic: 1
story: 5
batch: A
verification: [L0, L1, L2]
---

# Story 1.5: 未庆祝角标与补庆祝

Status: review

## Story

As a 用户，
I want 我不在场时解锁的成就，下次进里程碑页时补一次庆祝，
so that 我不会莫名其妙错过自己达成的成就。

---

> **依赖 Story 1.4**（字段、回报端点、未庆祝计数均由 1.4 交付）。
>
> **本 story 是 Epic 1 唯一有可见新 UI 的一条**，但新 UI 只有一个角标 —— 庆祝页视觉**一字不改**（重绘属批次 C）。

---

## Acceptance Criteria

### AC1 — 档案 Tab 角标（L2）

**Given** 存在「已完成且未庆祝」的条目
**When** 打开成长档案 Tab
**Then** 里程碑进度条上显示角标（红点 / +N） `[L2]`
**And** 角标样式**直接复用** `notification_bell.dart` 的 `notificationBadge`（danger 红底 / min-width 16 / 圆角 8 / 白字 10px），不另画 `[L0]`
**And** 档案 Tab **不弹**全屏庆祝 `[L2]`

### AC2 — 列表页补弹（L2）

**Given** 存在未庆祝条目
**When** 进入里程碑列表页
**Then** 自动弹一次庆祝页，取级别最高的一条 `[L2]`
**And** 其余条目由 KOLEKSI 圆点带过 `[L2]`
**And** 本次展示覆盖的**全部条目**一并回报置位 `[L1]`
**And** 返回档案 Tab 后角标消失 `[L2]`

### AC3 — S 级也补（L2）

**Given** 未庆祝条目中有 S 级
**When** 进入列表页
**Then** 同样补弹，不按级别过滤 `[L2]`

### AC4 — 🔴「去发布」路径不连弹（L2）

**Given** 用户走「去发布」路径完成打卡
**When** 庆祝页弹出 → 关闭发布 sheet → 自动跳转里程碑列表页
**Then** 列表页**不再重复弹出**同一条庆祝 `[L2]`
**And** 该抑制由客户端在同一次导航内持有「刚庆祝过的 code 集合」实现 `[L0]`
**And** **不依赖**回报是否已落库 `[L0]`
**And** **不得**把回报改为同步来绕开（那会让发布流程卡在一个可失败的写上） `[L0]`

### AC5 — 重温不改状态（L1 / L2）

**Given** 已完成的徽章
**When** 点击重温庆祝页
**Then** 正常展示庆祝 `[L2]`
**And** **不**改写 `celebrated_at`、**不**产生回报 `[L1]`

### AC6 — 埋点（L1）

**Given** 任一次庆祝页展示
**When** 上报 `milestone_celebration_shown`
**Then** 携带 `code`、`level`、`path` `[L1]`
**And** `path` 值域为 `instant` / `catchup` / `revisit` 三值之一 `[L0]`
**And** 两台设备同时补弹会各报一次（**接受双计**，回报仍幂等） `[L0]`

### AC7 — 既有路径不变（回归保护，L2）

**Given** 即时庆祝路径与 500/800/1200ms 三次短轮询
**When** 本 story 改完
**Then** 全部保留，补庆祝是兜底不是替代 `[L2]`
**And** 「多条同解锁只弹最高一条」规则扩展为全路径统一（含补庆祝） `[L2]`
**And** 庆祝页三种关闭方式、解锁振动、已完成徽章重温**均不改动** `[L2]`

---

## Tasks / Subtasks

- [x] 档案头部里程碑条接入未庆祝计数，渲染角标（复用通知红点组件）（AC1）
- [x] 列表页进入时判定并补弹，取级别最高（AC2/AC3）
- [x] 回报：提交本次展示覆盖的 code 列表（AC2）
- [x] 「刚庆祝过」集合在导航内传递，列表页补弹前扣除（AC4）
- [x] 重温路径不回报（AC5）
- [x] 埋点 `path` 三值接入（AC6）
- [ ] 🔴 **真机走查四条庆祝路径互不打架 —— L2，留本地（本 story 主要验收手段）**

## Dev Notes

### 🔴 头号陷阱：「去发布」路径会连弹两次

既定时序是「发布成功 → 回填打卡 → **弹庆祝** → 关发布 sheet → **跳里程碑列表页**」。
回报是异步的（AD-A3.1 失败静默），列表页极可能在回报落库前读到 `celebrated_at IS NULL` → **两秒内连弹两次同一条**。
AD-A2 的 Prevents 原先只挡了「档案 Tab × 列表页」这一对，**恰好没挡真正会撞的这一对**。

### 要改的文件

| 文件 | 改什么 |
|---|---|
| `petgo_app/lib/features/profile/presentation/widgets/diary_header.dart` | `archiveMilestoneBar` 加角标 |
| `petgo_app/lib/features/notify/presentation/notification_bell.dart` | **只读复用** `notificationBadge`，不改 |
| `petgo_app/lib/features/profile/presentation/milestone_list_page.dart` | 进入时补弹判定 |
| `petgo_app/lib/features/profile/presentation/widgets/milestone_celebration.dart` | **视觉不改**，只加回报与 path 埋点 |
| `petgo_app/lib/features/content/presentation/publish_compose_page.dart` | 「刚庆祝过」集合的传出点 |

### 验证层级

- `[L0]` 编译、path 值域、抑制逻辑不依赖回报
- `[L1]` 回报置位范围、重温不回报
- `[L2]` **四条庆祝路径的真机走查是主要验收手段**，尤其「去发布」连弹 —— 必须本地

### References

- 架构 delta `AD-A2`（含 3b 置位范围、3c 连弹抑制）· `AD-A3` · `AD-A26.5`（双计接受）

---

## Dev Agent Record

### Context Reference

- 架构 delta `AD-A1.3`（唯一判据）、`AD-A2`（含 2.1 档案 Tab 不弹 / 2.3b 置位范围 / 2.3c 连弹抑制 /
  2.4 S 级也补 / 2.5 只弹最高一条）、`AD-A3`（best-effort 且幂等、重温不回报）、`AD-A26.5`（双计接受）
- 依赖 Story 1.4（`celebrated_at` 列、回报端点、未庆祝计数）

### Agent Model Used

claude-opus-5[1m]（云端 headless session）

### Debug Log References

- backend L0：**1679 tests, 0 failures**
- `flutter analyze`：`No issues found!`
- `flutter test`：**1560 全过**（新增 `milestone_catchup_test.dart` 15 条 +
  `milestone_uncelebrated_badge_test.dart` 6 条 + `milestone_list_test.dart` 补弹 4 条）

**顺带发现并修正的既有测试夹具**：`milestone_list_test.dart` 的 `_sample()` 里已完成条目
**没有 `celebratedAt`** —— 按 1.5 的判据它们就是「未庆祝」，进页面会自动补弹（AC2 的正常行为），
把那 5 个用例的 `pumpAndSettle` 卡在庆祝动效上。夹具已补 `celebratedAt`，这也正是**线上的真实
形态**（1.4 的迁移把存量完成行全部回填了）。补弹本身另起 4 条用例专门验。

### Completion Notes List

**接口补口（1.4 有意留给本 story 的那一处）**

- `MilestoneItemResponse` 新增 `celebratedAt`（Jackson NON_NULL）。**「已完成且未庆祝」的线格式
  就是「有 `completed=true`、没有 `celebratedAt` 这个 key」** —— 契约测试专门钉了这一条，
  别为了「字段齐整」给它补 null 或哨兵值，那会让 NON_NULL 语义和判据同时失效。
- 打卡端点返回的那一条 `celebratedAt` 恒为 null（庆祝页还没弹），客户端据此把它计入本次要庆祝的集合。

**已交付（L0 全绿）**

1. **AC1 角标**：`ArchiveStats.milestoneUncelebrated` → `DiaryHeader.milestoneUncelebrated`
   → 里程碑进度卡右上角。**卡片本身布局与视觉一字未改**，角标由外层 `Stack` 叠上去。
   档案 Tab **只出角标、不弹全屏庆祝**（AD-A2.1），有测试反向钉住。
2. **AC2/AC3 补弹**：新增纯函数 `resolveCatchup`（`domain/milestone_catchup.dart`）——
   判据只有 `completed && celebratedAt == null`；**不按级别过滤，S 级也补**；
   多条只弹级别最高的一条（复用 `health_milestones.dart` 里既有的 `highestLevelMilestone`，
   **没有另写一份**）。回报的是**本次展示覆盖的全部 code**（含 KOLEKSI 圆点带过的），
   不是「所有未庆祝的」。
3. **🔴 AC4 连弹抑制**：`MilestoneListPage.justCelebrated` 经 go_router `extra` 传入，
   补弹判定前先扣除。「去发布」路径在 `publish_compose_page.dart` 弹完庆祝后
   `router.push(milestoneList, extra: {done.code})`。
   **抑制纯客户端、不依赖回报是否已落库**；也**没有**把回报改成同步。
   有一条断言专门钉这个设计：`resolveCatchup` 的入参里没有任何「落库信号」参数。
4. **AC5 重温不回报**：`_showCelebration`（点已完成徽章）走 `path: revisit`，
   **刻意没有** `_reportCelebrated` 调用。埋点仍上报 —— 埋点与回报是两件事。
5. **AC6 埋点**：新增 `milestone_celebration_shown`，携带 `code` / `level` / `path`，
   `path` 值域由 `MilestoneCelebrationPath` 枚举定死为 `instant` / `catchup` / `revisit` 三值。
   属性里没有 PII（测试反向断言宠物名没进埋点）。
6. **回报收口到一处**：新增 `reportMilestoneCelebrated(ref, codes)`
   （`data/milestone_celebration_reporter.dart`），三条庆祝路径（打卡即时 / 健康记录短轮询 /
   进列表页补弹）共用。理由：「不 await」「失败静默」「不得改同步」这三条纪律若在三处各写一遍，
   迟早有一处写成阻塞的。
7. **AC7 既有路径全保留**：即时庆祝、500/800/1200ms 三次短轮询、庆祝页三种关闭方式、
   解锁振动、已完成徽章重温**均未改动**；庆祝页视觉一字未动（重绘属批次 C）。
   本次只给它加了一个 `path` 入参和一行埋点。

**⚠️ 对 story 原文的一处偏离（视觉零变化，需复核确认）**

- Dev Notes 把 `notification_bell.dart` 标为「**只读复用** `notificationBadge`，不改」。
  但那颗角标当时是 `NotificationBell.build` 里的一段**内联 `Container`**，根本不是可复用单位
  —— 想「复用」只有两条路：把样式抄第二遍（违反 AC1「不另画」），或者把它抽出来。
  我选了抽出：新建 `shared/widgets/count_badge.dart`，`NotificationBell` 改用它、
  **视觉与 `ValueKey('notificationBadge')` 都保持原样**（既有通知中心测试未改仍绿）。
  这样「复用」才字面成立，且样式只剩一份。

### 🔴 L1/L2 待本地验收

**L1（需 Docker postgres + redis）**

| # | 验什么 | 怎么验 | 对应 AC |
|---|---|---|---|
| 1 | 回报置位范围 | 补弹覆盖 3 条 → 查库，**恰好这 3 条**的 `celebrated_at` 变非空，其余仍 NULL | AC2 |
| 2 | 🔴 重温不改状态 | 点已完成徽章重温 → 查库 `celebrated_at` **值不变**；抓包确认**没有**发出 `POST .../celebrations` | AC5 |
| 3 | 埋点属性齐 | 看板确认 `milestone_celebration_shown` 三属性都到位 | AC6 |
| 4 | 列表接口下发 | `GET .../milestones` 响应里，已完成未庆祝的条目**没有** `celebratedAt` 这个 key | 接口补口 |

**L2（需真机 / 模拟器；本 story 的主要验收手段）**

| # | 验什么 | 怎么验 | 对应 AC |
|---|---|---|---|
| 5 | 档案 Tab 角标 | 造未庆祝条目 → 打开成长档案 Tab → 里程碑进度条上出红点/+N，**且不弹全屏庆祝** | AC1 |
| 6 | 角标观感 | 红点位置不压住「X / N」文字、不被卡片圆角裁掉（`Positioned(right:-4, top:-4)` 是否合适**只能真机看**） | AC1 |
| 7 | 列表页补弹 | 进里程碑列表页 → 自动弹一次，弹的是**级别最高**那条，其余在 KOLEKSI 圆点带里 | AC2 |
| 8 | 角标消失 | 补弹并关闭后返回档案 Tab → **角标消失** | AC2 |
| 9 | S 级也补 | 只留一条 S 级未庆祝 → 进列表页照样弹 | AC3 |
| 10 | 🔴🔴 **「去发布」不连弹** | 走完整时序：发布成功 → 回填打卡 → 弹庆祝 → 关发布 sheet → 自动跳列表页 → **列表页不得再弹同一条**。⚠️ 这是本 story 头号陷阱，**必须真机连续走一遍**，不能只看单元测试 | AC4 |
| 11 | 🔴 弱网下的第 10 条 | 把网络限速/断网后重做第 10 条（让回报必定失败或极慢）→ **仍不得连弹**（抑制不依赖回报落库） | AC4 |
| 12 | 重温正常展示 | 点已完成徽章 → 庆祝页正常展示（含 L 级分享卡） | AC5 |
| 13 | 四条路径互不打架 | 依次走：打卡即时庆祝 / 存健康记录后短轮询庆祝 / 去发布庆祝 / 进列表页补弹 —— 两两之间不重复弹、不吞 | AC4/AC7 |
| 14 | 既有行为零回归 | 庆祝页三种关闭方式、解锁振动、KOLEKSI 圆点带、L 级分享卡**全部原样** | AC7 |
| 15 | EN / ID 两套语言 | 第 5、7、10 条各切 en 与 id 走一遍（**无中文包**） | 通用 |

### File List

**新增**
- `petgo_app/lib/shared/widgets/count_badge.dart`（角标样式唯一来源）
- `petgo_app/lib/features/profile/domain/milestone_catchup.dart`（补弹纯判定）
- `petgo_app/lib/features/profile/data/milestone_celebration_reporter.dart`（回报唯一发起点）
- `petgo_app/test/profile/milestone_catchup_test.dart`
- `petgo_app/test/profile/milestone_uncelebrated_badge_test.dart`

**修改（后端）**
- `petgo-backend/.../profile/dto/MilestoneItemResponse.java`（+`celebratedAt`）
- `petgo-backend/.../profile/service/MilestoneService.java`（下发 `celebratedAt`）
- `petgo-backend/.../profile/service/MilestoneCheckInService.java`
- `petgo-backend/src/test/.../MilestoneListResponseContractTest.java`、`CardPageControllerTest.java`

**修改（前端）**
- `lib/features/profile/domain/milestone.dart`（+`celebratedAt` / `isUncelebrated`）
- `lib/features/profile/domain/archive_stats.dart`（+`milestoneUncelebrated`）
- `lib/features/profile/data/milestone_repository.dart`（+`reportCelebrations`）
- `lib/core/network/api_paths.dart`（+`petProfileMilestoneCelebrations`）
- `lib/core/router/app_router.dart`（列表页路由接 `extra`）
- `lib/features/profile/presentation/milestone_list_page.dart`（补弹 + 回报 + path）
- `lib/features/profile/presentation/widgets/milestone_celebration.dart`（+`path` 入参 + 埋点）
- `lib/features/profile/presentation/widgets/diary_header.dart`（角标）
- `lib/features/profile/presentation/growth_archive_page.dart`（透传计数）
- `lib/features/profile/presentation/health_list_page.dart`（path + 回报）
- `lib/features/content/presentation/publish_compose_page.dart`（path + 回报 + `extra` 传出）
- `lib/features/notify/presentation/notification_bell.dart`（改用 `CountBadge`，视觉不变）
- 测试 fake：`milestone_checkin_test.dart` / `milestone_share_test.dart` / `milestone_celebration_test.dart`

---

## Change Log

| 日期 | 变更 | 作者 |
|---|---|---|
| 2026-09-10 | 创建 | bmad-create-epics-and-stories |
| 2026-09-10 | 实现完成，L0 全绿，转 review（L1/L2 待本地验收） | dev-story（云端） |
