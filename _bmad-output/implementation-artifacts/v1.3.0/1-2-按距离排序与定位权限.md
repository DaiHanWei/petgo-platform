# Story 1.2: 按距离排序与定位权限

Status: ready-for-dev

## Story

As a 在外面想就近找地方的用户,
I want 场所列表默认按离我的远近排,
so that 我一眼就能看到最近的那几个，而不用自己一个个看地址猜。

## Acceptance Criteria

**AC1 — 距离排序** `[L1]`
已授权定位时，请求携带当前经纬度，列表按直线距离升序，每项显示距离。

**AC2 — 服务端排序口径（AD-2）** `[L1]`
**经纬度矩形范围粗筛 + 直线距离计算**；纬度、经度**各有索引**且粗筛能吃到索引（贴执行计划确认）。

**AC3 — 禁止项** `[L0]`
代码与迁移里**没有** `CREATE EXTENSION`（禁 PostGIS / cube / earthdistance），**没有**任何三方 LBS 调用。

**AC4 — 两条独立分支** `[L0]`
「按距离」与「按最新」是**两条独立查询分支**；**不得**写成"距离为 null 排最后"混在一条 SQL 里。

**AC5 — 无权限态** `[L2]`
未授权定位 → 列表按最新排序，顶部提示条 +「开启定位」按钮（UX-DR2）。点按钮触发系统权限申请；用户拒绝后**不反复弹**，提示条保留。

**AC6 — 权限声明** `[L0]`
安卓清单与 iOS Info.plist 均已声明定位权限；iOS 用途文案为**印尼语**，说清「按距离排序附近的宠物友好场所」。

## Tasks / Subtasks

- [ ] **T1 后端：距离查询**（AC: 1, 2, 3, 4）
  - [ ] 列表接口接受可选 `lat` / `lng`；两条分支分开写，不合并
  - [ ] 加经纬度索引的迁移（时间戳版本号）
  - [ ] 贴 `EXPLAIN` 结果到 Completion Notes，确认走索引
- [ ] **T2 前端：定位能力**（AC: 1, 5, 6）
  - [ ] 选定位库并记录选型理由（见 Dev Notes「定位库是个决策点」）
  - [ ] 两端权限声明 + 印尼语用途文案
  - [ ] 权限态机：已授权 → 带坐标；未授权 → 提示条；拒绝后不复弹
- [ ] **T3 联调**（AC: 1, 5）
  - [ ] 模拟器改虚拟定位验证排序变化；关权限验证回落

## Dev Notes

### 🔴 定位库是个决策点，不是随手选

项目现在**没有任何定位库**。两条路：

1. **地图 SDK 自带的定位能力**（Story 1.4 会引入地图 SDK）—— 少一个依赖，但会让本 story 依赖 1.4，**违反顺序**（本 story 在前）
2. **独立定位库**（如 geolocator）—— 本 story 自足，代价是多一个依赖

**建议选 2**，并在 Completion Notes 写明选型与版本。⚠️ 选 1 会造成前向依赖，等于本 story 做不完。

### 权限请求照既有范式

`petgo_app/lib/shared/utils/media_permission.dart` 是本项目请求系统权限的既有写法（按平台与 SDK 版本分流、处理"永久拒绝"）。定位权限照这个形状写，**不要另起一套**。

### 🛡 两条分支为什么不能合并

写成一条 SQL 用「距离为 null 排最后」看似省事，实际是：无坐标时那条 SQL 仍会带着距离计算列跑全表，且排序语义混在一起，后面没人敢改。AD-2 明确要求分开。

### 埋点与日志红线

🔴 **经纬度禁止进日志、禁止进埋点属性**（NFR-4 / NFR-5）。埋点层有兜底黑名单会整条丢弃，但不要依赖兜底——源头就不要传。

### 要改的既有文件（UPDATE）

| 文件 | 改什么 | 必须不破坏 |
|---|---|---|
| `petgo_app/android/app/src/main/AndroidManifest.xml` | 加定位权限 | 既有权限声明与 intent-filter |
| `petgo_app/ios/Runner/Info.plist` | 加定位用途文案 | 既有 `CFBundleURLTypes`（深链） |
| Story 1.1 建的列表接口与列表页 | 加坐标参数与距离展示 | 1.1 的空态与入口 |

### 验证层级

L0：AC3/4/6 · L1：AC1/2 · L2：AC5。云端只跑 L0，L1/L2 标「待本地验收」。

### References

- [Source: _bmad-output/planning-artifacts/v1.3.0/epics-v1.3.0-batch-b1.md#Story 1.2]
- [Source: _bmad-output/planning-artifacts/v1.3.0/architecture-v1.3.0-batch-b1-delta.md#AD-2]
- [Source: _bmad-output/planning-artifacts/v1.3.0/epics-v1.3.0-batch-b1.md#RC-1]（商店合规不在本 story，在发版检查单）

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
