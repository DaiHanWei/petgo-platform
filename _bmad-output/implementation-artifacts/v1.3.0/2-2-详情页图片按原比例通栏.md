---
baseline_commit: ac3d57b733769b3bbc374e82db59a07082e26522
epic: 2
story: 2
batch: A
verification: [L0, L1, L2]
---

# Story 2.2: 详情页图片按原比例通栏

Status: ready-for-dev

## Story

As a 用户，
I want 从首页点进详情时图片不变形，
so that 我看到的还是我拍的那张图。

---

> **要消灭的问题**：详情页当前是 `AspectRatio(aspectRatio: 1)` + `BoxFit.cover`，强制裁成方图、两侧留边。从 Feed 点进详情，**同一张图会从原比例通栏跳变成 1:1 裁切**。
>
> **依赖 Story 2.1**（尺寸由它下发）。
>
> 🔴 **必须调用 Feed 的同一个出口函数**，不许照抄参数另写一套 —— 否则将来一边改了，跳变会重新出现。

---

## Acceptance Criteria

### AC1 — 元素顺序（L2）

**Given** 一条含图内容
**When** 打开详情页
**Then** 元素自上而下为：作者行 → 图片 → 文字 → 互动栏 → 评论区 `[L2]`
**And** 发布时间显示在作者名**下方**（不再在右侧） `[L2]`

### AC2 — 🔴 图片比例走同一套三段口径（L0 / L2）

**Given** 任一张图
**When** 计算图片区高度
**Then** 依次施加：取实际比例 → clamp 到 `0.75~1.34` **闭区间** → 高度护栏 `[L0]`
**And** 三步调用 Feed 使用的**同一个出口函数**（`feed_image_layout.dart`），不另写一套 `[L0]`
**And** 图片通栏全宽出血，不再 1:1 裁切、不再两侧留边 `[L2]`

### AC3 — 高度护栏按详情页重取口径（L2）

**Given** 高度护栏的可视区被减项
**When** 计算
**Then** 按**详情页实测**取值（详情页无底部导航栏、有固定底栏，与 Feed 不同） `[L2]`
**And** **不**照抄 Feed 的数字 `[L0]`
**And** 小屏机（360×640 量级）上极端长图不超出上限 `[L2]`

### AC4 — 多图（L2）

**Given** 一条多图内容
**When** 左右滑动
**Then** 容器高度按**首图**锁定，滑动过程中不变 `[L2]`
**And** 页码角标（右上）与装饰标签（左下）位置维持现状，两角互不遮挡 `[L2]`

### AC5 — 无图分支保留（回归保护，L2）

**Given** 一条无图内容（`detail.imageUrls.isEmpty`）
**When** 打开详情页
**Then** 不渲染图片轮播 `[L2]`
**And** 装饰标签显示为正文下方单独一行胶囊 `[L2]`

### AC6 — 时间显示（L2）

**Given** 一条发布超过 7 天的内容
**Then** 显示绝对日期，复用现成的 `formatDayMonthYear`（输出如「15 Jun 2025」），不新写格式化 `[L0]`
**Given** 未超 7 天
**Then** 显示相对时间 `[L2]`

### AC7 — 存量占位兜底（必做，L2）

**Given** 存量内容（无尺寸）
**When** 打开详情页
**Then** 有占位策略，加载完成前后的高度变化被压到最小 `[L2]`
**And** 占位比例与 Feed 同一套 `[L0]`

### AC8 — 不扩散（L0）

**Then** 不改动底栏（属 Story 2.3） `[L0]`
**And** 不改动评论区（属 2.5 / 2.6） `[L0]`
**And** 不改动灯箱（属 Epic 3） `[L0]`

---

## Tasks / Subtasks

- [ ] 详情页重排元素顺序，时间移到作者名下方（AC1）
- [ ] 图片区改调用 `feed_image_layout.dart` 的出口函数（AC2）
- [ ] 实测详情页可视区被减项，重取护栏口径（AC3）
- [ ] 多图容器按首图锁定高度（AC4）
- [ ] 保住无图分支（AC5）
- [ ] 时间显示接 `formatDayMonthYear`（AC6）
- [ ] 存量占位（AC7）
- [ ] 小屏真机走查（AC3/AC7）

## Dev Notes

### 🔴 头号陷阱：护栏口径不能照抄 Feed

Feed 的被减项是「条目其余部分 142 + 露出余量 40」，那是**列表**的口径。详情页没有底部导航栏、有一条固定底栏、没有「下一条露出」的概念 —— **必须按详情页实测重取**，照抄会算错。

### 要改的文件

| 文件 | 改什么 |
|---|---|
| `petgo_app/lib/features/content/presentation/content_detail_page.dart` | 元素顺序、`_ImageCarousel` 比例（当前 `AspectRatio(1)` 约 451 行、`BoxFit.cover` 约 461 行）、时间位置 |
| `petgo_app/lib/features/content/domain/feed_image_layout.dart` | **只读复用**，不改 |
| `petgo_app/lib/shared/utils/date_format.dart` | **只读复用** `formatDayMonthYear` |

### 验证层级

- `[L0]` 编译、护栏未照抄常量、复用同一函数
- `[L2]` **小屏真机是主要验收手段**：竖图/横图/超长图/多图/无图五类走查

### References

- 架构 delta `AD-A11` `AD-A12` · v1.1.6 `AD-6`（三段口径原文）

---

## Dev Agent Record

### Context Reference

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

---

## Change Log

| 日期 | 变更 | 作者 |
|---|---|---|
| 2026-09-10 | 创建 | bmad-create-epics-and-stories |
