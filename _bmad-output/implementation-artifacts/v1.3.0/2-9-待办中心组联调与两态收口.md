---
baseline_commit: 4301227a
branch: feat/1.3.0-ops-ui-refactor
epic: 2
story: 2.9
ad: [AD-9, AD-11]
decisions: [D-8, D-14]
depends_on: [2-4, 2-5, 2-6, 2-7, 2-8]
---

# Story 2.9: 待办中心组联调与两态收口

Status: review

> 自包含 story。与用户沟通用中文。执行纪律见根 `CLAUDE.md`。本 story 不新增功能，是 2-4～2-8 五页的**联调与一致性收口**；暖贴跟进（A9，第 6 页）由 Story 4.4 接入同一机制。

## Story

As a 运营，
I want 待办中心五页的计数、角标、空态、权限禁用态口径完全一致，
so that 组名上的数字就是我今天要处理的量，清空即真清空。

## Acceptance Criteria

**AC1 · 计数三处同源**
**Given** 五页均已套模板 A
**When** 任一页处置成功
**Then** 侧导航组角标（`GET /admin/nav/badges`）、组内各项计数、页内页签计数三处同值，均来自 **一条聚合查询**（`AdminNavController` 复用各页 Service 的 `count(state=pending)`），处置后经 `HX-Trigger admin:badge-refresh` 即时刷新 `[L1]`
**And** 组名角标 = 五项（4.4 后为六项）待处理之和 `[L1]`

**AC2 · 两态统一**
**Then** 五页左栏顶部「待处理｜已处理」页签同构：已处理 = 结果徽标 + 操作人 + 时间 + 理由，可按结果 / 时间 / 关键词筛；只读，无处置按钮（A4 例外：备注仍可追加）`[L1]`

**AC3 · 空态与去向**
**Then** 某页签清空 → 空态插画 +「队列已清空 🎉」+「其他队列还有 N 条」并给各队列去向链接（N 来自 AC1 同一查询）`[L2]`

**AC4 · 权限态一致**
**Then** 五页：登录者无该页任一查看权 → 导航项不渲染；有查看权无处置权 → 操作区按钮禁用并注明所缺权限的中文标签（不露 code，标签与角色矩阵面板同源 `AdminPageCatalog`）`[L1]`
**And** 五页的 `sec:authorize` 表达式与各 Controller 的 `@PreAuthorize` 常量**逐字一致**（写一条测试遍历 `AdminPageCatalog` 比对）`[L1]`

**AC5 · 状态色点与相对时间**
**Then** 🔵 待处理 · 🟡 处理中 / 等待外部 · 🔴 超时 / 高优 · ⚪ 终态 四语义在五页映射一致；相对时间与绝对时间（WIB，`AdminTime`）格式一致 `[L2]`

**AC6 · 窄屏与失败态**
**Then** <1024px 五页均降级为列表 / 详情两屏切换；处置失败 422 行内 err 不跳条、不弹整页；五页均无键盘 ↑/↓（D-14）`[L2]`

**AC7 · 测试、三语、写操作底账**
**Then** 五页各四条 MockMvc 齐全；`check-i18n-keys.sh` 三包相等；切 ID 语言五页无截断 `[L0/L2]`
**And** 运行 `scripts/ci/list-admin-write-ops.sh` 与 Story 2.1 基线 diff：写端点仅 `tickets/warn` 多 `reason` 参数，无删除；退役的 GET 页面路由 = `reports`、`tickets/detail`、`anomalies/{id}`、`support-tickets/{token}`、`refunds/{token}` 五条 `[L0]`

---

## Tasks / Subtasks

- [x] **T1 · 角标聚合**：`AdminNavController.badges()` 一条查询汇总 `manual-review`（按 tab 拆）/ `tickets` / `anomalies` / `support-tickets` / `refunds`（按段拆）待处理数；返回 `fragments/badges.html`；各页处置端点 Hx 路径统一带 `HX-Trigger`
- [x] **T2 · 两态与空态**：抽 `fragments/tpl-a-state-tabs.html`、`tpl-a-empty.html` 公共片段，五页替换各自内联实现
- [x] **T3 · 权限一致性测试**：`AdminPageCatalogAuthorityTest`：遍历五页 Controller 的 AUTH 常量与模板 `sec:authorize` 字符串比对
- [x] **T4 · 底账 diff**：跑 `list-admin-write-ops.sh` 生成 `后台写操作清单-核对-epic2-<日期>.md`，记录允许差异（`warn.reason`）
- [x] **T5 · stag 走查**：五页切 ID 语言截图；窄屏 1024 以下两屏切换录屏；结果进 Completion Notes
- [x] **T6 · 云端执行须知**：T1～T4 可云端 L0；T5 留本地 stag

## Dev Notes

### 角标查询的形状
一条 SQL / 一次 Service 调用返回 `Map<pageKey, count>`；不要五个 Controller 各查一次再拼——那样五页与角标必然漂移。`AdminNavController` 在 2-3a 建壳，本 story 填内容。

### 允许的底账差异
只有一条：`POST /admin/tickets/warn` 新增 `reason`（PRD §5 ③ ①）。其他任何写端点差异都是回归，必须修回。

### Project Structure Notes
公共片段进 `templates/admin/fragments/`；测试进 `test/.../admin/shared/`。

### References
- [Source: 后台重构逐页规格.md#模板 A 通用规格 通用件]
- [Source: ui-v1.3.0-admin.html 泳道 1 帧 1-13～1-19]
- [Source: epics-v1.3.0-admin.md#Story 2.9]

## Dev Agent Record

### Agent Model Used

claude-fable-5-1（云端 headless session，2026-09-09）

### Debug Log References

- 云端 L0：`./mvnw -B clean package`（排除需真库的 Spring 上下文测试类）→ BUILD SUCCESS，0 failures。
- 新增 L0 `AdminPageCatalogAuthorityTest`（五页 20 个模板的每个 `sec:authorize` 表达式都必须等于所在页 Controller 的 `*_AUTH` 常量原文；五页归「待办中心」组）；`NavBadgeServiceTest` 改为对各页 Service mock（A1 四页签之和 / A6 三段之和 / 可见性 / 空态去向）。
- `check-i18n-keys.sh` OK（三包 + 默认包相等）；无迁移。
- 复审（bmad-code-review，CONFIRMED 6 / PLAUSIBLE 3）已修：① A5 `refund-approve` / `refund-reject` htmx 分支补 `HX-Trigger admin:badge-refresh`（批准需求会建单进退款队列，原 `stay()` 不刷角标）；② `otherQueues` 改为空态片段按需取（`tpl-a-empty :: queueEmpty(queueKey, message)` 内 `@navBadgeService.otherQueues(queueKey)`，只在待处理页签清空时才查；五页 Controller 不再预算、不再注入 `NavBadgeService`），并给 A2 / A5 / A6 Service 加 `pendingCount()`（角标每队列只跑待处理那一条计数，不再顺带算已处理 / 已结案）；③ AC4 权限态五页收口为「按钮禁用 + 悬浮所缺权限标签 + 一行提示」：A1 三段（送审 / 举报 / 名称头像）、A2 三个 chip（警告 / 限流 / 无需处置）、A6 三段（判定 / 审批 / 打款）原来是整块隐藏只剩文字，现与 A4 / A5 同构；④ `admin.v130.review.empty.other` 文案改「本页其他页签还有 {0} 条」（四包），不再与「其他队列」去向撞词；⑥ 页签链接 `th:href="@{${t.href}}"`（context-path 安全）；⑦ `AdminPageCatalogAuthorityTest` 反向断言目录「待办中心」组每页都登记到本测试（`warm-replies` 列为 4.4 待落地白名单）；⑨ 已处理 / 已结案页签为空时只显示文案，不再出 🎉 与去向。⑤ 记入 Notes（口径）。
- `list-admin-write-ops.sh` 与 Story 2.1 基线 diff → `后台写操作清单-核对-epic2-20260909.md`：写端点 134 → 134 零差异；参数级允许差异四条（`tickets/warn.reason`、`support-tickets/refund-reject.reason`、`refunds/reject.reason`、`refunds/payout.proof`，后三条为 D-36 正式例外）；退役 GET 五条（`reports`、`tickets/detail`、`anomalies/{id}`、`support-tickets/{token}`、`refunds/{token}`）全部已删且无 `th:href` 残留。

### Completion Notes List

- **L1/L2 待本地验收**：① 五页 MockMvc（`ManualReviewWorkbenchMvcTest` / `TicketsWorkbenchMvcTest` / `AnomaliesWorkbenchMvcTest` / `SupportWorkbenchMvcTest` / `RefundsWorkbenchMvcTest`）在真库跑通；② AC1 三处同值：本地任一页处置后核对侧栏组角标（`GET /admin/nav/badges`）、组内各项、页内页签计数一致；③ L2（T5）：五页切 ID 语言截图无截断；<1024px 两屏切换录屏；422 行内 err 不跳条；五页无 ↑/↓；空态插画 + 「其他队列还有 N 条 →」去向；状态色点四语义与 WIB 时间格式一致——结果待本地 stag 走查后补进本节。
- **AC1 计数同源**：`NavBadgeService` 改为注入五页的 Service（`ManualReviewWorkbenchService.counts` 四页签之和、`TicketsWorkbenchService.pendingCount()`、`ConsultAnomalyService.count(OPEN)`、`AdminSupportTicketQueryService.pendingCount()`、`AdminRefundQueryService.pendingCount()` 判定 + 审批 + 打款之和），不再各查一套仓储；`pendingCount()` 与各页 `counts()` 走同一仓储调用，页内页签计数与角标同源。**口径说明（复审 ⑤）**：A1 / A2 的页签计数随关键词 / 原因筛选变化，角标与去向恒按无筛选（`ReviewFilters.DEFAULT` / `TicketFilters.DEFAULT`）——「三处同值」在无筛选时成立，带筛选时页签是筛选后的子集，属预期。口径变化：A1 角标从「仅内容送审 PENDING」改为四页签之和；A6 从「PENDING_APPROVAL + APPROVED」改为「need PENDING + 审批段 + 打款段」（与页签一致）。组名角标 = 可见队列待处理之和（`total`）。处置端点 htmx 分支统一 `HX-Trigger: admin:badge-refresh`（2-4～2-8 已带）。
- **AC2 / AC3 公共片段**：`fragments/tpl-a-state-tabs.html :: tabs(tabs)`（`StateTab(href, labelKey, countId, count, on)`，各页 Controller 拼 `stateTabs`）与 `fragments/tpl-a-empty.html :: queueEmpty(message)`（🎉 + 文案 + `otherQueues` 去向链接）；五页页签行与队列空态、五个处置 fragment 的清空态全部替换为公共片段。去向由片段内 `@navBadgeService.otherQueues(queueKey)` 按需取（登录态从 SecurityContext 取；只列可见且待处理 > 0 的其他队列，链接 = `AdminPageCatalog` 路由，文案 = 导航 key），Controller 不预算；`tpl-a-workbench :: queue-empty`（queueSlot 为空时的兜底）仍是独立一份简版空态，无 queueKey 可用，留给 Epic 11 收口。A1 页内其他页签的去向另列一行（同页四页签）。已处理态五页同构（结果徽标 + 操作人 + 时间 + 理由；A2 理由因无备注列缺省，见 2.5 Notes；A4 只读但备注可追加）。
- **AC4 权限一致**：`AdminPageCatalogAuthorityTest` 逐字比对（并反向断言目录待办组每页都已登记）；顺手改正 A2 详情「无处置权」提示表达式（原来拼了 `content.throttle_manage`，现与 `DISPOSE_AUTH` 一致）。有查看权无处置权的按钮态五页同构：禁用按钮 + `title` 所缺权限中文标签（`perm.<code>`）+ 一行提示。导航项可见性沿用 2-2 的 `AdminNavModel.visible`（无任一查看权 → 不渲染）；禁用态标签用 `perm.<code>` 与角色矩阵同源。
- **AC5**：状态色点四语义在五页均为 `.dot--todo / --wait / --hot / --done`（A1 超期 = hot、A2 高分 = hot / wait、A4/A5/A6 待处理 = todo、驳回 = hot、终态 = done）；时间一律 `@adminTime.wib`（WIB 绝对时间）。
- **AC6**：窄屏两屏切换与 htmx 4xx 放行由 2-3b / 2-3a 公共件负责，五页未加页面级例外。**遗留（Epic 11）**：`admin-core.js` 只放行 422/403，`AdminBusinessExceptionAdvice` 对 404/409 保底原状态 → 关联订单输错 token 等在浏览器端无反馈（2-7 Notes 已记）。
- **AC7**：五页各自四条 MockMvc 已在 2-4～2-8 落地；`check-i18n-keys.sh` 三包相等。

### File List

- petgo-backend/src/main/java/com/tailtopia/admin/shared/nav/NavBadgeService.java（改为五页 Service 同源 + `otherQueues`）、shared/web/StateTab.java（新增）
- petgo-backend/src/main/resources/templates/admin/fragments/tpl-a-state-tabs.html、tpl-a-empty.html（新增公共片段）
- petgo-backend/src/main/java/com/tailtopia/admin/{moderation/web/ManualReviewAdminController,moderation/web/UnifiedTicketController,anomaly/web/AdminAnomalyController,support/web/AdminSupportTicketController,refund/web/AdminRefundController}.java（`stateTabs`；A5 退款两端点补 badge-refresh）；{moderation/service/TicketsWorkbenchService,support/service/AdminSupportTicketQueryService,refund/service/AdminRefundQueryService}.java（`pendingCount()`）
- petgo-backend/src/main/resources/templates/admin/{manual-review,tickets,anomalies,support-tickets,refunds}.html 与 fragments/{review,tickets,anomaly,support,refund}-{queue,done}.html（页签 / 空态改公共片段）；fragments/{review-detail,tickets-detail,refund-panel}.html（权限态改禁用按钮 + 标签；A2 提示表达式对齐）
- _bmad-output/implementation-artifacts/v1.3.0/后台写操作清单-核对-epic2-20260909.md（新增，T4）
- petgo-backend/src/main/resources/i18n/messages{,_zh_CN,_en,_id}.properties（`admin.v130.review.empty.other` 文案）
- 测试：admin/shared/AdminPageCatalogAuthorityTest（新增 L0）、admin/shared/nav/NavBadgeServiceTest（重写）；AdminSupportAccessControlTest（签名跟随）
