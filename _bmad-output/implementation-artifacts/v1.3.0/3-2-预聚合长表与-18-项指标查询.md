---
baseline_commit: 4301227a
branch: feat/1.3.0-ops-ui-refactor
epic: 3
story: 3.2
ad: [AD-2, AD-8]
decisions: [D-6, D-15, D-16, D-26, D-28, D-29]
depends_on: [3-1]
---

# Story 3.2: 预聚合长表与 18 项指标查询（AD-2）

Status: review

> 自包含 story，可本地或云端 L0 执行。与用户沟通用中文。执行纪律见根 `CLAUDE.md`（Flyway 时间戳版本号、`ddl-auto=validate`、`mvn -B clean package`）。
> 纯后端：1 支迁移 + 实体/仓储 + 18 个指标查询类 + parity 测试。**不含跑批（3-3）与页面（3-4）**。

## Story

As a 运营，
I want 18 项指标每天各算一次存下来，帖子类 9 项同时存「真实用户」和「含种子」两个口径，
so that 看板取数快、历史数字不变、任一天都能和口径表对上。

## Acceptance Criteria

**AC1 · 长表**
**Given** 迁移 `V<yyyyMMdd_HHmm>__init_ops_daily_metrics.sql`（取创建时刻）
**Then** 建 `ops_daily_metrics(id BIGINT IDENTITY PK, report_date DATE NOT NULL, metric_key VARCHAR(48) NOT NULL, scope VARCHAR(8) NOT NULL CHECK IN ('ALL','REAL'), value NUMERIC(18,4) NOT NULL, computed_at TIMESTAMPTZ NOT NULL DEFAULT now(), UNIQUE(report_date, metric_key, scope))` + 索引 `idx_ops_daily_metrics_date (report_date)` `[L1]`
**And** 实体 `OpsDailyMetric`、`OpsDailyMetricRepository`（`existsByReportDate`、`findByReportDateBetweenOrderByReportDate`、`findDistinctReportDates`）通过 validate `[L1]`

**AC2 · 18 个 key 枚举**
**Then** `DashboardMetric` 枚举恰好 18 值，顺序与 PRD 口径表 # 一致，每值携带 `dualScope` 标记（#3 #4 #8～#14 为 true）与口径提示 message key `admin.v130.dashboard.metric.<key>.hint` `[L0]`

**AC3 · 每指标一个查询类**
**Given** `admin/dashboard/metrics/` 下接口 `MetricQuery { DashboardMetric key(); BigDecimal compute(LocalDate reportDate, MetricScope scope); }`
**Then** 18 个实现类各一，全部原生 SQL，切日一律 `(<ts> AT TIME ZONE 'Asia/Jakarta')::date`，日期参数为 WIB 自然日；对 `dualScope=false` 的指标传 `REAL` 与 `ALL` 结果相同或抛 `IllegalArgumentException`（统一：非双口径指标只接受 ALL）`[L0]`
**And** 「真实用户」口径统一引用 `SyntheticAccountSql.EXCLUDE_WHERE`（3-1），帖子类 REAL 口径 = **帖子作者为真实用户 且 点赞/评论发起者为真实用户** `[L0]`
**And** 「累计注册用户数」key 为 `cumulative_users`（D-16 改名），「新增建档用户数」按人去重（D-28）`[L0]`

**AC4 · 口径与参考 SQL 相等（Parity）**
**Given** 以 `内容运营所需数据-20260831.sql` 为对照
**When** 在 L1 测试库灌入固定样本（含跨 WIB 日界、Apple 用户、虚拟作者、UNDER_REVIEW 帖、已删评论、退款单）后，对任意 3 个日期跑 18 项 `ALL` 口径
**Then** 除 #5（D-28 新口径）外 17 项与参考 SQL 直跑结果逐项相等；#5 按「当日首次建档去重用户数」另行断言 `[L1]`
**And** 9 项 REAL 口径在「样本中无合成账号」时与 ALL 相等、在「有合成账号」时严格小于等于 ALL `[L1]`

**AC5 · 不物化、不写库**
**Then** 本 story 的查询类只读业务表，不写 `ops_daily_metrics`（写入归 3-3）`[L0]`

**AC6 · 回归**
**Then** `mvn -B clean package` 通过；`bash scripts/ci/check-flyway-versions.sh origin/main` 绿 `[L0]`

---

## Tasks / Subtasks

- [x] **T1 · 迁移**（AC1）
  ```sql
  -- V<yyyyMMdd_HHmm>__init_ops_daily_metrics.sql
  -- V1.3.0 AB-15A 数据看板 · Story 3.2（AD-2）。日粒度预聚合长表：一行 = (WIB 自然日, 指标, 口径)。
  -- 长表不选 27 列宽表：加指标不改 schema、双口径天然表达、图表按 key 过滤取数。
  -- 行一旦物化不再变（口径截止该日 24:00，同一天数字不随后续互动上涨）。
  CREATE TABLE ops_daily_metrics (
      id           BIGINT        GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
      report_date  DATE          NOT NULL,                 -- WIB 自然日
      metric_key   VARCHAR(48)   NOT NULL,                 -- DashboardMetric 枚举 18 值
      scope        VARCHAR(8)    NOT NULL,                 -- ALL | REAL
      value        NUMERIC(18,4) NOT NULL,
      computed_at  TIMESTAMPTZ   NOT NULL DEFAULT now(),
      CONSTRAINT ck_ops_daily_metrics_scope CHECK (scope IN ('ALL', 'REAL')),
      CONSTRAINT uq_ops_daily_metrics UNIQUE (report_date, metric_key, scope)
  );
  CREATE INDEX idx_ops_daily_metrics_date ON ops_daily_metrics (report_date);
  COMMENT ON TABLE ops_daily_metrics IS 'AB-15A 看板预聚合：一行一个(日,指标,口径)，历史行不可变；scope=REAL 仅帖子类 9 项。';
  ```
  - [x] 🔴 `NUMERIC(18,4)` ↔ Java `BigDecimal`；`DATE` ↔ `LocalDate`；别用 SMALLINT/INT 装 value（平均分有小数）
- [x] **T2 · 枚举与实体**（AC1/AC2）
  - [x] `admin/dashboard/domain/{OpsDailyMetric, DashboardMetric, MetricScope}.java`；`repository/OpsDailyMetricRepository.java`
  - [x] 枚举 18 值按下表顺序：`new_users cumulative_users posting_users new_posts new_pet_owners cumulative_pet_owners diary_pet_owners interacted_posts silent_posts engagement_score new_posts_score all_posts_avg_score interacted_posts_avg_score engagement_score_all_time paying_users_cash payments_cash paying_users_incl_pawcoin payments_incl_pawcoin`
- [x] **T3 · 18 个 `*MetricQuery`**（AC3）——每类一个文件，`@Component`，注入 `NamedParameterJdbcTemplate`；SQL 草案见 Dev Notes §口径表与 SQL
- [x] **T4 · Parity 测试**（AC4）
  - [x] `test/.../admin/dashboard/MetricQueryParityTest.java extends ApiIntegrationTest`：样本构造用 `jdbcTemplate` 直插（控制 created_at 到 WIB 23:30 / 00:30 边界）；参考 SQL 从 `_bmad-output/planning-artifacts/v1.3.0/内容运营所需数据-20260831.sql` 读入、把 `report_days` CTE 替换为固定日期列表后执行；逐项比对
  - [x] 🔴 参考 SQL 的 `::date` 按连接时区切日，测试里对照跑时必须 `SET TIME ZONE 'Asia/Jakarta'` 后再跑参考 SQL，否则边界样本必然不等
- [x] **T5 · 云端执行须知**
  - [x] 云端 L0：编译 + 枚举/接口单测；Parity 为 L1 留本地

---

## Dev Notes

### 口径表与 SQL 草案（权威：PRD §1 ①-a，原样带入；SQL 由参考 SQL 改写）

通用：`D` = 目标 WIB 自然日；`day(ts) := (ts AT TIME ZONE 'Asia/Jakarta')::date`；`REAL_U` := `SyntheticAccountSql.EXCLUDE_WHERE`（别名 `u`）；可见帖 `p.status='PUBLISHED' AND p.deleted_at IS NULL`；有效评论 `c.deleted_at IS NULL`；得分 = 赞×1 + 评×5。

| # | key | 口径（PRD） | SQL 草案（ALL；REAL 见备注） |
|---|---|---|---|
| 1 | `new_users` | 当日注册的真实用户数 | `SELECT count(*) FROM users u WHERE day(u.created_at)=:d AND REAL_U` |
| 2 | `cumulative_users` | 截至当日 24:00 累计真实用户数（原「总安装用户数」，D-16） | `… WHERE day(u.created_at)<=:d AND REAL_U` |
| 3 | `posting_users` | 当日发布可见帖子的去重作者数（双口径） | ALL：`SELECT count(DISTINCT p.author_id) FROM content_posts p WHERE day(p.created_at)=:d AND 可见`；REAL：`JOIN users u ON u.id=p.author_id AND REAL_U` |
| 4 | `new_posts` | 当日发布的可见帖子数（双口径） | 同上 `count(*)` |
| 5 | `new_pet_owners` | 当日**首次**建档的去重用户数（D-28） | `SELECT count(*) FROM (SELECT owner_id, min(created_at) f FROM pet_profiles GROUP BY owner_id) x WHERE day(x.f)=:d`（参考 SQL 的按档案计作废） |
| 6 | `cumulative_pet_owners` | 截至当日至少有一个档案的去重用户数 | `SELECT count(DISTINCT owner_id) FROM pet_profiles WHERE day(created_at)<=:d` |
| 7 | `diary_pet_owners` | 当日发布 `GROWTH_MOMENT` 可见帖且本人有档案的去重用户数 | `count(DISTINCT p.author_id) … p.type='GROWTH_MOMENT' AND EXISTS(pet_profiles pp WHERE pp.owner_id=p.author_id)` |
| 8 | `interacted_posts` | 当日收到 ≥1 赞或有效评论的去重帖子数（双口径） | `count(DISTINCT post_id)` over `interactions_raw`（likes ∪ comments deleted_at IS NULL）`WHERE day(created_at)=:d`；REAL：帖子作者 REAL 且互动者 REAL |
| 9 | `silent_posts` | 当日发布的可见帖中当日 0 赞 0 评的帖子数（双口径） | `count(*) FROM content_posts p WHERE day(created_at)=:d AND 可见 AND NOT EXISTS(interactions ir WHERE ir.post_id=p.id AND day(ir.created_at)=:d)`；REAL 同上加作者/互动者过滤 |
| 10 | `engagement_score` | 当日新产生 赞×1 + 有效评论×5（双口径） | `SUM(CASE source WHEN 'like' THEN 1 ELSE 5 END)` over interactions `WHERE day=:d` |
| 11 | `new_posts_score` | 当日发布可见帖各自截至当日 24:00 累计得分之和（双口径） | 参考 SQL `post_score_by_day` LATERAL，把 `::date` 换 `day()` |
| 12 | `all_posts_avg_score` | 截至当日已发布全部可见帖各自累计得分平均（双口径） | 参考 SQL `all_posts_avg_score_by_day`；`AVG … FILTER (WHERE p.id IS NOT NULL)`；空集返回 NULL → 存 0 并在 3-3 记「无样本」 |
| 13 | `interacted_posts_avg_score` | 当日有互动的帖子按各自当日新增得分取平均（双口径） | 参考 SQL `daily_interaction_avg_by_day` |
| 14 | `engagement_score_all_time` | 截至当日 24:00 全部历史 赞×1 + 有效评论×5（双口径） | `(count likes ≤d)*1 + (count comments deleted_at IS NULL ≤d)*5` |
| 15 | `paying_users_cash` | 当日 `payment_intents.status='PAID'`（到账时刻 = `updated_at`）的去重真实用户数 | `count(DISTINCT pi.user_id) FROM payment_intents pi JOIN users u … WHERE pi.status='PAID' AND day(pi.updated_at)=:d AND REAL_U` |
| 16 | `payments_cash` | 同 15 笔数 | `count(*)` |
| 17 | `paying_users_incl_pawcoin` | 当日 consult_orders.paid_at ∪ ai_consult_orders(COMPLETED).paid_at ∪ id_card_hd_purchases.purchased_at ∪ PawCoin 充值到账 的去重真实用户数 | 参考 SQL `spend_events` UNION ALL 四段，每段 JOIN users 加 REAL_U，`count(DISTINCT user_id)` |
| 18 | `payments_incl_pawcoin` | 同 17 笔数；已退款订单当日仍计 | `count(*)`；不加 `NOT IN ('REFUNDING','REFUNDED')` |

REAL 口径定义（D-29）：**帖子作者 ∈ 真实用户 且 互动发起者 ∈ 真实用户**；对 #11/#12 的 LATERAL 累计得分，REAL 口径下点赞/评论子查询各自再 `JOIN users u2 ON u2.id = cl.user_id / cm.author_id AND REAL_U(u2)`。

### 🔴 切日必须显式 WIB（OQ-B3）

参考 SQL 全部 `x::date`，含义随连接时区漂移。全部查询类统一写 `(ts AT TIME ZONE 'Asia/Jakarta')::date`，常量抽到 `MetricSql.DAY(expr)` 小工具；`LocalDate` 参数就是 WIB 日。`ScheduleWindow.WIB` 已有 `ZoneId.of("Asia/Jakarta")`，Java 侧复用。

### 🔴 两处参考 SQL 与现网 schema 的差异（拆 story 时核实）

1. **`comments.moderation_status` 列已存在**（参考 SQL 写作时 stag 无此列，故只过滤 `deleted_at`）。本 story 为保 parity **仍只按 `deleted_at IS NULL`**；「审核中 / 被拒评论是否应计入互动」列为待拍板（见文末问题 1），拍板后改一处常量 `VALID_COMMENT_WHERE`。
2. **`users.account_type` 已存在**，真实用户判定改走它（3-1），前缀判定退为兜底；参考 SQL 的前缀判定与之在现网等价（3-1 parity 保证）。

### 🔴 #5 有意偏离参考 SQL

参考 SQL `profiles_by_day` 数的是 `pet_profiles` 行数（一人多档多计）；D-28 拍板按人去重。Parity 测试对 #5 单独断言，不与参考 SQL 比。`pet_profiles.owner_id` 注释「一个用户至多一条档案」——若真如此两者相等，但仍按 D-28 写法实现，防将来放开多档。

### 现状代码要点

| 文件 | 现状 | 本 story |
|---|---|---|
| `admin/dashboard/service/AdminDashboardService.java` | `overview()` 复用各 repo count（真实用户 `countByAccountTypeAndDeletedAtIsNull(REAL)`、`posts.countByRealAuthor()` 等） | 不动；3-4 复用为底部摘要行 |
| `admin/dashboard/dto/OverviewMetrics.java` | 9 字段 record | 不动 |
| `shared/schedule/ScheduleWindow.java` | `WIB = ZoneId.of("Asia/Jakarta")` | 复用 |
| `content/repository/CommentRepository.java:55-57` | `countByRealAuthor` 按 accountType | 口径参考 |
| 表 `content_posts` | `type` DAILY/GROWTH_MOMENT/…；`status` PUBLISHED/UNDER_REVIEW/…；`deleted_at` | 只读 |
| 表 `payment_intents` | `purpose`（VET_CONSULT/PAWCOIN_TOPUP/…）、`status`（PENDING/PAID/…）、无 `paid_at` 用 `updated_at` | 只读 |
| 表 `consult_orders` / `ai_consult_orders` / `id_card_hd_purchases` | `paid_at` / `status,paid_at` / `purchased_at` | 只读 |

### 迁移约定

时间戳文件名取创建时刻；一支只建这一张表；不改任何既有表。`out-of-order` 常开。

### 测试标准

- L0：`DashboardMetric` 恰 18 值且 dualScope 集合 = {3,4,8..14}；每个 `MetricQuery` 对非双口径传 REAL 抛异常。
- L1：Parity（AC4）；样本必须含 WIB 23:30 与次日 00:30 两条边界记录，证明切日正确。
- L1 跑法：flush Redis DB0 → 重建 scratch 库 → `DB_NAME` env。

### Project Structure Notes

- `admin/dashboard/{domain,repository,metrics}`；`metrics/` 内每指标一个类 + `MetricQuery` 接口 + `MetricSql` 工具 + `SyntheticAccountSql`（3-1）。
- 查询类只依赖 JDBC，不注入业务 Repository（模式规则：admin 新模块不直查业务 Repository，此处用原生 SQL 是 AD-2 明示的例外）。

### References

- [Source: PRD-v1.3.0-admin.md#1 ①-a 18 项指标口径表]
- [Source: architecture-v1.3.0-admin-delta.md#AD-2 / §数据模型增量 #2 / Naming Patterns 指标 key]
- [Source: 内容运营所需数据-20260831.sql]
- [Source: 决策日志-admin.md D-6 D-16 D-26 D-28 D-29]
- [Source: docs/reference/db-schema-reference.md#users #content_posts #comments #payment_intents]

## Dev Agent Record

### Agent Model Used

claude-fable-5-1（云端 headless session，2026-09-09）

### Debug Log References

- 云端 L0：`./mvnw -B clean package`（排除需真库的 Spring 上下文测试类）→ BUILD SUCCESS，0 failures；`DashboardMetricTest`、`MetricQuerySqlTest`（L0）通过。
- 新增迁移 `V20260909_1432__init_ops_daily_metrics.sql`（时间戳版本号，创建时刻 14:32 UTC；沙箱无 `origin/main`，`check-flyway-versions.sh` 冻结 / 撞号检查待本地补跑）。
- `check-i18n-keys.sh` OK（+18 条口径提示 `admin.v130.dashboard.metric.<key>.hint` 三包 + 默认包）；`AdminMessagesParityTest` 要求文案里不得出现标识串，提示文案已改成自然语言（不写列名 / 枚举值）。
- 复审（bmad-code-review，CONFIRMED 2 / PLAUSIBLE 3）已修：① **#17/#18 身份证高清段**：V92 起 `id_card_hd_purchases` 是「支付尝试 / 收据」表（QRIS 下单即插行、intent 仍 PENDING），原参考 SQL 按 `purchased_at` 会把放弃 / 超时的二维码算成付费且把成交记在下单日 → 改为「PawCoin 行按 `purchased_at` ∪ ID_HD 支付单 PAID 按到账日」（parity 测试对参考 SQL 打同样补丁，并加「QRIS 尝试行 + PENDING intent 不计」样本）；② parity 的「REAL ≤ ALL」对 #9 沉默帖子语义不成立（只收到虚拟赞的真实帖在 REAL 里反而沉默）→ 豁免 #9；③ `MetricSql.realUser` 改 `\bu\.` 词边界替换；④ `:d` 直接绑 `LocalDate`（pgjdbc 原生 DATE，不绕 JVM 默认时区）；⑤ #7 档案存在性加「截至当日 24:00」上界（补跑可复现；参考 SQL 同步补丁）。

### Completion Notes List

- **L1/L2 待本地验收**：① 迁移在 stag 库 `validate` 通过（`ops_daily_metrics` 六列 + CHECK + UNIQUE + 日期索引 ↔ `OpsDailyMetric`）；② `MetricQueryParityTest extends ApiIntegrationTest`（真库）：三天 × 17 项与参考 SQL（D-38 补丁版：评论过滤补 `moderation_status='VISIBLE'`、`report_days` 换固定日期、同连接 `SET TIME ZONE 'Asia/Jakarta'`）逐项相等，未补丁原版在有 UNDER_REVIEW 评论时总互动得分恰差 5；样本增量断言（跨 WIB 日界 23:30 / 00:30、Apple 用户、虚拟作者与虚拟互动者、UNDER_REVIEW 帖、UNDER_REVIEW / 已删评论、PENDING 支付单、已退款问诊单、AI 待支付单）逐条核对；9 项双口径 D1（含合成账号）REAL ≤ ALL、D2（无合成账号）REAL 增量 = ALL 增量；③ 参考 SQL 在 stag 真数据上跑一遍与 18 项 `ALL` 抽样对照（AC4 的「任意 3 个日期」）。
- **AC1 / AC2**：长表 `ops_daily_metrics`（一行 = WIB 日 × 指标 × 口径，`NUMERIC(18,4)` ↔ `BigDecimal`），`OpsDailyMetric.of(...)` 工厂供 3-3 写入；`OpsDailyMetricRepository`（`existsByReportDate` / `findByReportDateBetweenOrderByReportDate` / `findDistinctReportDates`）。`DashboardMetric` 18 值按口径表 # 顺序，`key()` = snake_case 落库 key（`cumulative_users` 已按 D-16 改名），`dualScope` = {3,4,8～14}，`hintKey()` → `admin.v130.dashboard.metric.<key>.hint`；`MetricScope{ALL, REAL}`。
- **AC3 查询层**：`MetricQuery` 接口 + `AbstractMetricQuery`（口径校验 + `NamedParameterJdbcTemplate` 单值查询；非双口径传 REAL 抛 `IllegalArgumentException`）+ 18 个 `@Component`；公共片段 `MetricSql`：`day(expr)` = `(expr AT TIME ZONE 'Asia/Jakarta')::date`（OQ-B3，全部查询无裸 `::date`，L0 守卫）、`VISIBLE_POST_WHERE`、`VALID_COMMENT_WHERE`（**D-38：VISIBLE 且未删**）、`interactionsCte(scope)`（赞 ∪ 有效评论；REAL = 互动者真实 且 帖子作者真实，D-29）、`realAuthorJoin(scope)`、`postScoreLateral()`。真实用户口径只引用 3-1 的 `SyntheticAccountSql.EXCLUDE_WHERE`（`realUser(alias)` 仅换别名），无任何前缀散写（L0 守卫）。#5 按人去重（D-28，`min(created_at)` 落日）；#12 / #13 空集返回 `null`（3-3 存 0 并记「无样本」）；#17 / #18 已退款问诊单当日仍计。
- **AC5**：查询类只读业务表（L0 断言 SQL 无 INSERT / UPDATE / DELETE、不含 `ops_daily_metrics`），写入归 3-3。
- **与参考 SQL 的有意差异**（parity 测试已按此断言）：① 切日显式 WIB（参考 SQL 随连接时区）；② 有效评论加 `moderation_status='VISIBLE'`（D-38，影响 #8～#14）；③ #5 按人去重（D-28）；④ 真实用户按 `account_type` / `role` 列判定（3-1），参考 SQL 按前缀且 `role='USER'`——现网 `users.role` 只有 USER / ADMIN（VET 仅出现在 JWT，不落 users 表），两者等价；`role=ADMIN` 官方作者 shim 的口径分歧见 3-1 Notes（待拍板，本 story 按 `EXCLUDE_WHERE` 执行）；⑤ **🔴 #17/#18 身份证高清成交口径（需像 D-38 一样拍板回写 PRD ①-a #17 措辞）**：「身份证高清购买 `purchased_at`」改为「PawCoin 行 `purchased_at` ∪ ID_HD 支付单 PAID 到账」，理由见复审 ①——参考 SQL 写于 V92 之前，按原写法会系统性抬高含 PawCoin 付费人数 / 次数；⑥ #7 档案存在性加 `pp.created_at ≤ D 24:00`（PRD 通用规则「截止点 24:00」）。**已知补跑漂移**（与参考 SQL 同源，未改）：可见帖 `status='PUBLISHED' AND deleted_at IS NULL` 按查询时刻评估，帖子事后被删 / 下架会让补跑的历史日变小；3-3 「算完不再重算」兜底，记录备查。
- **提示文案**：18 条 hint 直接改写自 SQL 头注释 / PRD 口径列（三语），不引用列名 / 枚举值（`AdminMessagesParityTest` 约束）。

### File List

- petgo-backend/src/main/resources/db/migration/V20260909_1432__init_ops_daily_metrics.sql（新增）
- petgo-backend/src/main/java/com/tailtopia/admin/dashboard/domain/{DashboardMetric,MetricScope,OpsDailyMetric}.java、repository/OpsDailyMetricRepository.java（新增）
- petgo-backend/src/main/java/com/tailtopia/admin/dashboard/metrics/{MetricQuery,MetricSql,AbstractMetricQuery}.java + 18 个 `*MetricQuery.java`（新增）
- petgo-backend/src/main/resources/i18n/messages{,_zh_CN,_en,_id}.properties（+18 key `admin.v130.dashboard.metric.*.hint`）
- 测试：admin/dashboard/domain/DashboardMetricTest、admin/dashboard/metrics/MetricQuerySqlTest（新增 L0）；admin/dashboard/MetricQueryParityTest（新增 L1）
