---
baseline_commit: ac3d57b733769b3bbc374e82db59a07082e26522
epic: 1
story: 4
batch: A
verification: [L0, L1]
---

# Story 1.4: 庆祝状态记账与存量回填

Status: ready-for-dev

## Story

As a 用户，
I want 系统记得哪些成就我还没被告知过，
so that 我错过的庆祝能被补上而不是永远消失。

---

> **现状**：庆祝只有一次机会。不在场时解锁（如被点赞触发）→ 无庆祝；存健康记录后 500/800/1200ms 三次轮询没拉到 → 永远不弹。系统**根本不记录「庆祝过没有」**，所以也无从补。
>
> **本 story 只做记账，不做补弹**（补弹在 1.5）。三件事：加字段、回填存量、提供幂等回报通道。
>
> 🔴 **回填不可省**：不回填则上线首日全体老用户进列表页被补弹一堆旧成就，且不可撤回。**必须与建列同在一支迁移内。**

---

## Acceptance Criteria

### AC1 — 字段落在正确的表（L0 / L1）

**Given** 数据库
**When** 迁移执行完毕
**Then** `milestone_completions` 新增可空列 `celebrated_at`（`TIMESTAMPTZ`，UTC） `[L1]`
**And** 该列加在 `milestone_completions` 而非 `pet_milestones` `[L0]`
**And** 迁移文件名为时间戳制 `[L0]`

> ⚠️ PRD §3.2 原文写「`pet_milestones` 新增 `celebrated_at`」，**已由架构 delta AD-A1.1 订正**。`pet_milestones` 是每宠物的目录实例（建档即全量铺行），完成态在 `milestone_completions`（1:1，`uq_milestone_completions_milestone`）。

### AC2 — 🔴 存量回填（L1）

**Given** 上线前已存在的全部完成记录
**When** **同一支迁移**执行完毕
**Then** 其 `celebrated_at` 已回填为各自的 `completed_at` `[L1]`
**And** 回填与建列在同一支迁移内，不拆两次上线 `[L0]`

**Given** 通用宠物被错点亮的 3 条脏完成行（决策 A-9：不清理）
**When** 回填执行
**Then** 这 3 条一并被标记为「已庆祝」 `[L1]`
**And** 这是期望结果 —— 它们不该再被补弹 `[L1]`

### AC3 — 判据唯一（L0）

**Given** 全链路
**When** 判断某条是否「已完成且未庆祝」
**Then** 判据一律为「`milestone_completions` 存在行 且 `celebrated_at IS NULL`」 `[L0]`
**And** 前端不得另立任何本地标记 `[L0]`

### AC4 — 回报端点幂等（L1）

**Given** 一次庆祝回报请求，目标记录 `celebrated_at` 为空
**When** 服务端处理
**Then** 写入当前时刻 `[L1]`
**Given** 目标记录 `celebrated_at` 已有值
**Then** 不覆盖 `[L1]`
**And** 实现为 `UPDATE ... WHERE celebrated_at IS NULL` `[L0]`

### AC5 — 🔴 置位范围按客户端回传列表（L1）

**Given** 客户端提交的 code 列表
**When** 服务端置位
**Then** **只**置位列表中的条目 `[L1]`
**And** **不得**执行 `WHERE celebrated_at IS NULL` 的批量置位 `[L0]`

> 🔴 批量置位会吞掉「客户端读取 → 回报」之间新解锁的条目（被点赞、被评论都可能在这几百毫秒内发生），使其**永不补弹** —— 等于用 FR-111 造出 FR-111 要修的 bug。

### AC6 — 未庆祝计数随既有接口下发（L1）

**Given** 成长档案页头部所依赖的接口
**When** 返回里程碑进度数据
**Then** 一并返回「已完成且未庆祝」的条目数 `[L1]`
**And** **不**为此新开接口、**不**额外发一次请求 `[L0]`

### AC7 — 不扩散（L0）

**Then** 不实现角标 UI、不实现补弹（属 Story 1.5） `[L0]`
**And** 不改动庆祝页任何视觉（重绘属批次 C） `[L0]`

---

## Tasks / Subtasks

- [ ] 时间戳迁移：`milestone_completions` 加 `celebrated_at` + **同支内** `UPDATE ... SET celebrated_at = completed_at`（AC1/AC2）
- [ ] 实体与仓库补字段（AC1）
- [ ] 回报端点：接收 code 列表，按列表幂等置位（AC4/AC5）
- [ ] 未庆祝计数并入档案头部接口的既有响应（AC6）
- [ ] 测试：幂等、按列表置位、回填正确性、窗口期新解锁不被吞（AC4/AC5）

## Dev Notes

### 🔴 两个不可省

1. **回填与建列同一支迁移**。拆开上线 → 中间态所有老用户被补弹。
2. **按列表置位，不 mark-all**。这是对抗性评审 H-2 直接指出的洞。

### 要改的文件

| 文件 | 改什么 |
|---|---|
| `petgo-backend/src/main/resources/db/migration/V<ts>__*.sql` | **新增**：加列 + 回填 |
| `petgo-backend/.../profile/domain/MilestoneCompletion.java` | 补字段 |
| `petgo-backend/.../profile/repository/MilestoneCompletionRepository.java` | 幂等置位查询、未庆祝计数 |
| `petgo-backend/.../profile/web/MilestoneController.java` | **新增**回报端点 |
| 档案头部所依赖的响应 DTO | 补未庆祝计数字段 |

### 命名与口径

- 列名 `celebrated_at`（snake_case）↔ Java `celebratedAt` ↔ JSON `celebratedAt`
- 时间一律 UTC 存储（本 story 无 WIB 判定）
- 端点走 `/api/v1`，当前用户用 `/api/v1/me` 口径

### 验证层级

- `[L0]` 编译、迁移文件名、mark-all 未被使用
- `[L1]` 回填正确性、幂等、窗口期用例 —— 留本地

### References

- 架构 delta `AD-A1` `AD-A2.2/2.3b` `AD-A3` · 决策日志 `A-9`

---

## Dev Agent Record

### Context Reference

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

---

## Change Log

| 日期 | 变更 | 作者 |
|---|---|---|
| 2026-09-10 | 创建 | bmad-create-epics-and-stories |
