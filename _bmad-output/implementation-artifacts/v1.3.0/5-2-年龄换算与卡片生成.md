---
baseline_commit: ac3d57b733769b3bbc374e82db59a07082e26522
epic: 5
story: 2
batch: A
verification: [L0, L2]
---

# Story 5.2: 年龄换算与卡片生成

Status: ready-for-dev

## Story

As a 养猫或养狗的用户，
I want 看到我的宠物相当于人类多少岁并生成一张卡片，
so that 我能把它分享出去。

---

> 依赖 Story 5.1（入口）。**本 story 不含分享奖励**（属 5.3），去掉奖励也能独立交付并使用。
>
> **纯客户端**：换算、出图、分享全在端上，不落服务端、无 H5 回流页、零迁移。
>
> ⚠️ **卡面视觉以设计稿为准**（决策已拍板）。设计稿未到时可用纯色背景先跑通全链路。

---

## Acceptance Criteria

### AC1 — 狗需先选体型（L2）

**Given** 当前宠物是**狗**
**When** 点击年龄换算卡
**Then** 先进入体型四选一：小型 `< 9 kg` / 中型 `9–23 kg` / 大型 `23–41 kg` / 超大型 `> 41 kg` `[L2]`
**And** 档位名旁标注体重区间 `[L2]`
**And** 所选档位**不落档案字段、不持久化、不校验** `[L0]`

**Given** 当前宠物是**猫**
**Then** 直接进入卡片预览，不经过体型选择 `[L2]`

### AC2 — 换算规则（L0）

**Given** 一只猫
**Then** 首年 = 15，次年 +9（满 2 岁 = 24），之后每年 +4 `[L0]`
**Given** 一只狗
**Then** 首年 = 15，次年 +9，第 3 年起按体型每年 +4 / +5 / +6 / +7 `[L0]`
**And** 不足整年按月线性插值；结果四舍五入取整，卡面显示「≈ N 人岁」 `[L0]`
**And** 未满 1 岁按月龄比例（如 6 个月的猫 = 7.5 → ≈ 8） `[L0]`

### AC3 — 时区口径（L0）

**Given** 计算「当日」
**Then** 取**设备本地时区** `[L0]`
**And** **不用 WIB** —— 那是 Story 5.3 的资金口径，两者不得互相套用 `[L0]`
**And** 生日为建档必填完整日期，**无缺失兜底分支** `[L0]`

### AC4 — 预览页与画布（L2）

**Given** 卡片预览页
**Then** 结构为 AppBar + 画布区 + 底部吸底分享按钮 `[L2]`
**And** 画布支持 9:16（`1080×1920`，默认）与 1:1（`1080×1080`） `[L0]`
**And** 复用既有分享卡基建（`shared/card_render/`：画布、按目标宽度反算截图倍率、白底合成防黑角、系统分享面板），**不另起一套** `[L0]`

### AC5 — 卡面字段（L2）

**Given** 卡面
**Then** 包含：头像（无则字母头像）+ 名字 · 实际年龄 · 人类年龄当量 · 趣味文案 · 体型档（仅狗卡）· 底部品牌带 `[L2]`
**And** **不加水印**（水印只属 KTP/护照的付费保护场景） `[L0]`

### AC6 — 趣味文案分段（L0）

**Given** 趣味文案
**When** 取值
**Then** 按**卡面展示的当量整数 N** 分段：`N ≤ 19` / `20–39` / `40–59` / `N ≥ 60` `[L0]`
**And** 判定用展示的那个整数，**不用实际月龄**（否则会出现卡上写 ≈40 却配幼年文案） `[L0]`
**And** 同段内多条随机取一条 `[L0]`
**And** 猫狗**共用同一套分段**，不按物种分叉 `[L0]`
**And** 文案取自客户端 EN / ID 双语表（见 `文案需求清单.md` §5，共 4 段 × 5 条） `[L0]`

### AC7 — 🔴 二维码（L0 / L2）

**Given** 底部品牌带的二维码
**Then** 内容为固定的 App 下载页 URL，**走配置项、不硬编码、不带 token** `[L0]`
**And** 四周留白 ≥ 4 个码元 `[L0]`
**And** **导出分辨率下边长 ≥ 140px**（不可按预览尺寸的像素直接输出） `[L0]`
**And** 深色背景上有白色底板 `[L2]`
**And** 导出图实际可被主流扫码 App 扫出 `[L2]`

> 🔴 这三条是可扫底线，**不可为视觉让步**。`CardQr` 有构造期 assert，压缩品牌带会当场拦下。

### AC8 — 生成与分享（L2）

**Given** 生成与分享
**Then** 免费、无次数限制 `[L2]`
**And** 图片**纯客户端生成，不落服务端、无 H5 回流页** `[L0]`

### AC9 — 埋点值域（L1）

**Given** 任一次生成或分享
**Then** 上报 `age_card_generated` / `age_card_shared`，携带 `species`、`size_class`（狗）、`canvas` `[L1]`
**And** `canvas` 值域 `story` / `square`；`size_class` 值域 `small` / `medium` / `large` / `xlarge` `[L0]`

### AC10 — 不扩散（L0）

**Then** 本 story **不接入 PawCoin 奖励**（属 5.3） `[L0]`
**And** 不改动 KTP 的水印与出图行为 `[L0]`

---

## Tasks / Subtasks

- [ ] 换算规则实现 + 单测（猫/狗四档/插值/未满 1 岁边界）（AC2/AC3）
- [ ] 狗体型四选一页（不落库）（AC1）
- [ ] 卡片预览页，复用 `share_card_preview_page.dart` 结构与 `card_render/` 基建（AC4）
- [ ] 卡面模板：字段清单、无水印（AC5）
- [ ] 趣味文案池进两语表，按 N 分段随机（AC6）
- [ ] 二维码指下载页 URL（配置项），三条硬要求（AC7）
- [ ] 埋点（AC9）
- [ ] 真机导出扫码验证（AC7）

## Dev Notes

### 换算规则的调研依据（不要自行改口径）

采用 AVMA / AAHA / AAFP 的三段式分档，**不采用** UCSD 对数公式（`16×ln(年龄)+31`，只基于 104 只拉布拉多，且 1 岁 = 31 人岁反直觉、做社交分享会引发「算错了」）。
Roadmap 原文「猫前 2 年各 ≈15（2 岁 = 30）」与所有来源不符，**已按调研修正为 15 + 9**。

### 要改的文件

| 文件 | 改什么 |
|---|---|
| `petgo_app/lib/features/profile/presentation/`（新建） | 体型选择页、年龄卡预览页 |
| `petgo_app/lib/features/content/presentation/share_card/share_card_template.dart` | **参照**结构，新增年龄卡内容模板 |
| `petgo_app/lib/shared/card_render/*.dart` | **只读复用**（`card_canvas` / `card_export` / `card_qr` / `card_frame`） |
| `petgo_app/lib/l10n/app_en.arb` / `app_id.arb` | 趣味文案池、体型档名 |

### 验证层级

- `[L0]` 换算单测（边界：0 个月 / 11 个月 / 恰好 2 岁 / 四个体型档）、分段判定、二维码常量
- `[L2]` **导出图真机扫码**、卡面观感、分享面板

### References

- 架构 delta `AD-A18`（含 5b 分段规则、5c 长度约束）· `AD-A19` · `文案需求清单.md` §5

---

## Dev Agent Record

### Context Reference

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

---

## Change Log

| 日期 | 变更 | 作者 |
|---|---|---|
| 2026-09-10 | 创建 | bmad-create-epics-and-stories |
