---
baseline_commit: 4301227a
branch: feat/1.3.0-ops-ui-refactor
epic: 11
story: 11.3
ad: [AD-9, AD-11]
decisions: [D-11, D-23]
depends_on: [全部 Epic 2~10]
---

# Story 11.3: 退役路由与模板清理确认

Status: review

> 自包含 story，L0 为主（grep + MockMvc 404），可云端执行。与用户沟通用中文。
> D-23：旧地址**不做跳转兜底**，直接 404；本 story 只确认清理彻底，不新增功能。

## Story

As a 工程负责人，
I want 所有并入抽屉 / 页签的独立页面，其路由、模板、模板内链接、导航项、i18n 孤儿 key 都被清干净，
so that 重构后不会残留半死不活的页面和指向 404 的链接。

## Acceptance Criteria

**AC1 · 精确退役清单（基线 2026-09-09 代码核实）**
**Then** 以下 GET 路由与模板全部删除（负责 story 见括号）`[L0]`

| GET 路由 | 模板 | 归属 story |
|---|---|---|
| `/admin/content/{postId}` | `content-detail.html` | 7.1 |
| `/admin/users/{userId}` | `user-detail.html` | 8.1 |
| `/admin/consult-orders/{orderToken}` | `consult-order-detail.html` | 8.4 |
| `/admin/ai-orders/{orderToken}` | `ai-order-detail.html` | 8.4 |
| `/admin/anomalies/{id}` | `anomaly-detail.html` | 2.6 |
| `/admin/refunds/{refundToken}` | `refund-detail.html` | 2.8 |
| `/admin/support-tickets/{ticketToken}` | `support-ticket-detail.html` | 2.7 |
| `/admin/tickets/detail` | （内嵌于 `tickets.html`） | 2.5 |
| `/admin/shop/orders/{token}` | `shop-order-detail.html` | 10.2 |
| `/admin/shop/returns/{token}` | `shop-return-detail.html` | 10.1 |
| `/admin/vets/{id}/edit` | `vet-edit.html` | 9.1a |
| `/admin/vets/online` | `vet-online.html` | 9.1a |
| `/admin/vets/{id}/qualification`（GET） | `vet-qualification.html` | 9.1b |
| `/admin/vets/{id}/ratings` | `vet-ratings.html` | 9.1b |
| `/admin/ratings` | `ratings.html` | 9.1b |
| `/admin/reports` | `reports.html` | 2.4 |
| `/admin/content-schedules` | `content-schedules.html` | 7.5 |

**And** 保留（勿误删）：`POST /admin/vets/{id}/qualification*`、`POST /admin/reports/**` 处置端点、`POST /admin/content-schedules/{rowId}/*`、`/admin/shop/products/{id}` 与 `/new`（表单页保留独立，10.3）`[L0]`

**AC2 · 访问旧地址 404**
**When** MockMvc 以超管身份 GET 上表每一条
**Then** 404（Spring 无映射），**不是** 302 / 200 `[L1]`

**AC3 · 无残留引用**
**Then** `grep -rnE "content-detail|user-detail|anomaly-detail|refund-detail|support-ticket-detail|shop-order-detail|shop-return-detail|vet-edit|vet-online|vet-qualification\.html|vet-ratings|reports\.html|content-schedules\.html" petgo-backend/src/main` 为零 `[L0]`
**And** 模板内 `th:href` 不再指向上表任一 GET（`/admin/content/' + id`、`/admin/users/' + id`、`/vets/online`、`/admin/ratings`、`/admin/reports(status=…)`、`/{id}/edit`、`/{id}/qualification`、`/{id}/ratings`、`/admin/content-schedules` 等）；所有跨页跳转改为 `?open=<id>` 页内深链 `[L0]`
**And** `layout.html` / `fragments/nav.html` 无「在线状态」「评分查看」「定时发布」「举报队列」菜单项 `[L0]`

**AC4 · i18n 孤儿 key 处置**
**Then** 仅服务已删模板的 key（`admin.online.*`、`admin.ratingOverview.*`、`admin.ratings.back` 等）：**允许保留**但在 `messages_zh_CN.properties` 顶部注释登记「v1.3.0 退役页遗留，可清理」；不允许删掉仍被抽屉复用的 key（`admin.qualpage.*`、`admin.ratings.col.*`、`admin.vets.*`）`[L0]`

**AC5 · 测试类同步**
**Then** 针对已删 GET 的既有测试用例改为 404 断言或删除；`mvn -B clean package` 全绿 `[L0/L1]`

---

## Tasks / Subtasks

- [x] **T1 · 生成对照**：从 Story 2.1 基线 GET 列表与 11.1 after 列表取差集，与 AC1 表逐条核对；多删 / 少删都记入 11.1 报告
- [~] **T2 · 404 测试**：新增 `RetiredAdminRoutesTest`（`ApiIntegrationTest`），循环上表断言 404
- [x] **T3 · grep 三连**（AC3）：模板名 / `th:href` / 导航；结果贴 Completion Notes
- [x] **T4 · i18n 登记**（AC4）
- [x] **T5 · 云端执行须知**：T1/T3/T4 云端 L0；T2 需真库（MockMvc 全上下文）留本地或云端有 Docker 时跑

---

## Dev Notes

### 🔴 数量对不上是正常的，以本表为准

epics 各处写「8 个独立详情页」是 B 类页并入抽屉的口径；加上 9.1a/9.1b 的兽医四页 + 评分总览、2.4 的 `reports`、7.5 的 `content-schedules`、2.5 的 `tickets/detail`，**实际退役 17 条 GET 路由 / 16 个模板**。11.1 的 GET 白名单与本表必须一致。

### 🔴 `/admin/reports` 的 POST 端点保留

2.4 只退役页面路由，`reports/{id}/takedown`、`reports/{id}/dismiss`、`reports/post/{id}/dismiss-all` 由 A1 内容举报页签继续调用（PRD §5 ①「端点保留」）。`tickets.html` 354/357 行、`manual-review.html` 154/160 行现状引用这些 POST，重构后仍会引用——grep 时只查 GET 形态。

### 🔴 不做跳转兜底

D-23 拍板：运营收藏链接自行更新。**不要**顺手加 `@GetMapping` 302 到列表页；那会让 11.1 的 GET diff 多出白名单外条目。

### References

- [Source: epics-v1.3.0-admin.md#Story 11.3 / 2.4 / 2.5 / 2.6 / 2.7 / 2.8 / 7.1 / 7.5 / 8.1 / 8.4 / 9.1a / 9.1b / 10.1 / 10.2] · [Source: 决策日志-admin.md D-11 D-23] · [Source: 后台重构逐页规格.md#模板 B 通用规格]
- [Source: petgo-backend/src/main/resources/templates/admin/（2026-09-09 全表 64 模板）]

## Dev Agent Record

### Agent Model Used

Claude Code（云端 headless session；L0 全跑，L1 的 404 断言留本地）

### Debug Log References

- L0：`bash scripts/ci/l0-backend.sh` → **BUILD SUCCESS，Tests run 1846 / Failures 0 / Errors 0**。
- **负向自检（实跑，跑完还原）**：往 `vets.html` 逐条注入五种指向退役路由的写法（单引号 `th:href` / `hx-get` / 双引号 `th:href` / 带参 `@{...(tab='x')}` / Java 侧 `redirect:`）→ **五条全部报出**；还原后全绿。
- `bash scripts/ci/check-i18n-keys.sh` → OK（2865 key，四包集合相等）。
- AC1 的逐条核对不靠肉眼：直接拿 **Story 11.1 产出的 after 清单**（机器生成）对 AC1 那张表逐行查 `GET` 是否还在。

### Completion Notes

**AC1 · 17 条退役路由逐条核对结果**（依据 = `后台写操作清单-20260910-after.md`）：

| 状态 | 条数 | 明细 |
|---|---|---|
| ✅ 已删 | 13 | `/admin/reports`(2.4) · `/admin/tickets/detail`(2.5) · `/admin/anomalies/{id}`(2.6) · `/admin/support-tickets/{ticketToken}`(2.7) · `/admin/refunds/{refundToken}`(2.8) · `/admin/content/{postId}`(7.1) · `/admin/content-schedules`(7.5) · `/admin/users/{userId}`(8.1) · `/admin/consult-orders/{orderToken}` + `/admin/ai-orders/{orderToken}`(8.4) · `/admin/vets/online` + `/admin/vets/{id}/edit`(9.1a) · `/admin/ratings`(9.1b) |
| 🔄 **整页退役、路径保留** | 2 | `/admin/vets/{id}/qualification`、`/admin/vets/{id}/ratings` |
| ⏳ **未执行** | 2 | `/admin/shop/orders/{token}`(10.2)、`/admin/shop/returns/{token}`(10.1) |

对应的 14 份模板全部已删（两份 `shop-*-detail.html` 仍在，属 Epic 10）。

**🔴 两条 AC 偏差，都不改代码、按现状记账：**

1. **`GET /admin/vets/{id}/qualification` 与 `/admin/vets/{id}/ratings` 不删**。AC1 把它们列进退役表，但 Story 9.1b 实际是把它们改成了**抽屉页签的懒加载片段**：htmx 请求返 `drawer-vets :: qual-panel` / `:: rating-panel`，非 htmx 直达则 302 回列表并开抽屉。**整页模板确实删了**（`vet-qualification.html` / `vet-ratings.html` 不存在），退役的语义已经达成；删路径等于把抽屉的两个页签一起干掉。同路径的 `POST /admin/vets/{id}/qualification*` 更是 AC1 自己「保留（勿误删）」名单里的。已在 Story 11.1 的底账里把这两条的备注从 ⛔ 改成 🔄，避免下一个人照着 ⛔ 去删。
2. **商城两条不动**：Epic 10 前置是 v1.4.0 电商线合入（AD-12），本轮未执行，`/admin/shop/**` 整组没重构。它们已在 11.1 白名单里作为**预授权删除项**写着，Epic 10 落地时删掉不会让守门报事故。

**AC2 · 旧地址不再是 GET 路由**（`AdminRetiredRoutesTest`，L1，1 条映射断言 + 14 条参数化）。

🔴 **复审抓到一条「一上真库必红」**：`/admin/vets/online` 实际返回 **405 不是 404** ——
`POST /admin/vets/{id}` 在**路径层面**盖住了它（`{id}` 是路径变量，Spring 匹配路径时不看它声明成 long），
方法不匹配 → `HttpRequestMethodNotSupportedException` → 405 + `Allow: POST`。
本仓库 `GlobalExceptionHandler` 的 javadoc 里就写着这一条「只退役 GET、保留 POST 是重构里的常规动作」。
**405 同样证明 GET 映射不存在**（它恰恰说明这条路径上只剩 POST），但拿 404 去断言会让这条测试
一上真库就红，而红的原因跟「有没有删干净」毫无关系。全量交叉验证（把生成器的 267 条映射转成正则
逐条比对 14 个路径）确认**只有这一条**撞上同路径 POST。

连带修掉既有的 `AdminVetDrawerIntegrationTest.theTwoRetiredPagesAreGone`：它对同一条路径也断言 404，
而它的 javadoc 还特意写了「这条路径上没有同名的 POST，所以是干净的 404」—— **作者意识到了遮蔽这回事，
却把结论用反了**。

因此 AC2 改成两层：
1. `noRetiredPathStillHasAGetMapping` —— 直接问 `RequestMappingHandlerMapping` 要答案：这些路径上
   **不该再有任何 GET 映射**。这比断言状态码强，因为它不受路径遮蔽影响，也正是 AC2 真正想说的事。
2. 14 条状态码参数化 —— 13 条断言严格 404，`/admin/vets/online` 单独断言 405 + `Allow: POST` 并注明原因。

钉**严格** 404 而不是「非 2xx」：退役页有两种「还活着」的形态都不算删干净——**302**（留个 redirect 壳把旧地址转到新页，看着体贴，实际是永远删不掉的尾巴：书签 / 外部文档 / 硬编码全靠它续命，D-23 明确不做旧地址跳转）和 **200**（压根没落地）。用**超管**跑：普通账号缺权限拿到的是 403，403 和 404 都是「进不去」，但只有 404 能证明**路由不存在**。

**AC3 · 无残留引用**（`AdminRetiredRoutesStaticTest`，L0，4 条）：AC3 给的是一串 `grep`，但直接照抄会**恒红** —— 全仓有十几处注释写着「整页 `vet-edit.html` 已删，内容并入抽屉」，那正是应该写的东西。所以实现为**剥掉注释后**再查，且分成四条各管一件事：

- `everyRetiredPageTemplateIsActuallyGone` —— 模板文件真的不在了；
- `noControllerStillReturnsARetiredTemplate` —— 没有 Controller 还 `return "admin/<退役模板>"`。**这是「模板删了、返回值没删」的唯一机器证据**：视图名是字符串，编译期不报错，运行期才 500，而且只有真的访问那条旧地址才触发；
- `noControllerReintroducesAGetMappingForARetiredRoute` —— **路由复活**。前面几条都管不了这件事：
  模板文件删了、视图名不是退役模板名（返回一个片段就行），一条 `@GetMapping("/admin/reports")` 加回来
  **四条静态断言全绿**（复审 C4 实测）；而唯一能管的 AC2 是 L1、云端跑不了。补这条纯文本扫描代价近乎为零。
  注解写全限定名（`@org.springframework...GetMapping`）也认。
- `noTemplateStillLinksToARetiredPage` —— 没有链接还指向退役整页（跨页跳转一律改 `?open=<id>` 页内深链，D-23）。
  🔴 **一条路由的各种写法都要认得**：第一版只匹配 `th:href="@{...}"` 一种字符串，
  换成**单引号**、换成 `hx-get`、写成 Java 里的 `redirect:` 就静默放过 —— 而这三种在本仓库都是常见写法。
  改成正则（属性名可带 `th:`/`hx-` 前缀 + 单双引号 + 可选 `@{` + 路由 + 合法后继字符）并加 Java 侧 `redirect:` 扫描。
  后继字符集里 **`}` 不能漏**：`th:href="@{/admin/ratings}"` 正是最常见的形态，漏了它这条断言对绝大多数真实残留都不响（实测两种写法全漏）。
  五种写法逐条注入实测：单引号 `th:href` / `hx-get` / 双引号 `th:href` / 带参 `@{...(tab='x')}` / Java `redirect:` —— **全部报出**，还原后全绿。

  🔴 复审又抓出两个漏洞，都已修 + 实测：
  - **`th:attr="hx-get=@{...}"` 认不出来**（复审 C3）—— 而这是本仓库**用得最多**的 htmx 跳转写法
    （`th:attr="hx-post="` 99 处 / `"hx-get="` 73 处 / `"data-drawer-deeplink="` 17 处）。
    第一版正则要求「属性名后紧跟引号」，而这种写法属性名后面跟的是 `@{`，恰好把用得最多的那一种漏了。
  - **AC3 逐字点名的带参路由一条都没覆盖**（复审 C2）—— `@{'/admin/content/' + ${p.id}}`、
    `@{/admin/users/{id}(id=${u.id})}` 这类实测全绿。补 `RETIRED_PARAM_HREFS`（content / users /
    consult-orders / ai-orders / anomalies / refunds / support-tickets 七个前缀）。
    ⚠️ 判据是**「参数之后 URL 就结束了」**，不是一份「仍在服役的子路径」白名单 ——
    白名单那种写法要穷举 `/ban` `/note` `/drawer` 几十个在役子路由，漏一个就是一次假阳性
    （第一版实测误报 6 处）。退役的是**裸详情页**，所以只有「前缀 + id + URL 结束」才算残留。
  - 八条注入实测：4 条该红的全红（含全限定名 `@GetMapping`），4 条在役路由（`/users/{id}/drawer`、
    `/reports/{id}/takedown`、`/content/{postId}/drawer`）**无一误报**，还原后基线全绿。
- `navigationHasNoEntryPointingAtARetiredPage` —— 侧导航**与 `AdminPageCatalog`** 里都没有退役页。查 catalog 是必须的：导航由 catalog 驱动，只查模板的话，catalog 里留一条死页面照样会渲染出菜单项。**「菜单看得见、点了打不开」是三种半成品里最难受的一种。**

五条全绿。⚠️ 商城两条**故意不写进这个测试的名单**：Epic 10 未执行，写进去这条护栏从落地起就是红的，而**一条从来没绿过的护栏等于没有**。

**AC4 · 孤儿 key 处置**：按判据（key 在 `src/main` 的 java/html/js/sql 里一次都没出现，且不属于任何动态拼接前缀）扫出 **164 个**孤儿 key，分 26 组。**保留不删**，登记两处：

- `messages_zh_CN.properties` 顶部注释（AC4 原文要求的位置），列出涉及的前缀与红线；
- 逐条清单 + 归属 story：`_bmad-output/implementation-artifacts/v1.3.0/i18n-孤儿key登记-20260910.md`。

不删的理由写在文件里：判据是静态扫描，而 Java 里 `"admin." + module + ".title"` 这类**动态拼接永远可能漏**；四包同删才安全，漏删一包就破坏 `check-i18n-keys.sh` 的集合相等；删 key 换不来任何运行期收益，却换来一次真删错就上线报 `NoSuchMessage` 的风险。清理时机定在 Epic 10 落地、全站页面定型之后。

🔴 扫描前缀排除逻辑是**为了不误杀** `admin.qualpage.*` / `admin.ratings.col.*` / `admin.vets.*` 这类「整页退役但被抽屉页签原样复用」的 key —— AC4 点名不许删的正是它们，实测它们都不在孤儿名单里。

**AC5 · 测试类同步**：既有测试里针对已删 GET 的用例早已改成 404 断言（`AdminVetDrawerIntegrationTest` 的 `/admin/vets/online`、`AdminSchedulesTabIntegrationTest` 的 `/admin/content-schedules`、`TicketsWorkbenchMvcTest` 的 `/admin/tickets/detail`），本 story 把它们**收口成一条集中的参数化测试**，不动既有用例。`mvn -B clean package`（排除需真库的类）全绿。

**其它复审项**：`everyRetiredPageTemplateIsActuallyGone` 加了防空转锚（用 `Files.exists` 判「不存在」，
工作目录不对时 14 个 exists 全 false 会静默通过，P1）；14 条里的 `/admin/seed-batch` 来自 Story 7.6 而非
AC1 的表，已在注释里交代（P2）；`superAdmin()` 不再往共享库白扔一行 —— 那 14 条断言的权限全来自
`TestingAuthenticationToken`，一次库都不读，而 `ApiIntegrationTest` 不回滚（P6）；顺手补了 Story 11.1
生成器里漏掉的 `/admin/shop/returns/{token}` 退役备注（P3），并给孤儿 key 登记文档加了
「中文值按 60 字截断、真值以 properties 为准」的说明（P5）。

**🔬 L1 待本地验收**（云端无 Docker）：

- `AdminRetiredRoutesTest`：1 条映射断言 + 14 条状态码断言真库跑通。**这是 AC2 唯一的验收证据**，云端只保证它编译通过。
- 跑通后如有某条不是 404 而是 302/200，说明那一页只删了一半，回对应 story 修。

### File List

**新增**

- `petgo-backend/src/test/java/com/tailtopia/admin/web/AdminRetiredRoutesStaticTest.java`（AC3，L0 静态四条）
- `petgo-backend/src/test/java/com/tailtopia/admin/web/AdminRetiredRoutesTest.java`（AC2，L1 参数化 404 ×14）
- `_bmad-output/implementation-artifacts/v1.3.0/i18n-孤儿key登记-20260910.md`（AC4，164 个 key 分 26 组逐条登记）

**改**

- `petgo-backend/src/main/resources/i18n/messages_zh_CN.properties`（顶部注释登记退役页遗留 key）
- `petgo-backend/src/test/java/com/tailtopia/admin/vet/AdminVetDrawerIntegrationTest.java`（`/admin/vets/online` 改断言 405 + `Allow: POST`，并写明路径遮蔽的原因）
- `scripts/ci/list-admin-write-ops.sh` + 基线 v2 / after 清单（补 `/admin/shop/returns/{token}` 的退役备注，复审 P3）

**迁移**：无。
