# 后台写操作清单 · Epic 5 新增端点登记（2026-09-09）

> V1.3.0 Story 5.3 AC5（Story 2.1 底账的「PRD 定义的新增」列）。基线 = `后台写操作清单-20260909.md`（134 个写端点）。
> Story 11.1 重跑 `scripts/ci/list-admin-write-ops.sh` 做 diff 时，下列端点应且仅应出现在「新增」列（PRD AB-17A ② 处置动作表）；本文件供人工核对，不替代生成器输出。

## Story 5.3 · 场所处置（B6 抽屉操作条）

| 方法 | 路径 | Controller#方法 | @PreAuthorize | 页面组 | 审计 |
|---|---|---|---|---|---|
| POST | `/admin/places/{id}/edit` | `AdminPlaceController#edit` | `AdminPlaceController.VIEW_AUTH`（`hasRole('SUPER_ADMIN') or hasAuthority('place.manage')`） | 内容 | `PLACE_EDITED`（只记改动字段名） |
| POST | `/admin/places/{id}/delist` | `AdminPlaceController#delist` | 同上 | 内容 | `PLACE_DELISTED`（幂等 no-op 不记） |
| POST | `/admin/places/{id}/restore` | `AdminPlaceController#restore` | 同上 | 内容 | `PLACE_RESTORED`（幂等 no-op 不记） |
| POST | `/admin/places/{id}/photos/{photoId}/remove` | `AdminPlaceController#removePhoto` | 同上 | 内容 | `PLACE_PHOTO_REMOVED` |
| POST | `/admin/places/{id}/comments/{commentId}/remove` | `AdminPlaceController#removeComment` | 同上 | 内容 | `PLACE_COMMENT_REMOVED`（不记正文） |
| POST | `/admin/places/{id}/merge` | `AdminPlaceController#merge` | 同上 | 内容 | `PLACE_MERGED`（B(name) → A(name)） |

只读 GET（fragment，门控沿用 `VIEW_AUTH`）：`/admin/places`（5.2）、`/admin/places/{id}/drawer`（5.2）、`/admin/places/{id}/merge-candidates`（5.3）。

## 备注

- 六端点全部 htmx：成功 200 + 抽屉 fragment + oob 列表该行 + `HX-Trigger: admin:places-refresh`（筛选栏按当前筛选重拉表格与摘要条）；失败 422 / 404 fragment；无整页跳转。
- Story 5.4 的 `POST /admin/places`（新建，`PLACE_CREATED`）与举报处置端点另行登记。
