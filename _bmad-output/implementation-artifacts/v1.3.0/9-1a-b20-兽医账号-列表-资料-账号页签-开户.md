---
baseline_commit: 4301227a
branch: feat/1.3.0-ops-ui-refactor
epic: 9
story: 9.1a
ad: [AD-9, AD-11, AD-12]
decisions: [D-8, D-11, D-23]
depends_on: [2-3a, 2-3b]
---

# Story 9.1a: B20 兽医账号——列表、资料 / 账号页签、开户

Status: review

> 自包含 story，可本地或云端 L0 执行。与用户沟通用中文。执行纪律见根 `CLAUDE.md`（后端→前端→联调、AC 标 L0/L1/L2、`mvn -B clean package`）。
> 本 story **纯后台页面重构 + 两处路由退役**，零新迁移、零 App 端改动、**写端点零变更**。依赖 Story 2.3a（htmx 响应约定 / `HxRequest` / `AdminBusinessExceptionAdvice`）与 2.3b（`tpl-b-list` 壳、`admin-drawer.js`）已合入。

## Story

As a 运营，
I want 兽医账号页变成「列表 + 右侧抽屉」，在线状态并进列表和抽屉资料页签，开户和改密码封禁都在同一屏完成，
so that 管兽医不用在四个页面之间跳。

## Acceptance Criteria

**AC1 · 列表套模板 B**
**Given** `GET /admin/vets`（`vet.view`）
**When** 打开
**Then** 页面用 `fragments/tpl-b-list.html` 壳：筛选栏（账号状态 / 资质状态 / 在线态 三个下拉 `data-autosubmit` + 关键词 `q` 配显式「查询」钮）→ 摘要条 → 表格 → 抽屉容器 `[L1]`
**And** 摘要条四数字：兽医总数 · 在线数 · 资质待审 · 已封禁，随筛选联动（单条聚合查询）`[L1]`
**And** 表格列：ID ｜ 昵称 ｜ 账号 ｜ 状态 ｜ 资质态 ｜ 在线 ｜ **最后在线（WIB）** ｜ 评分 ｜ 创建日期；每页 20；行点击开抽屉 `[L1]`
**And** 「＋开户」按钮在筛选栏最右，`sec:authorize` 需 `vet.create`（UI 稿 6-1）`[L2]`

**AC2 · 抽屉四页签骨架 + 资料页签**
**Given** 点任一行
**When** `GET /admin/vets/{id}/drawer`（`vet.view`）
**Then** 返回抽屉 fragment，页签 = 资料 ｜ 资质 ｜ 评分 ｜ 账号；资质与评分页签本 story 只放占位（「由 9.1b 填充」），不报错 `[L1]`
**And** 资料页签 = 昵称 / 登录邮箱 / 联系手机 / 头像（上传钮）+ **在线态与最后在线时间**（原 `vet-online` 数据）+ 保存钮；提交 `POST /admin/vets/{id}`、`POST /admin/vets/{id}/avatar`（`vet.edit` 或 `vet.create`），**参数名与现状一致** `[L1]`
**And** htmx 提交：成功 200 返抽屉 fragment + `hx-swap-oob` 刷新该行；失败 422 行内 err；权限不足 403 禁用态 `[L1]`

**AC3 · 账号页签**
**When** 切到「账号」
**Then** 重置密码（新密码 ≥8 位，`POST /admin/vets/{id}/password`，`vet.reset_password`，data-confirm；成功后新密码**仅本次展示**明文 + 复制钮，关闭按钮文案强调「已记录」——UI 稿 6-4/6-7）`[L2]`
**And** 封禁 / 解封（`POST /admin/vets/{id}/status`，`vet.ban`，data-confirm 复述昵称；解封为封禁态下同位置按钮态切换；提示「封禁不影响历史订单」）`[L2]`

**AC4 · 开户**
**When** 点「＋开户」
**Then** 沿用现状模态框（UI 稿 6-3 明确照抄代码实现，不改抽屉）：显示名 / 登录邮箱 / 初始密码 / 联系手机（选填），必填不全禁用提交；`POST /admin/vets`（`vet.create`），`CreateVetForm` 校验不变 `[L1]`
**And** 创建成功后展示初始密码一次（常驻醒目提示 + 复制钮），关闭即不可再查（UI 稿 6-4）`[L2]`

**AC5 · 在线状态并入 + 两处退役（D-11 / D-23）**
**Then** 删除 `templates/admin/vet-online.html`、`vet-edit.html`；删除 `GET /admin/vets/online`、`GET /admin/vets/{id}/edit` 两个路由方法（`AdminWebController.vetOnline` / `editVetForm`）；旧地址 404，**不做跳转** `[L1]`
**And** `layout.html`（或 Story 2.2 后的 `fragments/nav.html`）「兽医与问诊」组去掉「在线状态」项；`admin.nav.online` key 可保留不用 `[L1]`
**And** `AdminVetService.onlineSnapshot(...)` 保留，改为给列表「最后在线」列与资料页签取数；`editForm(...)` 改为给抽屉资料页签取数 `[L0]`

**AC6 · 写端点零变更**
**Then** `POST /admin/vets`、`/admin/vets/{id}`、`/{id}/avatar`、`/{id}/password`、`/{id}/status` 路径 / 参数 / `@PreAuthorize` 与基线一致；写操作清单 diff 中本 story 无写端点增删 `[L0]`

**AC7 · 三语 + 测试 + 回归**
**Then** 新增 key 前缀 `admin.v130.vets.*`，zh/en/id 三包同批；切 ID 无截断 `[L0/L2]`
**And** MockMvc 四条（整页 200 / HX fragment / 422 / 403）+ 抽屉 fragment 测试；既有 `AdminVetCreateIntegrationTest` / `AdminVetEditIntegrationTest` / `AdminVetBanIntegrationTest` 全绿（改表单来源为抽屉，断言不变）`[L1]`

---

## Tasks / Subtasks

- [x] **T1 · 列表页套壳**（AC1）
  - [x] `templates/admin/vets.html` 重写为 `tpl-b-list` 槽位填充；保留现状 `hx-get="/admin/vets"` 行片段刷新（controller 已按 `HX-Request` 返 `admin/vets :: rows`）
  - [x] `AdminWebController.vets(...)` 增 `summary` model（`AdminVetService.summary(filter)` 新方法，一条聚合 SQL：总数 / 在线 / 资质 PENDING / 已封禁）
  - [x] `VetAdminView` 增 `lastSeenAt`（来自 `onlineSnapshot`，WIB 格式化走 `AdminTime`）；`list()` 合并 presence 快照一次取数，不 N+1
  - [x] 表格列头排序只对已有排序参数的列（现状无 → 不加）

- [x] **T2 · 抽屉 fragment**（AC2 / AC3）
  - [x] 新增 `templates/admin/fragments/drawer-vets.html`：四页签壳，资料 / 账号两页签实装，资质 / 评分占位 `<div th:fragment="qual-placeholder">`
  - [x] `AdminWebController` 新增 `GET /admin/vets/{id}/drawer`（`vet.view`）返 `fragments/drawer-vets :: drawer`，model = `adminVetService.view(id)` + `editForm(id)` + presence
  - [x] 现有 `updateVet` / `uploadVetAvatar` / `resetVetPassword` / `setVetStatus` 四个 POST：`HxRequest` 为真时返抽屉 fragment + oob 行（`admin/vets :: row(vet)`），否则维持 PRG（AD-9）
  - [x] 重置密码成功的「仅本次可见」明文：放 model 一次性属性，随 fragment 渲染；**绝不落日志 / 审计明文**（既有约束）

- [x] **T3 · 开户模态框**（AC4）
  - [x] 保留 `vets.html` 内现有 `createVetForm` 模态框与 PRG 重渲染逻辑；只套新视觉（UI 稿 6-3/6-4），加复制钮与「已记录」关闭文案

- [x] **T4 · 退役**（AC5）
  - [x] `git rm` `vet-online.html`、`vet-edit.html`；删 `vetOnline()`、`editVetForm()` 两方法
  - [x] 导航去掉「在线状态」；`grep -rn "vets/online\|vets/{id}/edit\|/edit'" templates/admin` 为零
  - [x] `AdminWebControllerTest`（如有）中针对两路由的用例改为断言 404

- [x] **T5 · 三语与测试**（AC7）
  - [x] `admin.v130.vets.summary.*`、`admin.v130.vets.drawer.tab.*`、`admin.v130.vets.lastSeen`、`admin.v130.vets.password.copied` 等三包同批；跑 `scripts/ci/check-i18n-keys.sh`
  - [x] 新增 `AdminVetDrawerIntegrationTest`：抽屉 200 / 无 `vet.view` 403 / 资料保存 422（邮箱格式错）/ HX 头返 fragment 不含 `<html>`

- [x] **T6 · 云端执行须知**
  - [x] 云端只跑 L0（`mvn -B clean package`，模板渲染单测）；L1 集成测试与 L2 模拟器视觉留本地；Completion Notes 标注

---

## Dev Notes

### 现状代码要点（读过的文件）

| 文件 | 现状 | 本 story 改什么 | 必须保留 |
|---|---|---|---|
| `admin/web/AdminWebController.java` 191-364 | `vets()` 四个筛选参数 + `HX-Request` 返 `:: rows`；`createVet` / `editVetForm`（GET edit）/ `uploadVetAvatar` / `updateVet` / `resetVetPassword` / `setVetStatus` / `vetOnline`（GET online，`:: results`）/ `vetRatings` | + `drawer` GET；四个 POST 加 HX 分支；删 `editVetForm` / `vetOnline` | 五个写端点路径、参数、`@PreAuthorize` 表达式逐字不变；`vetRatings` 留给 9.1b |
| `admin/service/AdminVetService.java` | `list(VetListFilter)`、`create`、`updateProfile`、`updateAvatar`、`resetPassword`、`setBanned`、`onlineSnapshot(Instant)`、`view`、`editForm`、`ratings`、`unratedConsults`、`qualificationExpiryStats` | + `summary(filter)`；`list` 合并 lastSeen | 全部业务方法与审计调用 |
| `admin/service/VetAdminView.java` | record(id, username, displayName, status, …, qualStatus, presence, ratingAvg) | + `lastSeenAt` | 既有字段顺序（模板引用） |
| `admin/dto/CreateVetForm.java` | displayName ≤64 必填 / username 邮箱格式 ≤64 / contactPhone 选填 / password | 不动 | 全部校验注解 |
| `templates/admin/vets.html`（179 行） | 筛选 + 表格 + 开户模态框 + 行内改密 / 封禁表单；`data-confirm="#{admin.vets.ban.confirm}"` | 套 `tpl-b-list`；行内操作移入抽屉账号页签 | i18n key `admin.vets.*` 继续用；模态框 |
| `templates/admin/vet-edit.html`（47 行） | 昵称 / 邮箱 / 手机 / 头像上传独立页 | **删除**，内容进抽屉资料页签 | — |
| `templates/admin/vet-online.html`（57 行） | 在线快照表（ID / 名 / 状态 / 最后在线 / 查询时间 WIB）+ 刷新 | **删除**，数据进列表列与资料页签 | `onlineSnapshot` 服务方法 |
| `templates/admin/layout.html` 91-108 | 兽医组四项：vets / online / failed-requests / ratings | 去 online（ratings 由 9.1b 去） | 其余项与 `sec:authorize` 表达式 |

### 🔴 UI 稿把开户定成「模态框」而不是抽屉

UI 稿 6-3 caption 明写「这里用模态框而不是抽屉，是照抄代码原实现（vets.html 弹窗，校验失败/成功都重渲染本页并重开该弹窗）」。**不要**为了统一把开户搬进抽屉；只套视觉。

### 🔴 「最后在线」列数据来源

现状列表没有这一列，`vet-online.html` 用 `AdminVetService.onlineSnapshot(now)` 单独查。并入后要在 `list()` 里一次性把快照 join 进来（Map<vetId, lastSeen>），**禁止逐行查 Redis / 表**。在线态的口径不变：兽医手动选的持久态，无 TTL 兜底（memory：vet-presence-explicit-only），不要顺手加心跳判断。

### 🔴 密码明文只此一屏

`create` 与 `resetPassword` 的明文只在响应渲染一次；现状代码已保证不进审计 detail 与日志（UI 稿 6-4「后端不存明文、这一屏是唯一获取窗口」）。抽屉化后同样只放一次性 model 属性，不要放 session flash 之外的任何持久位置。

### 页面归组与顺序（AD-12 / D-8）

本页属「兽医与问诊」泳道（UI 稿泳道 6，4 帧 ≈ 3 个路由页面），不受电商线合入门控；可在 Epic 2 完成后立即开工。

### 与 9.1b 的边界

- 本 story：列表 + 抽屉壳 + 资料 / 账号页签 + 开户 + 退役 `vet-edit` / `vet-online`。
- 9.1b：资质页签、评分页签、退役 `vet-qualification` / `vet-ratings` / `ratings` 与导航「评分查看」。
- 抽屉壳的页签容器 id 前缀统一 `#vets-drawer-*`（AD-11 命名规则），9.1b 只填内容不改壳。

### 测试标准

- 沿用 `src/test/java/com/tailtopia/admin/vet/AdminVet{Create,Edit,Ban}IntegrationTest`（`ApiIntegrationTest` 真库）；表单端点不变故断言基本不动，只把 GET edit 的用例换成 GET drawer。
- 新增 fragment 断言：响应体不含 `<html`、含 `id="vets-drawer"`。
- L1 跑法：flush Redis DB0 → 重建 scratch 库 → `DB_NAME` env。

### Project Structure Notes

- 改动全在既有 `admin/web/AdminWebController`、`admin/service/AdminVetService`、`templates/admin/`；新增文件只有 `fragments/drawer-vets.html` 与一个测试类。
- 不新建包；不动 `vetqual/**`、`rating/**`（9.1b）。

### References

- [Source: _bmad-output/planning-artifacts/v1.3.0/epics-v1.3.0-admin.md#Story 9.1a]
- [Source: _bmad-output/planning-artifacts/v1.3.0/后台重构逐页规格.md#B20 · 兽医账号 / 模板 B 通用规格 / C5]
- [Source: _bmad-output/planning-artifacts/v1.3.0/ui-v1.3.0-admin.html 泳道 6 帧 6-1 ～ 6-4、6-7]
- [Source: _bmad-output/planning-artifacts/v1.3.0/architecture-v1.3.0-admin-delta.md#AD-9 AD-11 AD-12]
- [Source: _bmad-output/planning-artifacts/v1.3.0/决策日志-admin.md D-11 / D-23]
- [Source: petgo-backend/src/main/java/com/tailtopia/admin/web/AdminWebController.java#L191-364]
- [Source: petgo-backend/src/main/resources/templates/admin/{vets,vet-edit,vet-online,layout}.html]

## Dev Agent Record

### Agent Model Used

云端 headless session（claude.ai/code），**L0 only**。

### Debug Log References

- L0：`cd petgo-backend && ./mvnw -B clean package` → `Tests run: 1835, Failures: 0, Errors: 0, Skipped: 6` / BUILD SUCCESS
- `bash scripts/ci/check-i18n-keys.sh` → OK（2856 个 key，四包集合相等）
- `bash scripts/ci/check-flyway-versions.sh origin/main` → OK（本 story **无新增迁移**）
- `bash scripts/ci/list-admin-write-ops.sh` → 149 写端点（grep 自检 149）；顺手修掉一处会污染 11.1 底账的备注（见下）
- 云端无 Docker（跑不了 L1）：另写离线 Thymeleaf 渲染探针（`VetRender`），把列表 / 抽屉 / 整页
  真渲染 20 组，**按「有处置权」/「只读」各跑一遍**，全绿。

### Completion Notes List

**L1 / L2 待本地验收**（云端 headless，无 Docker、无渲染）：本 story 全部 `[L1]` / `[L2]` 项，
尤其新增的 `AdminVetDrawerIntegrationTest`（13 条）、既有三个兽医集成测试的回归，
以及**头像 multipart 上传**、**页签切换**、**「仅本次可见」明文的真实交互**。

**AC 落地**

- AC1：套 `tpl-b-list`（`res='vet'` → `#vet-drawer`）；筛选栏三个下拉 `data-autosubmit` + 关键词配
  显式「查询」钮 + 「清空筛选」+ 最右「＋开户」；摘要条四格（总数 / 在线（含忙碌）/ 资质待审 / 已封禁），
  与表格**同一份 rows**、随筛选联动；表格九列（含**最后在线 WIB**）；**每页 20** + 分页控件
  （翻页链接带齐五个筛选参数）；点行开抽屉。
- AC2：抽屉四页签（资料 ｜ 资质 ｜ 评分 ｜ 账号），资质 / 评分留占位并写明「由后续 story 填充」；
  资料页签 = 昵称 / 邮箱 / 手机 / 头像 + **在线态与最后在线**（原 `vet-online` 的数据）；
  htmx 提交成功回抽屉 + 列表按当前筛选重拉，失败 **422** 连着表单回显，无权限为禁用态。
- AC3：账号页签 = 重置密码（≥8 位、`data-confirm` 复述昵称、成功后明文**仅本次可见** + 复制钮 +
  「我已记录」关闭钮）+ 封禁 / 解封（同位置按钮态切换、`data-confirm` 复述昵称、
  「不影响历史订单与结算」提示）。
- AC4：开户仍是**模态框**（UI 稿 6-3 明写照抄原实现），三个必填项加 `required`；
  成功后初始密码常驻醒目提示 + 复制钮 + 「我已记录」。
- AC5：`templates/admin/vet-online.html`、`vet-edit.html` 与 `vetOnline()` / `editVetForm()` 全部删除，
  旧地址 404、**不做跳转**；侧栏「在线状态」项删除；`onlineSnapshot(...)` 与 `editForm(...)` 保留改作它用。
- AC6：五个写端点的路径 / 参数名 / `@PreAuthorize` 逐字未动，只加 htmx 分支；新增仅一个 drawer GET。
- AC7：`admin.v130.vets.*` 三语同批；MockMvc 覆盖整页 200 / HX fragment / 422 / 403 四条 + 抽屉 fragment。

**复审（对抗式）修掉的 14 条**

高（P1）：
1. 🔴🔴 **重置密码成功后，明文落进一个 `hidden` 的页签里** —— 处置成功一律整体重渲染抽屉，
   而页签的 on 态与 panel 的 `hidden` 原来是**写死**的：界面跳回「资料」，明文在 DOM 里却看不见。
   运营只看到一条 toast，而后端不存明文、这一屏是唯一窗口 —— 等于**把密码弄丢了**。
   改为页签状态由服务端 `activeTab` 带回（改密 / 封禁停在「账号」，改资料 / 换头像停在「资料」），
   并补上钉住「账号页签必须是当前页签且 panel 不是 hidden」的断言 —— 原断言只查
   `contains(明文)`，正是它让这个缺陷漏过测试网。
2. 🔴🔴 **开户成功后的「复制」钮是个死钮**：`data-copy-target` 全后台没有处理器，
   唯一的实现写在抽屉片段的内联脚本里，而开户走的是**整页 PRG**，那段脚本根本不在页面上。
   改为 `admin-core.js` 全局委托（并去掉抽屉里那份重复实现），
   顺带处理 `navigator.clipboard` 在非 https / 旧浏览器不存在的情况（退回 execCommand → 选中文本），
   绝不静默什么都不做。

中（P2）：
3. 🟠 **抽屉处置成功后摘要条被换成全库数字**：那几个 POST 身上没有筛选参数，服务端只能按
   `VetListFilter.none()` 算 —— 筛「ACTIVE」时摘要条会跳成全库的总数与已封禁数，而表格还是筛选后的。
   跨页深链时更明显：行 oob 找不到 `#vet-row-<id>` 被静默丢弃，**只有摘要条跳变**。
   改为发 `admin:vet-list-refresh`，由页面上的刷新槽 `hx-include="#vet-filters-form"` 带着
   **当前筛选表单**整表重拉（读的是请求那一刻的表单值，不是烤进 URL 的旧串）；
   顺带省掉「为了拿一行数据把全量兽医重新装配一遍」的放大版 N+1。
4. 🟠 **资料校验失败回 200 而不是 422**（AC2 / AC7 都写了 422）：全站约定是「4xx 出 fragment」，
   回 200 会让任何按状态码判成败的监控与自动化把校验失败记成成功。已改 422，测试同步。
5. 🟠 **AC1 的「每页 20」完全没实现**（基线也没有）：已补分页（摘要条仍按**整个筛选集**统计，
   不随翻页变 —— 一个只统计本页的「在线数」会被抄进周报）。
6. 🟠 **Dev Notes 🔴「禁止逐行查 Redis」只做了一半**：`lastSeenAll()` 把「最后在线」变成一次
   ZRANGE，但**在线态本身**还是每行 2 次往返（`statusOf` = ZSCORE + SISMEMBER）。
   而在线集合的 key 集合就是「谁在线」—— 补 `busyAll()`（一次 SMEMBERS）后，
   整张列表的在线态从 2N 次降到**两次**。口径与 `statusOf` 逐字一致。

低（P3）：
7. 🟡 AC3 的「关闭按钮文案强调『已记录』」未落地 → 补 `data-dismiss-secret` 钮，文案「我已记录」
   （中性的「关闭」会被顺手点掉，而这一步不可逆）。
8. 🟡 AC4 的「必填不全禁用提交」未落地 → 三个必填项补 `required` / `minlength`。
9. 🟡 AC1 的「＋开户在筛选栏最右」位置不符（原在标题行）→ 已挪进筛选栏。
10. 🟡 `view(id)` 每开一次抽屉查两遍（第二次只为取 `avatarUrl`）→ 合成一次。
11. 🟡 **i18n 孤儿键 23 个 × 4 包**（`admin.online.*` 九个、`admin.vets.{back,edit,edit.title,avatar,
    avatar.none,refresh,ban.confirm,col.edit,col.resetPwd,col.ban,username,view}`、`admin.nav.online`）
    随两页退役失去引用，已删；另把新加的 `admin.v130.vets.avatar.upload` 撤掉，改用**既有同义**的
    `admin.vets.avatar.upload`（不凭空多一份要维护的三语文案）。
12. 🟡 **两处假绿断言**：① 明文块只断言 `contains`（见第 1 条）；② 深链用例只断言 `is3xxRedirection()`
    而不验 Location —— 把落点改成 `/admin/vets`（深链彻底失效）它照样绿，而方法名声称验的就是 `?open=`。
    两条都已加强；另补头像上传三条失败路径（非图片 / >5MB）的 422 断言。
13. 🟡 `ud-avatar` 全站无 CSS 规则 → 补上（头像与上传表单同一行、窄屏换行）。
14. 🟡 **写操作清单会给出自相矛盾的口径**：`retired_note` 把 `/admin/vets/{id}` 与
    `/admin/vets/{id}/edit` 一起标成「本版退役」，而 **`POST /admin/vets/{id}` 仍在服役**
    （AC6 要求它逐字不变）。已拆开，只标 `/edit` 与 `/online`。

**有意的偏离 / 待拍板**

- ⚠️ **非 htmx 的资料保存失败走 PRG 回 `?open=`，字段级错误回显不了**：整页 `vet-edit` 已删，
  抽屉内容是 htmx 拉的，而这一跳没有 htmx。这是无 JS 浏览器的降级路径，正常路径（有 JS）
  错误连着表单一起回显。**留待拍板：接受这个降级，还是给非 htmx 单独保留一份错误回显页。**
- ⚠️ **资质徽标不再是链接**（原来点它去 `/admin/vets/{id}/qualification`）：链接会与「点行开抽屉」
  抢同一次点击。资质页签由 9.1b 填充，在此之前**从这一页进资质审核的入口是断的** ——
  该页路由本身仍在，可直接访问。**留待拍板：9.1b 之前是否需要临时保留一个不与行点击冲突的入口。**
- ⚠️ 「最后在线」离线兽医显示「—」：含义是「当前不在线」而不是「数据缺失」（下线即移出在线集合，
  不记历史）。在线态是兽医手动选的持久态、**无 TTL 兜底**，这里没有顺手加心跳判断。

**架构护栏自查**：无新迁移、无新依赖、无新中间件；`ddl-auto=validate` 未动；
密码明文只进一次性 model 属性（审计 summary 与日志逐条核过，无明文；`/admin/**` 的响应体不进访问日志）；
日志无 PII / 健康数据 / 令牌。

### File List

**改**
- `petgo-backend/src/main/java/com/tailtopia/admin/web/AdminWebController.java`
- `petgo-backend/src/main/java/com/tailtopia/admin/service/AdminVetService.java`
- `petgo-backend/src/main/java/com/tailtopia/admin/service/VetAdminView.java`
- `petgo-backend/src/main/java/com/tailtopia/admin/shared/AdminPageCatalog.java`
- `petgo-backend/src/main/java/com/tailtopia/admin/shared/web/AdminHxEvents.java`
- `petgo-backend/src/main/java/com/tailtopia/vet/service/VetPresenceService.java`
- `petgo-backend/src/main/resources/templates/admin/vets.html`
- `petgo-backend/src/main/resources/static/admin/admin-core.js`（一次性密钥的复制 / 已记录全局委托）
- `petgo-backend/src/main/resources/static/admin/admin.css`（`.ud-avatar`）
- `petgo-backend/src/main/resources/i18n/messages{,_zh_CN,_en,_id}.properties`
- `petgo-backend/src/test/java/com/tailtopia/admin/service/AdminVetServiceListTest.java`
- `petgo-backend/src/test/java/com/tailtopia/admin/web/AdminVetsAccessControlTest.java`
- `petgo-backend/src/test/java/com/tailtopia/admin/web/{AdminPagesRenderSmokeTest,AdminNavSectionOpenTest}.java`
- `petgo-backend/src/test/java/com/tailtopia/admin/shared/AdminPageCatalogTest.java`
- `petgo-backend/src/test/java/com/tailtopia/admin/shared/nav/AdminNavModelTest.java`
- `scripts/ci/list-admin-write-ops.sh`（退役备注只标 `/edit` 与 `/online`）

**新增**
- `petgo-backend/src/main/java/com/tailtopia/admin/dto/VetSummary.java`
- `petgo-backend/src/main/resources/templates/admin/fragments/vets-list.html`
- `petgo-backend/src/main/resources/templates/admin/fragments/drawer-vets.html`
- `petgo-backend/src/test/java/com/tailtopia/admin/vet/AdminVetDrawerIntegrationTest.java`（L1，13 条）

**删除**
- `petgo-backend/src/main/resources/templates/admin/vet-online.html`
- `petgo-backend/src/main/resources/templates/admin/vet-edit.html`
