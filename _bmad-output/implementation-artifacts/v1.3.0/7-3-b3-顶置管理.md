---
baseline_commit: 4301227a
branch: feat/1.3.0-ops-ui-refactor
epic: 7
story: 7.3
ad: [AD-9, AD-11]
decisions: [D-8]
page: B3
depends_on: [2-3a, 2-3b]
---

# Story 7.3: B3 顶置管理

Status: review

> 自包含 story。与用户沟通用中文。纪律见根 `CLAUDE.md`。依赖 2.3a / 2.3b。端点零变更：新建 / 改时间 / 终止三个 POST 路径与参数不动，只把常驻页尾的新建表单收进抽屉。

## Story

As a 运营，
I want 顶置坑位列表有摘要条，新建 / 改时间 / 终止都在抽屉里完成，
so that 换坑位不用在长页面上下翻。

## Acceptance Criteria

**AC1 · 壳与摘要条**
**Given** `/admin/content-pins` 套 `tpl-b-list`
**Then** 筛选：坑位 `slot`（现状 data-autosubmit 保留）· 状态（生效中 / 待生效 / 已结束）；摘要条：生效中 · 待生效 · 已结束（随筛选联动，单条聚合）`[L1]`

**AC2 · 表格**
**Then** 列 = 类型（顶置内容 / 推广卡片）｜ 内容 / 标题 ｜ 生效时间（**WIB 标注**，起止）｜ 状态 ｜ 操作；默认生效时间倒序；每页 20 `[L2]`

**AC3 · 详情抽屉**
**When** 点行 → `GET /admin/content-pins/{id}/drawer`
**Then** 坑位内容预览（顶置 = 帖子快照：首图 + 正文摘要 + 作者；推广卡 = 卡面预览：图 + 标题 + 跳转链接）+ 时间设置（起止 WIB，行内可编辑）+ 操作条 `[L2]`

**AC4 · 新建走抽屉表单**
**When** 右上「＋新建」→ 抽屉表单
**Then** 字段 = 坑位 `slot` · 类型二选 `objectType`（CONTENT / PROMO；切到 PROMO 表单替换为 `promoImageFile` 或 `promoImageUrl` + `promoTitle` + `promoLinkUrl`）· 内容选择器（`GET /admin/content-pins/pick`，搜索 + 分页 + 筛选，**大结果集 1.2.0 既有要求保留**，keyup 300ms 防抖）· `startsAt` / `endsAt`（WIB）→ `POST /admin/content-pins`（参数名逐字不变）`[L1]`
**And** 现状页尾「新建顶置」常驻表单删除 `[L2]`

**AC5 · 操作（端点零变更）**
| 按钮 | 端点 | 权限 | 确认 |
|---|---|---|---|
| 新建 | `POST /admin/content-pins` | `content.pin_manage`（`AdminPermissions.CONTENT_PIN_MANAGE`） | 无 |
| 改时间 | `POST /admin/content-pins/{id}/edit` | 同上 | 无 |
| 终止 | `POST /admin/content-pins/{id}/terminate` | 同上 | data-confirm（`admin.pins.terminateConfirm` 沿用） |
**Then** htmx：200 + 抽屉 + oob 行 + 摘要条；422 行内 err `[L1]`
**And** 查看权限 `content.pin_view` 无 manage 时操作条禁用注明 `[L1]`

**AC6 · 边界**
**Then** 同一坑位同时段唯一：冲突时 422 行内报错并**显示冲突项**（id + 起止），服务层校验现状沿用 `[L1]`
**And** 已结束坑位抽屉只读 `[L2]`

**AC7 · 三语与回归**
**Then** `admin.v130.pins.*` 三包；`AdminContentPinIntegrationTest` 全绿；四条 MockMvc `[L0/L1]`

---

## Tasks / Subtasks
- [x] **T1** 摘要条聚合查询（生效中 / 待生效 / 已结束按 WIB 当前时刻分类）+ 状态筛选参数（GET 增强）
- [x] **T2** `content-pins.html` 套模板 B；删页尾新建表单；「＋新建」触发 `GET /admin/content-pins/new/drawer`
- [x] **T3** `fragments/drawer-content-pin.html`：详情态 / 新建态两种渲染；PROMO/CONTENT 字段切换用现有 `#pinObjectType` JS 逻辑迁入 `admin-drawer.js` 钩子（不重写）
- [x] **T4** 三个 POST 增加 `HxRequest` 分支；冲突异常 → 422 fragment 带冲突项
- [x] **T5** 三语 + 测试；**T6** 云端 L0，抽屉 L2 留本地

## Dev Notes

### 现状与改动边界
| 文件 | 现状 | 本 story | 保留 |
|---|---|---|---|
| `admin/pin/web/AdminContentPinController.java` | `GET /admin/content-pins`（VIEW）、`GET /pick`（VIEW）、`POST /admin/content-pins`（MANAGE，参数 slot/objectType/contentId/promoImageUrl/promoImageFile/promoTitle/promoLinkUrl/startsAt/endsAt）、`POST {id}/edit`、`POST {id}/terminate` | + `GET {id}/drawer`、`GET new/drawer`；POST 加 htmx 分支 | 全部参数名 |
| `templates/admin/content-pins.html` | 筛选（slot）+ 表格 + 行内改时间/终止表单 + 页尾新建表单（含 pick 候选 htmx） | 套模板 B；表单进抽屉 | `pick` 交互与防抖 |

### 🔴 内容选择器
现状 `hx-get=/admin/content-pins/pick` + `hx-trigger="keyup changed delay:300ms, search"` + `load` 首屏候选。抽屉内照搬，目标改 `#pinCandidates` 在抽屉内的 id；大结果集分页保留（1.2.0 既有界面要求）。

### 🔴 推广卡片字段
UI 稿 2-12 注记「类型切到推广卡片后表单替换为推广标题 / 图 / 跳转」，与现状 `promo*` 参数一一对应。**不新增字段**。

### 端点零变更清单
`POST /admin/content-pins` · `POST /admin/content-pins/{id}/edit` · `POST /admin/content-pins/{id}/terminate`。新增 GET：`{id}/drawer`、`new/drawer`。

### 测试标准 / 三语 / Project Structure
沿用 `AdminContentPinIntegrationTest`；冲突 422 fragment 断言含冲突项 id。key `admin.v130.pins.*`。改动落 `admin/pin/**`、`templates/admin/content-pins.html`、`fragments/drawer-content-pin.html`。

### References
- [Source: 后台重构逐页规格.md#B3 · 顶置管理]
- [Source: ui-v1.3.0-admin.html 泳道 2 帧 2-11～2-13]
- [Source: architecture-v1.3.0-admin-delta.md#AD-9 / AD-11]
- [Source: epics-v1.3.0-admin.md#Story 7.3]

## Dev Agent Record

### Agent Model Used

claude-opus-5（云端 headless session，2026-09-10）

### Debug Log References

- 云端 L0：`./mvnw -B clean package`（排除需真库的 Spring 上下文测试类）→ BUILD SUCCESS，1831 tests，0 failures；`AdminTemplateStructureTest`（含 6.5 的两条属性优先级守卫）、`AdminTemplateShellRenderTest`、`AdminMessagesExternalizedTest` / `AdminMessagesParityTest`、`AdminPageCatalogTest` 全绿。**无迁移**；写端点零新增。
- `check-i18n-keys.sh` OK（四包 2720 key 集合相等、无重复；+7 `admin.v130.pins.{filter.status.all, pager.page, drawer.title, preview.gone, preview.openContent, preview.noLink, endedReadonly}`；`admin.err.pins.overlap` 四包改词——加 `{0}` 带出冲突项）。
- 复审（bmad-code-review）：**CONFIRMED 4 条全修 + PLAUSIBLE 5 条已修 + 1 条留作记录**。
  - ① 🔴🛡 **提前结束一条「待生效」排期后界面什么都不变**：`ScheduleWindow` 的第一判据是 `now < startsAt` → 仍是 PENDING，于是 `editable()` 仍为 true —— 运营点完「提前结束」，状态还写「待生效」、按钮还在，再点一次才得到「该排期已结束，无需再操作」。这是 AC6「已结束坑位抽屉只读」的真实缺口，不只是测试问题。改：`PinRow` 新增 `ended()`（`phase == ENDED || terminatedAt != null`），`editable()`、行 / 抽屉的状态徽标、摘要条三格、状态筛选**一律改用它**。
  - ② 🔴 新 L1 测试的只读身份漏了 `ROLE_ADMIN`：`/admin/**` 在 URL 层就要求它，少了它列表会在方法门控之前 403，而「`new/drawer` 应 403」那条也会因为同一个原因**假绿**——`@PreAuthorize(MANAGE)` 一次都没被验到。改：补 `ROLE_ADMIN`（兄弟类的 helper 早写了这条注释）。
  - ③ 🔴 新测试没清库、日期区间还与兄弟类正面撞车：测试库共享且不回滚，上一轮残留的排期会撞重叠校验；更糟的是 `createPin` 走整页路径，而整页把重叠**吞成 flash + 302**，`.andExpect(302)` 照样通过，再用 `max(id)` 认领结果就会拿到别人的 pin，后面的断言全指向错误对象。改：`@BeforeEach` 清空排期、按 `contentId` 精确捞（捞不到当场红并说明原因）、日期挪到 +200 起。
  - ④ 🔴 `page * PAGE_SIZE` 在 int 域溢出（`?page=107374183` 起）→ 负下标进 `subList` → 500，而方法注释自己写着「手改 URL 不该出 500」。改：先把页码钳到 `[0, lastPage]` 再算 offset（顺带这就是页码越界回退）。
  - ⑤ 只读权限下操作条是**整块空白**（AC5 要的是「禁用注明」）→ 渲染 disabled 按钮并挂「需要「编辑顶置配置」权限」。
  - ⑥ 原生日期弹层在抽屉里会被裁：`admin.css` 的兜底是给 `main` 加底部留白，而抽屉是 `position: fixed` —— 对它毫无作用。改：给 `.drawer-body:has(input[type="datetime-local"])` 补等价留白。
  - ⑦ 一次列表请求把全表读了两遍（`page()` 与 `summary()` 各调一次 `list()`，还各自取 `Instant.now()`，跨秒时理论上能给出不一致的状态）→ 控制器取一次 now、读一次表，喂给两个纯函数。
  - ⑧ 新建表单的 err 槽在表单顶部而提交钮在很长的表单底部（类型 + 候选列表 + 推广字段 + 时间），422 落在 480px 抽屉视口外 → 槽挪到提交钮正上方。
  - ⑨ `newDrawer` 的非 htmx 重定向直接拼了未编码的 `slot` → `URLEncoder`。
  - ⑩ 既有测试 `pageLabelsTheTimezoneNextToTheTimeInputs` 已经空转：页面上不再有任何时间输入框，它只是撞到副标题里的「WIB（雅加达）」而恒绿 → 改成断言**新建表单抽屉**里 `datetime-local` 旁有 WIB。
  - **留作记录不改**：`row(r, oob)` 的 `oob` 形参目前恒为 false（处置成功走整表重拉，新建的行本来就不在页面上、oob 插不进去），保留是为了与其它模板 B 列表同形，注释已改成如实说明。

### Completion Notes List

- **L1/L2 待本地验收**：① 新增 `AdminContentPinDrawerIntegrationTest`（真库 + 真 Thymeleaf）：整页模板 B 壳 + 摘要条 + `#pins-rows` + `#pin-drawer`、坑位 / 状态两个筛选、行 `data-drawer-url`、「＋新建」是抽屉入口、**行内处置表单与页尾常驻新建表单都已消失**、时间旁仍有 WIB；状态筛选 + htmx 片段带摘要条 oob；抽屉预览（帖子首图 / 正文 / 作者）+ 时间设置 + 操作条 + `#pin-drawer-err`，非 htmx 直达 302 `?open=`；改时间（htmx）落库且回抽屉 oob + toast + `HX-Trigger: admin:pin-list-refresh`；提前结束后抽屉切只读；新建表单端点回的是含 `pinCreateForm` / `pinObjectType` / `pinCandidates` 的片段，htmx 新建成功 → 200 + 列表刷新事件；**重叠冲突 422 且报错里带得出冲突项（id + 起止）**且不落库；只持 `content.pin_view` 能看列表与抽屉、拿不到任何写入口、新建表单端点 403。② 既有回归：`AdminContentPinIntegrationTest` 全绿（整页 PRG、重叠拦截、首尾相接不算重叠、pick 搜索与分页、WIB 折算、推广卡片必填与本地上传、双权限码）。③ L2：本地 stag 对照 UI 稿 2-11 ～ 2-13：480px 抽屉预览与时间设置、新建抽屉里的类型切换与比例预警、内容选择器 300ms 防抖与分页、`?create=1` / `?open=` 深链、行高与状态色点。
- **AC1 壳与摘要条**：`content-pins.html` 套 `tpl-b-list`（res=pin）；筛选 = 坑位（沿用 `data-autosubmit`）+ 状态（生效中 / 待生效 / 已结束）；摘要条三格随筛选联动。
- **AC2 表格**：列 = 类型 / 内容·标题（含「内容失效未生效」徽标）/ 生效时间（**WIB**，起止 + 提前结束时刻）/ 状态（色点 + 徽标）；生效时间倒序、每页 20、底部分页。「操作」列整列删除（处置全在抽屉）。
- **AC3 详情抽屉**：`GET /admin/content-pins/{id}/drawer`（`VIEW`）→ `fragments/drawer-content-pin :: drawer`：坑位内容预览（顶置 = 首图 + 正文摘要 + 作者 + 「去内容管理」深链；推广 = 卡面图 + 标题 + 跳转目标）+ 时间设置（行内 `datetime-local`，旁边标 WIB）+ 操作条。**已结束只读**（`PinRow.editable()` 沿用）。
- **AC4 新建走抽屉**：右上「＋新建顶置」→ `GET /admin/content-pins/new/drawer`（`MANAGE`）→ `:: createForm`：坑位 / 类型二选 / 内容选择器（`GET /pick`，搜索 `keyup changed delay:300ms` + 首屏候选 + 分页，**逐字保留**）/ 推广卡片三字段 + 本地上传 + 比例预警 / 起止时间；**页尾常驻表单删除**。类型切换与比例预警脚本原样搬进抽屉片段（htmx 会执行 swap 进来的 `<script>`，每次开抽屉重新绑定；i18n 仍走 `th:inline`）。
- **AC5 操作**：三个 POST 路径与参数逐字不变，只加 htmx 分支 → `:: afterAction`（抽屉体 oob + toast）+ `HX-Trigger: admin:pin-list-refresh`。⚠️ 新建成功**不用 oob 插行** —— 新行在页面上还不存在，oob 会被直接丢掉；靠列表整表重拉让它出现（摘要条与状态筛选也要跟着变）。失败交给 `AdminBusinessExceptionAdvice` 出 422 行内 err，目标是**带 id 的** `#pin-drawer-err`（7.1 复审 C1 的教训）。查看权限无 manage 时不渲染写入口并注明所缺权限名。
- **AC6 边界**：「已结束」的判据含**手动提前结束**（见偏差 ⑥）；同坑位重叠仍由 `ContentPinService.requireNoOverlap` 校验（服务层唯一真相），本 story 只把**冲突项写进报错**（`admin.err.pins.overlap` 加 `{0}`：`#id MM-dd HH:mm–HH:mm`，按 WIB 呈现，与列表字面一致）——只说「已有排期」等于让运营回列表自己一条条比时间窗。已结束（含提前结束）的排期抽屉只读。
- **AC7 三语与回归**：7 个 key 四包同批 + 1 条改词。
- **偏差说明**：① **摘要条与状态筛选、分页都在应用层做**，不写 SQL：`phase` 的判定唯一来源是 `ScheduleWindow`（含「提前结束」这种只看 `starts_at/ends_at` 判不出来的情况），用 SQL 再算一遍就有第二份口径；一个坑位的全部排期是低基数数据。AC1 写的「单条聚合」按此偏离，理由记在 `PinSummary` 的类注释里。② 抽屉里的「类型切换 / 比例预警」脚本是**随片段搬迁**（story T3 说「迁入 admin-drawer.js 钩子」）—— 脚本里有三条 `#{...}` 文案，搬进静态 js 就得另造一套传参；放在片段里 htmx 每次 swap 都会重新执行，效果一致且不重写逻辑。③ 列表页码越界（处置掉最后一页最后一条后按原页码重拉）回退到最后一页，并把实际页码带给分页器（与 7.2 同一处理）。④ 新增 `ContentPinService.byId`（只读单条）——admin 侧仍不直连 `ContentPinRepository`。⑤ `createContentPin` / `createPromoPin` 改为返回新 id（抽屉建完要立刻打开它），既有调用方忽略返回值、行为不变。⑥ **「已结束」的判据加了 `terminatedAt != null`**（`PinRow.ended()`）：`ScheduleWindow` 对一条被提前结束的**待生效**排期仍判 PENDING，只看 phase 会让界面写着「待生效」、按钮还在，而它其实已经收手 —— 状态列 / 摘要条 / 状态筛选 / 只读判定四处一并改用 `ended()`；`ScheduleWindow` 本身未动（它是跨模块共享的判定）。
- **端点底账（Epic 11 diff 基线）**：不变 —— `POST /admin/content-pins`、`POST /admin/content-pins/{id}/edit`、`POST /admin/content-pins/{id}/terminate`、`GET /admin/content-pins/pick`；新增只读 `GET /admin/content-pins/{id}/drawer`、`GET /admin/content-pins/new/drawer`；`GET /admin/content-pins` 仅增查询参数（status / page / open / create）。

### File List

- petgo-backend/src/main/resources/templates/admin/content-pins.html（套模板 B；删页尾新建表单；`candidates` 片段原位保留）、fragments/pins-list.html（新增：摘要条 / 表格 / 单行）、fragments/drawer-content-pin.html（新增：详情态 / 新建态 / afterAction）
- petgo-backend/src/main/java/com/tailtopia/admin/pin/web/AdminContentPinController.java（状态筛选 + 分页 + 两个 drawer GET + 三个 POST 的 htmx 分支 + 上传逻辑抽出共用）、pin/service/AdminContentPinService.java（+`page` / `summary` / `detail`；两个 create 返回新 id）、pin/dto/{PinSummary,PinDetail}.java（新增）、content/service/ContentPinService.java（+`byId`；重叠报错带冲突项）
- petgo-backend/src/main/resources/static/admin/admin.css（`.pins-table` / `.pin-*`）；shared/web/AdminHxEvents.java（+`PIN_LIST_REFRESH`）；i18n/messages{,_zh_CN,_en,_id}.properties（+7 key、1 条改词）
- 测试：admin/pin/AdminContentPinDrawerIntegrationTest（新增 L1）
