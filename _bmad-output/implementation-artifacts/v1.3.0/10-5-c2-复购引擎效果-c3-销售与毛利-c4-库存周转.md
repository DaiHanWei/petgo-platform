---
baseline_commit: 4301227a
branch: feat/1.3.0-ops-ui-refactor
epic: 10
story: 10.5
ad: [AD-9, AD-11, AD-12]
decisions: [D-8]
pages: [C2, C3, C4]
ui_frames: [5-13, 5-14, 5-15]
gate: v1.4.0 电商线合入 dev_1.3.0
depends_on: [2-3a, 2-3b]
---

# Story 10.5: C2 复购引擎效果 + C3 销售与毛利 + C4 库存周转（模板 C 只读）

Status: ready-for-dev

> ⛔ **门控（AD-12）**：本 story 属 Epic 10，**必须等 v1.4.0 电商线合入 `dev_1.3.0` 之后再启动**。启动前先执行下方「电商线合入后的重核步骤」。依赖 Story 2-3a（htmx 响应约定 / `admin/shared`）与 2-3b（五套模板壳 / `admin-drawer.js` / `admin-workbench.js`）已合入。
> 自包含 story，可本地或云端 L0 执行。与用户沟通用中文。执行纪律见根 `CLAUDE.md`（后端→前端→联调、AC 标 L0/L1/L2、Flyway 时间戳版本号、`mvn -B clean package`）。本 story **零后端功能改动、零新端点**（AB-19A 原则），只改模板 / Controller 返回形态 / 页面路由退役。

## Story

As a 运营负责人 / 财务，
I want 三张经营报表统一为「图表卡在上、维度切换、明细表在下」的只读报表形态，
so that 看数口径和首页看板一致，不会误以为能在报表页改东西。

## Acceptance Criteria

**AC1 · C2 复购引擎效果**
**Given** `/admin/shop/repurchase-dashboard`（权限 `config.view` **或** `order.view`，与其余两页不同码——如实按现状）
**Then** 区块字段**现状原样**：触发（按类型 / 总数 / 转化 / 关闭 / 转化率 / FR-109 覆盖率）· 归因（补货卡 / 档案推荐 / 全部订单行 + 底账核对表）· 30 日复购率 · 粮量预估准确度（样本 / 平均偏差 / 区间）；数字卡图表化（Chart.js 只在有时间序列的区块使用，其余数字卡）；顶栏只读标识 `[L2]`

**AC2 · C3 销售与毛利**
**Given** `/admin/shop/margin`（`shop.finance_view`）
**Then** 核心卡（销售额 / 成本 / 毛利 / 毛利率 / 退款商品额 / 净额 / 售后成本）· 运费与手续费卡 · 售后成本按退货类型卡 · 按 SKU 下钻明细表；期间筛选 htmx 局部替换；只读标识 `[L2]`
**And** 进货价 / 成本相关数字**只对 `shop.finance_view` 可见**（本页整体门控，不再细分）`[L1]`

**AC3 · C4 库存周转**
**Given** `/admin/shop/inventory-turnover`（`shop.finance_view`）
**Then** 统计窗口（天，默认 30）筛选 + 售罄 SKU 计数卡 + 各 SKU 表（库存 / 库存金额 / 窗口销量 / 最近售出 / **建议动作**列保留）；只读标识 `[L2]`

**AC4 · 三页共性**
**Then** 无 form、无写端点、无导出（现状无，不新增）；三页拆为三帧独立路由（UI 稿 09-04 拆帧），不得合成一页多分区 `[L0]`
**And** 三个 GET 路径与权限不变；`HX-Request` 分支返卡区 fragment `[L1]`

---

## Tasks / Subtasks

- [ ] **T0 · 重核**（见 Dev Notes）
- [ ] **T1 · C2**（AC1）：`shop-repurchase-dashboard.html` 套 `tpl-c-report`；`RepurchaseDashboardService` 数据不动；有日序列的区块用 `admin-charts.js`（Story 3-4 建立的内嵌 JSON 范式）
- [ ] **T2 · C3**（AC2）：`shop-margin.html` 套 `tpl-c-report`；期间切换 fragment
- [ ] **T3 · C4**（AC3）：`shop-inventory-turnover.html` 套 `tpl-c-report`；窗口切换 fragment
- [ ] **T4 · 三语**：`admin.v130.shopReports.*`
- [ ] **T5 · 测试**（AC4）：三页 MockMvc 四条（无 422 场景则以非法期间参数替代）
- [ ] **T-云端 · 云端执行须知**
  - [ ] 云端只跑 L0：`mvn -B clean package`（跳过需 Docker 的 IT）；L1/L2 留本地
  - [ ] Completion Notes 标注「L1/L2 待本地验收」+ 重核步骤的 diff 结果

---

## Dev Notes

### 🔴 电商线合入后的重核步骤（启动本 story 的第一件事）

1. 在 `dev_1.3.0` 上重跑 `scripts/ci/list-admin-write-ops.sh`（Story 2-1 建），得到合入后的商城组写端点清单。
2. 与本 story「现状代码要点」表逐行 diff：**新增的端点 / 权限码 / 模板一律以新清单为准**，补进本 story 的「操作表」并在 Completion Notes 记录；被电商线删除的端点从本 story 移除。
3. 若电商线已改过本 story 涉及的模板（`shop-*.html`），以合入后的模板为改造起点，不要从本 story 写作时的版本覆盖回去。
4. 重核有实质差异时，先更新本 story 文件再开工；不要边写边猜。

### 现状代码要点（读过的文件）

| 文件 | 现状 | 本 story 改什么 | 必须保留 |
|---|---|---|---|
| `admin/shop/web/AdminRepurchaseDashboardController.java` | GET repurchase-dashboard；VIEW=`config.view` or `order.view` | fragment 分支 | 权限码原样（不规整已在 Story 1.5 注脚） |
| `admin/shop/web/AdminShopFinanceController.java` | GET margin / inventory-turnover / reconciliation；`shop.finance_view` | margin / turnover fragment 分支（reconciliation 归 10-2） | 路径 |
| `admin/shop/service/RepurchaseDashboardService.java`、`ShopFinanceDashboardService.java` | 聚合查询 | 不动 | 全部口径 |
| `templates/admin/shop-repurchase-dashboard.html`（121）/ `shop-margin.html`（108）/ `shop-inventory-turnover.html`（75） | 数字表格 | 套模板 C | 全部字段与「建议动作」列 |

### 🔴 三页权限码不一致（如实记录）
复购效果走 `config.view` 或 `order.view`；毛利 / 周转 / 对账走 `shop.finance_view`。本 story 不改码；`AdminPageCatalog` 按现状登记，Story 1.5 矩阵注脚说明。

### 只读 ≠ 无交互
期间 / 窗口切换是读参数变化，走 htmx 替换卡区；不引入任何写操作。

### 测试标准

- 每个改造后的 Controller GET 至少四条 MockMvc（`ApiIntegrationTest` 范式，参考 `test/.../admin/shop/web/*EndpointIntegrationTest.java`）：整页 200 / 带 `HX-Request` 返 fragment / 422 行内 err fragment / 403 禁用态 fragment。
- 写端点回归：既有 `Admin*EndpointIntegrationTest` 全绿，端点路径 / 参数 / `@PreAuthorize` 逐条与「现状代码要点」表一致（Story 11-1 diff 会再核一次）。
- L2：stag 部署后按 UI 稿对应帧逐页比对（Story 11-5），切 ID 语言无截断。
- L1 跑法（本地）：flush Redis DB0 → 重建 scratch 库 → `DB_NAME` env。

### Project Structure Notes
- 新 fragment：`fragments/cards-shop-repurchase.html`、`cards-shop-margin.html`、`cards-shop-turnover.html`。

### References
- [Source: _bmad-output/planning-artifacts/v1.3.0/后台重构逐页规格.md#C2 / #C3 / #C4 / 模板 C]
- [Source: _bmad-output/planning-artifacts/v1.3.0/ui-v1.3.0-admin.html 泳道 5 帧 5-13～5-15]
- [Source: _bmad-output/planning-artifacts/v1.3.0/architecture-v1.3.0-delta.md#AD-9 #AD-11 #AD-12]
- [Source: _bmad-output/planning-artifacts/v1.3.0/PRD-v1.3.0-admin.md#5 ① 商城组行 / #5 ② 模板表]
- [Source: _bmad-output/planning-artifacts/v1.3.0/决策日志.md D-8 / D-23]
- [Source: _bmad-output/planning-artifacts/v1.3.0/epics-v1.3.0.md#Story 10.5]
- [Source: petgo-backend/src/main/java/com/tailtopia/admin/shop/web/AdminShopFinanceController.java]

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
