---
baseline_commit: 4301227a
branch: feat/1.3.0-ops-ui-refactor
epic: 3
story: 3.5
ad: [AD-2, AD-3]
decisions: [D-17, D-26]
depends_on: [3-4]
---

# Story 3.5: 看板权限与验收对照

Status: review

> 自包含 story。与用户沟通用中文。执行纪律见根 `CLAUDE.md`。
> 后端权限门控 + stag 验收脚本；无迁移。

## Story

As a 产品，
I want 付费卡只对有财务查看权的人显示，且看板任一天任一指标能和口径表对上，
so that 数据既不泄露也可信。

## Acceptance Criteria

**AC1 · 付费卡权限（D-17）**
**Given** 登录账号无 `payment.view`（支付记录页 `AdminPaymentController.VIEW_AUTH` 同码）且非 SUPER_ADMIN
**When** 打开首页或请求 `/admin/charts`
**Then** 响应中不含付费卡（fragment 服务端按 `sec:authorize` / `hasAuthority('payment.view')` 决定是否渲染，且 `DashboardQueryService` 不为其取付费四项数据）；其余四卡正常 `[L1]`
**And** 有权限或 SUPER_ADMIN → 五卡齐全 `[L1]`

**AC2 · 页面可达**
**Then** 看板对所有后台账号可进（与现首页一致），不新增 permission_code；`AdminPageCatalog` 中首页维度的查看权限标记为「全员」 `[L1]`

**AC3 · 验收对照**
**Given** stag 已物化
**When** 运行 `scripts/local/verify_dashboard_parity.sh <date1> <date2> <date3>`（对 stag 只读库执行口径表 SQL）
**Then** 输出 3 日 × 18 项（含 REAL 口径 9 项）的「页面 JSON 值 / `ops_daily_metrics` 值 / 口径 SQL 直跑值」三列对照，全部相等；不等项标红 `[L2]`
**And** 脚本只读，用 `ops_readonly` 账号（memory：运营只读库账号）`[L0]`

**AC4 · 边界**
**Then** 不做导出、不做布局自定义；页面无 PII；付费卡口径提示常驻（D-17 不改口径只改可见性）`[L0]`

**AC5 · 回归**
**Then** MockMvc：`payment.view` 有/无两账号各一条 `[L1]`

---

## Tasks / Subtasks

- [x] **T1 · 服务端门控**（AC1）
  - [x] `DashboardQueryService.chartData(range, boolean includePayment)`；Controller 用 `AdminUserDetails.getAuthorities()` 含 `payment.view` 或 `hasRole('SUPER_ADMIN')` 决定
  - [x] fragment 内付费卡 `th:if="${charts.paymentCard != null}"`，同时模板层 `sec:authorize="hasAuthority('payment.view') or hasRole('SUPER_ADMIN')"` 双保险
  - [x] 🔴 不要只在模板隐藏——JSON 内嵌在页面里，服务端必须不下发付费数据
- [x] **T2 · `AdminPageCatalog` 登记**（AC2）
  - [x] 首页维度：查看 = 全员（无码）；付费卡 = `payment.view`（矩阵「其他操作权限」列注明）
- [x] **T3 · 验收脚本**（AC3）
  - [x] `scripts/local/verify_dashboard_parity.sh`（gitignored 目录，与 seed 脚本同处）：psql 连 stag 只读 → 跑 3-2 Dev Notes 的 18 项 SQL（WIB 切日）→ 查 `ops_daily_metrics` → curl 带 cookie 拉 `/admin/charts?range=30` 解析 JSON → 三列对照
  - [x] 脚本头注明：只读、不改库、用 `ops_readonly`
- [x] **T4 · 测试**（AC5）
  - [x] MockMvc：`@WithMockUser(authorities="payment.view")` 响应含 `card-payment`；无权限不含且 JSON 无 `paying_users_*`
- [x] **T5 · 云端执行须知**
  - [x] 云端 L0：编译 + MockMvc；验收脚本 L2 本地对 stag 跑

---

## Dev Notes

### 🔴 权限码用 `payment.view`，与支付记录页同码

`AdminPaymentController.VIEW_AUTH = "hasRole('SUPER_ADMIN') or hasAuthority('payment.view')"`（代码核实）。看板付费四项数据源是 `payment_intents` 等付款表，与支付记录页语义一致，故不新增码（D-17）。发货专员、客服等预置角色若无此码则看不到付费卡；四个预置角色的权限清单见 Story 1.4 迁移。

### 🔴 服务端不下发，比模板隐藏更重要

fragment 把 JSON 内嵌在 HTML 里，模板 `th:if` 隐藏卡片但数据仍可能在源码里。必须在 `DashboardQueryService` 层按权限决定是否查付费四项；模板层门控只是第二道。

### 验收口径（D-26）

「任一天任一指标 = 口径表定义」的验收在 stag 做三方对照：页面 JSON = 长表 = 口径 SQL 直跑。口径 SQL 以 3-2 Dev Notes 的改写版为准（显式 WIB 切日、REAL 双口径、#5 按人），**不是**原参考 SQL。

### 现状代码要点

| 文件 | 现状 | 本 story |
|---|---|---|
| `admin/payment/web/AdminPaymentController.java:23` | `VIEW_AUTH` 含 `payment.view` | 引用同码 |
| `admin/account/domain/AdminPermissions.java` | 72 常量，含 `payment.view` | 不新增 |
| `admin/dashboard/service/DashboardQueryService`（3-4） | `chartData(range)` | 加 `includePayment` |
| `scripts/local/`（gitignored） | seed_w3/w4/w5.py 等 | + 验收脚本 |

### Project Structure Notes

- 不新增文件到 `admin/` 之外；脚本在 `scripts/local/`。

### References

- [Source: PRD-v1.3.0-admin.md#1 ③ 边界（权限 D-17）/ ② 同一数字两处必须相等]
- [Source: 决策日志-admin.md D-17 D-26]
- [Source: architecture-v1.3.0-admin-delta.md#Authentication & Security 新权限码段]
- [Source: petgo-backend/src/main/java/com/tailtopia/admin/payment/web/AdminPaymentController.java]

## Dev Agent Record

### Agent Model Used

claude-fable-5-1（云端 headless session，2026-09-09）

### Debug Log References

- 云端 L0：`./mvnw -B clean package`（排除需真库的 Spring 上下文测试类）→ BUILD SUCCESS，0 failures；`DashboardQueryServiceTest`（+1 付费卡门控）、`MetricSqlDumpTest`（新增，导出 27 份口径 SQL 到 `target/metric-sql/`）、`AdminPageCatalogTest` / `AdminPermissionWiringTest` 全绿。无迁移、无 i18n 改动。
- 复审（bmad-code-review，CONFIRMED 3 条全修 + PLAUSIBLE 2 条处理）：① 脚本「只读事务」护栏无效——`psql -c` 里多语句同一隐式事务，事务内 `SET default_transaction_read_only` 对当前事务不生效 → 改为连接级 `PGOPTIONS='-c default_transaction_read_only=on'`（每个事务都 READ ONLY，写语句直接报错）；② 平均类指标 `.xxxx5` 边界误标红——Python 默认银行家舍入 vs PG NUMERIC(18,4) 四舍五入 → `ROUND_HALF_UP`；③ 模板门控用 `#authorization.expression` 绕开了「表达式须逐字等于 Controller 常量」的守卫且 `PAYMENT_CARD_AUTH` 零引用 → 改回 `sec:authorize="hasRole('SUPER_ADMIN') or hasAuthority('payment.view')"`（卡片体抽成 `chart-card(card)` fragment，外层 `th:block th:if + sec:authorize` 先于子树求值），并把看板纳入 `AdminPageCatalogAuthorityTest`（PAGES 加 dashboard → `AdminDashboardController` / `AdminPaymentController`；反向断言改 `containsAll`，组校验按 inbox / overview 区分）；④ 脚本 gitignored → 本次 `git add -f` 入库并在 Completion Notes 说明；⑤ 矩阵没有「全员」字样、「沿用」落在页名下而非「其他操作」列 → 记录为呈现形态偏差（见 AC2 说明），不加展示用字段。独立 Thymeleaf 渲染核对：重构后的 fragment 两卡（users / payment）都能渲染、`th:insert` 带参 fragment 正常。
- **顺序说明**：本 story 在 sprint-status 里排在 4-1 之前，但实际在 4-3 之后补做（前一轮启动 Epic 4 时漏了它）；与 4-x 无文件交集，不影响其结果。

### Completion Notes List

- **L1/L2 待本地验收**：① `DashboardChartsMvcTest.paymentCardOnlyForPaymentViewOrSuperAdmin`（真库 + 真 Thymeleaf）：`payment.view` 账号 fragment 含 `card-payment` / `paying_users_cash`；只有 `content.view` 的账号 fragment 与整页都不含 `card-payment` / `paying_users` / `payments_cash` / `payments_incl_pawcoin`，其余四卡齐全；无任何码的账号看板 200（AC2）；② L2（T3 / D-26）：本地对 stag 跑 `scripts/local/verify_dashboard_parity.sh <d1> <d2> <d3>`（需 `ADMIN_COOKIE` = 已登录超管 / 有 `payment.view` 账号的会话 Cookie；`STAG_PG_URL` 指 `petgo_stag` 只读账号 `ops_readonly`），3 日 × 27 个 (指标, 口径) 三列全绿。
- **AC1 服务端不下发（D-17）**：`DashboardQueryService.chartData(range, includePayment)`，`includePayment=false` 时付费 `CardSpec` 直接跳过——付费四项一行都不进 `ChartData` / 内嵌 JSON；控制器 `canViewPayment(Authentication)` = authorities 含 `ROLE_SUPER_ADMIN` 或 `payment.view`（与 `AdminUserDetails.getAuthorities()` 装配一致，表达式同支付记录页 `AdminPaymentController.VIEW_AUTH`，该常量改 public 并在看板控制器以 `PAYMENT_CARD_AUTH` 引用）。模板层 `dashboard-charts.html` 付费卡外层 `th:block th:if="${card.id == 'payment'}" sec:authorize="hasRole('SUPER_ADMIN') or hasAuthority('payment.view')"` 作第二道（表达式与 `AdminPaymentController.VIEW_AUTH` 逐字一致，`AdminPageCatalogAuthorityTest` 守卫）。单参 `chartData(int)` 已删，避免绕过门控的调用点。
- **AC2 页面可达 / 目录登记**：看板路由未加任何 `@PreAuthorize`（所有后台账号可进，与原首页一致）；`AdminPageCatalog` dashboard 页：查看 = 全员（无码），付费卡以 `sharedCodes=[payment.view]`「沿用」登记（权限矩阵页面名下显示「沿用：支付记录查看 *」）。**与 story T2 字面的偏差**：story 写「矩阵『其他操作权限』列注明」，但 `otherCodes` 会在矩阵里再画一个 `payment.view` 复选框（与支付记录页那个绑定同一值、两处勾选互相影响），且 `AdminPageCatalogTest` 要求一个码只能归属一页——`sharedCodes` 是目录里现成的「本页复用别页的码」语义（D-17 不新增码），故用它。
- **AC3 验收脚本（D-26）**：`scripts/local/verify_dashboard_parity.sh`：① `./mvnw test -Dtest=MetricSqlDumpTest`（L0，不连库）把 18 项 Java 指标类的 SQL（9 项双口径 → 27 份，`:d` 命名参数）原样导出到 `target/metric-sql/`——对照用的口径 SQL 与线上物化是**同一份代码**，不是手抄；② `curl` 带会话 Cookie + `HX-Request` 拉 `/admin/charts?range=30`，解析每卡内嵌 JSON 的 labels/series；③ `psql` 只读账号：长表 `ops_daily_metrics` 按日取 + 27 份 SQL 把 `:d` 换成 `DATE '…'` 直跑（连接级 `PGOPTIONS='-c default_transaction_read_only=on'` 只读，`SET TIME ZONE 'UTC'` 与物化连接一致；口径 SQL 本身显式 `AT TIME ZONE 'Asia/Jakarta'`）；④ 三列对照，NUMERIC(18,4) 口径下比较，平均类「SQL 直跑 NULL / 长表 0」按 3-3「无样本存 0」约定视为一致；不等标红、退出码 1。脚本头注明只读 / 不改库 / `ops_readonly`，并硬校验 `STAG_PG_URL` 含 `petgo_stag`、`ADMIN_BASE` 为 stag。**`scripts/local/` 在 .gitignore 里**：云端沙箱是一次性的，脚本若不入库会随之消失，故本次 `git add -f` 强制入库（文件本身无凭证、无数据）；若希望保持该目录不追踪，本地 `git rm --cached` 即可，但请把脚本留在本地。
- **AC4 边界**：不做导出、不做布局自定义；页面无 PII；付费卡口径提示 `admin.v130.dashboard.payNote` 随卡常驻（D-17 只改可见性不改口径）；无权限时卡与提示一起不渲染（提示本身不含数据）。
- **AC5 回归**：MockMvc 有 / 无 `payment.view` 各一条（真建账号，见 L1 ①）；L0 `DashboardQueryServiceTest.paymentCardIsNotProducedWithoutPaymentView`。

### File List

- petgo-backend/src/main/java/com/tailtopia/admin/dashboard/service/DashboardQueryService.java（`chartData(range, includePayment)`）、web/AdminDashboardController.java（`canViewPayment` / `PAYMENT_CARD_AUTH`）
- petgo-backend/src/main/java/com/tailtopia/admin/payment/web/AdminPaymentController.java（`VIEW_AUTH` 改 public）
- petgo-backend/src/main/java/com/tailtopia/admin/shared/AdminPageCatalog.java（dashboard 页 `sharedCodes=[payment.view]`）
- petgo-backend/src/main/resources/templates/admin/fragments/dashboard-charts.html（卡片体抽成 `chart-card(card)` fragment；付费卡外层 `sec:authorize` 与 `AdminPaymentController.VIEW_AUTH` 逐字一致）
- scripts/local/verify_dashboard_parity.sh（新增，`git add -f`）
- 测试：admin/dashboard/metrics/MetricSqlDumpTest（新增 L0，脚本依赖）、service/DashboardQueryServiceTest（+1）、web/DashboardChartsMvcTest（+1 L1）、admin/shared/AdminPageCatalogAuthorityTest（看板纳入逐字守卫）
