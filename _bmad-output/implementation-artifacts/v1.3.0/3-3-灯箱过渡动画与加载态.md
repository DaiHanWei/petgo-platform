---
baseline_commit: ac3d57b733769b3bbc374e82db59a07082e26522
epic: 3
story: 3
batch: A
verification: [L0, L2]
---

# Story 3.3: 灯箱过渡动画与加载态

Status: ready-for-dev

## Story

As a 用户，
I want 打开和关闭大图时画面是连续的、加载失败时能重试，
so that 体验不突兀、不卡死。

---

> 依赖 Story 3.1（组件接口，尤其 `heroTagPrefix`）。
>
> 现状：`_ImageCarousel` 的缩略图失败态只是一个灰色容器（`errorBuilder` 返回纯色块），**没有重试**。

---

## Acceptance Criteria

### AC1 — 双向过渡动画（L2）

**Given** 详情页某张缩略图
**When** 点击打开查看器
**Then** 图片从原位置放大飞入全屏 `[L2]`
**When** 关闭查看器
**Then** 图片缩回原缩略图位置 `[L2]`
**And** 开关双向都有动画，不是只有打开有 `[L2]`

### AC2 — 🔴 Hero tag 由调用方给前缀（L0 / L2）

**Given** 同一张图在一屏内出现多次
**When** 生成 Hero tag
**Then** tag 由调用方提供的 `heroTagPrefix` 保证唯一 `[L0]`
**And** **不从图片 URL 推导 tag** `[L0]`
**And** 构造一个「同图两处同屏」的用例验证不抛异常 `[L2]`

> 🔴 同 tag Hero 同屏时 Flutter **直接抛异常**。批次 B1 的场所页典型版式就是「头图 + 照片网格」，同一张图必然出现两次。本批次虽然没有这种版式，**接口仍须留口**。

### AC3 — 加载中模糊过渡（L2）

**Given** 原图尚未加载完成
**When** 查看器打开
**Then** 先显示已有缩略图，模糊过渡到清晰 `[L2]`
**And** 缩略图来源沿用 `_ImageCarousel` 现有的 `thumbWidth:1080`，不另取 `[L0]`

### AC4 — 失败重试（L2）

**Given** 图片加载失败
**When** 查看器渲染该张
**Then** 显示失败提示与**重试按钮** `[L2]`
**And** 点击重试重新加载 `[L2]`

### AC5 — 埋点值域（L0 / L1）

**Given** 任一次打开 / 关闭查看器
**When** 上报
**Then** `lightbox_opened` / `lightbox_dismissed` 携带 `source` 与 `max_zoom_used` `[L1]`
**And** `dismiss_gesture` 取值域四值之一：`close_button` / `tap` / `swipe_down` / `system_back` `[L0]`
**And** **系统返回键与 iOS 侧滑退出必须上报为 `system_back`**，不得漏报 `[L2]`

> ⚠️ `system_back` 这条路径此前没人管。不报的话，「关闭方式的分布」这份数据是错的。

---

## Tasks / Subtasks

- [ ] Hero 双向过渡，tag 用调用方前缀（AC1/AC2）
- [ ] 构造「同图两处同屏」用例验证（AC2）
- [ ] 缩略图模糊过渡到清晰（AC3）
- [ ] 失败态与重试按钮（AC4）
- [ ] 埋点接入，值域四值，覆盖系统返回键（AC5）

## Dev Notes

### 要改的文件

| 文件 | 改什么 |
|---|---|
| 共享查看器组件（3.1 新建） | Hero 包裹、加载态、失败重试、埋点 |
| `petgo_app/lib/features/content/presentation/content_detail_page.dart` | `_ImageCarousel` 侧的 Hero 包裹与 tag 前缀传入 |

### 验证层级

- `[L0]` 编译、`dismiss_gesture` 值域、tag 不从 URL 推导
- `[L2]` 动画连续性、失败重试、**系统返回键埋点**（模拟器可验）

### References

- 架构 delta `AD-A15.5/.6` · `AD-A14.2b` · `AD-A26.1`

---

## Dev Agent Record

### Context Reference

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

---

## Change Log

| 日期 | 变更 | 作者 |
|---|---|---|
| 2026-09-10 | 创建 | bmad-create-epics-and-stories |
