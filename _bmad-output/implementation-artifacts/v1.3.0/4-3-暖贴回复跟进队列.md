---
baseline_commit: 4301227a
branch: feat/1.3.0-ops-ui-refactor
epic: 4
story: 4.3
ad: [AD-6, AD-8]
decisions: [D-19, D-35]
depends_on: [4-2, 3-1]
---

# Story 4.3: 暖贴回复跟进队列（入队 / 出队）

Status: review

> 自包含 story，可本地或云端 L0 执行。与用户沟通用中文。执行纪律见根 `CLAUDE.md`。
> **纯后端**：一支迁移 + 两个事件监听 + 一个 content 侧新事件。无页面（页面在 4-4）。

## Story

As a 平台，
I want 真实用户回复了虚拟账号的一级评论就自动生成一条待跟进项，回复被删则自动撤掉，
so that 运营不会漏掉「已读不回」。

## Acceptance Criteria

**AC1 · 表**
**Given** 迁移 `V<yyyyMMdd_HHmm>__init_warm_reply_followups.sql`
**Then** 建表见 Dev Notes 完整 SQL；部分唯一索引 `UNIQUE (virtual_comment_id) WHERE status='PENDING'` 保证同一虚拟评论只有一条待跟进项 `[L1]`
**And** 实体 `WarmReplyFollowup`、枚举 `FollowupStatus{PENDING,HANDLED}`、`HandledAction{REPLIED,READ}`；`ddl-auto=validate` 通过 `[L1]`

**AC2 · 入队**
**Given** `ContentCommentedEvent`（评论**转可见**时发布：机审 PASS 的 `approveComment`，或人工审核 approve）
**When** `parentAuthorId` 非空且该用户 `account_type='VIRTUAL'`，且 `commenterId` 对应用户 `account_type='REAL'`
**Then** 若无 PENDING 项 → 新建（`pending_reply_count=1`，`last_reply_id/at` = 该回复）；已有 → `pending_reply_count+1`、更新 `last_reply_*` `[L1]`
**And** 监听器 `@TransactionalEventListener(AFTER_COMMIT)` + 自身方法 `@Transactional(REQUIRES_NEW)`（既有通知事故的修法）`[L1]`
**And** 以下**不**入队：一级评论（`parentAuthorId` null）；回复者是虚拟账号；被回复者是真实用户；审核中 / 被拒的回复（事件本就不发）`[L1]`

**AC3 · 出队（D-19）**
**Given** 触发入队的真实回复被作者自删（`CommentService.delete`）或被运营下架（`takedownComment`）
**Then** 对应 PENDING 项 `pending_reply_count-1`；归零 → 删除该项（不留 HANDLED 记录）`[L1]`
**And** content 侧新增领域事件 `CommentRemovedEvent(commentId, postId, authorId, parentId, reason AUTHOR_DELETE|ADMIN_TAKEDOWN, at)`，在 `delete`（含级联软删的每条二级）与 `takedownComment` 成功路径发布；`rejectComment` / `restoreComment` 不发 `[L1]`
**And** 已 HANDLED 的项不受出队影响（历史保留）`[L1]`

**AC4 · 结构限制（D-35）**
**Then** 二级虚拟评论被回复不入队（两级结构下该回复的 `parentAuthorId` 是一级作者，不是虚拟账号）——单测钉住此行为，并在监听器注释说明 X-2 启用后再扩展 `[L0]`

**AC5 · 计数供给**
**Then** `WarmReplyQueueService.pendingCount()` 供待办中心角标聚合查询（Story 2.9 的 `GET /admin/nav/badges`）调用；4-4 页面用 `listPending / listHandled(30d)` `[L1]`

**AC6 · 日志与测试**
**Then** 监听器日志只记 id，不记评论正文 `[L0]`
**And** L1：入队 / 合并累加 / 不入队四种 / 出队减一与归零删除 / 二级虚拟不入队 `[L1]`

---

## Tasks / Subtasks

- [x] **T1 · 迁移与实体**（AC1）—— `admin/warmreply/domain`、`repository`
- [x] **T2 · content 侧 `CommentRemovedEvent`**（AC3）
  - [x] `content/event/CommentRemovedEvent.java` record + `CommentRemovedReason` 枚举
  - [x] `CommentService.delete`：一级软删后对本条 + 每条级联二级各发一次；`takedownComment` 成功（返回非空）时发一次；**`rejectComment` 不发**（从未可见，不可能在队列里）
  - [x] 事件在事务内 `publishEvent`，由监听方决定 AFTER_COMMIT
- [x] **T3 · 监听器**（AC2 / AC3）—— `admin/warmreply/service/WarmReplyEnqueueListener`
  - [x] `onCommented(ContentCommentedEvent)`：`@TransactionalEventListener(AFTER_COMMIT)` → 委托 `WarmReplyQueueService.enqueue(...)`（`@Transactional(REQUIRES_NEW)`）；判定用 `UserRepository` 查两端 `accountType`
  - [x] `onRemoved(CommentRemovedEvent)`：同范式 → `WarmReplyQueueService.onReplyRemoved(commentId, parentId)`：只处理 `parentId` 非空且存在 PENDING 项的情况
  - [x] 并发：`enqueue` 用 `INSERT … ON CONFLICT (virtual_comment_id) WHERE status='PENDING' DO UPDATE SET pending_reply_count = +1, last_reply_id = …`（原生 SQL，部分唯一索引作冲突目标）
- [x] **T4 · 查询服务**（AC5）：`pendingCount()`、`listPending(page)`、`listHandled(since 30d, page)`、`markRead(id, adminId)`（4-4 用，本 story 先提供）
- [x] **T5 · 测试**（AC6）：`WarmReplyEnqueueIntegrationTest extends ApiIntegrationTest`：造真实作者帖 → 虚拟账号一级评论（直接 `comments.save(Comment.create)` 造 VISIBLE）→ 真实用户 `commentService.createReply` + `approveComment` 触发事件 → 断言入队；再回复 → 断言合并；`commentService.delete` → 断言减一/删除；虚拟账号回虚拟账号 → 不入队；虚拟发二级被回 → 不入队
- [x] **T6 · 云端执行须知**：L0 `mvn -B clean package`；L1 留本地

---

## Dev Notes

### 🔴 `ContentCommentedEvent.parentAuthorId` 的语义已核实——正是我们要的

`CommentService.publishCommented`：`parentAuthorId = c.isTopLevel() ? null : comments.findById(c.getParentId()).authorId`。二级回复挂在一级下，`parentId` 就是那条一级，所以 **parentAuthorId = 被回复的一级评论作者**。虚拟账号发的一级评论被真实用户回复 → `parentAuthorId` = 虚拟账号 id → 入队条件成立。

同时这也证实 D-35 的限制：虚拟账号发的**二级**被回复时，回复的 `parentId` 仍是那条一级（`createReply` 的「归并到一级父」逻辑），`parentAuthorId` 是一级作者，识别不出。别试图在监听器里「猜」。

### 🔴 事件只在评论**转可见**时发

`ContentCommentedEvent` 由 `approveComment`（机审 PASS 路径的 `CommentModerationListener`、或 A1 人工通过）发布；创建时只发 `CommentSubmittedEvent`。所以审核中的回复不会入队；机审拒绝的永远不入队——与 D-4 一致，不需要额外过滤审核态。

### 🔴 现状**没有**评论删除 / 下架事件——本 story 要在 content 模块新增一个

`CommentService.delete` 与 `takedownComment` 都不发事件（`ContentRemovedEvent` / `ContentUnavailableEvent` 是**帖子**级的）。出队必须有钩子，最小改动是加 `CommentRemovedEvent` 并在两处发布。这是本 story 唯一碰 content 模块的地方，不改任何既有行为，只多两行 `publishEvent`。范式参考 `ContentUnavailableEvent` 的注释（为什么不复用别的事件）。

`restoreComment`（下架后恢复）不重新入队：恢复的是「已存在的回复」，运营早看过；如产品认为要回来，另开需求。

### 表结构（完整 SQL）

```sql
-- V<ts>__init_warm_reply_followups.sql —— V1.3.0 AB-20A 暖贴回复跟进（AD-6）
CREATE TABLE warm_reply_followups (
    id                  BIGINT       GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    virtual_comment_id  BIGINT       NOT NULL REFERENCES comments (id) ON DELETE CASCADE,
    post_id             BIGINT       NOT NULL,
    virtual_user_id     BIGINT       NOT NULL,          -- 被回复的虚拟账号（users.id，account_type=VIRTUAL）
    pending_reply_count INT          NOT NULL DEFAULT 1 CHECK (pending_reply_count >= 0),
    last_reply_id       BIGINT,                          -- 最近一条触发入队的真实回复
    last_reply_at       TIMESTAMPTZ,
    status              VARCHAR(16)  NOT NULL DEFAULT 'PENDING',
    handled_by          BIGINT,                          -- admin_accounts.id
    handled_at          TIMESTAMPTZ,
    handled_action      VARCHAR(16),                     -- REPLIED | READ
    handled_comment_id  BIGINT,                          -- REPLIED 时运营以虚拟身份发出的回复 id
    created_at          TIMESTAMPTZ  NOT NULL DEFAULT now(),
    updated_at          TIMESTAMPTZ  NOT NULL DEFAULT now(),
    CONSTRAINT ck_warm_reply_status CHECK (status IN ('PENDING', 'HANDLED')),
    CONSTRAINT ck_warm_reply_action CHECK (handled_action IS NULL OR handled_action IN ('REPLIED', 'READ'))
);
-- 同一虚拟评论只允许一条待跟进项（多条真实回复合并累加）
CREATE UNIQUE INDEX uq_warm_reply_pending ON warm_reply_followups (virtual_comment_id) WHERE status = 'PENDING';
CREATE INDEX ix_warm_reply_status_time ON warm_reply_followups (status, last_reply_at DESC);
COMMENT ON TABLE warm_reply_followups IS '暖贴回复跟进队列：虚拟一级评论收到真实回复即入队；回复被删/下架减计数归零即删；处理后 HANDLED 保留 30 天可回看';
```

`ON DELETE CASCADE` 挂在 `comments(id)`：评论表是软删，物理删几乎不发生，留着只为一致性。

### 监听器伪码

```java
@Component
public class WarmReplyEnqueueListener {
    @TransactionalEventListener(phase = AFTER_COMMIT)
    public void onCommented(ContentCommentedEvent e) {
        if (e.parentAuthorId() == null) return;                 // 一级评论
        queue.enqueueIfWarmReply(e);                             // REQUIRES_NEW 内判 VIRTUAL/REAL 并 upsert
    }
    @TransactionalEventListener(phase = AFTER_COMMIT)
    public void onRemoved(CommentRemovedEvent e) {
        if (e.parentId() == null) return;
        queue.onReplyRemoved(e.commentId(), e.parentId());       // REQUIRES_NEW：-1，归零删行
    }
}
// WarmReplyQueueService.enqueueIfWarmReply（@Transactional(REQUIRES_NEW)）
//   User parent = users.findById(e.parentAuthorId()); User commenter = users.findById(e.commenterId());
//   if (parent.accountType != VIRTUAL || commenter.accountType != REAL) return;
//   jdbc: INSERT INTO warm_reply_followups (virtual_comment_id, post_id, virtual_user_id, last_reply_id, last_reply_at)
//         VALUES (:parentCommentId, :postId, :virtualUserId, :commentId, :at)
//         ON CONFLICT (virtual_comment_id) WHERE status='PENDING'
//         DO UPDATE SET pending_reply_count = warm_reply_followups.pending_reply_count + 1,
//                       last_reply_id = EXCLUDED.last_reply_id, last_reply_at = EXCLUDED.last_reply_at, updated_at = now();
```
⚠️ 事件里没有 `parentCommentId`（只有 `parentAuthorId`）——需要 `comments.findById(e.commentId()).getParentId()` 取一次，或给 `ContentCommentedEvent` 加一个可空字段 `parentCommentId`（推荐后者，record 加字段需同步改 `publishCommented` 与 notify 侧的构造调用点；测试里 `new ContentCommentedEvent(` 调用点也要补）。两种都可，Completion Notes 写明选了哪种。

### 为什么 AFTER_COMMIT + REQUIRES_NEW

`ContentNotifyListener` 是同步 `@TransactionalEventListener`，`NotificationService.send` 必须 `REQUIRES_NEW` 才能落库——项目 07 月「通知只涨角标不进通知中心」事故的修法（memory：notify-after-commit-tx-swallow-bug）。本监听器同款：AFTER_COMMIT 阶段外层事务已提交、无环境事务，不开新事务写入会静默丢。

### 现状代码要点

| 文件 | 现状 | 本 story 改什么 | 必须保留 |
|---|---|---|---|
| `content/service/CommentService.java` | `delete` 软删 + 级联；`takedownComment` 返回摘要；`publishCommented` 发 `ContentCommentedEvent` | + 在 delete / takedown 发 `CommentRemovedEvent`；（可选）`ContentCommentedEvent` 加 `parentCommentId` | 全部既有语义 |
| `content/event/ContentCommentedEvent.java` | record 6 字段 | 可选 + `parentCommentId` | 现有字段顺序（或新增放末尾） |
| `notify/service/ContentNotifyListener.java` | 消费 `ContentCommentedEvent` | 若 record 加字段，构造调用点（测试）同步 | 通知逻辑 |
| `auth/domain/User.java` | `accountType` REAL/VIRTUAL | 只读 | — |

### 测试标准

- 造数：`Comment.create(postId, null, virtualUserId, "暖评")` 直接 VISIBLE（绕过审核只为测试）；真实回复走 `commentService.createReply` 后 `commentService.approveComment` 触发事件。
- 事件断言可用 `@RecordApplicationEvents` 或直接查表。

### Project Structure Notes

- `admin/warmreply/{domain,repository,service}`；content 侧只加一个事件类 + 两处 publish。

### References

- [Source: PRD-v1.3.0-admin.md#6. AB-20A ③]
- [Source: architecture-v1.3.0-admin-delta.md#AD-6 / Infrastructure（AFTER_COMMIT + REQUIRES_NEW）]
- [Source: 决策日志-admin.md D-19 / D-35]
- [Source: petgo-backend/.../content/service/CommentService.java#publishCommented / delete / takedownComment]
- [Source: petgo-backend/.../content/event/ContentUnavailableEvent.java（新事件写法范例）]
- [Source: petgo-backend/.../notify/service/NotificationService.java#send（REQUIRES_NEW 说明）]

## Dev Agent Record

### Agent Model Used

claude-fable-5-1（云端 headless session，2026-09-09）

### Debug Log References

- 云端 L0：`./mvnw -B clean package`（排除需真库的 Spring 上下文测试类）→ BUILD SUCCESS，0 failures；`WarmReplyQueueServiceTest`（L0，仓储 / JDBC mock）通过；`ContentNotifyListenerTest` 8 处事件构造补 `parentCommentId=null` 后仍绿。
- 新增迁移 `V20260909_1539__init_warm_reply_followups.sql`（时间戳版本号，Dev Notes 完整 SQL 原样）；沙箱无 `origin/main`，`check-flyway-versions.sh` 待本地补跑。无 i18n 改动（无页面）。
- 复审（bmad-code-review，CONFIRMED 4 条全部修复 + PLAUSIBLE 2 条顺手处理）：① 出队误减——审核中 / 已拒 / 已下架的回复被作者删也发事件、虚拟号回虚拟号被删也减 → `CommentService.delete` 只对删前 VISIBLE 的（本条与级联二级）发 `CommentRemovedEvent`，`onReplyRemoved` 增加回复者真实用户判定；② L1 测试手动 `approveComment` 与 @Async 机审竞态（双发事件 / 断言早于 AFTER_COMMIT）→ 改为轮询转 VISIBLE 再等 150ms（同 `awaitProcessed` 范式），删 / 下架后 `settle()`；③ 虚拟一级评论本身被删 / 下架时 PENDING 项成孤儿（角标常驻、4-4 回复 404）→ 监听器 `parentId == null` 走 `onTopLevelRemoved`：`DELETE … WHERE virtual_comment_id = ? AND status='PENDING'`；④ AC4 L0 单测没钉住 D-35 → `CommentServiceTest` 新增：回复虚拟二级 → 归并到一级 → 转可见事件 `parentAuthorId / parentCommentId` 都是一级的；另加「删可见回复发事件、删审核中不发」。PLAUSIBLE：⑤ 回复者「真实用户」口径改走 Story 3.1 唯一出口 `!User.isSyntheticAccount()`（role=ADMIN 也不算真实用户，单测钉住）；⑥ 监听器加 `@Order(HIGHEST_PRECEDENCE)`，AFTER_COMMIT 链上通知侧抛异常不再连累入队。

### Completion Notes List

- **L1/L2 待本地验收**：① 迁移在 stag 库 `validate` 通过（`warm_reply_followups` ↔ `WarmReplyFollowup`，部分唯一索引 `uq_warm_reply_pending`）；② `WarmReplyEnqueueIntegrationTest`（真库）：真实用户回复虚拟一级评论并转可见 → 入队（count=1、post / virtual_user / last_reply 正确、`pendingCount` +1）；第二条真实回复 → 同一项累加到 2、`last_reply` 更新、仍只有一行；作者自删一条 → 减一；再删 → 归零删行；运营 `takedownComment` 同样出队；已 `markRead` 的 HANDLED 项不受出队影响且同一虚拟评论可再起新 PENDING 项；虚拟回虚拟 / 真实一级被回 / 虚拟二级被回（D-35）三种不入队。
- **AC2 入队**：`WarmReplyEnqueueListener.onCommented`（`@TransactionalEventListener(AFTER_COMMIT)`）→ `WarmReplyQueueService.enqueueIfWarmReply`（`REQUIRES_NEW`）：`parentAuthorId` 为 VIRTUAL 且 `commenterId` 为 REAL 才入队；upsert 原生 SQL `INSERT … ON CONFLICT (virtual_comment_id) WHERE status = 'PENDING' DO UPDATE SET pending_reply_count + 1, last_reply_*`（部分唯一索引作冲突目标，并发安全）。**事件字段选型（Dev Notes 二选一）→ 选「给 `ContentCommentedEvent` 末尾加可空 `parentCommentId`」**：`CommentService.publishCommented` 填 `c.isTopLevel() ? null : c.getParentId()`，监听器不再回查 comments；`ContentNotifyListenerTest` 的构造调用同步补 `null`。事件只在评论转可见时发（`approveComment`），审核中 / 被拒的回复天然不入队。
- **AC3 出队（D-19）**：content 侧新增 `CommentRemovedEvent(commentId, postId, authorId, parentId, reason AUTHOR_DELETE|ADMIN_TAKEDOWN, at)`，在 `CommentService.delete`（本条 + 级联软删的每条二级各一次）与 `takedownComment` 成功路径事务内发布；`rejectComment` / `restoreComment` 不发（恢复不重新入队）。`onReplyRemoved`（`REQUIRES_NEW`）只处理 `parentId` 非空且存在 PENDING 项：`UPDATE … pending_reply_count - 1 WHERE pending_reply_count > 0` → `DELETE … WHERE pending_reply_count = 0`；HANDLED 项不受影响（历史保留）。这是本 story 唯一碰 content 模块的地方：一个事件类 + 三处 `publishEvent`，既有行为一行未改。
- **AC4（D-35）**：虚拟账号发的二级被回复时事件的 `parentAuthorId` / `parentCommentId` 指向一级（真实作者），识别不出 → 不入队；L0 / L1 各钉一条；监听器注释说明 X-2 启用后再扩展。
- **AC5 供给**：`pendingCount()`（4-4 并入 `NavBadgeService` 角标聚合）、`listPending(page,size)`（`last_reply_at` 倒序）、`listHandled(page,size)`（HANDLED 且 `handled_at ≥ now-30d`）、`markRead(id, adminId)`、`markReplied(id, adminId, commentId)`；实体 `markHandled` 对非 PENDING 幂等 no-op。审计（`WARM_REPLY_READ` 等）归 4-4。
- **AC6 日志**：只记 `virtualCommentId / replyId / commentId` 与异常类名；监听器 try/catch 吞异常并 warn（入队失败不影响评论主流程，靠下一条回复的 upsert 自愈；D-19 出队失败会留一条多计数的项，运营标记已读即消——记录备查）。
- **已知偏差（复审 PLAUSIBLE，记录备查）**：上线前已存在 / 已归入 HANDLED 项的旧回复被删会扣到**新**的 PENDING 项（事件不带回复创建时间，无法与项创建时间比较）——影响极小，运营标记已读即消；减一后 `last_reply_id/at` 仍指向被删那条（4-4 渲染「最近回复」时按可见过滤）；两运营同时点「已读」后写覆盖 `handled_by`，READ 后不可再转 REPLIED（4-4 页面已读项不渲染操作区，自然规避）。
- **护栏**：只用 `@TransactionalEventListener` + DB 状态机，无 MQ；AFTER_COMMIT + `REQUIRES_NEW` 同 `NotificationService.send` 范式（07 月通知事故修法）。

### File List

- petgo-backend/src/main/resources/db/migration/V20260909_1539__init_warm_reply_followups.sql（新增）
- petgo-backend/src/main/java/com/tailtopia/content/event/{CommentRemovedEvent,CommentRemovedReason}.java（新增）、ContentCommentedEvent.java（+ `parentCommentId`）、service/CommentService.java（`publishCommented` 填 parentCommentId；`delete` / `takedownComment` 发 `CommentRemovedEvent`）
- petgo-backend/src/main/java/com/tailtopia/admin/warmreply/domain/{WarmReplyFollowup,FollowupStatus,HandledAction}.java、repository/WarmReplyFollowupRepository.java、service/{WarmReplyQueueService,WarmReplyEnqueueListener}.java（新增）
- 测试：admin/warmreply/WarmReplyQueueServiceTest（新增 L0）、WarmReplyEnqueueIntegrationTest（新增 L1）；content/service/CommentServiceTest（+2：D-35 事件字段、删可见才发事件）；notify/service/ContentNotifyListenerTest（事件构造跟随）
