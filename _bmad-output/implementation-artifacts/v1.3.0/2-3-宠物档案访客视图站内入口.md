# Story 2.3: 宠物档案访客视图（站内入口）—— Epic 4 复用

Status: ready-for-dev

## Story

As a 在别人主页上看到他养了只布偶猫的用户,
I want 点那只猫就能看到它的成长档案,
so that 我能看看别人是怎么养的，而不是只看到一个名字。

> 🔴 **本 story 交付的是共用件**：Epic 4 的推荐集合宠物卡点击**落同一个终点、走同一条通道**，不另建一套。
> 两个入口若各建一套，迟早分叉 —— 而分叉的表现是**私密数据从新入口漏出去**。

## Acceptance Criteria

**AC1 — 站内访客接口（AD-4）** `[L1]`
新建**按 petId 的站内访客接口**，返回只读投影，**复用 v1.1.6 已建的那层访客投影**，不新建第二套。
该投影**结构上就取不到**健康记录与问诊存档。
接口**仅对登录用户开放**；未登录一律拒绝。

**AC2 — 不下发分享 token** `[L0]`
🛡 **不向任何调用方下发对方宠物的分享 token**（B1-D1 否掉的方案：那等于把永久公开、可转发到站外的链接发给每个站内访客）。

**AC3 — 主页宠物区** `[L2]`
展示宠物卡片（头像 / 名字 / 物种 / 年龄），**可点击进入该宠物访客视图**。
卡上的 Tailsonality 角色小标位**天然空状态**（FR-117 在批次 B2），**不做占位设计**。

**AC4 — 站内进入隐藏来源横幅** `[L2]`
从站内入口进入 → **隐藏「由 XX 分享 · 仅可查看」横幅**（站内点击不存在"谁分享的"这个语境）。
从分享链接进入时该横幅**照旧显示**。
返回时回到上一页（主页），**不落首页**。

**AC5 — 没有日历** `[L2]`
⚠️ 访客视图**没有日历、没有「时间线/日历」切换行**（2026-08-18 与 08-28 两次拍板不做；UI 旧稿画错已于 2026-09-11 修订）。
**不要照旧稿补回去。**

## Tasks / Subtasks

- [ ] **T1 后端：petId 入口**（AC: 1, 2）
  - [ ] 新端点走**同一个** `VisitorProjectionService`，不复制逻辑
  - [ ] 登录校验；响应体不含 cardToken
- [ ] **T2 前端：宠物区与跳转**（AC: 3）
- [ ] **T3 前端：站内态差异**（AC: 4, 5）
  - [ ] 给 `VisitorArchiveView` 增加"入口来源"入参，据此决定是否渲染横幅
- [ ] **T4 联调**（AC: 1, 4）

## Dev Notes

### 既有实现（动手前必读）

| 东西 | 位置 | 现状 |
|---|---|---|
| 访客投影服务 | `petgo-backend/src/main/java/com/tailtopia/profile/visitor/VisitorProjectionService.java` | 已存在，结构上取不到健康/问诊 |
| 现有访客端点 | `VisitorPetController`，`/api/v1/public/shared-pets/{cardToken}/{profile,stats,timeline,calendar,day}` | **按 cardToken**，公开可访问 |
| 访客视图页面 | `petgo_app/lib/features/profile/presentation/visitor_archive_view.dart` | 接收 `token`；含来源横幅、返回按钮、时间线 |
| 路由 | `petgo_app/lib/core/router/app_router.dart` 的 `/pet/:token` | 分享链接落点，**未登录开放** |

### 🔴 两个入口，一层投影

新端点是 `/api/v1/...`（登录态、按 petId），既有端点是 `/api/v1/public/...`（游客、按 cardToken）。
**鉴权边界不同、投影必须同一份**。复制一份投影逻辑 = 将来改一处漏一处 = 私密数据从新入口漏出去。

### 🔴 AC5 别"补全"

`visitor_archive_view.dart` 里有一大段注释专门解释为什么没有日历，原话是「别再当成缺口补上去」。后端的访客日历接口**已建好且有测试**，是**有意留着的**——看到它不等于该接上。

### 页面要加"入口来源"概念

现在 `VisitorArchiveView` 只有 `token` 一个入参。本 story 要让它知道自己是**从分享链接**还是**从站内**打开的，据此决定横幅显隐与返回行为。加个枚举入参即可，**不要**为站内再复制一个页面。

### 验证层级

L0：AC2 · L1：AC1 · L2：AC3/4/5

### References

- [Source: _bmad-output/planning-artifacts/v1.3.0/architecture-v1.3.0-batch-b1-delta.md#AD-4]
- [Source: _bmad-output/planning-artifacts/v1.3.0/决策日志-batch-b1.md#B1-D1]
- [Source: _bmad-output/planning-artifacts/v1.3.0/决策日志-batch-b1.md#B1-D5]（不做日历）
- [Source: petgo_app/lib/features/profile/presentation/visitor_archive_view.dart]

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
