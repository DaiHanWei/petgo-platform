---
baseline_commit: ac3d57b733769b3bbc374e82db59a07082e26522
epic: 3
story: 2
batch: A
verification: [L0, L2]
---

# Story 3.2: 灯箱四个手势

Status: ready-for-dev

## Story

As a 用户，
I want 用手势直接操作大图，
so that 看图像用主流看图 App 一样自然。

---

> **本批次最重的一条。** 四个手势项目内无同款、`pubspec.yaml` 无任何图片查看器类/手势类第三方包，**全部自实现，且不许引包**（AD-A22）。
>
> 依赖 Story 3.1（组件外壳已就位）。
>
> 🔴 **手势优先级顺序是防冲突的全部依据** —— 三种手势都是「手指往下或往旁边划」，不定优先级必然互相抢。

---

## Acceptance Criteria

### AC1 — 下滑关闭（L2）

**Given** 图片未放大
**When** 单指下拖
**Then** 图片跟手移动，背景随拖拽距离渐透明 `[L2]`
**And** 超过阈值松手则关闭；未超过则回弹复位 `[L2]`

### AC2 — 放大态下滑是平移（L2）

**Given** 图片**已放大**
**When** 单指下拖
**Then** 执行图内平移，**不触发关闭** `[L2]`

### AC3 — 双击缩放（L2）

**Given** 任一张图
**When** 双击
**Then** 在「适应屏幕」与「2.5 倍」之间切换 `[L2]`
**And** 以双击点为缩放中心 `[L2]`

### AC4 — 放大态防误翻页（L2）

**Given** 图片已放大
**When** 横向滑动
**Then** 先执行图内平移 `[L2]`
**And** 平移到图片边缘后继续拖，才切换到下一张 `[L2]`

### AC5 — 🔴 优先级顺序（L0）

**Given** 手势判定实现
**When** 检视代码
**Then** 顺序严格为：**已放大→图内平移 > 到边缘续拖→翻页 > 未放大下拖→关闭** `[L0]`
**And** 该顺序不可调换 `[L0]`
**And** 有测试或注释明确钉住这一顺序 `[L0]`

### AC6 — 与单击关闭不冲突（回归保护，L2）

**Given** Story 3.1 保留的「单击图片或黑边即关闭」
**When** 本 story 的手势接入后
**Then** 单击关闭仍然有效，不被双击判定拖慢到不可用 `[L2]`

### AC7 — 不引包（L0）

**Given** `pubspec.yaml`
**When** 本 story 提交
**Then** **未新增任何第三方依赖** `[L0]`

### AC8 — 不扩散（L0）

**Then** 过渡动画与加载态不在本 story（属 3.3） `[L0]`

---

## Tasks / Subtasks

- [ ] 下滑关闭：跟手位移 + 背景透明度 + 阈值判定 + 回弹（AC1）
- [ ] 双击缩放：以双击点为中心，两档切换（AC3）
- [ ] 放大态平移与边缘续拖翻页（AC2/AC4）
- [ ] 手势优先级链路，注释与测试钉住顺序（AC5）
- [ ] 与单击关闭共存验证（AC6）
- [ ] **真机逐项走查，含边界：刚好卡在阈值、快速连续手势、横竖图混合**（全部 AC）

## Dev Notes

### 🔴 这一条的验收几乎全在真机上

「顺不顺手」是本 story 的唯一价值。编译通过、单测全绿都说明不了它能不能用。**排期务必给真机调试留出实打实的时间**，不要按 L0 绿灯就当完成。

### 常见错法

- 把下滑关闭直接挂在最外层 → 放大后想平移却把页面关了
- 双击缩放注册后没处理与单击的竞争 → 单击关闭延迟 300ms，手感变钝
- 横滑翻页与图内平移各自独立判定 → 放大态一滑就翻页

### 要改的文件

| 文件 | 改什么 |
|---|---|
| 共享查看器组件（Story 3.1 新建） | 手势层 |
| `petgo_app/pubspec.yaml` | **不得改动** |

### 验证层级

- `[L0]` 编译、无新依赖、优先级顺序有测试钉住
- `[L2]` **主要验收手段**，必须本地真机

### References

- 架构 delta `AD-A15.2/.3/.4` · `AD-A22`（不引包）

---

## Dev Agent Record

### Context Reference

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

---

## Change Log

| 日期 | 变更 | 作者 |
|---|---|---|
| 2026-09-10 | 创建 | bmad-create-epics-and-stories |
