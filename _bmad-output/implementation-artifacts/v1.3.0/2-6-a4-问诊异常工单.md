---
baseline_commit: 4301227a
branch: feat/1.3.0-ops-ui-refactor
epic: 2
story: 2.6
ad: [AD-9, AD-11]
decisions: [D-8, D-14, D-23]
depends_on: [2-3a, 2-3b]
---

# Story 2.6: A4 问诊异常工单

Status: review

> 自包含 story。与用户沟通用中文。执行纪律见根 `CLAUDE.md`。纯页面重构：两个写端点路径 / 参数 / 权限零变更；`anomaly-detail.html` 与其 GET 路由退役（D-23 不做跳转）。前置：2-3a、2-3b。

## Story

As a 运营，
I want 在双栏工作台里处理问诊异常工单，看会话、加备注、标记已处理，
so that 纠纷处置不用跳独立详情页。

## Acceptance Criteria

**AC1 · 页签与左栏**
**Given** `/admin/anomalies`（`VIEW_AUTH` = SUPER_ADMIN or `consult.view_anomalies`）
**Then** 页签 = 待处理（OPEN）｜ 已处理（RESOLVED），各带计数；左栏行第一行 = 工单号 + 异常类型标签 + 状态点，第二行 = 用户昵称 × 兽医名 + 生成时间；默认时间倒序；每页 20 滚动加载 `[L1]`
**And** 现状 `?status=open|resolved|all` 筛选参数兼容映射到页签 `[L1]`

**AC2 · 右栏四区**
**Then** ① 工单卡：工单号 / 类型 / 生成时间 / 状态；② 会话卡：会话号 / 会话状态 / 起止时间 + 「去取证」链接（跳 `/admin/consult-sessions?open=<会话号>`，新开标签，Story 9.2 落地 `?open=` 自动开抽屉，本 story 只生成链接）；③ 内部备注时间线（人 + 时间 + 内容）+ 底部追加输入框；④ 操作区 `[L2]`
**And** 处理图：`resolutionImageKey` 选填输入 + 已有处理图「查看处理图」预览（现状功能原样）`[L2]`

**AC3 · 操作表（零变更）**

| 按钮 | 端点 | 参数 | 权限（现状常量） | 确认 | 成功后 |
|---|---|---|---|---|---|
| 加备注 | `POST /admin/anomalies/{id}/note` | `note` | `HANDLE_AUTH` = SUPER_ADMIN or `consult.handle` | 无 | 停留本条（时间线追加） |
| 标记已处理 | `POST /admin/anomalies/{id}/resolve` | `resolutionImageKey?` | `HANDLE_AUTH` | 无 | 下一条 |

**Then** 路径 / 参数 / `@PreAuthorize` 与现状逐字一致 `[L0]`

**AC4 · 状态流与只读**
**Then** OPEN → RESOLVED 终态入「已处理」页签，右栏只读但**备注仍可追加**（现状允许）`[L1]`
**And** 有 `VIEW_AUTH` 无 `HANDLE_AUTH` → 两钮禁用 + 注明所缺权限中文标签 `[L2]`

**AC5 · htmx 动线**
**Then** 加备注：200 + 备注时间线 fragment（局部追加，不换条）；标记已处理：200 + 右栏 fragment（`data-next-id`）+ oob 左栏行与页签计数 + `HX-Trigger admin:badge-refresh`；422 行内 err；非 htmx 维持 PRG；无键盘 ↑/↓ `[L1]`

**AC6 · 退役**
**Then** `GET /admin/anomalies/{id}` 与 `anomaly-detail.html` 删除（并入 `GET /admin/anomalies/{id}/detail` fragment）；全库无 `th:href` 指向旧路由；不做跳转（D-23）`[L1]`

**AC7 · 测试与三语**
**Then** MockMvc 四条 + 备注追加不换条断言；既有 `AdminAnomalyController` / `ConsultAnomalyService` 测试全绿；新 key `admin.v130.anomalies.*`，复用 `admin.anomalies.*`；ID 无截断 `[L1/L2]`

---

## Tasks / Subtasks

- [x] **T1 · Controller**：`list` 增 `state/page`（`status` 兼容）；新增 `GET /admin/anomalies/{id}/detail`（fragment）、`GET /admin/anomalies/queue`；`note` / `resolve` Hx 分叉（note 返回 `fragments/anomaly-notes.html`，resolve 返回右栏 + oob）；删 `GET /admin/anomalies/{id}` 方法
- [x] **T2 · Service**：`ConsultAnomalyService` 增只读 `nextOpenAfter(id)`；计数查询供页签与角标（并入 `AdminNavController` 聚合）
- [x] **T3 · 模板**：`anomalies.html` 改模板 A 壳；`fragments/anomaly-ticket.html` / `anomaly-session.html` / `anomaly-notes.html` / `anomaly-actions.html`；删 `anomaly-detail.html`；「去取证」链接 `th:href="@{/admin/consult-sessions(open=${sessionId})}" target="_blank"`
- [x] **T4 · 三语**：`admin.v130.anomalies.tab.*` / `.evidence` / `.readonly`
- [x] **T5 · 测试**：`AnomaliesWorkbenchMvcTest` 四条 + 备注局部追加 + 已处理页签只读仍可加备注；清单 diff 零
- [x] **T6 · 云端执行须知**：同 2-4

## Dev Notes

### 现状代码要点

| 文件 | 现状 | 改什么 | 必须保留 |
|---|---|---|---|
| `admin/anomaly/web/AdminAnomalyController.java` | `GET /admin/anomalies`、`GET /admin/anomalies/{id}`（`VIEW_AUTH`）；`note(note)`、`resolve(resolutionImageKey?)`（`HANDLE_AUTH`） | 加 fragment GET、Hx 分叉、删详情 GET | 两个 AUTH 常量、两个 POST 签名 |
| `admin/anomaly/service/ConsultAnomalyService.java` | 含 `@Scheduled` 异常扫描（生成工单）+ 备注 / 结案 | 只加只读方法 | 扫描器与状态机 |
| `templates/admin/anomalies.html`（67 行）/ `anomaly-detail.html`（58 行） | 列表 + 详情两页，`admin.anomalies.*` 8 个 key，`resolutionImageUrl` 预览 | 合为模板 A 一页 | key 复用；处理图预览 |

### 「去取证」深链
现状详情页无跳转；逐页规格要求跳「兽医与问诊」组的历史会话查询页并带会话号。B22 在 Story 9.2 落地 `?open=` 自动开抽屉；本 story 只按约定生成 `/admin/consult-sessions?open=<会话号>`，9.2 之前点开是现状查询页（可手动查），不阻塞。

### Project Structure Notes
落 `admin/anomaly/**` 与 `templates/admin/anomalies.html` + `fragments/anomaly-*.html`；无新模块、无迁移。

### References
- [Source: 后台重构逐页规格.md#A4 · 问诊异常工单]
- [Source: ui-v1.3.0-admin.html 泳道 1 帧 1-9]
- [Source: architecture-v1.3.0-admin-delta.md#AD-9]
- [Source: petgo-backend/.../admin/anomaly/web/AdminAnomalyController.java]

## Dev Agent Record

### Agent Model Used

claude-fable-5-1（云端 headless session，2026-09-09）

### Debug Log References

- 云端 L0：`./mvnw -B clean package`（排除需真库的 Spring 上下文测试类）→ BUILD SUCCESS，0 failures。
- 新增 L0 `AnomalyNoteLineTest` 3/3（备注时间线编码 / 解析往返、旧数据兼容、`?state=` / 旧 `?status=` 映射）；`AdminAnomalyAccessControlTest` 语义不变（签名跟随）。
- `check-i18n-keys.sh` OK（2300 key，+9）；无迁移（沙箱无 `origin/main`，flyway 冻结检查待本地补跑）。
- `list-admin-write-ops.sh` 与 2-1 底账 diff：写端点 134 → 134 零差异；GET 侧「`/admin/anomalies/{id}` 退役 + 新增 `/admin/anomalies/queue`、`/admin/anomalies/{id}/detail`」。
- 复审（14 条）已修：① `appendNote` 读-拼-写会丢并发备注 → 改数据库端追加（`ConsultAnomalyRepository.appendNoteLine` 原生 UPDATE，0 行 = 404）；② AC1 行内「用户昵称 × 兽医名」原来只出 id → `populateQueue` 整页一次取昵称 / 兽医名（注销 / 查不到回退 `#id`）；⑤ 备注服务端限 500 字（`admin.err.anomaly.noteTooLong`）；⑥ 时间线解析要求首段为 13 位毫秒（`12|a|b` 旧文本不误判）；⑦ 非 htmx `resolve` 包 try/catch → flash，服务层加「已 RESOLVED 再处理 → 409」守卫（`admin.err.anomaly.alreadyResolved`）；⑧ `note` 去掉未用的 `response`，htmx 分支只填备注 model 不白签处理图 URL；⑨ 删 8 个无引用死 key（`admin.anomalies.filter.*/view/back/detail.title/empty/col.action`）；⑩ 三处冗余 null 判断改直接 `@adminTime.wib`；⑪ 无筛选 → 壳的 filters 槽传 `~{}`；⑬ `resolveState` 单测挪到 web 包 `AdminAnomalyStateTest`；⑭ MvcTest 中文断言带 `lang=zh_CN`。③ 旧 `addNote` 标 `@Deprecated`（不委托：旧测试断言覆盖语义）；④ 「下一条」= 全局最新 OPEN（与 2.4 队首口径一致），记入 Notes；⑫ `queueFragment` 的 list 分支留作页签 hx-get 备用。

### Completion Notes List

- **L1/L2 待本地验收**：① `AnomaliesWorkbenchMvcTest`（真库 + 真 Thymeleaf：整页 200 两态页签 + 行 / 旧 `?status=RESOLVED` 映射 / 旧详情路由 404 / `HX-Request` 行片段与四区片段（去取证深链 + 备注时间线 + 操作区）/ 只有查看权两钮禁用 + 「处理问诊异常」/ 加备注 200 只回备注时间线不换条 / 空备注 422 `inline-error` / 标记已处理 200 `data-next-id` + oob 删行 + 计数 + `HX-Trigger` / 非 htmx 302 / 已处理仍可加备注 / 查看权 htmx 备注 403）；② L2：本地 stag 开 `/admin/anomalies` 对照 UI 稿泳道 1 帧 1-9：两态页签计数、行两行式（工单号 + 类型标签 + 状态点 / 用户 × 兽医 + 时间）、四区（工单卡 / 会话卡 + 去取证新开标签 / 备注时间线人 + 时间 + 内容 + 底部追加 / 操作区）、处理图预览、处置后自动下一条 + toast、三语占位。
- **AC3 红线**：`POST /admin/anomalies/{id}/note`（`note`）与 `/{id}/resolve`（`resolutionImageKey?`）路径 / 参数 / `HANDLE_AUTH` 逐字不变；htmx 分支不 try/catch，422 / 403 交 `AdminBusinessExceptionAdvice`；非 htmx PRG 回工作台（旧详情页已退役，不再 redirect 到 `/admin/anomalies/{id}`）。
- **备注时间线的数据形态**：`consult_anomalies.internal_note` 仍是那一列 TEXT（story 明写无迁移、service 只加只读方法），但 AC2「人 + 时间 + 内容」需要逐条元数据 → 新增 `ConsultAnomalyService.appendNote(...)` 按行追加 `epochMillis|操作人|内容`（`AnomalyNoteLine.encode/parse`），旧覆盖式 `addNote` 与其测试原样保留；旧数据解析为一条无人无时间的行原样展示。Controller 改走 `appendNote`（操作人取登录快照 `displayName`，无则 `#账号id`）。这是本 story 唯一超出「只读方法」的 service 改动，无 schema 变化。
- **去取证**：`th:href="@{/admin/consult-sessions(open=${sessionId})}" target="_blank"`，门 `consult.view_sessions`（与目标页 `VIEW_AUTH` 一致，避免死链）；`?open=` 自动开抽屉由 Story 9.2 落地。
- **退役**：`GET /admin/anomalies/{id}` 与 `anomaly-detail.html` 删除；全库无 `th:href` 指向旧路由（D-23 不做跳转）。
- 加备注 form 带 `data-inline-error`（`admin-workbench.js` 对 `.wb-actions, [data-inline-error]` 内请求把 `HX-Target` 改为 `#admin-inline-error`），422 不会把备注时间线换成一行 err。
- **「下一条」口径**：标记已处理后 `data-next-id` = 当前最新一条 OPEN（时间倒序队首，与 2.4 一致），不是「被处理条之后的一条」；页面加载后新生成的工单不在 DOM 时 `selectNext` 找不到行即停在处置 fragment（同 2.4/2.5 既有行为，待 2.9 联调收口）。
- 不做 ↑/↓ 键盘切换（D-14）；角标计数沿用 `NavBadgeService.countByStatus(OPEN)`（Story 2.2）。`admin-workbench.js` 本次顺带加了 `?open=` 页内深链（`[data-open][data-open-url]`，D-23），A6 / A5 复用。

### File List

- petgo-backend/src/main/java/com/tailtopia/admin/anomaly/dto/AnomalyNoteLine.java（新增）
- petgo-backend/src/main/java/com/tailtopia/admin/anomaly/repository/ConsultAnomalyRepository.java（分页 / 下一条）、service/ConsultAnomalyService.java（`page/count/nextOpenId/appendNote`）、web/AdminAnomalyController.java（工作台双路径 / 删旧详情 / Hx 分叉）
- petgo-backend/src/main/resources/templates/admin/anomalies.html（重写为模板 A）、fragments/anomaly-{queue,panel,done}.html（新增）；删除 templates/admin/anomaly-detail.html
- petgo-backend/src/main/resources/static/admin/admin.css（时间线样式）、admin-workbench.js（`[data-inline-error]` 纳入 HX-Target 改写）
- petgo-backend/src/main/resources/i18n/messages{,_zh_CN,_en,_id}.properties（+11 key：`admin.v130.anomalies.*`、`admin.err.anomaly.noteTooLong/alreadyResolved`；-8 死 key）
- petgo-backend/src/main/java/com/tailtopia/admin/anomaly/web/AdminAnomalyController.java（+ `AccountQueryService` / `VetAccountRepository` 取昵称 / 兽医名）
- 测试：admin/anomaly/dto/AnomalyNoteLineTest、admin/anomaly/web/AdminAnomalyStateTest（新增 L0）、admin/anomaly/web/AnomaliesWorkbenchMvcTest（新增 L1）；AdminAnomalyAccessControlTest（签名跟随）
