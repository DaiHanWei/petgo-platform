---
baseline_commit: 4301227a
branch: feat/1.3.0-ops-ui-refactor
epic: 7
story: 7.6
ad: [AD-9, AD-11]
decisions: [D-8, D-27]
page: E1, E2
depends_on: [2-3a, 2-3b, 7-5]
---

# Story 7.6: E1 种子内容发布（单发）与 E2 批量工作台套模板 E

Status: review

> 自包含 story。与用户沟通用中文。纪律见根 `CLAUDE.md`。依赖 2.3a / 2.3b 与 7.5（批量页页签条）。本 story 是本版**唯一主动删写端点**的地方（旧轻量批量 `/admin/seed-batch*`），删除判定见 Dev Notes，结果须写进 Epic 11 的写操作清单 diff 说明。

## Story

As a 运营，
I want 单发页只保留单条发布，批量工作台四步走完有统一的步骤条与上一步/下一步，
so that 两条发布路径分工清楚、视觉一致。

## Acceptance Criteria

**AC1 · E1 单发页只留单发表单（模板 E 单步）**
**Given** `/admin/seed-post`
**Then** 保留：关联物种下拉（含「留空由算法推导」）/ 内容类型 / 正文 ≤1000 字（`maxlength=1000` 现状）/ 图片 ≤9 张单张 ≤10MB（`data-seed-file` 上传控件 + `imageUrlsRaw` / `imageSizesRaw` 隐藏域 + URL 兜底）/ 绑定宠物（`GET /admin/seed-post/pets`）/ 发布身份确认页流程（`publish-identity-confirm`，`seed.publish_as_real` 门控）`[L1/L2]`
**And** 页内「逐行批量发布」与「Excel 导入并发布」两个旧区块**删除**（`seed-post.html` 123-140）`[L2]`
**And** `POST /admin/seed-post`、`POST /admin/seed-post/images`、`GET /admin/seed-post/pets` 不变 `[L1]`

**AC2 · 旧轻量批量端点删除**
**Then** `AdminSeedBatchController`（`admin/virtual/web/`）的 `GET /admin/seed-batch`、`GET /admin/seed-batch/template`、`POST /admin/seed-batch`、`POST /admin/seed-batch/import` **删除**；其静态方法 `mayPublishAsReal(AdminUserDetails)` **迁移**到 `AdminPublishIdentityService`（或 `admin/shared`）并更新两个调用点（`AdminWebController:399`、`AdminSeedBatchWorkspaceController:366`）`[L0]`
**And** `AdminSeedBatchControllerTest` 删除；`AdminPublishIdentityIntegrationTest` 中打 `POST /admin/seed-batch` 的用例改为走 `/admin/seed-batches` 四步流或删除并说明 `[L1]`
**And** 删除的两个写端点在 Epic 11 diff 中标注「Story 7.6 有意删除，能力由 `/admin/seed-batches/**` 承载」`[L0]`

**AC3 · E2 批量工作台套模板 E**
**Given** `/admin/seed-batches/{batchId}`
**Then** 顶部步骤条：0 批次设置 → 1 素材 → 2 录内容 → 3 预览确认；当前步高亮，完成步可点回 `[L2]`
**And** 每步底部吸底操作条「← 上一步 / 下一步 →」；第 2 步有未保存编辑行时先提示保存 `[L2]`
**And** 步 1 素材：批次共用图池（上传 / 废弃 `assets/{assetId}/delete`，被行引用中的素材废弃时该行标红）+ 页顶「图片命名与引用说明」折叠卡默认展开 `[L2]`
**And** 步 2 录内容：新增空行 = **行内编辑态**（紫底高亮：正文 + 挑图 + 身份 / 时间 + 保存行 / 取消）/ 整表粘贴（`rows/paste`）/ Excel 导入（`import`，模板 `template` 六列：正文 / 图片文件名 / 发布账号 / 内容类型 / 关联物种 / 计划时间）；行级发布账号可混多账号；字段支持「继承批次默认」`[L2]`
**And** 步 3 预览确认：`GET {batchId}/preview` 校验结果列，校验不过的行标红且不可确认；点任意行 → 成帖效果抽屉（App Feed 卡片同构、多图缩略网格、可翻行）；确认 `POST {batchId}/confirm` data-confirm 复述行数 `[L2]`
**And** 全部既有端点（`settings` / `assets` / `assets/{id}/delete` / `rows` / `rows/paste` / `rows/{rowId}` / `rows/{rowId}/delete` / `import` / `template` / `preview` / `confirm`）路径参数零变更 `[L1]`

**AC4 · 批次列表**
**Then** `/admin/seed-batches` 页签一「批次」：列 = 批次号 ｜ 来源 ｜ 创建时间 ｜ 各行状态汇总 ｜ 操作；「新建批次」`POST /admin/seed-batches` 不变；页签二由 7.5 承载 `[L2]`

**AC5 · 三语与回归**
**Then** `admin.v130.seed.*` / `admin.v130.batch.*` 三包；`AdminSeedPostIntegrationTest`、`SeedBatch*IntegrationTest`（Asset / Entry / Publish / StateMachine / ListVisibility）全绿；四条 MockMvc `[L0/L1]`

---

## Tasks / Subtasks
- [x] **T1 · E1 收缩**：删 `seed-post.html` 123-140 两区块；套 `tpl-e-steps` 单步壳（步骤条只一步，视觉统一）
- [x] **T2 · 删旧批量端点**：迁 `mayPublishAsReal` → `AdminPublishIdentityService.mayPublishAsReal(AdminUserDetails)`；改两个调用点；删 `AdminSeedBatchController` 与其测试；核 `AdminSeedBatchService` 是否仍被工作台复用（复用则保留服务层，只删 web 层）
- [x] **T3 · E2 步骤条与吸底条**：`seed-batch-workspace.html` 拆为四步 fragment；`admin-workbench.js` 或新 `admin-steps.js` 处理「未保存行提示」（读 `data-dirty`）
- [x] **T4 · 行内编辑态**：现有 `rows/{rowId}` 行表单改为紫底编辑态样式 + 保存/取消；素材引用校验标红沿用服务层返回
- [x] **T5 · 预览抽屉**：`GET {batchId}/preview/{rowId}/drawer` fragment（Feed 卡同构，复用 App 侧卡片样式令牌）
- [x] **T6** 三语 + 测试；**T7** 云端 L0，四步流 L2 留本地

## Dev Notes

### 🔴 旧批量端点删除判定（已核实）
- `/admin/seed-batch*` 四个端点在 `AdminSeedBatchController`（Story 9.8 Part 2 轻量批量：选虚拟账号 + 多行文本逐条发）。
- 模板引用**只有** `seed-post.html` 123-140（`POST /admin/seed-batch`、`POST /admin/seed-batch/import`、`GET /admin/seed-batch/template`），grep 全库无其他模板引用。
- 代码引用：`AdminWebController:399` 与 `AdminSeedBatchWorkspaceController:366` 调用其**静态方法** `mayPublishAsReal(admin)`（真实身份发布门控，与 `AdminPublishIdentityController.REAL_AUTH` 同口径，注释明写「单条发布也用这一份，刻意不各写一遍」）——删控制器前必须把这个方法迁走，否则两处编译失败。
- 测试引用：`AdminSeedBatchControllerTest`（1 个 @Test）、`AdminPublishIdentityIntegrationTest`（5 处 `post("/admin/seed-batch")`，用它验证真实身份发布门控）。后者改用 `POST /admin/seed-batches/{id}/confirm` 路径验证同一门控，或删并在 Completion Notes 说明门控已由 `SeedBatchPublishIntegrationTest` 覆盖。
- **结论：删。** 能力（多账号批量、Excel）已由 `/admin/seed-batches/**` 四步流完整承载（Story 13.1～13.5）；`AdminSeedBatchService` 若仅被旧控制器用则一并删，若被工作台复用则保留。

### 现状与改动边界
| 文件 | 现状 | 本 story | 保留 |
|---|---|---|---|
| `admin/web/AdminWebController.java` | `GET/POST /admin/seed-post`（`virtual_account.manage`）；调 `AdminSeedBatchController.mayPublishAsReal` | 改调用点 | 端点 |
| `admin/seed/web/AdminSeedAssistController.java` | `POST /seed-post/images`、`GET /seed-post/pets` | 不动 | 全部 |
| `admin/seed/web/AdminSeedBatchWorkspaceController.java` | 13 个端点四步流 | 视觉套壳；改 `mayPublishAsReal` 调用点；+ 预览抽屉 GET | 13 个端点 |
| `admin/virtual/web/AdminSeedBatchController.java` | 4 个旧端点 + 静态门控方法 | **删除**（方法迁出） | — |
| `templates/admin/seed-post.html` | 单发表单 + 两个旧批量区块 | 删旧区块，套模板 E 单步 | 单发全部字段与上传控件 |
| `templates/admin/seed-batch-workspace.html` / `seed-batch-preview.html` | 四步同页（h2 分段，无 htmx） | 步骤条 + 分步 fragment + 吸底条 + 行内编辑态 | 全部表单字段与端点 |

### 🔴 E2 交互骨架不动
逐页规格 E2「现状骨架保留（建批次→编辑→预览→确认），只做视觉统一」。步骤条、吸底条、行内编辑态、预览抽屉都是渲染层；不改状态机（`SeedBatchStateMachineIntegrationTest` 必须原样绿）。

### 端点变更清单
删除写端点：`POST /admin/seed-batch`、`POST /admin/seed-batch/import`（+ 两个 GET）。其余 `seed-post*` / `seed-batches/**` 零变更。新增 GET：`/admin/seed-batches/{batchId}/preview/{rowId}/drawer`。

### References
- [Source: 后台重构逐页规格.md#E1 · 种子内容发布 / E2 · 批量内容工作台 / 模板 E 通用规格]
- [Source: ui-v1.3.0-admin.html 泳道 2 帧 2-19～2-20、2-25～2-30]
- [Source: architecture-v1.3.0-admin-delta.md#AD-11；§Deferred]
- [Source: petgo-backend …/admin/virtual/web/AdminSeedBatchController.java；…/templates/admin/seed-post.html 123-140]
- [Source: epics-v1.3.0-admin.md#Story 7.6 / Story 11.1]

## Dev Agent Record

### Agent Model Used

claude-opus-5（云端 headless session，2026-09-10）

### Debug Log References

- 云端 L0：`./mvnw -B clean package`（排除需真库的 Spring 上下文测试类）→ BUILD SUCCESS，1827 tests，0 failures。**无迁移**。
  （测试数比上一条少 8：随旧轻量批量控制器 / 服务一并删掉的两个单测类。）
- `check-i18n-keys.sh` OK（四包 2753 key 集合相等、无重复；+26 `admin.v130.{seed,batch}.*` 与 1 条新错误码，−26 条随旧路径作废的键）。
- 云端 L0 兜底渲染：`batch-steps :: bar`、`seed-batch-workspace` 的 `step0 / step1 / step2 / rowCard`、`drawer-batch-row :: drawer`（通过 / 有错两种）各真渲染一遍（把 `@adminTime` 换成常量），全 PASS。
- 复审（bmad-code-review）：**CONFIRMED 5 条全修 + PLAUSIBLE 6 条已修 + 1 条记录**。
  - ① 🔴 **运维 SOP 仍把人指向已删的 `?tab=batch`**：`docs/ops/daily-weekly-sop.md` 每日体检第 3 项（Feed 当日新内容 < 5 条）写着「去 `/admin/seed-post?tab=batch` 补种子」—— 那是每日固定动作，改完之后页面只剩单发表单、`?tab=` 没人读，运营看到的是一个跟平时一样的页面、没有任何东西告诉他入口搬了。改指 `/admin/seed-batches`（本 story 改了 30 天计划那份，漏了这份）。
  - ② 🔴 **新测试断言 `<details class="row-edit" open>` 与实际输出不符**：`th:open` 是布尔属性处理器，条件为真时输出 `open="open"` ⇒ 本地 L1 必红（离线渲染实测）。改断言（反过来把 `th:open` 换成 `th:attr` 才是真 bug：条件为假时会输出 `open=""`，在 HTML 里等于展开）。
  - ③ 🔴 **`doesNotContain("data-row-form")` 断在第 0 步上必红**：那个字面量在「未保存行提示」的内联脚本里、每一步都渲染。改断只可能出现在行卡片上的东西。
  - ④ 🔴 **「未保存行」提示只挡「下一步」**：同一根吸底条上的「← 上一步」、步骤条点回、「返回批次列表」都会静默丢改动，而右边一步之遥的「下一步」老老实实弹提示 —— 两个按钮行为不一致，本身就会让人以为提示是随机的。改：所有离场链接一律挂 `data-step-next`。
  - ⑤ 🔴 **AC2 第三条要求的核实记录没落笔**：Story 11.1 明写「允许删除的写端点仅限 7.6 注明的，且必须在 7.6 Completion Notes 有『仅服务旧入口』的核实记录」，否则它会判事故 → 见下方「删除写端点的核实记录」。
  - ⑥ 逐行保存**失败**时那一行被收起来了（只剩页顶一条 banner，看不出是哪一行）→ 失败分支带 `&edit=<rowId>` 回来，把那一行留在编辑态上。
  - ⑦ 成帖效果抽屉的作者栏把「继承批次默认」和「该账号已不在可选身份池」混成同一句，且行级留空（最常见的用法）时**永远不显示实际会用谁发** —— 而这个抽屉的立论正是「配错账号一眼看得出来」。改：解析出**生效作者**（行级 → 批次默认），解析不到时单独出声。
  - ⑧ `data-real-identity-confirm`（选中运营真实账号才二次确认）在 E1/E2 上**一直是死的**：钩子认死 `select[name="virtualUserId"]`，而种子链路的下拉叫 `authorUserId` / `defaultAuthorUserId`，唯一还叫 `virtualUserId` 的两张表单正是本 story 删掉的旧区块。改成按 `option[data-real]` 判，不再依赖字段名。
  - ⑨ 「取消」收起编辑态不清 `data-dirty`，接着点「下一步」还会弹一次假阳性 → 一并清掉。
  - ⑩ `?step=abc` 会走 Spring 类型转换失败出 400 错误页 → 参数改 `String` 宽松解析，非数字 / 越界一律夹回 `[0,2]`。
  - ⑪ 抽屉里的元信息借用了第 2 步的表单 label（印成「内容类型（留空 = 继承批次默认）: Moment」）→ 新增三个短标签 key。
  - ⑫ 门控用例 `removalIsNotABanAndLeavesHistoryIntact` 的 `hasSize(publishedBefore)` 在「一条都没发成」时是 `0 == 0` 恒绿 → 中间补一句前置断言；另一条 `doesNotContain("批量发布")` 恒真（旧 key 已从四包删除）→ 换成 `doesNotContain("data-tab-panel")`。
  - **记录不改**：成帖效果抽屉每翻一行都重跑整批 `preview()`（整批逐行校验 + 全量身份池）。`preview` 只读无副作用，是延迟随批次变大而线性增长的问题；本 story 明确「不动服务层」，按单行校验需要拆 `SeedBatchValidator` 的入口，留后续。
  - **复审逐条核过、确认无缺陷**：13 个既有端点路径参数逐字未动；旧路径在代码 / 模板 / i18n / 页面目录 / 导航里无残留；改造前后逐字段比对，工作台与单发页的表单字段、隐藏域、`data-*`、上传控件、三条红字提示一处未丢；`form="seedPostForm"` 与 `admin-core.js` 三个钩子的链路成立；模板 E 槽位写法（首个消费者）真渲染通过；新片段名不与 HTML 标签同名；抽屉壳 / `data-no-drawer` / 排除表 / 跨批次 404 自洽。

### Completion Notes List

- **L1/L2 待本地验收**：① 新增 `AdminSeedTemplateEIntegrationTest`：四个旧端点 404、单发页字段全在 + 吸底提交钮 + 两个旧区块消失、工作台一次只渲染一步且 step 越界夹回、新增空行落在编辑态、逐行处置回第 2 步、预览页是第 4 步 + 行抽屉 + 二次确认复述行数、抽屉只读、跨批次行 id 404、无 `virtual_account.manage` 全 403。② 既有回归：`SeedBatch{Asset,Entry,Publish,StateMachine,ListVisibility}IntegrationTest`、`AdminSeedPostIntegrationTest` 全绿；`AdminPublishIdentityIntegrationTest` 的 6 处旧端点用例改打 `/admin/seed-post`（同一份门控）与四步流 `confirm`（去重哈希 + 操作人）。③ L2：本地 stag 对照 UI 稿 2-19～2-20、2-25～2-30：步骤条与吸底条、行内编辑态紫底、素材墙、成帖效果抽屉翻行。
- **AC1 E1**：`seed-post.html` 套 `tpl-e-steps` 单步；页内「逐行批量发布」「Excel 导入并发布」两个区块与页签条删除；单发表单字段、上传控件、隐藏域、宠物下拉、发布身份确认流程逐字保留；三个端点未变。
- **AC2 旧端点删除**：`GET/POST /admin/seed-batch`、`GET /admin/seed-batch/template`、`POST /admin/seed-batch/import` 删除；`mayPublishAsReal` 迁到 `AdminPublishIdentityService`（它本来就是发布身份的判定）；`AdminSeedBatchService` 一并删（删控制器后全仓无调用方，Dev Notes 明确授权）。
- **AC3 E2**：步骤条 0 批次设置 / 1 素材 / 2 录内容 / 3 预览确认（工作台与预览页**共用同一份** `fragments/batch-steps :: bar`）；吸底条上一步 / 下一步；第 2 步有未保存编辑行时先提示；行内编辑态（`<details class="row-edit">`，展开紫底，新增空行直接落在编辑态上）；预览页点行开成帖效果抽屉（多图网格 + 正文 + 作者 + 类型 / 物种 / 时间 + 上一行 / 下一行）；13 个既有端点路径参数零变更。
- **AC4 批次列表**：页签一保持原状（7.5 已页签化），本 story 未改。
- **AC5 三语与回归**：26 个新 key 四包同批，另删 26 条随旧路径作废的键。

### 删除写端点的核实记录（Story 11.1 要求）

- **删除**：`POST /admin/seed-batch`、`POST /admin/seed-batch/import`（+ `GET /admin/seed-batch`、`GET /admin/seed-batch/template`）。能力由 `/admin/seed-batches/**` 四步流完整承载（Story 13.1～13.5）。
- **「仅服务旧入口」核实**：`AdminSeedBatchService` 全仓只被删掉的那个控制器引用（`SeedContentFingerprint`、`AdminContentService` 里只是注释提到类名，不是调用）；去重哈希的写入口仍在 `SeedBatchPublishService`（`hashes.save(SeedContentHash.of(...))`）；Excel 解析由 `SeedBatchExcelService` 独立承载；静态门控 `mayPublishAsReal` 已迁至 `AdminPublishIdentityService`，两个调用点（`AdminWebController`、`AdminSeedBatchWorkspaceController`）已改。四个旧端点在模板 / i18n / 页面目录 / 导航 / 运维文档中已无引用（两份 runbook 都已改指新入口）。
- **测试替代**：`AdminSeedBatchControllerTest` 与 `AdminSeedBatchServiceTest` 随代码删除；`AdminPublishIdentityIntegrationTest` 的 6 处旧端点用例改打 `POST /admin/seed-post`（与被删路径**共用同一份**真实身份门控）与四步流 `POST /admin/seed-batches/{id}/confirm`（去重哈希 + 操作人记录）；服务层门控另有 `AdminContentServiceTest` 的 L0 单测钉着。

### File List

- petgo-backend/src/main/resources/templates/admin/{seed-post,seed-batch-workspace,seed-batch-preview}.html、fragments/{batch-steps,drawer-batch-row}.html（新增）
- petgo-backend/src/main/java/com/tailtopia/admin/seed/web/AdminSeedBatchWorkspaceController.java（step/edit 参数 + 分步落点 + 成帖效果抽屉 GET）
- petgo-backend/src/main/java/com/tailtopia/admin/virtual/service/AdminPublishIdentityService.java（+`mayPublishAsReal`）、admin/web/AdminWebController.java（改调用点）
- **删除**：admin/virtual/web/AdminSeedBatchController.java、admin/virtual/service/AdminSeedBatchService.java 及其两个单测
- petgo-backend/src/main/resources/static/admin/admin.css（`.stp-actions` / `.seed-row-card` / `.row-edit` / `.bt-*` / `.preview-table`）、i18n/messages{,_zh_CN,_en,_id}.properties
- docs/ops/{seed-content-30d-plan,daily-weekly-sop}.md（两处指向旧路径的运营入口改指新入口）
- petgo-backend/src/main/resources/static/admin/admin-core.js（真实身份二次确认改按 `option[data-real]` 判，不再认死字段名）
- 测试：seed/AdminSeedTemplateEIntegrationTest（新增 L1）、virtual/AdminPublishIdentityIntegrationTest（6 处改打新路径）
