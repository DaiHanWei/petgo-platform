---
baseline_commit: 4301227a
branch: feat/1.3.0-ops-ui-refactor
epic: 8
story: 8.1
ad: [AD-9, AD-10, AD-11]
decisions: [D-8, D-23]
depends_on: [2-3a, 2-3b]
page_spec: B7
---

# Story 8.1: B7 用户列表 + 五页签详情抽屉

Status: review

> 自包含 story，可本地或云端 L0 执行。与用户沟通用中文。执行纪律见根 `CLAUDE.md`（后端→前端→联调、AC 标 L0/L1/L2、`mvn -B clean package`）。
> **零功能变更**（AB-19A 原则）：本 story 不新增、不删除任何写端点，只做页面形态重构 + 一条 PRD 例外（宠物档案增列性别 / 生日，纯读展示）。依赖 Story 2.3a（htmx 约定 / `AdminExportWriter`）与 2.3b（`tpl-b-list` 壳 / `admin-drawer.js`）。

## Story

As a 运营，
I want 用户列表有摘要条、点行在右侧抽屉里分页签看基本信息 / 宠物 / 内容 / 问诊，并在抽屉里直接停用、赠币、删除，
so that 处置一个用户不用跳独立详情页来回切。

## Acceptance Criteria

**AC1 · 列表套模板 B（逐页规格 B7）**
**Given** `/admin/users`
**When** 打开
**Then** 自上而下：筛选栏（关键词 ID / 昵称 / 邮箱 / 手机号 + 显式「查询」钮；状态下拉 `data-autosubmit`；手机号已填/未填筛选保留）→ 摘要条 **总用户数 · 今日新增 · 已停用 · 已删除**（单条 `COUNT(*) FILTER` 聚合，随筛选联动）→ 表格列 **ID ｜ 昵称 ｜ 注册邮箱 ｜ 注册时间（WIB）｜ 手机号 ｜ 状态**，默认注册时间倒序，每页 20，行高 ~48px，状态用色点 + 徽标 `[L1]`
**And** 「查看详情」按钮列删除，点行 → 抽屉（AC2）；`?open=<userId>` 自动开抽屉 `[L2]`
**And** 「导出召回名单」按钮保留：手机号筛选为「已填」时激活，否则禁用置灰 + 提示（现状行为）；改走 `AdminExportWriter`（xlsx，表头随 locale，规则 12）；权限 `user.phone_export` 不变 `[L1]`

**AC2 · 五页签抽屉吸收 `user-detail`**
**Given** 点某一行
**When** `GET /admin/users/{userId}/drawer`（`HX-Request`）
**Then** 480px 右滑抽屉，五页签：
1. **基本信息**：昵称 / 注册邮箱 / 手机号（`user.phone_view` 门控，无权限显示掩码或不渲染，与现状一致）/ 注册时间 / PawCoin 余额（`user.grant_pawcoin` 门控）/ 账号状态 `[L2]`
2. **宠物档案**：卡片网格（一宠一卡：头像位 + 名字 + 物种胶囊 + 品种 / **性别** 一行 + **生日** + KTP ID），只读，不分页；**性别 / 生日为 PRD §5 ③ 第 2 条例外**，数据取 `PetProfile.sex`（`PetSex`）/ `PetProfile.birthday`（`LocalDate`）`[L2]`
3. **发布内容**：ID / 类型 / 内容 / 状态，只读；点行 → 跳 `/admin/content?open=<postId>`（B1 抽屉，Story 7.1）`[L2]`
4. **历史问诊**：会话 / 兽医 / 时间 / 状态 / 评分，只读，顶部保留「仅元数据，不含对话内容」提示；点会话 → `/admin/consult-sessions?open=<sessionId>`（Story 9.2）`[L2]`
5. **处置**：三张卡一项一行——停用（必填 `reason`，data-confirm）/ 重新激活；赠送 PawCoin（`coins` + `reason` + 隐藏 `idempotencyToken`，data-confirm 复述昵称 + 金额）；删除账号（`type` 单选 `USER_REQUEST` / `VIOLATION` 必选 + `note` 必填，**双重 data-confirm** 复述昵称）`[L2]`
**And** 处置提交走 htmx：成功 200 + 抽屉 fragment 重渲染 + `hx-swap-oob` 刷该行；失败 422 处置卡行内 err；无权限 403 禁用态 + 注明所缺权限名 `[L1]`

**AC3 · 端点与参数零变更**
**Then** 写端点保持：`POST /admin/users/{userId}/grant-pawcoin`（coins / reason / idempotencyToken，`user.grant_pawcoin`）、`POST …/deactivate`（reason，`user.deactivate`）、`POST …/reactivate`（`user.deactivate`）、`POST …/delete`（type / note，`user.delete`）、`GET /admin/users/phone-recall.xlsx`（phone，`user.phone_export`）；路径、参数名、`@PreAuthorize` 表达式逐字不变 `[L0]`
**And** 服务层 `AdminUserService` 五个业务方法不改签名；审计动作码不变 `[L0]`

**AC4 · 退役**
**Then** 删除 `templates/admin/user-detail.html` 与 `GET /admin/users/{userId}` 页面路由；不做跳转兜底（D-23）；全库 grep 无残留 `th:href` 指向 `/admin/users/{id}` 页面 `[L0/L1]`

**AC5 · 边界**
**Then** 已删除账号：列表行置灰、抽屉全只读、处置页签不渲染任何按钮；已停用账号处置页签显示「重新激活」而非「停用」 `[L2]`
**And** 处置页签按权限逐钮禁用并注明缺失权限（`user.deactivate` / `user.grant_pawcoin` / `user.delete`）`[L1]`
**And** 无 `user.view` 的账号：导航不渲染「用户」项；直访 403 `[L1]`

**AC6 · 三语与回归**
**Then** 新增文案全部走 `admin.v130.users.*` key，zh / en / id 三包同批；切 ID 无截断（摘要条 2×2 网格、页签 nowrap）`[L0/L2]`
**And** 既有 `AdminUserIntegrationTest` / `AdminUserDeactivationIntegrationTest` / `AdminUserDeletionIntegrationTest` / `AdminUserPhoneIntegrationTest` 全绿；新增 MockMvc 四条（整页 200 / drawer fragment / 422 / 403）`[L1]`

---

## Tasks / Subtasks

- [x] **T1 · 列表页套壳**（AC1）
  - [x] `users.html` 改为 `th:replace="admin/fragments/tpl-b-list :: shell(...)"` 填四个槽位；摘要条数据由 `AdminUserService.summary(filter)` 一条聚合查询提供（新增只读查询方法，不动业务方法）
  - [x] 关键词搜索改为带显式「查询」钮的表单（现状是 `hx-get` 即输即查 + 搜索钮并存，统一为显式钮）；手机号筛选 `data-autosubmit` 保留
  - [x] 删「查看详情」列，行加 `hx-get="/admin/users/{id}/drawer" hx-target="#users-drawer"`
  - [x] 导出：`AdminUserService.exportRecallList` 内部改调 `AdminExportWriter.xlsx(...)`（2.3a），列与现状一致；未选「已填」禁用逻辑与提示原样保留
- [x] **T2 · 抽屉 fragment**（AC2）
  - [x] 新增 `fragments/drawer-users.html`：五页签（页签切换为前端切 `hidden`，一次性渲染，不再请求）
  - [x] `AdminUserController` 新增 `GET /admin/users/{userId}/drawer`（`@PreAuthorize(AUTH)`，复用 `service.detail(userId, includePhone)`）
  - [x] `AdminUserDetailView.PetRow` 增 `sex`（`PetSex` 三语 key 映射）与 `birthday`（WIB 日期）；数据来自 `PetProfile.sex` / `PetProfile.birthday`，只读
  - [x] 处置三卡的 `<form>` 加 `hx-post` + `hx-target="#users-drawer"`，保留 `th:data-confirm` 现有 key（`admin.userdetail.deactivateConfirm` / `grantConfirm` / `deleteConfirm`）；删除卡再叠一层确认（双确认）
  - [x] 三个 POST 处置端点：`HxRequest` 为真时返回抽屉 fragment + oob 行；否则维持现状 PRG（兼容）
- [x] **T3 · 退役**（AC4）
  - [x] 删 `user-detail.html`；删 `AdminUserController.userDetail` GET 方法；`grep -rn "/admin/users/" templates` 清残留链接
  - [x] Story 2.1 写操作清单里 `GET /admin/users/{userId}` 标「退役」
- [x] **T4 · 三语**（AC6）
  - [x] 新 key：`admin.v130.users.summary.{total,today,deactivated,deleted}`、`admin.v130.users.tab.{basic,pets,posts,consults,actions}`、`admin.v130.users.pet.{sex,birthday}`、`admin.v130.users.pet.sex.{MALE,FEMALE,UNKNOWN}`；复用现有 `admin.users.*` / `admin.userdetail.*`
- [x] **T5 · 测试**（AC6）
  - [x] MockMvc：`GET /admin/users` 200 含摘要条；`GET /admin/users/{id}/drawer` + `HX-Request` 返 fragment（含五页签 id）；`POST deactivate` 缺 reason → 422 fragment；无 `user.deactivate` → 403 禁用态
  - [x] 既有四个集成测试回归；`user-detail` 相关断言改为断言 drawer
- [x] **T6 · 云端执行须知**
  - [x] 云端只跑 L0（`mvn -B clean package`）；L1 集成测试与 L2 抽屉视觉留本地，Completion Notes 标注

---

## Dev Notes

### 🔴 端点零变更是硬约束，只加一个 GET drawer

写操作清单（Story 2.1）是 Epic 11 的验收分母。本 story 允许新增的只有 `GET /admin/users/{userId}/drawer`（读）与删除 `GET /admin/users/{userId}`（读）。五个 POST 的路径 / 参数名 / 权限表达式一个字不能动。

### 现状代码要点（读过的文件）

| 文件 | 现状 | 本 story 改什么 | 必须保留 |
|---|---|---|---|
| `admin/usermgmt/web/AdminUserController.java` | `GET /admin/users`（q / page / phone）、`GET /admin/users/{userId}`、`POST grant-pawcoin`（coins / reason / idempotencyToken）、`POST deactivate`（reason）、`POST reactivate`、`POST delete`（type / note）、`GET phone-recall.xlsx`（phone）；AUTH = `user.view`，EXPORT = `USER_PHONE_EXPORT`，DEACTIVATE = `user.deactivate`，DELETE = `user.delete`，GRANT = `user.grant_pawcoin` | + `GET {userId}/drawer`；− `GET {userId}` 页面；POST 方法加 `HxRequest` 分支 | 五个 POST 签名与 `@PreAuthorize` |
| `admin/usermgmt/service/AdminUserService.java` | `grantPawCoin(userId, coins, reason, idempotencyToken, actor)`、`deleteUser(userId, DeletionType, note, actor)`、`deactivate(userId, reason, actor)`、`reactivate`、`list` / `search` / `listByPhoneFilled`、`exportRecallList(actor, filled)`（POI xlsx）、`detail(userId[, includePhone])` | + `summary(filter)`；`exportRecallList` 改经 `AdminExportWriter`；`detail` 补宠物性别生日 | 业务方法签名、审计 |
| `admin/usermgmt/domain/DeletionType.java` | `USER_REQUEST` / `VIOLATION`，`fromOrNull` | 不动 | — |
| `admin/usermgmt/dto/AdminUserDetailView.java` | `pets: List<PetRow(id,name,petType,breed)>`、`posts`、`sessions` | `PetRow` + `sex` + `birthday` | 其余字段 |
| `profile/domain/PetProfile.java` | `petType` / `name` / `breed` / `birthday: LocalDate` / `sex: PetSex` | 只读取 | — |
| `templates/admin/users.html` | 两个 filter-bar（q 搜索 hx-get 即时；phone 下拉 autosubmit）、导出钮三态、表 7 列（含操作列）、分页 | 套壳、去操作列、行 hx-get | 导出三态逻辑与 `sec:authorize` 表达式（须与控制器逐字一致） |
| `templates/admin/user-detail.html` | 处置三卡（`sec:authorize` 分卡门控）+ 基本信息 + 宠物表 + 发布内容表 + 历史问诊表 | **删除**，内容平移进 `drawer-users.html` | 每张卡的 `sec:authorize` 表达式原样搬 |

### ⚠️ 现状与逐页规格的两处差异（如实处理，不扩范围）

1. 现状关键词搜索是 `hx-get` 即输即查；模板 B 通用规格要求「文本搜索必须配显式查询钮」。改为显式钮，去掉 `hx-trigger` 即时搜索。
2. UI 稿 3-1 写「导出召回名单 = 手机号筛选后才激活」，代码已如此（`exportNeedsFilter` 三态），照搬即可。

### `@adminTime` 已存在

`admin/web/AdminTime.java` 已是 bean（模板里 `@adminTime.wib(...)` 在用）。架构 §结构树写的 `admin/shared/time/AdminTime` 由 Story 2.3a 负责搬迁或直接沿用现有 bean；本 story 只调 `@adminTime.wib`，不自己格式化。

### 抽屉页签间跳转的目标

- 发布内容 → `/admin/content?open=<postId>`：依赖 Story 7.1 的 `?open=` 支持；7.1 未合入前链接照常渲染但落到列表页顶部，不算本 story 失败。
- 历史问诊 → `/admin/consult-sessions?open=<sessionId>`：依赖 Story 9.2，同上。

### 测试标准

- 沿用 `test/.../admin/usermgmt/` 四个 `IntegrationTest extends ApiIntegrationTest`；抽屉 fragment 断言用 `content().string(containsString("id=\"users-drawer\""))`。
- L1 本地跑法：flush Redis DB0 → 重建 scratch 库 → `DB_NAME` env。

### Project Structure Notes

- 改动全部在 `admin/usermgmt/**` + `templates/admin/{users.html, fragments/drawer-users.html}`；无迁移、无新表。
- 三语 key 前缀 `admin.v130.users.*`；`sec:authorize` 与 `@PreAuthorize` 逐字一致（历史事故：按钮在、点了 403）。

### References

- [Source: _bmad-output/planning-artifacts/v1.3.0/后台重构逐页规格.md#B7 · 用户 + 模板 B 通用规格]
- [Source: _bmad-output/planning-artifacts/v1.3.0/epics-v1.3.0-admin.md#Story 8.1]
- [Source: _bmad-output/planning-artifacts/v1.3.0/architecture-v1.3.0-admin-delta.md#AD-9 / AD-10 / AD-11]
- [Source: _bmad-output/planning-artifacts/v1.3.0/PRD-v1.3.0-admin.md#§5 ③ 第 2 条]
- [Source: _bmad-output/planning-artifacts/v1.3.0/决策日志-admin.md D-23]
- [Source: _bmad-output/planning-artifacts/v1.3.0/ui-v1.3.0-admin.html 泳道 3 帧 3-1 ～ 3-6]
- [Source: petgo-backend/src/main/java/com/tailtopia/admin/usermgmt/web/AdminUserController.java]

## Dev Agent Record

### Agent Model Used

云端 headless session（claude.ai/code），**L0 only**。

### Debug Log References

- L0：`cd petgo-backend && ./mvnw -B clean package` → `Tests run: 1828, Failures: 0, Errors: 0, Skipped: 6` / BUILD SUCCESS
- `bash scripts/ci/check-i18n-keys.sh` → OK（2766 个 key，四包集合相等）
- `bash scripts/ci/check-flyway-versions.sh origin/main` → OK（本 story **无新增迁移**）
- 云端无 Docker（跑不了 L1）：另写离线 Thymeleaf 渲染探针，把 `users.html` / `fragments/users-list.html` /
  `fragments/drawer-users.html` 真渲染 14 种组合（替换 `@adminTime` bean 引用），全绿。
  这类模板解析错 / 表达式错本来要 L1 才暴露。

### Completion Notes List

**L1 / L2 待本地验收**（云端 headless，无 Docker、无渲染）：本 story 全部 `[L1]` / `[L2]` 项，
尤其新增的 `AdminUserDrawerIntegrationTest`（12 条）与既有四个用户集成测试的回归。

**AC 落地**

- AC1：`users.html` 套 `tpl-b-list :: list(...)`（`res='user'` → `#user-drawer`）；摘要条四格走
  `AdminUserService.summary` 单条 `COUNT(*) FILTER` 聚合，随**关键词 + 手机号 + 状态**三条筛选联动；
  每页 20；「查看详情」列已删，点行开抽屉；`?open=` 深链兜底。
  **新增状态下拉**（B7 规格「筛选：关键词 · 状态」，原实现缺）：`active / deactivated / deleted`，
  `data-autosubmit`（只作用于 `<select>`，关键词框仍走显式「查询」钮）。
- AC2：`fragments/drawer-users.html` 五页签**一次性渲染**（切页签纯前端切 `hidden`，不再请求）；
  处置三卡的 `sec:authorize` 表达式逐字从整页搬来；删除双确认（`data-confirm` + `data-confirm-2` 复述昵称）。
- AC3：五个 POST 的路径 / 参数名 / `@PreAuthorize` 逐字未动；`AdminUserService` 五个业务方法签名与审计动作码未动。
- AC4：删 `templates/admin/user-detail.html` + `GET /admin/users/{userId}`；不做旧地址跳转（D-23）；
  全库无残留链接（三处作者链改 `?open=`）。
- AC5：已注销 → 行置灰、抽屉全只读；已停用 → 处置页签给「重新激活」；无权限逐钮置灰并注明所缺权限名。
- AC6：新增 25 条 `admin.v130.users.*` 三语同批；删掉随整页退役的 10 条孤儿 key。

**复审（对抗式）修掉的 7 条 CONFIRMED**

1. 🔴 `afterAction` 的 oob 行原来包在 `th:remove="tag"` 的假 table 里 → **裸 `<tr>`**。htmx 只在响应
   以 `<tr` 开头时才补 `<table><tbody>`；这里第一个标签是抽屉体的 div，走通用解析，
   `<tr>/<td>` 被 HTML 解析器丢掉：**列表那一行根本不会被换掉**，单元格文本反而泄进
   `#user-drawer-err` 显示成一条行内报错。改成真 `<table hidden>`（照 `drawer-content.html` 的先例）。
2. 🔴 新加的状态筛选没进导出判据 → 状态选「已注销」时「导出召回名单」仍是亮态，导出的却是全量
   （导出端点按 AC3 只认 `phone`）。同一类「屏幕一份、文件一份」。改成状态筛选生效时置灰；
   顺手把三态判据收敛成一个 `canExport` 变量（原先同一判据写三遍，迟早漂移）。
3. 🟠 搜索态下摘要条按手机号筛、列表不筛 → 「列了 8 行、总数写 3」。三个筛选合进同一个 form 之后
   这是日常路径。搜索分支改为在内存里补筛手机号 + 状态（不分页、封顶 50 条，不影响分页正确性）。
4. 🟠 删除是 7.3 的 `@Async` 级联，`deletedAt` 当次响应还没落库 → 重渲染的抽屉里三张处置卡还在，
   运营能在「注销已提交」下面**再点一次删除**（再写审计、再触发级联）。加 `deletionPending` 标记，
   删除那条路当即只读并说明原因；对应测试改为钉**删除请求自己的同步响应**，不再等异步（原写法 flaky，
   破了 `AdminUserDeletionIntegrationTest` 立的「不断言异步完成」规矩）。
5. 🟠 五处假绿断言重写：摘要条联动（原来只断言「摘要条在」→ 改钉 `data-sum="deactivated">0`）、
   `hx-trigger="keyup"`（旧模板从来没有过，改造前后都绿 → 改钉显式查询钮）、
   PII（原来只断言筛选下拉在 → 改成断言**号码本身**不在列表 HTML 里）、
   禁用态「赠送 PawCoin」（与卡片标题同字面 → 改钉完整的「需要「赠送 PawCoin」权限」）、
   oob 行（只看服务端字符串 → 改钉真 `<table hidden>` 外壳且在行之前）。
6. 🟡 删掉随整页详情退役的 10 条孤儿 key（`admin.users.col.action` / `admin.users.view` /
   `admin.userdetail.{back,basic,pets,posts,consults,status}` / `admin.userdetail.col.{petName,petType}`）。
7. 🟡 摘要条四格加 `data-sum` 锚点 —— 「随筛选联动」此前没有任何可被测试钉住的落点。

**摘要条与列表的口径对齐（本 story 改到的地方）**

`summary` 的关键词判据原先是「`nickname ILIKE` 或 `display_name ILIKE`」且不排注销，比 `search()` 的网更宽
（`search` 用 `coalesce(nickname, display_name)` + 邮箱模糊，且排除已注销）→ 搜索态下摘要条常年大于列表行数。
已逐条对齐（`qIsId` 分流 id / 邮箱精确、模糊路排注销）。
**唯一剩下的口径差**：`search` 模糊路封顶 50 条，摘要条数的是命中总数 —— 命中超过 50 时「总用户数」会大于
列表行数，这是有意的（那个数正是在提示「还有更多，缩小关键词」）。

**有意不做 / 待拍板**

- ⚠️ **AC1 的「关键词…手机号」未实现**：`search()` 只匹配 id / 邮箱 / 昵称。按手机号搜等于给
  **没有 `user.phone_view` 的人**开一条号码探测信道（输入一个号码，看有没有命中）。
  安全上不该在没有权限门控设计的前提下补，**留待拍板**。
- ⚠️ 删除提交后**列表那一行**当次仍显示旧状态（同一个 `@Async` 竞态；抽屉已给「注销已提交」提示）。
  异步跑完后下次加载即正确。要当次也准确得给行加一个「注销处理中」态，属新增展示态，未做。
- ⚠️ **既存问题（非本 story 引入）**：`User.anonymizeForDeletion` 清了 email / displayName / nickname /
  avatar / googleSub，**没清 `phone`** → 召回名单里「已注销」那些行仍带真实号码。
  若认为违反 D1「注销删除纯个人数据」，应另开 story，**留待拍板**。
- ⚠️ 抽屉内「发布内容 / 历史问诊」跳 `?open=` 依赖 Story 7.1 / 9.2（7.1 已合入；9.2 未做，
  链接照常渲染但落到列表页顶部，story 已声明不算本 story 失败）。
- PRG 兜底（非 htmx）跳回 `/admin/users?open=<id>` 时不带 q / phone / status —— 沿用改造前行为，未扩大。

**架构护栏自查**：无新迁移、无新中间件、无 MQ / 缓存层；对外无自增 id 新暴露面（本页 id 本就是后台内部展示）；
日志未新增任何 PII 落点；`ddl-auto=validate` 未动。

### File List

**改**
- `petgo-backend/src/main/java/com/tailtopia/admin/usermgmt/web/AdminUserController.java`
- `petgo-backend/src/main/java/com/tailtopia/admin/usermgmt/service/AdminUserService.java`
- `petgo-backend/src/main/java/com/tailtopia/admin/usermgmt/dto/AdminUserDetailView.java`
- `petgo-backend/src/main/java/com/tailtopia/auth/repository/UserRepository.java`
- `petgo-backend/src/main/java/com/tailtopia/auth/service/AccountQueryService.java`
- `petgo-backend/src/main/resources/templates/admin/users.html`
- `petgo-backend/src/main/resources/templates/admin/fragments/drawer-content.html`
- `petgo-backend/src/main/resources/templates/admin/fragments/drawer-comment.html`
- `petgo-backend/src/main/resources/static/admin/admin-core.js`
- `petgo-backend/src/main/resources/static/admin/admin.css`
- `petgo-backend/src/main/resources/i18n/messages{,_zh_CN,_en,_id}.properties`
- `petgo-backend/src/test/java/com/tailtopia/admin/usermgmt/AdminUserPhoneIntegrationTest.java`
- `petgo-backend/src/test/java/com/tailtopia/admin/usermgmt/service/AdminUserServiceTest.java`
- `petgo-backend/src/test/java/com/tailtopia/admin/usermgmt/web/AdminUserAccessControlTest.java`

**新增**
- `petgo-backend/src/main/resources/templates/admin/fragments/users-list.html`
- `petgo-backend/src/main/resources/templates/admin/fragments/drawer-users.html`
- `petgo-backend/src/test/java/com/tailtopia/admin/usermgmt/AdminUserDrawerIntegrationTest.java`（L1，12 条）

**删除**
- `petgo-backend/src/main/resources/templates/admin/user-detail.html`
