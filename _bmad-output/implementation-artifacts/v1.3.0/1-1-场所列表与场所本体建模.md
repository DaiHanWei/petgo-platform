# Story 1.1: 场所列表与场所本体建模

Status: review

<!-- 版本 v1.3.0 · 主题 batch-b1 · Epic 1 场所发现 · 本 epic 首条 -->

## Story

As a 想找宠物友好场所的用户,
I want 从首页进去看到已经有哪些场所,
so that 我能知道这个功能里已经攒了些什么地方，而不是面对一个空壳。

## Acceptance Criteria

**AC1 — 建表（AD-1）** `[L0]`
`places` 表含：名称 / 类型 / 标签集 / **纬度、经度两个独立数值列** / 文字地址 / 描述 / 照片列表 / 标记人 / 状态 / 不可枚举 token。
- 迁移文件名为 **时间戳版本号** `V<yyyyMMdd_HHmm>__init_places.sql`（取创建时刻），表名带模块前缀
- 表内**不含**任何地图厂商 POI id 列
- 表内**不预留** ⑧ 打卡相关的任何列（那是批次 B2 —— 预留即越界）

**AC2 — 列表接口** `[L1]`
`GET /api/v1/places` 未带坐标时按创建时间倒序，每项含：首图 · 名称 · 类型 · 距离位（此时 `null`）· 标签 · 照片数 · 评论数 · 推荐数 · 不推荐数。
- 列表项一次性**批量**取齐，**无 N+1**
- ⚠️ 本 story 内评论能力尚未交付，故评论数与两个计数**恒为 0**（字段先在契约里定下来，真实计数由 Story 1.8 接上）—— **不是前向依赖**

**AC3 — 对外标识** `[L0]`
返回体**不含自增 id**；场所标识一律是不可枚举 token。

**AC4 — 契约一致性（CROSS-STORY C5）** `[L0]`
后端字段集金标测试 + App 侧线上契约测试同时落地（详见 Dev Notes「契约测试怎么写」）。

**AC5 — 列表页渲染** `[L2]`
库里有场所时按 UX-DR2 渲染列表项（首图 + 名称 + 类型 + 标签 ≤2 个 +N + 照片数/评论数/推荐计数）。

**AC6 — 空态** `[L2]`
库里没有场所时显示空态，文案引导「标记一个场所」，复用既有空态组件。

**AC7 — 首页入口** `[L2]`
Sosial（首页 Feed）顶部新增**场所入口行**（单行导航条：图标 + 文字 + 箭头），点击进入场所列表页。
- ⚠️ **入口与列表页必须同条交付** —— 页面没有入口就不可达、无法验收
- AppBar（品牌标 + 通知铃）、分类 chips、瀑布流、底部 Tab **全部保留原样，一处不改**
- Epic 4 的宠物横滑行将加在本行**之下**，布局已在 UI 稿统一设计，**本 story 不需为其预留占位**

## Tasks / Subtasks

> 按 CLAUDE.md 纪律三段推进：**后端 → 前端 → 联调**，一次只碰一侧。

- [x] **T1 后端：建表与实体**（AC: 1）
  - [x] 新建迁移 `petgo-backend/src/main/resources/db/migration/V20260915_0409__init_places.sql`
  - [x] 纬度、经度用两个独立数值列（**不要**拼成字符串或 point 类型 —— Story 1.2 要对这两列各建索引）
  - [x] 新建模块 `com.tailtopia.place`，分层与 `com.tailtopia.content` 同构：`domain / repository / service / web / dto`
  - [x] `Place` 实体：枚举落库 `varchar` + UPPER_SNAKE；时间戳 `timestamptz` UTC
  - [x] token 生成沿用既有写法：32 位 BASE62 + `SecureRandom`（见 Dev Notes 引用）
- [x] **T2 后端：列表接口**（AC: 2, 3）
  - [x] `GET /api/v1/places`，返回 `PlaceListResponse` / `PlaceListItemResponse`（record）
  - [x] 批量聚合照片数/评论数/计数，**禁逐条查**
  - [x] 响应体不含自增 id
- [x] **T3 后端：契约测试**（AC: 4）
  - [x] 字段集金标测试（纯 Jackson 序列化，无 DB）
- [x] **T4 前端：场所列表页**（AC: 5, 6）
  - [x] 新建 `petgo_app/lib/features/place/{data,domain,presentation}`
  - [x] `api_paths.dart` 加常量；`app_router.dart` 加路由
  - [x] 列表页 + 空态（复用 `EmptyState`）
- [x] **T5 前端：首页入口行**（AC: 7）
  - [x] 在 `_BerandaTop` 内、分类 chips **之上**插入场所入口行
- [x] **T6 前端：线上契约测试**（AC: 4）
  - [x] 按 `test/profile/visitor_wire_contract_test.dart` 的形态写（用后端真实字段名走一次 `fromJson`）
- [ ] **T7 联调**（AC: 2, 5, 6, 7）—— ☁️ **云端不可做，L1/L2 待本地验收**
  - [ ] 真后端 + 模拟器：空库看空态、有数据看列表、点入口能进页面

## Dev Notes

### 🔴 三条最容易做错的

1. **不要预留打卡字段。** ⑧ 场所打卡在批次 B2，连 `content_posts` 的场所关联列都不属于本批次。"顺手预留一下"会让 B2 的建模被一个没讨论过的形状绑死。
2. **不要装地理扩展。** 本库**零扩展**（全部迁移里没有一条 `CREATE EXTENSION`）。距离排序是 Story 1.2 的事，口径已在 AD-2 定死为「纯 SQL + 范围粗筛 + 两列索引」。本 story 只要把经纬度**存成两个可建索引的数值列**即可。
3. **入口和页面必须一起交付。** 只做页面不做入口 = 不可达 = 无法验收。

### 可直接复用的现成件（别重造）

| 要做的事 | 用这个 | 位置 |
|---|---|---|
| 后端模块分层 | `content` 模块同构 | `petgo-backend/src/main/java/com/tailtopia/content/{domain,repository,service,web,dto}` |
| 不可枚举 token | 32 位 BASE62 + `SecureRandom` | `NotificationService.generateToken()`（`notify/service/NotificationService.java:182`）；同类先例 `PetProfile.cardToken` |
| 批量聚合计数 | `Map<Long, Long>` 批量模式 | `AdminContentManageService.likeCounts(...)` 的用法见 `admin/moderation/web/AdminContentManageController.java:143` |
| 后端契约测试 | `*ContractTest` | `petgo-backend/src/test/java/com/tailtopia/profile/dto/MilestoneListResponseContractTest.java` |
| App 空态 | `EmptyState`（`title` / `message` / `icon` / `actionLabel` / `onAction`） | `petgo_app/lib/shared/widgets/empty_state.dart` |
| App 线上契约测试 | 用**后端真实字段名**走一次 `fromJson`，断言解析出非零值 | `petgo_app/test/profile/visitor_wire_contract_test.dart` |

### 契约测试怎么写（AC4 的准确口径）

CROSS-STORY C5 原文写的是「后端 record + App data DTO + App mock + 契约 test 四处同步」。
⚠️ **`mock_backend.dart` 在当前代码里已经不存在了** —— 别去找、别新建。现在的等价做法是两条：

1. **后端**：字段集金标测试（纯 Jackson 序列化，无 DB）
2. **App**：线上契约测试 —— 用后端真实响应的字段名构造 JSON 走一次 `fromJson`，断言解析出**非零值**

> 第 2 条的存在理由（照抄那个测试的注释）：Dart 解析里的 `?? 0` 兜底会把字段名不匹配**悄悄吞掉** —— 页面照常渲染、不报错、日志里什么都没有，只是那个数字永远是 0。V1.1.6 真出过这个事故（后端 `diaryCount` vs 客户端 `happyMomentCount`）。

### 要改的既有文件（UPDATE —— 动手前先完整读一遍）

| 文件 | 现状 | 本 story 改什么 | 必须不破坏 |
|---|---|---|---|
| `petgo_app/lib/features/content/presentation/home_page.dart` | `_BerandaTop`（第 242 行起）= 品牌 AppBar + 通知铃 + 分类 chips；由 `header` 传给 `FeedMasonryView` | 在 chips **之上**插入场所入口行 | 通知铃角标、分类切换、游客态「Masuk」按钮 |
| `petgo_app/lib/core/network/api_paths.dart` | 集中式常量 | 加 `places` 路径 | 既有常量命名风格 |
| `petgo_app/lib/core/router/app_router.dart` | `StatefulShellRoute.indexedStack` 承载 4 Tab | 加场所列表路由 | ⚠️ **不要**加进 `_controlledLocations`（场所列表游客可看）；⚠️ **不要**把它做成 Tab 分支根 |

### 新建文件（NEW）

- 后端：`petgo-backend/src/main/java/com/tailtopia/place/**`、一条 Flyway 迁移
- 前端：`petgo_app/lib/features/place/{data,domain,presentation}/**`

### Project Structure Notes

- 命名映射链：DB `snake_case` ↔ Java/Dart `camelCase` ↔ JSON `camelCase`；表复数 snake_case；主键 `id`(bigint)；时间戳 `created_at`/`updated_at`(`timestamptz`, UTC)；枚举落库 `varchar` + UPPER_SNAKE
- API 前缀 `/api/v1`，资源小写复数连字符 → `/api/v1/places`
- 错误统一 RFC 9457 ProblemDetail，**绝不外泄堆栈**
- 日志 SLF4J + logback JSON；**禁记 PII / 令牌 / 签名 URL / 位置坐标**（NFR-4）
- `ddl-auto=validate` —— schema 归 Flyway；打包一律 `mvn -B clean package`（不 clean 会把旧迁移一起打进 jar，Flyway 报重复版本启动即崩）

### 验证层级与云端边界

- **L0**：`mvn -B clean package` / `flutter analyze` / `flutter test`
- **L1**：需 Docker + postgres + redis 真跑（AC2）
- **L2**：需模拟器视觉（AC5/6/7）
- ☁️ 云端 headless **只跑到 L0**；L1/L2 留本地，Completion Notes 必须写「L1/L2 待本地验收」，**story 不得标 done**

### References

- [Source: _bmad-output/planning-artifacts/v1.3.0/epics-v1.3.0-batch-b1.md#Story 1.1]
- [Source: _bmad-output/planning-artifacts/v1.3.0/architecture-v1.3.0-batch-b1-delta.md#AD-1]（场所存储与对外标识）
- [Source: _bmad-output/planning-artifacts/v1.3.0/architecture-v1.3.0-batch-b1-delta.md#AD-2]（为何经纬度要两个独立数值列）
- [Source: _bmad-output/planning-artifacts/v1.3.0/architecture-v1.3.0-batch-b1-delta.md#AD-6]（批量取数规则族）
- [Source: _bmad-output/planning-artifacts/v1.3.0/PRD-v1.3.0-batch-b1-places-social.md#2.1]（FR-112 ①②④⑥）
- [Source: _bmad-output/planning-artifacts/v1.3.0/ui-v1.3.0-batch-b1-places-social.html#B1-屏]（首页顶部统一区）
- [Source: _bmad-output/planning-artifacts/v1.3.0/ui-v1.3.0-batch-b1-places-social.html#A1-A3-屏]（列表三态）
- [Source: _bmad-output/implementation-artifacts/CROSS-STORY-DECISIONS.md#C3]（表名带模块前缀）
- [Source: _bmad-output/implementation-artifacts/CROSS-STORY-DECISIONS.md#C5]（契约一致性是可证伪验收）
- [Source: _bmad-output/implementation-artifacts/CROSS-STORY-DECISIONS.md#E7]（Flyway 时间戳制 + clean package）
- [Source: CLAUDE.md#实现一个-story-的纪律]

## Dev Agent Record

### Agent Model Used

云端 dev-story 会话（claude.ai/code，环境 = Flutter + Java 21 无 DB 的 L0 环境）。

### Debug Log References

- ⚠️ **环境偏差（记录备查，非本 story 改动）**：本次云端环境的 Flutter 是 **3.47.4**，
  而项目基线 / CI（`.github/workflows/frontend-ci.yml`）/ `scripts/cloud-setup.sh` 都钉 **3.44.1**。
  session 启动钩子的 `flutter pub get` 因此把 `pubspec.lock` 里 5 个包升了版
  （intl 0.20.3 / matcher 0.12.20 / meta 1.19.0 / test_api 0.7.12 / vector_math 2.4.2），
  `flutter analyze` 还会自动往 `analysis_options.yaml` 追加 exclude 段。
  **这两个文件已 revert、不进本 story 的 commit** —— 在 3.44.1 下 CI 会重新解析、这些升级从未被真正构建过，
  提交进去只会让 `--enforce-lockfile` 硬失败。SDK 基线要不要动是独立决策，不在本 story 范围。
- ⚠️ **`./mvnw -B clean package` 在无 DB 环境下不可能整体绿**：3090 个 testcase 里有 **1440 个**
  是 `@SpringBootTest`，启动即 `Connection to localhost:5432 refused`（属 L1）。
  本 story 的 L0 门槛因此定为：**编译（main 1163 + test 442 源文件）与打包全绿，且 surefire 里
  唯一的失败原因是「没有 postgres」** —— 实测 `真实失败=0`。
- ⚠️ 云端 locale 是 POSIX，而 `ScheduleWindowTest` 有中文 `@Nested` 类名，
  javac 会报 `bad filename`。跑 maven 前须 `LANG=C.utf8 LC_ALL=C.utf8`（既有测试的问题，未改它）。

### Completion Notes List

**已交付（L0 全绿）**

1. **`places` 建表**（AC1）：迁移 `V20260915_0409__init_places.sql`（时间戳版本号，E7）。
   名称 / 类型 / 标签集(JSONB) / **纬度、经度两个独立 `DOUBLE PRECISION` 列** / 文字地址 /
   描述 / 照片列表(JSONB) / 标记人 / 状态 / 不可枚举 `public_token`。
   - **零 `CREATE EXTENSION`**（AD-2 Rule 1/4：禁 PostGIS / cube / earthdistance）；
   - **无任何地图厂商 POI id 列**（AD-1 Rule 2：换厂商数据一行不动）；
   - **无任何 ⑧ 打卡预留列**（批次 B2，预留即越界）；
   - 取值域用 CHECK 守门（type / status / 经纬度区间），**加值必须 DROP + ADD 重列全集**。
   - 坐标选 `DOUBLE PRECISION` 而非 `NUMERIC`：Story 1.2 的直线距离要过三角函数（收 double），
     存 numeric 只会让每行多一次隐式转换。两列都能建索引，满足 AD-2 的范围粗筛要求。
2. **新模块 `com.tailtopia.place`**（AC1）：`domain / repository / service / web / dto` 与 `content` 同构。
   `PlaceTokenGenerator` = BASE62×32 + `SecureRandom`（沿用 `NotificationService.generateToken()` 形态，
   Dev Notes 指定）；不跨模块 import `profile` / `shop` 的同族生成器（§2 依赖图里没有那两条边）。
3. **`GET /api/v1/places`**（AC2/AC3）：`PlaceListResponse{items, sortMode}` +
   `PlaceListItemResponse{token,name,type,tags,firstPhotoUrl,photoCount,distanceMeters,
   commentCount,recommendCount,notRecommendCount}`。
   - **响应体不含自增 id**，对外只有不可枚举 token（AC3 / NFR-1）；
   - **只有一条 SQL，天然无 N+1**：照片数从行内 JSONB 长度算；评论数与两个态度计数
     本 story **恒为 0**（`place_comments` 归 1.7、Redis 计数器归 1.8）—— 字段现在就在契约里，
     免得 1.8 上线时客户端再改一次 DTO。服务层 javadoc 已写明「1.7/1.8 接真值时必须做批量 Map，
     不要在循环里逐条查」；
   - `sortMode` **由服务端下发**（`recent` / `distance`）：客户端带了坐标不等于服务端就走了距离分支
     （坐标非法、粗筛半径内无场所都会回落），客户端自己推会让距离位莫名空白。
4. **契约双向钉死**（AC4 / CROSS-STORY C5）：后端 `PlaceListResponseContractTest`（6 例，纯 Jackson 无 DB）
   + App `test/place/place_wire_contract_test.dart`（10 例，用后端真实字段名走 `fromJson`）。
   ⚠️ `mock_backend.dart` 已不存在，C5 的第三处按 Dev Notes 等价为这条线上契约测试。
5. **App 场所列表页 + 空态**（AC5/AC6）：`features/place/{domain,data,presentation}`；
   列表项按 UX-DR2 = 首图 + 名称 + 类型 + 标签(≤2 个 +N，**客户端截断**) + 照片数/评论数/👍/👎。
   推荐两个计数**列表页就出**（B1-D11）；空态复用既有 `EmptyState`。
6. **首页场所入口行**（AC7）：`_BerandaTop` 内、分类 chips **之上**插入 `PlaceEntryRow`
   （📍 + 文字 + ›，整行 ≥44 热区）。**AppBar / 分类 chips / 瀑布流 / 底部 Tab 一处未改**；
   **未为 Epic 4 的宠物横滑行预留占位**。路由 `/places` 走 `push`（保留返回栈），
   **未加进 `_controlledLocations`**（游客可看），**不是 Tab 分支根**。

**code-review 复审后修掉的（6 findings → 5 修 1 转本条）**

1. DTO 信封原名 `PlaceListPage` 与页面 widget **同名** → 改名 `PlaceListResult`。
   原样留着的话，Story 1.2 的页面或 widget 测试一旦同时 import 两者就 ambiguous import 编译失败。
2. `SecurityConfig` 原写 `GET /api/v1/places/**` 通配 → **收紧为精确路径 `/api/v1/places`**。
   通配等于替还不存在的端点预先授权：1.7/1.8 若加 `/places/{token}/my-reaction` 这类按调用者的读接口，
   它会默认匿名可达，而 controller 盲取的 currentUserId 是空 principal → 500 而不是 401。
   **子路径（详情 1.5 / H5 1.10）各自显式加一行**，每次放开都留痕。
3. 列表页原用 `async.when` → 改为「**有旧数据就保留旧数据 + SnackBar**」（CROSS-STORY F13）。
   原写法会让下拉刷新失败把已渲染的一整屏列表换成错误态，正是 F13 要避免的。
   `onRefresh` 也改成 `ref.refresh(...future)` 真的 await，刷新期间才有反馈。
4. `RefreshIndicator` 原只包列表 → **包住全部三态**，空态/错误态套可滚动容器。
   空态的 CTA 要等 1.3，如果空态还不能下拉，冷启动播种前进来的用户只能退出重进。
5. 列表查询原为**无上限全表读且端点匿名可达** → 加安全上限 `MAX_LIST_SIZE=200`（`Limit`）。
   **这不是分页**（不分页是 PRD ⑤ 既定决策，冷启动 10–20 个），是防「每个匿名请求都序列化整张表」。
   javadoc 写明：接近这个数就该上分页，不要调大。
6. （转 L1 清单）后端**没有** `PlaceControllerEndpointTest` 覆盖游客放行与倒序查询 ——
   这类 `@SpringBootTest` 在云端起不来（无 postgres），故列入下方 L1 清单第 1、5 条，**不在云端补写未验证的测试**。

**范围边界（本 story 有意不做，别当成漏做）**

- **类型 / 标签筛选 chips**（UI 稿 A1 顶部 `Jenis ▾ / Tag ▾`）不在 1.1 任何 AC 里；
- **距离位 + 「开启定位」提示条**（A2 变体）归 **Story 1.2**，本版 `distanceMeters` 恒为 null；
- **AppBar「+ 标记场所」与空态 CTA 按钮**归 **Story 1.3**（表单页还不存在）——
  空态只给引导**文案**：挂一个点了跳不到任何地方的按钮比没有按钮更糟；
- **列表项点击进详情**归 **Story 1.5**，故列表项当前不可点；
- **未新增任何地图依赖**（地图 SDK 归 Story 1.4）。

### 🔴 L1/L2 待本地验收

**L1（需 Docker postgres + redis 真跑）**

| # | 验什么 | 怎么验 | 对应 AC |
|---|---|---|---|
| 1 | 迁移能真的执行 + `ddl-auto=validate` 过 | 起服务 → `/actuator/health` = UP（validate 会逐列比对 `Place` 实体与 `places` 表） | AC1 |
| 2 | 列表按创建时间倒序 | 插 3 条场所 → `GET /api/v1/places` → 顺序为后建的在前 | AC2 |
| 3 | 无 N+1 | 开 `show-sql` / p6spy → 整个列表请求**只有一条 select**（不随场所数增长） | AC2 |
| 4 | 计数恒 0 且字段在 | 响应里 `commentCount`/`recommendCount`/`notRecommendCount` 都是 0 且**键存在** | AC2 |
| 5 | 🔴 游客可读 + 只读 | 不带 JWT `GET /api/v1/places` → **200**；`POST/PATCH/DELETE /api/v1/places` → 401/405（**无编辑端点**） | AC3 |
| 6 | 响应体无自增 id | 抓包看 JSON：无 `id`、无 `placeId` | AC3 |
| 7 | CHECK 约束真的挡 | 直接 SQL 插 `type='WAT'` / `latitude=91` → 被拒 | AC1 |

**L2（需模拟器 / 真机视觉）**

| # | 验什么 | 怎么验 | 对应 AC |
|---|---|---|---|
| 8 | 列表项渲染对齐 UX-DR2 | 库里放 3 条（含一条 3 标签、一条无照片）→ 首图/名称/类型/标签 ≤2+N/四个计数都对，无照片走占位不白屏 | AC5 |
| 9 | 空态 | 空库进页面 → 空态文案引导「标记一个场所」 | AC6 |
| 10 | 空态/错误态可下拉刷新 | 空态与首次失败态都能下拉；失败态有「重试」按钮 | AC6（F13） |
| 11 | 刷新失败保留已加载内容 | 有数据后断网下拉 → 列表**仍在**，只弹一条提示（不整屏变错误态） | AC5（F13） |
| 12 | 🔴 首页入口行 | Sosial 顶部出现单行导航条（📍+文字+›），点击进列表页；**AppBar 品牌标 + 通知铃角标 / 分类 chips 切换 / 瀑布流 / 底部 Tab 全部与改动前逐像素一致** | AC7 |
| 13 | 游客态 | 未登录点入口 → **直接进列表页**，不弹登录引导 | AC7 |
| 14 | 返回栈 | 从列表页按返回 → 回到 Sosial（不是退出 App） | AC7 |
| 15 | 印尼语文案 | 设备语言 id → 类型/标签/空态/错误文案均为印尼语，**界面无中文** | AC5/AC6 |

### File List

**新增 —— 后端**
- `petgo-backend/src/main/resources/db/migration/V20260915_0409__init_places.sql`
- `petgo-backend/src/main/java/com/tailtopia/place/domain/{Place,PlaceType,PlaceTag,PlaceStatus,package-info}.java`
- `petgo-backend/src/main/java/com/tailtopia/place/repository/{PlaceRepository,package-info}.java`
- `petgo-backend/src/main/java/com/tailtopia/place/service/{PlaceQueryService,PlaceTokenGenerator,package-info}.java`
- `petgo-backend/src/main/java/com/tailtopia/place/dto/{PlaceListResponse,PlaceListItemResponse,package-info}.java`
- `petgo-backend/src/main/java/com/tailtopia/place/web/{PlaceController,package-info}.java`
- `petgo-backend/src/test/java/com/tailtopia/place/dto/PlaceListResponseContractTest.java`

**新增 —— App**
- `petgo_app/lib/features/place/domain/place_summary.dart`
- `petgo_app/lib/features/place/data/place_repository.dart`
- `petgo_app/lib/features/place/presentation/{place_list_page,place_entry_row,place_labels}.dart`
- `petgo_app/test/place/place_wire_contract_test.dart`

**修改**
- `petgo-backend/src/main/java/com/tailtopia/shared/security/SecurityConfig.java`（GET `/api/v1/places` 放行，精确路径）
- `petgo_app/lib/core/network/api_paths.dart`（`places`）
- `petgo_app/lib/core/router/app_router.dart`（`/places` 顶层路由，**未动 `_controlledLocations`**）
- `petgo_app/lib/features/content/presentation/home_page.dart`（`_BerandaTop` 插入入口行）
- `petgo_app/lib/l10n/app_en.arb`、`petgo_app/lib/l10n/app_id.arb`（21 个 key，en/id 对齐）

---

## Change Log

| 日期 | 变更 | 作者 |
|---|---|---|
| 2026-09-15 | 实现完成，L0 全绿（后端编译+打包+非 DB 测试、`flutter analyze` 零问题、`flutter test` 1533 全过），code-review 复审并修掉 5 条，转 review（L1/L2 待本地验收） | dev-story（云端） |
