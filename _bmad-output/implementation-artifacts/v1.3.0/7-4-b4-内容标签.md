---
baseline_commit: 4301227a
branch: feat/1.3.0-ops-ui-refactor
epic: 7
story: 7.4
ad: [AD-9, AD-11]
decisions: [D-8]
page: B4
depends_on: [2-3a, 2-3b]
prd_exceptions: [§5 ③ 第 4 条（打标入口收拢）, 第 9 条（标签码自动生成 / 分配记录起止时间与状态）]
---

# Story 7.4: B4 内容标签（抽屉多页签，打标入口收拢）

Status: review

> 自包含 story。与用户沟通用中文。纪律见根 `CLAUDE.md`。依赖 2.3a / 2.3b。端点零变更（5 个 POST 路径参数不动）；变化在页面组织：主表只留标签一张表，分配记录进抽屉页签，独立「给内容打标」按钮废除。

## Story

As a 运营，
I want 内容标签页只看一张标签表，点开标签在抽屉里编辑、看分配记录、加内容，
so that 打标只有一个入口，不会在两张表之间迷路。

## Acceptance Criteria

**AC1 · 主表只有标签**
**Given** `/admin/content-tags` 套 `tpl-b-list`
**Then** 摘要条：生效标签数 · 生效中分配数；表格列 = 胶囊样式预览 ｜ 标签码（**系统自动 `ct-<id>` 不可填**，2026-09-02 代码已改）｜ 名称 ｜ 说明文案 ｜ 生效中分配数 ｜ 状态；现状页内第二张「分配记录」表与其筛选栏**删除** `[L2]`

**AC2 · 抽屉页签一「编辑」**
**When** 点行 → `GET /admin/content-tags/{id}/drawer`
**Then** 表单：名称（三语）/ 说明 / 胶囊样式 `badgeStyle`（枚举 `ContentTagBadgeStyle`，含「打标影响排序」提示 1.2.0 既有）/ 图标 `iconFile`（PNG / WebP，≤512KB，最短边 ≥42px，推荐 112×112，编辑时可不换）；未修改保存禁用 → `POST /admin/content-tags/{id}/edit`（multipart，参数不变）`[L1]`
**And** 下线 / 重新上线：`POST /admin/content-tags/{id}/retire`，data-confirm 复述**生效分配数**（沿用 `admin.tags.retireConfirm`）`[L1]`

**AC3 · 抽屉页签二「分配记录」**
**Then** 该标签生效中分配列表：内容（id + 摘要）｜ 打标时间 ｜ **起止时间（WIB，永久显「永久」）｜ 状态（生效中 / 已到期）** ｜ 操作人 ｜ 移除；分页 `[L2]`
**And** 「＋添加内容」= **唯一打标入口**：抽屉内选择器（`GET /admin/content-tags/pick`，正文关键词 / 帖子 ID，防抖 300ms）→ `POST /admin/content-tags/assign`（标签定死为当前抽屉标签，参数 `tagId` 隐藏传）`[L1]`
**And** 移除 → `POST /admin/content-tags/assignments/{id}/remove`，data-confirm `[L1]`
**And** 现状页内独立「给内容打标」区块与顶部按钮废除 `[L2]`

**AC4 · 新建标签**
**When** 右上「新建标签」→ 抽屉空表单（名称 / 说明 / 样式 / 图标必传）→ `POST /admin/content-tags`
**Then** 成功后**自动打开新标签抽屉并停在「分配记录」页签** `[L2]`
**And** 标签码不出现在表单里（自动生成）`[L1]`

**AC5 · 权限与 htmx**
**Then** 查看 `content.tag_view`（`CONTENT_TAG_VIEW`）；写 `content.tag_manage`（`CONTENT_TAG_MANAGE`）；无 manage 抽屉操作禁用注明；全部 htmx 200/422/403 fragment `[L1]`

**AC6 · 三语与回归**
**Then** `admin.v130.tags.*` 三包；`AdminContentTagIntegrationTest` 全绿；四条 MockMvc；图标校验（`AdminTagIconService.MIN_SIDE=42`、`MAX_BYTES=512KB`）回归 `[L0/L1]`

---

## Tasks / Subtasks
- [x] **T1** 摘要条聚合 + 分配记录查询增加 `status`（生效中 / 已到期，按 `ends_at` vs WIB now）字段
- [x] **T2** `content-tags.html` 套模板 B；删「给内容打标」区块与「分配记录」表；主表加胶囊预览列
- [x] **T3** `fragments/drawer-content-tag.html` 两页签（htmx 局部切换）；新建态复用同 fragment
- [x] **T4** 5 个 POST 加 `HxRequest` 分支；新建成功响应 `HX-Trigger: {"admin:drawer-open": {"url": "/admin/content-tags/{id}/drawer?tab=assignments"}}`
- [x] **T5** 三语 + 测试；**T6** 云端 L0，抽屉 L2 留本地

## Dev Notes

### 现状与改动边界
| 文件 | 现状 | 本 story | 保留 |
|---|---|---|---|
| `admin/contenttag/web/AdminContentTagController.java` | `GET /admin/content-tags`（VIEW）、`GET /pick`（VIEW）、`POST /admin/content-tags`（MANAGE）、`POST {id}/edit`、`POST {id}/retire`、`POST /assign`、`POST /assignments/{id}/remove` | + `GET {id}/drawer`、`GET new/drawer`；POST 加 htmx 分支 | 5 个 POST 参数 |
| `admin/tagicon/AdminTagIconService.java` | `MIN_SIDE=42`、`MAX_BYTES=512KB`、PNG/WebP | 不动 | 全部 |
| `templates/admin/content-tags.html` | 标签表 + 行内编辑/下线表单 + 页尾「新建标签」+「给内容打标」+「分配记录」表（已显示 startsAt / endsAt / 永久） | 套模板 B；三个区块收进抽屉 | `pick` 交互、`admin.tags.*` key |

### 🔴 起止时间已有，缺的是「状态」列
现状分配记录已显示 `startsAt` / `endsAt` / `permanent`（`content-tags.html` 212-232）。PRD §5 ③ 第 9 条要的增量是**状态列**（生效中 / 已到期）与把这张表挪进抽屉。不要重做时间字段。

### 🔴 标签码
2026-09-02 起 `ct-<id>` 由服务层生成，表单里若还有 `code` 输入请删掉（grep `name="code"`）。

### 端点零变更清单
`POST /admin/content-tags` · `{id}/edit` · `{id}/retire` · `/assign` · `/assignments/{id}/remove`。新增 GET：`{id}/drawer`、`new/drawer`。

### 与 Story 8.2 的同构
用户标签（B8）与本页同动线；8.2 复用本 story 的抽屉页签结构，但**没有徽章底色预设**（`UserTagBadgeColor` 已废弃）。先做本 story，8.2 照抄壳。

### References
- [Source: 后台重构逐页规格.md#B4 · 内容标签]
- [Source: ui-v1.3.0-admin.html 泳道 2 帧 2-14～2-18]
- [Source: PRD-v1.3.0-admin.md#5 ③ 第 4 / 9 条]
- [Source: epics-v1.3.0-admin.md#Story 7.4]

## Dev Agent Record

### Agent Model Used

claude-opus-5（云端 headless session，2026-09-10）

### Debug Log References

- 云端 L0：`./mvnw -B clean package`（排除需真库的 Spring 上下文测试类）→ BUILD SUCCESS，1833 tests，0 failures。**无迁移**；5 个 POST 端点零变更。
- `check-i18n-keys.sh` OK（四包 2733 key 集合相等、无重复；+20 `admin.v130.tags.*` / `admin.err.contentTag.postRequired`，−7 条随页尾三块一起废掉的 `admin.tags.assign*` / `byTag` / `byPost` / `postId`）。
- 云端 L0 兜底渲染：`fragments/tags-list :: rows` / `drawer-content-tag` 的 `drawer`(edit / assignments / assignments-retired) / `createForm` / `created` / `afterAction` 七个片段各真渲染一遍（把 `#authorization.expression` 与 `@adminTime` 两处替换掉），全 PASS —— 抽屉模板在云端没有 L1 可跑，解析错 / 表达式错必须在这里拦住。
- 复审（bmad-code-review）：**CONFIRMED 5 条全修 + PLAUSIBLE 6 条已修 + 2 条记录待拍板**。
  - ① 🔴 **排期中的分配被显示成「已到期」**：状态只有「生效中 / 已到期」两档，而本页明确支持「先排期、后生效」（打标表单的开始时间可以填未来，`admin.wibNow` 那句提示就是为此）。运营给一条内容排下周的标，打完就看到「已到期」，合理解读是排期作废 → 再打一次 → 同一内容同一标签重复分配。改：`AssignmentRow` 加 `pending`（`startsAt > now`）与 `expired()`，状态列改三态（待生效 / 生效中 / 已到期），+1 key。
  - ② 🔴 **不选内容直接提交 → 400 → 界面完全没反应**：候选是一排 radio、无 `required`，控制器 `postId` 是必填 `long` → 缺参数抛的是 **400 ProblemDetail**，而 `admin-core.js` 的 `htmx:beforeSwap` 只放行 422/403/404 → 响应被丢弃、err 槽空白、按钮弹回。改：radio 加 `required`（第一道）+ `postId` 改 `Long` 后自己抛 `AppException.validation`（判定点仍在服务端，422 落 `#tag-assign-err`）。⚠️ 顶置页那个同形的 `contentId` **刻意不动** —— 它在「推广卡片」类型下整个 fieldset 被 `hidden`，给隐藏的 radio 加 `required` 会让表单永远提交不了且没有任何提示，比现状更糟。
  - ③ 🔴 分配记录的「起止时间」列头误用了打标表单的字段标签（`admin.tags.window` = 「生效时间（WIB，结束可留空 = 永久）」）：只读表里出现「结束可留空」这种填表指令读不通，且它带 `nowrap`，单这一列 200px+，六列在 480px 抽屉里必横滚（右侧「取消打标」按钮默认看不见）→ 新增 `admin.v130.tags.col.window`。
  - ④ 🔴 **新加的 L1 测试对状态列是假绿**：三条夹具的时间全排在同一个未来日期上 ⇒ 三条在页面上是同一个状态，`.contains("已到期")` 恒真，把 `active` 写死成 false 也照样绿 —— 它本该抓住 ①。改：夹具改成相对**此刻**（−2h~−1h 已到期 / −1h 永久 生效中 / +7d 待生效），三态各断一次且互不相同。
  - ⑤ 🔴 列表页把 N+1 跑了两遍、抽屉页签二把整张分配表拉进内存：`summary()` 内部又调一次 `listTags`（30 个标签 = 62 次查询，每次 `admin:tag-list-refresh` 重来一遍），`assignmentPage` 先取全量再在 Java 里切片。改：`summary` 改成接收算好的行的**纯函数**（顺带消掉两次 `Instant.now()` 的跨秒不一致）、生效中分配数换成一条 `group by tag_id` 聚合、分页下推到 DB（`Page` + 越界回退最后一页）。
  - ⑥ 分配记录页签底部恒空 340px：`.drawer-body:has(input[type="datetime-local"])` 按元素存在判，而收起的 `<details>` 里那两个输入框仍在 DOM 里 → 该规则排除 B4，改由 `.tag-assign-details[open]` 自己留白。
  - ⑦ 新建表单挂了半套原生降级（`th:action` + `method` + `enctype`）却没有 CSRF 隐藏域，而它只存在于 htmx 载进来的抽屉里 —— 去掉，与另外三张表单一致。
  - ⑧ 「未修改保存禁用」的 `disabled` 原本写死在 HTML 里、靠内联脚本解开：将来上 CSP（htmx 的 `inlineScriptNonce` 是空串）脚本被拦 ⇒ 保存钮**永久禁用**、编辑整个不可用。反过来改成脚本初始化时加禁用，失效时退化成「总能点」。
  - ⑨ 「操作人」整列可能是「—」，界面上补一句说明（+1 key）—— 不写的话运营第一反应是功能坏了。
  - ⑩ 摘要条文案与状态列口径统一为「在线标签数」（AC1 字面是「生效标签数」，偏离见下）。
  - ⑪ 清掉随页尾三块一起作废的死代码：`assignableTags` / `assignmentsByTag` / `assignmentsByPost` 三个服务方法与 7 条 i18n key。
  - ⑫ `AdminHxEvents.DRAWER_OPEN` 与 `admin-drawer.js` 的注释把新事件误记成「2.3a」，对外契约也没跟上新的 `open(url, {res, rowEl, openId, keepOpenParam})` → 改正。
  - **复审逐条核过、确认无缺陷**：5 个 POST 端点零变更；`hx-target` 全部指向当前分支里真实存在且带 id 的元素；`hx-swap-oob="innerHTML:#tag-drawer .drawer-body"` 的选择器与 `tpl-b-list` 的 `res='tag'` 对得上；刷新监听 span 在 `#tags-rows` 之外不会被自己换掉；`HX-Trigger` 载荷 → htmx `ce()` → body 监听 → `open()` 整条链成立；权限表达式两侧逐字一致且只读身份渲染不出任何 `hx-post`；审计窄接口仍只读。

### Completion Notes List

- **L1/L2 待本地验收**：① 新增 `AdminContentTagDrawerIntegrationTest`（真库 + 真 Thymeleaf）：整页模板 B 壳 + 摘要条两格 + `#tags-rows` + `#tag-drawer`、行 `data-drawer-url`、「＋新建标签」是抽屉入口、**页尾三块（新建标签 / 给内容打标 / 分配记录表）都已消失**；htmx 局部刷新只回表格 + oob 摘要条；抽屉页签一（表单带当前值、无 `code` 字段、未修改保存禁用、err 槽带 id、下线确认复述生效分配数、×1.3 提示随表单进抽屉）；页签二（内容 / 打标时间 / 起止（永久）/ 状态（待生效 · 生效中 · 已到期三态各断一次）/ 操作人五列 + 分页越界回退 + ＋添加内容）；已下线标签不渲染打标表单只给原因；改标签（htmx）落库并回抽屉 oob + toast + `HX-Trigger: admin:tag-list-refresh`；**新建成功回 `HX-Trigger: admin:drawer-open` 带新标签抽屉 URL（tab=assignments）**；打标 / 取消打标回分配记录页签；私密内容打标 422 落在 `#tag-assign-err` 且不落库；只持 `content.tag_view` 看得到抽屉但拿不到任何写入口、`new/drawer` 与 `{id}/edit` 403。② 既有回归：`AdminContentTagIntegrationTest` 全绿（私密内容不可打标、下线只挡新分配、标签码自动生成、胶囊底色下发色值、双权限码、×1.3 提示）。③ L2：本地 stag 对照 UI 稿 2-14～2-18：480px 抽屉两页签切换、胶囊预览、内容选择器 300ms 防抖与分页、`?create=1` / `?open=` 深链、新建后自动停在分配记录页签。
- **AC1 主表只有标签**：`content-tags.html` 套 `tpl-b-list`（res=tag）；摘要条两格（在线标签数 · 生效中分配数）；表格列 = 胶囊预览 ｜ 标签码 ｜ 名称 ｜ 说明文案 ｜ 生效中分配数 ｜ 状态。页内第二张「分配记录」表与其筛选栏、页尾「新建标签」与「给内容打标」两块**全部删除**。
- **AC2 抽屉页签一「编辑」**：`GET /admin/content-tags/{id}/drawer`（`VIEW`，`tab=edit` 默认）→ `fragments/drawer-content-tag :: drawer`；名称 / 说明 / 胶囊底色 / 图标（可不换）+ 未修改保存禁用；下线 / 重新上线沿用 `admin.tags.retireConfirm` 并补一句生效分配数。
- **AC3 抽屉页签二「分配记录」**：`?tab=assignments`；**含已到期**（AC3 要的状态列正是为此）、打标时间倒序、每页 20、分页；「＋添加内容」是唯一打标入口，`tagId` 隐藏传。
- **AC4 新建标签**：右上「＋新建标签」→ `GET /admin/content-tags/new/drawer`（`MANAGE`）→ `:: createForm`；成功回 `HX-Trigger: {"admin:drawer-open":{"url":"/admin/content-tags/{id}/drawer?tab=assignments","id":{id}}}` + `admin:tag-list-refresh`。
- **AC5 权限与 htmx**：5 个 POST 路径与参数逐字不变，只加 htmx 分支；失败交给 `AdminBusinessExceptionAdvice` 出 422 / 403 行内错，目标一律**带 id 的**槽。
- **AC6 三语与回归**：20 个 key 四包同批，另删 7 条随页尾三块作废的旧 key。
- **偏差说明**：① **状态列是三态**（待生效 / 生效中 / 已到期），AC3 字面只写了两态 —— 两态会把「先排期、后生效」这种本页支持的用法写成「已到期」（复审 ①）。② **摘要条第一格叫「在线标签数」**，AC1 字面是「生效标签数」：同屏的状态列写的就是「在线 / 已下线」，两个词指同一件事会让人以为「生效」另有含义。③ **`POST /assign` 的 `postId` 从 `long` 改成 `Long`**（路径与参数名不变），只为把「没选内容」变成看得见的 422 而不是被 htmx 丢弃的 400（复审 ②）。④ **`GET /admin/content-tags` 悄悄少了 `tagId` / `postId` 两个查询参数** —— 它们是页内第二张表的筛选条件，随表一起废；全仓已无链接指过来，但旧书签 `?tagId=3` 现在是**静默忽略**（不报错也不筛选）。⑤ 分配记录的**分页下推到 DB**（`Page` + 越界回退最后一页），不再取全量再切片。
- **待拍板（记录，未自行改）**：① **分配记录的「操作人」只对经后台打的标可查** —— 分配表没有操作人列，现取自审计里那条 `CONTENT_TAG_ASSIGN`；要让它恒有值得给 `content_tag_assignments` 加列 + 迁移，超出本 story 的增量范围（Dev Notes 明确写了增量只是「状态列 + 挪进抽屉」）。界面上已补一句说明。② 旧书签 `?tagId=` 的静默忽略是否需要一个「该筛选已并入抽屉」的提示。

### File List

- petgo-backend/src/main/resources/templates/admin/content-tags.html（套模板 B；删页尾三块；`candidates` / `chip` 片段原位保留）、fragments/tags-list.html（新增）、fragments/drawer-content-tag.html（新增）
- petgo-backend/src/main/java/com/tailtopia/admin/contenttag/web/AdminContentTagController.java、service/AdminContentTagService.java、dto/{TagSummary(新增),AssignmentRow}.java
- petgo-backend/src/main/java/com/tailtopia/admin/shared/web/{AdminHxEvents,AdminFragmentResponses}.java（+`TAG_LIST_REFRESH` / `DRAWER_OPEN` / 带载荷的 `trigger`）
- petgo-backend/src/main/java/com/tailtopia/admin/audit/repository/AdminAuditLogRepository.java（+只读批量查询）、content/domain/ContentTagAssignment.java（+`getCreatedAt`）、content/repository/ContentTagAssignmentRepository.java（+按标签分页 + 生效中条数聚合）
- petgo-backend/src/main/resources/static/admin/admin-drawer.js（+`admin:drawer-open` 监听与 `openId`）、admin.css（`.tags-table` / `.tag-*`）、i18n/messages{,_zh_CN,_en,_id}.properties（+20 key，−7 条作废旧 key）
- 测试：admin/contenttag/AdminContentTagDrawerIntegrationTest（新增 L1）、AdminContentTagIntegrationTest（WIB 那条改断抽屉）、admin/shared/web/AdminFragmentResponsesTest（+载荷保全）、admin/web/AdminTemplateStructureTest（打标表单守卫改判据）
