---
baseline_commit: 4301227a
branch: feat/1.3.0-ops-ui-refactor
epic: 9
story: 9.1b
ad: [AD-9, AD-11]
decisions: [D-11, D-23]
depends_on: [9-1a]
---

# Story 9.1b: B20 兽医账号——资质页签与评分并入

Status: review

> 自包含 story，可本地或云端 L0 执行。与用户沟通用中文。执行纪律见根 `CLAUDE.md`。
> 本 story 在 9.1a 的抽屉壳里填充「资质」「评分」两个页签，并退役三个独立页；**写端点零变更**、零迁移。

## Story

As a 运营，
I want 在兽医抽屉里直接录入 / 续期 / 审核资质，并看到这位兽医的评分明细，
so that 资质审核和评分查看不再各占一个页面。

## Acceptance Criteria

**AC1 · 资质页签（vet-qualification 全字段并入）**
**Given** 9.1a 抽屉已存在
**When** 切到「资质」页签（`GET /admin/vets/{id}/drawer?tab=qual` 或页签内 hx-get 片段，`vet.qualify_view` 或 `vet.qualify`）
**Then** 「当前资质」卡：状态（`QualificationStatus`）+ 驳回理由（若有）+ 证图预览（KTP / SIPDH / STRV / 学位证 / PDHI / 档案照，走既有预签名 URL，**证件 key 与签名 URL 绝不落日志审计**）`[L1]`
**And** 「直录 / 续期」表单：KTP 编号；SIPDH 编号 / 机构 / 有效期 / 证图 key；STRV 编号 / 机构 / 有效期 / 证图 key；学位证 / PDHI / 档案照 key；专长（多选）；`mode=record|renew` 二选（续期只更新证件与有效期，不重置认证状态——UI 稿 6-5）；提交 `POST /admin/vets/{id}/qualification`（`vet.qualify`），`QualificationForm` 字段名不变 `[L1]`
**And** 审核区：通过 `POST …/qualification/approve`；驳回 `POST …/qualification/reject` 理由必填（`reason`），data-confirm `[L1]`
**And** htmx：成功返抽屉 fragment + oob 行（资质态列刷新）；422 行内 err；403 禁用态注明缺 `vet.qualify` `[L1]`

**AC2 · 评分页签（vet-ratings 并入）**
**When** 切到「评分」（`rating.view`）
**Then** 摘要：均分 / 已评数 / 未评数 / 总问诊量；逐条评价列表（星级 / 评价 / 时间 WIB）；「未评问诊单」区（会话 / 原因 / 时间，Story 6.2 既有 `unratedConsults`）`[L2]`
**And** 无 `rating.view` 时页签渲染为禁用并注明所缺权限，不 403 整个抽屉 `[L1]`

**AC3 · 评分总览页退役，摘要指标并入列表**
**Given** `GET /admin/ratings`（`ratings.html`，按均分 / 问诊量排序的总览）与 `GET /admin/vets/{id}/ratings`（`vet-ratings.html`）
**Then** 两路由方法与两模板删除；列表页「评分」列保留均分数字并支持按均分 / 问诊量排序（`sort=avgDesc|avgAsc|volumeDesc`，复用 `AdminRatingService.overview` 的排序逻辑）；`from` / `to` 评价时间段筛选并入列表筛选栏 `[L1]`
**And** `templates/admin/vet-qualification.html` 与 `GET /admin/vets/{id}/qualification` 删除 `[L1]`
**And** 导航「兽医与问诊」组去掉「评分查看」（`admin.nav.ratings`）；旧地址 404 不跳转（D-23）`[L1]`

**AC4 · 写端点零变更**
**Then** `POST /admin/vets/{id}/qualification`、`…/approve`、`…/reject` 路径 / 参数 / `@PreAuthorize` 不变 `[L0]`

**AC5 · 三语 + 测试 + 回归**
**Then** 新 key `admin.v130.vets.qual.*`、`admin.v130.vets.rating.*` 三包同批 `[L0]`
**And** 既有 `vetqual/VetQualificationIntegrationTest`、`VetQualificationReviewIntegrationTest`、`GrandfatherQualificationIntegrationTest`、`SipdhExpiryScanIntegrationTest`、`rating/AdminRatingOverviewIntegrationTest`、`VetRatingDetailIntegrationTest` 全绿（总览 / 详情两页的 GET 用例改为列表排序 + 抽屉页签断言）`[L1]`

---

## Tasks / Subtasks

- [x] **T1 · 资质页签 fragment**（AC1）
  - [x] `fragments/drawer-vets.html` 填 `qual` 页签：把 `vet-qualification.html` 116 行的表单与「当前资质」区搬入，i18n 继续用 `admin.qualpage.*` 既有 key（不改名）
  - [x] `AdminVetQualificationController.qualification()`（GET）改为返页签 fragment（`HX-Request`）；`save` / `approve` / `reject` 三 POST 加 HX 分支（成功返 fragment + oob 行，422 行内）
  - [x] 证图预览沿用现状预签名逻辑；确认 controller 类注释「证件 key/签名 URL/证件号绝不落日志/审计」在 fragment 路径同样成立（不把 form 打进 debug 日志）

- [x] **T2 · 评分页签**（AC2）
  - [x] `vetRatings()` 改为返页签 fragment；model 沿用 `adminVetService.ratings(id)` + `unratedConsults(id)`
  - [x] 缺 `rating.view` → 页签禁用态 fragment（`sec:authorize` 在抽屉壳内判）

- [x] **T3 · 评分总览并入列表 + 退役**（AC3）
  - [x] `AdminWebController.vets(...)` 增 `sort` / `from` / `to` 参数；`AdminVetService.list` 支持按 `AdminRatingService.overview` 的排序键排序（或直接注入 `AdminRatingService`）
  - [x] 删 `AdminRatingController.overview()`（若类内仅此一方法则整类删）、`vetRatings()`、`AdminVetQualificationController.qualification()` 的整页分支
  - [x] `git rm` `ratings.html`、`vet-ratings.html`、`vet-qualification.html`
  - [x] 导航去「评分查看」；`grep -rn "/admin/ratings\|/ratings'\|/qualification'" templates/admin` 为零（保留 POST action 的 `/qualification` 路径）

- [x] **T4 · 测试**（AC5）
  - [x] 改 `AdminRatingOverviewIntegrationTest`：断言列表 `sort=avgDesc` 顺序
  - [x] 改 `VetRatingDetailIntegrationTest`：断言 `GET /admin/vets/{id}/drawer?tab=rating` 含评分明细
  - [x] `vetqual/web` 下用例：GET 页面 → GET 页签 fragment；POST 三条不变

- [x] **T5 · 云端执行须知**
  - [x] L0 云端；L1（真库 + OSS 预签名 mock）与 L2 视觉留本地

---

## Dev Notes

### 现状代码要点

| 文件 | 现状 | 本 story 改什么 | 必须保留 |
|---|---|---|---|
| `admin/vetqual/web/AdminVetQualificationController.java` | GET `/admin/vets/{id}/qualification`（VIEW_AUTH = `vet.qualify_view` 或 `vet.qualify`）；POST 同路径 `mode=record\|renew` → `recordByOps` / `renew`；POST `approve` / `reject(reason)` | GET 改 fragment；POST 加 HX 分支 | 三个 POST 的路径、参数、`AUTH`；service 层审计 |
| `admin/vetqual/service/VetQualificationService.java` | `recordByOps` / `approve` / `reject` / `renew` / `expiryStats` / `scanExpiry` | 不动 | 全部 |
| `admin/vetqual/domain/VetQualification.java` | ktpNo / ktpPhotoKey / sipdh{No,Issuer,Expiry,PhotoKey} / strv{…} / degreePhotoKey / profilePhotoKey / pdhiPhotoKey / specialties / status / rejectReason | 不动（字段全展示） | — |
| `admin/rating/web/AdminRatingController.java` | GET `/admin/ratings`（`rating.view`）总览 `sort/from/to` | 删；逻辑并入列表排序 | `AdminRatingService.overview` 服务方法 |
| `admin/web/AdminWebController.vetRatings` | GET `/admin/vets/{id}/ratings` → `vet-ratings.html` | 改 fragment | `ratings(id)` / `unratedConsults(id)` |
| `templates/admin/vet-qualification.html`（116）/ `vet-ratings.html`（65）/ `ratings.html`（66） | 三个独立页 | **删除** | i18n key `admin.qualpage.*`、`admin.ratings.*`、`admin.ratingOverview.*` 复用 |
| `layout.html` 107-108 | 「评分查看」导航项 | 删 | — |

### 🔴 「未成功请求」维度的评价不并入

UI 稿 6-6 caption：「未成功请求维度评价（含原因）仍在独立页 6-8（低频取证场景，不并入）」。评分页签只并入 `vet-ratings.html` 的内容（均分 / 明细 / 未评问诊单）；不要把 B21 的东西搬进来。

### 🔴 续期语义

`mode=renew` 走 `VetQualificationService.renew`：只更新证件与有效期，**不重置认证状态**；`mode=record` 走 `recordByOps`。表单里两个提交钮（`admin.qualpage.submitRecord` / `submitRenew`）分别带 `mode`，现状如此，保留。

### 🔴 SIPDH 到期扫描不受影响

`SipdhExpiryScanner`（03:00 UTC）读同一张表；本 story 只改展示层，回归 `SipdhExpiryScanIntegrationTest` 即可。生产曾把兽医 4 的 `sipdh_expiry` 置空以免被扫回 EXPIRED（memory），不要在录入表单加「有效期必填」。

### 与 9.1a 的边界

页签容器 id 沿用 9.1a 的 `#vets-drawer-tab-qual` / `#vets-drawer-tab-rating`；只填内容不改壳。

### 测试标准

沿用 `test/.../admin/vetqual/**`、`admin/rating/**` 既有集成测试类；fragment 断言不含 `<html`。

### Project Structure Notes

改动落 `admin/vetqual/web`、`admin/rating/web`、`admin/web/AdminWebController`、`fragments/drawer-vets.html`；删除三个模板与一个 Controller（或方法）。

### References

- [Source: epics-v1.3.0-admin.md#Story 9.1b] · [Source: 后台重构逐页规格.md#B20 / C6] · [Source: ui-v1.3.0-admin.html 泳道 6 帧 6-5 / 6-6] · [Source: 决策日志-admin.md D-11 D-23]
- [Source: petgo-backend/src/main/java/com/tailtopia/admin/vetqual/web/AdminVetQualificationController.java]
- [Source: petgo-backend/src/main/java/com/tailtopia/admin/rating/web/AdminRatingController.java]

## Dev Agent Record

### Agent Model Used

云端 headless session（claude.ai/code），**L0 only**。

### Debug Log References

- L0：`cd petgo-backend && ./mvnw -B clean package` → `Tests run: 1838, Failures: 0, Errors: 0, Skipped: 6` / BUILD SUCCESS
- `bash scripts/ci/check-i18n-keys.sh` → OK（2850 个 key，四包集合相等）
- `bash scripts/ci/check-flyway-versions.sh origin/main` → OK（本 story **无新增迁移**）
- 云端无 Docker（跑不了 L1）：离线 Thymeleaf 渲染探针（`VetRender`）扩到 26 组，
  **按「有权限」/「只读」各跑一遍**，全绿。

### Completion Notes List

**L1 / L2 待本地验收**（云端 headless，无 Docker、无渲染）：本 story 全部 `[L1]` / `[L2]` 项，
尤其 `AdminVetDrawerIntegrationTest` 新增的 7 条、既有 `vetqual/**` 与 `rating/**` 集成测试的回归，
以及**证件图预签名 URL 的真实预览**、**页签懒加载的真实交互**。

**AC 落地**

- AC1：资质页签 = 整页 `vet-qualification.html` 的四段逐字平移（当前资质 + 六张证图 + 审核区 +
  直录 / 续期），i18n 继续用 `admin.qualpage.*` 一个都没改名；三个写端点的路径 / 参数 /
  `@PreAuthorize` 逐字未动，只加 htmx 分支。
- AC2：评分页签 = `vet-ratings.html` 的两段（均分明细 + 未评问诊）；**未把 B21 的「未成功请求维度评价」
  搬进来**（UI 稿 6-6：低频取证场景，留在独立页）。缺 `rating.view` 时页签禁用 + 注明所缺权限，
  不 403 整个抽屉。
- AC3：`ratings.html` / `vet-ratings.html` / `vet-qualification.html` 三个模板与
  `AdminRatingController` 删除；排序（三个键）与「问诊时间段」并入兽医列表筛选栏；
  侧栏「评分查看」删除。
- AC4 / AC5：写端点零变更；`admin.v130.vets.qual.*` / `admin.v130.vets.rating.*` 三语同批。

**🔴 两个页签做成懒加载**（本 story 最重要的实现选择）

资质页签一打开要签**六个证件图的短 TTL 预签名 URL**。跟着抽屉一起渲染的话，等于每开一次
任意兽医的抽屉就凭空签六个 —— 而大部分时候根本没人看那一页。改为
`hx-trigger="click from:[data-vtab='qual']"` 懒加载；资质处置成功也**只换那一块**
（整体重渲染会把两个懒加载页签打回未加载态，运营刚看的证件图与评分明细全没了）。

**复审（对抗式）修掉的 11 条**

高：
1. 🔴🔴 **排序键对不上，三个选项里两个静默失效**。筛选栏按 AC3 的行文用了 camelCase
   （`avgDesc`），而服务层常量一直是 snake_case（`avg_desc`）—— 未知值落进 `default`
   = 均分倒序。于是「均分低→高」和「问诊量多→少」都变成「均分高→低」，
   **而下拉框仍高亮着运营选的那一项，界面上一点异常都看不出**，只能靠人肉核对数字。
   唯一碰巧对的是 `avgDesc`（撞进 default），也正是当时那条新单测唯一验的方向。
   改为：模板用服务层常量（snake_case，顺带让退役那页发出去的旧书签继续有效）+
   服务层加一层归一（两种拼法都认）+ 新增 `AdminRatingSortKeyTest` **逐个钉住六种拼法**
   与「三个键各不相同」。
2. 🔴🔴 **评分数据从 `rating.view` 降级到了 `vet.view`**。退役前 `GET /admin/ratings` 归
   `rating.view` 独占，抽屉评分页签也仍守它；但并进列表的那一份一道 rating 门都没有 ——
   只有 `vet.view` 的 STAFF 打开 `/admin/vets` 就能拿到全量兽医的「已评 / 总问诊量」、
   按均分排序、按时间窗切片，等于把整张退役页白拿。与「安全规则层只升不降」直接相抵触。
   改为：控制器按码判一次，**没有就不装**（服务端不查不返，不是模板里藏），筛选栏那两块与
   评分列一并不渲染；新增断言「手改 URL 带上排序参数也拿不到评分列」。
3. 🔴 **9.1a 的抽屉测试留着已删除的占位断言**（`vet-qual-placeholder`），L1 一跑必红。
   云端没 Docker 所以看不出来。已同步为懒加载槽的断言。

中：
4. 🟠 **「消除 N+1」实为放大**：`AdminRatingService.overview` 为每个兽医拉全部 CLOSED 会话、
   再逐会话查评分（那个类自己写着「低频总览页可接受」）。挂到每次列表渲染上，查询量从
   O(兽医数) 变成 **O(全部已结束会话数)** —— 而这一页三个下拉是 autosubmit，每变一次就重拉。
   改为：**给了排序或时间窗才走总览**（= 运营真的在做评分这件事），否则沿用原来
   「每行一次 forVet」的便宜路径、只出均分；两个计数也只在那时才装（也只有那时才带窗口口径）。
5. 🟠 **抽屉与列表的「总问诊量」口径分叉**：抽屉的「已评」是该兽医全部评分行（不分会话状态、
   无时间窗）、「未评」含 INTERRUPTED；列表的总量只数 CLOSED 且受时间窗约束。
   两个数并排出现就会是「列表 12 / 抽屉 15」这种没人解释得清的组合。
   改为**抽屉不给那个数**（宁可不给，也不给一个会跟旁边打架的数），并写明两处口径不同。
6. 🟠 **懒加载的 `hx-target="this"` 落在无 id 的元素上**：htmx 只在目标带 id 时才发
   `HX-Target` 头，4xx 会被 retarget 到 `#admin-inline-error`（表格下方，抽屉打开时看不见）；
   叠加 `once` 之后**再点页签也不会重试**，页签永远停在「正在加载资质…」。
   改为给两个槽加 id + **去掉 `once`**（每次点页签重拉，顺带解决短 TTL 预签名 URL 过期的问题）。
7. 🟠 **直录成功后表单原样回显刚提交的证件号与六个 key**（`@ModelAttribute` 已把它放进同一个
   Model，而 `populateQual` 只在「没有」时才塞新的）。整页时代 `redirect:` 之后是空表单。
   改为成功分支显式换成空表单。

低：
8. 🟡 **「评价时间段」筛的其实是问诊发生的时间**（会话 `created_at`）：9/1 的问诊 9/20 才补评，
   按「9/15–9/30」查是查不到它的；而且它**只影响评分那三个数、不减少行**（运营设完发现
   一行都没少，会以为筛选坏了）。已改名「问诊时间段（UTC）」并在筛选栏写明这两件事。
9. 🟡 缺权限的页签按钮原来仍可点（点开才说没权限）→ 改为 `disabled` + `title` 注明所缺权限。
10. 🟡 **i18n 孤儿键 19 个 × 4 包**（`admin.ratingOverview.*` 十一个、`admin.ratings.{back,title,count}`、
    `admin.qualpage.{back,title}`、`admin.nav.ratings`、9.1a 引入而本次两处引用全删的
    `admin.v130.vets.tab.placeholder`、随抽屉去掉总量而失去引用的 `admin.v130.vets.rating.volume`）已删。
11. 🟡 T4 的测试任务基本没做（唯一的新用例把 `ratingService` 整个 mock 掉、`sort` 被 `any()` 吞掉，
    传什么都全绿 —— 第 1 条那个 bug 正好落在这个盲区）。已补：排序键六种拼法、
    权限降级、便宜路径 / 总览路径分流、懒加载属性与槽位 id、三页退役落点。

**有意的偏离 / 待拍板**

- ⚠️ **AC3 的「旧地址 404 不跳转」对两条 per-vet GET 未执行**：
  `GET /admin/vets/{id}/qualification` 与 `…/ratings` 改成非 htmx 时 `redirect:/admin/vets?open={id}`。
  理由：前者路径上**还有三个 POST**，删成 404 会让「点了旧书签」与「路径写错」看起来一样；
  后者为保持一致。`GET /admin/ratings` 是真 404（整类删除）。
  **留待拍板：接受这个取舍，还是两条也一并 404。**
- ⚠️ **排序参数用 snake_case 而不是 AC3 写的 camelCase**：snake_case 是服务层一直以来的常量，
  也是退役那页发出去的链接里的拼法（运营存了书签）。服务层两种都认，所以 AC3 的拼法也能用。
- ⚠️ **评分列的「已评 / 总量」只在做评分查询时出现**（见复审第 4 条）：平时那一列只有均分，
  与 9.1b 之前一致。**留待拍板：是否值得为「平时也显示两个计数」付一次
  O(全部已结束会话) 的聚合**（真要的话应该先给 `ConsultQualityQueryService` 加一条 group-by 查询）。

**架构护栏自查**：无新迁移、无新依赖、无新中间件；`ddl-auto=validate` 未动；
证件 key / 预签名 URL / 证件号不进日志、不进审计 summary、不进错误响应；
证件链接补了 `rel="noopener"`（旧页没有）；日志无 PII。

### File List

**改**
- `petgo-backend/src/main/java/com/tailtopia/admin/vetqual/web/AdminVetQualificationController.java`
- `petgo-backend/src/main/java/com/tailtopia/admin/web/AdminWebController.java`
- `petgo-backend/src/main/java/com/tailtopia/admin/service/AdminVetService.java`
- `petgo-backend/src/main/java/com/tailtopia/admin/service/VetAdminView.java`
- `petgo-backend/src/main/java/com/tailtopia/admin/rating/service/AdminRatingService.java`（排序键归一）
- `petgo-backend/src/main/java/com/tailtopia/admin/shared/AdminPageCatalog.java`
- `petgo-backend/src/main/resources/templates/admin/vets.html`
- `petgo-backend/src/main/resources/templates/admin/fragments/{vets-list,drawer-vets}.html`
- `petgo-backend/src/main/resources/i18n/messages{,_zh_CN,_en,_id}.properties`
- `petgo-backend/src/test/java/com/tailtopia/admin/vet/AdminVetDrawerIntegrationTest.java`
- `petgo-backend/src/test/java/com/tailtopia/admin/service/AdminVetService{List,Create,Edit}Test.java`
- `petgo-backend/src/test/java/com/tailtopia/admin/vetqual/web/AdminVetQualAccessControlTest.java`
- `petgo-backend/src/test/java/com/tailtopia/admin/web/{AdminVetRatingsAccessControlTest,AdminVetsAccessControlTest,AdminPagesRenderSmokeTest,AdminNavSectionOpenTest}.java`
- `petgo-backend/src/test/java/com/tailtopia/admin/shared/AdminPageCatalogTest.java`

**新增**
- `petgo-backend/src/test/java/com/tailtopia/admin/rating/service/AdminRatingSortKeyTest.java`（L0）

**删除**
- `petgo-backend/src/main/resources/templates/admin/{vet-qualification,vet-ratings,ratings}.html`
- `petgo-backend/src/main/java/com/tailtopia/admin/rating/web/AdminRatingController.java`
- `petgo-backend/src/test/java/com/tailtopia/admin/rating/web/AdminRatingAccessControlTest.java`（所测端点已删）

## 拍板回写（2026-09-09）

- **D-46**：产品知悉无异议——评分总览的排序 / 时间段筛选并入兽医列表筛选栏；开户用弹窗为模板 B 有意例外。逐页规格 B20 已补注。
