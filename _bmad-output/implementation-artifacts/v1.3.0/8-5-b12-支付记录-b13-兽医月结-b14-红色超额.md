---
baseline_commit: 4301227a
branch: feat/1.3.0-ops-ui-refactor
epic: 8
story: 8.5
ad: [AD-9, AD-10, AD-11]
decisions: [D-8, D-23]
depends_on: [2-2, 2-3a, 2-3b]
page_spec: [B12, B13, B14]
---

# Story 8.5: B12 支付记录 + B13 兽医月结 + B14 红色超额（套模板 B）

Status: review

> 自包含 story，可本地或云端 L0 执行。与用户沟通用中文。执行纪律见根 `CLAUDE.md`。
> **零写端点变更**。三页现状都是单页无独立详情页，本 story 只加摘要条 / 抽屉 / htmx 提交。依赖 2.2（`@StagOnly` 门控注解，与 STAG 角标同源）、2.3a / 2.3b。

## Story

As a 运营 / 财务，
I want 支付记录、兽医月结、红色超额三页有摘要条、抽屉和统一的操作交互，支付记录的测试用「模拟回调」只在 staging 出现，
so that 钱相关的三页口径一致，生产环境不会出现测试按钮。

## Acceptance Criteria

**AC1 · B12 支付记录列表**
**Given** `/admin/payments` 套模板 B
**When** 打开
**Then** 筛选栏：用户 ID / 用途 / 状态 / 时间段（WIB，含止当天）+ 显式「查询」钮 + 「清空筛选」（现状 `userId / purpose / status / from / to` 参数不变）；摘要条 **本期支付笔数 · 成功金额 · 失败/过时数**（来自现状 `AdminPaymentSummary`：orderCount / paidCount / cashIncome / coinSpent，币种不一致时显示「币种不一致，不做换算」原提示；「本期」= 当前筛选集）；表格列 **用户 ｜ 支付号 ｜ 用途 ｜ 渠道 ｜ 金额 ｜ 币种 ｜ 状态 ｜ 时间（WIB）**，每页 20 `[L1]`
**And** 「导出当前结果」按钮：`GET /admin/payments/export.xlsx` 接收与列表同一套筛选参数（现状已如此），权限 `payment.list_export` 不变；实现改经 `AdminExportWriter.xlsx`（现状 `AdminPaymentExportService` 本就是 POI 范式，迁到统一写入器后行为不变）`[L1]`
**And** 点行 → 抽屉 `GET /admin/payments/{intentToken}/drawer`：支付意图全字段只读（含用途、渠道、金额、状态、创建 / 更新时间 WIB）`[L2]`

**AC2 · B12 模拟回调三钮 — 仅 staging**
**Given** 逐页规格 B12 描述的「🧪 模拟回调（成功 / 失败 / 过时三钮）」
**Then** 🔴 **代码核实：`dev_1.3.0` / main 上不存在任何模拟回调代码**（全库 grep `simulate` / `模拟` 无命中）；该能力只存在于 stag 分支的 stag-only 提交。本 story 在主线上**只预留门控与槽位**：抽屉操作条渲染一个 `th:if="${@stagGate.enabled}"`（`@StagOnly` 的模板侧表达）的「仅测试环境」区块，生产 / dev 不渲染；三钮本体与端点由 stag 分支合入时接入该槽位并加 `@StagOnly` `[L0]`
**And** 终态（PAID / FAILED / EXPIRED）行在 stag 也不渲染模拟钮（槽位内按状态 `th:unless` 终态）`[L0]`

**AC3 · B13 兽医月结**
**Given** `/admin/settlements` 套模板 B
**Then** 摘要条 **待打款单数 · 待打款金额合计 · 本月已打款**（status=PENDING_FINANCE 计数与 Σ payout；本月（WIB）status=PAID/ARCHIVED 的 Σ payout）；表格列 **兽医 ｜ 月份 ｜ 单数 ｜ 成交额 ｜ 到手 ｜ 状态 ｜ 凭证 ｜ 打款时间 ｜（操作移入抽屉）**；页首提示「每月 1 日生成上月月结…」保留 `[L1]`
**And** 点行 → 抽屉 `GET /admin/settlements/{id}/drawer`：该月结的订单构成列表（订单 / 金额 / 兽医分成快照）+ 凭证（现状 `proof` 为**文本参考号 ≤512**，不是文件上传——如实展示文本；逐页规格「凭证图」与现状不符，见 Dev Notes）+ 操作条 `[L2]`
**And** 操作：确认打款（`POST /admin/settlements/{id}/pay`，`proof` 文本，data-confirm 复述**兽医名 + 到手金额**）仅 `PENDING_FINANCE` 渲染；归档（`POST {id}/archive`）仅 `PAID` 渲染且**凭证为空时禁用**（现状仅服务层拒绝，抽屉前置禁用 + 提示）；`ARCHIVED` 只读；权限 `settlement.payout`，无权限禁用注明 `[L1/L2]`

**AC4 · B14 红色超额**
**Given** `/admin/red-overage` 套模板 B（与月结拆开的独立页、独立权限 `risk.view` / `risk.edit`）
**Then** 摘要条 **待核查用户数**（reviewStatus=TO_VERIFY 计数）；表格列 **用户 ｜ RED 计数 ｜ 复核态 ｜ 备注**；页首「仅观测，不自动处置」提示保留 `[L1]`
**And** 点行 → 抽屉 `GET /admin/red-overage/{userId}/drawer`：该用户红色评级历史（RED 分诊任务列表：任务 / 时间）+ 标记操作（`status` TO_VERIFY / RESOLVED + `note` ≤255 → `POST /admin/red-overage/{userId}/review`，`risk.edit`），htmx 提交刷抽屉 + oob 行 `[L2]`

**AC5 · 端点与权限零变更**
**Then** B12：`GET /admin/payments`（`payment.view`）、`GET export.xlsx`（`payment.list_export`）；B13：`GET /admin/settlements`（`settlement.view`）、`POST {id}/pay(proof)`、`POST {id}/archive`（`settlement.payout`）；B14：`GET /admin/red-overage`（`risk.view`）、`POST {userId}/review(status,note)`（`risk.edit`）——路径 / 参数 / 权限逐字不变；仅新增三个 `GET …/drawer`（各页 VIEW 权限）`[L0]`

**AC6 · 三语与回归**
**Then** `admin.v130.payments.*` / `admin.v130.settlements.*` / `admin.v130.redOverage.*` 三语同批；`AdminPaymentFilterIntegrationTest`、`AdminSettlementIntegrationTest` 全绿；MockMvc 四条 × 3 页；`@StagOnly` 槽位在 `stag` profile 渲染、`dev` / `prod` 不渲染各一条测试 `[L0/L1]`

---

## Tasks / Subtasks

- [x] **T1 · B12 套壳**（AC1）：`payments.html` 套 `tpl-b-list`；摘要条复用 `AdminPaymentQueryService.summarize(Filter)`（已存在）；表去操作列；新增 `fragments/drawer-payments.html` + `AdminPaymentController` `GET {intentToken}/drawer`（VIEW_AUTH，按 public token 查 `PaymentIntent`）
- [x] **T2 · B12 导出**（AC1）：`AdminPaymentExportService.exportXlsx` 内部改调 `AdminExportWriter.xlsx`（列、表头 key、WIB 时间列不变；审计照旧）；按钮文案 key 改「导出当前结果」
- [x] **T3 · B12 stag 槽位**（AC2）：抽屉操作条加 `<div th:if="${@stagGate.enabled} and !terminal" data-stag-only>` 空槽位 + 「仅测试环境」徽标文案；`stagGate` 由 Story 2.2 的 `@StagOnly` 配套 bean 提供（若 2.2 命名不同，以 2.2 为准并回写本文件）；**不实现三钮与端点**，在 Completion Notes 写明「模拟回调本体在 stag 分支，合入时接入此槽位」
- [x] **T4 · B13 套壳 + 抽屉**（AC3）：`settlements.html` 套壳；`AdminSettlementService.summary()`（三数）；新增 `fragments/drawer-settlements.html`（订单构成需 `AdminSettlementService.detail(id)` 新增只读查询：按 settlement 关联 `consult_orders` 列出）；`pay` / `archive` POST 加 `HxRequest` 分支；归档按钮在 `proof` 空时 `disabled` + title 提示
- [x] **T5 · B14 套壳 + 抽屉**（AC4）：`red-overage.html` 套壳；`RedOverageMonitorService.summary()`；新增 `fragments/drawer-red-overage.html`（RED 历史来自 `TriageTaskRepository` 按 userId + level=RED）；`review` POST 加 `HxRequest` 分支
- [x] **T6 · 三语**：三页 `admin.v130.<page>.summary.*`、`drawer.*`、`admin.v130.payments.stagOnly`、`admin.v130.settlements.archiveNeedsProof`；复用现有 `admin.payments.*` / `admin.settlements.*` / `admin.redOverage.*`
- [x] **T7 · 测试**：三页 MockMvc 四条；`@stagGate` 在 `@ActiveProfiles("stag")` 与默认下渲染差异各一条；集成测试回归
- [x] **T8 · 云端执行须知**：L0 云端；L1 / L2 留本地

---

## Dev Notes

### 🔴 模拟回调三钮在主线不存在——这是本 story 最重要的事实
PRD / 逐页规格 / UI 稿都把「模拟回调（成功 / 失败 / 过时）」写成 B12 的现状功能，但 `dev_1.3.0`（= main 3c2581c5 + 文档提交）里 `grep -rn "simulate\|模拟"` 在 `admin/**`、`pay/**`、`templates/admin/**` 全无命中。逐页规格自己也写了「stag 专用（代码注明绝不合并主线）」。所以：
- 主线只能做**门控 + 槽位**，三钮和 `POST …/simulate-*` 端点属于 stag 分支的 stag-only 提交，合 stag 时接进槽位并套 `@StagOnly`。
- 写操作清单（Story 2.1）在主线跑不会出现模拟端点，属正常；Epic 11 diff 时若 stag 分支已合入才会出现，需在核对说明里标为「stag-only，非本版新增」。
- **需拍板 / 需知悉**：如果产品希望主线也带模拟回调能力（只在 stag 渲染），那是把 stag-only 代码正式化，超出本 story 范围，另开 story。

### 🔴 B13 凭证是文本，不是图
`AdminSettlementController.pay(id, proof)` 的 `proof` 是 `String` ≤512（模板 placeholder「打款凭证/参考号」），没有文件上传。逐页规格 B13 写「凭证图」与现状不符；本 story 按「零功能变更」如实做**文本凭证**，抽屉展示文本参考号。若要改为图片上传（OSS objectKey）属新功能，超出本 story，在 Completion Notes 记为待产品确认。

### `@StagOnly` 门控来源
Story 2.2 引入 `@StagOnly`（`spring.profiles.active` 含 `stag`）用于顶栏 STAG 角标；本 story 的模板侧判断用同一 bean（建议 `StagGate.enabled()`），不要自己再读 `Environment`。现状项目只有 `@Profile("dev")` 用法（`NotifyTestController` / `DevUserSeeder`），没有 stag 门控先例。

### 现状代码要点

| 文件 | 现状 | 改什么 | 保留 |
|---|---|---|---|
| `admin/payment/web/AdminPaymentController.java` | `GET /admin/payments(userId,purpose,status,from,to,page)`（`payment.view`）、`GET export.xlsx(同参)`（`payment.list_export`） | + `GET {intentToken}/drawer` | 参数、权限 |
| `admin/payment/service/AdminPaymentQueryService.java` | `Filter` record、`search(f,page,size)`、`searchAll(f,limit)`、`summarize(f)` → `AdminPaymentSummary`（orderCount / paidCount / cashIncome / coinSpent / currency / currencyIsMixed） | 复用 `summarize` 作摘要条 | — |
| `admin/payment/service/AdminPaymentExportService.java` | `exportXlsx(actor, Filter)`：POI，表头 key，WIB 时间列，审计 | 内部改经 `AdminExportWriter.xlsx` | 列与审计 |
| `templates/admin/payments.html` | 筛选表单 + 汇总卡 + 8 列表 + 分页 + 导出钮（`sec:authorize` 与 EXPORT_AUTH 逐字一致的注释） | 套壳、汇总卡→摘要条、行 hx-get、抽屉 | 筛选参数名、`sec:authorize` |
| `admin/settlement/web/AdminSettlementController.java` | `GET /admin/settlements`（`settlement.view`）、`POST {id}/pay(proof)`、`POST {id}/archive`（`settlement.payout`） | + drawer GET；POST 加 `HxRequest` | — |
| `admin/settlement/service/AdminSettlementService.java` | `list()`、`markPaid(id, proof, adminId)`、`archive(id, adminId)`（审计） | + `summary()`、`detail(id)` 只读 | 两个写方法 |
| `templates/admin/settlements.html` | 9 列表 + 行内 pay（proof 输入）/ archive 表单 | 套壳、操作进抽屉 | 状态机 PENDING_FINANCE → PAID → ARCHIVED |
| `admin/risk/web/AdminRedOverageController.java` | `GET /admin/red-overage`（`risk.view`）、`POST {userId}/review(status,note)`（`risk.edit`） | + drawer GET；POST 加 `HxRequest` | — |
| `admin/risk/service/RedOverageMonitorService.java` | `list()`（TriageTask RED 计数 + review）、`mark(userId,status,note,adminId)` | + `summary()`、`history(userId)` | `mark` |
| `templates/admin/red-overage.html` | 5 列表 + 行内标记表单 | 套壳、标记进抽屉 | 「仅观测」提示 |

### 测试标准
沿用 `AdminPaymentFilterIntegrationTest` / `AdminSettlementIntegrationTest`（真库）；`risk` 目前只有 service 单测，新增 MockMvc 四条。

### Project Structure Notes
改动在 `admin/payment/**`、`admin/settlement/**`、`admin/risk/**` + 三模板 + 三新 fragment；无迁移。

### References
- [Source: 后台重构逐页规格.md#B12 / B13 / B14] · [Source: epics-v1.3.0-admin.md#Story 8.5] · [Source: ui-v1.3.0-admin.html 泳道 4 帧 4-3 / 4-4 / 4-5] · [Source: architecture-v1.3.0-admin-delta.md#AD-9/AD-10/AD-11 + Deferred（`@StagOnly` 提前到本版）] · [Source: petgo-backend/.../admin/payment/web/AdminPaymentController.java] · [Source: petgo-backend/.../admin/settlement/web/AdminSettlementController.java] · [Source: petgo-backend/.../admin/risk/web/AdminRedOverageController.java]

## Dev Agent Record

### Agent Model Used

云端 headless session（claude.ai/code），**L0 only**。

### Debug Log References

- L0：`cd petgo-backend && ./mvnw -B clean package` → `Tests run: 1835, Failures: 0, Errors: 0, Skipped: 6` / BUILD SUCCESS
- `bash scripts/ci/check-i18n-keys.sh` → OK（2852 个 key，四包集合相等）
- `bash scripts/ci/check-flyway-versions.sh origin/main` → OK（本 story **无新增迁移**）
- `bash scripts/ci/list-admin-write-ops.sh` → 149 写端点（grep 自检 149），三个模拟端点已按 🧪 stag-only 单列
- 云端无 Docker（跑不了 L1）：另写离线 Thymeleaf 渲染探针（`MoneyRender`），把三页与六个新片段
  真渲染 36 组，并**按「有处置权 + stag」/「无处置权 + 非 stag」各跑一遍**，全绿。

### Completion Notes List

**L1 / L2 待本地验收**（云端 headless，无 Docker、无渲染）：本 story 全部 `[L1]` / `[L2]` 项，
尤其新增的 `AdminMoneyPagesDrawerIntegrationTest`（15 条）与 `AdminPaymentSimulateStagTest`（5 条，
需 `@ActiveProfiles({"dev","stag"})` 的独立上下文），以及**模拟回调真跑一遍下游入账链**、
**抽屉里打款 / 归档 / 标记的真实交互**。

**AC 落地**

- AC1：`/admin/payments` 套 `tpl-b-list`（`res='payment'` → `#payment-drawer`）；筛选栏参数名
  `userId / purpose / status / from / to` 逐字未动、显式「查询」+「清空筛选」；摘要条三格；
  点行 → 抽屉（支付意图全字段只读）；导出改经 `AdminExportWriter.xlsx`（列序 / 数字单元格 /
  截断行位置 / 审计口径不变，表头改随界面语言），按钮文案改「导出当前结果」。
- AC2 / **D-41**：模拟回调三钮**正式进主线**，但整条链 `@StagOnly`（`@Profile("stag")`，
  生产**不注册 bean、路由不存在**，不是 403）+ `SUPER_ADMIN` + 终态不渲染；走**真实回调收口**
  `PaymentIntentService.applyCallback` 而非直接改状态（幂等三闸、到账事件、下游入账全照常发生 ——
  模拟的意义正是把那一整条链真跑一遍）。写操作清单已能识别 `@StagOnly` 并单列（见下）。
- AC3：`/admin/settlements` 套壳；摘要条三格（待打款单数 · 待打款金额合计 · 本月已打款）；
  抽屉 = 台账 + **订单构成**（读时按 (兽医, 月份窗口) 重算，口径与 `VetSettlementService` 逐字一致）
  + 凭证（**文本参考号**，D-42）+ 操作条；`pay` / `archive` 加 htmx 分支；页首提示保留。
- AC4：`/admin/red-overage` 套壳；摘要条「待核查用户数」；抽屉 = 观测 + RED 历史 + 标记；
  「仅观测，不自动处置」提示在列表与抽屉各保留一份。
- AC5：三页原有端点的路径 / 参数 / `@PreAuthorize` 逐字未动；新增仅三个 drawer GET（各页 VIEW 权限）
  + 三个 stag-only 模拟 POST；列表 GET 多一个可选 `open` 读参。
- AC6：`admin.v130.payments.* / settlements.* / redOverage.*` 三语同批。

**复审（对抗式）修掉的 12 条**

高：
1. 🔴🔴 **「打款时凭证留空」会让月结永久卡在 PAID**。第一版照 AC3 字面做了「凭证为空则禁用归档」，
   但 AC3 的前提「现状仅服务层拒绝」**不成立** —— `VetSettlement.archive()` 只判 PAID、从不看 proof。
   而 `paymentProof` 的唯一写入点 `markPaid` 带 `requireStatus(PENDING_FINANCE)`：一旦进了 PAID，
   **再没有任何入口能补填凭证**。于是那条月结归档按钮永远灰着、提示叫人「先回填凭证」，
   而回填凭证的表单已经随状态消失 —— UI 里逃不出去，只能改库。
   改为：**拦在打款那一步**（凭证输入框 `required`），存量缺凭证的**仍可归档**但把话说清楚。
   ⚠️ **这是对 AC3 字面要求的有意偏离，见下「有意的偏离」。**
2. 🔴🔴 **`@StagOnly` 在真实 staging 上根本不生效**：runbook / `deploy-backend-stag.sh` 生成的
   `~/.env.petgo-stag` 写的是 `SPRING_PROFILES_ACTIVE=prod`，**不含 `stag`**。
   于是 AC2 的全部可见成果（三钮、刷新槽）在 staging 上都看不到，而页面一切正常 ——
   看起来像「功能没做」。同一个门还挂着 Story 2.2 的 STAG 角标与 Story 3.3 的手动跑批端点，
   它们**一直**是空转的。已在 `docs/runbooks/runbook-staging.md` 的 C2 步骤改成 `prod,stag`
   并写明后果。**⚠️ 服务器上现存的 `~/.env.petgo-stag` 需要人工改一次并重启容器**（本会话不碰服务器）。
3. 🔴 **测试自相矛盾，L1 一跑必红**：`doesNotContain("症状")` 与抽屉里那句边界提示
   「此处不展示**症状**描述与解析结果」撞字。云端没 Docker 所以看不出来。
   改用哨兵串 `SYMPTOM-SENTINEL-8x5` 做种子症状文本 —— 这条守门这才真的生效。

中：
4. 🟠 **`sim-` 前缀不是可靠判据**：收口只在 `gateway_ref` 为空时才回填，而下过单、拿到过收款码的
   意图（**正是最值得模拟的那一批**）早被 `attachCharge` 写进了真实网关号。原测试只因为种子造的是
   裸意图才绿。改为注释说实话 + 判据落在 `rawMeta.simulated` 与审计行上。
5. 🟠 **`AdminPaymentDetail.terminal()` 与领域枚举脱钩**：逐个列举 PAID/FAILED/EXPIRED，
   而 `PaymentStatus.isTerminal()` 是 `!= PENDING`。枚举以后多一个值就当场分叉：抽屉照渲三钮、
   前置检查放行、收口静默返回 → 运营看到 toast「已模拟回调」而状态一个字没变
   （正是那段注释自己声称要避免的形态）。改成 `!"PENDING".equals(status)`。
6. 🟠 **红码标记会先写库再 500**：`service.mark` 对任意 userId 都写 review 行，而列表只列有 RED 记录的人。
   对没有 RED 的 id 直接 POST（改地址栏 / 脚本）→ 写库成功 → `afterAction` 找不到行抛
   `NoSuchElementException` → 500，库里留下一条**永远不会出现在任何页面上的孤儿行**。
   改为 htmx 分支先判是否在聚合结果里，不在就 404（与抽屉同一条判据），**拒绝在写库之前**。
7. 🟠 **`data-confirm` 复述的是兽医数字 id**，AC3 要的是**兽医名**。财务同时开着几笔月结、点错行时，
   一个他不认识的 id 挡不住任何东西。`AdminSettlementDetail` 加 `vetName`（查不到回退 `#<id>`），
   台账与确认文案都改用名字。
8. 🟠 **两处纯函数零数值覆盖**：`AdminSettlementService.summary` 与 `RedOverageMonitorService.summary`
   都不碰 DB，而全部断言只钉了 `data-sum="…"` 这个标记存在 —— 把「本月已打款」的判据从 `paidAt`
   换回 `period`、把 WIB 换成系统时区、把 `isBefore` 写反，**全绿**。新增两个 L0 测试
   （`AdminSettlementSummaryTest` 4 条 + 红码 1 条），含月初边界。
9. 🟠 **「订单构成」——本 story 风险最高的口径——零覆盖**：原测试的月结下面一单都没有。
   补成真的塞五单（窗口内 2 含左端点、窗口前 1、窗口后 1、别的兽医 1），钉住 `[start, end)`、
   `sessionEndedAt` 归月与 WIB 三件事。
10. 🟠 **D-41 的清单动作没有落点**：`list-admin-write-ops.sh` 只按路径前缀 `/admin/_*` 排除
    stag-only（对 kitchen-sink 那种命名有效），**不识别 `@StagOnly` 注解** —— 三个模拟端点会以
    普通生产写端点的身份进 Story 11.1 的 diff。改为按注解判定并在备注列打
    「🧪 stag-only（@StagOnly，生产不注册）」，同时在清单表头说明这一列的含义。

低：
11. 🟡 **i18n 孤儿键 4 个 × 4 包**（`admin.payments.btn.export` / `sum.orders` / `sum.paid` / `sum.cash`，
    随汇总卡改摘要条后失去引用）已删；`admin.payments.sum.note` 的文案还写着「**收入**只计…」，
    而那一格现在叫「成功金额」—— 三语同步改词（模板里的原型文本会被 `th:text` 覆盖，改模板没用）。
12. 🟡 **导出截断说明的行数被加了千分位**：`msg.get(key, EXPORT_MAX_ROWS)` 传 `int` → MessageFormat
    交给 NumberFormat → 「上限 5,000 行」，而这份表里其它数字都没有千分位。改传 `String.valueOf`。

**有意的偏离 / 待拍板**

- ⚠️⚠️ **AC3 的「凭证为空时禁用归档」没有照做**（见复审第 1 条）。照做会造出一个 UI 逃不出去的死角，
  而 AC 给出的理由（「现状仅服务层拒绝」）是事实错误。现实现 = 入口 `required` 拦住 +
  存量缺凭证仍可归档 + 明确警告。**留待拍板：接受这个改法，还是改成服务层 `markPaid` 拒绝空凭证
  （那是写端点行为变更，超出本 story 的「零写端点变更」）。**
- ⚠️ **摘要第三格叫「未成交笔数」而不是 AC1 的「失败/过时数」**：`AdminPaymentSummary` 只有
  `orderCount` 与 `paidCount`（DB 里一次聚合，没有分状态计数），差值里**还含待支付** ——
  标成「失败/过时」会把一批还在付款窗里的单子说成失败。副标注明「含待支付 / 失败 / 已过期」。
  **留待拍板：接受这个口径，还是补一个分状态计数的聚合。**
- ⚠️ **模拟回调块对非 SUPER_ADMIN 是「整块消失」而不是「禁用态 + 原因」**，与本页其它处置的约定相反。
  刻意：这是测试专用能力，对没有它的人「不存在」比「存在但不给用」更合适。Epic 11.4 权限矩阵收口时勿当漏做。
- ⚠️ **`badge-neutral` 顺手补了 CSS 定义**（`admin.css`）：这个类名十余个既有模板一直在用，
  却**从未被定义过**，落地效果是「一个没有底色的徽标」，与旁边有底色的 ok / danger 摆在一起
  会被读成「这一格没加载出来」。补定义会同时改动那十余个页面的观感，属 **Epic 11.5 UI 稿比对范围**。
- ⚠️ **D-42 已按「文字流水号」落地**；若产品要改成凭证图上传（OSS objectKey）属新功能，另开 story。

**架构护栏自查**：无新迁移、无新中间件（导出仍走既有 POI）；`ddl-auto=validate` 未动；
对外一律用不可枚举 token（支付按 `publicToken` 定位，模拟端点同）；无凭证入库；
健康数据边界守住（`RedTaskRow` 只有 id / 状态 / 时间，症状文本与解析结果不进 DTO 与模板）；
`gateway_meta` 不进抽屉；审计只记 result / status，不记 note 与 PII。

### File List

**改**
- `petgo-backend/src/main/java/com/tailtopia/admin/payment/web/AdminPaymentController.java`
- `petgo-backend/src/main/java/com/tailtopia/admin/payment/service/AdminPaymentQueryService.java`
- `petgo-backend/src/main/java/com/tailtopia/admin/payment/service/AdminPaymentExportService.java`
- `petgo-backend/src/main/java/com/tailtopia/admin/settlement/web/AdminSettlementController.java`
- `petgo-backend/src/main/java/com/tailtopia/admin/settlement/service/AdminSettlementService.java`
- `petgo-backend/src/main/java/com/tailtopia/admin/risk/web/AdminRedOverageController.java`
- `petgo-backend/src/main/java/com/tailtopia/admin/risk/service/RedOverageMonitorService.java`
- `petgo-backend/src/main/java/com/tailtopia/admin/shared/web/AdminHxEvents.java`
- `petgo-backend/src/main/java/com/tailtopia/consult/repository/ConsultOrderRepository.java`
- `petgo-backend/src/main/java/com/tailtopia/triage/repository/TriageTaskRepository.java`
- `petgo-backend/src/main/resources/templates/admin/{payments,settlements,red-overage}.html`
- `petgo-backend/src/main/resources/static/admin/admin.css`（补 `badge-neutral` 定义）
- `petgo-backend/src/main/resources/i18n/messages{,_zh_CN,_en,_id}.properties`
- `petgo-backend/src/test/java/com/tailtopia/admin/risk/service/RedOverageMonitorServiceTest.java`
- `scripts/ci/list-admin-write-ops.sh`（识别 `@StagOnly` 并单列）
- `docs/runbooks/runbook-staging.md`（C2：`SPRING_PROFILES_ACTIVE=prod,stag`）

**新增**
- `petgo-backend/src/main/java/com/tailtopia/admin/payment/dto/AdminPaymentDetail.java`
- `petgo-backend/src/main/java/com/tailtopia/admin/payment/web/AdminPaymentSimulateController.java`（`@StagOnly`）
- `petgo-backend/src/main/java/com/tailtopia/admin/settlement/dto/{SettlementSummary,AdminSettlementDetail}.java`
- `petgo-backend/src/main/java/com/tailtopia/admin/risk/dto/{RedOverageSummary,RedTaskRow}.java`
- `petgo-backend/src/main/resources/templates/admin/fragments/{payments-list,drawer-payment,settlements-list,drawer-settlement,red-overage-list,drawer-red-overage}.html`
- `petgo-backend/src/test/java/com/tailtopia/admin/payment/AdminMoneyPagesDrawerIntegrationTest.java`（L1，15 条）
- `petgo-backend/src/test/java/com/tailtopia/admin/payment/AdminPaymentSimulateStagTest.java`（L1 + stag profile，5 条）
- `petgo-backend/src/test/java/com/tailtopia/admin/settlement/service/AdminSettlementSummaryTest.java`（L0，4 条）

## 拍板回写（2026-09-09）

- **D-41**：B12 模拟回调三钮（成功 / 失败 / 过时）**正式进主线**：从 stag 分支移植 Controller 端点与服务逻辑，类与端点均 `@StagOnly`（stag profile 注册，生产不存在）；行内三钮加「仅测试环境」标注；终态不可再模拟。写操作清单里这三个端点单列为 stag-only。
- **D-42**：B13 凭证维持文字流水号（≤512 字），不做图片上传。
