---
baseline_commit: ac3d57b733769b3bbc374e82db59a07082e26522
epic: 1
story: 2
batch: A
verification: [L0, L1]
---

# Story 1.2: 通用宠物健康类改自动达成

Status: review

## Story

As a 养「其他」类宠物的用户，
I want 「第一次看兽医」和「第一次健康检查」像猫狗一样只能真做了才点亮，
so that 成就的含金量对所有物种一致。

---

> **补齐 1.1.2 FR-86 的漏网项。** FR-86 当初按后缀 `M3/M4/M5/M9` 取消打卡，通用清单的 `G-M1`（看兽医）/ `G-M2`（健康检查）后缀不命中，**至今仍是打卡类** —— 同一件事猫狗要真问诊/真录记录才点亮，其他宠物点「已打卡」就行。属规则漏网，非有意差异。
>
> **本 story 最容易被漏掉的一半是存量迁移。** `pet_milestones.trigger_type` **建档那一刻就物化成行**，而铺清单入口 `MilestoneService.assignRoster` 有 `existsByPetProfileId` 短路、**对已建档宠物永不回改**。只改编译期目录 → 只对新建档宠物生效，**存量宠物照旧渲染打卡按钮**。先例：V77 正是为同类问题补的迁移。

---

## Acceptance Criteria

### AC1 — 目录改判（L0）

**Given** 编译期里程碑目录
**When** 读取 `G-M1` / `G-M2` 的触发类型
**Then** 为 `SYSTEM_AUTO`，不再是 `USER_CHECKIN` `[L0]`

### AC2 — 🔴 存量迁移（L1，最易漏）

**Given** 一只本次上线**之前**已建档的 `OTHER` 类宠物
**When** 迁移执行完毕后查询其 `pet_milestones`
**Then** `G-M1` / `G-M2` 的 `trigger_type` 已改为 `SYSTEM_AUTO` `[L1]`
**And** 客户端不再为这两条渲染打卡按钮 `[L1]`
**And** 迁移文件名为时间戳制 `V<yyyyMMdd_HHmm>__<snake>.sql` `[L0]`

### AC3 — 触发源接入（L1）

**Given** 一只 `OTHER` 类宠物
**When** 一次**真人**兽医咨询结束
**Then** 自动完成 `G-M1` `[L1]`
**When** 一次 AI 分诊结束
**Then** **不**完成 `G-M1`（AI 不发 `ConsultClosedEvent`，模块隔离天然满足，**不得再加一层 AI 判断分支**） `[L0]`

**Given** 一只 `OTHER` 类宠物
**When** 录入 `VACCINE` 类型健康记录
**Then** 自动完成 `G-M2`（决策 A-1：只认疫苗，不新增「体检」类型） `[L1]`

### AC4 — 打卡护栏与候选过滤（L1）

**Given** 任一客户端
**When** 对 `G-M1` / `G-M2` 发起打卡请求
**Then** 后端拒绝 `[L1]`
**And** 这两条不出现在「已打卡候选内容」的可选列表中 `[L1]`

### AC5 — 存量完成态不回滚（L1）

**Given** 一只已通过打卡完成 `G-M1` 或 `G-M2` 的宠物
**When** 迁移执行完毕
**Then** 其完成态保持不变，不回滚 `[L1]`

### AC6 — 文案三处同步（L0）

**Given** `G-M1` / `G-M2` 的说明文案
**When** 在 Java 中文目录、印尼语标题表、客户端 en/id 双语表三处查看
**Then** 打卡引导措辞已作废，改为与猫狗 `M5` / `M3` 同类的说明文案 `[L0]`
**And** 三处同步，跨库测试通过 `[L0]`

### AC7 — 不扩散（L0）

**Then** 不改动自动完成的寻址实现（属 Story 1.1） `[L0]`
**And** 不改动灰徽章点击去向（属 Story 1.3） `[L0]`
**And** 不新增「体检」健康记录类型 —— `health_records.type` 的 CHECK 约束**一字不动** `[L0]`

---

## Tasks / Subtasks

- [x] 目录中 `G-M1` / `G-M2` 改 `SYSTEM_AUTO`（AC1）
- [x] 🔴 写一支时间戳迁移，把存量 `pet_milestones` 中这两条的 `trigger_type` 改判（AC2）
- [x] `G-M1` 接入真人兽医咨询结束事件；`G-M2` 接入 `VACCINE` 健康记录（AC3）
- [x] 健康类集合纳入 `G-M1` / `G-M2`，覆盖打卡护栏与候选过滤（AC4）
- [x] 三处文案同步 + 跨库测试（AC6）
- [ ] 🔴 **用「上线前已建档的宠物」验证 AC2 —— 需 DB，留本地**

---

## Dev Notes

### 🔴 头号陷阱：只改目录不写迁移 = 对存量用户完全失效

`MilestoneService.assignRoster` 只在 `!existsByPetProfileId(petProfileId)` 时铺行。已建档宠物的 `trigger_type` 是**建档当时物化的快照**，之后任何目录改动都不会回写。
**验收时务必用一只上线前就存在的宠物测**，用新建档的测不出问题。

### 要改的文件

| 文件 | 改什么 |
|---|---|
| `petgo-backend/.../profile/domain/MilestoneCatalog.java` | `G-M1` / `G-M2` → `SYSTEM_AUTO` |
| `petgo-backend/.../profile/domain/HealthMilestones.java` | 集合纳入这两条完整 code |
| `petgo-backend/.../profile/service/MilestoneCheckInService.java` | 打卡护栏 |
| `petgo-backend/.../profile/service/MilestoneAutoCompleteListener.java` | 真人问诊 → `G-M1` |
| `petgo-backend/src/main/resources/db/migration/V<ts>__*.sql` | **新增**：存量 `trigger_type` 改判 |
| `petgo_app/lib/features/profile/domain/health_milestones.dart` | 前端门控集合同步 |
| `petgo_app/lib/l10n/app_en.arb` / `app_id.arb` | 说明文案 |

### 验证层级

- `[L0]` 编译、跨库文案测试、CHECK 约束未变
- `[L1]` **存量宠物**迁移效果、触发源、打卡拒绝 —— 留本地
- Flyway 自查：`bash scripts/ci/check-flyway-versions.sh origin/main`

### References

- 架构 delta `AD-A5`（含 3b 存量迁移条款）· 决策日志 `A-1`

---

## Dev Agent Record

### Context Reference

- 架构 delta `AD-A5`（含 3b 存量迁移条款、3c 脏行不清理）、`AD-A4.6`（五份集合定责）
- 决策日志 `A-1`（只认疫苗，不新增「体检」类型）、`A-9`（存量脏行不清理）

### Agent Model Used

claude-opus-5[1m]（云端 headless session）

### Debug Log References

- backend L0：**1663 tests, 0 failures, 0 errors**
- `flutter analyze`：`No issues found!`；`flutter test`：全绿
- `bash scripts/ci/check-flyway-versions.sh origin/main`：OK（树内 170 个迁移，无重号、无撞号）

### Completion Notes List

**已交付（L0 全绿）**

1. **目录改判**（AC1）：`MilestoneCatalog` 里 `G-M1` / `G-M2` 由 `USER_CHECKIN` 改 `SYSTEM_AUTO`。
2. **🔴 存量迁移**（AC2）：`V20260910_1053__generic_health_milestones_auto_trigger.sql`
   —— `UPDATE pet_milestones SET trigger_type='SYSTEM_AUTO' WHERE code IN ('G-M1','G-M2')`。
   只改 `trigger_type`，**不碰 `milestone_completions` 一行**（AD-A5.3 存量不回滚）；
   不加列、不改约束（`ck_pet_milestones_trigger` 一字不动，`SYSTEM_AUTO` 本就是其合法取值）
   → `ddl-auto=validate` 不受影响；`WHERE trigger_type <> 'SYSTEM_AUTO'` 保证重跑幂等。
3. **触发源接入**（AC3）：映射表 `CONSULT_CLOSED` 的 OTHER 列由 `null` 改 `G-M1`；
   `HEALTH_RECORD_VACCINE` → `G-M2` 已在 Story 1.1 落地。**没有加任何「排除 AI」的分支**
   —— AI 分诊走 triage 模块、不发 `ConsultClosedEvent`，模块隔离天然满足（OQ-17）。
4. **健康类集合**（AC4）：`HealthMilestones` 由后缀集合改为**完整 code 集合**（10 条），纳入
   `G-M1` / `G-M2`。打卡护栏（`MilestoneCheckInService`）与埋点归类自动跟随。
   前端 `kAutoOnlyHealthMilestoneCodes` 同步改完整 code，两侧逐字等长。
5. **时间线集合改为派生**：`TimelineClassifier.HEALTH_MILESTONE_CODES` = `HealthMilestones.CODES`
   ∪ 三条 `S4`（AD-A4.6：③ 与 ① 对齐、额外含 S4）。**刻意派生而不是再抄一份名单**
   —— 两份手维护的名单正是这一连串错误的来源。这也顺带闭合了 Story 1.1 遗留的中间态
   （通用宠物录疫苗当天曾会同时出现健康胶囊与 G-M2 banner）。
6. **新增跨库契约测试** `HealthMilestonesCrossRepoTest`：后端读 App 的 `.dart` 源码，断言
   两侧集合逐字等长（沿用 `MilestoneCatalogI18nTest` 的既有做法，读不到文件**明确失败**）。
   **这正是本 story 这个 bug 当初能活下来的原因** —— 此前没有任何测试会因两侧走散而变红。

**关于 AC6 文案（实际无需新写文案，说明原委）**

- 三处标题**本就已同步**（Java `titleZh`/`titleId` + Dart `kMilestoneTitles` 的 en/id），
  `MilestoneCatalogI18nTest` 一直在钉，本次核对无差异。
- 「打卡引导措辞作废」不是删文案，而是**渲染分支切换**：`milestone_list_page.dart` 早有
  `autoOnlyHealth ? l10n.milestoneHintAutoHealth : ...` 分支，G-M1/G-M2 进入门控集合后即自动
  改用「怎样才会自动点亮」的说明，与猫狗 M3/M5 完全同构。
- `milestone_checkin_prompt_copy.dart` 里 G-M1/G-M2 的旧提问式条目**保留未删**，与猫狗四条健康类
  的既有处置一致（它们同样早已是 dead copy）。删除属跨 story 的清理，不在本 story 范围。

**范围边界（按 AC7 未做）**

- 未改自动完成的寻址实现（属 Story 1.1，已在上一条 story 完成）；未改灰徽章点击去向（属 Story 1.3）；
- **未新增「体检」健康记录类型** —— `health_records.type` 的 CHECK 约束一字未动（决策 A-1 已接受
  「只做体检、没打疫苗的用户点不亮 G-M2」这一代价）。
- 未清理存量脏完成行（决策 A-9 / AD-A5.3c）。

### 🔴 L1/L2 待本地验收

**L1（需 Docker postgres + redis，云端无 Docker daemon 跑不了）**

| # | 验什么 | 怎么验 | 对应 AC |
|---|---|---|---|
| 1 | 🔴 **存量宠物**迁移生效 | **必须用一只上线前就建档的 OTHER 宠物**（新建档的测不出问题：`assignRoster` 铺的是新目录）。跑迁移 → 查 `pet_milestones`，`G-M1`/`G-M2` 的 `trigger_type` = `SYSTEM_AUTO` | AC2 |
| 2 | 迁移幂等 | 连跑两次，第二次影响 0 行 | AC2 |
| 3 | 存量完成态不回滚 | 迁移前造一条「已通过打卡完成 G-M1」的完成行 → 迁移后该完成行仍在、`completed_at` 不变 | AC5 |
| 4 | 真人问诊结束 → `G-M1` | OTHER 宠物走完一次真人兽医会话 → 查完成行 | AC3 |
| 5 | 录 `VACCINE` → `G-M2` | OTHER 宠物录疫苗 → 查完成行 | AC3 |
| 6 | 打卡被后端拒绝 | 直接 POST 打卡 `G-M1` / `G-M2` → 返回校验错误（含「自动点亮」字样） | AC4 |
| 7 | 反向回归 | 打卡 `G-M3`（陪伴满 30 天）/ `G-M4`（记录满 10 条）**仍须放行** —— 按后缀判会误伤这两条 | AC4 |

**L2（需真机 / 模拟器视觉，云端 headless 跑不了）**

| # | 验什么 | 怎么验 | 对应 AC |
|---|---|---|---|
| 8 | 🔴 **存量 OTHER 宠物**的里程碑列表页不再渲染打卡按钮 | 用第 1 条那只老宠物登录 → 里程碑列表页 → 点 `G-M1`/`G-M2` 灰徽章 → **不出现「已打卡」「去发布」两个按钮** | AC2 |
| 9 | 说明文案已换 | 同上弹层里显示的是「自动点亮」说明（`milestoneHintAutoHealth`），不是提问式打卡引导 | AC6 |
| 10 | EN / ID 两套语言各看一遍 | 切 en 与 id，两种语言下第 9 条的文案都正确（**无中文包，不要找中文**） | AC6 |

### File List

**新增**
- `petgo-backend/src/main/resources/db/migration/V20260910_1053__generic_health_milestones_auto_trigger.sql`
- `petgo-backend/src/test/java/com/tailtopia/profile/domain/HealthMilestonesCrossRepoTest.java`

**修改（后端）**
- `petgo-backend/src/main/java/com/tailtopia/profile/domain/MilestoneCatalog.java`（G-M1/G-M2 → SYSTEM_AUTO）
- `petgo-backend/src/main/java/com/tailtopia/profile/domain/HealthMilestones.java`（改完整 code 集合，+G-M1/G-M2）
- `petgo-backend/src/main/java/com/tailtopia/profile/domain/MilestoneAutoCompleteMap.java`（CONSULT_CLOSED · OTHER → G-M1）
- `petgo-backend/src/main/java/com/tailtopia/profile/service/TimelineClassifier.java`（集合改派生）
- `petgo-backend/src/test/java/com/tailtopia/profile/service/HealthMilestoneCheckInRefusedTest.java`
- `petgo-backend/src/test/java/com/tailtopia/profile/service/HealthMilestoneAutoPathTest.java`
- `petgo-backend/src/test/java/com/tailtopia/profile/service/MilestoneAnalyticsTest.java`
- `petgo-backend/src/test/java/com/tailtopia/profile/domain/MilestoneAutoCompleteMapTest.java`

**修改（前端）**
- `petgo_app/lib/features/profile/domain/health_milestones.dart`（门控集合改完整 code，+G-M1/G-M2）
- `petgo_app/test/profile/health_milestone_checkin_removed_test.dart`

---

## Change Log

| 日期 | 变更 | 作者 |
|---|---|---|
| 2026-09-10 | 创建 | bmad-create-epics-and-stories |
| 2026-09-10 | 实现完成，L0 全绿，转 review（L1/L2 待本地验收） | dev-story（云端） |
