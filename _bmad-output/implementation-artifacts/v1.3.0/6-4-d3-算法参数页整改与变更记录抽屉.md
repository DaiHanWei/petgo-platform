---
baseline_commit: 4301227a
branch: feat/1.3.0-ops-ui-refactor
epic: 6
story: 6.4
ad: [AD-9, AD-11]
decisions: [D-10]
depends_on: [2-3a, 2-3b]
---

# Story 6.4: D3 算法参数页整改与变更记录抽屉

Status: review

> 自包含 story。后台模板 + 一个新的只读 fragment 端点；`POST /admin/algo-params` 与全部校验**不变**。依赖 2-3a / 2-3b。

## Story

As a 产品，
I want 算法参数页去掉编号术语、11 项一行一项两栏排布，历史变更点按钮进抽屉筛着看，
so that 页面清爽、历史可查而不占版面。

## Acceptance Criteria

**AC1 · 页头与分组**
**Given** `/admin/algo-params` 套 `tpl-d-config-card`
**Then** 页头无「FR-95」等编号；分组名「算法参数」（原「首页推荐 / 打分参数」）；右栏底部保留「不对普通运营开放 / 生效范围」提醒 `[L2]`

**AC2 · 参数排布**
**Then** 全部参数（现表单 14 个输入：新鲜度 / 互动度 / 评论权重 / 曝光衰减 / 抖动 / 限流系数 / 曝光窗口 / 配比窗口 / 属性配比 ×3 / 物种配比 ×3）一行一项、两栏排布填满卡宽；PRD 写「11 项」，**以代码 14 项为准**（见 Dev Notes）`[L2]`
**And** 保存 = 确认弹层复述改前改后（模板 D 通用三态，同 6.3）；校验错误行内呈现（配比之和 / 区间等 `admin.err.config.*` 现有码全部保留）`[L1/L2]`

**AC3 · 变更记录进抽屉**
**Then** 页尾常驻表删除；页头「变更记录」按钮 → `GET /admin/algo-params/changes/drawer?field=&from=&to=&page=` 返抽屉 fragment：时间（WIB）/ 参数 / 改前 / 改后 / 操作人，每页 20，按参数下拉（14 个字段名 message key）与时间段筛选；默认最近 20 条 `[L1/L2]`
**And** 数据来自 `config_change_logs` `config_type=FEED_RANK`（现状 `findTop20…` 只取 20 条，本 story 改为分页 + 筛选查询）`[L1]`

**AC4 · 权限不变**
**Then** 查看 / 修改沿用 `AdminAlgoParamController` 现有 `VIEW` / `EDIT` 常量；抽屉端点用 `VIEW` `[L1]`

**AC5 · 三语与回归**
**Then** 新 key `admin.v130.algo.*`（按钮 / 抽屉标题 / 筛选标签 / 空态）三包；既有 `admin.algo.*` key 中含「FR-95」「打分参数」的文案改词；`AdminAlgoParam` 相关测试全绿 `[L0/L1]`

---

## Tasks / Subtasks

- [x] **T1 · 仓储**：`ConfigChangeLogRepository` 新增 `Page<ConfigChangeLog> findByConfigTypeAndFieldContainingAndChangedAtBetween(...)` 或 Specification（字段可空 → 用 `@Query` 带 `(:field IS NULL OR field = :field)`）
- [x] **T2 · Controller**：`GET /admin/algo-params/changes/drawer`（`VIEW`）→ model `changes`（Page）、`fields`（14 个字段名）、筛选回显 → 返 `admin/fragments/drawer-algo-changes :: drawer`；`GET /admin/algo-params` 不再塞 `changeLogs`
- [x] **T3 · 模板**：`algo-params.html` 套 tpl-d：单卡「算法参数」两栏 14 项（`grid-template-columns: 1fr 1fr`，每项独占一行格）+ 右栏提醒；删 `data-section="algo-changelog"` 表；页头按钮 `hx-get` 抽屉（2-3b `admin-drawer.js`）
- [x] **T4 · 抽屉 fragment**：`fragments/drawer-algo-changes.html`：筛选行（参数下拉 + 起止日期 WIB + 查询钮，`hx-get` 自刷）+ 表 + 分页
- [x] **T5 · 文案**：`admin.algo.title/section.feedRank` 去编号改名；新 key 三包
- [x] **T6 · 测试**：抽屉端点 200 fragment / 403；筛选 field + 时间段命中；`POST /admin/algo-params` 回归（校验码不变）
- [x] **T7 · 云端执行须知**：同前

---

## Dev Notes

### 🔴 PRD「11 项」与代码 14 个输入不一致——以代码为准

`FeedRankForm` 实际字段：freshnessWeight、interactionWeight、commentWeight、exposureDecay、shuffleStrength、throttleFactor、seenWindowDays、windowSize、attrFun/Edu/LifeQuota、speciesMain/Other/GeneralQuota = **14**。PRD/UI 稿写 11 是把三个配比组各算一项。一行一项就是 14 行，两栏 7×2。已回报给上游，story 不等 PRD 改。

### 🔴 变更记录已是 WIB

现状表头 `admin.algo.col.at`=「时间（WIB）」，值经 `@adminTime.wib(...)`，抽屉照抄即可，**不要**自己另起格式化（AD 模式规则：WIB 单一出口 `AdminTime`）。

### 🔴 校验逻辑一行不动

`AdminConfigService.updateFeedRank` 十余条 `require` 与 `AttributeTemplate.rejectUnusableQuotasDetailed` 是「保存通过了但生成出来的模板是坏的」的唯一防线；本 story 只改展示。

### 现状代码要点

| 文件 | 现状 | 改什么 | 保留 |
|---|---|---|---|
| `admin/config/web/AdminAlgoParamController.java` | `GET` 塞 `feedRank` + `findTop20ByConfigTypeOrderByChangedAtDesc(FEED_RANK)`；`POST` 校验保存 | GET 去 changeLogs；+ 抽屉端点 | `VIEW`/`EDIT`、POST |
| `config/repository/ConfigChangeLogRepository.java` | `findTop100…`、`findTop20ByConfigType…` | + 分页筛选查询 | 旧方法（其它页可能在用） |
| `templates/admin/algo-params.html`（139 行） | 单栏表单 + 页尾变更表 + 4 段 notice | 套 tpl-d 两栏；删表；加按钮 | notice 文案（去编号） |
| `admin/web/AdminTime.java` | `@Component("adminTime")` WIB 格式化 | 不改 | — |

### References

- [Source: PRD §5 ③ 第 8 条、决策日志 D-10]、[逐页规格 D3]、[UI 稿 7-4 / 7-5]、[architecture AD-9 / AD-11]、[epics Story 6.4]
- [Source: petgo-backend …/admin/config/web/AdminAlgoParamController.java]、[…/templates/admin/algo-params.html]

## Dev Agent Record

### Agent Model Used

claude-fable-5-1（云端 headless session，2026-09-09）

### Debug Log References

- 云端 L0：`./mvnw -B clean package`（排除需真库的 Spring 上下文测试类）→ BUILD SUCCESS，1828 tests，0 failures；`AdminTemplateStructureTest`（新页 / 抽屉 fragment 过守卫）、`AdminTemplateShellRenderTest`、`AdminMessagesExternalizedTest` / `AdminMessagesParityTest`、`AdminPageCatalogTest` 全绿。**`POST /admin/algo-params` 与全部校验一行不动、无迁移**。
- `check-i18n-keys.sh` OK（+7 key：`admin.v130.algo.{drawer.title,filter.field,filter.all,filter.from,filter.to,filter.query,scope}`；`admin.algo.section.feedRank` 四包改词「首页推荐算法」→「算法参数」，`admin.flash.algo.paramsSaved` / `admin.algo.noAbTest` / `admin.algo.quotaNote` 改词指向页头抽屉；按钮 / 空态复用既有 `admin.algo.changelog` / `changelog.empty`；仓库文案里本就无「FR-95」「打分参数」）。
- 复审（bmad-code-review，CONFIRMED 1 条已修 + PLAUSIBLE 3 条已修 + 偏差说明 8 条）：① 🔴 L1 复用同一个 `MockHttpServletRequestBuilder` 三次 `perform`，`param` / `header` 就地累加（第二次 `throttleFactor` 变成双值取首个、第三次仍带 `HX-Request`）→ 改工厂方法每次新建 builder，首个用例真改限流系数并断言落库 + 抽屉里能看到该条变更，`finally` 还原共享单行；② 文案仍指向「下方变更记录」→ `admin.flash.algo.paramsSaved` / `admin.algo.noAbTest` 四包改「页头「变更记录」」，`quotaNote`「下面两组配比」改「属性 / 物种两组配比」；同时复用既有 `admin.algo.changelog` / `changelog.empty` 作按钮 / 抽屉空态文案（不再新增重复 key，也不留孤儿）；③ 「生效范围」提醒整宽且与页顶重复、配比说明方位反了 → scope 提醒放进两栏网格右栏末格（`.cfg-grid-right`），配比说明放回配比行之前（`.cfg-grid-full`），限流说明留卡底；④ 手改 URL 的非法日期 / 页码会以 400 ProblemDetail JSON 被 htmx swap 进抽屉 → `from / to / page` 改收 String 宽松解析（非法即不筛 / 第 0 页），`field` 不在 14 列名内也归空；排序改由 `Pageable` 携带（`changedAt desc, id desc`），Specification 不再 `orderBy`。偏差说明（不改代码）：操作人显示名优于裸 id；14 项以代码为准；`findTop20ByConfigTypeOrderByChangedAtDesc` 按 story 保留（现无调用方）；分页文案借用 `admin.v130.places.pager.*`；页头按钮走 `data-drawer-open` 而非 `hx-get`（等价）；`AuditLogAdminController` 的日期区间按 UTC 折算与本页 WIB 口径不同（属 6.5 范围）。

### Completion Notes List

- **L1/L2 待本地验收**：① `AdminAlgoParamDrawerIntegrationTest`（真库 + 真 Thymeleaf）：整页单卡 `#cfg-algo`（`hx-post` / `action` 均 `/admin/algo-params`）+ 页头「变更记录」按钮（`data-drawer-open=/admin/algo-params/changes/drawer`）+ 抽屉壳 `#algo-drawer`，14 个输入齐、`.cfg-grid-2` 两栏，三段提醒锚点（`algo-not-for-ops` / `algo-no-ab` / `algo-scope`）在、页尾 `data-section="algo-changelog"` 表不在、无「FR-95」「打分参数」；只持 `config.algo_param_view` → 卡只读（输入禁用 + 「需要「改算法参数」权限」）；抽屉：造 3 条 FEED_RANK 变更（一条拨到 40 天前）→ 全部列出（≤20 行、参数显示名、WIB 时间、操作人显示名）；按参数筛选只剩该参数且下拉回显；按 WIB 起止日筛选命中 / 排除；无命中空态；非 htmx 302 整页；无权限 403；保存回归：htmx 真改限流系数 → 落库 + 200 卡 fragment + `HX-Retarget #cfg-algo` + toast oob，抽屉里立刻可见该条变更；限流系数取 1 → 422 `data-code="admin.err.config.throttleFactorRange"`（校验码不变）且库值不变；整页 PRG 不变。② L2：本地 stag 对照 UI 稿 7-4 / 7-5：单卡两栏 7×2、保存钮三态 + 确认弹层复述改前改后（同 6.3）、校验错误红边 + 卡脚红字、点「变更记录」右侧抽屉（筛选行 + 表 + 分页）、切 ID 无截断。
- **AC1 页头与分组**：页头 h1「算法参数」（无编号；仓库文案本就没有「FR-95」）+ 「变更记录」按钮；分组名 `admin.algo.section.feedRank` 改「算法参数」；「不对普通运营开放 / 生效范围」提醒 = 两栏网格右栏末格（`admin.v130.algo.scope`「不对普通运营开放；生效范围全平台首页推荐序，改这里无需发版、下一次刷新即生效」），配比说明在配比行前、限流说明在卡底。
- **AC2 参数排布**：`algo-params.html` 套 `tpl-d-config-card`（6.3 扩展后的壳：`cardId='cfg-algo'`、`hxPost`、`confirmDiff`、`errorFields`、`readonly` 按 `config.algo_param_edit`）；14 项一行一项两栏（`.cfg-grid-2`，窄屏单栏）；保存 = 模板 D 三态 + `data-confirm-diff` 复述改前改后；htmx 提交成功回 `admin/algo-params :: saved`（卡 + toast oob，`HX-Retarget #cfg-algo` / `outerHTML`），失败由 `AdminBusinessExceptionAdvice` 出 422 行内 err，`ERROR_FIELDS`（13 个 `admin.err.config.*` 码 → 字段）给对应输入红边；非 htmx 维持 PRG + flash。**`AdminConfigService.updateFeedRank` 与 `AttributeTemplate.rejectUnusableQuotasDetailed` 未动**。
- **AC3 变更记录抽屉**：页尾常驻表删除；`GET /admin/algo-params/changes/drawer?field=&from=&to=&page=`（`VIEW`；非 htmx 302 整页）→ `fragments/drawer-algo-changes :: drawer`：筛选行（参数下拉 = `FIELDS` 14 个列名 → `admin.algo.*` 标签 key；起止日期 `type=date`，按 WIB 自然日折算为 UTC 半开区间 `[from 00:00, to+1 00:00)`，与审计页同口径；查询钮 `hx-get` 自刷本体）+ 表（时间 `@adminTime.wib` / 参数显示名 / 改前 / 改后 / 操作人显示名，`changed_by` → `AdminAccountRepository.findAllById` 批量取名，缺失回退 `#id`）+ 分页每页 20（`PAGE_SIZE`，默认最近 20 条）。数据 = `ConfigChangeLogRepository.search(FEED_RANK, field, from, to, pageable)`（新增 `JpaSpecificationExecutor` + default 方法；排序由 `Pageable` 携带 `changed_at desc, id desc`）；`findTop20ByConfigTypeOrderByChangedAtDesc` 保留未删（其它调用方可能在用）。抽屉壳：`tpl-shared :: drawer(res, title)` 新增独立壳（与 `tpl-b-list` 内抽屉同构），`admin-drawer.js` 的 `[data-drawer-open]` 入口（5.4 加的）打开。
- **AC4 权限不变**：查看 / 修改沿用 `AdminAlgoParamController.VIEW / EDIT`；抽屉端点 `VIEW`；模板只读态用 `#authorization.expression` 同字面 `config.algo_param_edit`。
- **AC5 三语与回归**：`admin.v130.algo.*` 七个 key 三包 + 默认包，三条既有 `admin.algo.*` 改词；`AdminPagesRenderSmokeTest.algoParamPageRendersItsFormAndChangeLog` 锚点由页尾表改为抽屉入口（并反向断言表已删）；`pageHeadingsMatchTheirNavLabels` h1 未变。
- **偏差说明**：① PRD「11 项」以代码 14 个输入为准（Dev Notes）；② 抽屉筛选行沿用 `data-autosubmit`（改下拉 / 日期即查），查询钮保留；③ 操作人显示后台账号显示名而非 id（AC 未指明，比裸 id 可读）；④ `AdminAlgoParamController` 新增 `AdminAccountRepository` / `AdminTime` 注入（WIB 单一出口，不自起格式化 / 时区）。

### File List

- petgo-backend/src/main/java/com/tailtopia/admin/config/web/AdminAlgoParamController.java（去 changeLogs 模型；+ htmx 保存分支、`FIELDS` / `ERROR_FIELDS`、`GET /admin/algo-params/changes/drawer`）
- petgo-backend/src/main/java/com/tailtopia/config/repository/ConfigChangeLogRepository.java（+`JpaSpecificationExecutor` / `search`）
- petgo-backend/src/main/resources/templates/admin/algo-params.html（套模板 D 两栏 + 抽屉入口 + `saved` fragment）、fragments/drawer-algo-changes.html（新增）、fragments/tpl-shared.html（+`drawer(res, title)` 壳）
- petgo-backend/src/main/resources/static/admin/admin.css（`.cfg-grid-2`）
- petgo-backend/src/main/resources/i18n/messages{,_zh_CN,_en,_id}.properties（+7 key，`admin.algo.section.feedRank` 等四条改词）
- 测试：admin/config/AdminAlgoParamDrawerIntegrationTest（新增 L1）、admin/web/AdminPagesRenderSmokeTest（锚点）
