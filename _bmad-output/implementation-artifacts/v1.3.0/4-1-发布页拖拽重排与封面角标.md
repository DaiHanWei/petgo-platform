---
baseline_commit: ac3d57b733769b3bbc374e82db59a07082e26522
epic: 4
story: 1
batch: A
verification: [L0, L2]
---

# Story 4.1: 发布页拖拽重排与封面角标

Status: ready-for-dev

## Story

As a 发帖用户，
I want 长按拖动照片调整顺序、并知道哪张是封面，
so that 我不用为了换个顺序把照片删了重选。

---

> **现状**：`publish_compose_page.dart` 的 `_imageRow` 按用户选择顺序排列，**没有任何排序手段**。选错顺序只能删掉重选。
>
> 首图同时决定 **Feed 卡片封面、详情页首图、分享卡主图** —— 三处都由这一个顺序决定。
>
> **零后端、零迁移、零依赖，可随时插队开工。**

---

## Acceptance Criteria

### AC1 — 拖拽重排（L2）

**Given** 发布页已选若干张照片
**When** 长按其中一张
**Then** 该张抬起（放大 + 阴影） `[L2]`
**When** 拖到目标位置并松手
**Then** 其余照片自动让位，顺序更新 `[L2]`

### AC2 — 封面角标（L2）

**Given** 照片网格
**When** 渲染
**Then** 第一张左上角常驻「封面」角标 `[L2]`
**And** 文案为 EN `Cover` / ID `Sampul`（新增 l10n key，项目内无现成） `[L0]`
**And** 重排后角标实时跟随新的第一张 `[L2]`

### AC3 — 顺序只有一份（L0 / L2）

**Given** 用户调整后的顺序
**When** 点击发布
**Then** 按调整后的顺序上传与展示 `[L2]`
**And** 界面顺序与上传顺序是**同一份数据**，不存在两套 `[L0]`
**And** 顺序的唯一住所是发布页的表单状态 `[L0]`

### AC4 — 🔴 上传期锁定（L2）

**Given** 上传已经开始（状态机已取走顺序快照）
**When** 用户尝试拖拽重排
**Then** 重排入口不响应 `[L2]`
**And** 封面角标不再跟随 `[L2]`
**And** 锁定持续到上传结束或失败回到可编辑态 `[L2]`

> 🔴 「上传中还能拖」必然产生两份顺序：界面一份、上传状态机手里的快照一份。

### AC5 — 部分成功后重试（L2）

**Given** 上传部分成功后重试
**When** 继续上传
**Then** 沿用开始时取的顺序快照，**不重新读取表单状态** `[L2]`
**And** 用户若要改顺序，须先整体取消回到可编辑态 `[L2]`

### AC6 — 仅发布前可调（L0）

**Given** 已发布的内容
**When** 查看
**Then** 不提供图片重排入口 `[L0]`

> 已发布内容的图片重排属内容编辑范畴，**本版本不做**。

### AC7 — 网格其余部分不变（回归保护，L2）

**Given** 照片网格
**Then** 3 列布局、虚线添加格、右上角 ✕ 删除均与现状一致 `[L2]`

### AC8 — 不引包（L0）

**Given** `pubspec.yaml`
**When** 本 story 提交
**Then** **未新增任何 reorder 类第三方依赖** `[L0]`

---

## Tasks / Subtasks

- [ ] 九宫格支持长按抬起 + 拖拽 + 让位（自实现）（AC1）
- [ ] 首图封面角标，随重排跟随；新增两语 l10n key（AC2）
- [ ] 顺序单一数据源；上传状态机取快照（AC3）
- [ ] 上传开始即锁定重排入口（AC4/AC5）
- [ ] 真机走查：拖拽手感、角标跟随、上传中锁定、部分失败重试（AC1/AC2/AC4/AC5）

## Dev Notes

### 要改的文件

| 文件 | 改什么 |
|---|---|
| `petgo_app/lib/features/content/presentation/publish_compose_page.dart` | `_imageRow` 加拖拽与角标；顺序单一数据源；上传期锁定 |
| `petgo_app/lib/l10n/app_en.arb` / `app_id.arb` | 「封面」角标 key |
| `petgo_app/pubspec.yaml` | **不得改动** |

### 角标不落字段

「封面」只是顺序的**投影**，不新设「哪张是封面」的字段。另设字段必然出现「界面第一张是 A、封面却是 B」。

### 验证层级

- `[L0]` 编译、无新依赖、无封面字段
- `[L2]` **拖拽手感必须真机验**；上传中锁定与部分失败重试要专门构造

### References

- 架构 delta `AD-A16` `AD-A27`（顺序住所与上传锁定）· `文案需求清单.md` §3

---

## Dev Agent Record

### Context Reference

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

---

## Change Log

| 日期 | 变更 | 作者 |
|---|---|---|
| 2026-09-10 | 创建 | bmad-create-epics-and-stories |
