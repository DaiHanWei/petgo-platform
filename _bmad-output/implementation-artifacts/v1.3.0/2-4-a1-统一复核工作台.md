---
baseline_commit: 4301227a
branch: feat/1.3.0-ops-ui-refactor
epic: 2
story: 2.4
ad: [AD-9, AD-11]
decisions: [D-8, D-14]
depends_on: [2-3a, 2-3b]
---

# Story 2.4: A1 统一复核工作台

Status: ready-for-dev

> 自包含 story。与用户沟通用中文。执行纪律见根 `CLAUDE.md`（后端→前端→联调、AC 标 L0/L1/L2、`mvn -B clean package`）。
> 本 story 是**纯页面重构**：不新增、不删除、不改任何写端点的路径 / 参数 / 权限；只把 `manual-review.html` 从「混排 + 类型筛选 + 独立 reports 页」改成模板 A 双栏工作台，并让 `reports.html` 页面路由退役。
> 前置：2-3a（`HxRequest` / 422·403 fragment / `HX-Trigger admin:badge-refresh` / `AdminExportWriter`）与 2-3b（`tpl-a-workbench` 壳、`admin-workbench.js`）。

## Story

As a 运营，
I want 内容送审、内容举报、名称审核、头像审核在一个双栏工作台里按页签清队列，处置完自动到下一条，
so that 早巡不用在四个页面间来回点。

## Acceptance Criteria

**AC1 · 页签与筛选（模板 A 顶部两行）**
**Given** `/admin/manual-review`（`QUEUE_AUTH` 不变）
**When** 打开
**Then** 页签行 = 内容送审（`CONTENT_SUBMISSION`）｜ 内容举报（`CONTENT_REPORT`）｜ 名称审核（`ACCOUNT_IDENTITY` 的 `NICKNAME` + `PET_NAME` 两 subType）｜ 头像审核（`USER_AVATAR` + `PET_AVATAR`），各带计数 `[L1]`
**And** 筛选行 = 违规类别（七选）· 优先级（P0/P1/P2）· 关键词（账号 id / 昵称）；名称 / 头像页签追加 subType 二选（用户 / 宠物）；下拉 `data-autosubmit`，文本搜索配显式「查询」钮 `[L2]`
**And** 现状的「type 筛选下拉 + 混排」删除，由页签取代；`?type=` 查询参数继续被接受（旧链接兼容，映射到对应页签）`[L1]`

**AC2 · 左栏队列行**
**Then** 行高 ~56px 两行式：第一行 = 对象标识（送审 / 举报 = `preview` 摘要；名称 = 待审名称文本；头像 = `targetNickname`）+ 右对齐状态色点；第二行 = 违规类别标签 + 优先级分 `score` + `earliestAt` 相对时间 `[L2]`
**And** `overdue=true`（送审挂起 >24h）整行红字 + 红点 + 挂起时长标签（现有规则原样保留）`[L1]`
**And** 默认排序：优先级分降序、同分按 `earliestAt` 升序；每页 20（`PAGE_SIZE` 沿用），滚动到底 htmx 加载下一页 `[L1]`
**And** 左栏顶部「待处理｜已处理」两态页签；已处理 = 只读回看（处置结果徽标 + 操作人 + 时间 + 理由，可筛结果 / 时间 / 关键词）`[L1]`

**AC3 · 右栏三卡（按页签渲染）**
**Then** ① 对象快照卡：送审 / 举报 = 帖子全文 + 全部图片（点开大图）+ 作者行（头像 / 昵称 / `targetUserId`，`targetDeleted` 置灰标注）；名称 = 新旧名称对照（大字号待审名）；头像 = 待审头像大图 + 当前生效头像对照 `[L2]`
**And** ② 上下文卡：举报 = 该帖全部举报列表（`reporterCount` / `reportCount` / `frequentCount` + 各条类型与时间）；送审 = 机审命中原因；名称 / 头像 = `disposalCount` 历史处置次数 `[L2]`
**And** ③ 操作区：按下表渲染；**选择参数类操作常驻操作区，未选全按钮禁用，不做「点按钮→展开面板」二级步** `[L2]`

**AC4 · 操作表（端点 / 权限 / 参数零变更）**

| 页签 | 按钮 | 端点 | 权限（现状常量） | 确认 | 成功后 |
|---|---|---|---|---|---|
| 内容送审 | 通过（放行发布） | `POST /admin/manual-review/{itemId}/approve` | `DECIDE_AUTH` = SUPER_ADMIN or `content.takedown` or `content.manual_review` | 无 | 下一条 |
| 内容送审 | 拒绝（丢弃，从未公开） | `POST /admin/manual-review/{itemId}/reject`（`category` 七选 + `note`） | 同上 | **原因未选拒绝钮禁用**，无弹层 | 下一条 |
| 内容送审 | 调优先级 P0/P1/P2 | `POST /admin/manual-review/{itemId}/priority`（`priority`） | 同上 | 无 | 停留本条 |
| 内容举报 | 下架（已公开内容） | `POST /admin/reports/{id}/takedown` | SUPER_ADMIN or `content.takedown` | `data-confirm` | 下一条 |
| 内容举报 | 无需处置（驳回本条） | `POST /admin/reports/{id}/dismiss` | 同上 | 无 | 下一条 |
| 内容举报 | 整帖一键驳回 | `POST /admin/reports/post/{postId}/dismiss-all` | 同上 | `data-confirm` 复述举报条数 | 下一条 |
| 名称审核 | 通过 / 违规（重置昵称或宠物名） | `POST /admin/name-moderation/{recordId}/decide` | SUPER_ADMIN or `content.takedown` | 违规需 `data-confirm` | 下一条 |
| 头像审核 | 通过 / 违规（重置默认头像） | `POST /admin/avatar-review/{reviewId}/decide` | SUPER_ADMIN or `content.takedown` | 违规需 `data-confirm` | 下一条 |

**Then** 上表 8 个端点的路径、请求参数名、`@PreAuthorize` 表达式与现状逐字一致（写操作清单 diff 为零）`[L0]`

**AC5 · 处置动线 = htmx 局部更新**
**When** 右栏任一操作提交（带 `HX-Request`）
**Then** 成功 200：右栏 fragment（含 `data-next-id`）+ `hx-swap-oob` 带出左栏该行（转终态压暗）与页签计数 + 响应头 `HX-Trigger: {"admin:badge-refresh":{}}`；`admin-workbench.js` 读 `data-next-id` 自动请求下一条；toast 2s `[L1]`
**And** 失败 422：操作区 fragment 行内 err，不跳条、不整页报错；403：禁用态 fragment 注明所缺权限名 `[L1]`
**And** 非 htmx 请求（无 `HX-Request`）维持现状 PRG（同一 Controller 方法两条路径共用 Model）`[L1]`
**And** **不做键盘 ↑/↓ 切换**（D-14）`[L0]`

**AC6 · 总开关归位**
**Then** 「人工复核总开关」（`POST /admin/settings/manual-review`，`TOGGLE_AUTH` = SUPER_ADMIN）从页内移到右上齿轮弹层，弹层注明当前开关态与影响范围；非超管不渲染齿轮 `[L2]`

**AC7 · `reports` 页面退役**
**Then** `GET /admin/reports`（`AdminWebController:131`）与 `reports.html` 删除；`POST /admin/reports/{id}/takedown`、`/dismiss`、`/post/{postId}/dismiss-all`、`/batch` 四个写端点**保留**（页签调用；`batch` 本页不用但不删）`[L1]`
**And** 侧导航无「举报队列」项；`AdminPageCatalog` 中 `reports` 页面维度标记退役、其权限码 `content.view_reports` 仍在矩阵 `[L1]`

**AC8 · 边界与权限态**
**Then** 被审对象已被作者删除 → 快照卡「内容已删除」占位，下架钮禁用，驳回类可清队列；`targetDeleted` → 名称 / 头像单据只读作废态 `[L1]`
**And** 有 `QUEUE_AUTH` 无 `DECIDE_AUTH`（理论上 `content.manual_review` 已含在 DECIDE 内，此处覆盖仅 `content.view_*` 的组合）→ 操作区按钮禁用 + 注明所缺权限的中文标签 `[L2]`
**And** 某页签清空 → 空态插画 +「队列已清空 🎉」+「其他队列还有 N 条」去向链接 `[L2]`
**And** 页签结构可扩展：`TicketType` 新值（Story 5.4 的 `PLACE_REPORT`）加一行即出现新页签 `[L0]`

**AC9 · 测试与三语**
**Then** MockMvc 四条：整页 200 / `HX-Request` 返 fragment / 422 行内 err / 403 禁用态；既有 `ManualReviewAdminController` 测试全绿 `[L1]`
**And** 新增 key 走 `admin.v130.review.*`，zh/en/id 三包同批；切 ID 语言页签、按钮、表头无截断 `[L2]`

---

## Tasks / Subtasks

- [ ] **T1 · Controller：整页 / fragment 双路径**（AC1、AC5）
  - [ ] `ManualReviewAdminController.queue` 增 `tab`（默认 `CONTENT_SUBMISSION`）、`state=pending|handled`、`subType`、`page` 参数；`?type=` 兼容映射到 `tab`
  - [ ] 新增 `GET /admin/manual-review/{itemId}/detail`（fragment，右栏三卡）与 `GET /admin/manual-review/queue`（fragment，左栏分页）
  - [ ] 8 个写方法：`HxRequest` 为真 → 处置后返回右栏 fragment + oob 片段 + `HX-Trigger`；否则原 `redirect:` 不动
  - [ ] `data-next-id` 由 `UnifiedTicketQueryService` 按当前页签 / 筛选 / 排序取「下一条」（新增只读方法，不改既有 CTE 语义）
- [ ] **T2 · 已处理两态**（AC2）
  - [ ] `UnifiedTicketQueryService` 增 `state` 过滤：`pending` = 现状；`handled` = 终态单据（`ManualReviewItem` / `ContentReport` / 名称·头像审核记录的已决状态），列出结果 / 操作人 / 时间 / 理由（字段现成，读各自 domain）
- [ ] **T3 · 模板**（AC2、AC3、AC6、AC8）
  - [ ] `manual-review.html` 改为 `th:replace="admin/fragments/tpl-a-workbench :: shell(...)"` 填槽：tabs / filters / queue / detail / actions
  - [ ] 三张卡片 fragment：`fragments/review-snapshot.html`、`review-context.html`、`review-actions.html`（按 `subType` 分支）
  - [ ] 拒绝原因七选 + 备注常驻 `review-actions`（送审页签），`category` 未选时拒绝钮 `disabled`
  - [ ] 齿轮弹层承载 `settings/manual-review` 表单（`sec:authorize="hasRole('SUPER_ADMIN')"`）
  - [ ] 空态 + 去向提示（其他页签计数来自同一聚合查询）
- [ ] **T4 · `reports` 退役**（AC7）
  - [ ] 删 `AdminWebController` 的 `GET /admin/reports` 方法与 `reports.html`；保留四个 `POST /admin/reports/**`
  - [ ] 全库 grep `admin/reports` 的 `th:href` 引用清零（导航、其他页链接）
  - [ ] `AdminPageCatalog` 标记退役
- [ ] **T5 · 三语**（AC9）：`admin.v130.review.tab.*` / `.state.*` / `.empty.elsewhere` / `.gear.*`，复用既有 `admin.review.*` 不重复造 key
- [ ] **T6 · 测试**（AC9）
  - [ ] 新增 `ManualReviewWorkbenchMvcTest`：四条标准测试 + `?type=` 兼容 + `data-next-id` 存在
  - [ ] 既有 `ManualReviewAdminController` / `NameAvatarReviewAdminController` / `NameModerationAdminController` 测试不改语义
  - [ ] 写操作清单 diff：本 story 后 `list-admin-write-ops.sh` 与基线相比**零差异**（`GET /admin/reports` 属 GET 不计入写操作）
- [ ] **T7 · 云端执行须知**：云端只跑 L0（`mvn -B clean package -DskipITs`）；L1/L2 留本地 stag，Completion Notes 标注

---

## Dev Notes

### 🔴 本 story 的红线：写端点一个字都不改

8 个写端点分散在 4 个 Controller（`ManualReviewAdminController`、`AdminWebController` 的 `reports/**`、`NameModerationAdminController`、`NameAvatarReviewAdminController`）。重构只允许：① 在方法内按 `HxRequest` 分叉返回 fragment；② 新增只读 GET fragment 端点。任何 `@PostMapping` 路径 / `@RequestParam` 名 / `@PreAuthorize` 表达式的改动都会让 Epic 11 的清单 diff 变红。

### 现状代码要点

| 文件 | 现状 | 改什么 | 必须保留 |
|---|---|---|---|
| `admin/moderation/web/ManualReviewAdminController.java` | `GET /admin/manual-review`（`type` 筛选、`QUEUE_AUTH`）；`POST settings/manual-review`（`enabled`，`TOGGLE_AUTH`）；`approve` / `reject(category, note)` / `priority(priority)`（`DECIDE_AUTH`） | 加 `tab/state/subType` 参数、两个 fragment GET、写方法 Hx 分叉 | 三个 AUTH 常量、`RedirectAttributes flash` PRG 路径 |
| `admin/moderation/service/UnifiedTicketQueryService.java` | `UNIFIED_CTE` 把 `CONTENT_SUBMISSION / CONTENT_REPORT / ACCOUNT_IDENTITY(NICKNAME/PET_NAME/USER_AVATAR/PET_AVATAR) / ACCOUNT_REPORT` 合成 `UnifiedTicketRow`（字段：`sourceId, subType, targetUserId, targetNickname, targetDeleted, reporterCount, reportCount, frequentCount, score, earliestAt, preview, actionRef, contentRefId, disposalCount, overdue`） | 加 `state` 过滤 + `nextAfter(...)` 只读方法 | CTE 的排序与 `overdue` 口径；`ACCOUNT_REPORT` 属 A2 不在本页 |
| `admin/web/AdminWebController.java:131-190` | `GET /admin/reports`（`content.view_reports`）+ 四个 `POST /admin/reports/**`（`content.takedown`） | 删 GET + 模板；POST 全留 | 四个 POST 的路径与参数 |
| `namemoderation/web/NameModerationAdminController.java` | `POST /admin/name-moderation/{recordId}/decide`（`content.takedown`） | 仅 Hx 分叉返回 | 全部 |
| `admin/moderation/web/NameAvatarReviewAdminController.java` | `POST /admin/avatar-review/{reviewId}/decide`（`content.takedown`） | 仅 Hx 分叉返回 | 全部 |
| `templates/admin/manual-review.html`（304 行） | 已有 `hx-post` 到 name / avatar decide；拒绝面板为展开式；七类 `admin.review.cat.*`、`admin.review.overdue` 等 key 齐全 | 重写为模板 A 壳 + 三卡 fragment | 既有 `admin.review.*` key 全部复用 |
| `templates/admin/reports.html`（111 行） | 独立举报队列页（含 `reports/batch`） | 删除 | — |

### 「拒绝原因常驻」的实现细节

现状 `reject` 端点参数 `category`（required=false）+ `note`。UI 拍板（09-01）拒绝原因七选 + 备注**常驻**操作区，**未选原因时拒绝钮禁用**——前端 `required` + `disabled` 联动即可，后端保持 `required=false` 不动（避免改端点契约）；服务端若收到空 category 仍按现状处理。

### fragment 响应形状（AD-9）

- 成功：`review-actions` 主片段（根元素 `data-next-id="<下一条 sourceId 或空>"`）+ `<tr id="review-row-<id>" hx-swap-oob="true">`（终态压暗）+ `<span id="review-tab-count-<tab>" hx-swap-oob="true">`；`HX-Trigger: {"admin:badge-refresh":{}}`。
- 422 / 403 由 2-3a 的 `AdminBusinessExceptionAdvice` 统一返回，本 story 只保证异常带正确 `errKey`。
- htmx 1.9.12：4xx 渲染靠 `admin-core.js` 的 `htmx:beforeSwap` 放行（2-3a 已做），本 story 不加扩展。

### 已处理两态的数据来源

四类单据的「已处理」都在各自表有终态：`manual_review_items.status`（APPROVED / REJECTED）、`content_reports.status`（RESOLVED / DISMISSED）、名称 / 头像审核记录的决定字段。`UnifiedTicketQueryService` 新增 `state=handled` 分支读这些终态并带出操作人 / 时间 / 理由；不新建表。

### 退役清单（进 Epic 11 核对）

- 删除：`GET /admin/reports`、`templates/admin/reports.html`。
- 保留：`POST /admin/reports/{id}/takedown`、`/dismiss`、`/post/{postId}/dismiss-all`、`/batch`。
- 导航：`nav.html` 不再有「举报队列」；`AdminPageCatalog.reports` → `retired=true`。

### Project Structure Notes

- 改动落 `admin/moderation/**`、`admin/web/AdminWebController`、`templates/admin/manual-review.html` + 新 `fragments/review-*.html`；不新建模块。
- 依赖 2-3a `admin/shared/web/*` 与 2-3b `fragments/tpl-a-workbench.html`、`static/admin/admin-workbench.js`。

### References

- [Source: _bmad-output/planning-artifacts/v1.3.0/后台重构逐页规格.md#模板 A 通用规格 / A1 / 待办中心通用·已处理视图]
- [Source: _bmad-output/planning-artifacts/v1.3.0/ui-v1.3.0-admin.html 泳道 1 帧 1-1～1-5、1-15～1-19]
- [Source: _bmad-output/planning-artifacts/v1.3.0/architecture-v1.3.0-delta.md#AD-9 / AD-11]
- [Source: _bmad-output/planning-artifacts/v1.3.0/epics-v1.3.0.md#Story 2.4]
- [Source: _bmad-output/planning-artifacts/v1.3.0/决策日志.md D-8 / D-14]
- [Source: petgo-backend/src/main/java/com/tailtopia/admin/moderation/web/ManualReviewAdminController.java]
- [Source: petgo-backend/src/main/java/com/tailtopia/admin/moderation/service/UnifiedTicketQueryService.java]

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
