---
baseline_commit: 4301227a
branch: feat/1.3.0-ops-ui-refactor
epic: 7
story: 7.5
ad: [AD-9, AD-11]
decisions: [D-8, D-23]
page: B5
depends_on: [2-3a, 2-3b]
prd_exceptions: [§5 ③ 第 3 条（排期并入批量内容页签）]
---

# Story 7.5: B5 排期发布并入批量内容页签

Status: ready-for-dev

> 自包含 story。与用户沟通用中文。纪律见根 `CLAUDE.md`。依赖 2.3a / 2.3b。写端点 `content-schedules/{rowId}/time` 与 `/cancel` 零变更；变化是 GET 页面 `/admin/content-schedules` 退役、内容并入批量内容页第二页签、POST 成功后重定向目标改为批量内容页页签。

## Story

As a 运营，
I want 在「批量内容」页第二页签直接看排期、改时间、取消，
so that 发完一批不换页就知道什么时候发、发没发。

## Acceptance Criteria

**AC1 · 页签结构**
**Given** `/admin/seed-batches`
**Then** 页顶两页签「批次」｜「排期发布」（htmx 局部切换 `#batches-tab`）；现状页尾用 `th:replace="~{admin/content-schedules :: scheduleSection}"` 内嵌的排期区块**改为第二页签内容**，不再与批次表同屏堆叠 `[L2]`

**AC2 · 排期页签内容（模板 B）**
**Then** 摘要条：待发布 · 今日已发（WIB）· 失败；筛选：发布账号 `authorId`（现状有）· 状态 · 日期；表格列 = 内容摘要 ｜ 发布账号 ｜ 计划时间（WIB）｜ 状态 ｜ 失败原因 ｜ 操作；默认计划时间升序；每页 20 `[L1/L2]`
**And** 失败行红点 + 原因悬浮完整展示；已发布行只读 `[L2]`

**AC3 · 改时间 = 行内编辑态**
**When** 点「改时间」
**Then** 该行变为 WIB 日期时间输入 + 保存 / 取消（不弹层，UI 稿 2-23 注记 2026-09-02）→ `POST /admin/content-schedules/{rowId}/time`（参数不变）→ htmx 返回该行 fragment `[L1]`

**AC4 · 取消**
**Then** `POST /admin/content-schedules/{rowId}/cancel`，data-confirm（`admin.schedule.cancelConfirm` 沿用）→ 行 oob 刷新 + 摘要条刷新 `[L1]`

**AC5 · 详情抽屉**
**When** 点行（非操作区）→ `GET /admin/content-schedules/{rowId}/drawer`
**Then** 完整内容预览（正文 + 图缩略 + 发布账号 + 类型 + 物种）+ 排期信息 + 操作条 `[L2]`

**AC6 · 独立页退役**
**Then** `GET /admin/content-schedules` 页面路由与 `content-schedules.html` 整页部分删除；`scheduleSection` fragment 迁为 `fragments/schedules-tab.html`；两个 POST 非 htmx 时重定向到 `/admin/seed-batches?tab=schedules`（带筛选参数回传）`[L1]`
**And** 导航无独立项（现状已无）；不做旧地址跳转（D-23）`[L1]`
**And** `SeedScheduleIntegrationTest` 中 6 处打 `GET /admin/content-schedules` 的用例改为 `GET /admin/seed-batches?tab=schedules`（或 htmx `GET /admin/seed-batches/schedules` fragment）`[L1]`

**AC7 · 三语与回归**
**Then** `admin.v130.schedules.*` 三包；`SeedScheduleIntegrationTest`、`SeedBatchListVisibilityIntegrationTest` 全绿；四条 MockMvc `[L0/L1]`

---

## Tasks / Subtasks
- [ ] **T1** `AdminSeedBatchWorkspaceController.list` 增加 `tab` 参数；新增 `GET /admin/seed-batches/schedules`（fragment，权限同现状 `virtual_account.manage`）承接原 `AdminContentScheduleController.list` 的查询逻辑（含 `authorId` 筛选）
- [ ] **T2** `AdminContentScheduleController`：删 `GET /admin/content-schedules`；两个 POST 保留，增加 `HxRequest` 分支返回行 fragment；非 htmx 重定向改 `/admin/seed-batches?tab=schedules`
- [ ] **T3** 模板：`seed-batches.html` 套页签条 + 模板 B（排期页签）；`content-schedules.html` 拆为 `fragments/schedules-tab.html`（表 + 行 fragment `schedule-row`）与 `fragments/drawer-schedule.html`；行内编辑态 fragment `schedule-row-edit`
- [ ] **T4** 摘要条聚合（待发布 / 今日已发 / 失败）
- [ ] **T5** 三语 + 测试改造（6 处 GET）；**T6** 云端 L0，L2 留本地

## Dev Notes

### 现状与改动边界
| 文件 | 现状 | 本 story | 保留 |
|---|---|---|---|
| `admin/seed/web/AdminContentScheduleController.java` | `GET /admin/content-schedules`（AUTH=`virtual_account.manage`，带 `authorId` 筛选）、`POST {rowId}/time`、`POST {rowId}/cancel`（成功后 redirect 回 `/admin/content-schedules`） | 删 GET；POST 重定向目标改；加 htmx 分支 | 两个 POST 路径参数 |
| `admin/seed/web/AdminSeedBatchWorkspaceController.java` | `GET /admin/seed-batches`（批次列表）等 | + `tab` 参数 + `GET /schedules` fragment | 其余全部 |
| `templates/admin/seed-batches.html` | 批次表 + 页尾 `th:replace` 排期区块 | 页签化 | 批次表内容 |
| `templates/admin/content-schedules.html` | 整页 + `scheduleSection` fragment | 拆 fragment，整页删除 | 表列与操作 |
| `templates/admin/layout.html` 28-40 注释 | 说明「排期已并入批量内容，独立页保留可访问是因为 POST 落点与 6 条测试」 | 本 story 正是处理这两个原因；改注释 | — |

### 🔴 为什么现状保留了独立页——本 story 的两件必做
`layout.html` 注释写明：独立页留着是因为①两个 POST 处理完要重定向回它②它带按发布账号筛选而批量页那段没有③6 条集成测试。本 story 三件都要处理：重定向改批量页页签、页签内保留 `authorId` 筛选、6 条测试改打新地址。缺一样就不能删页。

### 端点零变更清单
`POST /admin/content-schedules/{rowId}/time` · `POST /admin/content-schedules/{rowId}/cancel`。删除 GET：`/admin/content-schedules`。新增 GET：`/admin/seed-batches/schedules`（fragment）、`/admin/content-schedules/{rowId}/drawer`。

### 与 7.6 的关系
7.6 改批次工作台四步流与批次列表视觉；本 story 只动 `seed-batches.html` 的页签条与第二页签。先做本 story，7.6 在其上套模板 E。

### References
- [Source: 后台重构逐页规格.md#B5 · 排期发布]
- [Source: ui-v1.3.0-admin.html 泳道 2 帧 2-23～2-24]
- [Source: PRD-v1.3.0-admin.md#5 ③ 第 3 条]
- [Source: petgo-backend …/templates/admin/layout.html 28-40 注释；…/admin/seed/web/AdminContentScheduleController.java；test …/seed/SeedScheduleIntegrationTest.java]
- [Source: epics-v1.3.0.md#Story 7.5]

## Dev Agent Record
### Agent Model Used
### Debug Log References
### Completion Notes List
### File List
