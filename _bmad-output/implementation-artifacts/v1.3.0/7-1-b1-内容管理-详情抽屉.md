---
baseline_commit: 4301227a
branch: feat/1.3.0-ops-ui-refactor
epic: 7
story: 7.1
ad: [AD-9, AD-10, AD-11]
decisions: [D-8, D-12, D-23]
page: B1
depends_on: [2-3a, 2-3b]
---

# Story 7.1: B1 内容管理 + 详情抽屉

Status: ready-for-dev

> 自包含 story。与用户沟通用中文。纪律见根 `CLAUDE.md`（后端→前端→联调、AC 标 L0/L1/L2、`mvn -B clean package`）。
> 依赖 Story 2.3a（`HxRequest` / `AdminBusinessExceptionAdvice` / `AdminExportWriter` / `AdminTime`）与 2.3b（`tpl-b-list` 壳、`admin-drawer.js`）。**端点与参数零变更**，只重排渲染层；唯一后端改动是新增摘要条聚合查询与抽屉 fragment 端点（GET），以及导出改走 `AdminExportWriter`。

## Story

As a 运营，
I want 内容管理列表点行就在右侧抽屉看全文、数据、物种归属、限流并直接处置，
so that 不再跳独立详情页。

## Acceptance Criteria

**AC1 · 模板 B 壳与筛选栏**
**Given** `/admin/content` 套 `tpl-b-list`
**Then** 筛选栏保留现状全部条件：类型（Diary/Moment/Tips）· 作者 ID · 状态 · 日期口径（发布日 / 点赞日）· 起止日期 · 关键词 · 物种 · 物种来源；下拉即选即查（data-autosubmit），**文本搜索配显式「查询」钮** `[L2]`
**And** 筛选参数名与现状 `type / authorId / status / dateBasis / from / to / q / species / speciesSource` 逐字不变 `[L1]`

**AC2 · 摘要条**
**Then** 筛选栏下方摘要条 5 个数字：总帖数 · 今日新增 · 审核中 · 限流中 · 已下架，**随当前筛选联动**，单条 `COUNT(*) FILTER (WHERE …)` 聚合查询实现 `[L1]`
**And** 「今日」按 WIB 自然日 `[L1]`

**AC3 · 表格**
**Then** 列 = ID ｜ 类型 ｜ 作者 ｜ 正文预览 ｜ 图片（首图缩略）｜ 点赞数（口径随 dateBasis）｜ 浏览次数 ｜ 浏览人数 ｜ 评论数（可排序）｜ 创建时间 ｜ 状态 ｜ 限流标记（`content.throttle_view` 可见）｜ 物种 ｜ 推导来源；默认创建时间倒序，每页 20，行高 ~48px，状态用色点 + 文字徽标 `[L2]`
**And** 现状列一个不少（含 bug 20260828 加的点赞/浏览列与「取代内容统计页」的口径注释语义）`[L1]`

**AC4 · 详情抽屉吸收 `content-detail`**
**When** 点行 → `GET /admin/content/{postId}/drawer`（`HX-Request` 返 fragment）
**Then** 抽屉 480px 内容按序：① 内容全文 + 全部图片（点开大图）② 数据卡：作者（注销置灰 + 原 id）/ 发布时间 / 点赞数 / 评论数 / **浏览次数 / 浏览人数**（现状 `content-detail` 字段全保留）③ 物种归属卡：当前物种 + 推导来源 + 覆写下拉（含「清除覆写」；真实用户内容只读）④ 限流卡：当前限流态（粒度 / 到期 WIB / 永久）+ 限流面板（粒度：仅本条 / 该账号全部 × 期限 7 天 / 30 天 / 永久，chips 常驻未选全禁用）/ 解除 ⑤ 评论区（一级 + 展开二级，分页，状态徽标）⑥ 底部固定操作条 `[L2]`
**And** 现状 `content-detail.html` 与 `GET /admin/content/{postId}` 整页路由**删除**，不做跳转（D-23）；`AdminContentDetailIntegrationTest` 改为断言 drawer fragment `[L1]`

**AC5 · 操作（端点与参数零变更）**
| 按钮 | 端点 | 权限 | 确认 | 成功后 |
|---|---|---|---|---|
| 下架（需 reason） | `POST /admin/content/{id}/takedown` | `content.proactive_takedown` | data-confirm 复述对象与后果 | 200 + 行 oob 刷新 + 抽屉刷新 |
| 恢复 | `POST /admin/content/{id}/restore` | `content.restore` | 无 | 同上 |
| 物种覆写 / 清除 | `POST /admin/content/species`（支持批量，参数 `postIds` / `species`） | `content.proactive_takedown` | 无 | 抽屉物种卡刷新 |
| 限流 | `POST /admin/throttles`（`postId` / `scope` / `duration`） | `content.throttle_manage` | 无（参数常驻） | 抽屉限流卡 + 行刷新 |
| 解除限流 | `POST /admin/throttles/lift` | `content.throttle_manage` | 无 | 同上 |
**Then** 全部走 htmx（AD-9）：成功 200 + fragment + `hx-swap-oob` 行；失败 422 操作区行内 err；权限不足按钮禁用 + 注明所缺权限名 `[L1]`

**AC6 · 边界**
**Then** 审核中（UNDER_REVIEW）帖抽屉注明「不可下架，走 A1 送审队列」，下架钮禁用 `[L2]`
**And** 作者注销：列表与抽屉作者列置灰 + `#id` `[L2]`
**And** `?open=<postId>` 打开页面自动开该抽屉（Story 8.1 用户详情「发布内容」页签跳转靠它）`[L2]`

**AC7 · 导出走 `AdminExportWriter`**
**Then** `GET /admin/content/export.csv` 路径、参数、权限 `content.list_export` 不变；实现改用 `AdminExportWriter`（RFC 4180 转义、前导 `=` 防注入、表头随 locale、保留 UTF-8 BOM），列与现状一致 `[L1]`

**AC8 · 三语与回归**
**Then** 新增 key 走 `admin.v130.content.*`，三包同批；`AdminContentManageIntegrationTest` / `ContentSpeciesIntegrationTest` / throttle 相关测试全绿；新增 MockMvc 四条（整页 200 / drawer fragment / 422 / 403）`[L0/L1]`

---

## Tasks / Subtasks

- [ ] **T1 · 摘要条查询**（AC2）
  - [ ] `AdminContentManageService` 新增 `summary(filter)`：一条原生 SQL 五个 `COUNT(*) FILTER`（总数 / `created_at` 落 WIB 今日 / `status='UNDER_REVIEW'` / 存在生效限流 / `deleted_at IS NOT NULL OR status IN (下架态)`），复用现有筛选 where 片段
  - [ ] 摘要条 fragment `fragments/content-summary.html`，随筛选 `hx-swap-oob` 一起返回
- [ ] **T2 · 列表套 `tpl-b-list`**（AC1 / AC3）
  - [ ] `content.html` 改为填 `filters / summary / table / drawer` 四槽位；`#content-rows` 目标 id 沿用现有 htmx 局部刷新
  - [ ] 「查看详情」链接改为 `hx-get=/admin/content/{id}/drawer hx-target=#content-drawer`
  - [ ] 行内的下架 / 限流 / 物种表单**从行内移除**，收进抽屉操作条（列表只留状态与限流标记列）
- [ ] **T3 · 抽屉 fragment**（AC4 / AC5 / AC6）
  - [ ] `AdminContentManageController` 新增 `GET /admin/content/{postId}/drawer`（`DETAIL_AUTH` 同现状详情页权限 `content.view`），复用 `contentManage.detail(...)` 现有 DTO（含评论分页）
  - [ ] `fragments/drawer-content.html`：六区块；限流面板 chips 复用 `content.html` 现有 `duration` / scope 选项与 message key
  - [ ] 处置端点响应：`HX-Request` 下返回 `drawer-content` fragment + oob `<tr id="post-{id}">`；非 htmx 维持 PRG
  - [ ] 删除 `content-detail.html` 与 `GET /admin/content/{postId}` 方法；`DETAIL_AUTH` 常量迁到 drawer 方法
- [ ] **T4 · 导出改写**（AC7）
  - [ ] `AdminContentManageService.exportCsv` 内部改为构建行数据交 `AdminExportWriter.csv(headers, rows, locale)`；Controller 保留 BOM 与 `Content-Disposition`
  - [ ] 表头 key 走现有 `admin.content.col.*`
- [ ] **T5 · 三语 + 测试**（AC8）
  - [ ] `admin.v130.content.{summary.total|summary.today|summary.underReview|summary.throttled|summary.takenDown|drawer.title|drawer.underReviewNoTakedown}` 三包
  - [ ] `AdminContentDetailIntegrationTest` 改为 `GET /admin/content/{id}/drawer` + `HX-Request` 断言区块；新增 `GET /admin/content/{id}` 返 404
  - [ ] 摘要条数字与筛选联动测试（L1）
- [ ] **T6 · 云端执行须知**
  - [ ] 云端 `mvn -B clean package` L0；抽屉视觉与 `?open=` 深链 L2 留本地 stag；Completion Notes 标注

## Dev Notes

### 现状与改动边界

| 文件 | 现状 | 本 story | 保留 |
|---|---|---|---|
| `admin/moderation/web/AdminContentManageController.java` | `GET /admin/content`（`VIEW_AUTH`）、`GET /admin/content/{postId}`（`DETAIL_AUTH`）、`GET export.csv`（`content.list_export`）、`POST species`（`BROWSE_AUTH`，批量）、`POST {id}/takedown`、`POST {id}/restore`（`content.restore`） | + `GET {postId}/drawer`；删整页详情；处置端点加 `HxRequest` 分支 | 5 个写/导出端点路径与参数 |
| `admin/moderation/service/AdminContentManageService.java` | `exportCsv` 自拼 CSV；`likeCounts`；`setSpeciesOverride` 批量；`takedown(reason)` / `restore` | + `summary`；`exportCsv` 改走 `AdminExportWriter` | 服务层权威校验（真实用户内容物种只读） |
| `admin/throttle/web/AdminThrottleController.java` | `POST /admin/throttles`、`POST /admin/throttles/lift`（`MANAGE` = `content.throttle_manage`） | 调用点从列表行内移到抽屉；响应加 htmx 分支 | 端点与参数 |
| `templates/admin/content.html` | 筛选栏 + 表格（行内三个表单：下架 / 限流 / 物种覆写）+ 导出链接 + 限流说明横幅 | 套模板 B；行内表单移入抽屉 | 全部筛选参数、`#content-rows` 局部刷新、限流两条说明文案（`admin.throttle.notTakedown` / `allTabOnly`）挪到抽屉限流卡 |
| `templates/admin/content-detail.html` | 独立页：头部状态 / 数据表 / 正文图片 / 评论分页 | **删除**，内容进 `fragments/drawer-content.html` | 字段一个不少 |

### 🔴 「内容统计」页已撤，积分与导出就在这一页

`content.html` 第 47/89/138 行注释明写：2026-08-28 产品撤掉「内容互动积分」整页，点赞列（口径随 `dateBasis`）与导出并入本页（D-12）。**不要**再造一个统计页；看板 PRD 里「导出走 AB-3G」已改为「导出走内容管理列表」。

### 🔴 限流入口的位置

现状限流表单在列表行内（`content.html` 279-292）。逐页规格 B1 要求「限流选择常驻抽屉限流卡内」（UI 稿 2-3 注记 2026-09-01）。移动时保留：`scope`（仅本条 / 该账号全部）× `duration`（7d / 30d / 永久）两组选项、`admin.throttle.*` 全部 key、`content.throttle_view` 与 `content.throttle_manage` 两档门控（列表标记列只需 view；面板需 manage）。

### 🔴 删整页路由的连带

- `AdminContentDetailIntegrationTest` 直接打 `GET /admin/content/{id}`，须改。
- `layout.html` / 其他模板若有 `@{/admin/content/{id}}` 链接（grep `admin/content/` + `th:href`），一律改 `hx-get drawer` 或 `?open=`。Story 8.1 用户详情「发布内容」页签跳本页用 `?open=<postId>`。
- 不做 301/302 兜底（D-23）。

### 端点零变更清单（写入 Story 2.1 底账后 Epic 11 diff 的基线）

`POST /admin/content/species` · `POST /admin/content/{postId}/takedown` · `POST /admin/content/{postId}/restore` · `POST /admin/throttles` · `POST /admin/throttles/lift` · `GET /admin/content/export.csv`。新增 GET：`/admin/content/{postId}/drawer`。删除 GET：`/admin/content/{postId}`。

### 测试标准

- 沿用 `AdminContentManageIntegrationTest`（真库）范式；抽屉 fragment 用 MockMvc + `HX-Request: true` 头断言 `#content-drawer` 根元素与六区块 id。
- 导出：断言首字符 BOM、字段含逗号/引号/换行时正确转义、`=` 开头字段被前缀保护。
- L1 跑法：flush Redis DB0 → 重建 scratch 库 → `DB_NAME` env。

### 三语与 CI
新增 key 前缀 `admin.v130.content.*`；`scripts/ci/check-i18n-keys.sh`（Story 2.2）必绿。ID 文案按 UX-DR12（表头 nowrap + 省略号）。

### Project Structure Notes
- 改动落 `admin/moderation/**`、`admin/throttle/**`（调用点）、`templates/admin/content.html`、`fragments/drawer-content.html`、`fragments/content-summary.html`。不新建模块。
- 遵守 AD-9：`HX-Request` → fragment；422/403 fragment 由 `admin-core.js` beforeSwap 放行（2.3a 已建）。

### References
- [Source: 后台重构逐页规格.md#B1 · 内容管理 / 模板 B 通用规格]
- [Source: ui-v1.3.0-admin.html 泳道 2 帧 2-1～2-5]
- [Source: architecture-v1.3.0-admin-delta.md#AD-9 / AD-10 / AD-11 / §Implementation Patterns]
- [Source: PRD-v1.3.0-admin.md#5 AB-19A ②③ · §1 ③（导出走内容管理列表，D-12）]
- [Source: 决策日志-admin.md D-8 / D-12 / D-23]
- [Source: epics-v1.3.0-admin.md#Story 7.1]
- [Source: petgo-backend …/admin/moderation/web/AdminContentManageController.java；…/templates/admin/content.html；content-detail.html]

## Dev Agent Record
### Agent Model Used
（dev-story 填写）
### Debug Log References
### Completion Notes List
### File List
