---
baseline_commit: 38f8819df5bd4481edfd4ef7327e9df7b22698df
---

# Story 3.7: 内容举报与运营人工审核队列

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a **用户与运营**,
I want **举报不当内容并由运营人工处理**,
so that **平台内容保持健康可信**。

> 本 Story 实现内容举报（FR-25，**含 Admin 审核队列**）——用户侧「···」举报单选类型提交，运营侧 Admin 后台审核队列人工处理（**无自动下架**）。**后端中（含 Admin）、前端轻**：后端建举报工单表 + 举报提交端点（moderation 模块）+ Admin 队列页（复用 Story 3.1 的 Admin shell）+ 人工下架（复用 Story 3.6 的 `softDelete`）；前端「···」举报菜单 + 类型单选 + 提交反馈。
>
> **做**：① 用户侧「···」菜单举报（类型单选：违法违规/虚假信息/不当内容/骚扰/其他）；② 提交反馈「已收到你的举报…」+ 写举报工单表；③ 进运营审核队列**无自动下架**；④ Admin 后台举报队列页（复用 3.1 shell 预留入口位）查看详情 + 人工下架（复用 content `softDelete`，所有决定人工做出）。
> **不做**：评论举报 / 用户举报（V1 明确不做，仅内容举报）；自动下架/机器审核（V1 人工）；Admin shell 框架（Story 3.1 已建，本 Story 填充举报队列入口）。
> **依赖前序**：Story 3.1（Admin slice shell + ADMIN 门控 + 举报队列入口位）、Story 3.6（`ContentService.softDelete` 复用做人工下架）、Story 3.3（详情页「···」菜单容器 + 举报入口位）、Story 3.2（Feed 卡片长按 context menu——举报/分享）、Story 1.4/1.5（FR-0C，举报需登录）。

## Acceptance Criteria

> **验证层标注**：**L0**（编译/lint/widget test/Service 单元 + MockMvc） · **L1**（需 postgres+redis，验举报写入 + 队列读取 + 人工下架 + ADMIN 门控） · **L2**（真机，举报菜单交互）。
> 举报提交/队列/下架/门控为 **L1**（DB + Admin slice 是核心）；「···」举报菜单/类型单选为前端 **L0 + L2**。无外部第三方依赖。

### AC1 — 用户举报单选类型 + 反馈 + 进队列无自动下架

**Given** 用户在内容卡片/详情页「···」菜单
**When** 发起举报并选择类型（违法违规/虚假信息/不当内容/骚扰/其他，单选）
**Then** 提交后显示「已收到你的举报，我们会尽快处理」，举报进入运营审核队列（写举报工单表，本故事创建该表）（FR-25）
**And** V1 不做自动下架、不做评论/用户举报
> 验证层：**L1**（举报工单表创建 + 提交写入含 type/reporterId/postId/status=PENDING；**提交后内容仍正常展示——无自动下架**；防重复举报策略：同用户对同内容重复举报幂等/合并）。「···」举报菜单 + 5 类型单选 + 提交反馈文案为前端 **L0 widget test** + **L2**；举报需登录→未登录触发 FR-0C。

### AC2 — Admin 审核队列人工下架

**Given** 运营在 Admin 后台举报队列
**When** 处理举报工单
**Then** 可查看举报详情并人工下架内容（复用 content service 软删），所有处理决定由人工做出（G-1 落点）
> 验证层：**L1**（ADMIN 门控：仅 ADMIN 可访问举报队列；队列按时间/状态列举工单；点工单看详情含被举报内容 + 举报类型/数；「下架」调 `ContentService.softDelete(postId, reason=admin_takedown)` → 内容走 404（Story 3.3）+ 从 Feed/时间线移除（Story 3.6 一致）；工单状态流转 PENDING→RESOLVED/DISMISSED）。**所有决定人工**——无规则自动下架。

### AC3 — 运营判定后处理（违规下架 + 不回告举报人）+ 对齐发布时三方审核 `[R2·回改]`

> **代码已实现，本轮回改补齐**（review→ready-for-dev）。落实决策 **F10**（内容审核反转 F1）：关键词/图像自动过滤已**前移到发布时由三方系统执行**（FR-12，Story 2.3），本举报模块**仅处理已发布内容举报**；并明确运营判定后的三处同步下架与作者通知。

**① 举报人侧不回告审核结果**
**Given** 用户提交举报（AC1）
**When** 运营后续处理（无论下架或驳回）
**Then** 举报人侧**仅**收到提交时的「已收到你的举报，我们会尽快处理」，**审核结果不回告举报人**（不通知下架/驳回，V1 无举报进度查询）

**② 运营判定违规 → 内容下架（三处同步移除 + 通知作者）**
**Given** 运营在 Admin 队列判定某工单内容违规
**When** 执行下架（`ContentService.softDelete(postId, ADMIN_TAKEDOWN)`）
**Then** 内容从 **Feed**、**「我的发布」**（Story 7.1/2.3 列表）、**成长档案时间线**（Story 2.4）**同步移除**（复用 Story 3.6 软删全套同步，详情走 404）
**And** 内容作者收到系统通知「你发布的内容因违反社区规范已被移除」（经领域事件 → notify 消费，Epic 6；**不说明举报人**、V1 **无申诉入口**）；工单置 RESOLVED 记 `handled_by`/`handled_at`

**③ 运营判定无问题 → 静默保留**
**Given** 运营判定内容无问题
**When** 驳回工单（DISMISSED）
**Then** 内容**静默保留**、**无任何通知**（举报人与作者均不通知）

**④ 模块描述对齐（F10）**
**Then** 本举报模块文档/范围对齐：**关键词过滤 + 三方图像识别已前移到发布时由三方系统执行（FR-12，决策 F10 反转原 F1「人工审核、不做关键词/机器过滤」）**；本模块仅处理**已发布内容**的用户举报与运营人工下架，**不含**发布时自动过滤逻辑（在 Story 2.3）。

> 验证层：**L1**（① 下架后 Feed/「我的发布」/成长档案时间线三处均不再返回该内容 + 详情 404；② 下架时发布「内容被移除」领域事件 → 通知作者（admin action），驳回不发任何通知、举报人侧零结果通知；工单状态流转 + handled_by/handled_at）。举报人提交反馈文案、作者通知文案为 **L0**（双语 .arb、≤1 emoji、不含举报人信息）。任务：后端 admin 下架 action + 作者通知事件；前后端文档描述对齐 F10。

---

## Tasks / Subtasks

> **三段组织**，后端中（含 Admin）前端轻。建议执行顺序：后端举报表+提交端点 → Admin 队列页 → 前端举报菜单 → 联调。

### 🟦 后端子任务（petgo-backend / Spring Boot — moderation 模块 + admin slice）

- [x] **B1. 举报工单表** (AC: 1)
  - [x] Flyway 建 `content_reports`（moderation 模块）：`id`(bigint)、`post_id`(fk content_posts)、`reporter_id`(fk users)、`reason_type`(varchar UPPER_SNAKE ∈ {ILLEGAL, MISINFO, INAPPROPRIATE, HARASSMENT, OTHER})、`status`(varchar UPPER_SNAKE ∈ {PENDING, RESOLVED, DISMISSED})、`created_at`/`updated_at`(timestamptz UTC)、`handled_by`(可空, ADMIN id)、`handled_at`(可空)；索引 `idx_content_reports_status`、`idx_content_reports_post`。
  - [x] 防重复：同 reporter 对同 post 重复举报→幂等（唯一约束 `(post_id,reporter_id)`）。
- [x] **B2. 举报提交端点** (AC: 1)
  - [x] `POST /api/v1/content-posts/{postId}/reports`：body 含 `reasonType`（单选，必填，枚举校验）；**需 JWT**（未登录→401→前端 FR-0C）；带 `Idempotency-Key`。
  - [x] 写工单 status=PENDING；**不触发任何自动下架**（AC1）；返回 200 + 反馈语义（前端显「已收到…」）。
  - [x] **V1 仅内容举报**——无评论/用户举报端点。
- [x] **B3. Admin 举报队列（复用 Story 3.1 shell）** (AC: 2)
  - [x] `admin` slice 填充举报队列：`AdminWebController` 增 `/admin/reports`（Thymeleaf 页，复用 3.1 的 layout shell 与预留入口位）；**ADMIN 门控**（3.1 已建 `/admin/**` 规则，本 Story 复用）。
  - [x] 队列列表：按 status=PENDING 倒序 + 简单分页；点工单看详情（被举报内容快照/链接 + reason_type + 举报次数）。
  - [x] `AdminModerationService` 复用 moderation repository 读工单 + 调 `ContentService.softDelete(postId, "admin_takedown")` 下架（**经 content service，禁直接改 content 表**）；工单状态置 RESOLVED（下架）/ DISMISSED（驳回），记 `handled_by`/`handled_at`。
  - [x] **所有决定人工**——无任何自动下架规则；下架后内容走 Story 3.6 软删的全套同步（Feed/时间线移除、详情 404）。
- [x] **B4. 运营下架 → 通知作者领域事件 (AC3 · R2)** (AC: 3)
  - [x] 新增 `ContentRemovedEvent(postId, authorId, removedAt)`；`ContentService.doSoftDelete` 仅在 `ADMIN_TAKEDOWN` 时 publish（作者自删 `AUTHOR_DELETE` 不发）。
  - [x] `NotificationType.CONTENT_REMOVED` + `ContentNotifyListener.onContentRemoved` → 通知作者「内容已被移除/你发布的内容因违反社区规范已被移除」（**不说明举报人**、V1 **无申诉入口**）。
  - [x] 驳回（DISMISSED）不发事件 → 作者/举报人均零通知；举报人侧仅提交反馈、**审核结果不回告**。
  - [x] F10 范围对齐：`ReportService` javadoc 注明关键词/图像自动过滤已前移到发布时（Story 2.3），本模块仅处理已发布内容举报、无自动下架。

### 🟩 前端子任务（petgo_app / Flutter — features/content；Admin 为 Thymeleaf 无 Flutter）

- [x] **F1. 用户侧「···」举报菜单** (AC: 1)
  - [x] 详情页「···」菜单（Story 3.3 容器）+ Feed 卡片长按 context menu（Story 3.2/UX-DR12）增「举报」项。
  - [x] 举报 sheet：5 类型**单选**（违法违规/虚假信息/不当内容/骚扰/其他，双语 .arb）→ 提交调 B2；未登录触发 FR-0C（复用 1.4）。
  - [x] 提交成功 toast「已收到你的举报，我们会尽快处理」（双语，每条 ≤1 emoji）。
- [x] **F2. Admin 队列页（Thymeleaf）** (AC: 2)
  - [x] `templates/admin/reports.html`（队列列表 + 工单详情 + 下架/驳回按钮）——服务端渲染，**不在 Flutter App 内**。

### 🟨 联调验收子任务（举报写入 + 队列 + 人工下架 + 门控）

- [x] **J1. 举报提交无自动下架** (AC: 1 / **L1**)
  - [x] 用户举报某帖（选类型）→ 工单 PENDING 落库；**该帖仍正常在 Feed/详情展示**（无自动下架）；同用户重复举报→幂等；未登录举报→401→FR-0C。（L0 MockMvc 绿；L1 真跑待本地）
- [x] **J2. Admin 队列 + 人工下架** (AC: 2 / **L1**)
  - [x] ADMIN 登录后台 `/admin/reports` 见 PENDING 工单；非 ADMIN→403（复用 3.1 门控）；点工单看详情；「下架」→内容软删→Feed/时间线移除、详情 404；工单→RESOLVED 记 handled_by；「驳回」→DISMISSED。（L0 绿；L1 真跑待本地）
- [x] **J3. 前端举报菜单** (AC: 1 / L0+**L2**)
  - [x] widget test：5 类型单选、提交反馈文案。真机：详情/Feed 长按举报、未登录 FR-0C。（L0 widget test 绿；L2 真机待本地）

---

## Dev Notes

### 关键架构约定（本 Story 必须落实）

**举报模型**（FR-25）：卡片「···」菜单举报**单选类型**；进运营人工审核队列**无自动下架**；**V1 不做评论/用户举报**。这是「不可协商地基」之一（架构 §Non-Negotiables 第 5 条：内容举报入口 + 人工下架能力）。

**Admin slice 复用**（架构 §Gap G-1 / Story 3.1）：举报队列**挂 Story 3.1 首建的 `com.tailtopia.admin/` shell**，复用 `/admin/**` ADMIN 门控与 layout——**不另造后台**。本 Story 是 3.1 预留「举报队列」入口位的填充。下架经 `ContentService.softDelete`（Story 3.6 封装），所有决定人工。

**模块归属**：举报工单表/提交端点在 `com.tailtopia.moderation/`；Admin 队列页在 `com.tailtopia.admin/`（经 moderation service 读工单 + content service 下架）。跨模块只经 service，**禁 admin/moderation 直接访问 content repository**。

**命名映射链**：`content_reports`(post_id/reporter_id/reason_type/status/handled_by/handled_at/created_at)；枚举 UPPER_SNAKE（reason_type {ILLEGAL,MISINFO,INAPPROPRIATE,HARASSMENT,OTHER}、status {PENDING,RESOLVED,DISMISSED}）；索引 `idx_content_reports_status`；DB snake_case ↔ JSON camelCase。

**人工下架联动**：下架 = `softDelete(postId, "admin_takedown")` → 走 Story 3.6 全套软删同步（Feed 不返回、时间线移除、详情 404 统一防枚举文案 UX-DR18 ④）。

### 强制护栏（架构 §Enforcement —— 违反即返工）

- **禁止擅自引入 MQ / 通用缓存层 / 新中间件**；举报同步写库。
- `ddl-auto=validate`；`content_reports` 表/索引走 Flyway。
- **无自动下架**——无任何规则自动改内容状态，全部人工经 Admin。
- 模块边界：moderation 提交 + admin 处理，下架经 content service `softDelete`，**禁跨 repository**。
- ADMIN 端点复用 3.1 `/admin/**` 门控，绝不混入公开 `/api/v1`；非 ADMIN→403。
- 举报需登录（FR-0C）；写端点 Idempotency-Key + 防重复举报。
- V1 仅内容举报——**不加评论/用户举报端点**。

### 范围边界（防 scope creep —— 本 Story 明确不做）

- ❌ 评论举报 / 用户举报（V1 明确不做）。
- ❌ 自动下架 / 机器审核（V1 人工）。
- ❌ Admin shell 框架/登录/门控（Story 3.1 已建，本 Story 复用填充举报队列）。
- ❌ 内容软删机制本身（Story 3.6 已封装 `softDelete`，本 Story 调用）。
- ✅ 只做：举报工单表 + 用户举报提交（单选类型，无自动下架）+ Admin 举报队列页（复用 shell）+ 人工下架（复用 softDelete）。

### Project Structure Notes

- 后端：`com.tailtopia.moderation/{web,service,domain,repository,dto}`（举报提交 + 工单）；`com.tailtopia.admin/web`（`/admin/reports` 队列页，复用 3.1 shell）；下架经 `com.tailtopia.content.ContentService.softDelete`（Story 3.6）。
- 前端：`lib/features/content/{data,domain,presentation}`——「···」举报菜单 + 类型单选 sheet（详情 + Feed 长按）；Admin 队列为 `templates/admin/reports.html`（Thymeleaf，无 Flutter）。

### References

- [Source: architecture.md#不可协商地基 / 第 5 条] — 内容举报入口 + 人工下架能力。
- [Source: architecture.md#Gap Analysis Results / G-1] — admin slice 承载举报工单处理/人工下架，复用模块 service。
- [Source: architecture.md#Requirements Overview / moderation] — 举报工单进人工队列，V1 无自动下架。
- [Source: architecture.md#Architectural Boundaries] — 跨模块经 service；admin/moderation 不直接访问 content repository。
- [Source: epics.md#Story 3.7] — 两组原始 AC（用户举报单选无自动下架 / Admin 人工下架）。
- [Source: epics.md#FR-25] — 内容举报全文（V1 不做评论/用户举报）。
- [Source: epics.md#Story 3.1] — Admin shell 首建，本 Story 复用举报队列入口位。
- [Source: epics.md#Story 3.6 / FR-36] — softDelete 复用做人工下架，软删同步移除。

## Dev Agent Record

### Agent Model Used

Claude（云端 headless dev agent）

### Debug Log References

- 后端：`./mvnw -B test -Dtest=ReportServiceTest,AdminWebControllerTest,AdminContentServiceTest` → 绿；`./mvnw -B -DskipTests package` → 绿。
- 前端：`flutter analyze` → No issues；`flutter test` → 111 用例全绿（含本 Story 2 个举报用例）。

### Completion Notes List

**后端（L0 绿）：**
- ✅ AC1 — V11 `content_reports` 表（moderation 模块）：`reason_type` {ILLEGAL,MISINFO,INAPPROPRIATE,HARASSMENT,OTHER}、`status` {PENDING,RESOLVED,DISMISSED}、`handled_by`/`handled_at`、唯一约束 `(post_id,reporter_id)` 防重。
- ✅ AC1 — `POST /api/v1/content-posts/{postId}/reports`（`ReportController`）需 JWT，单选 `reasonType` 必填；写工单 PENDING、**不触发任何自动下架**；重复举报幂等；内容不存在 404。内容存在性经 `ContentService.isVisible`（**不直读 content 表**）。
- ✅ AC2 — Admin 举报队列复用 Story 3.1 shell：`AdminWebController` 增 `/admin/reports`（列表）+ `/admin/reports/{id}/takedown` + `/dismiss`；`reports.html` Thymeleaf 队列页；layout 导航「举报队列」入口位填充（3.1 预留）。ADMIN 门控复用 3.1 的 `/admin/**`。
- ✅ AC2 — `AdminModerationService` 经 `ReportService`（读工单）+ `ContentService.softDelete(postId, ADMIN_TAKEDOWN)`（人工下架，复用 Story 3.6）编排；**所有决定人工**，无自动下架；下架走 3.6 全套同步（Feed/时间线移除、详情 404）；工单 RESOLVED/DISMISSED 记 handled_by/handled_at。
- ✅ 护栏：无 MQ/缓存层；`ddl-auto=validate`；跨模块只经 service（moderation/admin 不直读 content repo）；ADMIN 端点不混入公开 `/api/v1`；V1 仅内容举报（无评论/用户举报）。

**前端（L0 绿）：**
- ✅ AC1/F1 — `openReport(context,ref,postId)` 复用入口：详情「···」「举报」项 + Feed 卡片**长按** context menu 均触发；未登录先触发 FR-0C（不弹 sheet）；`ReportReason` 5 类型**单选** `RadioGroup` sheet → 提交调 `submitReport`；成功 SnackBar「已收到你的举报…🐾」（双语，≤1 emoji）。
- ✅ F2（Admin 队列）为 Thymeleaf，无 Flutter。

**R2 回改 — AC3 运营判定后处理 + F10 对齐（L0 绿）：**
- ✅ AC3 ② — 新增领域事件 `ContentRemovedEvent(postId, authorId, removedAt)`；`ContentService.doSoftDelete` **仅 `ADMIN_TAKEDOWN`** publish（注入 `ApplicationEventPublisher`），作者自删 `AUTHOR_DELETE` **不发**（不自通知）。下架同步移除（Feed/「我的发布」/成长档案时间线/详情 404）复用 Story 3.6 软删全套，无新增同步逻辑。
- ✅ AC3 ② — `NotificationType.CONTENT_REMOVED` + `ContentNotifyListener.onContentRemoved`（`@TransactionalEventListener`，跨模块经事件，content 不直调 notify）→ 通知作者「内容已被移除 / 你发布的内容因违反社区规范已被移除」；**不含举报人信息**、V1 **无申诉入口**；内容已 404 故深链仅承载 postId 作内部标识。
- ✅ AC3 ① / ③ — 举报人侧仅提交反馈、**审核结果不回告**（`ReportService` 不通知举报人）；驳回（DISMISSED）`AdminModerationService.dismiss` 仅置状态、**不发任何事件**→ 作者/举报人均零通知（静默保留）。
- ✅ AC3 ④ — F10 范围对齐：`ReportService` javadoc 注明关键词过滤 + 三方图像识别已前移到发布时（FR-12，Story 2.3 `ContentService.publish`），本举报模块仅处理已发布内容举报、无任何规则自动下架。
- ✅ 测试：`ContentServiceTest` +2（`adminTakedownPublishesContentRemovedEvent` 验事件含 authorId=作者 / `authorDeleteDoesNotPublishRemovedEvent` 验自删不发）；`ContentNotifyListenerTest` +1（`contentRemovedNotifiesAuthor`）。`./mvnw -B -o test` → **512 用例全绿**。
- 🔧 顺带修复既有失败用例 `FeedServiceTest`（与 3.7 无关、baseline 38f8819 即红）：测试用过时状态码 `"A"/"B"/"C"`，生产已对齐 PRD 枚举 `HAS_PET/PLANNING/ENTHUSIAST`（`petStatusOf` 返回 `PetStatus.name()`）；改测试字面量对齐生产契约（`"B"→"PLANNING"` 等），`statusBExcludesGrowthMoment`/`statusAAndGuestDoNotExcludeGrowth` 改名为 `planning…`/`hasPetAndGuest…`。生产代码未改。

**待本地验收：**
- ⏳ **L1（Docker postgres+redis）**：J1 举报提交无自动下架（工单 PENDING、帖仍在 Feed/详情、重复幂等、未登录 401）；J2 Admin 队列 + 人工下架（ADMIN 见 PENDING、非 ADMIN 403、下架→软删同步、工单 RESOLVED；驳回 DISMISSED）；AC3 下架后真发 `CONTENT_REMOVED` 通知落 `notifications` 行 + 作者角标自增、驳回不落通知。
- ⏳ **L2（真机）**：J3 详情/Feed 长按举报、5 类型单选、提交反馈、未登录 FR-0C；作者侧通知中心见「内容已被移除」。

**Flyway 序号：** V11（接 V10 之后单调分配，决策 E2）；AC3 回改**无新增迁移**（事件/通知纯应用层，决策 E2 冻结迁移）。

### File List

**后端新增：** `moderation/domain/{ReportReason,ReportStatus,ContentReport}.java`、`moderation/repository/ContentReportRepository.java`、`moderation/dto/ReportRequest.java`、`moderation/service/ReportService.java`、`moderation/web/ReportController.java`、`admin/service/{AdminModerationService,ReportQueueItem}.java`、`db/migration/V11__init_content_reports.sql`、`templates/admin/reports.html`、测试 `moderation/service/ReportServiceTest.java`；**`[R2·AC3]` `content/event/ContentRemovedEvent.java`**
**后端修改：** `content/service/ContentService.java`（isVisible/findSummary/PostSummary；**`[R2·AC3]` 注入 `ApplicationEventPublisher`、`doSoftDelete` ADMIN_TAKEDOWN publish `ContentRemovedEvent`**）、`admin/web/AdminWebController.java`（/admin/reports + takedown/dismiss）、`templates/admin/layout.html`（举报队列入口激活）、测试 `admin/web/AdminWebControllerTest.java`（构造注入）；**`[R2·AC3]`** `notify/domain/NotificationType.java`（+CONTENT_REMOVED）、`notify/service/ContentNotifyListener.java`（+onContentRemoved）、`moderation/service/ReportService.java`（F10 范围对齐 javadoc）、测试 `content/service/ContentServiceTest.java`（+publisher mock +2 用例）、`notify/service/ContentNotifyListenerTest.java`（+1 用例）、`content/service/FeedServiceTest.java`（修复既有红：状态码对齐 PRD 枚举）
**前端新增：** `features/content/domain/report_reason.dart`、`features/content/presentation/report_sheet.dart`、测试 `test/content/report_test.dart`
**前端修改：** `features/content/data/detail_repository.dart`（submitReport）、`features/content/presentation/content_detail_page.dart`（「···」举报启用）、`features/content/presentation/home_page.dart`（长按举报）、`features/content/presentation/feed_view.dart`（onLongPressItem）、`shared/widgets/masonry_card.dart`（onLongPress）、`l10n/app_en.arb`、`l10n/app_id.arb`、测试 fake 补 submitReport
（R2·AC3 前端**无代码改动**：①举报人不回告已满足、②作者通知走通用 notify 中心、④仅文档；`flutter analyze` 净、`flutter test` 282 绿。）

### Change Log

| 日期 | 变更 | 说明 |
|---|---|---|
| 2026-06-09 | R2 回改 AC3 完成 | 运营下架→`ContentRemovedEvent` 通知作者（仅 ADMIN_TAKEDOWN，无举报人信息/无申诉）；DISMISSED 静默零通知；举报人审核结果不回告；F10 范围对齐文档。顺带修复既有红用例 `FeedServiceTest`（状态码对齐 PRD 枚举，生产未改）。`./mvnw -B -o test` 512 绿 / `flutter test` 282 绿 / analyze 净。L1/L2 待本地。 |
