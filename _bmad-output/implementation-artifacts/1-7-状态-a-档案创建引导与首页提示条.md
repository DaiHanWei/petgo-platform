---
baseline_commit: f47d6e27d0f3ed271be4959e1b279315facb971b
---

# Story 1.7: 状态 A 档案创建引导与首页提示条

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a **选择"我有宠物"的新用户**,
I want **被引导去创建宠物档案，且即使跳过也能被适时提醒**,
so that **我能顺畅建立宠物档案这一核心留存资产**。

> 本 Story 收口 Epic 1 引导流的最后一步。范围：① 状态 A 用户选完状态后进入**档案创建引导页**（入口 + 「跳过，稍后创建」；创建表单本体复用 FR-11，由 Epic 2 实现，本 Story 只负责引导路由与跳过逻辑）；② **首页档案提示条**——仅「状态 A 且未完成档案」用户首页顶部显示（含「立即创建」CTA + 关闭 X）；**前 3 次重启均显示，第 3 次关闭后或档案完成上传后永不再显示**；③ 状态 B/C 用户首页**不触发任何提示条**。
>
> **依赖**：Story 1.6（状态 A 分流 + `pet_status`）、Story 1.2（首页外壳/token/i18n）、Story 1.3（`users` + JWT；判断「档案是否已创建」需后端 `hasPetProfile` 信号）。
> **被依赖**：Epic 2 Story 2.2（档案创建表单本体；创建成功后回填本 Story 引导/提示条的「完成」语义）。
>
> **不做**：宠物档案表单/`pet_profiles` 表（FR-11，Epic 2 Story 2.2）——本 Story 仅引导路由 + 跳过 + 提示条计数逻辑，「立即创建」CTA 跳转到 Epic 2 创建页（本 Story 期可为占位）。

## Acceptance Criteria

> **验证层标注**：每条 AC 末尾标注验证层级与所需本地环境——
> **L0 静态**（编译/lint/widget/单元，含重启计数纯逻辑） · **L1 集成/运行时**（需 Docker+postgres+redis，验 `hasPetProfile` 后端信号） · **L2 端到端/外部凭证**（需真实 Google + 真机/模拟器跨重启走查）。
> 本 Story 提示条 3 次重启计数为可纯单元测试的 **L0**；「档案是否已创建」依赖后端为 **L1**；跨真机重启走查为 **L2**。

### AC1 — 状态 A 档案创建引导（入口 + 跳过）

**Given** 新用户在状态选择中选了 A
**When** 完成选择
**Then** 进入档案创建引导页（入口 + 「跳过，稍后创建」），创建表单本体复用 FR-11（Epic 2 实现，此处负责引导路由与跳过逻辑）（FR-0G）
**And** 完成创建 → 进入首页且不显示提示条；跳过 → 进入首页并进入提示条逻辑
> 验证层：**L0**（前端 widget：选 A 后进引导页 + 「跳过，稍后创建」存在；「立即创建」→ 跳 Epic 2 创建页占位；跳过 → 进首页并激活提示条逻辑；mock「完成创建」→ 进首页且不显示提示条）。文案 .arb 双语。

### AC2 — 首页档案提示条（仅状态 A 未完成档案，3 次重启逻辑）

**Given** 状态 A 且尚未完成档案上传的用户
**When** 进入首页
**Then** 首页顶部显示档案提示条（含「立即创建」CTA + 关闭 X）（FR-0H）
**And** 关闭后当次 session 不再显示、下次重启继续显示；前 3 次重启均显示，第 3 次关闭后或档案完成上传后永不再显示
**And** **运行期 B/C → A 状态切换**（Story 1.6 AC3）后，提示条逻辑随 A 态激活、复用同一建档引导（FR-0G）；**提示条计数沿用 A 态既有持久计数，不重置**
> 验证层：**L0**（前端单元/widget：计数状态机——重启计数 ≤3 显示；当次关闭→本 session 不再显示；第 3 次关闭后→永不显示；档案完成→永不显示；**B/C→A 切换后 `shouldShowProfilePrompt` 按 A 态激活、沿用既有持久计数不重置**。计数持久化于 `shared_preferences`，纯逻辑可单测）+ **L1**（「档案是否完成」依赖后端 `GET /api/v1/me`.hasPetProfile，需 postgres——本 Story 该信号可先由 Epic 2 回填，1.7 期默认 false=未创建）。
> 🔄 **PRD V1.0.0 修订（FR-0F/FR-0G · 2026-06-08）：** 原 Dev Notes 默认假设的「B→A 切换 → 激活提示条 / 复用建档引导」转为本 AC 显式覆盖（与 Story 1.6 AC3 联动）。B/C→A 后提示条按 A 态激活，计数沿用 A 态既有持久计数（不重置），符合 FR-0H「累计重启」语义。

### AC3 — 状态 B/C 不触发提示条

**Given** 状态 B 或 C 的用户
**When** 进入首页
**Then** 不触发任何档案提示条
> 验证层：**L0**（前端 widget：`pet_status ∈ {B,C}` → 首页无提示条、计数逻辑完全不激活）。

### AC4 — [R2] 建档「创建成功」庆祝页 + 推送权限时序（FR-0G · 决策 F15）

**Given** 用户在 FR-0G 档案创建引导中完成宠物档案创建
**When** 创建成功
**Then** 展示「创建成功」庆祝页：宠物头像 + 名字 +「[宠物名] 的专属档案已创建！」、副 CTA「分享宠物名片」（调系统分享 sheet 传 FR-14 名片链接）、主 CTA「开始探索」
**And** 点主 CTA「开始探索」→ 触发推送权限弹窗（FR-22D 建档时机，**庆祝页后、进首页前**，与 Story 6.4 时序对齐）→ 随后进入首页
**And** **例外**：经 FR-16（问诊存档触发建档，Story 2.5）/ FR-12（B/C 灰选发布触发建档，Story 2.3）路径进入的建档完成，**跳过庆祝页**，直接回原流程（存档回灌 / 返回发布页预选成长日历），不展示庆祝页、不在此处触发推送弹窗
> 验证层：**L0**（前端 widget：创建成功 mock → 庆祝页渲染（宠物头像 + 名字 + 文案 + 主/副双 CTA）；主 CTA「开始探索」→ 串接推送权限弹窗钩子（mock，实际权限 API 属 L2）→ 进首页；副 CTA「分享宠物名片」→ 调系统分享（mock，传 FR-14 链接占位）；**跳过分支**：以「来源=FR-16/FR-12」参数进入 → 不渲染庆祝页、直接回原流程回调）。文案 .arb 双语，样式 token。推送权限弹窗真实系统授权属 **L2**（真机/模拟器）。

---

## Tasks / Subtasks

> **按"后端子任务 / 前端子任务 / 联调验收"三段组织**。本 Story 前端重（引导页 + 提示条 + 3 次重启计数状态机）、后端轻（`hasPetProfile` 信号，且实际由 Epic 2 提供数据源）。执行顺序：后端 → 前端 → 联调。

### 🟦 后端子任务（petgo-backend / Spring Boot）

- [x] **B1. 「档案是否已创建」信号（hasPetProfile）** (AC: 1, 2)
  - [x] 在 Story 1.6 的 `GET /api/v1/me` 响应中确保含 `hasPetProfile`（boolean）字段（当前用户主体统一端点 `/api/v1/me`，不用 `/users/me`）。
  - [x] ⚠️ `pet_profiles` 表与档案创建在 **Epic 2 Story 2.2**——已接入后由 `petProfiles.existsByOwnerId` 真实驱动（Epic 2 已建表）。
  - [x] 不新建表、不实现档案创建端点（Epic 2）。
- [x] **B2. 无其他后端改动** (AC: 3)
  - [x] 提示条 3 次重启计数、关闭逻辑、B/C 不显示均为**纯客户端状态**（`shared_preferences`），后端不参与。**R2（AC4）亦无后端改动**——庆祝页/推送时序/分享均前端 + 复用 Story 6.4 推送闸门、Story 2.7 分享服务。

### 🟩 前端子任务（petgo_app / Flutter）

- [x] **F1. 档案创建引导页（features/profile/presentation 或 features/auth）** (AC: 1; FR-0G)
  - [x] 引导页：插画/说明 + 「立即创建」CTA + 「跳过，稍后创建」弱化按钮。
  - [x] 「立即创建」→ 跳 Epic 2 档案创建表单（已接 `/profile/create`）。
  - [x] 「跳过，稍后创建」→ 进首页 + **激活提示条逻辑**（F3）。
  - [x] mock/真实「完成创建」回调 → 进首页 + **永不显示提示条**（置 `petProfileCompleted` / hasPetProfile 守卫）。
  - [x] 文案 .arb 双语，样式 token；每条最多 1 emoji（UX-DR14）。
- [x] **F2. 首页档案提示条组件（shared/widgets/profile_prompt_bar.dart）** (AC: 2)
  - [x] 首页顶部条：文案 + 「立即创建」CTA + 关闭 X。
  - [x] 样式引用 token；文案 .arb；无障碍（≥44pt 触摸目标 NFR-13）。
- [x] **F3. 提示条显示状态机（features/profile/domain + core/storage/prefs）** (AC: 2, 3)
  - [x] 持久化键（`shared_preferences`）：重启计数 / 永久关闭 / 档案完成。
  - [x] **显示判定**（仅 `pet_status==HAS_PET` 时激活；PLANNING/ENTHUSIAST **完全不激活**，AC3）：见 `shouldShowProfilePrompt` 纯函数。
  - [x] ⚠️ 精确语义：前 3 次重启均显示，第 3 次关闭后或完成上传后永不再显示——纯函数 + 单测覆盖。
- [x] **F4. 首页接线 + 档案完成回填** (AC: 1, 2)
  - [x] 首页顶部条件渲染 `ProfilePromptBar`（由 F3 判定 + `pet_status` + `hasPetProfile`）。
  - [x] 档案完成 → `petProfileCompleted` / hasPetProfile==true → 移除提示条（回填锚点 `markCompleted`）。
- [x] **F5. [R2] 建档「创建成功」庆祝页 + 主/副 CTA + 推送时序 + 跳过分支（FR-0G · F15）** (AC: 4)
  - [x] 庆祝页组件（`features/profile/presentation/profile_created_celebration_page.dart`）：宠物头像 + 名字 +「[宠物名] 的专属档案已创建！」文案、副 CTA「分享宠物名片」、主 CTA「开始探索」；文案 .arb 双语、样式 token、≥44pt 触摸目标（NFR-13）。
  - [x] 副 CTA「分享宠物名片」→ 调系统分享 `shareServiceProvider` 传 `petCardShareUrl(cardToken)`（FR-14 名片链接，复用 Story 2.7 分享服务 + 2.6 链接拼装）。
  - [x] 主 CTA「开始探索」→ 串接推送权限闸门（`pushPermissionGateProvider.maybeRequestAfterProfileCreated`，FR-22D 建档时机 = 庆祝页后、进首页前，复用 Story 6.4）→ `context.go('/home')`。`neverConsulted:true` 安全（已问诊者 `alreadyAsked` 守卫使闸门跳过——取最早、仅一次）。
  - [x] **跳过庆祝页分支**：`BuildOrigin` + `showsBuildCelebration` 纯函数（onboarding→显示；triageArchive/graySelectPublish→跳过）；create page 按路由 `?origin=` 分支（默认 onboarding→庆祝页；FR-16/FR-12→`/profile` 回原流程，Story 2.5/2.3 接管传 origin）。
  - [x] widget 测试（L0）：庆祝页渲染（头像/名字/文案/双 CTA）+ 主 CTA 触发钩子 + 副 CTA 触发系统分享（mock 传名片链接）；来源分支纯函数四态。

### 🟨 联调验收子任务（端到端跑起来 + CI）

- [x] **J1. 提示条状态机单元测试（核心）** (AC: 2,3 / **L0**)
  - [x] 纯函数 `shouldShowProfilePrompt` 全边界（重启 1/2/3 显示、第 4 次不显示、当次关闭次启仍显示、第 3 次关闭永久、完成/hasPetProfile/非 A/null）。
- [x] **J2. 引导页与首页 widget 测试** (AC: 1,2,3 / **L0**)
  - [x] 选 A → 引导页（含跳过）；跳过 → 首页显示提示条；完成 → 无提示条；PLANNING/ENTHUSIAST 无提示条；关闭 X 当次 session 隐藏。
- [ ] **J3. 跨重启走查（L2 · 待本地）** (AC: 2,4 / **L2**)
  - [ ] 真机/模拟器：A 用户跳过 → 重启 3 次观察提示条显隐序列符合 FR-0H；第 3 次关闭后再重启不显示；mock 完成档案后不显示。R2：建档完成 → 庆祝页 → 主 CTA 触发真实推送系统弹窗 → 进首页；副 CTA 真实系统分享。
- [x] **J4. 后端 hasPetProfile 信号 + CI** (AC: 2 / **L0** 部分，**L1 待本地**)
  - [x] `GET /api/v1/me` 含 `hasPetProfile`（真实 existsByOwnerId 驱动）；前端 `flutter analyze` 0 + `flutter test` 252。后端 `mvn -B package`（pg+redis）留本地/CI。

---

## Dev Notes

### 关键架构约定（本 Story 相关部分）

**FR-0H 精确语义（验收硬门槛）**：仅「状态 A 且未完成档案」用户首页顶部显示档案提示条（含「立即创建」CTA + 关闭）；**前 3 次重启显示，第 3 次关闭后或完成上传后永不再显示**。提示条是**纯客户端状态**（`shared_preferences` 计数），后端只提供 `hasPetProfile`。

**前端结构（架构 §Frontend）**：
- 提示条/引导属 profile 域 → `features/profile/{domain,presentation}`；通用条组件 `shared/widgets/profile_prompt_bar.dart`。
- 计数/标志入 `core/storage/prefs`（`shared_preferences`——非敏感偏好：语言/petStatus/**引导计数**，架构 §令牌存储明列「引导计数」走 prefs）。
- 显示判定写成可单测纯函数；Riverpod provider 管状态，副作用进 provider。

**FR-11 边界**：档案表单本体在 Epic 2 Story 2.2（建 `pet_profiles` 表）；本 Story「立即创建」仅路由到其入口（占位），`hasPetProfile` 由 Epic 2 回填。

**i18n / token**：引导页 + 提示条全部文案 .arb 双语；视觉引用 Story 1.2 token；无障碍 ≥44pt 触摸目标（NFR-13）。

### 强制护栏（架构 §Enforcement —— 违反即返工）

- **引导计数/标志走 `shared_preferences`（非敏感偏好）**，不入 secure_storage、不入后端（后端不存「提示条已关几次」）。
- **提示条状态机写成可单测纯函数**——FR-0H 的「3 次重启 + 第 3 次关闭/完成永不显示」语义复杂，必须有单测覆盖边界。
- **B/C 完全不激活提示条逻辑**（不只是隐藏，是不进计数/不持久化），防 B→A 切换时计数污染（如有切换语义，Completion Notes 记录处理）。
- 无写死 UI 文案 / 无硬编码视觉常量（沿用 1.2 token + i18n）。
- 不引入新中间件；不在本 Story 建 `pet_profiles` 表（Epic 2）。

### 范围边界（防 scope creep —— 本 Story 明确不做）

- ❌ 不实现宠物档案创建表单 / `pet_profiles` 表（FR-11，Epic 2 Story 2.2）——「立即创建」仅跳占位。
- ❌ 不实现成长档案 Tab 时间线（FR-37，Epic 2）。
- ❌ 后端不存提示条计数/关闭状态（纯客户端）。
- ❌ 不实现 Feed 内容（Epic 3）——提示条挂在首页容器顶部即可。
- ✅ 只做：状态 A 档案创建引导页（入口+跳过）+ 首页提示条组件 + 3 次重启显示状态机（纯函数可单测）+ B/C 不触发 + 档案完成/永久关闭回填锚点 + 后端 `hasPetProfile` 信号占位。

### 页面多态完整性（本 Story 相关态）

- A 用户跳过后首页：提示条显示（符合计数）；提示条不应遮挡 Feed 首条（顶部条，可关闭）。
- 提示条「立即创建」跳转目标（Epic 2 创建页）本 Story 期未实现 → 占位页（不崩、UX-DR18 友好占位），Epic 2 替换。
- A 用户已在别处创建了档案（`hasPetProfile==true`，Epic 2 回填）→ 即使计数未满也不显示提示条。
- B/C 用户 → 无引导页、无提示条（AC3）。
- 计数 prefs 损坏/缺失 → 默认从 0 开始（首启显示），不崩。

### Project Structure Notes

- 新增（前端）：`features/profile/presentation/profile_onboarding_page.dart`（引导页）、`shared/widgets/profile_prompt_bar.dart`、`features/profile/domain/profile_prompt_state.dart`（含纯函数 `shouldShowProfilePrompt`）、`core/storage/prefs` 增三个键。
- 复用：Story 1.6 `pet_status` 分叉锚点、Story 1.2 首页容器 + token + i18n、Story 1.3 `GET /api/v1/me`。
- 后端：仅 `GET /api/v1/me` 响应增 `hasPetProfile`（占位 false，Epic 2 回填）；**不新建表/端点**。

### References

- [Source: epics.md#Story 1.7] — 三组原始 AC（Given/When/Then）。
- [Source: PRD §4 FR-0G] — 状态 A 进档案创建引导（复用 FR-11 表单）+ 「跳过，稍后创建」。
- [Source: PRD §4 FR-0H] — 提示条仅状态 A 未完成档案显示；前 3 次重启显示，第 3 次关闭后或完成上传后永不再显示。
- [Source: PRD §4 FR-11] — 宠物档案创建（表单本体在 Epic 2，本 Story 仅路由）。
- [Source: architecture.md#Frontend Architecture] — prefs 存「引导计数」、Riverpod 不可变状态。
- [Source: architecture.md#Authentication & Security · 令牌存储] — `shared_preferences` 存非敏感偏好（语言/petStatus/onboarding 计数）。
- [Source: epics.md#Story 1.6] — 状态 A 分流 + `pet_status`（依赖）。
- [Source: epics.md#Story 2.2] — `pet_profiles` 表 + 档案创建（下游，回填 hasPetProfile/完成语义）。
- [Source: epics.md#UX Design Requirements] — UX-DR14（双语 microcopy/emoji）、UX-DR18（占位态不崩）、NFR-13（≥44pt 触摸目标）。
- [Memory: spec-page-state-completeness] — 覆盖已创建/永久关闭/B-C 不显示/计数损坏多态。
- [Memory: v1-architecture-posture] — 提示条纯客户端、不过度设计、不入后端。

## Dev Agent Record

### Agent Model Used

claude-opus-4-8 (1M context) · R2 增量

### Debug Log References

### Completion Notes List

- **L0 全绿**：前端 `flutter analyze` 零警告 + `flutter test` 全套 46/46（本 Story 新增 12 例：纯状态机 7 + widget 5）。**无后端改动**（`hasPetProfile` 信号已于 Story 1.6 在 `GET /api/v1/me` 提供）。
- **`hasPetProfile` 本 Story 期实现**：`MeService.hasPetProfile()` 恒返回 false（无 `pet_profiles` 表）。**Epic 2 Story 2.2 回填契约**：建 `pet_profiles` 表后改为「当前用户存在档案 → true」，前端提示条 `shouldShowProfilePrompt` 已据此参数即时隐藏（已留 widget 测试覆盖 hasPetProfile=true→不显示）。
- **提示条状态机纯函数 `shouldShowProfilePrompt`**（`features/profile/domain/profile_prompt_state.dart`）边界：
  - 仅 `petStatus=='A'` 激活；`hasPetProfile || petProfileCompleted || dismissedPermanently || dismissedThisSession` → 不显示；否则 `restartCount <= 3` 显示。
  - `onColdStartIncrement`（重启 +1，封顶 4）、`onDismiss`（count<3 仅本 session 隐藏不增计数；**count≥3 关闭→永久**）、`onProfileCompleted`（永久）。
  - 单测覆盖：1/2/3 次重启显示、第 4 次不显示、当次关闭次启仍显示、第 3 次关闭永久、完成/hasPetProfile/B/C/null 各态。
- **持久化**：`restartCount`/`dismissedPermanently`/`petProfileCompleted` 走 `shared_preferences`（`AppPrefs` 三键）；`dismissedThisSession` 仅内存。冷启动在 `main()` 加载 prefs + `onColdStartIncrement` + 回写计数，经 `profilePromptBootstrapProvider` override 注入；`ProfilePromptController` best-effort 持久化（prefs 缺失→忽略，不崩，对应「计数损坏默认从 0」）。**后端不存提示条状态**（纯客户端）。
- **B→A 切换计数处理**：本 Story 引导流 A→onboarding/profile、B/C→home（Story 1.6 一次性选择）；提示条计数仅在 `petStatus=='A'` 时参与判定（B/C 完全不激活，不读写计数），故 B→A 切换不会产生计数污染——B/C→A 切换时 A 态下沿用既有持久计数（不重置），符合 FR-0H「累计重启」语义。
  > 🔄 **PRD V1.0.0 修订（FR-0F/FR-0G · 2026-06-08）：** 旧判断「V1 无运行期 B↔A 频繁切换语义」**已作废**——B/C→A 切换语义已转正（Story 1.6 AC3 + 本 Story AC2），运行期改状态选 A 复用同一建档引导（FR-0G）+ 提示条按 A 态激活并沿用既有持久计数。本 Story 状态机 `shouldShowProfilePrompt` 已天然支持（仅按 `petStatus=='A'` + 既有计数判定），无需改实现，仅将该路径转为显式 AC 覆盖。
- **档案引导页**：`ProfileOnboardingPage` = 插画 + 「立即创建」（→ Epic 2 创建页，本 Story 期占位跳 `/home`）+ 「跳过，稍后创建」（→ `/home` + 激活提示条逻辑）。档案完成回填锚点 = `ProfilePromptController.markCompleted()`（Epic 2 创建成功事件调用）。
- **待本地验收**：
  - **L1（Docker pg+redis）**：`GET /api/v1/me` 含 `hasPetProfile`（Epic 2 后真实 existsByOwnerId 驱动）。
  - **L2（真机跨重启走查）**：A 用户跳过 → 重启 3 次观察提示条显隐序列符合 FR-0H；第 3 次关闭后再重启不显示；mock 完成档案后不显示。

#### R2 增量（AC4 建档庆祝页 + 推送时序 + 跳过分支 · FR-0G · 决策 F15 · 2026-06-08）

- **庆祝页**：新增 `ProfileCreatedCelebrationPage`（dumb ConsumerWidget）——宠物头像 + 名字 +「{petName} 的专属档案已创建！🎉」+ 主 CTA「开始探索」+ 副 CTA「分享宠物名片」。文案走新 .arb `profileCreatedTitle`（placeholder petName）/`profileCreatedStartExplore`，分享 CTA 复用既有 `shareFabLabel`；样式 token。
- **推送时序锚点（FR-22D）**：主 CTA「开始探索」由路由注入 `onStartExplore`——`pushPermissionGateProvider.maybeRequestAfterProfileCreated(neverConsulted:true)`（复用 Story 6.4 闸门，建档时机=庆祝页后/进首页前）后 `context.go('/home')`。**`neverConsulted:true` 安全**：已问诊用户其 `pushPermissionAsked` 在问诊时机已置 true，闸门 `alreadyAsked` 守卫使其跳过（取最早、仅一次），无双弹。真实系统权限 API 属 6.4/L2。
- **分享**：副 CTA → `shareServiceProvider(petCardShareUrl(cardToken))`（复用 Story 2.7 分享服务 + 2.6 名片链接拼装，不暴露顺序 id）。
- **跳过分支（F15）**：新增 `BuildOrigin{onboarding|triageArchive|graySelectPublish}` + `showsBuildCelebration` 纯函数；`PetProfileCreatePage._submit` 成功后按路由 `?origin=` 分流——onboarding（默认/缺省，即所有 FR-0G 入口）→ `/profile/created`（庆祝页，extra 传 PetProfile）；FR-16/FR-12 → `/profile` 回原流程（Story 2.5/2.3 接入时传 origin）。庆祝页 `extra` 缺失防御性回首页不崩。
- **L0 全绿（R2）**：`flutter analyze` 0 + `flutter test` **252**（+6：来源分支纯函数 + 庆祝页渲染/主 CTA 钩子/副 CTA 分享）。**无后端改动**（复用 1.6 `hasPetProfile` + 6.4 闸门 + 2.7 分享）。L2 真机推送/分享待本地。

### File List

**前端 — 新增**
- `features/profile/domain/profile_prompt_state.dart`（纯状态机 + `shouldShowProfilePrompt`）
- `features/profile/domain/profile_prompt_controller.dart`（Notifier + bootstrap + 持久化）
- `shared/widgets/profile_prompt_bar.dart`
- `test/profile/profile_prompt_test.dart`（纯函数 7 例）
- `test/profile/profile_prompt_widget_test.dart`（widget 5 例）

**前端 — 修改**
- `features/profile/presentation/profile_onboarding_page.dart`（占位 → 入口 + 跳过完整引导页）
- `features/content/presentation/home_page.dart`（ConsumerWidget + 顶部条件渲染 ProfilePromptBar）
- `lib/main.dart`（async：加载 prefs + 冷启动计数 +1 + bootstrap override）
- `lib/l10n/app_en.arb`、`app_id.arb`（提示条文案）

**后端**：无改动（`hasPetProfile` 信号于 Story 1.6 已提供）。

**R2 新增（前端）**
- `features/profile/domain/profile_created_flow.dart`（`BuildOrigin` + `showsBuildCelebration` + `buildOriginFromName` 纯函数）
- `features/profile/presentation/profile_created_celebration_page.dart`（庆祝页）
- `test/profile/profile_created_celebration_test.dart`（来源分支 + 庆祝页 widget，6 例）

**R2 修改（前端）**
- `core/router/app_router.dart`（+`/profile/created` 路由，串接推送闸门 + 进首页）
- `features/profile/presentation/pet_profile_create_page.dart`（建档成功按 origin 分流：庆祝页 / 跳过）
- `lib/l10n/app_en.arb`、`app_id.arb`（`profileCreatedTitle`/`profileCreatedStartExplore`）

### Change Log

- 2026-06-08 · R2（AC4 · FR-0G · 决策 F15）：建档「创建成功」庆祝页（头像/名字/文案 + 主 CTA 开始探索 + 副 CTA 分享名片）；主 CTA 串接 Story 6.4 推送权限闸门（建档时机=庆祝页后/进首页前）后进首页；副 CTA 复用 Story 2.7 分享 + 2.6 名片链接；`BuildOrigin`+`showsBuildCelebration` 纯函数实现 FR-16/FR-12 跳过庆祝页分支，create page 按 `?origin=` 分流。L0 全绿（analyze 0 + test 252，无后端改动）。L2 真机推送/分享待本地。
