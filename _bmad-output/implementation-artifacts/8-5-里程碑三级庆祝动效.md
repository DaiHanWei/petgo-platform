---
baseline_commit: 9ed7293
---

# Story 8.5: 里程碑三级庆祝动效

Status: review

> 前端：完成里程碑后按级触发 S/M/L 三级庆祝动效（mint 风格，无第三方动画包，手绘 implicit 动画）。
> 视觉/计时观感是 **L2**（云端 headless 验不了，teleport 本地模拟器看），本 Story 落代码 + L0 构建/计时逻辑测试。

## Story
As a **用户**, I want **完成里程碑时收到与其分量匹配的庆祝**, so that **我的成长记录有仪式感与情绪回报**。

## Acceptance Criteria
### AC1 — S 半屏弹层 1-2s（FR-42）。**L0**（渲染 + 自动消失）/ **L2** 观感。
### AC2 — M 全屏 ~3s + 徽章解锁。**L0** / **L2**。
### AC3 — L Duolingo 开宝箱式交互 + 衔接分享卡（8.6）；mint token。**L0**（开箱交互）/ **L2**。

## Tasks / Subtasks
### 🟩 前端
- [x] `widgets/milestone_celebration.dart`：`showMilestoneCelebration(context, item, {onShare})` 按 level 分流
  —— S `_half`（elasticOut scale，1.5s 自动消失）/ M `_full`（全屏 fade+scale，3s）/ L `_chest`（宝箱点击开 →
  奖杯爆发，4s 后衔接 onShare 8.6）。mint token（mintTint/mint700/gold）。
- [x] 触发：picker 打卡成功（`_confirm`）以 checkIn 返回的 item 按级庆祝。
- [x] l10n（en/id）：milestoneCelebrateUnlocked / OpenChest。

### 🔗 验收
- L0：`milestone_celebration_test`（S 自动消失 / M 渲染 / L 开宝箱）+ analyze 0 issue。
- **L2 待本地**：三级动效真机视觉/计时观感、L 宝箱交互手感、mint 配色 —— teleport 本地模拟器验收。

## Dev Agent Record
### Completion Notes
- 无第三方动画包（避免新依赖）：纯 AnimationController + Curves（elasticOut/easeInOut）手绘；confetti/复杂粒子
  留作 L2 本地打磨增强点。
- 触发点：本 Story 接 8.4 用户打卡成功路径；系统自动完成（8.3）发生在服务端，App 侧不强推庆祝（避免打断），
  用户下次进里程碑页见彩色徽章即可。L 级达成推送 + 分享卡在 8.6。
- onShare 回调预留给 8.6 注入分享卡自动弹出（L 级）。
- **L2 待本地**：动效视觉/计时/手感。
