# Story 5.10: Epic 5 联调

Status: **review**（L1 全绿；**L2 模拟器走通待本地人工**）

## 交付物
`petgo-backend/src/test/java/com/tailtopia/shop/Epic5ChainIntegrationTest.java`（4 例，L1）。

## AC 与验证
| AC | 层级 | 验证 |
|---|---|---|
| 三行混合支付订单退 1 行 → 审核 → 寄回 → 质检 → 退款 | **L1** | `partialThenFullReturnChain` 第一段 |
| 🔴 订单主状态不变（不被整单标记已退款） | L1 | 同上，断言仍是 `DELIVERED` |
| PawCoin 段按比例退回，`pawcoin_transactions` 有 REFUND 记录 | L1 | 同上 |
| 去程运费未退回（部分退） | L1 | 同上，`refundedTotal == 该行金额` |
| 退货商品以退货批次入库、可售库存增加 | L1 | 同上，`RETURN_INBOUND` + `purchase_no = 原订单号` |
| 退完剩余 2 行 → 🔴 订单此时才转 REFUNDED | L1 | `partialThenFullReturnChain` 第二段 |
| 🔴🔴 **累计 PawCoin 恰好等于 coin_amount，零漂移**（AD-2 核心验收） | **L1** | 同上 |
| 整单退完时去程运费也已退回 | L1 | 同上，`refundedTotal == totalAmount` |
| 🔴 不存在任何路径让 PawCoin 段退成真钱（后台/CS/API） | L0 + L1 | `noPathAnywhereTurnsCoinSegmentIntoCash`（四层断言） |
| L2 模拟器走通 | **L2** | ⏳ 待本地人工 |

## 一处刻意的测试设计
**金额刻意选除不尽的三档**（100 001 / 89 999 / 95 000 + 运费 20 000）。
整除的数据会让「按单次取整」的错误实现也能通过 —— 那样这条 AD-2 核心验收就是假绿。
5.2 的单测里另有一条对照用例专门守「测试数据不能被改成整除的」。

## 🔒 防套现红线的四层断言
1. `RefundExecutionService` 源码只有 `wallet.credit(`，没有 `wallet.debit(`；
2. `AdminReturnService` **完全不出现 `wallet.`** —— 后台若能直接动钱包，职责分离形同虚设；
3. `MeReturnController` 里不出现 `coinDestination`；
4. `CashDestination` 只有 2 个值、`RefundSplit` 只有 2 个分量。

后端全量：`mvn -B test` → **1921 通过 / 0 失败 / 6 跳过**。
