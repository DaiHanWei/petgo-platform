---
title: "Story 2.2: 服务范围与运费表配置（后台 AB-11C）"
epic: 2
story: 2
version: v1.4.0
created: 2026-08-17
flyway: V107
baseline_commit: b77018595c3ae8d46bf54aa7ea43769ae2e969fb
---

# Story 2.2: 服务范围与运费表配置（后台 AB-11C）

Status: review

> ✅ **SPEC-8 阻塞已由 C-14 解除**：原「配送方式 × 区域」二维运费表**降为一维**，配送方式只剩 Reguler 一档。

## Acceptance Criteria
- [x] 以 **Kecamatan 为粒度**维护可配送列表
- [x] **区域一维**固定运费表（C-14）
- [x] 🔴 界面与数据模型**不出现「配送方式」维度**；Sameday 本版本不做
- [x] 免运费门槛可配
- [x] 🔴 **不接承运商实时计费 API**（FR-99）

## Dev Notes

### 🔴 为什么表里没有 `delivery_method` 列
C-14 把二维降一维后，留一个恒等于 `REGULER` 的列**只会让人误以为多档已经支持**。
将来真要加档位，那是加一列 + 改唯一约束，不是现在先占个位。已有断言看住这一点。

### 🔴 停用区域用 `active=false`，不删行
删了就丢掉「这个区域曾经可配送、运费是多少」，而**历史订单的运费需要可追溯**（AB-13D 对账）。

### 承运商不是运费维度
JNE / SiCepat / Anteraja 仍是多家，但只在发货时由运营选择并录单号（Epic 4 AB-11B）。运费由本表决定。

## Dev Agent Record
### Agent Model Used
claude-opus-5[1m]（Claude Code）
### Debug Log References
后端全量 **1698 通过 / 0 失败**
### File List
**新增**：`V107__init_shipping_zones.sql` · `shop/shipping/{domain,repository,service/AdminShippingZoneService}`
**修改**：⚠️`AuditActions.java`（共享文件，+2 动作码）

## ⚠️ 2026-08-18 补丁：AB-11C 后台页面当时没建

本 story 原先只落了 `AdminShippingZoneService` 与 V107 迁移，**没有后台页面** ——
运营因此无从维护可配送区域与运费，而**没有可配送区域时整个商城一单也发不出去**。
由 Epic 5 的 S-7（退货收件地址是 AB-11C 增配项）顺带补齐：

- 新增 `admin/AdminShippingController` + `templates/admin/shop-shipping.html`（三语 i18n）
- 页面含：区域列表与增改、启停（🔴 停用不删行，历史订单运费要可追溯）、免运门槛、退货收件地址
- 🔴 界面**不出现「配送方式」维度**（C-14 一维口径）——这条原 AC 在页面层面到此才真正被验证
- 权限沿用既有 `config.view` / `config.edit`（运费是钱，与定价同类），未新增权限码

## Change Log
| 2026-08-17 | 创建并实现 → `review` |
| 2026-08-18 | 补齐 AB-11C 后台页面（原实现缺页面），随 Epic 5 提交 |
