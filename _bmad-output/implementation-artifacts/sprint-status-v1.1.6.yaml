# generated: 2026-08-17
# last_updated: 2026-08-21
# project: TailTopia V1.1.6
# project_key: NOKEY
# tracking_system: file-system
# story_location: {project-root}/_bmad-output/implementation-artifacts/v1.1.6
# source_epics: {project-root}/_bmad-output/planning-artifacts/epics-v1.1.6.md (10 Epic / 29 Story)
# architecture: {project-root}/_bmad-output/planning-artifacts/architecture-v1.1.6-delta.md (AD-1~AD-17，权威)
#               ✅ 2026-08-16 AD-1 新增 Rule 9（访客视图不套拉黑过滤，闭合 OA-6）
# prd: {project-root}/_bmad-output/planning-artifacts/v1.1.6/PRD-v1.1.6.md
#      + PRD-v1.1.6-admin.md（后台侧，AB-* 编号）
#
# NOTE: 本文件是 V1.1.6 增量 sprint 跟踪，与 V1.0 的 sprint-status.yaml、
#       V1.1 的 sprint-status-v1.1.yaml、V1.1.2 的 sprint-status-v1.1.2.yaml 并存，均不覆盖。
#       story 文件另放子目录 v1.1.6/，避免与各版的 1-1/2-1 编号混淆。
#
# ⚠️ 版本编号：本版本**原名 1.1.4，2026-08-11 交叉重编号后顺延为 1.1.6**。
#    「1.1.4」已被另一批需求取走（社区管控：FR-58 举报 / FR-94 拉黑 / FR-96 身份证分享），
#    该版本与本版本并行开发、**先于本版本发版**。判断某条能力归哪个版本，
#    **用 FR / AB 编号比用版本号可靠** —— 编号全程没有重新分配。

# STATUS DEFINITIONS:
# ==================
# Epic Status:   backlog -> in-progress -> done
# Story Status:  backlog -> ready-for-dev -> in-progress -> review -> done
# Retrospective: optional <-> done
# Action Item:   open -> in-progress -> done
#
# WORKFLOW NOTES:
# ===============
# - Epic 首个 story 创建(create-story)时自动 -> in-progress
# - 每 story 三段推进：后端 -> 前端 -> 联调（CLAUDE.md 纪律）
# - 每条 AC 已标 L0(静态) / L1(集成) / L2(端到端) 层级
# - 云端 headless 只跑到 L0 绿灯；L1/L2 留本地，在 Completion Notes 标注
# - Dev 完成后转 review，再跑 code-review（建议新开 context）
#
# ===== Epic 依赖链（全后向，无环） =====
# 零依赖、可立刻开工：E1 · E3 · E6 · E7 · E8 · E9
# E1 (1-1 性别字段) -> E2      # 访客视图基础档案区要展示性别；1-1 极小，不构成真实阻塞
# E3 -> E4                     # 顶置条目与普通条目同构，卡片改版未定稿会改两次
# E3 -> E5                     # 两类标签都要挂在 3-4 交付的图片区叠加层里
# E9 独立                      # 2026-08-11 对抗性评审已删除原「依赖 Epic 2」那条边
# 全部 -> E10                  # 埋点收尾，沿用 V1.1.2「埋点集中一条收尾 story」惯例
#
# ===== 跨版本依赖（唯一一条） =====
# E4 的 Story 4-4 需 V1.1.4 的拉黑过滤层已上线（架构 delta AD-8 Rule 6b）
#   ✅ 2026-08-16 复核：V1.1.4 拉黑已实现完毕（分支 hex/v1.1.4，Story 1.1~4.1 全部 review，
#      L0+L1 全绿、L2 视觉验收已过），**待合入 main**。阻塞已解除，4-4 从「等对方开工」
#      降为「等对方发版」。若最终 V1.1.4 发版晚于本版本，回头重估（自建过滤 / 记为已知缺陷）。
#
# ===== Epic 内部顺序（刻意为之，勿调换） =====
# E1: 1-1 性别字段 -> 1-2 主页面重做（1-2 要展示性别）；1-3 / 1-4 无前后依赖，可并行插入
# E3: 3-1 后端取数与尺寸列 -> 3-2 通栏容器 -> 3-3 图片三段口径 -> 3-4 轮播与叠加层 -> 3-5 上传裁剪
#     反过来先做渲染会拿不到尺寸数据，只能先写死比例再返工
# E4: 4-1 数据模型与时间窗 -> 4-2 坑位渲染 -> 4-3 推广卡片；4-4 拉黑过滤单列且可延后
# E8: 8-1 权限状态快照（P0 前置，先有观测再改行为）-> 8-2 用户侧 -> 8-3 兽医端
# E9: 9-1 卡片生成基建 -> 9-2 两套模板 -> 9-3 入口与落地分流
#
# ===== 🔴 Flyway 号：本版本从 V105 起，不是 V100 =====
# ✅ 2026-08-17 Story 1-1 已实际占用 V105（add_pet_profile_sex）。**后续 story 从 V106 起。**
# 架构 delta 的 frontmatter 写着 flywayBaseline: 'V99'、「新增从 V100 起顺延」——**那已过期**
# （2026-08-11 写的，当时 V100 与 V1.1.4 的四个迁移都还不存在）。2026-08-17 实核：
#   本分支已占：…V99、V100（add_id_card_student_fields）
#   hex/v1.1.4 已占：V101 / V102 / V103 / V104（拉黑关系表、账号举报、账号处置、通知类型扩容）
#   且 V1.1.4 **先于本版本发版**，其号是既成事实。
# 故本版本第一个迁移取 **V105**，后续依次 V106、V108…… 取 V101 会同号不同内容，
# 两分支合并后 Flyway 校验失败、启动即崩。
# （CLAUDE.md 决策 E2：序号按执行顺序单调分配，**勿照搬 architecture 示例号**。）
#
# 🔴 2026-08-20 号段实核（撞号事故后重新盘点，取号前必读）：
#   全仓 100–125 **已全部占用**：
#     V100        本线
#     V101–V104   hex/v1.1.4（拉黑关系表 / 账号举报 / 账号处置 / 通知类型扩容）
#     V105 V106   本线（宠物性别 / 图片尺寸列）
#     V107        hex/v1.1.4 + stag（view_tickets 权限回填）——**已 applied 在 petgo_stag**
#     V108–V112   本线（顶置 / 用户标签 / 内容标签 / S-M 里程碑通知类型 / 手机号）
#     V113–V125   ~~shawn/oneline-ecommerce~~ → **2026-08-20 电商已整体迁至 V140+（团队口头确认）**，
#                 这一段**已空出**。⚠️ 但**远端 origin/shawn/oneline-ecommerce（最后更新 08-18）
#                 仍占着 113–125**，上面只有一条「改号方案（待发 Dai/何鑫）」的文档提交 ——
#                 改号尚未推送。取这一段的号前**先复核那条分支**，别照本行当既成事实。
#     V126        本线（通知游标索引；原取 V107，与 1.1.4/stag 的 V107 撞号，2026-08-20 改号）
#   ⇒ **下一个新迁移取 V127**（决策 E2：取全局 max 之后顺延）。
#     113–125 虽已空出也**不回填** —— 回填低号只在 out-of-order 下才安全，且会让
#     「当前 max」这个取号依据失效，下一个人又要重新盘一遍。
#   ⚠️ 天花板：电商从 V140 起，本线从 V127 起 ⇒ **本线最多再用 13 个号（V127–V139）**。
#     接近时须与电商侧重新划段，别一路顺延撞进 140。
#   教训：本线原先「V105 起依次顺延」的写法只盘了 hex/v1.1.4，漏了电商那条分支，
#   而 V107 是两条线各自顺延到同一个号上撞的 —— 取号前要盘**全部远端分支**，
#   不只是自己知道的那两条：
#     for b in $(git branch -r | grep -v HEAD); do \
#       git ls-tree -r --name-only $b -- petgo-backend/src/main/resources/db/migration; \
#     done | sed -n 's|.*/V\([0-9]*\)__.*|\1|p' | sort -n -u | tail -20
#
# ===== 本版本必须如期交付的一条埋点 =====
# E-3（顶置重复曝光）归 Epic 4、随顶置功能一并上线，**不得推迟到 Epic 10**：
# 它是下游 V1.1.8 判断「要不要为顶置做去重」的唯一数据来源（其 OQ-B3 明确依赖 E-3 的
# serp_position 分布）。

generated: 2026-08-17
last_updated: 2026-08-21
project: TailTopia V1.1.6
project_key: NOKEY
tracking_system: file-system
story_location: "{project-root}/_bmad-output/implementation-artifacts/v1.1.6"

development_status:
  # ===== Epic 1: 宠物主页对外分享页改版（H5）· FR-92 §①③④ + E-23~E-27 =====
  # 零依赖起点之一。共享文件：三个 Thymeleaf 模板 + 名片页控制器 —— 埋点接入与模板改造同文件，
  # 故并入本 Epic，避免两次改动同一文件。
  epic-1: in-progress
  1-1-宠物性别字段落地: review                    # 现状是编辑页纯前端占位（选了不提交不入库），顺手修掉；存量为空须容错。零依赖，且是 Epic 2 的前置
  1-2-h5主页面按定稿视觉重做: review                 # 按 E1/E2 屏；主色纠正为品牌紫；单 CTA 与吸底并入本条
  1-3-失效页印尼化新增加载失败页: review             # 失效页现为全篇英文且被两条链路共用；加载失败页当前不存在（异常时浏览器白屏），属新增页面。零依赖
  1-4-h5埋点从零接入: review                        # 页面现无任何埋点；服务端上报不引前端 SDK；点击与结果拆两个事件
  epic-1-retrospective: optional

  # ===== Epic 2: App 内访客只读视图 · FR-92 §② =====
  # 依赖 Epic 1 的性别字段（1-1 极小，不构成真实阻塞）。反向不依赖。
  # ⚠️ AD-1 Rule 9（2026-08-16 闭合 OA-6）：访客视图**不套拉黑过滤**，且响应体不得下发任何
  #    拉黑相关信号。实现上什么都不用做 —— 那组 AC 是防止有人主动加上去。
  epic-2: in-progress
  2-1-访客只读投影层: review                        # 唯一访客数据出口；结构上不持有健康记录/问诊仓库
  2-2-日历视图与某天详情的访客版: review
  2-3-访客态页面渲染: review                        # 作者态减法，不得另画一套
  2-4-宠物名片深链落点分流: review                   # 现状错误落在给游客做的示例页
  epic-2-retrospective: optional

  # ===== Epic 3: Feed 卡片改造与图片规范化 · FR-71 + FR-93 + E-8/E-22 =====
  # 零依赖。是 Epic 4 与 Epic 5 的前置 —— 先做完它，顶置与标签才不用改两次卡片。
  epic-3: in-progress
  3-1-feed取数扩张与图片尺寸列: review               # 后端；一律批量聚合（AD-7），禁逐条查、禁冗余计数列
  3-2-通栏容器与操作行: review                      # 点赞并入本条（原单列一条属人为切分）
  3-3-图片渲染三段口径与加载期占位: review                # 按实际比例 -> clamp 0.75~1.34 -> 高度护栏（AD-6，顺序固定）
  3-4-多图轮播与图片区叠加层: review                      # 叠加层是 Epic 5 两类标签的挂载点
  3-5-发布页图片上传裁剪: review                         # ✅ 拍板自研，本版本 App 端零新增依赖（AC4 规定了按钮位置，第三方库界面重塑不了）
  epic-3-retrospective: optional

  # ===== Epic 4: 内容顶置坑位 · FR-68 + E-1/E-2/E-3 =====
  # 依赖 Epic 3。E-3 必须随本 Epic 交付，不得推迟到 Epic 10（下游 V1.1.8 的唯一数据来源）。
  epic-4: in-progress
  4-1-顶置配置数据模型与时间窗判定: review                # 查询时判定，不落状态列、不建扫描器（AD-9）；时间窗判定须是唯一实现、四处共用
  4-2-首页坑位渲染与只首屏让位: review                    # 顶置 content_id 仅从第一页排除，后续页仍可出现（AD-8）
  4-3-推广卡片: review                                   # 不对应真实帖子
  4-4-顶置坑位套用拉黑过滤: review                   # ⚠️ 跨版本依赖 V1.1.4（已实现待发版）；套 findFeed 的**账号维度**那条子查询，两条并列不合并
  epic-4-retrospective: optional

  # ===== Epic 5: 运营标签体系 · FR-74 + FR-75 =====
  # 依赖 Epic 3（两类标签挂在 3-4 的图片区叠加层；装饰标签占左下角，与顶置角标的右上角互不重叠）。
  epic-5: in-progress
  5-1-用户标签四处展示位与tooltip: review                 # 七处展示位一律批量取数（AD-11），评论列表尤其禁止逐条
  5-2-内容装饰标签三处展示位: review      
  epic-5-retrospective: optional

  # ===== Epic 6: 里程碑通知补齐 · FR-76 =====
  # 全版本最小的一条，零依赖，可立刻开工。
  epic-6: in-progress
  6-1-sm级里程碑写入通知中心: review                      # 新增独立通知类型：进通知中心不发 push、计入未读角标、多个解锁写 N 条、存量不回填（AD-13）
  epic-6-retrospective: optional

  # ===== Epic 7: 手机号采集 · FR-70 =====
  # 无代码依赖。🔴 OA-2 阻塞上线：用户协议是否已覆盖「手机号用于运营召回与后续触达」。
  # 2026-08-18 OA-5 收窄：7-1 可以开工（不碰时机）；🔴 7-2 仍被挡 —— 旧的两个时机在线上
  #    已死，而新时机至今无人定义（AD-14 只写了「须自行实现时机判定」，没给口径）。
  epic-7: in-progress
  7-1-手机号字段与保存清空: review                        # 选填、允许清空写 null、禁入日志，脱敏在展示层（AD-17）
  7-2-软引导与设置页常驻入口: review                 # 🔴 OA-5 仍挡这一条：新时机口径待产品给出（旧的两个时机在线上已不再触发，AD-14）
  epic-7-retrospective: optional

  # ===== Epic 8: 推送权限重构 · FR-85 + E-19/E-20/E-21 =====
  # 无代码依赖。✅ 2026-08-18 OA-5 收窄后**可以开工**：技术口径已由 AD-14 全部定死，
  #    四个触发点的时机 PRD 本就写明、并未失效；剩下的是源目录 PRD 回写（文档同步，不挡代码）。
  # ⚠️ AD-14：全部触发点只走「跳系统设置」，PRD 的「原生弹窗归属」整段作废
  #    （首启即申请已于 2026-08-07 上线）；照 PRD 字面实现会做出一段永远走不到的分支。
  epic-8: in-progress
  8-1-系统通知权限状态快照: review                   # P0 前置：先有观测再改行为
  8-2-用户侧三触发点与通知中心引导条: review
  8-3-兽医端触发点: review                          # 切换为在线时
  epic-8-retrospective: optional

  # ===== Epic 9: 单条内容分享卡 · FR-73 =====
  # 无依赖（2026-08-11 对抗性评审已删除原「依赖 Epic 2」那条边）。
  # 客户端生成，复用宠物身份证既有做法；服务端出图能力仅供社交预览缩略图，不挪用（AD-15）。
  epic-9: in-progress
  9-1-卡片生成基建: review                          # 双尺寸 + 水印 + 文字排版
  9-2-两套模板与下载二维码: review                   # 有图 / 纯文字
  9-3-分享入口与链接落地分流: review
  epic-9-retrospective: optional

  # ===== Epic 10: 埋点收尾（App 侧） =====
  # 依赖前述各 Epic 的功能须先在。E-3 已随 Epic 4 交付，不在此。
  epic-10: in-progress
  10-1-app侧剩余埋点与回写全局文档: review           # ✅ OA-7 已闭合（2026-08-18 定案改名）：实现按新名，见埋点清单 §1 的新旧对照表
  epic-10-retrospective: optional

  # ===== Epic 11：运营后台配置补齐（2026-08-21 拆入）=====
  # 🔴 本 Epic 是「已交付功能的开关」，不是新功能。App 侧 FR-68/70/74/75 已全部进 review、
  #    表也建好了（content_pins V108 / user_tags V109 / content_tags V110 / users.phone V112），
  #    但后台一个页面都没有 —— 这四条不做，那四个 FR 就是上线即空壳。
  # ⚠️ 后台 PRD 的 OQ-18 结论已过期（它写「确认与 App 端同步开发」，实际 App 侧已做完、后台未开工）。
  # 四条彼此零耦合，可并行、可任意顺序。
  epic-11: in-progress
  11-1-顶置管理feed坑位: review                     # AB-10A ⚠️ 同坑位时间重叠校验全系统不存在，后台是唯一能挡的地方
  11-2-内容装饰标签管理: review                     # AB-10C 需加「下线」列；打标界面必须提示 ×1.3 排序副作用
  11-3-用户标签配置与分配: review                   # AB-12A 需加「下线」列；排序口径以 App 侧 AuthorView 为权威，勿另立
  11-4-用户手机号查看与召回名单导出: review         # AB-11A 前半（PII 双权限：查看 / 导出各一码）
  11-5-标签图标上传与尺寸规范: review                # 🔴 实现与 PRD 不符：icon 做成了文本框，FR-74/75 原文写的是"上传"
  11-6-装饰标签单标签约束: review                    # 🔴 2026-08-25 产品新定；顺带修「首页只显一个/详情页显全部」的既有不一致
  11-7-后台印尼语支持: review                        # 实际 883 键；🔴 翻译待母语校对（钱/处置/时区三类）；L2 逐页扫版待本地
  epic-11-retrospective: optional

  # ===== Epic 12–15：种子内容发布整改（2026-08-21 拆入，后台 PRD 剩余六条）=====
  # 与 Epic 11 的区别：那四条阻塞 App 侧已交付的功能；这六条不阻塞任何 App 功能，
  # 但解决的是运营**实际在用后台时**撞到的三个痛点（单条发布连账号都没得选 / 图只能填 URL /
  # 批量没有校验预览，提交即上线，50 行错 3 行就是 3 条线上真帖）。
  #
  # 🔴 依赖顺序不可乱排：
  #   12-1 身份池 ──┬─→ 12-2 单条整改 ──┐
  #                 └─→ 13-3 批量录入   ├─→ 14-1 物种标记（必须最后）
  #   13-1 草稿/排期模型 ─→ 13-2/13-3/13-4 ─→ 13-5 定时发布
  #   15-1 互动积分（完全独立，可任意插）
  # 理由：12-1 是两处选择器的数据源；13-1 的草稿态与排期态是同一模型（先做"只支持立即发布"
  #   会在同一处返工两次，OQ-21）；14-1 的两个触点挂在 12-2 的选择器与 13-3 的 Excel 模板上，
  #   而那两处基础从未交付过（PRD §7.4 原文误以为已随 V1.1.0 落地）。
  epic-12: in-progress
  12-1-运营发布身份池: review                        # AB-3I 🔴 必须最先；独立权限码 seed.publish_as_real，与 virtual_account.manage 解耦
  12-2-单条发布整改: review                          # AB-3J 落地 V1.1.0 AB-1.1-03 两条从未交付的要求
  epic-12-retrospective: optional
  epic-13: in-progress
  13-1-草稿与排期状态机及批次模型: review            # AB-3K/3L 地基 🔴 状态挂行上、批次不持有状态；草稿不得写进 content_posts
  13-2-批量素材上传与宽高比警告: review              # AB-3K Step1 🔴 清理对象是行不是批次（按批次清会删掉已排期的行）
  13-3-批量内容录入在线与excel: review               # AB-3K Step0+2 🔴 粘贴分行限制必须界面明示（拆错不报错）
  13-4-校验预览确认发布与去重口径修正: review        # AB-3K Step3 🔴 指纹加作者维度 + 删除时清理（现无清理→文案永久无法重发）
  13-5-定时发布与排期管理: review                    # AB-3L 🛡 走既有发布链路含自动审核、无豁免
  epic-13-retrospective: optional
  epic-14: in-progress
  14-1-种子内容物种归属标记: review                  # AB-3H 🔴 必须排在 12-2 与 13-3 之后；读时推导、改账号定位零回填
  epic-14-retrospective: optional
  # ===== Epic 16–18：1.1.6 补充 PRD（2026-08-24 拆入）=====
  # 来源 V1.1.6/1-1-6补充prd.md。FR-95 与 FR-96 功能互不依赖，PRD 定的顺序是 FR-95 先。
  # 🔴 限流曝光（Epic 17）依赖 Epic 16 —— 限流是打分公式里的乘法系数，没有推荐序就没有落点。
  #
  # 🔴 拆分前对 PRD 做过一次代码核查，五处已过期/不符（详见 epics 文档的核查表）：
  #    ① A-B8 顶置首屏去重**已实现且更严**（4-2 排除整个第一页 20 条，PRD 写前 10 条）→ 不收窄
  #    ② A-C2「连 ShareResult 都没读」**已过期**（10-1 已读 success）→ 产品拍板：只在 success 时发币
  #    ③ 优先级 1（用内容自己的 pet_id）14-1 未实现 → 单宠平台下不阻塞，随 FR-64 复核
  #    ④ 优先级 4「无档案 → GENERAL」与 14-1 的「推不出来」冲突 → 产品拍板：两个语义分开
  #    ⑤ 举报者隐藏与拉黑**刻意不合并**（AD-9）→ 不合并
  #
  # ⚠️ 平台无灰度分流基建。产品拍板：全量上线不加开关。
  #    已告知并被确认的代价：降级链只在**出错**时回落，"排序效果不好"不算出错 ⇒ 只能发版回退。
  epic-16: done       
  16-1-曝光记录与序列快照基建: review                        # §8.1 两项硬前置 🔴 缺则曝光衰减失效/翻页重复
  16-2-打分与配比引擎: review                              # 🔴 纯计算不碰 DB/HTTP，配比与打分要能穷举单测
  16-3-候选池与推荐序接入all-tab: review                # 🛡 举报者隐藏与拉黑不合并；非 ALL Tab 完全不动
  16-4-打分参数配置化: review                                # 🔴 非优化项：第一次校准必然在发版后（OQ-B1）
  16-5-feed埋点补tab与排序模式: review                  # 两条排序路径必须可区分，否则效果无法归因
  epic-16-retrospective: optional
  epic-17: backlog
  17-1-限流状态模型与到期解除: review                      # 🛡 降权不下架、对用户不可见不通知
  17-2-后台限流处置动作与状态可见: review                # 🔴 确认前须明示"不是下架"，否则运营当下架用
  epic-17-retrospective: optional
  epic-18: backlog
  18-1-全局分享额度总控: review                              # 🔴 FR-50B 的前置能力先行交付，范围严限于"上限控制"
  18-2-卡面页分享入口与奖励发放: review                  # 🔴 按**档案**去重不按卡（卡可无限建）；success 才发币
  18-3-ab-3m后台配置: review                             # 🔴 配置页须同屏展示 HD 价并算出兑换比值
  epic-18-retrospective: optional

  epic-15: in-progress
  15-1-内容互动积分与双口径统计: review              # AB-3G ✅ A-2 已闭合：赞/评时间戳都在（V9/V10），口径B 无需新增记录；本条零迁移
  epic-15-retrospective: optional

# ===== 开工前须知的开放问题（详见架构 delta §8） =====
# 🔴 OA-2  阻塞 FR-70 上线：用户协议是否已覆盖「手机号用于运营召回与后续触达」
#          （未覆盖则采集用途超出已获同意范围）—— 上线前由产品/法务确认
# 🟡 OA-5  2026-08-18 收窄，**不再阻塞两个 Epic 开工**：技术口径已由 AD-14 全部定死，
#          且 FR-85 四个触发点的时机 PRD 本就写明、并未失效 —— 作废的只有「原生弹窗 vs
#          跳设置」那一段分流。✅ Epic 8 全部、7-1 可开工。
#          🔴 仍挡的只有 7-2：手机号软引导的**新时机至今无人定义**，待产品给口径。
#          源目录 PRD §2.8 回写属文档同步，不挡写代码
# ⚠️ OA-3  后台 4 条必须同版本模块的排期未定 —— App 端单独上线即空壳（FR-68/70/74/75）
# ⚠️ OA-1  图片高度护栏的露出余量实机值 —— 不阻塞开工，卡 FR-71/FR-93 的 L2 视觉验收
# ✅ OA-7  已于 2026-08-18 闭合：四个事件名**按规范改掉**（动作在词尾且须是动词）——
#          phone_prompt_responded / push_permission_responded /
#          push_permission_state_reported / post_share_card_sent。
#          定案时它们一行代码都还没写，改名零成本。🔴 发版后锁死，改名会切断历史序列
# ✅ OA-6  已于 2026-08-16 闭合：访客只读视图**不套拉黑过滤**（见 AD-1 Rule 9）

# ===== Epic 11（后台）的 Flyway 与后移项（2026-08-21）=====
# Flyway：本 Epic **只有一处**真需要迁移 —— user_tags / content_tags 两张表没有「下线」列，
#   而 PRD 要求「标签创建 / 编辑 / 下线」。下线不能用删除代替（分配表有 FK 指向标签）。
#   两条 story 各加一列，**号取时间戳** V<yyyyMMdd_HHmm>__<snake_case>.sql（决策 E6 已反转 E2 的序号分配，
#   禁用序列号；CI flyway-guard 强制。提交前自查 bash scripts/ci/check-flyway-versions.sh origin/main）。
#   其余三项零迁移：顶置表字段全齐、users.phone 已在、权限码是代码常量（AdminPermissions.java）
#   —— 新功能刻意**不做权限回填**，不给存量账号自动授予。
#
# 后移：AB-11A 的「批量催填推送」不在本轮。后台目前没有任何面向用户的群发推送能力
#   （现存 broadcast 只给兽医广播接单请求），那是名单筛选 + 文案 + 发送任务 + 失败处理 +
#   送达统计一整套新基建。导出名单已足够让运营开工（走 WhatsApp 触达本就是 FR-70 的目的）。
#
# 不在本轮：AB-3G 互动积分 / AB-3H 物种标记 / AB-3I~3L 种子内容整改（单独一轮）。
#   它们不阻塞任何 App 侧功能，而本 Epic 四条阻塞。
#   ⚠️ 若那一轮要拆，注意 AB-3H 必须排在 AB-3J / AB-3K 之后（其两处触点依赖它们提供的基础）。

# ===== Epic 12–15 的 Flyway（2026-08-21）=====
# 需要新迁移的：12-1（授权关联表）、13-1（批次表 + 行表）、13-4（去重指纹改主键为
#   (content_hash, author_id)）、14-1（账号物种定位 + content_posts 可空覆写列）。
# 零迁移的：12-2、13-2、13-3、13-5、15-1。
# 🔴 一律时间戳号 V<yyyyMMdd_HHmm>__<snake_case>.sql（决策 E6，禁用序列号）；
#   提交前自查 bash scripts/ci/check-flyway-versions.sh origin/main。
#
# 至此后台 PRD 八条全部拆完：Epic 11（AB-10A/10C/11A/12A）+ Epic 12–15（AB-3G/3H/3I/3J/3K/3L）。
# 仍后移的两项：AB-11A 的批量催填推送（后台无面向用户的群发能力）；AB-3K 的整批撤回（OQ-22）。
