# Story 6.4: 推送权限申请时机与拒绝处理

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a **用户**,
I want **App 在恰当时机请求推送权限而非一上来就要**,
so that **我更愿意授权，且拒绝后不被反复打扰**。

> 本 Story 处理**推送运行时权限的申请时机与拒绝兜底**（FR-22D 的推送部分）：推送权限在**完成第一次问诊后**才申请，拒绝后不再主动弹系统弹窗、改由「我的」页引导手动去设置。
> **依赖**：「完成第一次问诊」信号来自 **Epic 5（consult 会话结束/完成态）**；权限授予后真机推送能收到依赖 **Story 6.1** 的通道接线。**范围澄清**：相机/相册权限基础引导模式在 **Epic 2（Story 2.1）已建**，本 Story 仅复用其「去设置」深链统一样式（AC2 为对齐项），核心新增只有**推送权限**。

## Acceptance Criteria

> **验证层标注**：**L0** 静态/单测 · **L1** 集成/运行时 · **L2** 端到端/外部（真机推送权限系统弹窗、首次问诊完成态触发、系统设置页深链）。
> 推送权限系统弹窗、跳系统设置页为 L2（真机）；触发时机判定（首次问诊完成 + 未问过权限）、拒绝后不再弹的状态记录可在 L0/L1 验证。

### AC1 — 推送权限在「完成首次问诊」或「完成建档（仅从未问诊用户）」两时机取最早触发，仅弹一次，拒绝后不再主动弹

**Given** 用户**完成第一次问诊** **或** **完成宠物档案创建（仅从未问诊用户）**
**When** 取两时机中最早触发的一个，触发推送权限申请
**Then** 完成首次问诊触发时弹「开启通知，兽医回复时第一时间提醒你」；完成建档触发时弹「开启通知，第一时间收到 [宠物名] 的生日提醒和陪伴纪念日」（均不在 App 首次启动时请求）（FR-22D）
**And** 仅弹一次：两时机均触发过、或用户已授权时不再弹；用户拒绝后不再主动弹起系统权限弹窗，可在「我的」页面引导手动前往设置开启
> 验证层：**L2**（真机系统推送权限弹窗 + 首次问诊完成态 / 建档完成态触发为端到端）。**L0/L1**：触发门控判定从单布尔改为 `((首次问诊完成 OR 建档完成且从未问诊) && !asked) ⇒ 触发一次` + 拒绝/触发后状态持久化（`prefs` 记 `pushPermissionAsked=true`，再不主动弹）单测/集成。
> 🔄 **PRD V1.0.0 修订（Fx · 2026-06-08）：** 推送权限申请由单时机（完成首次问诊后）改为双时机——「完成首次问诊后」**或**「完成宠物档案创建后（仅从未问诊用户）」取最早触发、仅弹一次；判定从单布尔 `pushPermissionAsked` 改为 `(首次问诊完成 OR 建档完成) && !asked`；新增建档触发文案二（决策 F7）。

### AC2 — 相机/相册被拒的统一引导深链（对齐 Story 2.1 基础模式）

**Given** 相机/相册权限被拒（Story 2.1 已建基础模式）
**When** 用户再次触发上传
**Then** 统一弹引导「需要相册权限才能上传，请前往设置开启」+「去设置」深链至系统权限设置页（FR-22D）
> 验证层：**L2**（「去设置」打开系统设置页为真机端到端）。**L0**：引导弹窗文案 + 「去设置」action 触发 `openAppSettings` 单测。**说明**：相机/相册权限请求与首次引导本体在 Story 2.1，本 AC 仅确保被拒后引导样式与推送拒绝引导统一。
> 🔄 **PRD V1.0.0 修订（Fx · 2026-06-08）：** 相机/相册触发场景**删除「兽医咨询发送图片/视频(FR-30)」一行**（V1.0.0 兽医 IM 仅文字/图片、全程无视频）；「AI 问诊上传照片」保持（V1.0.0 仅图片，视频随收费模式开放）。相机/相册触发场景收敛为：AI 问诊上传照片(FR-1) / 档案头像上传(FR-11) / 成长日历快乐时刻上传(FR-12)（决策 F4）。

---

## Tasks / Subtasks

> 三段组织。本 Story 形态 = **前端重（权限时机门控 + 拒绝兜底 + 设置深链 + 「我的」引导入口）+ 后端轻（仅消费 6.1，无新后端）**。执行顺序：前端为主 → 联调。

### 🟦 后端子任务（petgo-backend / Spring Boot）

- [ ] **B1. 无新增后端逻辑** (AC: 1)
  - [ ] 本 Story 不新增后端端点；推送链路完全复用 6.1 `NotificationService`。仅确认：权限拒绝设备在 IM 离线投递失败时仅记日志、不报错、不重试风暴（与 6.1 一致）。

### 🟩 前端子任务（petgo_app / Flutter）

- [ ] **F1. 推送权限触发门控（首次问诊完成 或 建档完成，取最早）** (AC: 1)
  - [ ] 监听「首次问诊完成」信号（consult 完成态，Epic 5）**与「宠物档案创建完成」信号（仅从未问诊用户，Epic 2）**；判定 `((首次问诊完成 OR 建档完成且从未问诊) && prefs.pushPermissionAsked == false)` → 取最早触发者弹对应前置软引导（问诊触发→「开启通知，兽医回复时第一时间提醒你」；建档触发→「开启通知，第一时间收到 [宠物名] 的生日提醒和陪伴纪念日」）→ 用户同意再触发系统权限弹窗。**绝不在 App 首次启动时请求；两时机仅弹一次**。
  > 🔄 **PRD V1.0.0 修订（Fx · 2026-06-08）：** 门控由单时机/单布尔改为双时机取最早，新增监听建档完成信号 + 文案二（决策 F7）。
  - [ ] 引入推送权限插件（如 `permission_handler` / IM SDK 权限 API，以项目既有依赖为准）。
- [ ] **F2. 拒绝后不再主动弹 + 状态持久化** (AC: 1)
  - [ ] 无论同意/拒绝，置 `prefs.pushPermissionAsked = true`（`core/storage/prefs`）；拒绝后**不再主动弹系统弹窗**。
- [ ] **F3. 「我的」页手动开启引导** (AC: 1)
  - [ ] `features/me` 设置项：未授权推送时显示「开启通知提醒」引导 + 「去设置」深链至系统通知设置页（`openAppSettings`）。
- [ ] **F4. 相机/相册被拒统一引导（对齐 2.1）** (AC: 2)
  - [ ] 复用 Story 2.1 基础模式：被拒后弹「需要相册权限才能上传，请前往设置开启」+「去设置」深链系统设置页；确保与推送拒绝引导**视觉/交互一致**（同一引导组件）。

### 🟨 联调验收子任务

- [ ] **J1. 推送权限时机真机验收** (AC: 1 / L2)
  - [ ] 全新安装 → App 首启**不**弹推送权限；完成第一次问诊后 → 弹软引导 → 同意 → 系统弹窗 → 授予后真机能收 6.2 推送。
- [ ] **J2. 拒绝兜底验收** (AC: 1 / L2)
  - [ ] 首次问诊后拒绝推送 → 后续不再主动弹系统弹窗；「我的」页「去设置」可跳系统通知设置。
- [ ] **J3. 相册被拒引导** (AC: 2 / L2)
  - [ ] 相册权限拒后再次上传 → 弹统一引导 + 「去设置」打开系统设置页。
- [ ] **J4. 门控/状态单测** (AC: 1 / L0)
  - [ ] 触发门控（首启不触发、首次问诊完成且未问过才触发、已问过不再弹）+ `pushPermissionAsked` 持久化单测。

---

## Dev Notes

### 关键架构约定（沿用全局）

- 前端 `lib/features/notify/{data,domain,presentation}`（权限时机/引导）+ `lib/features/me`（手动开启入口）+ `lib/core/storage/prefs`（权限已问标志，与 lang/petStatus/onboarding 同位）。
- 「首次问诊完成」信号来自 consult（Epic 5），notify 监听，不直访 consult repository。

### 推送专项约定

- 权限**时机** = 完成第一次问诊后（NFR-12 权限/时机），**绝不首启请求**——[epics.md FR-22D / architecture.md NFR-12 落点]。
- 拒绝后不反复打扰：一次性 `pushPermissionAsked` 标志 + 「我的」页被动引导。
- 权限拒绝设备的离线投递失败仅记日志，不重试风暴（6.1 已定）。

### 强制护栏（违反即返工）

- **禁** App 首次启动即请求推送权限（违反 FR-22D 时机）。
- **禁** 拒绝后反复主动弹系统弹窗。
- **禁** 新增后端推送服务/MQ；本 Story 纯前端 + 复用 6.1。
- 日志严禁记录 PII。

### 范围边界（本 Story 明确不做）

- ❌ 不做推送基建/深链路由表（Story 6.1）。
- ❌ 不做具体推送内容（6.2/6.3）。
- ❌ 不实现相机/相册权限**请求本体与首次引导**（Story 2.1 已建——本 Story 仅复用「去设置」引导样式统一被拒兜底）。
- ❌ 不实现问诊完成态本体（Epic 5——仅监听其完成信号作触发时机）。
- ✅ 只做：推送权限申请时机门控（首次问诊后）+ 拒绝兜底 + 「我的」手动开启引导 + 相册被拒引导对齐。

### Project Structure Notes

- 权限触发监听点建议放 notify feature，监听 consult 完成事件/状态；避免在 consult 里硬编码推送权限逻辑（保持模块边界）。
- 「去设置」深链统一封装为 `shared/widgets` 引导组件，推送与相册复用，避免两套样式。

### References

- [Source: epics.md#Story 6.4] — 两条原始 AC（FR-22D 推送权限时机/拒绝、相册被拒引导）。
- [Source: epics.md#Epic 6] — 推送权限在完成第一次问诊后才申请。
- [Source: architecture.md#技术决策 NFR] — NFR-12 权限/时机；复用 IM 离线推送。
- [Source: architecture.md#前端约定] — `core/storage/prefs` 存 lang/petStatus/onboarding（本 Story 增 pushPermissionAsked）。
- [Memory: story-acceptance-layering] — AC 标 L0/L1/L2；真机推送权限/系统设置页为 L2。
- [Memory: v1-architecture-posture] — 不过度设计，纯前端门控 + 复用既有推送链路，无新后端中间件。

## Dev Agent Record

### Agent Model Used

云端 dev agent（Epic 6 批量）

### Debug Log References

### Completion Notes List

**L0 绿（云端已验）**：前端 `flutter analyze` 零问题 + `flutter test`(180) 绿（新增 `push_permission_gate_test`(3) + `push_enable_guide_test`(1)）。后端无新增（复用 6.1）。

**关键实现**：
- **时机门控（核心，L0 测）**：`PushPermissionGate.shouldRequest(firstConsultDone, alreadyAsked)` = `firstConsultDone && !alreadyAsked`——**绝不首启请求**；`maybeRequestAfterFirstConsult` 请求一次后置 `prefs.pushPermissionAsked=true`（无论同意/拒绝/异常），**拒绝后不再主动弹**。系统权限请求注入（真机 `permission_handler` 通知权限 / 测试 fake）。
- **持久化**：`AppPrefs.pushPermissionAsked`（`shared_preferences`，与 lang/petStatus/onboarding 同位）。
- **「我的」页引导（F3）**：`PushEnableGuide` 组件（未授权时显示 + 「去设置」`openAppSettings`），Epic 7 me-page 本体挂载（当前 me_page 为占位）。
- **去设置统一样式（AC2）**：复用 Story 2.1 `permission_handler` `openAppSettings` + `mediaOpenSettings` 文案；相机/相册被拒引导本体在 2.1，本故事对齐推送拒绝引导同款。
- **provider**：`pushPermissionGateProvider`（FutureProvider，AppPrefs ad-hoc 创建，与项目一致）。

**待本地（L2，真机）**：J1 首启不弹 / 首次问诊完成弹软引导→系统弹窗→授予后收 6.2 推送；J2 拒绝后不再弹 + 「我的」去设置跳系统通知设置；J3 相册被拒引导。
**待集成**：软引导弹窗 + 首次问诊完成信号挂接（监听 Epic 5 consult 完成态触发 `maybeRequestAfterFirstConsult`）；me-page 挂 `PushEnableGuide`（Epic 7）。

### File List

**前端（新增）**：`features/notify/domain/push_permission_gate.dart`、`features/notify/data/push_permission_providers.dart`、`features/notify/presentation/push_enable_guide.dart`、`test/notify/{push_permission_gate_test,push_enable_guide_test}.dart`
**前端（修改）**：`core/storage/prefs.dart`（+pushPermissionAsked）、`l10n/app_en.arb`、`l10n/app_id.arb`
**后端**：无（复用 6.1）
