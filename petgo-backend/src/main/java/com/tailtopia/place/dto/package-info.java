/**
 * TailTopia place 模块 — dto 层（V1.3.0 batch-b1 Story 1.1 起）。
 *
 * <p>对外 JSON 形状以本包的 record 为权威落地（CROSS-STORY C4）；改任一 record 必须同步
 * App data DTO 与契约测试（C5），否则 PR 不绿。
 */
package com.tailtopia.place.dto;
