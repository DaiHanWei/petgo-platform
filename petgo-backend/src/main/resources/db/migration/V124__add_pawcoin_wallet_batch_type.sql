-- V1.4.0 精选自营电商 · Story 8.3 —— 对账（AB-13D）的钱包侧预留位（S-12 折中）。
-- 工作线：V1.4.0 电商 · 独占号段 V101–V139。
-- ⚠️ 本迁移给共享表 pawcoin_wallets 加一列 —— 但【只加、可空、无默认行为、无任何读写方】，
--    既有行为逐字节不变。已在台账登记。
--
-- 🔴🔴【S-12 是 13 条代拍决议里最值得复议的一条】：
--    充值币（有现金垫底）与赠币（无现金垫底）的性质不同，精确对账需要【钱包侧余额批次分层】。
--    本版本【不做】分层（工程量大、跨 Epic 3/5），改为在 AB-13D 上给一行
--    「其中：赠币核销额（近似）」。
--    ⚠️ 近似值对财务口径是否够用【须财务确认】；若不够用则需回头改钱包，代价最高。
--
-- 🔴 本列【预留但不启用】：没有任何代码读写它，加它只是为了将来精确分层时
--    不必再动一次这张高频表。加了不用是刻意的 —— 不是忘了接。

ALTER TABLE pawcoin_wallets ADD COLUMN batch_type VARCHAR(16);

COMMENT ON COLUMN pawcoin_wallets.batch_type IS
    '🔴 S-12 预留位，本版本【不启用、无读写方】。将来做充值币/赠币批次分层时用；'
    '当前对账用 AB-13D 的「赠币核销额（近似）」代替。';
