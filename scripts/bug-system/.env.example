# Bug 系统拉取脚本凭证（第一期 / 只读）
#
# 用法：复制本文件为同目录的 .env，填入真实值。.env 已 gitignore，绝不入库（NFR1）。
# 凭证经本地环境注入，不写死代码、不在团队内明文传播。
#
# 取值来源见 SETUP.md「Story 2.1 配置只读应用」：
#   - APP_ID / APP_SECRET：Lark 开放平台「企业自建应用」凭证
#   - APP_TOKEN：Bug 多维表格的 app_token（表格 URL 里 /base/<app_token> 段）
#   - TABLE_ID：Bug 表的 table_id（URL ?table=<table_id> 或字段管理处）

APP_ID=cli_xxxxxxxxxxxxxxxx
APP_SECRET=
APP_TOKEN=bascnxxxxxxxxxxxxxxxxxxxxxx
TABLE_ID=tblxxxxxxxxxxxxxx
